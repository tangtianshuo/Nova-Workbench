---
phase: 05-task-crud
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified: []
autonomous: false
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, TASK-09]

must_haves:
  truths:
    - "Phase 5 全部 9 个 TASK-* 需求在运行时通过用户验证"
    - "persist v1 → v2 migration 在已有数据上不丢失任务"
    - "快速连续创建多个任务,无 ID 碰撞"
    - "DnD 与 click-to-expand 不互相干扰"
    - "产品删除后任务卡片上的产品徽章优雅降级"
  artifacts: []
  key_links: []
---

<objective>
Phase 5 完成后的人机协同验证关卡,确保 8 条 Success Criteria 全部 TRUE。

Purpose: 在 Phase 6 (Schedule CRUD) 开始前,确认 Phase 5 task 全生命周期可用。用户按 MEMORY.md 偏好批量跑使用动线,不按 phase 切片。
Output: HUMAN-UAT.md 中 Phase 5 区段打勾 + 发现的缺陷列表(若有)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-task-crud/05-CONTEXT.md
@.planning/phases/05-task-crud/05-UI-SPEC.md
@.planning/phases/05-task-crud/05-01-SUMMARY.md
@.planning/phases/05-task-crud/05-02-SUMMARY.md
@.planning/phases/05-task-crud/05-03-SUMMARY.md
@.planning/phases/05-task-crud/05-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 5 全量 UAT 验证 (TASK-01..09 + 8 条 Success Criteria)</name>
  <action>启动 dev server,用户按使用动线批量验证 Phase 5 全部 9 个 TASK-* 需求与 8 条 Success Criteria,确认 persist v2 migration 不丢数据、crypto.randomUUID() 无碰撞、DnD 与 click-to-expand 不互扰、产品删除后徽章优雅降级。</action>
  <what-built>
    Phase 5 完整交付:
    1. taskStore 扩展 5 个 CRUD action + Task 类型加 projectId?/scheduledEventId? + persist v2 migration
    2. AppContext 兼容层暴露全部新 action
    3. TaskDialog 创建/编辑双模式 + Combobox 产品选择器 + 嵌套删除确认
    4. TaskKanban 卡片展开为可编辑 inputs(400ms debounced autosave)+ DotsThree 菜单 + @dnd-kit/core 跨列拖拽 + 产品徽章 + ProductSummaryDrawer
    5. 新增可复用 Drawer 组件(Phase 9 chat panel 将复用)
  </what-built>
  <how-to-verify>
    **前置准备:**
    - 启动 dev:`cd D:/Projects/Nova/pm-workspace && npm run dev`(或 `npm run tauri:dev`)
    - 浏览器/Tauri 窗口打开,切到"任务管理"tab

    **使用动线(UAT,按 MEMORY.md 偏好批量跑):**

    1. **创建动线 (TASK-02, TASK-09)**:
       - 点 "新建任务" 按钮 → TaskDialog 打开,标题应为 "新建任务"
       - 输入标题"UAT 测试任务 A",描述"测试描述",优先级=高,截止日期=今天
       - 关联产品 Combobox 点开 → 输入产品名搜索(确认筛选生效)→ 选一个产品 → Combobox trigger 显示产品名
       - 点 Combobox trigger 内的 X 清除 → 确认 product 清空
       - 重新选产品,点 "创建任务"
       - **快速连续**创建 3 个任务(每个间隔 < 1s),全部出现在看板第一列
       - 验证 ID 形如 UUID(非 Date.now 数字串),且 3 个 ID 不同(TASK-09 验证)

    2. **Inline 编辑动线 (TASK-01, D-01, D-02)**:
       - 点 "UAT 测试任务 A" 卡片展开
       - **展开即为可编辑**: 标题/描述/优先级/截止日期/分类直接显示为 input/select,无需额外点击
       - 修改标题为 "UAT 测试任务 A 改名",停顿 500ms+
       - 刷新页面 → 标题已保存(updateTask 生效)
       - 修改优先级为低,分类下拉切换到另一列 → 任务移到目标列(moveTask via category Select)
       - 验证无 "保存" 按钮,字段层 400ms 自动保存

    3. **DotsMenu 动线 (D-03)**:
       - 在卡片右上角找到 DotsThree 图标(若卡片已折叠,左侧应有 CheckCircle 在 right-10,DotsThree 在 right-3)
       - 点击展开 4 项菜单:在对话框中编辑 / 复制 ID / 重新打开 / 删除
       - 点 "复制 ID" → Toast 显示 "已复制任务 ID",粘贴板验证
       - 点 "在对话框中编辑" → TaskDialog 打开 mode=edit,标题应为 "编辑任务",所有字段预填
       - 修改描述,点 "保存修改" → Toast "已保存",刷新验证

    4. **重新打开动线 (TASK-04)**:
       - 完成一个任务(底部 "标记完成" 按钮),任务标题划掉
       - DotsMenu 中 "重新打开" 现在可点击(此前 disabled) → 点击,任务状态变回 "未开始"
       - 或在 TaskDialog 中改状态 Select 为 "未开始",保存 → 同效果

    5. **删除动线 (TASK-03)**:
       - DotsMenu 点 "删除" → 弹出确认对话框,标题 "删除任务?",正文含任务标题
       - 点取消 → 对话框关闭,任务还在
       - 再点删除 → 确认 → Toast "任务已删除",任务从看板消失
       - 在 TaskDialog edit 模式下也试一次左下角 "删除" 按钮 → 同样的确认对话框

    6. **DnD 动线 (TASK-05, D-05, D-06, D-07)**:
       - 看板至少有 3 列(需求评审 / 产品设计 / 开发实现)
       - 短距离(< 8px)拖动一个卡片 → 不触发 DnD,触发 click 展开
       - 长距离拖动一个卡片到另一列 → 卡片 opacity-60 + scale 1.02,目标列出现 ring-2 ring-accent/40
       - **拖拽中实时观察**:源列计数 -1,目标列计数 +1(动画 motion.span key=count)
       - 松手 → 任务出现在新列,store 数据更新(刷新验证)
       - 拖拽完成后卡片展开状态保持不变(不强制 collapse)

    7. **产品徽章 + Drawer (TASK-06, D-11, D-12)**:
       - 选一个有 projectId 的任务卡片(若没有,先用 TaskDialog 给某任务关联产品)
       - 卡片上应显示产品徽章(Badge variant="accent" + FolderSimple 图标 + 产品名)
       - 点击徽章 → 右侧 360px Drawer 滑入,显示产品名(title)+ tagline + 阶段进度 ProgressBar + 最多 3 个里程碑
       - 点 "打开详情" → activeTab 切到 "product",selectedProductId 已设,产品详情视图打开
       - ESC / overlay 点击 / X 按钮 → Drawer 关闭

    8. **Persist 迁移动线 (TASK-07, TASK-08)**:
       - 关闭 dev server,在浏览器 devtools Application > Local Storage 找 'nova-task' key
       - 检查其 `state.categories[].tasks[]` 中每个任务是否有 `projectId` 字段(可能为 null,字段存在即可)
       - 版本字段应为 `version: 2`
       - 若有旧 v1 数据,迁移后任务不丢失

    9. **错误降级 (TASK-06 边界)**:
       - 在 taskStore 中手动给某任务设置一个不存在的 projectId(如 'fake-id'),刷新
       - 卡片徽章若存在,点击 → ProductSummaryDrawer 显示 "该产品已被删除"(不崩溃)

    10. **AppContext 兼容性**:
        - 现有使用 useApp() 的 30+ 个 view 仍能正常工作(completeTask, addTask, addCategory, getProjectTaskCount 不变;新 action updateTask/deleteTask/reopenTask/moveTask/setTaskProject 在 useApp() 上也可访问)
        - 简单 grep 验证:`grep -r "useApp().updateTask\|useApp().deleteTask" src/` 应为 0(没有调用方依赖,但接口可访问)

    **预期结果**:8 条 Success Criteria 全部 TRUE,无 crash,无数据丢失。
  </how-to-verify>
  <resume-signal>回复 "approved" 或描述发现的缺陷(如"DnD 拖拽到空列不工作"、"Drawer 关闭后焦点丢失"等)</resume-signal>
</task>

</tasks>

<verification>
用户在 HUMAN-UAT.md 中确认 Phase 5 全部 Success Criteria 通过,或列出待修缺陷。
</verification>

<success_criteria>
1. Phase 5 8 条 Success Criteria 全部通过用户验证
2. 发现的任何 P0/P1 缺陷记录到 .planning/phases/05-task-crud/05-DEFECTS.md(若有)
3. 用户确认 Phase 5 完成,可启动 Phase 6 plan-phase
</success_criteria>

<output>
完成后在 `.planning/STATE.md` 更新 Phase 5 状态为 completed,并在 `.planning/HUMAN-UAT.md` (若存在) 中标记 Phase 5 区段。
</output>
