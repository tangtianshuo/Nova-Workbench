---
phase: 06-schedule-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/scheduleStore.ts
  - src/store/AppContext.tsx
autonomous: true
requirements: [SCHED-05, SCHED-06, SCHED-07, SCHED-08]

must_haves:
  truths:
    - "ScheduleEvent.date 类型从 number 改为 string (YYYY-MM-DD 格式)"
    - "ScheduleEvent 新增 projectId?: string、taskId?: string 弱关联字段"
    - "ScheduleEvent.type 从 string 收窄为 'meeting' | 'deadline' | 'task' | 'reminder' | 'review' | 'sync' 联合"
    - "scheduleStore 暴露 createEvent / updateEvent / deleteEvent 三个 action"
    - "scheduleStore persist version 升级到 2,migrate 函数把旧 number date 基于 May 2025 锚点转 YYYY-MM-DD 字符串"
    - "AppContext 暴露 createEvent / updateEvent / deleteEvent 三个新 action (delegate 到 useScheduleStore)"
    - "INITIAL_EVENTS mock 数据 date 字段改为 YYYY-MM-DD 字符串"
  artifacts:
    - path: "src/stores/scheduleStore.ts"
      provides: "ScheduleEvent 类型扩展 + 3 个新 action + persist v2 migrate"
      contains: "updateEvent"
    - path: "src/store/AppContext.tsx"
      provides: "AppContextType 暴露 3 个新 action"
      contains: "updateEvent"
  key_links:
    - from: "src/store/AppContext.tsx"
      to: "src/stores/scheduleStore.ts"
      via: "useScheduleStore selector delegates"
      pattern: "useScheduleStore\\(\\(s\\) => s\\.(updateEvent|deleteEvent|createEvent)\\)"
---

<objective>
扩展 ScheduleEvent 类型(date number→string + 弱关联字段 + type 枚举)、补齐 scheduleStore 三个 CRUD action、迁移 persist v1→v2 (基于 May 2025 锚点的 number→YYYY-MM-DD 转换)、AppContext 兼容层暴露新 action。

Purpose: Phase 6 所有 UI workstream (ScheduleDialog / ScheduleView 月历) 都依赖 string date + CRUD action。先交付契约层,后续 UI plan 直接消费。
Output: scheduleStore 类型扩展 + 3 个新 action + persist v2 migrate + AppContext 委托
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schedule-crud/06-CONTEXT.md

@src/stores/scheduleStore.ts
@src/stores/taskStore.ts
@src/store/AppContext.tsx

<interfaces>
<!-- 现有 ScheduleEvent (src/stores/scheduleStore.ts,内联定义) -->
```typescript
export interface ScheduleEvent {
  id: string;
  title: string;
  time: string;       // '10:00 - 11:30' 格式
  date: number;       // ← 改为 string (YYYY-MM-DD)
  type: string;       // ← 收窄为联合类型
  location: string;
}
```

<!-- 现有 scheduleStore (src/stores/scheduleStore.ts) -->
```typescript
interface ScheduleState {
  events: ScheduleEvent[];
  addEvent: (event: ScheduleEvent) => void;
  setEvents: (events: ScheduleEvent[]) => void;
  _hasHydrated: boolean;
  _setHydrated: () => void;
}
// persist: { name: 'nova-schedule', version: 1, migrate: passthrough }
```

<!-- 现有 INITIAL_EVENTS mock (写死 2025-05 月,date 是 1-31 的 day number) -->
```typescript
export const INITIAL_EVENTS: ScheduleEvent[] = [
  { id: '1', title: '需求评审会', time: '10:00 - 11:30', date: 15, type: 'meeting', location: '会议室 3A' },
  { id: '2', title: '设计走查', time: '14:00 - 15:00', date: 15, type: 'review', location: '线上会议' },
  { id: '3', title: '团队周报对齐', time: '17:00 - 18:00', date: 15, type: 'sync', location: 'Tencent Meeting' },
];
```

<!-- Phase 5 taskStore.ts persist v2 migrate 参考模式 -->
```typescript
migrate: (persisted: any, version: number) => {
  if (!persisted || !persisted.events) return persisted as Partial<ScheduleState>;
  if (version < 2) {
    // v1 → v2: 把旧 number date 转 YYYY-MM-DD (May 2025 锚点)
    persisted.events = persisted.events.map((e: any) => ({
      ...e,
      date: typeof e.date === 'number' ? `2025-05-${String(e.date).padStart(2, '0')}` : e.date,
      projectId: e.projectId ?? undefined,
      taskId: e.taskId ?? undefined,
    }));
  }
  return persisted as Partial<ScheduleState>;
},
```

<!-- AppContext schedule section (现有,需要扩展) -->
```typescript
// AppContextType (line ~118)
addEvent: (event: ScheduleEvent) => void;

// AppProvider (line ~211)
const addEvent = useScheduleStore((s) => s.addEvent);

// value object (line ~258)
addEvent,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScheduleEvent 类型扩展 + scheduleStore 3 action + persist v2 migration</name>
  <files>src/stores/scheduleStore.ts</files>
  <read_first>
    - src/stores/scheduleStore.ts (现有 ScheduleEvent / INITIAL_EVENTS / persist config)
    - src/stores/taskStore.ts (Phase 5 persist v2 migrate 参考模式)
    - .planning/phases/06-schedule-crud/06-CONTEXT.md (D-01..D-04 决策)
  </read_first>
  <behavior>
    - ScheduleEvent 接口:`date: number` 改为 `date: string`;`type: string` 改为联合 `'meeting' | 'deadline' | 'task' | 'reminder' | 'review' | 'sync'`;新增 `projectId?: string` 和 `taskId?: string`
    - INITIAL_EVENTS 三条 mock 数据 date 改为 '2025-05-15' 字符串
    - createEvent(event: ScheduleEvent): 调用方传 crypto.randomUUID() 作 id,store 只负责去重 + 插入 + 按 date+time 字符串排序
    - updateEvent(eventId, updates): 找不到返回原 state;找到时合并 updates
    - deleteEvent(eventId): 过滤掉目标 id
    - migrate(persisted, version): version < 2 时,把 number date 基于 `2025-05-XX` 锚点转 string (padStart 2 位);同时补 projectId/taskId 默认 undefined
    - addEvent 保留(legacy 兼容,不破坏 AppContext 现有委托)
    - 排序逻辑:`.sort((a, b) => a.date.localeCompare(b.date) || a.time.localeCompare(b.time))`
  </behavior>
  <action>
1. 编辑 `src/stores/scheduleStore.ts`:

   a. 替换 `ScheduleEvent` 接口:
   ```typescript
   export type ScheduleEventType = 'meeting' | 'deadline' | 'task' | 'reminder' | 'review' | 'sync';

   export interface ScheduleEvent {
     id: string;
     title: string;
     time: string;        // 'HH:mm - HH:mm' 或 'HH:mm'
     date: string;        // YYYY-MM-DD (D-01)
     type: ScheduleEventType;
     location: string;
     projectId?: string;  // D-03 弱关联
     taskId?: string;     // D-03 弱关联 (Phase 7 使用)
   }
   ```

   b. 替换 `INITIAL_EVENTS`:
   ```typescript
   export const INITIAL_EVENTS: ScheduleEvent[] = [
     { id: '1', title: '需求评审会', time: '10:00 - 11:30', date: '2025-05-15', type: 'meeting', location: '会议室 3A' },
     { id: '2', title: '设计走查', time: '14:00 - 15:00', date: '5', type: 'review', location: '线上会议' },
     { id: '3', title: '团队周报对齐', time: '17:00 - 18:00', date: '2025-05-15', type: 'sync', location: 'Tencent Meeting' },
   ];
   // 注意:Mock 数据直接用完整 YYYY-MM-DD 字符串;migrate 函数只处理旧持久化数据
   ```
   实际上把 mock 数据全改为 date: '2025-05-15'。上面 `'5'` 是笔误,请用 '2025-05-15'。

   c. `ScheduleState` 接口加 3 个新 action 签名:
   ```typescript
   createEvent: (event: ScheduleEvent) => void;
   updateEvent: (eventId: string, updates: Partial<ScheduleEvent>) => void;
   deleteEvent: (eventId: string) => void;
   ```

   d. 抽排序 helper(模块级,避免重复):
   ```typescript
   const sortByDateTime = (events: ScheduleEvent[]) =>
     events.sort((a, b) => a.date.localeCompare(b.date) || a.time.localeCompare(b.time));
   ```

   e. 实现 3 个新 action(在 create(persist(...)) 内,addEvent 之后):
   ```typescript
   createEvent: (event) =>
     set((state) => {
       if (state.events.some((e) => e.id === event.id)) return state;
       return { events: sortByDateTime([...state.events, event]) };
     }),

   updateEvent: (eventId, updates) =>
     set((state) => ({
       events: state.events.map((e) => (e.id === eventId ? { ...e, ...updates } : e)),
     })),

   deleteEvent: (eventId) =>
     set((state) => ({
       events: state.events.filter((e) => e.id !== eventId),
     })),
   ```

   f. 把现有 `addEvent` 内部 `sort((a, b) => a.date - b.date)` 改为用 `sortByDateTime`(因为 date 现在是 string,不能减)。

   g. persist 配置 `version: 1` 改为 `version: 2`;migrate 从 passthrough 改为:
   ```typescript
   migrate: (persisted: any, version: number) => {
     if (!persisted || !persisted.events) return persisted as Partial<ScheduleState>;
     if (version < 2) {
       // v1 → v2: number date → YYYY-MM-DD (May 2025 锚点, D-02) + 补弱关联字段
       persisted.events = persisted.events.map((e: any) => ({
         ...e,
         date: typeof e.date === 'number'
           ? `2025-05-${String(e.date).padStart(2, '0')}`
           : e.date,
         projectId: e.projectId ?? undefined,
         taskId: e.taskId ?? undefined,
       }));
     }
     return persisted as Partial<ScheduleState>;
   },
   ```
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    ScheduleEvent.date 已是 string,3 个新 action 实现,persist v2 migration 把旧 number date 转 YYYY-MM-DD,弱关联字段就位。AppContext 还未委托(下一步)。
  </done>
</task>

<task type="auto">
  <name>Task 2: AppContext 暴露 createEvent / updateEvent / deleteEvent</name>
  <files>src/store/AppContext.tsx</files>
  <read_first>
    - src/store/AppContext.tsx (现有 schedule delegation: addEvent 模式)
    - src/stores/scheduleStore.ts (Task 1 完成后,验证 3 个新 action 签名)
  </read_first>
  <behavior>
    - AppContextType 接口加 3 个新方法签名(与 scheduleStore 一致)
    - AppProvider 内部用 useScheduleStore((s) => s.X) 订阅 3 个新 action
    - value 对象暴露这 3 个新 action
  </behavior>
  <action>
1. 编辑 `src/store/AppContext.tsx`:

   a. 在 `AppContextType` interface 中,现有 `addEvent: (event: ScheduleEvent) => void;` 之后加:
   ```typescript
   createEvent: (event: ScheduleEvent) => void;
   updateEvent: (eventId: string, updates: Partial<ScheduleEvent>) => void;
   deleteEvent: (eventId: string) => void;
   ```

   b. 在 `AppProvider` 内,现有 `const addEvent = useScheduleStore((s) => s.addEvent);` 之后加:
   ```typescript
   const createEvent = useScheduleStore((s) => s.createEvent);
   const updateEvent = useScheduleStore((s) => s.updateEvent);
   const deleteEvent = useScheduleStore((s) => s.deleteEvent);
   ```

   c. 在 `const value: AppContextType = { ... }` 对象中,现有 `addEvent,` 之后加:
   ```typescript
   createEvent, updateEvent, deleteEvent,
   ```

   d. 修一处类型破损:line ~60 `setEvents: Dispatch<SetStateAction<ScheduleEvent[]>>` 这行原本兼容 legacy,setEvents 现在签名是 `(events: ScheduleEvent[]) => void`,如果 lint 报错,把 AppContextType 里的 setEvents 改为 `(events: ScheduleEvent[]) => void` 并把 value 里 `setEvents: setEvents as any` 的 `as any` 去掉。如果 lint 已通过则不动。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    AppContext 兼容层暴露 3 个新 action,legacy useApp() 调用方可见。新代码(ScheduleDialog/ScheduleView)优先直接 useScheduleStore hooks。
  </done>
</task>

</tasks>

<verification>
- `npm run lint` (tsc --noEmit) 通过,无类型错误
- grep: `src/stores/scheduleStore.ts` 包含 `date: string` / `ScheduleEventType` / `createEvent:` / `updateEvent:` / `deleteEvent:` / `version: 2` / `2025-05-`
- grep: `src/store/AppContext.tsx` 包含 `createEvent` / `updateEvent` / `deleteEvent` selector + value 字段
- 现有 ScheduleView.tsx 可能出现类型错误(`e.date === day` 比较变成 number vs string),这是预期的,Plan 03 会重写整个 view;**Plan 01 不需要修 ScheduleView,只需 lint 通过 store + AppContext 文件**。如果整体 lint 失败,临时在 ScheduleView 里把 `events.some(e => e.date === day)` 改为 `events.some(e => Number(e.date.slice(-2)) === day)`,但优先以 Plan 03 完整重写为准 —— 此处如果 lint 失败请修最小改动使 lint 通过
</verification>

<success_criteria>
1. ScheduleEvent.date 从 number 迁移到 string (YYYY-MM-DD) — SCHED-05 ✓
2. ScheduleEvent 支持 projectId?/taskId? 弱关联 + type 联合包含 'task' — SCHED-06 ✓, SCHED-07 ✓
3. scheduleStore 3 个新 action 实现: createEvent / updateEvent / deleteEvent — SCHED-01/02/03 数据层就绪
4. persist v2 migration 把旧 number date 转 string (May 2025 锚点) — SCHED-08 ✓
5. AppContext 暴露新 action,legacy useApp() 调用方无破坏
</success_criteria>

<output>
After completion, create `.planning/phases/06-schedule-crud/06-01-SUMMARY.md`
</output>
