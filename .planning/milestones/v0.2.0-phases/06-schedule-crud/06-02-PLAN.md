---
phase: 06-schedule-crud
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/ScheduleDialog.tsx
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03]

must_haves:
  truths:
    - "ScheduleDialog 支持 create + edit 双模式 (mode prop 切换)"
    - "用户可以在 ScheduleDialog 编辑全部字段:标题/日期/时间/类型/地点/关联产品"
    - "用户可以从 ScheduleDialog 删除日程,触发嵌套确认 Dialog,确认后从 store 移除 (SCHED-03)"
    - "创建模式新建事件 ID 使用 crypto.randomUUID()"
    - "ScheduleDialog open→true 时所有字段重置 (edit 模式从 event prop 同步,create 模式清空) — Pitfall P13"
    - "关联产品 Combobox 复用 Phase 5 模式 (Popover + Input + 列表筛选)"
  artifacts:
    - path: "src/components/ScheduleDialog.tsx"
      provides: "ScheduleDialog 创建/编辑双模式 + Combobox + 嵌套删除确认"
      contains: "ScheduleDialog"
      min_lines: 240
  key_links:
    - from: "src/components/ScheduleDialog.tsx"
      to: "src/stores/scheduleStore.ts"
      via: "useScheduleStore 直接调用 createEvent/updateEvent/deleteEvent"
      pattern: "useScheduleStore\\(\\(s\\) => s\\.(createEvent|updateEvent|deleteEvent)\\)"
    - from: "src/components/ScheduleDialog.tsx"
      to: "src/stores/productStore.ts"
      via: "useProductStore 读取产品列表供 Combobox"
      pattern: "useProductStore"
---

<objective>
新建 ScheduleDialog 组件,支持 create/edit 双模式 + 6 个表单字段 + 关联产品 Combobox + 嵌套删除确认。完整复用 Phase 5 TaskDialog 模式,不重新发明。

Purpose: 交付 SCHED-01 (创建)、SCHED-02 (编辑预填)、SCHED-03 (删除带确认)。
Output: src/components/ScheduleDialog.tsx 单文件,Plan 03 在 ScheduleView 接入
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-schedule-crud/06-CONTEXT.md

@src/components/TaskDialog.tsx
@src/components/ui/Dialog.tsx
@src/components/ui/Popover.tsx
@src/components/ui/Input.tsx
@src/components/ui/Select.tsx
@src/components/ui/DatePickerInput.tsx
@src/components/ui/Button.tsx
@src/components/ui/Toast.tsx
@src/stores/scheduleStore.ts
@src/stores/productStore.ts

<interfaces>
<!-- Plan 01 完成后的 ScheduleEvent 接口 -->
```typescript
export type ScheduleEventType = 'meeting' | 'deadline' | 'task' | 'reminder' | 'review' | 'sync';

export interface ScheduleEvent {
  id: string;
  title: string;
  time: string;            // 'HH:mm - HH:mm' 格式
  date: string;            // YYYY-MM-DD
  type: ScheduleEventType;
  location: string;
  projectId?: string;
  taskId?: string;
}
```

<!-- Plan 01 完成后的 scheduleStore actions -->
```typescript
createEvent: (event: ScheduleEvent) => void;
updateEvent: (eventId: string, updates: Partial<ScheduleEvent>) => void;
deleteEvent: (eventId: string) => void;
```

<!-- TaskDialog 参考 (Phase 5 已建立的模式) -->
- Props shape: { open, onOpenChange, mode: 'create'|'edit', event?: ScheduleEvent, defaultDate?: string }
- useEffect([open, mode, event?.id]) 表单重置 (Pitfall P13)
- DialogFooter 删除按钮在 edit mode only,mr-auto
- 顶层嵌套 Dialog (z-modal 自动叠加)
- Combobox: Popover + Input + 列表 button rows + X 清除

<!-- Dialog API (src/components/ui/Dialog.tsx) -->
```typescript
Dialog, DialogContent, DialogHeader, DialogBody, DialogFooter
// DialogHeader: { title, description? }
// DialogBody: px-5 py-4 space-y-4
// DialogFooter: flex justify-end gap-2
```

<!-- DatePickerInput API -->
```typescript
interface DatePickerInputProps {
  value?: string;        // YYYY-MM-DD
  onChange?: (date: string) => void;
  label?: string;
  placeholder?: string;
}
```

<!-- productStore API -->
```typescript
products: Product[];   // { id, name, tagline, stage, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ScheduleDialog.tsx (create/edit 双模式 + Combobox + 嵌套删除确认)</name>
  <files>src/components/ScheduleDialog.tsx</files>
  <read_first>
    - src/components/TaskDialog.tsx (Phase 5 参考实现,完整复用模式)
    - src/components/ui/Dialog.tsx (DialogContent/Header/Body/Footer API)
    - src/components/ui/Popover.tsx (PopoverTrigger/Content API)
    - src/components/ui/Input.tsx (Input + label API)
    - src/components/ui/Select.tsx (SelectTrigger/Content/Item)
    - src/components/ui/DatePickerInput.tsx (value/onChange API)
    - src/components/ui/Button.tsx (variant: primary/secondary/danger)
    - src/components/ui/Toast.tsx (useToast API)
    - src/stores/scheduleStore.ts (Plan 01 后 ScheduleEvent 类型 + 3 action)
    - src/stores/productStore.ts (Product 类型 + products selector)
  </read_first>
  <behavior>
    - Props: { open: boolean, onOpenChange: (open: boolean) => void, mode: 'create'|'edit', event?: ScheduleEvent, defaultDate?: string }
    - create 模式默认值:title='', time='09:00 - 10:00', date=defaultDate ?? 今天, type='meeting', location='', projectId=undefined;autoFocus 在标题
    - edit 模式:全部字段从 event prop 同步
    - useEffect([open, mode, event?.id]):open=true 时重置 (Pitfall P13)
    - 标题校验:trim() 非空,否则主按钮 disabled
    - create 模式提交: createEvent({ id: crypto.randomUUID(), title, time, date, type, location, projectId })
    - edit 模式提交: updateEvent(event.id, { title, time, date, type, location, projectId })
    - 删除按钮 (edit mode only): DialogFooter 左侧 mr-auto,variant="danger" size="sm",触发 showDeleteConfirm=true
    - 嵌套删除确认 Dialog: title="删除日程?" body=`日程 "${event.title}" 将被永久删除,此操作无法撤销。`,确认后 deleteEvent(event.id) + 关闭外层 + Toast success "日程已删除"
    - Combobox 关联产品:同 Phase 5 TaskDialog 实现,trigger 显示已选产品名或 "搜索产品...",清除按钮 aria-label="清除产品关联"
    - 字段顺序:标题 / 日期+时间(grid 2 cols) / 类型+地点(grid 2 cols) / 关联产品 Combobox
    - 时间字段:用原生 `<input type="time">` 包一层 Input-style wrapper,或直接用 Input + placeholder="HH:mm - HH:mm"。**推荐简化**:两个 `<input type="time">`(开始/结束),内部组合成 'HH:mm - HH:mm'。如果太复杂,退化为单个 Input placeholder="09:00 - 10:00" 让用户手填(Claude's discretion per 06-CONTEXT.md)
  </behavior>
  <action>
1. 创建 `src/components/ScheduleDialog.tsx`,完整骨架(复用 TaskDialog 结构,字段替换):

   ```tsx
   import { useState, useEffect, useMemo } from 'react';
   import { Trash, X, MagnifyingGlass } from '@phosphor-icons/react';
   import { ScheduleEvent, ScheduleEventType } from '@/src/stores/scheduleStore';
   import { useScheduleStore } from '@/src/stores/scheduleStore';
   import { useProductStore } from '@/src/stores/productStore';
   import { useToast } from '@/src/components/ui/Toast';
   import {
     Dialog, DialogContent, DialogHeader, DialogBody, DialogFooter,
   } from '@/src/components/ui/Dialog';
   import { Popover, PopoverTrigger, PopoverContent } from '@/src/components/ui/Popover';
   import { Input } from '@/src/components/ui/Input';
   import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/src/components/ui/Select';
   import { DatePickerInput } from '@/src/components/ui/DatePickerInput';
   import { Button } from '@/src/components/ui/Button';
   import { cn } from '@/src/lib/utils';

   interface ScheduleDialogProps {
     open: boolean;
     onOpenChange: (open: boolean) => void;
     mode: 'create' | 'edit';
     event?: ScheduleEvent;
     defaultDate?: string;  // YYYY-MM-DD, create 模式预填
   }

   const todayISO = () => new Date().toISOString().slice(0, 10);

   const DEFAULT_EVENT: Omit<ScheduleEvent, 'id'> = {
     title: '',
     time: '09:00 - 10:00',
     date: todayISO(),
     type: 'meeting',
     location: '',
   };

   export function ScheduleDialog({ open, onOpenChange, mode, event, defaultDate }: ScheduleDialogProps) {
     const createEvent = useScheduleStore((s) => s.createEvent);
     const updateEvent = useScheduleStore((s) => s.updateEvent);
     const deleteEvent = useScheduleStore((s) => s.deleteEvent);
     const products = useProductStore((s) => s.products);
     const { toast } = useToast();

     const [title, setTitle] = useState('');
     const [date, setDate] = useState<string>(todayISO());
     const [time, setTime] = useState('09:00 - 10:00');
     const [type, setType] = useState<ScheduleEventType>('meeting');
     const [location, setLocation] = useState('');
     const [projectId, setProjectId] = useState<string | undefined>(undefined);
     const [showDeleteConfirm, setShowDeleteConfirm] = useState(false);
     const [comboboxOpen, setComboboxOpen] = useState(false);
     const [productSearch, setProductSearch] = useState('');

     useEffect(() => {
       if (!open) return;
       if (mode === 'edit' && event) {
         setTitle(event.title);
         setDate(event.date);
         setTime(event.time);
         setType(event.type);
         setLocation(event.location);
         setProjectId(event.projectId);
       } else {
         setTitle('');
         setDate(defaultDate ?? todayISO());
         setTime('09:00 - 10:00');
         setType('meeting');
         setLocation('');
         setProjectId(undefined);
       }
       setProductSearch('');
       setShowDeleteConfirm(false);
       // eslint-disable-next-line react-hooks/exhaustive-deps
     }, [open, mode, event?.id]);

     const selectedProduct = products.find((p) => p.id === projectId);
     const filteredProducts = useMemo(() => {
       const q = productSearch.trim().toLowerCase();
       if (!q) return products;
       return products.filter((p) => p.name.toLowerCase().includes(q));
     }, [products, productSearch]);

     const handleSubmit = () => {
       if (!title.trim()) return;
       const payload = {
         title: title.trim(),
         date,
         time: time.trim(),
         type,
         location: location.trim(),
         projectId,
       };
       if (mode === 'create') {
         createEvent({ ...DEFAULT_EVENT, id: crypto.randomUUID(), ...payload });
         toast({ type: 'success', title: '日程已创建' });
       } else if (event) {
         updateEvent(event.id, payload);
         toast({ type: 'success', title: '已保存' });
       }
       onOpenChange(false);
     };

     const handleDelete = () => {
       if (!event) return;
       deleteEvent(event.id);
       setShowDeleteConfirm(false);
       onOpenChange(false);
       toast({ type: 'success', title: '日程已删除' });
     };

     return (
       <>
         <Dialog open={open} onOpenChange={onOpenChange}>
           <DialogContent className="max-w-md">
             <DialogHeader
               title={mode === 'create' ? '新建日程' : '编辑日程'}
               description={mode === 'create' ? '安排一个新的日程事件' : `编辑 ${event?.title ?? ''}`}
             />
             <DialogBody>
               {/* 1. 标题 */}
               <Input
                 placeholder="日程标题"
                 value={title}
                 onChange={(e) => setTitle(e.target.value)}
                 autoFocus={mode === 'create'}
               />

               {/* 2. 日期 / 时间 (grid 2 cols) */}
               <div className="grid grid-cols-2 gap-2">
                 <DatePickerInput value={date} onChange={setDate} placeholder="日期" />
                 <Input
                   placeholder="09:00 - 10:00"
                   value={time}
                   onChange={(e) => setTime(e.target.value)}
                 />
               </div>

               {/* 3. 类型 / 地点 (grid 2 cols) */}
               <div className="grid grid-cols-2 gap-2">
                 <Select value={type} onValueChange={(v) => setType(v as ScheduleEventType)}>
                   <SelectTrigger className="h-9"><SelectValue placeholder="类型" /></SelectTrigger>
                   <SelectContent>
                     <SelectItem value="meeting">会议</SelectItem>
                     <SelectItem value="deadline">截止</SelectItem>
                     <SelectItem value="task">任务</SelectItem>
                     <SelectItem value="reminder">提醒</SelectItem>
                     <SelectItem value="review">评审</SelectItem>
                     <SelectItem value="sync">同步</SelectItem>
                   </SelectContent>
                 </Select>
                 <Input
                   placeholder="地点"
                   value={location}
                   onChange={(e) => setLocation(e.target.value)}
                 />
               </div>

               {/* 4. 关联产品 Combobox (复用 Phase 5 模式) */}
               <div className="space-y-1.5">
                 <label className="block text-sm font-medium text-text-primary">关联产品 (可选)</label>
                 <Popover open={comboboxOpen} onOpenChange={setComboboxOpen}>
                   <PopoverTrigger asChild>
                     <button
                       type="button"
                       className={cn(
                         'w-full h-9 px-3 text-sm rounded-[var(--radius-md)]',
                         'bg-bg-input border border-border text-text-primary',
                         'flex items-center justify-between gap-2 text-left',
                         'cursor-pointer transition-colors duration-fast',
                         'hover:border-text-tertiary/30',
                         'focus:outline-none focus:ring-2 focus:ring-accent/20 focus:border-accent',
                       )}
                     >
                       <span className={cn(!selectedProduct && 'text-text-placeholder')}>
                         {selectedProduct ? selectedProduct.name : '搜索产品...'}
                       </span>
                       {selectedProduct ? (
                         <button
                           type="button"
                           aria-label="清除产品关联"
                           onClick={(e) => { e.stopPropagation(); setProjectId(undefined); }}
                           className="text-text-tertiary hover:text-text-primary"
                         >
                           <X size={14} />
                         </button>
                       ) : (
                         <MagnifyingGlass size={14} className="text-text-tertiary" />
                       )}
                     </button>
                   </PopoverTrigger>
                   <PopoverContent className="w-72 p-1" align="start">
                     <Input
                       placeholder="搜索产品..."
                       value={productSearch}
                       onChange={(e) => setProductSearch(e.target.value)}
                       className="mb-1"
                       autoFocus
                     />
                     <div className="max-h-[200px] overflow-y-auto">
                       {filteredProducts.length === 0 ? (
                         <p className="text-xs text-text-tertiary py-6 text-center">未找到匹配的产品</p>
                       ) : (
                         filteredProducts.map((p) => (
                           <button
                             key={p.id}
                             type="button"
                             onClick={() => { setProjectId(p.id); setComboboxOpen(false); setProductSearch(''); }}
                             className="w-full text-left px-2.5 py-1.5 text-sm rounded-[var(--radius-sm)] hover:bg-bg-secondary text-text-primary"
                           >
                             {p.name}
                           </button>
                         ))
                       )}
                     </div>
                   </PopoverContent>
                 </Popover>
               </div>
             </DialogBody>

             <DialogFooter>
               {mode === 'edit' && (
                 <Button
                   variant="danger"
                   size="sm"
                   className="mr-auto"
                   onClick={() => setShowDeleteConfirm(true)}
                 >
                   <Trash size={14} />
                   删除
                 </Button>
               )}
               <Button variant="secondary" onClick={() => onOpenChange(false)}>取消</Button>
               <Button
                 variant="primary"
                 disabled={!title.trim()}
                 onClick={handleSubmit}
               >
                 {mode === 'create' ? '创建日程' : '保存修改'}
               </Button>
             </DialogFooter>
           </DialogContent>
         </Dialog>

         {/* 嵌套删除确认 */}
         <Dialog open={showDeleteConfirm} onOpenChange={setShowDeleteConfirm}>
           <DialogContent className="max-w-sm">
             <DialogHeader title="删除日程?" />
             <DialogBody>
               <p className="text-sm text-text-secondary">
                 日程 "{event?.title}" 将被永久删除,此操作无法撤销。
               </p>
             </DialogBody>
             <DialogFooter>
               <Button variant="secondary" onClick={() => setShowDeleteConfirm(false)}>取消</Button>
               <Button variant="danger" onClick={handleDelete}>删除</Button>
             </DialogFooter>
           </DialogContent>
         </Dialog>
       </>
     );
   }
   ```

   注意 import:ScheduleEvent + ScheduleEventType 都从 `@/src/stores/scheduleStore` 导出(Plan 01 已加 `export type ScheduleEventType`)。如果 import 别名冲突,合并成一行。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    ScheduleDialog 单文件就绪。create 模式新建 + edit 模式预填 + 删除嵌套确认 + Combobox 关联产品全部实现。Plan 03 在 ScheduleView 接入。
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 通过
- ScheduleDialog 在 create 模式下打开字段为默认值
- ScheduleDialog 在 edit 模式下字段从 event prop 同步 (Pitfall P13 fix)
- 删除确认嵌套 Dialog 可正常打开关闭
- Combobox 可搜索筛选产品,清除按钮可清空
</verification>

<success_criteria>
1. ScheduleDialog 创建模式:标题/日期/时间/类型/地点/关联产品 6 字段 → createEvent (SCHED-01 ✓)
2. ScheduleDialog 编辑模式:全字段从 event 预填 → updateEvent (SCHED-02 ✓)
3. 删除触发嵌套确认 Dialog,确认后从 store 移除 (SCHED-03 ✓)
4. Combobox 复用 Phase 5 模式 (D-11 ✓)
5. crypto.randomUUID() 生成新事件 ID
</success_criteria>

<output>
After completion, create `.planning/phases/06-schedule-crud/06-02-SUMMARY.md`
</output>
