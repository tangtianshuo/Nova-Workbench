---
phase: 07-cross-module
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/TaskKanban.tsx
  - src/views/ScheduleView.tsx
autonomous: false
requirements: [CROSS-01, CROSS-02, CROSS-04, CROSS-06]

must_haves:
  truths:
    - "用户在任务卡片 DotsMenu 看到 '安排到日历' 选项 (CROSS-01)"
    - "点击 '安排到日历' 后任务标题出现在当月对应日期的日程中,toast '已添加到日历'"
    - "已有 scheduledEventId 的任务再次点击 → 二次确认对话框 '已安排,是否重新安排?' (D-03)"
    - "任务卡片显示日程徽章 (如 Calendar icon,如有 scheduledEventId),点击切换到 schedule tab"
    - "日程视图事件 chip 显示任务徽章 (如 CheckSquare icon,如有 taskId),点击切换到 task tab (CROSS-04)"
    - "日程视图事件 chip 显示产品徽章 (如有 projectId),点击打开 ProductSummaryDrawer (复用 Phase 5, CROSS-06)"
    - "已完成的日程 chip 视觉降饱和 + 删除线 + 完成 icon (CROSS-07 视觉)"
  artifacts:
    - path: "src/components/TaskKanban.tsx"
      provides: "DotsMenu '安排到日历' + 任务卡片日程徽章"
      contains: "arrangeOnCalendar"
    - path: "src/views/ScheduleView.tsx"
      provides: "日程 chip 任务徽章 + 产品徽章 + 完成态视觉"
      contains: "taskId"
  key_links:
    - from: "src/components/TaskKanban.tsx"
      to: "AppContext.arrangeOnCalendar + useToast + useUIStore.setActiveTab"
      via: "DotsMenu 点击 → arrangeOnCalendar → toast / 切 tab"
      pattern: "arrangeOnCalendar|setActiveTab\\('schedule'\\)"
    - from: "src/views/ScheduleView.tsx"
      to: "useUIStore.setActiveTab + ProductSummaryDrawer"
      via: "chip 徽章点击 → 切 tab / 打开 drawer"
      pattern: "setActiveTab\\('task'\\)|ProductSummaryDrawer"
---

<objective>
在任务看板和日程视图之间打通 UI 联动:任务卡片可以"安排到日历",日程视图可以看到任务/产品徽章并点击跳转。

Purpose: Phase 7 用户体验的核心 — 用户不再需要在两个 tab 之间手动同步信息。CROSS-01 (安排到日历) 是最高频操作;CROSS-04/CROSS-06 (徽章可视化) 是关联感知的基础。
Output: TaskKanban DotsMenu + 日程徽章;ScheduleView chip 任务/产品徽章 + 完成态视觉
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-cross-module/07-CONTEXT.md
@.planning/phases/07-cross-module/07-01-SUMMARY.md

@src/components/TaskKanban.tsx
@src/views/ScheduleView.tsx
@src/components/ui/Badge.tsx
@src/components/ProductSummaryDrawer.tsx
@src/components/ui/DropdownMenu.tsx
@src/components/ui/Dialog.tsx
@src/store/AppContext.tsx
@src/stores/uiStore.ts

<interfaces>
<!-- 来自 07-01 的契约 (executor 直接消费) -->

```typescript
// AppContext 暴露 (07-01 Task 3 完成):
arrangeOnCalendar(taskId: string): { success: boolean; event?: ScheduleEvent; reason?: 'no-deadline' | 'already-arranged' | 'task-not-found' };

// Task 接口 (Phase 5 扩展):
interface Task {
  // ...
  projectId?: string;
  scheduledEventId?: string;  // 存在则显示日程徽章
}

// ScheduleEvent 接口 (07-01 Task 1 扩展):
interface ScheduleEvent {
  id: string;
  title: string;
  time: string;
  date: number;          // day-of-month 1-31
  type: 'meeting' | 'review' | 'sync' | 'task' | string;
  location: string;
  projectId?: string;    // 存在则显示产品徽章
  taskId?: string;       // 存在则显示任务徽章
  status?: '未开始' | '进行中' | '已完成';  // '已完成' 触发降饱和视觉
}
```

<!-- uiStore 现有 -->
```typescript
interface UIState {
  activeTab: string;
  setActiveTab: (tab: string) => void;
  selectedProductId: string | null;
  setSelectedProductId: (id: string | null) => void;
}
```

<!-- TaskKanban DotsMenu 已有 (Phase 5) -->
```tsx
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <Button variant="ghost" size="xs"><DotsThree size={14} /></Button>
  </DropdownMenuTrigger>
  <DropdownMenuContent>
    <DropdownMenuItem onClick={() => onRequestDialogEdit(task)}>编辑</DropdownMenuItem>
    <DropdownMenuItem onClick={() => onRequestDelete(task)}>删除</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskKanban DotsMenu 加 "安排到日历" + 任务卡片日程徽章</name>
  <files>src/components/TaskKanban.tsx</files>
  <read_first>
    - src/components/TaskKanban.tsx (现有 DotsMenu 位置 + 卡片渲染 + Phase 5 产品徽章参考)
    - .planning/phases/07-cross-module/07-01-SUMMARY.md (arrangeOnCalendar 契约)
    - .planning/phases/07-cross-module/07-CONTEXT.md (D-01/D-02/D-03 决策)
  </read_first>
  <behavior>
    - DotsMenu 在 "编辑" 之后、"删除" 之前新增 "安排到日历" 项 (Calendar icon)
    - 点击行为: 调 useApp().arrangeOnCalendar(task.id) → 根据 return 分支:
      - success: toast 成功 '已添加到日历' (D-02)
      - reason === 'already-arranged': 弹 Dialog "已安排,是否重新安排?" 二次确认 (D-03) → 确认后: updateTask 清 scheduledEventId → deleteEvent 删旧 → 再 arrangeOnCalendar 创建新
      - reason === 'no-deadline': toast 错误 '任务缺少截止日期,请先编辑设置截止日期'
      - reason === 'task-not-found': toast 错误 '任务不存在'
    - 任务卡片底部 (产品徽章旁) 加日程徽章: 仅在 task.scheduledEventId 存在时显示,样式 success token,点击 → setActiveTab('schedule')
  </behavior>
  <action>
1. 在 `src/components/TaskKanban.tsx` 顶部 import 补:
   ```typescript
   import { Calendar } from '@phosphor-icons/react';   // 新增 (DotsThree 等已有)
   import { useApp } from '../store/AppContext';
   import { useUIStore } from '@/src/stores/uiStore';
   import { useScheduleStore } from '@/src/stores/scheduleStore';  // 二次确认删旧 event 用
   ```
   (useToast、useTaskStore、Dialog、Button 等已在文件中 import,无需重复)

2. 在 TaskKanban 主组件 (约 line 40) 内补 hook:
   ```typescript
   const { arrangeOnCalendar } = useApp();
   const setActiveTab = useUIStore((s) => s.setActiveTab);
   const { toast } = useToast();
   ```

3. 新增 confirm rearrange dialog state (与现有 confirmDeleteTask 同层,约 line 50):
   ```typescript
   const [confirmRearrangeTask, setConfirmRearrangeTask] = useState<Task | undefined>();
   ```

4. 新增 handleArrangeOnCalendar handler (与 handleRequestDelete 同层):
   ```typescript
   const handleArrangeOnCalendar = (task: Task) => {
     const result = arrangeOnCalendar(task.id);
     if (result.success) {
       toast({ type: 'success', title: '已添加到日历', description: `「${task.title}」已同步到日程` });
     } else if (result.reason === 'already-arranged') {
       setConfirmRearrangeTask(task);
     } else if (result.reason === 'no-deadline') {
       toast({ type: 'error', title: '无法安排', description: '任务缺少截止日期,请先编辑设置截止日期' });
     }
   };

   const handleConfirmRearrange = () => {
     if (!confirmRearrangeTask) return;
     const taskId = confirmRearrangeTask.id;
     const oldEventId = confirmRearrangeTask.scheduledEventId;
     // ponytail: 重新安排 = 清 scheduledEventId → 删旧 event → arrange 创建新 event
     useTaskStore.getState().updateTask(taskId, { scheduledEventId: undefined });
     if (oldEventId) useScheduleStore.getState().deleteEvent(oldEventId);
     const result = arrangeOnCalendar(taskId);
     if (result.success) {
       toast({ type: 'success', title: '已重新安排', description: '旧日程已替换为新日程' });
     }
     setConfirmRearrangeTask(undefined);
   };
   ```

5. 在 TaskCard 子组件 DotsMenu 内 (Phase 5 已建立的位置) 加菜单项 — "编辑" 之后:
   ```tsx
   <DropdownMenuItem onClick={() => onRequestArrange?.(task)}>
     <Calendar size={14} weight="duotone" />
     <span>安排到日历</span>
   </DropdownMenuItem>
   ```
   KanbanColumn / TaskCard 子组件需要新增 prop `onRequestArrange?: (task: Task) => void`,从顶层 `<KanbanColumn ... onRequestArrange={handleArrangeOnCalendar} />` 透传 (与 onRequestDialogEdit/onRequestDelete 同模式)。

6. 在任务卡片底部 (现有产品徽章旁边) 加日程徽章:
   ```tsx
   {task.scheduledEventId && (
     <button
       type="button"
       onClick={(e) => { e.stopPropagation(); setActiveTab('schedule'); }}
       className="inline-flex items-center gap-0.5 px-1.5 py-0.5 rounded-full bg-success/10 text-success text-[10px] font-medium hover:bg-success/20 transition-colors"
       title="跳转到日程"
     >
       <Calendar size={10} weight="duotone" />
       日程
     </button>
   )}
   ```
   ponytail: 用 button 而非 Badge 以便 onClick;success token 与产品徽章 (accent) 区分。

7. 在 confirmDelete Dialog 之后加 confirmRearrange Dialog:
   ```tsx
   <Dialog open={!!confirmRearrangeTask} onOpenChange={(o) => !o && setConfirmRearrangeTask(undefined)}>
     <DialogContent className="max-w-md">
       <DialogHeader title="已安排,是否重新安排?" description="将删除旧日程并基于最新截止日期创建新日程。" />
       <DialogFooter>
         <Button variant="secondary" onClick={() => setConfirmRearrangeTask(undefined)}>取消</Button>
         <Button variant="danger" onClick={handleConfirmRearrange}>重新安排</Button>
       </DialogFooter>
     </DialogContent>
   </Dialog>
   ```
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    TaskKanban DotsMenu 含 "安排到日历",点击后任务出现在日历 + toast。已安排任务二次确认。任务卡片显示日程徽章 (如有 scheduledEventId),点击切到 schedule tab。
  </done>
</task>

<task type="auto">
  <name>Task 2: ScheduleView 日程 chip 任务徽章 + 产品徽章 + 完成态视觉</name>
  <files>src/views/ScheduleView.tsx</files>
  <read_first>
    - src/views/ScheduleView.tsx (现有 EVENT_COLORS + 日历网格 chip 渲染 + 议程列表)
    - src/components/ProductSummaryDrawer.tsx (Phase 5 已建立)
    - src/stores/uiStore.ts (setActiveTab)
    - .planning/phases/07-cross-module/07-01-SUMMARY.md (ScheduleEvent 扩展契约)
    - .planning/phases/07-cross-module/07-CONTEXT.md (D-07/D-08 决策)
  </read_first>
  <behavior>
    - EVENT_COLORS 新增 task 类型色 (与 meeting/review/sync 区分)
    - 日历网格 chip 渲染: 若 event.status === '已完成' → 加 opacity-60 + line-through + CheckCircle icon;若 event.taskId 存在 → 在 chip 内加小号 CheckSquare 徽章按钮 (点击切到 task tab);若 event.projectId 存在 → 加 FolderSimple 徽章按钮 (点击打开 ProductSummaryDrawer)
    - 议程列表 (右侧 upnext) 同步加徽章
    - 引入 ProductSummaryDrawer (从 Phase 5 复用) + useUIStore + useTaskStore (找任务标题用于 tooltip)
  </behavior>
  <action>
1. 编辑 `src/views/ScheduleView.tsx`:

   a. 顶部 import 补:
   ```typescript
   import { CheckSquare, FolderSimple, CheckCircle } from '@phosphor-icons/react';
   import { useUIStore } from '@/src/stores/uiStore';
   import { useTaskStore } from '@/src/stores/taskStore';
   import { ProductSummaryDrawer } from '@/src/components/ProductSummaryDrawer';
   import { useState } from 'react';   // 若未 import
   ```

   b. EVENT_COLORS 扩展,加 task 类型:
   ```typescript
   const EVENT_COLORS: Record<string, { bg: string; text: string; dot: string }> = {
     meeting: { bg: 'bg-accent/10', text: 'text-accent', dot: 'bg-accent' },
     review:  { bg: 'bg-purple-500/10', text: 'text-purple-600', dot: 'bg-purple-500' },
     sync:    { bg: 'bg-success/10', text: 'text-success', dot: 'bg-success' },
     task:    { bg: 'bg-warning/10', text: 'text-warning', dot: 'bg-warning' },  // 🆕 与 success (任务完成) 区分
   };
   ```

   c. 在 ScheduleView 组件内补 hook + drawer state:
   ```typescript
   const { events } = useApp();
   const setActiveTab = useUIStore((s) => s.setActiveTab);
   const tasks = useTaskStore((s) => s.categories);  // 用于查找 taskId 对应的 task 标题 (tooltip)
   const [drawerProductId, setDrawerProductId] = useState<string | undefined>();

   // 辅助: taskId → task 标题
   const findTaskTitle = (taskId?: string) => {
     if (!taskId) return '';
     for (const cat of tasks) {
       const t = cat.tasks.find((x) => x.id === taskId);
       if (t) return t.title;
     }
     return '';
   };
   ```

   d. 找到现有 chip 渲染处 (约 line 83-90 — `{day.dayEvents.map(e => { const c = EVENT_COLORS[e.type] || ...; return <div ...>{e.time.split(' ')[0]} {e.title}</div> })}`),增强 chip 渲染:
   ```tsx
   {day.dayEvents.map(e => {
     const c = EVENT_COLORS[e.type] || EVENT_COLORS.meeting;
     const isDone = e.status === '已完成';
     return (
       <div
         key={e.id}
         className={cn(
           'px-1 py-px text-[10px] rounded font-medium truncate flex items-center gap-0.5 group/chip',
           c.bg, c.text,
           isDone && 'opacity-50 line-through',
         )}
       >
         {isDone && <CheckCircle size={8} weight="fill" className="shrink-0" />}
         <span className="truncate">{e.time.split(' ')[0]} {e.title}</span>
         {e.taskId && (
           <button
             type="button"
             onClick={(ev) => { ev.stopPropagation(); setActiveTab('task'); }}
             className="shrink-0 hover:scale-110 transition-transform"
             title={`任务: ${findTaskTitle(e.taskId)}`}
           >
             <CheckSquare size={8} weight="fill" />
           </button>
         )}
         {e.projectId && (
           <button
             type="button"
             onClick={(ev) => { ev.stopPropagation(); setDrawerProductId(e.projectId); }}
             className="shrink-0 hover:scale-110 transition-transform"
             title="查看产品"
           >
             <FolderSimple size={8} weight="fill" />
           </button>
         )}
       </div>
     );
   })}
   ```

   e. 在组件 return JSX 末尾 (Card 之后) 加 ProductSummaryDrawer:
   ```tsx
   <ProductSummaryDrawer
     productId={drawerProductId}
     open={!!drawerProductId}
     onOpenChange={(o) => !o && setDrawerProductId(undefined)}
   />
   ```
   **注意:** 先读 `src/components/ProductSummaryDrawer.tsx` 确认其 props 接口 (Phase 5 已建立)。若 props 签名不同 (例如 open 控制方式),按实际签名适配。

   f. 议程列表 (约 line 99+ "Agenda" Card) 同步加徽章 — 找到 `upcomingEvents.map(...)` 内的事件渲染,补同样的 taskId/projectId 徽章 (复用上面的 JSX 结构)。ponytail: 若议程列表空间紧凑,只加 icon 不加额外 onClick,保持视觉简洁。

2. (可选) 若 ProductSummaryDrawer 不接受 open/productId props,需要先读它的接口确认。Phase 5 已封装此组件,本 plan 假设接口兼容;若不兼容,executor 应按实际接口调用 (例如可能是 `<ProductSummaryDrawer productId={x} onClose={() => setDrawerProductId(undefined)} />`)。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    日程 chip 含任务徽章 (CheckSquare) + 产品徽章 (FolderSimple) + 完成态视觉 (opacity-50 + line-through + CheckCircle)。点击任务徽章切到 task tab,点击产品徽章打开 ProductSummaryDrawer。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 7 Plan 02 UAT - 安排到日历 + 关联徽章</name>
  <action>启动 dev server,按 how-to-verify 10 步动线验证:安排到日历 -> toast + 徽章;已安排二次确认;无截止日期错误提示;chip 徽章点击跳转/drawer;任务完成联动视觉。</action>
  <what-built>
    TaskKanban 安排到日历 DotsMenu 项 + 任务卡片日程徽章;ScheduleView 日程 chip 任务/产品徽章 + 完成态视觉降饱和。
  </what-built>
  <how-to-verify>
    1. 启动 dev server: `npm run dev`,打开 http://localhost:3000
    2. 切到任务 tab,找一个**有截止日期**的任务
    3. 点击任务卡片三点菜单 → 看到 "安排到日历"
    4. 点击 → 预期: toast "已添加到日历" + 任务卡片底部出现绿色"日程"徽章
    5. 再次点击三点菜单 → "安排到日历" → 预期弹"已安排,是否重新安排?"对话框 → 点"重新安排" → toast "已重新安排"
    6. 找一个**无截止日期**的任务,点击"安排到日历" → 预期 toast 错误 "任务缺少截止日期"
    7. 切到日程 tab,预期:
       - 对应日期的事件 chip 显示 task 色调 (warning)
       - chip 上有 CheckSquare 任务徽章
       - chip 上有 FolderSimple 产品徽章 (若 task 带 projectId)
    8. 点击 chip 上的 CheckSquare → 预期切回任务 tab
    9. 点击 chip 上的 FolderSimple → 预期打开 ProductSummaryDrawer
    10. 回任务 tab,把那个任务标记完成 (completeTask),回日程 tab → 预期对应 chip 视觉降饱和 + 删除线 + CheckCircle icon
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` 通过
- 手动 UAT: 上述 10 步全部通过
</verification>

<success_criteria>
1. 任务卡片 DotsMenu 含 "安排到日历" 项 (CROSS-01 ✓)
2. 点击后任务出现在日历,toast 提示成功 (CROSS-02 ✓, D-01/D-02 ✓)
3. 已安排任务再点 → 二次确认 (D-03 ✓)
4. 任务卡片显示日程徽章,点击切 tab (CROSS-04 部分 ✓)
5. 日程 chip 显示任务徽章 + 产品徽章,点击触发跳转/drawer (CROSS-04/CROSS-06 ✓)
6. 已完成日程视觉降饱和 (CROSS-07 视觉 ✓)
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-module/07-02-SUMMARY.md`
</output>
