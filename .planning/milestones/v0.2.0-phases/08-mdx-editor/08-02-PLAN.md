---
phase: 08-mdx-editor
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/components/product/ProductKnowledgeTab.tsx
autonomous: true
requirements: [EDITOR-02]

must_haves:
  truths:
    - "ProductKnowledgeTab 编辑模式下的 <Textarea> 替换为 <MarkdownEditor>,用户输入时实时看到 Markdown 格式化效果"
    - "ProductKnowledgeTab 'create new' 模态框中的 <Textarea> 也替换为 <MarkdownEditor>"
    - "editContent state (Markdown 原文) 通过 onChange 回调同步,保存时 updateKnowledgeItem / addKnowledgeItem 接收 Markdown 原文"
    - "非编辑模式下 ReactMarkdown 只读渲染保持不变"
    - "MarkdownEditor 在 ProductKnowledgeTab 内通过 React.lazy 加载,首次进入编辑模式触发 chunk 拉取"
    - "AI 润色结果 (setEditContent) 正确注入 MarkdownEditor value"
  artifacts:
    - path: "src/components/product/ProductKnowledgeTab.tsx"
      provides: "编辑/新建模式使用 MarkdownEditor 替代 Textarea"
      contains: "MarkdownEditor"
  key_links:
    - from: "src/components/product/ProductKnowledgeTab.tsx"
      to: "src/components/ui/MarkdownEditor.tsx"
      via: "import { MarkdownEditor } from '@/src/components/ui'"
      pattern: "MarkdownEditor"
    - from: "src/components/product/ProductKnowledgeTab.tsx (编辑模式)"
      to: "editContent local state"
      via: "value={editContent} onChange={setEditContent}"
      pattern: "value=\\{editContent\\}"
    - from: "src/components/product/ProductKnowledgeTab.tsx (handleSaveEdit)"
      to: "updateKnowledgeItem (store action)"
      via: "content: editContent (Markdown 原文透传)"
      pattern: "content:\\s*editContent"
---

<objective>
将 ProductKnowledgeTab 编辑面板和"新建知识词条"模态框中的 `<Textarea>` 替换为 `<MarkdownEditor>`,用户在编辑时实时看到 Markdown 格式化效果 (粗体/标题/列表/表格/代码块)。

Purpose: EDITOR-02 要求解决"用户必须看 Markdown 原文符号编辑"的痛点。ProductKnowledgeTab 是 6 处 react-markdown 中**唯一真正的编辑场景**,优先级 P0。
Output: src/components/product/ProductKnowledgeTab.tsx 两处 Textarea → MarkdownEditor 替换
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-mdx-editor/08-CONTEXT.md
@.planning/phases/08-mdx-editor/08-01-SUMMARY.md

@src/components/product/ProductKnowledgeTab.tsx
@src/components/ui/MarkdownEditor.tsx
@src/components/ui/MarkdownEditorInner.tsx

<interfaces>
<!-- Wave 1 交付的 MarkdownEditor API -->
```typescript
interface MarkdownEditorProps {
  value: string;
  onChange: (markdown: string) => void;
  readOnly?: boolean;
  placeholder?: string;
  className?: string;
  minHeight?: string; // 默认 '320px'
}
export const MarkdownEditor = forwardRef<HTMLDivElement, MarkdownEditorProps>(...);
// 用法: <MarkdownEditor value={md} onChange={setMd} placeholder="..." />
```

<!-- 从 barrel 导入 -->
```typescript
import { MarkdownEditor } from '@/src/components/ui';
```

<!-- ProductKnowledgeTab 关键现有结构 (Wave 2 替换目标) -->
```typescript
// 编辑面板 (line ~341-346):
<Textarea
  value={editContent}
  onChange={(e) => setEditContent(e.target.value)}
  placeholder="知识正文..."
  className="text-xs font-mono min-h-[320px] leading-relaxed"
/>

// 新建模态框 (line ~437-442):
<Textarea
  value={editContent}
  onChange={(e) => setEditContent(e.target.value)}
  placeholder="# 业务背景与规则规范..."
  className="text-xs font-mono min-h-[200px]"
/>

// handleSaveEdit (line ~92-99):
updateKnowledgeItem(product.id, selectedItem.id, {
  title: editTitle,
  category: editCategory,
  content: editContent,  // ← Markdown 原文透传,无需改动
  tags: editTags.split(',').map(t => t.trim()).filter(Boolean)
});

// handleAIPolish (line ~126):
if (isEditing) setEditContent(polished);  // ← AI 润色结果注入,MarkdownEditor 自动同步
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductKnowledgeTab 编辑面板 + 新建模态框 Textarea → MarkdownEditor 替换</name>
  <files>src/components/product/ProductKnowledgeTab.tsx</files>
  <read_first>
    - src/components/product/ProductKnowledgeTab.tsx (完整文件,特别是 line 30-50 imports / line 320-360 编辑面板 / line 430-455 新建模态框 / line 82-134 handlers)
    - .planning/phases/08-mdx-editor/08-01-SUMMARY.md (确认 MarkdownEditor API 和 import 路径)
  </read_first>
  <behavior>
    - 编辑模式下用户输入 `# 标题` 回车 → 实时渲染为 H1 (不用切预览)
    - 用户输入 `**粗体**` → 实时渲染为粗体
    - 用户点 toolbar 表格按钮 → 插入 Markdown 表格骨架
    - editContent state 始终保存 Markdown 原文 (不是 HTML),保存到 store 时透传
    - AI 润色后 setEditContent(polished) → MarkdownEditor value 更新,内容刷新
    - 取消编辑 (setIsEditing(false)) → MarkdownEditor 卸载,chunk 可被 GC
    - 非编辑模式 ReactMarkdown 渲染保持不变 (line 358-361)
  </behavior>
  <action>
1. 编辑 `src/components/product/ProductKnowledgeTab.tsx`:

   a. **imports 区** (line 30-35 附近): 现有 `import { Input, Textarea } from '@/src/components/ui/Input';` 改为:
   ```typescript
   import { Input } from '@/src/components/ui/Input';
   import { MarkdownEditor } from '@/src/components/ui';
   ```
   (保留 Textarea 的 import 仅当文件其他地方还在用 — 但实际只在这两处用 Textarea,所以可以从 import 中移除. ponytail: 不删 Textarea.tsx 文件本身, 只是这个 view 不再 import.)

   b. **编辑面板** (line ~341-346): 替换 `<Textarea>` 为 `<MarkdownEditor>`:
   ```tsx
   <MarkdownEditor
     value={editContent}
     onChange={setEditContent}
     placeholder="知识正文 (支持 Markdown 实时格式化)..."
     minHeight="320px"
   />
   ```
   注意: onChange 直接传 setEditContent (签名一致,不需要 `(e) => setEditContent(e.target.value)`).

   c. **新建模态框** (line ~437-442): 同样替换:
   ```tsx
   <MarkdownEditor
     value={editContent}
     onChange={setEditContent}
     placeholder="# 业务背景与规则规范... (支持 Markdown 实时格式化)"
     minHeight="240px"
   />
   ```

   d. **不改 handlers**: handleSaveEdit (line 90-100) / handleCreateNew (line 102-118) / handleAIPolish (line 120-134) 中的 `editContent` 用法保持不变. Markdown 原文透传到 store.

2. **可选清理** (ponytail: 如果 Textarea import 已无其他引用):
   - 确认文件中除了 line 341/437 之外没有其他 `<Textarea>` 使用 (grep 验证)
   - 如果是, 从 import 中移除 Textarea, 只留 Input

3. **dev 验证**:
   - 启动 `npm run dev`,切到"产品管理" → 选一个产品 → 切到"知识库" tab
   - 点某个词条的编辑按钮 → 进入编辑模式 → 看到 MarkdownEditor (首次加载有 ~1s chunk 拉取,Skeleton 显示)
   - 输入 `# 测试标题` 回车 → 实时显示为 H1
   - 输入 `**粗体**` → 实时显示粗体
   - 点 toolbar 的表格按钮 → 插入表格骨架
   - 点 "AI 排版润色" → 等待 → editContent 更新 → MarkdownEditor 内容刷新
   - 点 "保存词条" → 退出编辑模式 → ReactMarkdown 渲染保存后的内容
   - 点 "新建知识词条" → 模态框打开 → MarkdownEditor 在模态框中渲染正常 (z-index 不被遮挡)
   - 中文输入法测试: 切换到中文输入,输入"测试" → 不卡顿,无丢字
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/product/ProductKnowledgeTab.tsx 中 `<Textarea` 出现 0 次 (grep 验证)
    - src/components/product/ProductKnowledgeTab.tsx 中 `<MarkdownEditor` 至少出现 2 次 (编辑面板 + 新建模态框)
    - src/components/product/ProductKnowledgeTab.tsx 包含 `import { MarkdownEditor } from '@/src/components/ui'` 或等价路径
    - handleSaveEdit 仍透传 `content: editContent` (Markdown 原文)
    - handleAIPolish 仍 `setEditContent(polished)` (AI 注入路径不变)
    - 非编辑模式 `<ReactMarkdown>{selectedItem.content}</ReactMarkdown>` 保持不变
    - `npm run lint` 通过
    - 运行时验证 (SUMMARY 记录): 编辑模式实时渲染 Markdown, AI 润色结果正确注入, 中文输入法不卡顿, 模态框 z-index 正常
  </acceptance_criteria>
  <done>
    ProductKnowledgeTab 编辑面板和新建模态框全部使用 MarkdownEditor, 用户实时看到格式化效果. AI 润色 / 保存 / 取消 全链路无破坏. 下一步 plan 03 处理 KnowledgeBaseView.
  </done>
</task>

</tasks>

<verification>
- `npm run lint` (tsc --noEmit) 通过
- grep 验证: src/components/product/ProductKnowledgeTab.tsx 中 `Textarea` 出现 0 次 (除非还有未替换处)
- grep 验证: src/components/product/ProductKnowledgeTab.tsx 中 `MarkdownEditor` 出现 ≥2 次
- 运行时验证: 编辑模式实时渲染 / AI 润色注入 / 中文 IME / 模态框 z-index (SUMMARY 记录)
</verification>

<success_criteria>
1. ProductKnowledgeTab 编辑面板 Textarea → MarkdownEditor 替换完成 (EDITOR-02 ✓)
2. ProductKnowledgeTab 新建模态框 Textarea → MarkdownEditor 替换完成
3. editContent (Markdown 原文) 在编辑器 ↔ store ↔ ReactMarkdown 渲染之间无损透传
4. AI 润色结果正确注入 MarkdownEditor value
5. 非编辑模式 ReactMarkdown 渲染保持不变 (D-04 ✓)
6. 运行时无 crash, 中文输入法不卡顿
</success_criteria>

<output>
After completion, create `.planning/phases/08-mdx-editor/08-02-SUMMARY.md`
SUMMARY 必须包含:
- 两处替换位置 (行号)
- 实时渲染测试结果 (H1/粗体/列表/表格/代码块)
- AI 润色注入测试结果
- 中文输入法 IME 测试结果
- 模态框内 MarkdownEditor z-index 验证结果
- 是否移除了 Textarea import
</output>
