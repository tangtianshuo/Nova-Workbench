---
phase: 11-ai-file-knowledge
plan: 01
type: execute
wave: 1
depends_on: [9-03, 9-04, 10-03]
files_modified:
  - src/ai/tools/workspaceTools.ts
  - src/ai/index.ts
  - src/ai/registry.ts
autonomous: true
---

# Objective

Register bounded workspace and read-only knowledge tools in the Phase 9 registry.

## Tasks

1. Add `listWorkspaceFiles({ workspaceId? })` using `useWorkspaceStore.getState().workspaces`.
   Return only workspace metadata and a capped list of `Workspace.files`; mark truncation and call the content `contentSnippet`/index summary.
2. Add `readKnowledgeArticle({ productId, itemId })` using `useRndStore.getState()` and reject missing or cross-product IDs with a structured tool error.
3. Export/register both tools through the existing `src/ai` barrel and add focused store-scope tests.

## Verification

`npm run lint`; tool-level tests prove workspace limits and product scoping.

## Output

`.planning/phases/11-ai-file-knowledge/11-01-SUMMARY.md`
