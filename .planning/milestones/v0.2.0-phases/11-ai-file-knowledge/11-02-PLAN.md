---
phase: 11-ai-file-knowledge
plan: 02
type: execute
wave: 2
depends_on: [11-01, 08-02, 08-03]
files_modified:
  - src/ai/tools/knowledgeTools.ts
  - src/ai/confirmations.ts
  - src/components/product/ProductKnowledgeTab.tsx
autonomous: true
---

# Objective

Implement the candidate -> explicit confirmation -> persisted product knowledge workflow.

## Tasks

1. Add `writeKnowledgeArticle` with a confirmation token/state required by the execution layer. Use `addKnowledgeItem` for creates and `updateKnowledgeItem` for updates; do not manufacture IDs or timestamps.
2. Extend the chat/tool confirmation state so unconfirmed writes are rejected and candidates are visible before mutation.
3. Connect generated or polished Markdown to the existing product knowledge edit/preview flow and preserve cancel behavior.
4. Add persistence and scope tests for create/update, invalid product/item, and rehydration.

## Verification

`npm run lint` and focused store/tool tests; no write occurs without confirmation.

## Output

`.planning/phases/11-ai-file-knowledge/11-02-SUMMARY.md`
