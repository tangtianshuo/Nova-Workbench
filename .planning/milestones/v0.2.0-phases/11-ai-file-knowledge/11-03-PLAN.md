---
phase: 11-ai-file-knowledge
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - src/ai/tools/productDocumentTools.ts
  - src/ai/tools/rndTools.ts
  - src/ai/tools/knowledgeSearchTools.ts
  - src/components/product/FullDeliverablesTab.tsx
autonomous: true
---

# Objective

Add product-document assistance, R&D deliverable generation, and the reserved knowledge organization/search contract.

## Tasks

1. Add read-only product context tools for PRD/document drafts; keep existing product-document writes draft-only because no update contract exists.
2. Register `generateDeliverable` around `generateDeliverableAI`, returning store-backed status/content after generation and preserving `generating -> ready`.
3. Add `listKnowledgeArticles`, `searchKnowledgeBase`, and tagging/category proposal contracts. Search is bounded lexical fallback only; no vector or filesystem claims.
4. Replace direct legacy R&D AI calls where the common tool loop owns the action, with a confirmation-aware candidate preview.

## Verification

`npm run lint`, tool registration checks, and deliverable state/content assertions.

## Output

`.planning/phases/11-ai-file-knowledge/11-03-SUMMARY.md`
