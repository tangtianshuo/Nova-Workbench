---
phase: 13-event-log-toolloop
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/ai/toolLoop.ts
  - src/ai/__tests__/phase13ToolLoopEvents.test.ts
  - src/ai/__tests__/phase13ReplayParity.test.ts
  - package.json
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - EVT-03
  - EVT-06
  - EVT-08

must_haves:
  truths:
    - "toolLoop 内不存在跨迭代存活的第二份 messages 数组 — 每次迭代前从 session.getMessagesForLLM() 重新派生"
    - "toolCallId 全部为 crypto.randomUUID(),旧位置计数器 `${iteration}-${name}-${count}` 消失"
    - "一次完整 turn 的事件序列为 user_message → tool_call → tool_result → assistant_message → turn_ended,seq 连续、同 turn 共享 correlation_id"
    - "确认等待(知识写入 / 破坏性操作)以普通 tool_result(ok:false, awaitingConfirmation:true)落事件,配对保持平衡;onToolEnd 的 error 参数对确认仍为 undefined(0bbc3f2 金丝雀)"
    - ">4KB 工具结果经 prepareToolResult 存 artifact,模型历史只剩 summary + artifactId + head 片段(512 字符)"
    - "同一会话回放两次派生的 LLM messages 完全一致(永久 replay parity 测试),且 runToolLoop 公开签名与 ToolLoopCallbacks 接口零变化"
  artifacts:
    - path: "src/ai/toolLoop.ts"
      provides: "单历史 toolLoop:每步经 ChatSession 落事件,UUID toolCallId,turn_ended 收尾,turn 末审计配对"
      contains: "crypto.randomUUID()"
    - path: "src/ai/__tests__/phase13ToolLoopEvents.test.ts"
      provides: "事件序列/配对/artifact/确认等待语义测试"
    - path: "src/ai/__tests__/phase13ReplayParity.test.ts"
      provides: "永久 replay parity 测试(回放两次完全一致)"
    - path: "package.json"
      provides: "npm test 覆盖 src/ai/__tests__ 与 src/stores/__tests__"
  key_links:
    - from: "src/ai/toolLoop.ts"
      to: "src/ai/chatSession.ts"
      via: "每步只调 session.addMessage / recordTurnEnd(单一写入路径,无本地历史)"
      pattern: "session\\.getMessagesForLLM\\(\\)"
    - from: "src/ai/toolLoop.ts"
      to: "src/ai/events/artifacts.ts"
      via: "prepareToolResult + getEventStore().saveArtifact"
      pattern: "prepareToolResult"
    - from: "src/ai/toolLoop.ts"
      to: "src/ai/events/invariants.ts"
      via: "turn 末 checkEventStream 审计,违规 console.error 报告"
      pattern: "checkEventStream"
    - from: "src/ai/__tests__/phase13ReplayParity.test.ts"
      to: "ChatSession.fromEvents"
      via: "同一事件流重建两次 → getMessagesForLLM deepEqual"
      pattern: "fromEvents"
---

<objective>
重写 toolLoop 为单历史事件驱动循环:公开签名与 ToolLoopCallbacks 不变;内部删除第二份 messages 数组,每次 LLM 调用前从 ChatSession 投影重新派生;每步(用户消息/模型输出/工具调用/工具结果/确认等待/turn 结束)经 session 落事件;toolCallId 用 UUID;>4KB 结果 artifact 化;turn 末执行配对审计并报告。落地永久 replay parity 测试与 npm test 覆盖。

Purpose: 关闭 SC1/SC3/SC4/SC5 — 双历史 bug 从结构上消失,事件流完整配对,回放确定性可测试。
Output: 重写后的 toolLoop.ts + 两个测试文件 + package.json test 脚本扩围。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-event-log-toolloop/13-CONTEXT.md
@.planning/phases/13-event-log-toolloop/13-01-SUMMARY.md
@.planning/phases/13-event-log-toolloop/13-02-SUMMARY.md
@src/ai/toolLoop.ts
@src/ai/chatSession.ts
@src/ai/events/artifacts.ts
@src/ai/events/invariants.ts
@src/ai/confirmations.ts
@src/components/ChatPanel.tsx
@src/components/CmdKPalette.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 toolLoop.ts — 单历史 + UUID toolCallId + 事件化每一步 + turn 末审计</name>
  <files>src/ai/toolLoop.ts</files>
  <read_first>
    - src/ai/toolLoop.ts(当前全文 — 逐段对照重写;onToolEnd 对 ConfirmationRequiredError 传 undefined error 的 ponytail 行为必须原样保留)
    - src/ai/chatSession.ts(Plan 02 产物:addMessage 的 payload 参数、setCorrelationId、recordTurnEnd、flushEvents 签名)
    - src/ai/events/artifacts.ts(prepareToolResult 输入输出)
    - src/ai/events/invariants.ts(checkEventStream)
    - src/ai/events/eventStore.ts(getEventStore/setEventScopeProvider/saveArtifact)
    - src/ai/confirmations.ts(ConfirmationRequiredError、两类 candidate — 不改)
    - src/components/ChatPanel.tsx 与 src/components/CmdKPalette.tsx(调用方 — 确认 runToolLoop 参数与回调使用方式,重写后不得需要改动这两个文件)
    - .planning/phases/13-event-log-toolloop/13-CONTEXT.md(硬约束:公开签名/回调接口不变、确认流内存态不动、{ ok:false } 语义保留)
  </read_first>
  <action>
重写 `src/ai/toolLoop.ts`。`ToolLoopCallbacks`、`RunToolLoopArgs`、`ToolLoopResult` 三个接口字段逐字不变;confirmations 再导出块不变;`MAX_ITERATIONS = 5` 不变;`isDestructiveConfirmation` 不变。具体变更:

1. **删除** `function stringifyResult(...)`(盲切 2000 字符,由 prepareToolResult 取代)。

2. **新增导入**(追加到现有 import 区):
```ts
import { getEventStore, setEventScopeProvider } from './events/eventStore';
import { checkEventStream } from './events/invariants';
import { prepareToolResult } from './events/artifacts';
```

3. **模块级注册事件 scope 提供者**(放在 import 之后、MAX_ITERATIONS 之前):
```ts
// Event scope stamping: current selected product. workspace/project linkage is
// deferred (see 13-CONTEXT deferred ideas) — columns exist, values stay null.
setEventScopeProvider(() => ({
  workspaceId: null,
  productId: useUIStore.getState().selectedProductId,
  projectId: null,
}));
```

4. **turn 末审计辅助函数**(模块级):
```ts
/** Loud, never-silent audit: report pairing/seq issues at turn boundaries. */
async function auditSessionEvents(sessionId: string): Promise<void> {
  try {
    const events = await getEventStore().listEvents(sessionId);
    const issues = checkEventStream(events);
    if (issues.length > 0) {
      console.error('[event-log] invariant violations at turn end', issues);
    }
  } catch (error) {
    console.error('[event-log] turn-end audit failed', error);
  }
}
```

5. **runToolLoop 主体重写**为(逐字结构,注意每个 return 前都有 turn 收尾):
```ts
export async function runToolLoop(args: RunToolLoopArgs): Promise<ToolLoopResult> {
  const session = args.session ?? new ChatSession();
  // One turn = one correlation id; every event emitted this run carries it.
  const correlationId = crypto.randomUUID();
  session.setCorrelationId(correlationId);
  session.addMessage('user', args.userMessage);
  const systemPrompt = args.systemPromptOverride ?? buildSystemPrompt({ coreContext: buildCoreContext() });
  const argErrorCount = new Map<string, number>();
  let content = '';
  let toolCallsExecuted = 0;

  const endTurn = async (outcome: 'completed' | 'tool_limit' | 'awaiting_confirmation' | 'awaiting_destructive_confirmation', iterations: number) => {
    session.recordTurnEnd({ outcome, iterations, toolCallsExecuted, correlationId });
    await session.flushEvents();
    await auditSessionEvents(session.sessionId);
  };

  for (let iteration = 1; iteration <= MAX_ITERATIONS; iteration += 1) {
    // Single source of truth: derive the LLM messages from the session projection
    // on EVERY iteration. No second history array survives across iterations.
    const messages: ChatMessage[] = session.getMessagesForLLM().map((message) => ({
      role: message.role === 'tool' ? 'user' : message.role,
      content: message.content,
    }));

    const result = await chatWithTools({
      messages,
      tools: toolsToSchemas(),
      systemPrompt,
      provider: args.provider,
      // ponytail: pull from store via getState() (this is a plain async fn, not a
      // component). Only Ollama branch reads it; undefined for others.
      ollamaModel: args.provider === 'ollama' ? useUIStore.getState().ollamaModel : undefined,
      onToken: args.callbacks?.onToken,
      signal: args.signal,
    });
    content = result.content;

    if (result.toolCalls.length === 0) {
      session.addMessage('assistant', content);
      await endTurn('completed', iteration);
      return { content, iterations: iteration, toolCallsExecuted, truncated: false };
    }

    for (const call of result.toolCalls) {
      // EVT-06: UUID replaces the positional `${iteration}-${name}-${count}` id.
      const toolCallId = crypto.randomUUID();
      session.addMessage('assistant', content || '[requesting tools]', toolCallId, call.name, { args: call.args });
      args.callbacks?.onToolStart?.(call.name, call.args);
      try {
        const toolResult = await executeTool(call.name, call.args);
        if (isDestructiveConfirmation(toolResult)) {
          args.callbacks?.onToolEnd?.(call.name, toolResult);
          args.callbacks?.onDestructiveConfirmationRequired?.(toolResult);
          // WAIT outcome lands as a normal tool_result ({ ok: false } semantics)
          // so tool_call/tool_result pairing stays balanced across the pause.
          const waitText = `[tool_result ${call.name}] ${JSON.stringify({ ok: false, awaitingConfirmation: true, summary: toolResult.summary })}`;
          session.addMessage('tool', waitText, toolCallId, call.name, { ok: false, awaitingConfirmation: true });
          await endTurn('awaiting_destructive_confirmation', iteration);
          return {
            content,
            iterations: iteration,
            toolCallsExecuted,
            truncated: false,
            pendingDestructiveConfirmation: toolResult,
          };
        }
        toolCallsExecuted += 1;
        args.callbacks?.onToolEnd?.(call.name, toolResult);
        // EVT-08: >4KB results go to the artifacts store; model history keeps
        // summary + artifactId + head fragment only.
        const prepared = prepareToolResult({ sessionId: session.sessionId, toolCallId, toolName: call.name, value: toolResult });
        if (prepared.artifact) {
          await getEventStore().saveArtifact(prepared.artifact);
        }
        session.addMessage('tool', prepared.modelText, toolCallId, call.name, {
          ok: true,
          artifactId: prepared.artifact?.artifactId ?? null,
        });
      } catch (error) {
        const isConfirmation = error instanceof ConfirmationRequiredError;
        const errorMessage = error instanceof Error ? error.message : String(error);
        // ponytail: ConfirmationRequiredError 是预期的"等待确认"流程,不是错误。
        // confirmation 时 onToolEnd 的 error 参数保持 undefined,trace 显示 ok —
        // 这是 0bbc3f2 金丝雀行为,不得改动。
        args.callbacks?.onToolEnd?.(
          call.name,
          null,
          isConfirmation ? undefined : errorMessage,
        );
        if (isConfirmation) {
          // Key order deliberately matches the destructive branch ({ ok, awaitingConfirmation, ... })
          // so both WAIT branches share one greppable `ok: false, awaitingConfirmation: true` prefix.
          // (JSON 解析与键序无关,语义不变。)
          const waitText = `[tool_result ${call.name}] ${JSON.stringify({ ok: false, awaitingConfirmation: true, error: errorMessage })}`;
          session.addMessage('tool', waitText, toolCallId, call.name, { ok: false, awaitingConfirmation: true, error: errorMessage });
          args.callbacks?.onConfirmationRequired?.(error.candidate);
          await endTurn('awaiting_confirmation', iteration);
          return {
            content,
            iterations: iteration,
            toolCallsExecuted,
            truncated: false,
            pendingConfirmation: error.candidate,
          };
        }
        const previousErrors = argErrorCount.get(call.name) ?? 0;
        const isRetryAvailable = error instanceof ToolArgError && previousErrors < 1;
        if (error instanceof ToolArgError) {
          argErrorCount.set(call.name, previousErrors + 1);
        }
        const errorText = `[tool_error ${call.name}] ${errorMessage}. ${isRetryAvailable
          ? 'Please correct the arguments and retry once.'
          : 'No more argument retries are available for this tool call.'}`;
        session.addMessage('tool', errorText, toolCallId, call.name, { ok: false, error: errorMessage, retryAvailable: isRetryAvailable });
      }
    }
  }

  const limitedContent = `${content}${content ? '\n\n' : ''}[tool loop reached the ${MAX_ITERATIONS}-iteration limit]`;
  // Close the turn in the projection too (old code dropped this from the session).
  session.addMessage('assistant', limitedContent);
  await endTurn('tool_limit', MAX_ITERATIONS);
  return {
    content: limitedContent,
    iterations: MAX_ITERATIONS,
    toolCallsExecuted,
    truncated: true,
  };
}
```

6. **禁止事项检查清单**(重写后逐条自查):不再有循环外的 `const messages: ChatMessage[] = session.getMessagesForLLM()...`;不再有任何 `messages.push(`;不再有 `` `${iteration}-` `` 形式的 toolCallId;`confirmations.ts` 零改动;ChatPanel.tsx / CmdKPalette.tsx 零改动。
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint && npx tsx --test src/ai/__tests__/knowledgeWrite.test.ts src/ai/__tests__/phase10PromptContext.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/toolLoop.ts 包含 `const toolCallId = crypto.randomUUID();`
    - src/ai/toolLoop.ts 不包含字符串 `messages.push(`(grep 0 命中)
    - src/ai/toolLoop.ts 不包含字符串 `${iteration}-`(旧位置计数器消失)
    - src/ai/toolLoop.ts 不包含 `slice(0, 2000)`(盲切消失)
    - src/ai/toolLoop.ts 包含 `session.getMessagesForLLM()` 且该调用位于 for 循环体内部(每迭代重新派生)
    - src/ai/toolLoop.ts 包含 `session.recordTurnEnd(`、`await session.flushEvents()`、`checkEventStream(`、`prepareToolResult(`、`setEventScopeProvider(`
    - src/ai/toolLoop.ts 包含 `isConfirmation ? undefined : errorMessage`(0bbc3f2 金丝雀:确认时 trace 不翻红)
    - src/ai/toolLoop.ts 的确认等待分支含 `JSON.stringify({ ok: false, awaitingConfirmation: true` 前缀的 waitText(两处:destructive 与 knowledge — knowledge 分支键序已统一为 { ok, awaitingConfirmation, error })
    - `ToolLoopCallbacks` 接口的五个可选回调字段(onToken/onToolStart/onToolEnd/onConfirmationRequired/onDestructiveConfirmationRequired)签名逐字不变
    - src/ai/confirmations.ts、src/components/ChatPanel.tsx、src/components/CmdKPalette.tsx 三个文件 git diff 为空
  </acceptance_criteria>
  <done>toolLoop 单历史化:每步经 session 落事件,UUID 配对,确认等待也落 tool_result,turn 末审计,调用方零改动</done>
</task>

<task type="auto">
  <name>Task 2: phase13ToolLoopEvents.test.ts — 事件序列/配对/artifact/确认等待语义</name>
  <files>src/ai/__tests__/phase13ToolLoopEvents.test.ts</files>
  <read_first>
    - src/ai/toolLoop.ts(重写后的事件发射点与 modelText 格式 — 测试按 toolLoop 的实际调用序列模拟)
    - src/ai/chatSession.ts(addMessage payload 参数、recordTurnEnd、flushEvents)
    - src/ai/events/eventStore.ts(getMemoryEventStore/resetMemoryEventStore/saveArtifact/getArtifact)
    - src/ai/events/artifacts.ts(prepareToolResult、ARTIFACT_THRESHOLD_CHARS、ARTIFACT_HEAD_CHARS — 阈值与 head 窗口大小)
    - src/ai/events/invariants.ts(checkEventStream / assertEventStreamValid — 测试 4 直接 import)
    - src/ai/events/types.ts(AgentEvent 形状 — 测试 4 手工构造事件对象)
    - src/ai/__tests__/phase13ChatSessionProjection.test.ts(Plan 02 测试风格)
  </read_first>
  <action>
新建 `src/ai/__tests__/phase13ToolLoopEvents.test.ts`,node:test + node:assert/strict,文件头 `// Phase 13 Plan 03 — toolLoop event emission semantics (simulated write path; the live loop needs an LLM provider and is covered by UAT).`,每个测试开头 resetMemoryEventStore()。必须包含以下测试(名称逐字):

1. `test('a tool turn lands as a paired, contiguous, correlated event sequence', async () => ...)`:
按 toolLoop 的调用序列模拟一个含工具调用的 turn(sessionId 'loop-1'):
```ts
const session = new ChatSession({ sessionId: 'loop-1' });
const correlationId = crypto.randomUUID();
session.setCorrelationId(correlationId);
session.addMessage('user', '帮我建个任务');
const toolCallId = crypto.randomUUID();
session.addMessage('assistant', '好的,先查询上下文', toolCallId, 'createTask', { args: { title: '写周报' } });
const prepared = prepareToolResult({ sessionId: 'loop-1', toolCallId, toolName: 'createTask', value: { taskId: 't-1' } });
session.addMessage('tool', prepared.modelText, toolCallId, 'createTask', { ok: true, artifactId: null });
session.addMessage('assistant', '任务已创建');
session.recordTurnEnd({ outcome: 'completed', iterations: 1, toolCallsExecuted: 1, correlationId });
await session.flushEvents();
```
断言 listEvents('loop-1') 的 eventType 序列 deepEqual ['session_created', 'user_message', 'tool_call', 'tool_result', 'assistant_message', 'turn_ended'];seq deepEqual [1,2,3,4,5,6];user_message 到 turn_ended 六条中后五条(seq 2-6)correlationId 均等于 correlationId;tool_call.payload.toolCallId === tool_result.payload.toolCallId === toolCallId;checkEventStream(events) deepEqual []。

2. `test('confirmation WAIT outcome lands as a normal balanced tool_result with ok:false semantics', async () => ...)`:
session 'loop-2' 模拟确认等待:user → assistant(toolCallId tc-wait,toolName 'writeKnowledgeArticle')→ tool(modelText `[tool_result writeKnowledgeArticle] ${JSON.stringify({ ok: false, awaitingConfirmation: true, error: 'Explicit confirmation is required before writing knowledge.' })}`(键序与 toolLoop 两处 WAIT 分支一致),payload { ok: false, awaitingConfirmation: true })→ recordTurnEnd({ outcome: 'awaiting_confirmation' })。
断言:checkEventStream(events) deepEqual [](配对平衡,无孤儿 tool_call);tool_result 事件 payload.ok === false 且 payload.awaitingConfirmation === true;turn_ended payload.outcome === 'awaiting_confirmation'。

3. `test('oversized tool results leave only summary + artifact reference in model history', async () => ...)`:
toolCallId 'tc-big';`const prepared = prepareToolResult({ sessionId: 'loop-3', toolCallId: 'tc-big', toolName: 'getProductDocumentContext', value: { document: 'P'.repeat(6000) } });`
断言 prepared.artifact !== null;`await getEventStore().saveArtifact(prepared.artifact!);` 断言 prepared.modelText.length < 2000(模型可见文本上界 = `[tool_result ...] ` 前缀 + reference JSON(summary + artifactId + 512 字符 head),远小于 6000 全文)。
session 'loop-3':user → assistant(toolCallId tc-big)→ tool(prepared.modelText, payload { ok: true, artifactId: prepared.artifact!.artifactId })→ assistant('文档已读取')→ flush。
断言:session.getMessagesForLLM() 的所有 content 拼接后不包含 'P'.repeat(1000)(**阈值必须 > head 窗口 512**:JSON 前缀约 31 字符,head 本身合法地包含约 481 个连续 'P',用 ≤512 的阈值会误判正确实现;1000 既放过 head 又拦住任何全文泄漏)、包含 prepared.artifact.artifactId、包含 '"summary"';`getMessagesForLLM()` 与 `ChatSession.fromEvents(events).getMessagesForLLM()` deepEqual;`await getEventStore().getArtifact(prepared.artifact!.artifactId)` 的 content 包含 'P'.repeat(6000)(全文可在 artifacts 查到 — 正向锚点保留)。

4. `test('audit reports a turn whose tool_result never arrived (never silent pass)', async () => ...)`:
手工构造事件流(不经 session):user_message(seq1) → tool_call(seq2, toolCallId 'tc-orphan', payload 含 toolCallId/toolName) → turn_ended(seq3)。
断言 checkEventStream(events) 长度 1、code 'MISSING_TOOL_RESULT'、toolCallId 'tc-orphan';`assert.throws(() => assertEventStreamValid(events), /invariant violation/)。(import { assertEventStreamValid } from '../events/invariants')`
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npx tsx --test src/ai/__tests__/phase13ToolLoopEvents.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test src/ai/__tests__/phase13ToolLoopEvents.test.ts` 退出码 0,4 个具名测试全部通过
    - 测试文件包含 eventType 序列断言 `['session_created', 'user_message', 'tool_call', 'tool_result', 'assistant_message', 'turn_ended']`
    - 测试文件包含 `'MISSING_TOOL_RESULT'` 与 `assert.throws(` 断言(缺失 tool_result 被检测且抛错 — SC2 锚点)
    - 测试文件包含 artifact 断言:getArtifact 返回 content 包含完整 6000 字符内容
    - 测试文件的超大结果"全文未进历史"断言使用 `'P'.repeat(1000)` 缺失(阈值 > ARTIFACT_HEAD_CHARS=512)与 `modelText.length < 2000` 上界,不使用 ≤512 的连续 'P' 阈值(避免与合法 head 片段自相矛盾)
  </acceptance_criteria>
  <done>toolLoop 事件语义(序列/配对/artifact/确认等待/审计)全部有自动化锚点</done>
</task>

<task type="auto">
  <name>Task 3: phase13ReplayParity.test.ts 永久测试 + npm test 扩围</name>
  <files>src/ai/__tests__/phase13ReplayParity.test.ts, package.json</files>
  <read_first>
    - src/ai/chatSession.ts(fromEvents 行为)
    - src/ai/events/eventStore.ts(getEventStore/listEvents/saveArtifact — parity 会话落事件与 artifact 全文取回)
    - src/ai/events/artifacts.ts(prepareToolResult / ARTIFACT_HEAD_CHARS — >4KB artifact 轮构造与 head 窗口大小)
    - src/ai/events/invariants.ts(assertEventStreamValid)
    - src/ai/__tests__/phase13ToolLoopEvents.test.ts(Task 2 的模拟写法)
    - package.json(现有 test 脚本 `"test": "tsx --test src/stores/__tests__/*.test.ts"`)
    - .planning/phases/13-event-log-toolloop/13-CONTEXT.md(Locked:replay parity 为永久测试)
  </read_first>
  <action>
1. 新建 `src/ai/__tests__/phase13ReplayParity.test.ts`,文件头:
```ts
// Phase 13 Plan 03 — PERMANENT replay parity test (do not delete).
// The same session replayed twice must derive byte-identical LLM messages.
// Canary lineage: 0bbc3f2 trace-color regression folded into the same suite.
```
node:test + node:assert/strict。包含:

`test('PERMANENT: replaying the same session twice derives identical LLM messages', async () => ...)`:
- resetMemoryEventStore()。构造多轮复合会话 'parity-1'(覆盖:user/assistant 纯文本轮、含 2 个并行工具调用的轮、tool_error 重试提示轮、确认等待轮、>4KB artifact 轮):
```ts
const live = new ChatSession({ sessionId: 'parity-1' });
live.setCorrelationId(crypto.randomUUID());
live.addMessage('user', '先查任务和产品');
const tcA = crypto.randomUUID(); const tcB = crypto.randomUUID();
live.addMessage('assistant', '同时查两个', tcA, 'listTasks', { args: {} });
live.addMessage('assistant', '同时查两个', tcB, 'listProducts', { args: {} });
live.addMessage('tool', '[tool_result listTasks] {"ok":true,"data":[]}', tcA, 'listTasks', { ok: true });
live.addMessage('tool', '[tool_result listProducts] {"ok":true,"data":[]}', tcB, 'listProducts', { ok: true });
live.addMessage('assistant', '查询完成');
live.recordTurnEnd({ outcome: 'completed', iterations: 1, toolCallsExecuted: 2 });
live.setCorrelationId(crypto.randomUUID());
live.addMessage('user', '创建一个任务');
const tcC = crypto.randomUUID();
live.addMessage('assistant', '好的', tcC, 'createTask', { args: {} });
live.addMessage('tool', '[tool_error createTask] Tool "createTask" arg validation failed: []. Please correct the arguments and retry once.', tcC, 'createTask', { ok: false, retryAvailable: true });
const tcD = crypto.randomUUID();
live.addMessage('assistant', '重试', tcD, 'createTask', { args: { title: '写周报' } });
const big = prepareToolResult({ sessionId: 'parity-1', toolCallId: tcD, toolName: 'createTask', value: { blob: 'B'.repeat(6000) } });
if (big.artifact) await getEventStore().saveArtifact(big.artifact);
live.addMessage('tool', big.modelText, tcD, 'createTask', { ok: true, artifactId: big.artifact?.artifactId ?? null });
live.addMessage('assistant', '创建完成');
live.recordTurnEnd({ outcome: 'completed', iterations: 2, toolCallsExecuted: 1 });
await live.flushEvents();
```
- `const events = await getEventStore().listEvents('parity-1');`
- `assertEventStreamValid(events);`(回放前先验配对/seq 合法)
- `const replayA = ChatSession.fromEvents(events); const replayB = ChatSession.fromEvents(events);`
- 断言 `replayA.getMessagesForLLM()` deepEqual `live.getMessagesForLLM()`(活会话与重建同派生);
- 断言 `replayB.getMessagesForLLM()` deepEqual `replayA.getMessagesForLLM()`(两次回放一致);
- 断言 `JSON.stringify(replayB.getMessagesForLLM()) === JSON.stringify(replayA.getMessagesForLLM())`(字节级);
- 断言 `big.modelText.length < 2000`(模型可见上界,远小于 6000 全文),且派生消息中存在一条 content 含 big.artifact.artifactId、所有消息 content 均不包含 'B'.repeat(1000)(**阈值必须 > head 窗口 512**:JSON 前缀约 27 字符,head 合法包含约 485 个连续 'B',用 'B'.repeat(100) 会误判正确实现;1000 放过 head、拦住全文)(artifact 引用进历史、全文不进);
- 断言派生消息中存在 content 含 'Please correct the arguments and retry once.'(错误重试文本也在单一派生里)。

2. 修改 `package.json` 的 scripts.test:
```json
"test": "tsx --test src/stores/__tests__/*.test.ts src/ai/__tests__/*.test.ts"
```
(其余字段不动。)
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npx tsx --test src/ai/__tests__/phase13ReplayParity.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test src/ai/__tests__/phase13ReplayParity.test.ts` 退出码 0
    - src/ai/__tests__/phase13ReplayParity.test.ts 包含字符串 'PERMANENT: replaying the same session twice derives identical LLM messages'
    - src/ai/__tests__/phase13ReplayParity.test.ts 包含两次 `ChatSession.fromEvents(events)` 调用与二者 getMessagesForLLM() 的 deepEqual 断言
    - src/ai/__tests__/phase13ReplayParity.test.ts 包含 `assertEventStreamValid(events)`
    - src/ai/__tests__/phase13ReplayParity.test.ts 的全文缺失断言使用 `'B'.repeat(1000)`(> head 窗口 512,而非 'B'.repeat(100)),并含 `big.modelText.length < 2000` 上界断言
    - package.json 包含 `"test": "tsx --test src/stores/__tests__/*.test.ts src/ai/__tests__/*.test.ts"`
    - `npm test` 退出码 0(stores 2 个文件 + ai 全部测试文件通过 — replay parity 进入常驻回归)
  </acceptance_criteria>
  <done>永久 replay parity 测试落地并纳入 npm test;SC4 的回放一致性有字节级断言</done>
</task>

</tasks>

<verification>
1. `npm run lint` — 0 类型错误
2. `npm test` — 全绿(stores + ai 全部测试,含 phase13 三个新测试文件)
3. 金丝雀回归:`npx tsx --test src/ai/__tests__/knowledgeWrite.test.ts`(0bbc3f2 相关:确认流的 { ok:false } 与 trace 语义)— 6/6;`npx tsx --test src/ai/__tests__/phase10PromptContext.test.ts` — 4/4
4. grep 三连(双历史消失证明):toolLoop.ts 中 `messages.push(` 0 命中、`${iteration}-` 0 命中、`slice(0, 2000)` 0 命中
5. 调用方零改动:`git diff --stat src/components/ChatPanel.tsx src/components/CmdKPalette.tsx src/ai/confirmations.ts` 输出为空
6. 手动 UAT(tauri:dev,SC1 的实表验证):打开 ChatPanel → 发送"帮我建个任务 写周报"触发工具调用 → 对话结束后用任意 SQLite 浏览器打开 app data 目录下 nova.db,`SELECT seq, event_type, correlation_id FROM agent_events ORDER BY session_id, seq` 应见 session_created→user_message→tool_call→tool_result→assistant_message→turn_ended 完整配对序列,seq 连续;`SELECT count(*) FROM agent_artifacts` 在大结果场景 ≥1
7. **Residual risk(已知残留风险 — 必须原样写进 SUMMARY)**:`SqliteEventStore.append` 返回的 AgentEvent.seq 恒为 -1(权威 seq 在行内、SQL 侧分配),且所有 Node 测试走 MemoryEventStore — 真实 INSERT SQL(含 `(SELECT COALESCE(MAX(seq), 0) + 1 ...)` 子查询与 UNIQUE(session_id, seq) 约束)没有任何自动化测试覆盖;SC1 的实表证据完全依赖上方第 6 步的手动 UAT 查询。UAT 若发现 seq 空洞/重复,修复点在 Plan 01 的 SqliteEventStore/迁移 DDL,不得改为 JS 侧分配 seq(locked by research:seq 永远 SQL 侧)。
</verification>

<success_criteria>
- SC1:含工具调用的对话结束后 agent_events 出现完整配对事件序列,每条含会话内连续 seq 与 correlation_id(自动化:模拟序列测试;实表:UAT 查询 — 见 verification 第 7 条残留风险)
- SC2:缺失 tool_result 的事件流被 checkEventStream/assertEventStreamValid 检测并报告(自动化锚点 + turn 末审计代码路径)
- SC3:toolLoop 不再有第二份历史 — 每次迭代从 session.getMessagesForLLM() 重新派生,grep 证明 messages.push 消失
- SC4:replay parity 永久测试通过(同一会话回放两次派生字节级一致);中文 token 估算已在 Plan 01/02 修复并有测试锚点
- SC5:>4KB 工具结果在模型历史中只剩 summary + artifactId + head(512 字符),全文在 artifacts 表/存储可查
- runToolLoop 公开签名、ToolLoopCallbacks、确认流(内存 Map)零变化
</success_criteria>

<output>
After completion, create `.planning/phases/13-event-log-toolloop/13-03-SUMMARY.md`.
SUMMARY 必须包含 "Residual risk" 小节(内容与 <verification> 第 7 条一致):SqliteEventStore.append 返回 seq:-1(权威 seq 在行内、SQL 侧分配);Node 测试不执行真实 INSERT SQL;SC1 实表证据依赖手动 UAT 查询;seq 异常时修复点在 Plan 01 的 SqliteEventStore/迁移 DDL,不得改为 JS 侧分配。
</output>
