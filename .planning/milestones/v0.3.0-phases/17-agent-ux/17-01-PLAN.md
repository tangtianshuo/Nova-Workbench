---
phase: 17-agent-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/chatConsoleStore.ts
  - src/components/AgentConsole.tsx
  - src/components/ChatPanel.tsx
  - src/views/AgentWorkspaceView.tsx
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "Agent 标签页与侧栏 Drawer 显示同一场对话（共享 store，非两份状态）"
    - "流式输出/HITL 卡片进行中切换宿主（Drawer ↔ agent 标签页），消息流与 pending 卡片连续不丢失、不重跑恢复"
    - "Agent 标签页呈现真实 agent 会话能力（流式、tools、HITL 卡片、会话恢复），309 行 mock 全部消失"
  artifacts:
    - path: "src/stores/chatConsoleStore.ts"
      provides: "会话主体状态（messages/input/streaming/pending 卡片/sessionRef）唯一归属"
      contains: "export const useChatConsoleStore"
    - path: "src/components/AgentConsole.tsx"
      provides: "双宿主会话组件（drawer/page 两种 layout）"
      contains: "layout"
    - path: "src/views/AgentWorkspaceView.tsx"
      provides: "agent 工作区页宿主"
      contains: "max-w-3xl"
  key_links:
    - from: "src/components/ChatPanel.tsx"
      to: "src/components/AgentConsole.tsx"
      via: "Drawer 壳内渲染 AgentConsole"
      pattern: "AgentConsole"
    - from: "src/views/AgentWorkspaceView.tsx"
      to: "src/components/AgentConsole.tsx"
      via: "glass Card 内渲染 AgentConsole layout=\"page\""
      pattern: "AgentConsole"
    - from: "src/components/AgentConsole.tsx"
      to: "src/stores/chatConsoleStore.ts"
      via: "zustand selectors 订阅全部会话状态"
      pattern: "useChatConsoleStore"
---

<objective>
把 ChatPanel 663 行的会话主体抽取为共享 AgentConsole（状态落入 transient chatConsoleStore），ChatPanel 退化为 Drawer 壳，AgentWorkspaceView 309 行 mock 全删重写为页宿主 — UX-01「功能一致」由共享状态同构保证（17-UI-SPEC Surface 1，含 chatConsoleStore 归属决策）。

Purpose: Agent 成为一等入口的骨架；后续 UX-02/03/04 全部挂在这副骨架上。
Output: chatConsoleStore.ts、AgentConsole.tsx 新建；ChatPanel.tsx、AgentWorkspaceView.tsx 重写。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-agent-ux/17-UI-SPEC.md

@src/components/ChatPanel.tsx
@src/views/AgentWorkspaceView.tsx

<interfaces>
<!-- 抽取源 ChatPanel.tsx 依赖的既有契约（执行者直接使用，无需探索） -->

From src/ai (index.ts barrel):
```typescript
runToolLoop(opts: {
  userMessage: string;
  provider: Provider;
  session: ChatSession;
  callbacks: {
    onToken: (t: string) => void;
    onToolStart: (name: string) => void;
    onToolEnd: (name: string, result: unknown, error?: unknown) => void;
    onDestructiveConfirmationRequired: (c: DestructiveActionCandidate | null) => void;
  };
}): Promise<{ content: string; pendingConfirmation?: KnowledgeWriteCandidate; pendingDestructiveConfirmation?: DestructiveActionCandidate; truncated?: boolean }>;
executeTool(name: string, args: Record<string, unknown>): Promise<unknown>;
confirmKnowledgeWrite(token: string): Promise<KnowledgeWriteCandidate>;
rejectKnowledgeWrite(token: string): Promise<void>;
confirmDestructiveAction(token: string): Promise<DestructiveActionCandidate>;
rejectDestructiveAction(token: string): Promise<void>;
```

From src/ai/confirmations.ts:
```typescript
listPendingDeliverableDrafts(): Promise<DeliverableDraftCandidate[]>;
confirmDeliverableDraft(token: string): Promise<DeliverableDraftCandidate>;
rejectDeliverableDraft(token: string): Promise<void>;
```

From src/ai/chatSession.ts / sessionRestore.ts / memoryStore.ts:
```typescript
new ChatSession({ tokenBudget: 8_000 });
session.appendAuxEvent(type: string, payload: object): void;
session.flushEvents(): Promise<void>;
session.getAllMessages(): Array<{ role: string; content: string; toolCallId?: string }>;
restoreLatestSession(): Promise<null | {
  session: ChatSession;
  pendingKnowledgeWrites: KnowledgeWriteCandidate[];
  pendingDestructiveActions: DestructiveActionCandidate[];
}>;  // 模块级 promise dedupe（Phase 14），双 mount 安全
getMemoryStore(): { listPending(): Promise<MemoryCandidate[]>; listRecentUserDirected(n: number): Promise<MemoryCandidate[]>; confirm(token): Promise<void>; reject(token): Promise<void>; consumeIntoMemories(token): Promise<void> };
```

From src/components/ui/Drawer.tsx: Drawer, DrawerContent (width prop, onOpenAutoFocus), DrawerHeader (title, description)。DrawerBody/DrawerFooter 在 ChatPanel 壳中不再使用（AgentConsole 自带列 chrome）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chatConsoleStore — 会话状态唯一归属</name>
  <read_first>src/components/ChatPanel.tsx, .planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 1 DECISION 段), src/stores/uiStore.ts, src/ai/sessionRestore.ts</read_first>
  <files>src/stores/chatConsoleStore.ts</files>
  <action>
创建 transient zustand store（`create<ChatConsoleState>()(...)`，**不用 persist** — 与 modal flags 同类）。把 ChatPanel.tsx :99-122 的全部组件状态与 :124-492 的全部 handler 原样搬入 store（逻辑零改动，仅改 setState 形态）：

```typescript
// 状态（ChatPanel :99-115 全部）
messages: ChatMessage[]; input: string;
streamingResponse: string; streamingTrace: ToolTraceItem[];
loading: boolean; restoreComplete: boolean;
pendingConfirmation: KnowledgeWriteCandidate | null;
pendingDestructiveAction: DestructiveActionCandidate | null;
pendingMemory: MemoryCandidate | null;
autoRemembered: MemoryCandidate | null;
memoryBusy: boolean;
pendingPrdDraft: DeliverableDraftCandidate | null;
prdDraftSnapshot: DeliverableDraftCandidate | null;
prdBusy: boolean; prdDialogOpen: boolean;
// 动作
setInput(v: string);
restore(): Promise<void>;                 // 幂等：模块级 restorePromise 缓存
submit(): Promise<void>;                  // provider 从 useUIStore.getState().activeAIProvider 读取
confirmKnowledgeWrite(); rejectKnowledgeWrite();
confirmDestructiveAction(); rejectDestructiveAction();
confirmMemory(); rejectMemory();
rejectDraft(); commitToSlot(draft: string);
openPrdDialog();                          // = setPrdDraftSnapshot(pendingPrdDraft) + setPrdDialogOpen(true)
setPrdDialogOpen(open: boolean);          // false 时同时清 prdDraftSnapshot（现 :591-594 语义）
dismissAutoRemembered();
refreshMemoryCards(); refreshPrdCard();  // 供 onToolEnd 回调（现 :254-255）
```

类型 `ChatMessage` / `ToolTraceItem`（ChatPanel :43-56）与 `PROVIDER_LABELS`（:58-64）、`formatMemoryTime`（:66-68）一并搬入并 export，AgentConsole 从这里 import。

模块级（store 文件顶部）：
- `export const sessionRef = { current: new ChatSession({ tokenBudget: 8_000 }) }`（17-UI-SPEC 锁定：sessionRef 模块级）
- `let nextId = 0` 计数器 + streamingResponse/streamingTrace 的模块级 ref 镜像（现 :120-122 语义，submit/trace 更新内同步）
- `let restorePromise: Promise<void> | null = null` — `restore()` 若已存在直接返回，否则调用 `restoreLatestSession()` 并在 .then 内完成 history 投影 / pending 恢复 / refreshMemoryCards / refreshPrdCard / setRestoreComplete(true)，.catch 同 ChatPanel :192-195（console.error + restoreComplete=true）。StrictMode 双 mount 安全。
- Toast 桥：`let toastSink: ((t: { type: 'success' | 'error' | 'warning'; title: string; description?: string }) => void) | null = null; export function bindToast(fn) { toastSink = fn; }`，store 内 `emitToast(...)` 未绑定时 no-op（Node 测试无 React）。原 handler 中所有 `toast({...})` 调用改走 emitToast，文案逐字保留。

submit() 逐字保留 ChatPanel :209-300 的守卫（`!restoreComplete || !trimmed || loading` return）、runToolLoop callbacks（onToken/onToolStart/onToolEnd 含 proposeMemory/generateDeliverable 刷新钩子）、truncated warning toast、catch error toast、finally 复位。commitToSlot 逐字保留 :443-485（含 deliverable_committed audit event 与 ponytail 注释）。
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; grep -c "export const useChatConsoleStore\|export const sessionRef\|export function bindToast" src/stores/chatConsoleStore.ts</automated>
  </verify>
  <acceptance_criteria>
- src/stores/chatConsoleStore.ts 存在，grep `useChatConsoleStore` 命中 create 定义
- grep `restoreLatestSession` 命中（恢复逻辑在 store 内）
- grep `runToolLoop` 命中（submit 在 store 内）
- grep `deliverable_committed` 命中（commitToSlot 审计事件保留）
- grep `persist` 零命中（transient store，不落盘）
- npm run lint 通过
  </acceptance_criteria>
  <done>会话全部状态与动作在 chatConsoleStore，无 persist，lint 通过</done>
</task>

<task type="auto">
  <name>Task 2: AgentConsole 组件 + ChatPanel 退化为 Drawer 壳</name>
  <read_first>src/components/ChatPanel.tsx, src/stores/chatConsoleStore.ts, src/components/ui/Drawer.tsx, .planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 1)</read_first>
  <files>src/components/AgentConsole.tsx, src/components/ChatPanel.tsx</files>
  <action>
新建 `src/components/AgentConsole.tsx`，签名 `export function AgentConsole({ layout = 'drawer' }: { layout?: 'drawer' | 'page' })`：

- 列 chrome（替换 DrawerBody/DrawerFooter，17-UI-SPEC 锁定值）：主体 `<div className="flex-1 overflow-y-auto px-5 pb-4 space-y-4">`，输入区 `<div className="border-t border-border-subtle px-5 py-4">`（原 DrawerFooter `className="block"` 的 form + Enter hint 逐字迁入）。
- 内部 JSX 从 ChatPanel :510-658 **逐字迁移**（空态 Sparkle hero、messages.map 气泡、流式气泡、知识库写入卡、删除确认卡、PRD 草稿卡、PrdDraftDialog、pendingMemory/autoRemembered 卡、bottomRef、form/textarea/发送按钮、`Enter 发送，Shift + Enter 换行`）— 零视觉改动。
- 状态全部来自 `useChatConsoleStore` selectors；handler 全部调 store 动作。textareaRef/bottomRef 留在组件（DOM ref）。
- 挂载时：`bindToast(toast)`（useToast）+ `void useChatConsoleStore.getState().restore()`（useEffect []，幂等）+ `textareaRef.current?.focus()`（page 宿主 mount 即聚焦；drawer 宿主由 DrawerContent onOpenAutoFocus 聚焦）。
- 滚动 effect（原 :124-126）留在组件，依赖 store 字段。
- `layout` 仅影响容器：page 宿主由外层负责宽度；组件本身两种 layout DOM 相同（气泡 max-w-[88%] 不变）。

重写 `src/components/ChatPanel.tsx` 为薄 Drawer 壳（17-UI-SPEC Surface 1 JSX 逐字）：

```tsx
export function ChatPanel() {
  const isOpen = useUIStore((state) => state.isChatPanelOpen);
  const setOpen = useUIStore((state) => state.setChatPanelOpen);
  const provider = useUIStore((state) => state.activeAIProvider);
  const textareaFocus = (event: Event) => { event.preventDefault(); /* AgentConsole 内 textarea 由其自身 mount focus 处理；保留 preventDefault 防焦点抢夺 */ };
  return (
    <Drawer open={isOpen} onOpenChange={setOpen}>
      <DrawerContent width={480} className="max-w-[100vw]" onOpenAutoFocus={textareaFocus}>
        <DrawerHeader title="AI 助手" description={`当前 provider：${PROVIDER_LABELS[provider]}`} />
        <AgentConsole />
      </DrawerContent>
    </Drawer>
  );
}
```

（PROVIDER_LABELS 从 chatConsoleStore import。）Drawer 打开时聚焦 textarea：AgentConsole 内 `useEffect` 监听 `useUIStore((s) => s.isChatPanelOpen)`，true 时 focus。
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
- src/components/AgentConsole.tsx 存在；grep `layout` 与 `今天需要处理什么？` 命中；grep `Enter 发送，Shift + Enter 换行` 命中
- grep `flex-1 overflow-y-auto px-5 pb-4 space-y-4` 命中（列 chrome 值逐字）
- ChatPanel.tsx 中 grep `handleSubmit\|runToolLoop\|useState` 零命中（纯壳），grep `AgentConsole` 命中，grep `AI 助手` 命中
- AgentConsole.tsx 中 grep `useChatConsoleStore` 命中且 grep `useState<ChatMessage` 零命中
- npm run lint 与 npm test 全绿
  </acceptance_criteria>
  <done>AgentConsole 承载全部会话 UI；ChatPanel 仅剩 Drawer 壳 + provider 标题</done>
</task>

<task type="auto">
  <name>Task 3: AgentWorkspaceView 重写为页宿主</name>
  <read_first>src/views/AgentWorkspaceView.tsx, .planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 1 page host JSX)</read_first>
  <files>src/views/AgentWorkspaceView.tsx</files>
  <action>
**删除全部 309 行 mock**（锁定决策：mock 深度耦合 useApp 假动作，清理成本高于重写），整文件重写为 17-UI-SPEC Surface 1 页宿主 JSX（预留晨报位，17-03 会填）：

```tsx
import { Card } from '@/src/components/ui';
import { AgentConsole } from '@/src/components/AgentConsole';

export function AgentWorkspaceView() {
  return (
    <div className="mx-auto flex h-[calc(100dvh-var(--titlebar-h)-var(--header-h)-48px)] w-full max-w-3xl flex-col gap-4">
      {/* MorningReport 由 17-03 插入此处 */}
      <Card variant="glass" className="flex min-h-0 flex-1 flex-col overflow-hidden">
        <AgentConsole layout="page" />
      </Card>
    </div>
  );
}
```

高度公式沿用现 mock :98 的 `h-[calc(100dvh-var(--titlebar-h)-var(--header-h)-48px)]`（只保留高度数学，其余删除）。保持具名导出 `AgentWorkspaceView`（App.tsx lazy 引用名不变）。不渲染 provider 标签（17-UI-SPEC 锁定：page 宿主无 provider chrome）。
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; test $(grep -c "max-w-3xl" src/views/AgentWorkspaceView.tsx) -ge 1 &amp;&amp; ! grep -q "useApp" src/views/AgentWorkspaceView.tsx</automated>
  </verify>
  <acceptance_criteria>
- AgentWorkspaceView.tsx 行数 < 30（mock 全删）
- grep `max-w-3xl`、`variant="glass"`、`AgentConsole layout="page"`、`h-[calc(100dvh-var(--titlebar-h)-var(--header-h)-48px)]` 均命中
- grep `useApp\|setTimeout\|Sparkle` 零命中（mock 动作全消失；Sparkle 属空态，已在 AgentConsole 内）
- npm run lint 通过
  </acceptance_criteria>
  <done>agent 标签页呈现居中 max-w-3xl 玻璃卡内的真实会话控制台，与 Drawer 同一场对话</done>
</task>

</tasks>

<verification>
- npm run lint 通过；npm test 全绿（既有 80+ 测试无回归）
- ChatPanel.tsx < 60 行；AgentWorkspaceView.tsx < 30 行；AgentConsole.tsx 承载原 :509-659 全部 JSX
- 双宿主同构：grep 确认两宿主均渲染 AgentConsole，会话状态仅存在于 chatConsoleStore
</verification>

<success_criteria>
UX-01 达成：AgentWorkspaceView 真实可用，与 ChatPanel 功能一致（共享 store 同构），多 agent 形态未引入。
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-ux/17-01-SUMMARY.md`
</output>
