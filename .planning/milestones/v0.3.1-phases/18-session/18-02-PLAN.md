---
phase: 18-session
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/ai/toolLoop.ts
  - src/ai/confirmations.ts
  - src/ai/sessionRepo.ts
  - src/ai/__tests__/phase18SessionRepo.test.ts
autonomous: true
requirements: [SESS-06, SESS-05]
must_haves:
  truths:
    - "Every new agent_event is stamped with the current activeWorkspaceId at write time"
    - "Every confirmation candidate row carries the ambient sessionId (no more sessionId:null)"
    - "New sessions get a sessions row via upsertSessionMeta at turn start"
    - "sessionRepo lists sessions by workspace for the app layer"
  artifacts:
    - path: "src/ai/sessionRepo.ts"
      provides: "MemorySessionRepo/SqliteSessionRepo behind getSessionRepo()"
      min_lines: 50
  key_links:
    - from: "src/ai/toolLoop.ts"
      to: "src/stores/workspaceStore.ts"
      via: "setEventScopeProvider reading useWorkspaceStore.getState().activeWorkspaceId"
      pattern: "activeWorkspaceId"
    - from: "src/ai/confirmations.ts"
      to: "src/ai/agentScope.ts"
      via: "getActiveAgentScope()?.sessionId read inside candidate factories"
      pattern: "getActiveAgentScope"
---

<objective>
写入时 stamping 修复 + sessionRepo:toolLoop scope provider 写真实 workspaceId,confirmations 候选带 sessionId,新 session 在 turn 开始时 upsert 元数据行。

Purpose: SESS-06 写入侧 + SESS-05 数据层缺口(sessionId:null 修复);Phase 19/21 在此之上接线。
Output: 修改后的 toolLoop.ts/confirmations.ts,新 sessionRepo.ts + 测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session/18-RESEARCH.md
@src/ai/events/eventStore.ts
@src/ai/agentScope.ts
@src/ai/confirmations.ts

<interfaces>
From src/ai/agentScope.ts:
```typescript
// setActiveAgentScope set by toolLoop each turn; scope carries sessionId
export function getActiveAgentScope(): { sessionId: string; /* ... */ } | null;
```

From src/ai/events/eventStore.ts:258-274 — dual memory/sqlite singleton pattern:
```typescript
// getEventStore() returns Memory-impl in Node tests, Sqlite-impl under Tauri via isTauri()
```

Scope provider contract (src/ai/toolLoop.ts:39):
```typescript
setEventScopeProvider(() => ({ workspaceId: string | null, productId: string | null, projectId: string | null }));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: toolLoop workspaceId stamping + confirmations sessionId 修复</name>
  <files>src/ai/toolLoop.ts, src/ai/confirmations.ts</files>
  <read_first>
    - src/ai/toolLoop.ts:37-43 (scope provider to replace)
    - src/ai/confirmations.ts:130-230 (both `sessionId: null` sites at ~152 and ~217, and computeParamsHash at ~321)
    - src/ai/agentScope.ts (getActiveAgentScope signature)
    - src/stores/workspaceStore.ts (activeWorkspaceId field)
  </read_first>
  <action>
    1. toolLoop.ts: replace the provider body:
    ```typescript
    setEventScopeProvider(() => ({
      workspaceId: useWorkspaceStore.getState().activeWorkspaceId,
      productId: useUIStore.getState().selectedProductId,
      projectId: null,
    }));
    ```
    Add `import { useWorkspaceStore } from '@/src/stores/workspaceStore';` and update the adjacent comment (workspace linkage no longer deferred; SESS-06).
    2. confirmations.ts: in `createKnowledgeWriteCandidate` and `createDestructiveActionCandidate`, change `sessionId: null` to `sessionId: getActiveAgentScope()?.sessionId ?? null` (import getActiveAgentScope from './agentScope'). Direct read inside the factories covers all callers at once (root-cause fix per research).
    3. CRITICAL — do NOT add sessionId to computeParamsHash inputs (Pitfall 6: would break dedup of pre-upgrade pending candidates). Verify hash function untouched.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/toolLoop.ts contains `useWorkspaceStore.getState().activeWorkspaceId` inside setEventScopeProvider
    - src/ai/confirmations.ts contains zero occurrences of `sessionId: null` in the two candidate factories (grep shows `getActiveAgentScope()?.sessionId ?? null` twice)
    - computeParamsHash unchanged (no sessionId in hash inputs)
    - npm test exits 0; npm run lint (tsc --noEmit) exits 0
  </acceptance_criteria>
  <done>All new events carry workspaceId; all new candidates carry sessionId; no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sessionRepo.ts + turn-start upsert 接线</name>
  <files>src/ai/sessionRepo.ts, src/ai/__tests__/phase18SessionRepo.test.ts, src/ai/toolLoop.ts</files>
  <read_first>
    - src/ai/events/eventStore.ts (dual repo singleton pattern to mirror, incl. isTauri gate and SQL row mapping)
    - src-tauri/migrations/0007_sessions.sql (sessions column list — row mapping must be column-complete)
    - src/ai/__tests__/phase13EventLog.test.ts (memory-repo test style)
    - src/ai/toolLoop.ts (runToolLoop entry — where turn starts, where getActiveAgentScope is set)
  </read_first>
  <behavior>
    - Test 1: `upsertSessionMeta({sessionId, workspaceId})` inserts created_at/last_active_at=now on first call; second call updates last_active_at only, never overwrites title/workspace (INSERT OR IGNORE semantics in memory impl).
    - Test 2: `updateTitle(sessionId, t)` sets title, leaves other rows untouched.
    - Test 3: `listSessionsByWorkspace('ws-1')` returns only ws-1 sessions ordered by last_active_at DESC; NULL-workspace sessions visible under any workspaceId query (documented NULL=all behavior).
    - Test 4 (SQL parity): run real 0001+0007 SQL via node:sqlite, exec SqliteSessionRepo SQL statements (or its SQL strings) against it, assert same behaviors as memory tests (no DDL drift — Pitfall 4).
  </behavior>
  <action>
    Create `src/ai/sessionRepo.ts` (~80-120 LOC) mirroring eventStore.ts:
    - `export interface SessionMeta { sessionId: string; workspaceId: string | null; title: string | null; titleSource: string | null; parentSessionId: string | null; forkCutSeq: number | null; createdAt: string; lastActiveAt: string; }`
    - `MemorySessionRepo` (Map-based) and `SqliteSessionRepo` (INSERT OR IGNORE ... ON CONFLICT update last_active_at only — SQLite `INSERT INTO sessions(...) VALUES(...) ON CONFLICT(session_id) DO UPDATE SET last_active_at=excluded.last_active_at`) with methods `upsertSessionMeta(input: { sessionId: string; workspaceId: string | null }): void|Promise<void>`, `listSessionsByWorkspace(workspaceId: string | null): SessionMeta[]|Promise<SessionMeta[]>`, `updateTitle(sessionId: string, title: string): void|Promise<void>`.
    - `getSessionRepo()` singleton with the same `isTauri()` gate as eventStore.ts:258-274. File header comment documents: NULL workspace_id = visible in all workspaces.
    - Wire in toolLoop.ts: at the start of each turn (where setActiveAgentScope is called), call `getSessionRepo().upsertSessionMeta({ sessionId: scope.sessionId, workspaceId: useWorkspaceStore.getState().activeWorkspaceId })` — fire-and-forget with .catch(() => {}) so repo failure never blocks a turn. This guarantees every new session gets a row even before Phase 19 rewires it (per research Open Question 1 recommendation).
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase18SessionRepo.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/sessionRepo.ts exports SessionMeta, MemorySessionRepo, SqliteSessionRepo, getSessionRepo
    - SqliteSessionRepo upsert SQL uses `ON CONFLICT(session_id) DO UPDATE SET last_active_at` (title/workspace never overwritten)
    - sessionRepo.test.ts executes real 0007 SQL via node:sqlite (contains `0007_sessions.sql`)
    - toolLoop.ts contains `upsertSessionMeta` call
    - Both commands exit 0; full `npm test` exits 0
  </acceptance_criteria>
  <done>sessionRepo shipped with memory/sqlite parity tests; every turn start persists session metadata.</done>
</task>

</tasks>

<verification>
- npm test full suite passes
- npm run lint passes
</verification>

<success_criteria>
- New events stamped with workspaceId; candidates stamped with sessionId; sessions table maintained at runtime via upsert; repo testable in Node (memory) and against real 0007 SQL (sqlite)
</success_criteria>

<output>
After completion, create `.planning/phases/18-session/18-02-SUMMARY.md`
</output>
