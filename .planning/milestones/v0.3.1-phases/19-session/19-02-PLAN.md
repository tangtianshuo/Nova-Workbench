---
phase: 19-session
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/stores/chatConsoleStore.ts
  - src/stores/workspaceStore.ts
  - src/stores/__tests__/phase19Runtime.test.ts
autonomous: true
requirements: [SESS-02, SESS-03, SESS-04]
must_haves:
  truths:
    - "用户进入应用即处于新 session(activeSessionId 为新 ChatSession 的 id),工作区为上次退出的 activeWorkspaceId"
    - "switchSession(sessionId) 后对话区显示该会话完整历史,与原会话逐字一致"
    - "streaming 中 switchSession 返回 { success: false, reason: 'streaming' } 且状态不变"
    - "streaming 中 setActiveWorkspaceId 被守卫拒绝,不产生跨会话事件串流"
    - "工作区切换成功 = 结束当前 session 进入新 session"
  artifacts:
    - path: "src/stores/chatConsoleStore.ts"
      provides: "activeSessionId + startNewSession + switchSession with streaming guard"
      contains: "switchSession"
    - path: "src/stores/workspaceStore.ts"
      provides: "guarded setActiveWorkspaceId"
      contains: "success: false"
    - path: "src/stores/__tests__/phase19Runtime.test.ts"
      provides: "lifecycle + guard tests"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "src/ai/sessionRestore.ts"
      via: "restoreSession(sessionId)"
      pattern: "restoreSession\\("
    - from: "src/stores/workspaceStore.ts"
      to: "src/stores/chatConsoleStore.ts"
      via: "useChatConsoleStore.getState().loading guard + startNewSession()"
      pattern: "useChatConsoleStore\\.getState"
---

<objective>
Wire the multi-session runtime into stores: activeSessionId state, startup default new session, switchSession lifecycle (restore + pending refresh), and streaming guards on session switch and workspace switch (UI disable is consumed later; store guard is the bottom line).

Purpose: SESS-02 (new session on entry), SESS-03 (switch restores history), SESS-04 (no cross-session streaming).
Output: chatConsoleStore + workspaceStore runtime changes with tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-session/19-CONTEXT.md
@.planning/phases/19-session/19-01-SUMMARY.md
@src/stores/chatConsoleStore.ts
@src/stores/workspaceStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: chatConsoleStore — activeSessionId, startNewSession, switchSession, startup default new session</name>
  <files>src/stores/chatConsoleStore.ts</files>
  <read_first>
    - src/stores/chatConsoleStore.ts (entire file — sessionRef L75, restorePromise L80, restore() L187-222, loading flag L239/L310)
    - src/ai/sessionRestore.ts (post-19-01: restoreSession(sessionId?), resetRestoreForTesting)
    - src/ai/chatSession.ts (ChatSession constructor / sessionId getter — find the exact accessor, e.g. session.sessionId)
    - src/ai/agentScope.ts (setActiveAgentScope / getActiveAgentScope)
  </read_first>
  <action>
    1. Add state: `activeSessionId: string` (initial value: `sessionRef.current.sessionId` — check ChatSession for the public sessionId accessor; if none, add one readonly getter). NOT persisted (store is transient; startup always new session per CONTEXT decision).
    2. Add `startNewSession: () => { success: boolean; reason?: string }`:
       - Guard: `if (get().loading) return { success: false, reason: 'streaming' };`
       - Replace `sessionRef.current = new ChatSession({ tokenBudget: 8_000 })`; reset `messages: []`, `pendingConfirmation: null`, `pendingDestructiveAction: null`, `pendingPrdDraft: null`, `pendingMemory: null`; `set({ activeSessionId: sessionRef.current.sessionId })`. Do NOT touch restoreComplete. Then `void refreshMemoryCards(); void refreshPrdCard();`.
    3. Add `switchSession: (sessionId: string) => Promise<{ success: boolean; reason?: string }>`:
       - Guard: `if (get().loading) return { success: false, reason: 'streaming' };`
       - `const restored = await restoreSession(sessionId);` If null → `return { success: false, reason: 'not_found' };`
       - Atomically: `sessionRef.current = restored.session;` set `activeSessionId`, rebuild `messages` using the EXACT same filter/map as restore() L194-201 (user + assistant without toolCallId, nextId++ ids, content verbatim), set `pendingConfirmation`/`pendingDestructiveAction` from restored arrays (last element, same as restore()), then `void refreshMemoryCards(); void refreshPrdCard();`.
    4. Startup default new session (SESS-02): change `restore()` so the app-start path does NOT auto-restore the latest session into the conversation. Keep the promise dedupe shape but body becomes: `set({ restoreComplete: true })` + refresh pending cards — i.e. entry lands in the fresh module-level ChatSession created at L75. (restoreSession() no-arg remains available in sessionRestore.ts as crash-recovery API for future paths; chatConsoleStore no longer calls restoreLatestSession.)
    5. Import `restoreSession` from '@/src/ai/sessionRestore' (drop restoreLatestSession import).
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/chatConsoleStore.ts` contains `activeSessionId: string` in state interface
    - contains `startNewSession` and `switchSession: (sessionId: string)`
    - `switchSession` body contains `if (get().loading) return { success: false, reason: 'streaming' }` (or equivalent early-return with reason 'streaming')
    - `switchSession` calls `restoreSession(sessionId)`
    - no import of `restoreLatestSession` remains
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Runtime lifecycle in place; entry is a fresh session; switching restores verbatim history; streaming blocked at store level.</done>
</task>

<task type="auto">
  <name>Task 2: workspaceStore — guarded setActiveWorkspaceId ending current session</name>
  <files>src/stores/workspaceStore.ts</files>
  <read_first>
    - src/stores/workspaceStore.ts (setActiveWorkspaceId L139, persist config L133/L199-213 — do not break persist partialize)
    - src/stores/chatConsoleStore.ts (post-Task-1: startNewSession signature)
  </read_first>
  <action>
    Change `setActiveWorkspaceId` signature to `(id: string) => { success: boolean; reason?: string }` (update interface at L115 accordingly):
    1. Guard: `if (id === get().activeWorkspaceId) return { success: true };`
    2. Guard: `const chat = useChatConsoleStore.getState(); if (chat.loading) return { success: false, reason: 'streaming' };` — import `useChatConsoleStore` from './chatConsoleStore' (no circular import: chatConsoleStore does not import workspaceStore; verify before committing).
    3. On pass: `set({ activeWorkspaceId: id })` then `const r = useChatConsoleStore.getState().startNewSession(); return r;` (workspace switch = end current session, enter new session — CONTEXT locked decision).
    Any existing UI callers of setActiveWorkspaceId (currently none in views) are unaffected since the return value is ignorable; TypeScript void-returns still compile.
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/workspaceStore.ts` interface declares `setActiveWorkspaceId: (id: string) => { success: boolean; reason?: string }`
    - body contains `useChatConsoleStore.getState()` guard returning `reason: 'streaming'`
    - body calls `startNewSession()` after `set({ activeWorkspaceId: id })`
    - persist partialize untouched (still contains `activeWorkspaceId: s.activeWorkspaceId`)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Workspace switch is store-guarded and session-ending; activeWorkspaceId persistence intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: runtime lifecycle + guard tests</name>
  <files>src/stores/__tests__/phase19Runtime.test.ts</files>
  <read_first>
    - src/stores/__tests__/ (any existing store test for setup patterns)
    - src/ai/__tests__/phase19SessionSwitch.test.ts (DB fixture pattern from 19-01)
    - src/stores/chatConsoleStore.ts and src/stores/workspaceStore.ts (post-Task-1/2 state)
  </read_first>
  <behavior>
    - Test 1 (SESS-02 startup): fresh store — restore() resolves with restoreComplete true and messages stays [] and activeSessionId equals sessionRef.current.sessionId (no latest-session auto-restore).
    - Test 2 (SESS-03 switch): seed DB with a completed session (2 turns); switchSession(id) → messages match the persisted turn contents verbatim; activeSessionId === id; sessionRef.current.sessionId === id.
    - Test 3 (SESS-04 guard): set loading true (simulate via store.setState({ loading: true })); switchSession(id) returns { success: false, reason: 'streaming' } and messages/activeSessionId unchanged.
    - Test 4 (SESS-04 workspace guard): with chat loading true, setActiveWorkspaceId('other') returns { success: false, reason: 'streaming' } and activeWorkspaceId unchanged.
    - Test 5 (workspace switch ends session): with loading false and a restored session active, setActiveWorkspaceId('other') returns { success: true }, activeWorkspaceId === 'other', and chat messages === [] with a NEW activeSessionId (≠ previous).
    - Test 6 (unknown session): switchSession('nope') returns { success: false, reason: 'not_found' }.
  </behavior>
  <action>
    Write tests first against the Task 1/2 API; use node:sqlite temp DB fixture like phase19SessionSwitch.test.ts; reset module state between tests (resetRestoreForTesting, fresh stores). Use zustand's setState on the store for loading simulation.
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npx tsx --test src/stores/__tests__/phase19Runtime.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/__tests__/phase19Runtime.test.ts` exists with 6 tests covering startup-new-session, switch-verbatim, switch-guard, workspace-guard, workspace-switch-ends-session, not-found
    - test file exits 0; `npm test` full suite exits 0
  </acceptance_criteria>
  <done>All 6 runtime tests green; full suite green.</done>
</task>

</tasks>

<verification>
npm test green; store-level streaming guards reject session and workspace switches with observable { success: false, reason }.
</verification>

<success_criteria>
- SESS-02: app entry = fresh session + persisted workspace.
- SESS-03: switchSession restores verbatim history via 19-01 engine.
- SESS-04: dual guards (store bottom line; UI disable consumed by later surfaces).
</success_criteria>

<output>
After completion, create `.planning/phases/19-session/19-02-SUMMARY.md`
</output>
