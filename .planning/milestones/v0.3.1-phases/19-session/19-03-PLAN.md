---
phase: 19-session
plan: "03"
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/ai/confirmations.ts
  - src/ai/memoryStore.ts
  - src/stores/chatConsoleStore.ts
  - src/ai/__tests__/phase14Confirmations.test.ts
autonomous: true
requirements: [SESS-05]
must_haves:
  truths:
    - "pending 知识写入/删除确认/PRD 草稿/记忆候选只出现在其所属 session 的对话区,不跨会话串卡"
    - "切换 session 后 pending 卡片按新 activeSessionId 刷新"
  artifacts:
    - path: "src/ai/confirmations.ts"
      provides: "sessionId-scoped listPending* + candidate sessionId exposure"
      contains: "sessionId?: string | null"
    - path: "src/ai/memoryStore.ts"
      provides: "sessionId-scoped listPending"
    - path: "src/stores/chatConsoleStore.ts"
      provides: "session-filtered pending card refresh"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "src/ai/confirmations.ts"
      via: "listPending*(get().activeSessionId)"
      pattern: "listPendingDeliverableDrafts\\(get\\(\\)\\.activeSessionId"
    - from: "src/stores/chatConsoleStore.ts"
      to: "src/ai/memoryStore.ts"
      via: "store.listPending(activeSessionId)"
      pattern: "listPending\\(get\\(\\)\\.activeSessionId"
---

<objective>
Filter every pending-card read path by session: listPendingKnowledgeWrites / listPendingDestructiveActions / listPendingDeliverableDrafts gain an optional sessionId filter; memory store listPending gains the same; chatConsoleStore passes activeSessionId on every refresh. Also stop candidateFromRow/destructiveFromRow from dropping the row's sessionId.

Purpose: SESS-05 — pending cards never bleed across sessions.
Output: session-scoped pending reads + refreshed cards on switch.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-session/19-CONTEXT.md
@.planning/phases/19-session/19-02-SUMMARY.md
@src/ai/confirmations.ts
@src/ai/memoryStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: session-scoped listPending* + candidate sessionId exposure</name>
  <files>src/ai/confirmations.ts</files>
  <read_first>
    - src/ai/confirmations.ts (candidateFromRow L112, destructiveFromRow L116, listPendingKnowledgeWrites L279, listPendingDestructiveActions L284, listPendingDeliverableDrafts L418, KnowledgeWriteCandidate L29, DestructiveActionCandidate L54, DeliverableDraftCandidate L293 — note the last already has sessionId)
    - the confirmation store's listActive implementation (grep `listActive` — find where rows live and whether sessionId is on the row; row.sessionId is stamped at insert per L153/L218 and surfaced at L349)
  </read_first>
  <action>
    1. Types: add `sessionId: string | null` to `KnowledgeWriteCandidate` (extends KnowledgeWriteDraft) and to `DestructiveActionCandidate`.
    2. `candidateFromRow`: include `sessionId: row.sessionId ?? null`. `destructiveFromRow`: same. (Row data already carries sessionId from Phase 18 stamping — this stops discarding it.) If `PersistedConfirmation` lacks a sessionId field in its type, add it and confirm the underlying row/select includes it (Phase 18 stamped at insert; check the SQL columns).
    3. Filters: change signatures to `listPendingKnowledgeWrites(sessionId?: string)`, `listPendingDestructiveActions(sessionId?: string)`, `listPendingDeliverableDrafts(sessionId?: string)`. No arg = current behavior (all pending — backward compat for sessionRestore.ts's restore path, which lists across sessions). With arg: filter rows to `row.sessionId === sessionId` AFTER listActive (JS filter is fine — pending sets are small; no SQL change needed).
    4. Deliverable draft mapping (L349 area) already sets sessionId — leave as-is.
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint && npx tsx --test src/ai/__tests__/phase14Confirmations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/confirmations.ts` declares `listPendingKnowledgeWrites(sessionId?: string)`, `listPendingDestructiveActions(sessionId?: string)`, `listPendingDeliverableDrafts(sessionId?: string)`
    - `KnowledgeWriteCandidate` and `DestructiveActionCandidate` contain `sessionId: string | null`
    - `candidateFromRow` and `destructiveFromRow` both contain `row.sessionId`
    - existing phase14Confirmations tests pass (no-arg compat); `npm run lint` exits 0
  </acceptance_criteria>
  <done>Pending reads filterable by session; candidates expose sessionId; no-arg callers unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: memoryStore listPending sessionId filter</name>
  <files>src/ai/memoryStore.ts</files>
  <read_first>
    - src/ai/memoryStore.ts (MemoryCandidate sessionId field L31, listPending interface L107 + impls L295 and L641 — there are TWO implementations, sqlite and fallback; row mapping L460 already reads session_id)
  </read_first>
  <action>
    1. Interface + both implementations: `listPending(sessionId?: string): Promise<MemoryCandidate[]>`. No arg = current behavior. With arg: filter `r.sessionId === sessionId` (JS filter after existing status/expiry filter — small sets, no SQL change).
    2. Do not change listRecentUserDirected (已记住 info card is informational, not a per-session confirmation card — keep global).
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/memoryStore.ts` interface declares `listPending(sessionId?: string)`
    - both listPending implementations (grep `async listPending` — 2 occurrences) accept and apply sessionId
    - `listRecentUserDirected` signature unchanged
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Memory pending candidates filterable by session; both impls consistent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: wire activeSessionId into card refresh + filter tests</name>
  <files>src/stores/chatConsoleStore.ts, src/ai/__tests__/phase14Confirmations.test.ts</files>
  <read_first>
    - src/stores/chatConsoleStore.ts (refreshMemoryCards L132, refreshPrdCard L155, restore/switchSession from 19-02)
    - src/ai/__tests__/phase14Confirmations.test.ts (existing patterns to extend)
  </read_first>
  <behavior>
    - Test 1: insert two pending knowledge_write candidates with different sessionIds; listPendingKnowledgeWrites(sessA) returns only sessA's candidate.
    - Test 2: same isolation for listPendingDestructiveActions and listPendingDeliverableDrafts.
    - Test 3: candidate returned by listPending carries its sessionId (non-null when stamped).
    - Test 4: memory store listPending(sessA) filters by session across two candidates.
    - Test 5 (store level): with activeSessionId = A and a pending candidate for session B in the store, refreshMemoryCards/refreshPrdCard leave pendingMemory/pendingPrdDraft null (no cross-session card).
  </behavior>
  <action>
    1. In chatConsoleStore: `refreshMemoryCards` uses `store.listPending(get().activeSessionId)`; `refreshPrdCard` uses `listPendingDeliverableDrafts(get().activeSessionId)`. In restore()/switchSession(): the pendingKnowledgeWrites/pendingDestructiveActions sourced from RestoredSession must ALSO be filtered to the restored sessionId — change sessionRestore.ts call sites OR filter in store: filter the restored arrays by `restored.sessionId` in chatConsoleStore before taking the last element (one-line, keeps 19-01 untouched: `restored.pendingKnowledgeWrites.filter(c => c.sessionId === restored.sessionId)`).
    2. Add the 5 tests above (extend phase14Confirmations.test.ts for 1-4; add Test 5 in src/stores/__tests__/phase19Runtime.test.ts if a store fixture exists there, else as a section in phase14Confirmations.test.ts).
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/chatConsoleStore.ts` contains `listPending(get().activeSessionId)` and `listPendingDeliverableDrafts(get().activeSessionId)`
    - filtered restore path present (grep `filter` near pendingKnowledgeWrites)
    - 5 new tests present and passing; `npm test` full suite exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Every pending card read path is session-scoped; switch/startNew refresh leaves zero cross-session cards.</done>
</task>

</tasks>

<verification>
npm test green. Pending cards (knowledge write, destructive, memory, PRD draft) visible only in their own session after restore/switch.
</verification>

<success_criteria>
- SESS-05 complete: pending cards session-filtered across all four card types, refreshed on every session transition.
</success_criteria>

<output>
After completion, create `.planning/phases/19-session/19-03-SUMMARY.md`
</output>
