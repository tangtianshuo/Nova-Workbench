---
phase: 20-fork
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ai/__tests__/fork.test.ts
  - src/ai/fork.ts
  - src/ai/sessionRepo.ts
  - src/ai/sessionRestore.ts
  - src/ai/compaction.ts
autonomous: true
requirements: [FORK-02, LIST-03]
must_haves:
  truths:
    - "buildForkEventStream 前缀 seq 1..cut 恒等、子事件 +cut 偏移,输出连续 1..N 流"
    - "mid-turn / 无 turn_ended / 前缀配对不干净 的切点被拒绝并返回类型化 invalid reason"
    - "子事件 compaction 载荷 coveredSeqStart/End/splitSeq 按 +prefix.length remap,前缀事件恒等"
    - "resolveSessionEvents 递归解析 fork-of-fork;sessionRestore 与 compaction 两处 listEvents 接线完成"
    - "sessionRepo 可 createForkSession(写 parent_session_id/fork_cut_seq)且 listSessionsByWorkspace 返回 parentTitle"
  artifacts:
    - path: "src/ai/__tests__/fork.test.ts"
      provides: "fork 纯函数全套测试(先于实现)"
      contains: "buildForkEventStream"
    - path: "src/ai/fork.ts"
      provides: "buildForkEventStream / findForkCutSeq / resolveSessionEvents"
      contains: "export function buildForkEventStream"
    - path: "src/ai/sessionRepo.ts"
      provides: "createForkSession / getSession / parentTitle enrichment"
      contains: "createForkSession"
  key_links:
    - from: "src/ai/sessionRestore.ts"
      to: "src/ai/fork.ts"
      via: "resolveSessionEvents 替换 listEvents"
      pattern: "resolveSessionEvents\\("
    - from: "src/ai/compaction.ts"
      to: "src/ai/fork.ts"
      via: "maybeCompactSession 用 resolveSessionEvents"
      pattern: "resolveSessionEvents\\("
    - from: "src/ai/fork.ts"
      to: "sessionRepo"
      via: "get(sessionId) 读 parentSessionId/forkCutSeq"
      pattern: "getSessionRepo|parentSessionId"
---

<objective>
TDD 交付 fork 纯函数层(ROADMAP 成功标准 5:测试先行)。先写 src/ai/__tests__/fork.test.ts(RED),再实现 src/ai/fork.ts(buildForkEventStream + findForkCutSeq + resolveSessionEvents)+ sessionRepo 扩展(createForkSession / getSession / parentTitle)+ sessionRestore.ts:96 与 compaction.ts:130 两处接线(GREEN)。零父事件复制(引用式锁定决策)。

Purpose: 最高风险纯逻辑(P-C)先于 UI 隔离交付,配对不变量/remap/parity 由测试锁死。
Output: fork.ts + fork.test.ts + repo 扩展 + 双接线。
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/20-fork/20-CONTEXT.md
@.planning/phases/20-fork/20-RESEARCH.md
@src/ai/events/invariants.ts
@src/ai/events/eventStore.ts
@src/ai/sessionRepo.ts
@src/ai/sessionRestore.ts
@src/ai/compaction.ts
</context>

<interfaces>
<!-- 既有契约(执行者直接使用,无需探索) -->
src/ai/events/invariants.ts: checkEventStream(events): string[] — 五类违例码,空数组=干净
src/ai/events/eventStore.ts: store.listEvents(sessionId): Promise<AgentEvent[]>;store.append(...) seq 由 SQL MAX+1 分配
src/ai/sessionRepo.ts: listSessionsByWorkspace 已返回 parentSessionId/forkCutSeq;MemorySessionRepo 需镜像新方法
src/ai/sessionRestore.ts:96: `let events = await store.listEvents(targetSessionId)` ← 接线点
src/ai/compaction.ts:130: maybeCompactSession 内 listEvents ← 接线点
compaction 载荷含 seq 的仅三处: compaction_started.payload.splitSeq / compaction_completed.payload.coveredSeqStart / coveredSeqEnd
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RED — fork.test.ts 测试先行(实现不存在,必须失败)</name>
  <files>src/ai/__tests__/fork.test.ts</files>
  <read_first>
    - src/ai/events/types.ts (AgentEvent 字段: sessionId/seq/eventType/eventId/payload/correlationId)
    - src/ai/events/invariants.ts (checkEventStream 签名与违例码)
    - src/ai/__tests__/ 目录下任一既有测试(fake events 构造 helper 模式、node:test 风格)
    - src/ai/compaction.ts (compaction_started/completed payload 字段名)
  </read_first>
  <action>
    新建 src/ai/__tests__/fork.test.ts(node:test + 既有 fake events helper 模式),从 `../fork.ts` 导入 buildForkEventStream / findForkCutSeq / resolveSessionEvents(此时文件不存在,测试必须 RED)。用例按 20-RESEARCH "Core test cases" 全部落:
    1. 配对不变量: 多 tool turn 的 fork 输出流 → `checkEventStream(out.events)` 返回 `[]`
    2. seq 归一化: 前缀保留 1..cut,子事件 = cut+1..cut+M,连续无空洞
    3. compaction remap 两规则: 子事件 payload.splitSeq/coveredSeqStart/coveredSeqEnd === 原值 + prefix.length;前缀事件中的 compaction 载荷恒等不变
    4. replay parity: ChatSession.fromEvents(normalized) 的消息投影 === parent 投影截至 cut(逐字)
    5. mid-turn 拒绝: cutSeq 指向 tool_call seq → `{ events: [], invalid: 'MID_TURN' }`
    6. 无完整 turn 前缀 → `'NO_PREFIX_TURN'`;前缀含未配对 tool_call(checkEventStream 非空)→ `'UNBALANCED'`
    7. fork-of-fork: 子的 resolved 流作为孙的前缀再切,输出仍连续且 parity
    8. findForkCutSeq: 返回目标 assistant_message seq 之后第一个 turn_ended 的 seq;不存在 → null
    9. round-trip(内存 repo + 内存 eventStore): fork → 子追加一个 turn → 强制 compaction(maybeCompactSession)→ resolveSessionEvents → fromEvents → 投影含 summary + compaction 后 turn,父前缀未从上下文丢失
    10. repo: createForkSession 落 parent_session_id/fork_cut_seq;listSessionsByWorkspace 返回 parentTitle(LEFT JOIN 语义);MemorySessionRepo 镜像一致
    运行 `npx tsx --test src/ai/__tests__/fork.test.ts` 确认全部失败(模块不存在)后提交 RED。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/fork.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/__tests__/fork.test.ts 存在且 contains "buildForkEventStream"、"findForkCutSeq"、"resolveSessionEvents"、"MID_TURN"、"UNBALANCED"、"NO_PREFIX_TURN"、"createForkSession"、"parentTitle"
    - 覆盖上述 10 类用例(grep "fork-of-fork" 或等价命名、compaction round-trip 用例存在)
    - RED 阶段: 测试因 ../fork.ts 不存在而失败(退出码非 0)
  </acceptance_criteria>
  <done>测试套件落盘,RED 确认,已提交</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — fork.ts 实现 + sessionRepo 扩展 + restore/compaction 双接线</name>
  <files>src/ai/fork.ts, src/ai/sessionRepo.ts, src/ai/sessionRestore.ts, src/ai/compaction.ts</files>
  <behavior>
    - buildForkEventStream: 前缀 1..cut 恒等,子 +cut;三道 Gate(MID_TURN/NO_PREFIX_TURN/UNBALANCED);remap 仅动 splitSeq/coveredSeqStart/coveredSeqEnd
    - resolveSessionEvents: 无 parent 直接返回 own;有 parent 递归 resolve 再融合
    - createForkSession: INSERT 带 parent_session_id/fork_cut_seq;SQL 常量与 MemorySessionRepo 双实现
    - sessionRestore.ts:96 与 compaction.ts:130 的 listEvents 换成 resolveSessionEvents
  </behavior>
  <read_first>
    - src/ai/__tests__/fork.test.ts (Task 1 产物 — 实现目标)
    - src/ai/sessionRestore.ts 全文(96 行接线点 + orphan append 逻辑)
    - src/ai/compaction.ts 全文(130 行接线点 + applyCompactionResult 的 coveredSeq 写入空间)
    - src/ai/sessionRepo.ts 全文(SQL 常量导出模式 + MemorySessionRepo + Phase 18 parity 测试模式)
    - src/ai/events/invariants.ts (checkEventStream 直接复用,勿自写配对扫描)
    - src/ai/chatSession.ts fromEvents(coveredSeqEnd 过滤语义)
  </read_first>
  <action>
    1. src/ai/fork.ts(约 120 LOC),签名按 20-RESEARCH Pattern 1/2:
       - `export function buildForkEventStream(parentEvents: AgentEvent[], cutSeq: number, childEvents: AgentEvent[]): { events: AgentEvent[]; invalid?: 'MID_TURN' | 'NO_PREFIX_TURN' | 'UNBALANCED' }` — Gate1: cut 事件必须存在且 eventType === 'turn_ended';Gate2: 前缀含 ≥1 turn_ended;Gate3: `checkEventStream(prefix).length > 0` → UNBALANCED。归一化: prefix.forEach((e,i) => seq=i+1),child.forEach((e,i) => seq=prefix.length+i+1)。remap 两规则(勿写通用 map): 前缀事件中的 splitSeq/coveredSeqStart/coveredSeqEnd 恒等;子事件中的这三字段 += prefix.length。
       - `export function findForkCutSeq(events: AgentEvent[], assistantSeq: number): number | null` — 第一个 `seq > assistantSeq && eventType === 'turn_ended'` 的 seq,无则 null。
       - `export async function resolveSessionEvents(sessionId: string): Promise<AgentEvent[]>` — own = listEvents(sessionId);meta = getSessionRepo().getSession(sessionId);无 parent 或 forkCutSeq == null → own;否则 `resolveSessionEvents(meta.parentSessionId)` 递归后 buildForkEventStream(...).events(取 .events,invalid 时不融合,降级返回 own 并 console.error)。
       - 文件头注释: "fork_cut_seq 是父空间 seq,仅作 buildForkEventStream 输入"(Pitfall 6)。
    2. src/ai/sessionRepo.ts 扩展(接口 + SQL 实现 + MemorySessionRepo 镜像,遵循既有 SQL 常量导出模式):
       - `getSession(sessionId): Promise<SessionRecord | null>`(SELECT by session_id)
       - `createForkSession({ sessionId, workspaceId, parentSessionId, forkCutSeq, title: null })`(INSERT INTO sessions 含 parent 列)
       - listSessionsByWorkspace 的 SQL 增加 `LEFT JOIN sessions p ON p.session_id = s.parent_session_id` 选 `p.title AS parentTitle`;内存版同步。若既有 sessionRepo parity 测试文件存在($N→? 适配),为新 SQL 常量补 parity 断言。
    3. 接线两处(各一行替换): sessionRestore.ts:96 `store.listEvents(targetSessionId)` → `resolveSessionEvents(targetSessionId)`;compaction.ts:130 maybeCompactSession 内 listEvents → resolveSessionEvents。不改 fromEvents、不在 fromEvents 内特判 fork(反模式)。
    4. 子压缩 coveredSeq 空间锁定: maybeCompactSession 传给 applyCompactionResult 的是 normalized 流,写入的 coveredSeq* 必须与 restore 时 fromEvents 比较的空间一致(normalized)— Task 1 用例 9 round-trip 锁定。
    5. `npm test` 全绿(既有 190+ 通过 + 新增 fork 套件通过)。
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/fork.ts contains "export function buildForkEventStream"、"export function findForkCutSeq"、"export async function resolveSessionEvents"
    - src/ai/sessionRepo.ts contains "createForkSession"、"getSession"、"parentTitle"
    - src/ai/sessionRestore.ts 与 src/ai/compaction.ts 各至少一处 contains "resolveSessionEvents"
    - src/ai/sessionRestore.ts 不再在 96 行路径直接 contains "listEvents(targetSessionId)"
    - npm test 退出码 0,fork.test.ts 全部用例通过
  </acceptance_criteria>
  <done>fork 纯函数层 GREEN,全套测试通过,双接线完成</done>
</task>

</tasks>

<verification>
- `npm test` 全绿(fork 套件 + 既有 190+)
- grep 确认两处接线与 repo 新方法存在
</verification>

<success_criteria>
- fork 纯函数测试先行(RED→GREEN)已践行,覆盖不变量/remap/parity/mid-turn/fork-of-fork/compaction round-trip
- 引用式语义: 零父事件复制,无任何写父 session 的代码路径
</success_criteria>

<output>
After completion, create `.planning/phases/20-fork/20-01-SUMMARY.md`
</output>
