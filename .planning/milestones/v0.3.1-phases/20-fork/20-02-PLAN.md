---
phase: 20-fork
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/stores/chatConsoleStore.ts
  - src/components/AgentConsole.tsx
autonomous: true
requirements: [FORK-01, FORK-02, FORK-03, LIST-03]
must_haves:
  truths:
    - "用户 hover assistant 消息卡片时,卡片下方浮出分支与复制 icon(CSS group-hover + group-focus-within)"
    - "用户点击分支 icon 后以该 turn 的 turn_ended 为切点创建新 session 并 switchSession 跳转,原会话不动"
    - "无事件支撑的 store-only ack 消息不渲染分支 icon(复制仍可用)"
    - "用户点击复制 icon 后消息全文进系统剪贴板;失败弹 error toast,不静默"
    - "fork 出的 session 在对话区顶部显示来源徽章 '来自 {parentTitle}'(LIST-03 in-console 部分,数据含 parentTitle)"
    - "streaming 中分支入口 disabled(SESS-04 一致)"
  artifacts:
    - path: "src/stores/chatConsoleStore.ts"
      provides: "forkFromMessage action + forkable 消息解析"
      contains: "forkFromMessage"
    - path: "src/components/AgentConsole.tsx"
      provides: "hover toolbar + provenance badge + copy"
      contains: "group-hover:opacity-100"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "src/ai/fork.ts"
      via: "findForkCutSeq + resolveSessionEvents"
      pattern: "findForkCutSeq|resolveSessionEvents"
    - from: "src/stores/chatConsoleStore.ts"
      to: "src/ai/sessionRepo.ts"
      via: "createForkSession"
      pattern: "createForkSession"
    - from: "src/components/AgentConsole.tsx"
      to: "navigator.clipboard"
      via: "writeText + toast"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
在单一渲染点 AgentConsole.tsx 落 hover 分支/复制工具栏、fork 成功跳转、复制 toast、来源徽章。store 侧新增 forkFromMessage(ordered content-match 定位事件 → findForkCutSeq → createForkSession → session_forked 标记事件 → switchSession)。UI 完全按已批准的 20-UI-SPEC.md 执行。

Purpose: FORK-01/02/03 + LIST-03 的用户可见收口。
Output: chatConsoleStore.forkFromMessage + AgentConsole hover toolbar/badge。
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/20-fork/20-CONTEXT.md
@.planning/phases/20-fork/20-UI-SPEC.md
@.planning/phases/20-fork/20-RESEARCH.md
@.planning/phases/20-fork/20-01-SUMMARY.md
@src/stores/chatConsoleStore.ts
@src/components/AgentConsole.tsx
</context>

<interfaces>
<!-- 来自 20-01 的契约 -->
src/ai/fork.ts: findForkCutSeq(events, assistantSeq): number | null; resolveSessionEvents(sessionId): Promise<AgentEvent[]>
src/ai/sessionRepo.ts: createForkSession({ sessionId, workspaceId, parentSessionId, forkCutSeq, title }); getSession(sessionId); listSessionsByWorkspace() 返回 parentTitle
<!-- 既有契约 -->
chatConsoleStore: switchSession(sessionId)(Phase 19);emitToast/bindToast(63-71 行)已绑定 useToast;loading 状态(SESS-04)
AgentConsole.tsx:136-154 — 消息唯一渲染点(Drawer + workspace 双宿主);carry-chip 徽章 recipe 见 247 行
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: chatConsoleStore — forkFromMessage action + forkable 解析</name>
  <files>src/stores/chatConsoleStore.ts</files>
  <read_first>
    - src/stores/chatConsoleStore.ts 全文(重点: switchSession 实现 ~236 行的消息过滤、365/390/415/434/534/548 行 store-only ack 注入点、emitToast 桥 63-71 行、loading 守卫)
    - src/ai/fork.ts(20-01 产物: findForkCutSeq / resolveSessionEvents 签名)
    - src/ai/sessionRepo.ts(createForkSession / getSession 签名)
    - src/ai/events/eventStore.ts(append 签名 — session_forked 标记事件)
    - src/ai/sessionRestore.ts(restoreSession(childId) 语义)
  </read_first>
  <action>
    1. 新增 action `forkFromMessage(messageId: string): Promise<void>`,按 20-RESEARCH Pattern 3:
       a. 守卫: `if (get().loading) return`(toast "当前回复尚未完成，请稍后再试", SESS-04)。
       b. `const events = await resolveSessionEvents(activeSessionId)`(父可能本身是 fork)。
       c. 定位目标消息事件: 将 store messages 中 user + assistant(无 toolCallId,同 switchSession ~236 行的过滤)与事件投影(user_message + assistant_message)按 **ordered content match** zip;第一个内容不匹配处标记 store-only 消息(不可 fork)。命中的 assistant_message 记 assistantSeq;未命中 → toast "创建分支时出错，原会话未受影响" 并 return。`# ponytail:` 相同内容的两条 assistant 消息解析到第一条 — 相邻重复至多隔一个 turn,可接受。
       d. `const cutSeq = findForkCutSeq(events, assistantSeq)`;null → toast mid-turn 文案并 return。
       e. `const childId = crypto.randomUUID()`;`await createForkSession({ sessionId: childId, workspaceId: 当前工作区, parentSessionId: activeSessionId, forkCutSeq: cutSeq, title: null })`。
       f. 追加标记事件: `store.append({ sessionId: childId, eventType: 'session_forked', payload: { parentSessionId, parentCutSeq: cutSeq, parentEventCount: cutSeq } })`(保证子 ≥1 行,restoreSession 非 null;rebuildMessages default:break 已忽略)。
       g. `await switchSession(childId)` — 成功即跳转(无成功 toast,UI-SPEC 锁定);异常 catch → error toast "创建分支时出错，原会话未受影响"。
    2. forkable 解析(eager,~20 LOC,消息变化时计算一次而非 per-hover): 派生函数 `resolveForkableMessages()` — 跑上面步骤 c 的 zip,给可 fork 的 assistant 消息打标(如 message 上 `forkable?: boolean` 或单独 Set<messageId> 存 store)。在 messages 变化的 action(send 完成 / restore / switch)后调用。
    3. store 状态: 记录 `parentTitle`(switchSession 恢复后经 getSession(activeSessionId) 取 parentSessionId,再取父 title;null → '原会话'),供徽章渲染。
    4. 若既有 store 测试文件覆盖 chatConsoleStore,补 forkFromMessage 守卫用例(loading 拒绝、ack 消息不可 fork);`npm test` 全绿。
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/chatConsoleStore.ts contains "forkFromMessage"、"resolveSessionEvents"、"findForkCutSeq"、"createForkSession"、"session_forked"
    - contains toast 文案 "当前回复尚未完成" 与 "创建分支时出错"
    - forkFromMessage 内含 `loading` 守卫(grep "loading" 于该 action 作用域)
    - npm test 退出码 0
  </acceptance_criteria>
  <done>forkFromMessage 全链路可调用,forkable 解析与 parentTitle 就绪</done>
</task>

<task type="auto">
  <name>Task 2: AgentConsole — hover 工具栏 + 来源徽章 + 复制 toast</name>
  <files>src/components/AgentConsole.tsx</files>
  <read_first>
    - src/components/AgentConsole.tsx 全文(136-154 消息渲染行、247 carry-chip 徽章 recipe、滚动容器结构、宿主 props)
    - .planning/phases/20-fork/20-UI-SPEC.md(逐条对照 — 本任务是它的落地)
    - src/stores/chatConsoleStore.ts(Task 1 产物: forkFromMessage / forkable / parentTitle)
    - src/components/ui/Toast.tsx(useToast 用法)
  </read_first>
  <action>
    严格按 20-UI-SPEC.md 交互契约落地,所有值照抄 UI-SPEC:
    1. 消息行(AgentConsole.tsx:136-154)外层加 `group`;assistant 气泡下方加工具栏:
       `<div className="mt-1 flex gap-1 opacity-0 transition-opacity duration-150 group-hover:opacity-100 group-focus-within:opacity-100">`
       - 分支按钮(仅当消息 forkable 时渲染,不可 fork 的 ack 消息完全不渲染分支 icon): `<button onClick={() => forkFromMessage(message.id)} disabled={loading} className={cn('p-1 text-text-tertiary hover:text-text-primary', loading && 'cursor-not-allowed opacity-50')} title="从这里创建分支" aria-label="从这里创建分支"><GitBranch size={14} /></button>`
       - 复制按钮(assistant 消息恒渲染): `<button onClick={() => copyMessage(message.content)} className="p-1 text-text-tertiary hover:text-text-primary" title="复制" aria-label="复制这条回复"><Copy size={14} /></button>`
       - 顺序: 分支在前、复制在后。纯 `<button>`,不用 ui/Button。
    2. copyMessage handler: `navigator.clipboard.writeText(content).then(() => toast success {title:'已复制', description:'消息已复制到剪贴板'}), () => toast error {title:'复制失败', description:'无法访问剪贴板，请重试'})` — 失败绝不静默。
    3. 来源徽章(LIST-03 in-console): 消息列表滚动容器第一个子元素、首条消息之上、`mb-2`,仅当当前 session 有 parentSessionId 时渲染。样式照抄 carry-chip recipe: `inline-flex items-center gap-1 rounded-full border border-accent/20 bg-accent-subtle px-2 py-0.5 text-xs text-accent`,前导 `<GitBranch size={12} weight="fill" />`,文本 `来自 {parentTitle}`(parentTitle 为 null → `来自原会话`),title 容器 `max-w-[160px] truncate` + `title={parentTitle}` 全文 tooltip。静态,无动效。
    4. 图标从 @phosphor-icons/react 导入 GitBranch / Copy(默认 duotone,badge 内 fill 除外)。
    5. `npm run lint`(tsc --noEmit)通过。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AgentConsole.tsx contains "group-hover:opacity-100"、"group-focus-within:opacity-100"、"GitBranch"、"navigator.clipboard.writeText"、"来自 "
    - contains "已复制"、"复制失败"、"无法访问剪贴板"(经 toast 调用或文案常量)
    - contains "bg-accent-subtle"、"max-w-[160px] truncate"(徽章 recipe)
    - 不 contains "ui/Button" 新增导入(工具栏用原生 button)
    - npm run lint 退出码 0;npm test 退出码 0
  </acceptance_criteria>
  <done>FORK-01/02/03 + LIST-03 in-console 徽章全部可见可用,UI-SPEC 逐条落地</done>
</task>

</tasks>

<verification>
- npm test 全绿;npm run lint 通过
- 手动 UAT(dev): hover 浮出双 icon;点分支跳新 session 且顶部出现"来自 {parentTitle}"徽章;原会话切回历史完整;ack 消息无分支 icon;复制成功/失败 toast;streaming 中分支 disabled
- packaged-build 复制为 UAT 项(Pitfall 5,失败才引入 clipboard plugin)
</verification>

<success_criteria>
- 用户可从任意 assistant 消息一键建分支并跳转,原会话逐字不动
- 复制永不静默失败;徽章数据(parentTitle)为 Phase 21 列表 UI 就绪
</success_criteria>

<output>
After completion, create `.planning/phases/20-fork/20-02-SUMMARY.md`
</output>
