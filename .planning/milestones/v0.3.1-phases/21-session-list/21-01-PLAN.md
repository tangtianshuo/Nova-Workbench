---
phase: 21-session-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/sessionRepo.ts
  - src/lib/utils.ts
  - src/ai/__tests__/phase21SessionList.test.ts
autonomous: true
requirements: [LIST-01, TITLE-02]
must_haves:
  truths:
    - "One SQL round-trip returns sessions of a workspace each with a message count (no N+1)"
    - "updateTitle refuses to overwrite an existing title (SQL WHERE title IS NULL + memory-repo parity)"
    - "updateTitle records titleSource ('llm' | 'fallback')"
    - "formatRelativeTime maps age buckets to 刚刚/X 分钟前/X 小时前/X 天前/M月D日"
  artifacts:
    - path: "src/ai/sessionRepo.ts"
      provides: "countMessagesBySession + guarded updateTitle(sessionId, title, source)"
      contains: "SESSION_MESSAGE_COUNT_SQL"
    - path: "src/lib/utils.ts"
      provides: "formatRelativeTime"
    - path: "src/ai/__tests__/phase21SessionList.test.ts"
      provides: "repo + helper tests"
      min_lines: 40
  key_links:
    - from: "SESSION_MESSAGE_COUNT_SQL"
      to: "agent_events"
      via: "COUNT over event_type IN ('user_message','assistant_message')"
      pattern: "event_type IN \\('user_message','assistant_message'\\)"
---

<objective>
Data layer for the session list and auto-titling: message-count aggregate SQL, guarded/source-aware updateTitle, and the formatRelativeTime helper. UI lands in 21-03; title trigger lands in 21-02.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/21-session-list/21-CONTEXT.md
@src/ai/sessionRepo.ts
@src/ai/events/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Message-count aggregate + guarded updateTitle in sessionRepo</name>
  <files>src/ai/sessionRepo.ts, src/ai/__tests__/phase21SessionList.test.ts</files>
  <read_first>src/ai/sessionRepo.ts, src/ai/events/types.ts, src/ai/__tests__/phase18SessionRepo.test.ts</read_first>
  <behavior>
    - Test: MemorySessionRepo — upsert 2 sessions, append user_message/assistant_message events via getEventStore() (or insert rows directly); countMessagesBySession([idA, idB]) returns { idA: nA, idB: nB }; sessions with zero messages absent or 0.
    - Test: updateTitle('s1','新标题','llm') sets title + titleSource='llm'; second updateTitle('s1','另标题','fallback') is a NO-OP (title stays '新标题').
    - Test: SESSION_TITLE_SQL string contains "WHERE title IS NULL".
    - Test: SESSION_MESSAGE_COUNT_SQL contains "GROUP BY session_id" and "event_type IN ('user_message','assistant_message')".
  </behavior>
  <action>
    NOTE: message events are `user_message` / `assistant_message` (AGENT_EVENT_TYPES in src/ai/events/types.ts) — CONTEXT.md said "llm_message", the real type is assistant_message.
    1. Export constant:
       `export const SESSION_MESSAGE_COUNT_SQL = \`SELECT session_id, COUNT(*) AS message_count FROM agent_events WHERE session_id IN ($1, $2, $3, $4, $5) AND event_type IN ('user_message','assistant_message') GROUP BY session_id\`;`
       (In-memory impl ignores SQL; SQLite impl builds the IN-list dynamically from ids — keep the exported constant as the canonical text with a comment, and in SqliteSessionRepo build placeholders from the ids array: `db.select(sql, ids)` with sql = same shape using mapped '?'→ the repo currently uses $N placeholders adapted by the adapter; follow SESSION_LIST_SQL's $1 style, generating $1..$n from ids length, cap callers at ≤ 50 ids).
    2. Add to SessionRepo interface:
       `countMessagesBySession(sessionIds: string[]): Promise<Map<string, number>>`
       Memory impl: iterate `getEventStore()`? No — keep repo self-contained: MemorySessionRepo cannot see agent_events. Instead memory impl accepts an injectable counter? Simpler: memory impl reads from the in-memory event store via `getEventStore().listEvents(sessionId)` per id (test/web-dev only, N+1 acceptable there — add `// ponytail:` comment naming it). SQLite impl: one aggregate SQL.
    3. Change updateTitle signature: `updateTitle(sessionId: string, title: string, titleSource: 'llm' | 'fallback'): Promise<void>`
       - SESSION_TITLE_SQL → `'UPDATE sessions SET title = $1, title_source = $2 WHERE session_id = $3 AND title IS NULL'`
       - Memory impl parity: `if (row && row.title === null) { row.title = title; row.titleSource = titleSource; }`
    4. Create test file src/ai/__tests__/phase21SessionList.test.ts (node:test + assert, follow phase18SessionRepo.test.ts style; use resetMemorySessionRepo() / resetForTesting of event store in beforeEach).
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase21SessionList.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/sessionRepo.ts contains `SESSION_MESSAGE_COUNT_SQL` and `countMessagesBySession(sessionIds: string[]): Promise<Map<string, number>>`
    - src/ai/sessionRepo.ts contains `title_source = $2` and `AND title IS NULL`
    - MemorySessionRepo.updateTitle guards with `row.title === null`
    - Test file exits 0 and includes a no-overwrite assertion
  </acceptance_criteria>
  <done>Aggregate count + guarded title write available on both repo impls, tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: formatRelativeTime helper</name>
  <files>src/lib/utils.ts, src/ai/__tests__/phase21SessionList.test.ts</files>
  <read_first>src/lib/utils.ts</read_first>
  <behavior>
    - Test: formatRelativeTime(now) → '刚刚'
    - Test: age 5min → '5 分钟前'; age 59min → '59 分钟前'; age 3h → '3 小时前'; age 6d → '6 天前'
    - Test: age 7d (or 30d) → 'M月D日' (e.g. '7月21日')
  </behavior>
  <action>
    In src/lib/utils.ts add:
    ```ts
    /** Phase 21 (LIST-01): relative time for session list. <60s 刚刚, <60min X 分钟前,
     * <24h X 小时前, <7d X 天前, ≥7d M月D日. Input is an ISO string or Date. */
    export function formatRelativeTime(input: string | Date, now: Date = new Date()): string {
      const t = typeof input === 'string' ? new Date(input) : input;
      const sec = Math.max(0, Math.floor((now.getTime() - t.getTime()) / 1000));
      if (sec < 60) return '刚刚';
      const min = Math.floor(sec / 60);
      if (min < 60) return `${min} 分钟前`;
      const hr = Math.floor(min / 60);
      if (hr < 24) return `${hr} 小时前`;
      const day = Math.floor(hr / 24);
      if (day < 7) return `${day} 天前`;
      return `${t.getMonth() + 1}月${t.getDate()}日`;
    }
    ```
    Append tests to phase21SessionList.test.ts (it can import from '@/src/lib/utils' the same way other tests resolve the alias; if tests use relative paths, use '../../lib/utils').
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase21SessionList.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/utils.ts contains `export function formatRelativeTime`
    - Returns exactly: 刚刚 / `${n} 分钟前` / `${n} 小时前` / `${n} 天前` / `${M}月${D}日`
  </acceptance_criteria>
  <done>Helper + tests green</done>
</task>

</tasks>

<verification>
npx tsx --test src/ai/__tests__/phase21SessionList.test.ts && npm run lint && npx tsx --test src/ai/__tests__/phase18SessionRepo.test.ts (no regression)
</verification>

<success_criteria>
countMessagesBySession batch aggregate works; updateTitle is overwrite-proof and records source; formatRelativeTime buckets per spec.
</success_criteria>

<output>
Create `.planning/phases/21-session-list/21-01-SUMMARY.md`
</output>
