---
phase: 22-loop-replay-parity
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified: [src-tauri/src/engine/mod.rs, src-tauri/src/engine/event_log.rs, src-tauri/src/engine/confirmations.rs]
autonomous: true
requirements: [ENG-02, ENG-04]
must_haves:
  truths:
    - "Rust event_log 可 append 事件(SQL 侧 seq 分配)且事件流通过五种违规码校验"
    - "确认候选 confirm/consume/reject 原子条件 UPDATE,并发恰一成功"
    - "真事务包住「事件 append + artifact 写 + candidate 落库」"
  artifacts:
    - path: "src-tauri/src/engine/event_log.rs"
      provides: "agent_events/agent_artifacts/sessions 读写 + invariants + 事务"
      exports: ["append", "list_events", "check_event_stream", "prepare_tool_result", "upsert_session"]
    - path: "src-tauri/src/engine/confirmations.rs"
      provides: "候选创建/dedup/confirm/consume/reject + memory_candidates 写者"
      exports: ["create_candidate", "confirm", "consume", "reject", "list_pending"]
  key_links:
    - from: "src-tauri/src/engine/confirmations.rs"
      to: "src-tauri/src/engine/params_hash.rs"
      via: "dedup 比对 params_hash + consume 重算 hash"
      pattern: "params_hash\\("
    - from: "src-tauri/src/engine/event_log.rs"
      to: "agent_events 表"
      via: "INSERT 子查询分配 seq(勿在 Rust 分配)"
      pattern: "COALESCE\\(MAX\\(seq\\)"
---

<objective>
事件存储与确认队列移植:event_log(SQLite append/seq/list/invariants/artifacts/sessions/事务)+ confirmations(候选全生命周期 + 原子 UPDATE + memory_candidates 唯一写者)。

Purpose: 这是 Rust 接管 agent_* 四表唯一写者的落库层(ENG-02/ENG-04)。
Output: 两个模块 + cargo test 覆盖(含并发恰一测试)
</objective>

<context>
@22-RESEARCH.md
@src/ai/events/eventStore.ts
@src/ai/events/invariants.ts
@src/ai/events/artifacts.ts
@src/ai/confirmations.ts
@src/ai/confirmationStore.ts
<interfaces>
来自 22-02:
- engine::params_hash::params_hash(&Value) -> String
- engine::db::open / assert_schema
fixture 路径常量: concat!(env!("CARGO_MANIFEST_DIR"), "/../src/ai/__tests__/fixtures")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: event_log.rs — append/list/invariants/artifacts/sessions/事务</name>
  <files>src-tauri/src/engine/event_log.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/events/eventStore.ts:37-256(规格源:INSERT SQL/listEvents/listSessions/artifact 读写)
  - src/ai/events/invariants.ts:8-83(规格源:五种违规码)
  - src/ai/events/artifacts.ts:24-52(规格源:阈值与 modelText 前缀)
  - src-tauri/migrations/0002_init_agent_log.sql(列契约)
  - src-tauri/migrations/0007_sessions.sql
  - 22-RESEARCH.md §事件 schema 契约 + Code Examples
  </read_first>
  <behavior>
  - append 后 list_events 返回的 seq 从 1 连续递增;并发(双线程)append 同 session 无 UNIQUE 冲突、无空洞
  - check_event_stream 对合法流返回 Ok,对孤儿 tool_result / seq 断裂 / 非法序等五种违规各返回与 TS 逐字相同的违规码字符串
  - prepare_tool_result:>4096 字节 JSON 触发 artifact 化,返回的 modelText 前缀 `[tool_result <name>] ` + 截断 512 字节语义与 TS 一致;artifact 行 byte_size = json 字符长度
  </behavior>
  <action>
  1. struct `EventInput { session_id, event_type, workspace_id/product_id/project_id/correlation_id: Option<String>, payload: Value }`。
  2. `append(conn, &EventInput) -> Result<i64>`:INSERT 用 RESEARCH Code Example 的 SQL 逐字(`(SELECT COALESCE(MAX(seq), 0) + 1 FROM agent_events WHERE session_id = ?2)` 分配 seq);event_id = uuid v4;created_at = `chrono::Utc::now().format("%Y-%m-%dT%H:%M:%S%.3fZ")`(毫秒恒 3 位);payload_json = serde_json::to_string。返回 last_insert_rowid 前先 `SELECT seq` 拿真 seq。
  3. `list_events(conn, session_id) -> Result<Vec<AgentEvent>>`(ORDER BY seq;payload parse 回 Value);`upsert_session`(0007 列)。
  4. `check_event_stream(&[AgentEvent]) -> Result<(), String>`:invariants.ts 1:1,五个违规码字符串逐字相同(以 TS 常量为准,执行时照抄)。
  5. `prepare_tool_result(conn, tool_name, result_json) -> ToolResultOutcome`:4096/512 阈值常量、modelText 前缀逐字节;artifact INSERT 列 = (artifact_id uuid, session_id, tool_name, byte_size=content.len(), content, created_at)。
  6. `append_in_transaction(conn, ...)`:rusqlite `conn.transaction()` 包「tool_result 事件 + artifact 行 + candidate 行」;事务内不做任何 LLM/网络调用。
  7. 测试用内存库 `Connection::open_in_memory()` + 逐条执行 migrations/*.sql(读 CARGO_MANIFEST_DIR/migrations 目录);违规码/阈值/artifact 行为按 behavior 断言。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml event_log</automated>
  </verify>
  <acceptance_criteria>
  - `grep "COALESCE(MAX(seq)" src-tauri/src/engine/event_log.rs` 命中(SQL 侧 seq 分配)
  - `grep "%.3fZ" src-tauri/src/engine/event_log.rs` 命中(ISO 毫秒格式)
  - `grep "tool_result " src-tauri/src/engine/event_log.rs` 命中(modelText 前缀)
  - `cargo test event_log` 通过,含五种违规码与 4096 阈值断言
  </acceptance_criteria>
  <done>事件存储层与 TS 语义一致,真事务就位</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: confirmations.rs — 候选生命周期 + 原子 UPDATE + memory_candidates 写者</name>
  <files>src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/confirmationStore.ts:279-375(规格源:三条原子 UPDATE SQL 逐字)
  - src/ai/confirmations.ts(规格源:创建/dedup/consume 重算 hash 语义)
  - src/ai/memoryStore.ts SQLite 分支(规格源:memory_candidates 写路径 SQL — research 未精读,执行时必须先读)
  - src-tauri/migrations/0003_confirmation_candidates.sql + 0006 + 0004(memory_candidates 列)
  </read_first>
  <behavior>
  - confirm→consume→(二次 consume 失败):consume 条件 UPDATE 受影响行数 ==1 恰一成功;两线程并发 consume 同一 token,恰一 Ok 一 Err
  - 同 params 已有 active 候选(destructive/deliverable)时 create_candidate 走 dedup 返回既有 token,不新插
  - consume 对 deliverable 编辑后参数重算 hash 与落盘 params_hash 匹配才放行(confirmations.ts 语义)
  - rejected:rowsAffected==1 + expires_at > now 条件
  - memory_candidates 写路径(insert)与 memoryStore.ts SQLite 分支 SQL 一致
  </behavior>
  <action>
  1. 三条 UPDATE SQL 从 confirmationStore.ts:283-346 **逐字照抄**(含 `COALESCE(confirmed_at,$2)`、`expires_at > $2` 条件);rusqlite `execute` 返回受影响行数,==1 判定成功。
  2. `create_candidate(conn, kind, params, session_id)`:kind ∈ {knowledge_write, destructive_action, deliverable_draft};destructive/deliverable 先查 active(status IN ('pending','confirmed'))候选比对 params_hash 与关键字段(dedup);token = uuid v4;expires_at = now + 24h。
  3. `consume(conn, token, kind, edited_params: Option<&Value>)`:从 candidate 原始 params 重算 hash,deliverable 编辑后语义 = 重算值与 params_hash 比对。
  4. memory_candidates 写函数:照抄 memoryStore.ts SQLite 分支 INSERT(执行时先精读该文件,列清单以其为准)。
  5. 并发测试:std::thread 两线程 + 各自连接(WAL)同 consume 一个 token,断言恰一成功。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml confirmations</automated>
  </verify>
  <acceptance_criteria>
  - `grep "COALESCE(confirmed_at" src-tauri/src/engine/confirmations.rs` 命中
  - `grep "expires_at > " src-tauri/src/engine/confirmations.rs` 命中
  - `cargo test confirmations` 通过,含并发恰一测试(测试名含 concurrent)
  - memory_candidates INSERT 列与 memoryStore.ts SQLite 分支一致(执行时逐列核对)
  </acceptance_criteria>
  <done>HITL 落库层跨边界语义与现状一致,原子消费保持</done>
</task>

</tasks>

<verification>
`cargo test` 全绿(event_log + confirmations);ENG-02/ENG-04 的落库层就位
</verification>

<success_criteria>
Rust 可作为唯一写者向 agent_* 四表写入,并发语义与 TS 一致
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-03-SUMMARY.md`
</output>
