---
phase: 22-loop-replay-parity
plan: 07
type: execute
wave: 5
depends_on: ["22-04", "22-06"]
files_modified: [src-tauri/src/engine/parity.rs, src/ai/__tests__/parity.rust.test.ts, src/ai/__tests__/fixtures/realdb-sample-*.json]
autonomous: true
requirements: [ENG-03]
must_haves:
  truths:
    - "Rust 引擎逐位回放合成 fixture + 真实 v0.3.x DB 抽样,投影 JSON 与 TS 输出一致(永久 cargo test)"
    - "TS 侧 node:test 用同一批 fixture 跑 TS 投影(单源双侧)"
  artifacts:
    - path: "src-tauri/src/engine/parity.rs"
      provides: "canonical 化 + 时间戳白名单 diff + fixture 回放测试"
      contains: "canonical"
  key_links:
    - from: "src-tauri/src/engine/parity.rs"
      to: "src/ai/__tests__/fixtures/"
      via: "遍历全部 fixture JSON,双侧投影 diff"
      pattern: "projection-cases|realdb"
---

<objective>
replay parity 永久测试收口:parity harness(canonical 化 + 时间戳白名单)、TS 侧同 fixture 双跑、真实 v0.3.x DB 抽样 fixture 端到端。

Purpose: ENG-03 验收 — 永久测试锁定,新语义分支 = 新 fixture 双侧加。
Output: cargo test parity 永久绿 + TS parity 测试
</objective>

<context>
@22-RESEARCH.md §JSON 序列化 parity 规范化规则、§Validation Architecture
@src-tauri/src/engine/chat_session.rs
@src/ai/chatSession.ts
<interfaces>
fixture 单源: src/ai/__tests__/fixtures/(projection-cases.json 来自 22-04)
Rust 投影入口: engine::chat_session::from_events(&[AgentEvent]).get_messages_for_llm()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parity.rs harness + Rust 回放测试</name>
  <files>src-tauri/src/engine/parity.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - 22-RESEARCH.md §JSON parity 规范化规则 五条(键序/时间戳白名单/null vs 缺键/数字/modelText)
  - src-tauri/src/engine/chat_session.rs(diff 对象)
  - src/ai/__tests__/fixtures/projection-cases.json
  </read_first>
  <behavior>
  - 规范化五条全部实现:canonical 键序(parse→canonical string)、时间戳/ID 白名单置占位(`timestamp/createdAt/generatedAt/startedAt/lastEventAt/eventId/artifactId/correlationId/toolCallId/confirmationToken` → "&lt;ts&gt;"/"&lt;uuid&gt;")、null vs 缺键视为不同、整数 i64、modelText 逐字节
  - 遍历 fixtures/ 全部 projection 用例:Rust 投影 vs fixture expectedMessages,规范化后字符串相等
  </behavior>
  <action>
  1. `parity.rs`:`fn normalize(v: &mut Value)`(白名单键递归置占位)+ `fn canonical_string(v: &Value) -> String`(键字典序重排后序列化)+ `fn diff(a: &Value, b: &Value) -> Option<String>`。
  2. `#[test] fn parity_projection_cases()`:读 projection-cases.json 逐例 from_events → get_messages_for_llm → normalize → canonical_string 与期望比对;失败输出用例名 + 首个差异路径。
  3. 白名单实现为常量数组,占位 "&lt;ts&gt;"(时间戳类)/"&lt;uuid&gt;"(ID 类)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml parity</automated>
  </verify>
  <acceptance_criteria>
  - `grep "correlationId" src-tauri/src/engine/parity.rs` 命中(白名单)
  - `cargo test parity` 通过,覆盖 projection-cases.json 全部 ≥8 用例
  </acceptance_criteria>
  <done>Rust 侧永久 parity 测试就位</done>
</task>

<task type="auto">
  <name>Task 2: TS 侧同 fixture 双跑 + 真实 DB 抽样</name>
  <files>src/ai/__tests__/parity.rust.test.ts, src/ai/__tests__/fixtures/realdb-sample-1.json, src/ai/__tests__/fixtures/realdb-sample-2.json</files>
  <read_first>
  - src/ai/__tests__/fixtures/projection-cases.json(同源用例)
  - src/ai/chatSession.ts fromEvents(投影入口)
  - 22-RESEARCH.md §fixture 机制(真实 DB 导出方式)
  </read_first>
  <action>
  1. `parity.rust.test.ts`(node:test):读 projection-cases.json 逐例,TS ChatSession.fromEvents(events).getMessagesForLLM() 与 expectedMessages 断言(键序无关 — TS 侧字符串字段直接相等比较)。单源双侧锁定成立。
  2. 真实 DB 抽样:开发机 nova.db `SELECT * FROM agent_events WHERE session_id=? ORDER BY seq` 导出 2 个有代表性的 session(含 tool 配对 + 一次压缩/fork 最好),存 `realdb-sample-1.json`/`realdb-sample-2.json`(格式同 projection-cases 单例,expectedMessages 由 TS 生成)。
  3. Rust parity 测试把两个 realdb 样本纳入同一遍历循环(fixtures 目录 glob)。
  </action>
  <verify>
    <automated>node --test src/ai/__tests__/parity.rust.test.ts && cargo test --manifest-path src-tauri/Cargo.toml parity</automated>
  </verify>
  <acceptance_criteria>
  - `ls src/ai/__tests__/fixtures/realdb-sample-*.json` 两个文件存在
  - `node --test src/ai/__tests__/parity.rust.test.ts` 全绿
  - `cargo test parity` 输出含 realdb 样本用例且通过
  - 全量 `node --test src/ai/__tests__/` + `cargo test` 全绿(phase gate)
  </acceptance_criteria>
  <done>ENG-03 达成:双侧永久 parity 测试 + 真实存量日志回放通过</done>
</task>

</tasks>

<verification>
- cargo test 全量 + node --test 全量双绿(phase gate)
- 真实 v0.3.x 日志回放逐位通过
</verification>

<success_criteria>
SC-4 达成;新语义分支流程(双侧加 fixture)已文档化于测试文件头注释
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-07-SUMMARY.md`
</output>
