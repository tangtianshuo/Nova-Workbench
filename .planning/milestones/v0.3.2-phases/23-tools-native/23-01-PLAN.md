---
phase: 23-tools-native
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs, src-tauri/src/engine/channel.rs, src-tauri/src/engine/commands.rs, src/stores/chatConsoleStore.ts, src/lib/api.ts]
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-04]
must_haves:
  truths:
    - "工具执行存在异步入口 execute_async,loop 分发点统一 await,既有 118 个 cargo 测试不破"
    - "EngineEvent 新增 tool_output 变体,旧 webview 收到未知 kind 不崩(前端默认忽略)"
    - "engine_run 接受 workspace_root 参数并传入 LoopContext;webview 从 activeWorkspace.folderPath 传值"
    - "workspace_root 为 None 时工具返回 Failed(arg_error=false)而非 panic"
  artifacts:
    - path: "src-tauri/src/engine/tools.rs"
      provides: "execute_async 异步分发入口 + LoopContext.workspace_root"
      contains: "execute_async"
    - path: "src-tauri/src/engine/channel.rs"
      provides: "ToolOutput 流式事件变体"
      contains: "tool_output"
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_run workspace_root 参数"
      contains: "workspace_root"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "engine_run (commands.rs)"
      via: "engineRun invoke 传 workspace_root = activeWorkspace.folderPath"
      pattern: "workspace_root"
    - from: "src-tauri/src/engine/loop_runner.rs"
      to: "tools::execute_async"
      via: "loop 工具分发点 await"
      pattern: "execute_async"
---

<objective>
异步工具执行地基(exec/deliverable 的硬前置)+ ToolOutput 流式事件变体 + workspace_root 参数贯通。本 plan 是纯机械改动,不实现任何新工具。

Purpose: 23-RESEARCH §1 裁定 — 现有 tools::execute 是同步 &Connection,exec 必须异步;这是本 phase 唯一结构性改动,先行落地避免后续 plan 互相踩。
Output: execute_async 分发入口、EngineEvent::ToolOutput、workspace_root 参数链(lib → command → LoopContext)
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@23-CONTEXT.md
@23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: execute_async 入口 + LoopContext.workspace_root</name>
  <files>src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs</files>
  <read_first>
  - src-tauri/src/engine/tools.rs(execute 签名 :156、ToolOutcome、ToolCtx/LoopContext)
  - src-tauri/src/engine/loop_runner.rs(分发点 :304、run_tool_loop 签名 :166、EventCallback :29)
  - 23-RESEARCH.md §1「异步执行的结构性改动」、Pitfalls 1/2/3
  </read_first>
  <action>
  1. tools.rs:
     - `LoopContext` 新增 `pub workspace_root: Option<std::path::PathBuf>`。
     - 新增 `pub async fn execute_async(conn: &Connection, name: &str, args: &serde_json::Value, ctx: &ToolCtx, cancel: tokio_util::sync::CancellationToken, on_event: &(dyn Fn(EngineEvent) + Send + Sync)) -> ToolOutcome`。函数体:目前所有工具均为同步 → 直接 `execute(conn, name, args, ctx)`(加 `#[allow(unused_variables)]` 或以 `_` 前缀暂不使用 cancel/on_event,23-02 接入 exec 时启用)。签名现在就定型,后续 plan 不再改。
  2. loop_runner.rs:分发点(:304 附近)由 `tools::execute(...)` 改为 `tools::execute_async(..., cancel.clone(), on_event).await`。cancel 与 EventCallback 已在 run_tool_loop 作用域内(loop_runner.rs:166/:29),直接传。
  3. 不改任何既有工具行为;execute 保留原样(同步工具内路径)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
  - `grep "execute_async" src-tauri/src/engine/tools.rs src-tauri/src/engine/loop_runner.rs` 双侧命中
  - `grep "workspace_root" src-tauri/src/engine/tools.rs` 命中(LoopContext 字段)
  - cargo test 全绿(118+ baseline 不减)
  </acceptance_criteria>
  <done>异步入口 + workspace_root 字段落位,分发点 await,全部既有测试绿</done>
</task>

<task type="auto">
  <name>Task 2: ToolOutput 事件变体 + engine_run workspace_root 参数贯通 + webview 传参</name>
  <files>src-tauri/src/engine/channel.rs, src-tauri/src/engine/commands.rs, src-tauri/src/engine/loop_runner.rs, src/stores/chatConsoleStore.ts, src/lib/api.ts</files>
  <read_first>
  - src-tauri/src/engine/channel.rs(EngineEvent 七变体 :29-44,serde tag="kind")
  - src-tauri/src/engine/commands.rs(engine_run 签名与 run_tool_loop 调用处 :163-191)
  - src/lib/api.ts(engineRun 包装先例 :80-109)
  - src/stores/chatConsoleStore.ts(engineRun 调用处 :489-497)
  - 23-RESEARCH.md §1「流式回传」、Pitfall 7
  </read_first>
  <action>
  1. channel.rs:EngineEvent 新增变体 `#[serde(rename = "tool_output")] ToolOutput { name: String, stream: String, is_stderr: bool }`。补一条 serde 序列化测试:断言 kind == "tool_output"。
  2. commands.rs:engine_run 参数新增 `workspace_root: Option<String>`;构造 ToolCtx 时 `workspace_root: workspace_root.map(PathBuf::from)`。
  3. api.ts:engineRun 包装透传 workspace_root。
  4. chatConsoleStore.ts:engineRun 调用处从 workspaceStore 读 activeWorkspace.folderPath 传入(`useWorkspaceStore.getState()`,参照 store 间访问惯例 getState())。
  5. 检查 webview EngineEvent 处理(ChatPanel/事件分发处):确认未知 kind 走 default 忽略分支;若无 default fallthrough 则补上(Pitfall 7)。本阶段前端对 tool_output 可先忽略(23-02 再渲染)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `grep "tool_output" src-tauri/src/engine/channel.rs` 命中
  - `grep "workspace_root" src-tauri/src/engine/commands.rs src/lib/api.ts src/stores/chatConsoleStore.ts` 均命中
  - cargo test + npm test + tsc 全绿
  </acceptance_criteria>
  <done>ToolOutput 变体 + workspace_root 参数从 webview 一路贯通到 LoopContext,双侧测试绿</done>
</task>

</tasks>

<verification>
- cargo test / npm test / npm run lint 全绿
- execute_async 签名定型(cancel + on_event 参数在位),后续 plan 不再改分发结构
</verification>

<success_criteria>
- 分发点唯一改动完成:loop_runner await execute_async
- EngineEvent 可扩展性验证(tool_output 序列化测试)
- workspace_root 链路贯通且 None 安全
</success_criteria>

<output>
完成后创建 `.planning/phases/23-tools-native/23-01-SUMMARY.md`;每 task 一次 commit(feat/test(23-01): ...)。
</output>
