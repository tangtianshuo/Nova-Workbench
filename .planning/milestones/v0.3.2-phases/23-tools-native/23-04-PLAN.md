---
phase: 23-tools-native
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified: [src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs, src-tauri/src/engine/commands.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts]
autonomous: true
requirements: [TOOL-01, TOOL-03]
must_haves:
  truths:
    - "generate_deliverable 工具 = 纯候选入队(kind deliverable_draft,model 在 args 提供 draft,工具内零 LLM 调用)"
    - "落槽仍是 webview 用户确认动作(TS 写 rndStore 过渡期合法),agent_events 落库改走 engine_commit_deliverable Rust command"
    - "chatConsoleStore appendAuxEvent('deliverable_committed') TS 直写路径消灭"
    - "系统提示含适配版工具指南块 + PM CRUD 缺席降级说明(引导用户手动操作,预告 v0.3.3 回归)"
    - "模型 schema 中无 PM CRUD 工具(无桥)"
  artifacts:
    - path: "src-tauri/src/engine/tools.rs"
      provides: "generate_deliverable ToolSpec"
      contains: "deliverable_draft"
    - path: "src-tauri/src/engine/loop_runner.rs"
      provides: "ROLE_AND_TOOL_RULES 适配版(工具清单+降级说明)"
      contains: "manually"
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_commit_deliverable command"
      contains: "engine_commit_deliverable"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "engine_commit_deliverable"
      via: "commitToSlot 内 executeTool 成功后 invoke(替换 appendAuxEvent+flushEvents)"
      pattern: "engineCommitDeliverable|engine_commit_deliverable"
    - from: "src-tauri/src/engine/tools.rs"
      to: "confirmations::create_candidate"
      via: "deliverable_draft 候选入队(既有四键 dedup :176-179)"
      pattern: "create_candidate"
---

<objective>
deliverable 生成原生工具(纯候选入队,TS parity)+ PM CRUD 降级说明 + carry-in 接缝①迁移(engine_commit_deliverable)。

Purpose: TOOL-01 第四类工具;TOOL-03 降级提示;SC-5 前半。
Output: generate_deliverable 工具 + ROLE_AND_TOOL_RULES 适配版 + engine_commit_deliverable + TS 接缝替换
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@23-CONTEXT.md
@23-RESEARCH.md
@.planning/phases/23-tools-native/23-02-SUMMARY.md
@src/ai/tools/generateDeliverable.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate_deliverable 工具(纯候选入队)+ 降级说明指南块</name>
  <files>src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs</files>
  <read_first>
  - src/ai/tools/generateDeliverable.ts(schema :28-33、draft 语义 :81-84、SLOT_BY_CODE :26)
  - src-tauri/src/engine/confirmations.rs(deliverable_draft 四键 dedup :176-179 — 已有)
  - src-tauri/src/engine/loop_runner.rs(ROLE_AND_TOOL_RULES :89-93 注释明说 Phase 23 重开)
  - 23-RESEARCH.md §4、§6
  </read_first>
  <action>
  1. tools.rs 注册 `generate_deliverable`:schema `{ code: enum["prd"], title: min1, draft: min1, confirmationToken?: min1 }` strict(TS 逐字段复刻);description 复刻 "You produce the full draft content yourself in the `draft` parameter" 语义 + PORT_01_SUFFIX;idempotency = VerifyFirst。执行 = validate args → `create_candidate(conn, "deliverable_draft", params, ...)`(dedup 既有)→ AwaitConfirmation,wait_value 如 "Explicit confirmation is required before committing the deliverable."。**工具内零 LLM 调用**(research Open Question 2 裁定:严格复刻 TS 语义)。
  2. loop_runner.rs:替换/扩展 ROLE_AND_TOOL_RULES 为适配版指南块:列出现有原生工具(knowledge / exec / fs / generate_deliverable)+ 降级文案:"Task and schedule CRUD tools are not available in this version; guide the user to create them manually in the Tasks/Schedule views. They return in a later release."(中英择一,与现有 prompt 语言一致)。
  3. cargo 测试:工具 happy path 入候选;重复同 draft dedup;build_system_prompt 断言含降级关键词(grep "manually" 或中文等价)且不含 PM CRUD 工具名;tools::schemas() 断言无 task/schedule CRUD 工具(TOOL-03 锁定)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml deliverable && cargo test --manifest-path src-tauri/Cargo.toml build_system_prompt</automated>
  </verify>
  <acceptance_criteria>
  - `grep "deliverable_draft" src-tauri/src/engine/tools.rs` 命中;tools.rs 无 llm 调用(纯候选)
  - build_system_prompt 测试断言降级文案存在 + schema 无 CRUD 工具
  - cargo test 全量不减
  </acceptance_criteria>
  <done>四类工具集齐(TOOL-01 完),降级说明落系统提示(TOOL-03)</done>
</task>

<task type="auto">
  <name>Task 2: engine_commit_deliverable + 接缝① TS 直写消灭</name>
  <files>src-tauri/src/engine/commands.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts</files>
  <read_first>
  - src/stores/chatConsoleStore.ts(commitToSlot :786-791 现状、:803-804 双写取舍注释)
  - src/ai/tools/generateDeliverable.ts(confirm→consume→写 顺序不变量 :48-49)
  - src-tauri/src/engine/confirmations.rs(consume expected_params=None,hash 恒匹配 :241-244 注释)
  - src-tauri/src/engine/commands.rs(append_tool_result 模式 :262-275)
  </read_first>
  <action>
  1. commands.rs:`engine_commit_deliverable(session_id, token, code, title, edited_draft, product_id, doc_id, version, fts_hit_count, fts_immediate_hit) -> Result<(), AppError>`:`confirmations::confirm` → `consume(token, None)` → `event_log::append` "deliverable_committed"(payload 与 chatConsoleStore.ts:786-790 逐字段一致:docId/version/slotCode/code/ftsImmediateHit/ftsHitCount/sessionId/eventId;scope 继承 candidate 所在 session)。顺序 consume→append,勿倒置(Pitfall 4)。lib.rs 注册,api.ts 包装 engineCommitDeliverable。
  2. chatConsoleStore.ts:commitToSlot 内 executeTool 落槽成功后,把 appendAuxEvent('deliverable_committed', ...)+ flushEvents 两行(:786-791)替换为一次 `engineCommitDeliverable({...payload, sessionId: sessionRef.current.sessionId})` invoke。payload 字段逐字保留。失败时保留 TS fallback?**不保留** — 与 22-VERIFICATION 缺口关闭语义一致,失败抛 toast(双写取舍已在 :803-804 注释,沿用其处理)。
  3. cargo 测试:commit command happy path(event 落库字段全)+ 重复 consume 恰一;TS 测试更新受影响用例。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml commit_deliverable && npm test && npm run lint && ! grep -rn "deliverable_committed'" src/stores/chatConsoleStore.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep "appendAuxEvent('deliverable_committed'" src/stores/ src/ai/` 零命中(SC-5① grep gate)
  - `grep "engine_commit_deliverable" src-tauri/src/engine/commands.rs src-tauri/src/lib.rs src/lib/api.ts` 均命中
  - 全部测试绿
  </acceptance_criteria>
  <done>deliverable_committed 事件 Rust 唯一写者,接缝①关闭</done>
</task>

</tasks>

<verification>
- cargo test / npm test / npm run lint 全绿
- grep gates:appendAuxEvent('deliverable_committed') 零命中;deliverable 工具内无 llm.rs 调用
</verification>

<success_criteria>
- TOOL-01 四类工具集齐;TOOL-03 双证据(schema 无 CRUD + prompt 有降级说明);SC-5① 关闭
</success_criteria>

<output>
完成后创建 `.planning/phases/23-tools-native/23-04-SUMMARY.md`;每 task 一次 commit。
</output>
