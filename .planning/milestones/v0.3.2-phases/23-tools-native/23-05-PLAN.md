---
phase: 23-tools-native
plan: 05
type: execute
wave: 5
depends_on: ["23-04"]
files_modified: [src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/commands.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts]
autonomous: true
requirements: [TOOL-04]
must_haves:
  truths:
    - "engine_consume_memory command 在 Rust 内完成 confirm UPDATE + 原子 consume UPDATE + insertMemory INSERT(含 supersede),chatConsoleStore consumeIntoMemories TS 直写消灭"
    - "memory reject 同迁(engine_reject_memory),memory_candidates 表 TS 写路径清零"
    - "loop 集成测试:scripted FakeLlm 触发白名单 exec 命令,断言 tool_result + ToolOutput 事件 + 子进程退出(TOOL-04 锁)"
    - "全 phase grep gates 通过(两接缝 TS 写零命中;engine 执行路径零 invoke)"
  artifacts:
    - path: "src-tauri/src/engine/confirmations.rs"
      provides: "memory confirm/consume/insertMemory/supersede SQL 移植"
      contains: "insert_memory"
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_consume_memory / engine_reject_memory commands"
      contains: "engine_consume_memory"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "engine_consume_memory"
      via: "store.confirm+consumeIntoMemories 两行替换为一次 invoke"
      pattern: "engineConsumeMemory|engine_consume_memory"
---

<objective>
carry-in 接缝②迁移(consumeIntoMemories 三条 SQL 移植 Rust,含 reject 顺手同迁)+ TOOL-04 集成测试锁定 + 全 phase grep gates 收口。

Purpose: 关闭 22-VERIFICATION SC-3 残余;TOOL-04 测试锁定(无头=有头一致性)。
Output: engine_consume_memory / engine_reject_memory + loop 集成测试 + 收口证据
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@23-CONTEXT.md
@23-RESEARCH.md
@.planning/phases/23-tools-native/23-04-SUMMARY.md
@src/ai/memoryStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: engine_consume_memory / engine_reject_memory + 接缝②迁移</name>
  <files>src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/commands.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts</files>
  <read_first>
  - src/ai/memoryStore.ts(:600-687 confirm 条件 UPDATE / consume 原子 UPDATE / insertMemory INSERT;:698-712 supersede 逻辑;reject :735 附近)
  - src-tauri/src/engine/confirmations.rs(named_params 先例 :199-266、insert_memory_candidate :324)
  - src/stores/chatConsoleStore.ts(:713-715 两行接缝;:735 rejectMemory)
  - 23-RESEARCH.md §5 接缝②
  </read_first>
  <action>
  1. confirmations.rs 移植三条 SQL(named_params 风格,逐字节对齐 memoryStore.ts:600-687 语义):confirm UPDATE(status pending→confirmed 条件)、consume 原子 UPDATE(恰一语义)、insert_memory INSERT 含 supersede 逻辑(memoryStore.ts:698-712:旧 active 记忆标 superseded 再插新)。表结构以 TS 侧 schema 为准(同一 sqlite 库,勿改表)。
  2. commands.rs:
     - `engine_consume_memory(candidate_token) -> Result<Value, AppError>`:先 confirm 再 consume 再 insert_memory,一次动作完成(用户点「已记住」是一次操作);返回 MemoryRecord 形状 Value 供 TS toast。
     - `engine_reject_memory(candidate_token) -> Result<(), AppError>`:移植 reject UPDATE。
     - lib.rs 注册;api.ts 包装 engineConsumeMemory / engineRejectMemory。
  3. chatConsoleStore.ts::713-715 两行(store.confirm + store.consumeIntoMemories)替换为一次 engineConsumeMemory(token) invoke;:735 附近 rejectMemory 换 engineRejectMemory。toast 行为保留(用返回的 MemoryRecord)。
  4. cargo 测试:concurrent_consume_exactly_one_wins 式移植测试(两条线程同时 consume 恰一成功)+ supersede 链路一条;npm 测试更新受影响用例。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml memory && npm test && npm run lint && ! grep -rn "consumeIntoMemories" src --include="*.ts" --include="*.tsx" | grep -vE "__tests__|\.test\."</automated>
  </verify>
  <acceptance_criteria>
  - `grep -rn "consumeIntoMemories" src` 仅测试/归档命中(SC-5② grep gate)
  - `grep "engine_consume_memory" src-tauri/src/engine/commands.rs src-tauri/src/lib.rs src/lib/api.ts` 均命中
  - memoryStore.ts 中 confirm/consume/reject 的 SQL 执行路径已无 chatConsoleStore 调用方( store 方法可留作死代码或删除,executor 定;删除优先)
  - 全部测试绿
  </acceptance_criteria>
  <done checked>true</done>
  <!-- done: memory 三写路径(confirm/consume/reject)Rust 唯一写者,SC-5② 关闭 -->
</task>

<task type="auto">
  <name>Task 2: TOOL-04 集成测试锁定 + 全 phase 收口 gates</name>
  <files>src-tauri/src/engine/loop_runner.rs</files>
  <read_first>
  - src-tauri/src/engine/loop_runner.rs(既有 FakeLlm/scripted 测试先例)
  - 23-RESEARCH.md §6「TOOL-04 测试锁定」
  </read_first>
  <action>
  1. loop 集成测试(scripted FakeLlm):触发白名单 exec 命令(Windows `cmd /c echo` / cfg 分支),断言三件事:tool_result 落库 ok、EngineEvent 流中出现 kind=tool_output、子进程已退出(waitpid 返回)。再触发一个白名单外命令,断言进入 WAIT + exec_approval 候选存在。
  2. tools::schemas() 断言:name 集合 == 有头暴露集(单一注册表,天然一致,测试固化此不变量)。
  3. 收口 gates(写入 SUMMARY 作为 verification 证据,跑通后贴输出):
     - `grep -rn "invoke" src-tauri/src/engine/` — 执行路径(exec.rs/fs_ops.rs/tools.rs)零命中,commands.rs 仅 command 定义
     - `grep -rn "appendAuxEvent('deliverable_committed'" src` 零命中
     - `grep -rn "consumeIntoMemories" src` 仅测试命中
     - `cargo test && npm test && npm run lint` 全绿
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - 集成测试覆盖 exec 直行 + exec HITL 两路
  - schemas 一致性测试存在
  - 四项 grep gates 输出贴入 SUMMARY
  </acceptance_criteria>
  <done checked>true</done>
  <!-- done: TOOL-04 测试锁定,Phase 23 全部 grep gates 收口 -->
</task>

</tasks>

<verification>
- cargo test / npm test / npm run lint 全绿(最终态)
- SC-5 两接缝 grep 零命中;engine 执行路径零 webview invoke
</verification>

<success_criteria>
- SC-5 全关闭;TOOL-04 有集成测试与结构性 grep 双锁定
- Phase 23 五项 success criteria 证据齐备,可进 verification
</success_criteria>

<output>
完成后创建 `.planning/phases/23-tools-native/23-05-SUMMARY.md`;每 task 一次 commit。
</output>
