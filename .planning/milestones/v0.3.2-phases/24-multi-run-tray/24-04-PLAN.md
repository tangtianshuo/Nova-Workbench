---
phase: 24-multi-run-tray
plan: 04
type: execute
wave: 4
depends_on: ["24-01", "24-02", "24-03"]
files_modified: [src-tauri/src/engine/commands.rs, src-tauri/src/engine/scheduler.rs, src-tauri/src/engine/exec.rs, src-tauri/src/state.rs]
autonomous: true
requirements: [SCHED-04]
must_haves:
  truths:
    - "取消 running run(含窗口隐藏时的后台 run)后:子进程被树杀、事件日志无孤儿半写、run 出注册表出队列"
    - "取消 queued run 立即生效且不占并发槽(24-01 已测,此处全链路复验)"
    - "取消确认等待态 = 候选 REJECT 语义保持(engine_cancel 注释既有裁定不回归)"
  artifacts:
    - path: "src-tauri/src/engine/scheduler.rs"
      provides: "取消全链路测试(cap 后 permit 释放、队列移除、托盘 rebuild)"
      contains: "cancel"
  key_links:
    - from: "engine_cancel"
      to: "scheduler + engine_runs"
      via: "token cancel + 队列移除 + rebuild"
      pattern: "engine_cancel"
---

<objective>
SCHED-04 取消全链路锁定 + Phase 24 收口 gates。

Purpose: cancel 的各部分散在 23-02(树杀)、24-01(队列);本 plan 补跨层集成测试锁语义,防托盘/通知层引入回归。
Output: 后台取消集成测试 + 全 phase gates 跑通
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@24-CONTEXT.md
@.planning/phases/24-multi-run-tray/24-01-SUMMARY.md
@.planning/phases/24-multi-run-tray/24-02-SUMMARY.md
@.planning/phases/24-multi-run-tray/24-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 取消全链路集成测试 + 收口 gates</name>
  <files>src-tauri/src/engine/commands.rs, src-tauri/src/engine/scheduler.rs</files>
  <read_first>
  - src-tauri/src/engine/commands.rs(engine_cancel :227-233、engine_run 完成清理 :205/:210)
  - src-tauri/src/engine/exec.rs(取消 → kill_tree → Failed("cancelled"),23-02)
  - src-tauri/src/engine/event_log.rs(cancelled run 的尾事件语义 —— interrupted/cancelled 事件如何落盘)
  </read_first>
  <behavior>
  - Test 1: run A(running,fake-LLM 含 exec 长命令)+ run B(running)+ C(queued);cancel A → A 的子进程被杀(exec 层已有单测,此处链路复验)、event_log 无未配对 tool_call 孤儿(PORT-01 语义)、B 不受影响继续、C 仍排队
  - Test 2: cancel C(queued)→ 立即出队,B 完成后槽空,scheduler snapshot 不含 C
  - Test 3: engine_cancel 传入不存在 run_id → Ok(幂等既有,不回归)
  </behavior>
  <action>
  1. 在 commands.rs / scheduler.rs 测试区补上述集成测试(fake Llm + temp WAL DB + scheduler 实例;tray AppHandle 注入 None 路径)。若 cancelled run 的尾事件孤儿语义有缺口(interrupted 标记),在 loop_runner 取消出口补 —— 22-x 已有恢复侧处理,只验证不重写。
  2. 收口 gates:cargo test 全量 + cargo build(release 可选)+ npm test + npm run lint 全绿;grep 锁定:engine_cancel 同时触达 token cancel 与 scheduler 队列移除。
  3. 若发现真实缺口:修在该 task 内(预期小;引擎取消语义 22/23 已锁,风险低)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - 三个新测试存在且全绿;全量 gates 绿
  - cancel 后 scheduler snapshot / engine_runs 注册表均无残留(grep 或断言)
  </acceptance_criteria>
  <done checked>SCHED-04 取消语义(含后台/排队)集成锁定,Phase 24 gates 全绿</done>
</task>

</tasks>

<verification>
- 全量 cargo test / npm test / npm run lint / cargo build 绿
- 人工 UAT 清单(交 VERIFICATION):双 run 并行 → 关窗后台 → 通知 → 托盘跳转 → 后台取消 → 重开确认卡恢复
</verification>

<success_criteria>
- SCHED-01..04 全部有结构/测试证据;carry-in 关闭
</success_criteria>

<output>
完成后创建 `.planning/phases/24-multi-run-tray/24-04-SUMMARY.md`;每 task 一次 commit。
</output>
