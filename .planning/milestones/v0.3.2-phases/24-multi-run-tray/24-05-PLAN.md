---
phase: 24-multi-run-tray
plan: 05
type: execute
wave: 5
depends_on: ["24-04"]
gap_closure: true
files_modified: [src/stores/chatConsoleStore.ts, src/components/AgentConsole.tsx, src/ai/api.ts]
autonomous: true
requirements: [SCHED-04]
must_haves:
  truths:
    - "chatConsoleStore 每 session 存储活跃 runId(engineRun 发起时 mints 的 crypto.randomUUID() 存入 per-session run state,完成/出错/取消时清除)"
    - "用户在 ChatPanel 流式输出中可点「停止」按钮取消运行中 run(UI 入口存在且有调用方)"
    - "engineCancel(api.ts:90)有生产调用方(gap 前:零调用方)"
    - "取消后 run 状态复位、后续可重新发起;取消语义 Rust 侧已测(24-04 三条集成测试),本 plan 只补 UI→Rust 接线"
  artifacts:
    - path: "src/stores/chatConsoleStore.ts"
      provides: "activeRunId 存储 + cancelRun action(调 engineCancel + 本地状态复位)"
      contains: "cancelRun"
    - path: "src/components/AgentConsole.tsx"
      provides: "流式中「停止」按钮"
      contains: "停止"
  key_links:
    - from: "src/components/AgentConsole.tsx"
    - to: "chatConsoleStore.cancelRun"
      via: "停止按钮 onClick"
      pattern: "cancelRun"
    - from: "src/stores/chatConsoleStore.ts"
      to: "engineCancel"
      via: "engineCancel(activeRunId) invoke"
      pattern: "engineCancel"
---

<objective>
SCHED-04 gap 修复(v0.3.2 milestone audit 发现):UI 取消入口接线 — chatConsoleStore 存 runId + cancelRun action + ChatPanel「停止」按钮。

Purpose: Rust cancel 全链路已测(24-04),但 webview→Rust 触发断裂:engineCancel 零调用方、runId 未存储,用户无法取消。SC 承诺「用户可取消运行中的 run(含后台)」当前不成立。
Output: runId 存储 + 停止按钮 + engineCancel 活调用方 + 测试
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/24-multi-run-tray/24-CONTEXT.md
@.planning/v0.3.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: runId 存储 + cancelRun action + 停止按钮</name>
  <files>src/stores/chatConsoleStore.ts, src/components/AgentConsole.tsx, src/ai/api.ts</files>
  <read_first>
  - src/stores/chatConsoleStore.ts(:603 附近 engineRun 发起处 — runId mints 后存入该 session run state;on_event 完成/错误分支与 finally 处清除;per-session streaming guard 结构)
  - src/components/AgentConsole.tsx(:236 附近排队中状态条 — 停止按钮放同区域,流式/排队中显示;样式走 token 体系,参照现有按钮)
  - src/ai/api.ts(:90 engineCancel — 已存在,确认签名)
  - src-tauri/src/engine/commands.rs(engine_cancel — Rust 侧幂等:未知 runId 亦 Ok,24-04 测试)
  </read_first>
  <action>
  1. chatConsoleStore:engineRun 发起处将 mints 的 runId 存入该 session 的 run state(如 activeRunId);run 结束(完成/出错/finally)清除。新增 action `cancelRun(sessionId)`:读 activeRunId → invoke engineCancel → 本地按取消处理(run state 复位;引擎侧会以事件流收尾,以 Rust 事件为准,本地不重复投影)。queued 状态的 run 同样可取消(Rust acquire 取消分支已处理)。
  2. AgentConsole.tsx:流式中/排队中的 session 显示「停止」按钮(危险/ghost 变体 discretion;与排队中状态条同区域),onClick → cancelRun。仅该 session 有 activeRunId 时显示。
  3. api.ts 确认 engineCancel 包装无需改动(预期零改;若有类型问题顺修)。
  4. 测试:store 层加一条(cancelRun 调用后 activeRunId 清除/状态复位,mock engineCancel);组件测试若无既有模式则不强制(store 测试为准)。
  </action>
  <verify>
    <automated>grep -rn "engineCancel" src --include="*.ts" --include="*.tsx" | grep -v __tests__ && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `grep -rn "engineCancel" src` 非测试文件 ≥2 处命中(api.ts 定义 + chatConsoleStore 调用方)
  - `grep -n "cancelRun\|activeRunId" src/stores/chatConsoleStore.ts` 命中;AgentConsole 有停止按钮
  - npm test 全绿(217+ 基线,新增测试允许 +1/+2)、tsc clean
  </acceptance_criteria>
  <done checked>SCHED-04 用户路径接通:UI 可取消运行中/排队中 run,engineCancel 有活调用方</done>
</task>

</tasks>

<verification>
- npm test / npm run lint 全绿;engineCancel 有生产调用方
- Rust 侧不动(24-04 已测);本 plan 仅 UI→Rust 接线
</verification>

<success_criteria>
- milestone audit SCHED-04 gap 关闭:用户可在 UI 取消 run,取消后状态复位可重发
</success_criteria>

<output>
完成后创建 `.planning/phases/24-multi-run-tray/24-05-SUMMARY.md`(标注 gap_closure);每 task 一次 commit。
</output>
