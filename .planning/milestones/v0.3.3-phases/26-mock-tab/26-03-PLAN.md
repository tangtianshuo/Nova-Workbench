---
phase: 26-mock-tab
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - src/components/rnd/TabRunPanel.tsx
  - src/views/RndCenterView.tsx
  - src/components/product/AIRequirementsTab.tsx
autonomous: true
requirements: [TAB-01, TAB-02, TAB-03]

must_haves:
  truths:
    - "用户在需求 tab 点生成按钮,触发真实 engine_run(携带 tab 上下文、独立 session、batch priority)"
    - "tab 内看到流式状态摘要 + 可折叠事件列表,可取消"
    - "产物候选走全局确认队列 + PrdDraftDialog 式编辑落槽,与 PRD 生产线同构"
    - "web 模式(非 Tauri)按钮禁用 + tooltip,无死路径"
  artifacts:
    - path: "src/components/rnd/TabRunPanel.tsx"
      provides: "共享进度/事件/取消面板"
      exports: ["TabRunPanel"]
    - path: "src/components/product/AIRequirementsTab.tsx"
      provides: "试点接线(真实 run 替代 generateRequirementAI)"
  key_links:
    - from: "src/components/product/AIRequirementsTab.tsx"
      to: "useTabRunStore.startTabRun"
      via: "生成按钮 onClick"
      pattern: "startTabRun"
    - from: "src/components/rnd/TabRunPanel.tsx"
      to: "useTabRunStore"
      via: "按 tabId 订阅 run 记录渲染状态"
      pattern: "useTabRunStore"
---

<objective>
TabRunPanel 共享组件 + 需求 tab 试点端到端(research Build Order step 1:最小 seam 先验证独立 session 约定)。

Purpose: 验证 26-01 地基 + 26-02 优先级在真实 tab 上成立;26-04 其余 tab 是机械复制本 plan 模式。
Output: TabRunPanel + 需求 tab 真实 run + 候选→HITL→落槽复用。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-mock-tab/26-CONTEXT.md
@.planning/phases/26-mock-tab/26-UI-SPEC.md(视觉/交互/文案契约——逐条遵守)
@.planning/phases/26-mock-tab/26-01-SUMMARY.md

<interfaces>
26-01 交付:
```typescript
// src/stores/tabRunStore.ts
useTabRunStore.getState().startTabRun({ tabId, kind, productId, userMessage, coreContext, sessionTitle?, workspaceId?, workspaceRoot? }) => runId
cancelTabRun(runId); clearRun(runId);
// state: runs: Record<runId, TabRunRecord{ tabId, kind, sessionId, status, currentStep, events, candidateCount, error }>, runsByTab
// TabRunStatus: 'queued'|'running'|'waiting-for-confirmation'|'done'|'error'|'cancelled'
```
26-02 交付:`engineRun` 可传 `priority?: 'interactive' | 'batch'`(tab run 一律 'batch')。
`buildCoreContext(tabPayload?: { kind: string; instruction: string })`。

候选→落槽既有链(照抄形状,勿重造):
chatConsoleStore.ts 1062-1114 commitToSlot——confirm → executeTool('generateDeliverable', {code,title,draft,confirmationToken}) → engineCommitDeliverable(...) → rndStore.commitDeliverableDraft 投影。PrdDraftDialog.tsx 为纯展示编辑 Dialog(props: open/onOpenChange/title/description/initialDraft/busy/onCommit)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabRunPanel 共享组件(按 26-UI-SPEC 契约)</name>
  <files>src/components/rnd/TabRunPanel.tsx</files>
  <read_first>
    - .planning/phases/26-mock-tab/26-UI-SPEC.md 全文(组件契约 1、色彩状态表、文案表、动画段)
    - src/components/ui/index.ts + Card.tsx + Button.tsx + Badge.tsx + Tooltip.tsx(组装材料)
    - src/stores/tabRunStore.ts(26-01 交付的 TabRunRecord 形状)
    - src/lib/utils.ts(cn)、src/components/ui/ProgressBar.tsx(若存在;否则用 indeterminate CSS)
  </read_first>
  <action>
创建 `src/components/rnd/TabRunPanel.tsx`(新目录 src/components/rnd/,首个文件),命名导出 `TabRunPanel`,props `{ tabId: string; className?: string }`。

行为:从 useTabRunStore 选 `runsByTab[tabId]` 对应 run(无 run 或已 clearRun → 返回 null)。

结构(全部按 26-UI-SPEC §Component Contracts 1):
- 根:`Card variant="default"` compact、`rounded-[var(--radius-lg)]`、`shadow-sm`、内边距 p-4;置于 tab 内容区顶部(由调用方摆放)。
- 展开动画:`motion.div` height/opacity,spring `{ stiffness: 350, damping: 30 }`;run 开始自动展开;done/error 后 ~2s 自动收起为单行摘要(setTimeout,组件卸载清理);收起行保留至下一次 run 或手动 dismiss。
- 摘要行:状态徽章 + 当前步骤一-liner(13px truncate)+ elapsed(起点 run.startedAt,每秒更新)+ chevron 切换(CaretDown 14,展开时 rotate-180)+ 取消按钮。
- 状态→视觉(严格按 UI-SPEC 色彩表):queued=text-text-tertiary+Spinner(12px);running=accent+Spinner+indeterminate 进度条;waiting-for-confirmation=Badge variant="warning" + 链接式按钮(variant="link")文案「打开确认队列」→ 调既有全局确认队列打开 action(grep 确认队列 store 的 open 入口,read_first 后接真实 action 名);done=Badge variant="success" + CheckCircle 14;error=Badge variant="danger" + 错误文案。
- 事件列表(默认收起):max-h-48 overflow-y-auto,每行 = 时间戳(11px `font-mono text-text-tertiary`)+ 事件 kind + 工具名/摘要(13px),行底 `bg-tertiary/50`;空态文案「暂无事件」;不渲染 markdown。
- 取消:`Button variant="danger" size="sm"` 文案「取消生成」,tooltip「已确认落槽的产物会保留」,onClick=cancelTabRun(runId);无确认弹窗。
- 文案逐字用 UI-SPEC Copywriting 表:正在生成…/等待确认 — 产物待你确认后落槽/生成完成 — N 份候选已提交确认/已取消 — 已确认落槽的产物保留,未确认候选已作废/生成失败:{摘要}…+ 重试 primary Button(重试 = 重新 startTabRun 同参数,参数从 run record 的 kind/productId 重建,原 userMessage 存 TabRunRecord 需在 26-01 基础上补存——若 26-01 未存,在本 task 给 TabRunRecord 加 `userMessage: string` 字段并同步 tabRunStore)。

只用语义 token 类(bg-*/text-*/border-border-subtle),禁 hex/gray/white。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "variant=\"danger\"" src/components/rnd/TabRunPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "打开确认队列" src/components/rnd/TabRunPanel.tsx 命中;grep "暂无事件" 命中;grep "取消生成" 命中
    - grep -E "bg-white|text-gray|#[0-9A-Fa-f]{6}" src/components/rnd/TabRunPanel.tsx 为 0 行
    - grep "useTabRunStore" 命中;grep "motion" 命中(spring 动画存在)
    - npx tsc --noEmit 零错误
  </acceptance_criteria>
  <done>一个组件可被六个 tab 复用:状态/事件/取消/确认队列入口/重试齐备,视觉与文案与 UI-SPEC 逐字一致</done>
</task>

<task type="auto">
  <name>Task 2: 需求 tab 试点接线(真实 run 替代 mock,端到端)</name>
  <files>src/components/product/AIRequirementsTab.tsx, src/views/RndCenterView.tsx</files>
  <read_first>
    - src/components/product/AIRequirementsTab.tsx 全文(找到现有 AI 按钮、generateRequirementAI 调用点、prompt 输入)
    - src/views/RndCenterView.tsx(RndCenter 的 tab 组装方式)
    - src/stores/tabRunStore.ts、src/ai/context.ts(26-01 接口)
    - src/stores/chatConsoleStore.ts 1040-1120(候选确认→落槽链,照抄形状)
    - src/ai/tools/generateDeliverable.ts(落槽工具参数:code/title/draft/confirmationToken)
    - src/components/PrdDraftDialog.tsx(编辑 Dialog 直接复用)
    - src/lib/api.ts(isTauri)
  </read_first>
  <action>
在 AIRequirementsTab(需求 tab 真实组件文件,位于 src/components/product/)接线:

1. **生成按钮** onClick 改为:
```typescript
const runId = useTabRunStore.getState().startTabRun({
  tabId: 'requirement', kind: 'requirement', productId,
  userMessage: `生成需求文档。${promptText || ''}`,
  coreContext: buildCoreContext({ kind: 'requirement',
    instruction: `为产品生成需求规格文档(PRD Markdown)。产物须经 generateDeliverable 工具产出候选(code=prd 或需求类目 code),等待用户确认落槽。${scenarioTemplate ? `场景模板:${scenarioTemplate}` : ''}` }),
  sessionTitle: `${product.name} · 需求文档生成`,
});
```
   并确认 tabRunStore startTabRun 内 engineRun 调用带 `priority: 'batch'`(若 26-01 未带,在本 task 补上——tab run 一律 batch,TAB-06)。
2. **面板**:tab 内容区顶部渲染 `<TabRunPanel tabId="requirement" />`;运行中(`status` 为 queued/running/waiting-for-confirmation)时生成按钮 `disabled` + tooltip「本 tab 已有生成任务进行中」。
3. **候选落槽**:tabRunStore 的 confirmation 路由已把候选送全局确认队列(26-01 Task 1);本 task 补 tab 侧编辑 Dialog:候选为 knowledge_write/generateDeliverable 形状时,复用 `PrdDraftDialog`(open 状态存本组件,initialDraft=候选 draft,onCommit 走与 chatConsoleStore.commitToSlot 相同的链:confirm → executeTool('generateDeliverable', {...}) → engineCommitDeliverable({sessionId 用 tab run 的 sessionId}) → 成功后 rndStore 投影自动经 commitDeliverableDraft 刷新,并 toast「已落槽」)。若 26-01 路由已让确认队列卡自带此链,则只做「队列确认后 tab 槽位刷新」验证,不重复造第二条链(以全局队列单链为准则,D-05)。
4. **web fallback**:`!isTauri()` 时生成按钮 `disabled` + Tooltip「此功能需要桌面引擎,请使用桌面版 Nova。」。
5. **mock 停用**:删除本 tab 对 `generateRequirementAI` 的调用(方法本体删除留 26-04 统一清);tab 数据空态渲染文案「还没有需求文档。点击上方生成按钮,AI 将生成候选供你确认。」。
6. RndCenterView 仅需确认 TabRunPanel 挂载点与 tabId 传参正确。
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "generateRequirementAI" src/components/product/AIRequirementsTab.tsx 为 0 行(UI 不再调 mock)
    - grep -n "startTabRun" src/components/product/AIRequirementsTab.tsx 命中;grep "TabRunPanel" 命中
    - grep -n "priority" src/stores/tabRunStore.ts 命中且值为 'batch'(或 engineRun 调用含 priority:'batch')
    - grep "此功能需要桌面引擎" src/components/product/AIRequirementsTab.tsx 命中(web fallback)
    - npx tsc --noEmit 与 npm run build 零错误
  </acceptance_criteria>
  <done>需求 tab 点生成→真实 engine_run(batch 优先级、独立 session、tab 上下文)→面板流式→候选进全局队列→PrdDraftDialog 编辑→落槽投影刷新;web 模式禁用无死路径</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit、npm run build 全绿
- 桌面模式手工路径留给 26-04 末 checkpoint 统一验证
</verification>

<success_criteria>
- TAB-01/02/03 在一个 tab 上端到端成立,模式被验证可供 26-04 机械复制
</success_criteria>

<output>
完成后创建 .planning/phases/26-mock-tab/26-03-SUMMARY.md
</output>
