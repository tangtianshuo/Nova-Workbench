---
phase: 29-pm-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/0012_pm_crud.sql
  - src-tauri/src/lib.rs
  - src-tauri/src/engine/pm_store.rs
  - src-tauri/src/engine/mod.rs
  - src/stores/storage/initializeDatabase.ts
autonomous: true
requirements: [PM-04]

must_haves:
  truths:
    - "tasks/schedules/task_categories 关系表存在于真实设备 nova.db(schema_version=12)"
    - "旧 kv JSON 数据(nova-task/nova-schedule)已按 id 幂等搬入关系表,kv key 原样保留未删除"
  artifacts:
    - path: "src-tauri/migrations/0012_pm_crud.sql"
      provides: "tasks/schedules/task_categories 表 + pm_write confirmation kind"
      contains: "CREATE TABLE IF NOT EXISTS tasks"
    - path: "src-tauri/src/engine/pm_store.rs"
      provides: "SQL CRUD 帮助函数(工具层与搬移共用)"
      exports: ["insert_task", "upsert_task_from_json", "list_tasks", "delete_task", "upsert_schedule_from_json", "list_schedules", "delete_schedule", "migrate_kv_pm_data"]
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/0012_pm_crud.sql"
      via: "sql_migrations() version 12 注册"
      pattern: "version: 12"
    - from: "src-tauri/src/engine/pm_store.rs"
      to: "kv_store (nova-task/nova-schedule)"
      via: "migrate_kv_pm_data 读 kv JSON → INSERT OR IGNORE"
      pattern: "nova-task"
---

<objective>
关系化地基(PM-04):migration 0012 建 tasks/schedules/task_categories 关系表 + pm_write 确认 kind;Rust 侧 pm_store SQL 层 + 启动一次性 kv→关系表幂等搬移。这是引擎直写的唯一安全落点(SC-4 裁定:同期关系化)。

Purpose: 引擎直写 kv JSON 快照会与 webview persist 整快照写互相覆盖(丢写),关系表是唯一安全落点。
Output: 0012 migration + pm_store.rs + lib.rs 注册 + 启动搬移钩子。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-pm-crud/29-CONTEXT.md

字段名真相源(参数 schema 对齐 TS 既有字段名,不发明新 schema):
- Task(src/data/mockTasks.ts): id, title, status('未开始'|'进行中'|'已完成'), priority('high'|'medium'|'low'), deadline(YYYY-MM-DD text), description, project, projectId?, assignee, assigneeAvatar, time, aiSuggestions, scheduledEventId?
- TaskCategory: id('cat-*'), name, color, tasks[]
- ScheduleEvent(src/stores/scheduleStore.ts): id, title, time('HH:mm - HH:mm' text), date(YYYY-MM-DD text), type('meeting'|'deadline'|'task'|'reminder'|'review'|'sync'), location, projectId?, taskId?, status?('未开始'|'进行中'|'已完成')
- kv 快照形状: kv_store key 'nova-task' → {"state":{"categories":[...]},"version":2};'nova-schedule' → {"state":{"events":[...]},"version":3}(zustand persist JSON)

先例(照抄结构):
- 0011_confirmation_kind_ingestion.sql — CHECK 扩展 = copy→drop→rename 重建表
- 0010_ingested_documents.sql — 幂等 CREATE IF NOT EXISTS
- lib.rs sql_migrations() — 每个 migration 必须注册(27-04 教训:未注册 = 真机缺表,有 registry_matches_migration_tests 守门)
- lib.rs setup() 里 db open 后的 spawned async block — 搬移钩子挂同一位置
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0012 — 关系表 + pm_write kind + lib.rs 注册</name>
  <files>src-tauri/migrations/0012_pm_crud.sql, src-tauri/src/lib.rs, src/stores/storage/initializeDatabase.ts</files>
  <read_first>
    - src-tauri/migrations/0011_confirmation_kind_ingestion.sql (CHECK 扩展先例,逐行照抄结构)
    - src-tauri/migrations/0010_ingested_documents.sql (幂等建表先例)
    - src-tauri/src/lib.rs (sql_migrations + registry_matches_migration_tests)
    - src/stores/scheduleStore.ts (ScheduleEvent 字段与 type/status 枚举)
    - src/data/mockTasks.ts (Task/TaskCategory 字段)
  </read_first>
  <action>
创建 src-tauri/migrations/0012_pm_crud.sql,内容(日期/时间全 TEXT 零转换,per CONTEXT 日期格式裁定):

```sql
-- Phase 29 (29-01): PM CRUD 关系化 — tasks/schedules/task_categories + pm_write kind.

CREATE TABLE IF NOT EXISTS task_categories (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  color TEXT NOT NULL DEFAULT 'bg-blue-500',
  sort INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE IF NOT EXISTS tasks (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT '未开始',
  priority TEXT NOT NULL DEFAULT 'medium' CHECK (priority IN ('high','medium','low')),
  deadline TEXT NOT NULL DEFAULT '',
  description TEXT NOT NULL DEFAULT '',
  project TEXT NOT NULL DEFAULT '',
  project_id TEXT,
  assignee TEXT NOT NULL DEFAULT '',
  assignee_avatar TEXT NOT NULL DEFAULT '',
  time TEXT NOT NULL DEFAULT '',
  category_id TEXT NOT NULL DEFAULT '',
  scheduled_event_id TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_tasks_status ON tasks (status);
CREATE INDEX IF NOT EXISTS idx_tasks_project_id ON tasks (project_id);
CREATE INDEX IF NOT EXISTS idx_tasks_deadline ON tasks (deadline);

CREATE TABLE IF NOT EXISTS schedules (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  time TEXT NOT NULL DEFAULT '',
  date TEXT NOT NULL,
  type TEXT NOT NULL DEFAULT 'reminder' CHECK (type IN ('meeting','deadline','task','reminder','review','sync')),
  location TEXT NOT NULL DEFAULT '',
  project_id TEXT,
  task_id TEXT,
  status TEXT NOT NULL DEFAULT '未开始',
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_schedules_date ON schedules (date);
CREATE INDEX IF NOT EXISTS idx_schedules_project_id ON schedules (project_id);

-- pm_write confirmation kind(照抄 0011 的 copy→drop→rename 重建):
CREATE TABLE agent_confirmation_candidates_v12 (
  confirmation_token TEXT PRIMARY KEY,
  kind TEXT NOT NULL CHECK (kind IN ('knowledge_write', 'destructive_action', 'deliverable_draft', 'exec_approval', 'fs_write', 'ingestion_batch', 'pm_write')),
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'confirmed', 'consumed', 'rejected')),
  params_hash TEXT NOT NULL,
  params_json TEXT NOT NULL,
  summary TEXT,
  session_id TEXT,
  created_at TEXT NOT NULL,
  expires_at TEXT NOT NULL,
  confirmed_at TEXT,
  consumed_at TEXT,
  rejected_at TEXT
);
INSERT INTO agent_confirmation_candidates_v12 SELECT * FROM agent_confirmation_candidates;
DROP TABLE agent_confirmation_candidates;
ALTER TABLE agent_confirmation_candidates_v12 RENAME TO agent_confirmation_candidates;
CREATE INDEX IF NOT EXISTS idx_confirmation_candidates_active ON agent_confirmation_candidates (kind, status, expires_at);
CREATE INDEX IF NOT EXISTS idx_confirmation_candidates_params_hash ON agent_confirmation_candidates (params_hash);

INSERT OR IGNORE INTO meta (key, value) VALUES ('schema_version', '12');
UPDATE meta SET value = '12' WHERE key = 'schema_version';
```

lib.rs:在 sql_migrations() 末尾(version 11 之后)加:
```rust
Migration {
    version: 12,
    description: "pm crud tables + confirmation kind pm_write",
    sql: include_str!("../migrations/0012_pm_crud.sql"),
    kind: MigrationKind::Up,
},
```

src/stores/storage/initializeDatabase.ts:`export const APP_SCHEMA_VERSION = 11` → `12`(27-04 同款 bump)。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib migration_registry_tests && cargo test -p nova-lib engine::db (全部 exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/migrations/0012_pm_crud.sql 存在且包含 `CREATE TABLE IF NOT EXISTS tasks`、`CREATE TABLE IF NOT EXISTS schedules`、`CREATE TABLE IF NOT EXISTS task_categories`、`'pm_write'`、`UPDATE meta SET value = '12'`
    - src-tauri/src/lib.rs 包含 `version: 12` 与 `include_str!("../migrations/0012_pm_crud.sql")`
    - grep -n "APP_SCHEMA_VERSION = " src/stores/storage/initializeDatabase.ts 输出 `= 12`
    - cd src-tauri && cargo test migration_registry_tests exit 0(注册守门测试不破)
  </acceptance_criteria>
  <done>0012 migration 写好并在 sql_migrations() 注册,cargo 测试全绿,APP_SCHEMA_VERSION=12</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pm_store.rs — SQL CRUD 层 + kv 一次性幂等搬移 + 启动钩子</name>
  <files>src-tauri/src/engine/pm_store.rs, src-tauri/src/engine/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/engine/ingest.rs (engine 内 SQL 模块的错误类型/测试惯例)
    - src-tauri/src/engine/db.rs (testing::mem_conn / run_migrations)
    - src-tauri/src/engine/event_log.rs (now_iso)
    - src-tauri/src/lib.rs (setup() spawned db open block — 搬移钩子插入点)
    - src/stores/taskStore.ts + src/stores/scheduleStore.ts (kv JSON 形状:migrate 函数解析 {state:{categories|events}})
  </read_first>
  <behavior>
    - Test 1: mem_conn 上 migrate_kv_pm_data:预插 kv_store key 'nova-task' = {"state":{"categories":[{"id":"cat-1","name":"待办","color":"bg-blue-500","tasks":[{"id":"t1","title":"写 PRD","priority":"high","status":"未开始","deadline":"2026-09-03"}]}]},"version":2} 与 'nova-schedule' = {"state":{"events":[{"id":"e1","title":"评审","time":"10:00","date":"2026-09-03","type":"meeting","location":"A"}]},"version":3} → 搬移后 SELECT tasks 含 id='t1' (priority='high', deadline='2026-09-03', category_id='cat-1'),task_categories 含 id='cat-1',schedules 含 id='e1' (status='未开始' 默认回填)
    - Test 2: 幂等 — 同一 kv 再跑一次 migrate_kv_pm_data,行数不变(INSERT OR IGNORE by id);再插一条 kv 里没有的新 task 后重跑,新行不丢
    - Test 3: kv key 不删 — 搬移后 SELECT value FROM kv_store WHERE key='nova-task' 仍存在
    - Test 4: meta 门闩 'pm_kv_migrated_v29' — 搬移后存在且为 'true';已置位时再调 migrate 直接 Ok 不读 kv
    - Test 5: 坏 JSON / 缺 key → Ok(1)(跳过),不 panic
    - Test 6: insert_task/list_tasks/delete_task、upsert_schedule/delete_schedule 基本往返(list 返回行含全字段)
  </behavior>
  <action>
新建 src-tauri/src/engine/pm_store.rs(Rust 唯一写者边界内),src-tauri/src/engine/mod.rs 加 `pub mod pm_store;`:

```rust
// src-tauri/src/engine/pm_store.rs
// Phase 29 (29-01) — task/schedule 关系表 SQL 层 + kv→关系表一次性幂等搬移。
// 29-02 的 9 个 PM 工具与启动搬移共用这一层。日期/时间全 TEXT 零转换。

use rusqlite::{params, Connection};
use serde_json::Value;
use crate::engine::event_log::now_iso;

pub type Error = Box<dyn std::error::Error>;
type Result<T> = std::result::Result<T, Error>;
```

公开函数(全部接 &Connection):
- `pub fn insert_task(conn: &Connection, t: &Value) -> Result<String>` — id 服务端生成(uuid,若 t.id 已给出则沿用),字段按 tasks 列名一一取值(缺省列用 DEFAULT 值同款字面量),INSERT OR IGNORE(by id 幂等),返回 id
- `pub fn update_task(conn, id: &str, updates: &Value) -> Result<bool>` — 只更新 updates 中出现的列(title/status/priority/deadline/description/project/project_id/assignee/assignee_avatar/time/category_id/scheduled_event_id 白名单,拼接 SET,updated_at=now_iso);行不存在返回 false
- `pub fn delete_task(conn, id: &str) -> Result<bool>` — DELETE tasks WHERE id;同时 `UPDATE schedules SET task_id = NULL WHERE task_id = ?1`(清反向弱链,对齐 taskStore.deleteTask 的 clearTaskLink 语义)
- `pub fn list_tasks(conn, filters: &Value) -> Result<Vec<Value>>` — filters 支持 status/project_id/deadline(=)/limit(默认 50,学 knowledge_search MAX_ARTICLES);每行映射回 TS Task camelCase 形状(id/title/status/priority/deadline/description/project/projectId/assignee/assigneeAvatar/time/categoryId/scheduledEventId)
- `pub fn upsert_schedule_from_json(conn, e: &Value) -> Result<String>` / `pub fn update_schedule(conn, id, updates)` / `pub fn delete_schedule(conn, id)` / `pub fn list_schedules(conn, filters)` — 同构;schedules 列映射回 ScheduleEvent camelCase(projectId/taskId/status)
- `pub fn migrate_kv_pm_data(conn: &Connection) -> Result<()>` — 门闩实现:
  1. `SELECT value FROM meta WHERE key='pm_kv_migrated_v29'` 已是 'true' → return Ok(())
  2. 读 kv_store 'nova-task':serde_json 解析 `["state"]["categories"]`,遍历 categories(INSERT OR IGNORE task_categories)与其中 tasks(缺省字段回填:status='未开始', priority='medium', deadline='', …;categoryId = 所属 category.id)
  3. 读 'nova-schedule':解析 `["state"]["events"]`,逐条 upsert(status 缺省 '未开始')
  4. 任何一步解析失败(缺 key/坏 JSON)→ 记 eprintln 并跳过该源,不返回 Err
  5. 全部完成后 `INSERT OR REPLACE INTO meta (key,value) VALUES ('pm_kv_migrated_v29','true')`
  6. kv 行原样保留,不删不写(per CONTEXT「kv key 备份保留不删」)

lib.rs setup():在 spawned async block 中 `engine::db::open` 成功、`restore_latest_session` 之后、`*db.0.lock().unwrap() = Some(conn)` 之前,插:
```rust
if let Err(e) = engine::pm_store::migrate_kv_pm_data(&conn) {
    eprintln!("[engine] pm kv migration failed: {e}");
}
```
(失败只打日志不阻断启动 — 下次启动门闩未置位会重试,幂等安全。)

测试放 pm_store.rs `#[cfg(test)] mod tests`,用 `crate::engine::db::testing::mem_conn`(自动跑全部 migrations 含 0012),按 <behavior> 六条写。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test pm_store exit 0</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/engine/pm_store.rs 存在,grep -c "#\[test\]" ≥ 6
    - grep -n "pm_kv_migrated_v29\|INSERT OR IGNORE\|nova-task\|nova-schedule" src-tauri/src/engine/pm_store.rs 全部命中
    - grep -n "migrate_kv_pm_data" src-tauri/src/lib.rs 命中(启动钩子接上)
    - grep -n "UPDATE schedules SET task_id = NULL" src-tauri/src/engine/pm_store.rs 命中(delete_task 反向弱链清理)
    - cargo test pm_store exit 0,六条 behavior 全过
    - cargo test --lib 全绿(无回归)
  </acceptance_criteria>
  <done>pm_store SQL 层 + 幂等 kv 搬移 + 启动钩子落地,cargo 测试全绿</done>
</task>

</tasks>

<verification>
- cd src-tauri && cargo test --lib 全绿
- npm run lint (tsc --noEmit) exit 0
- 0012 文件 + lib.rs version 12 注册 + APP_SCHEMA_VERSION=12 三处一致
</verification>

<success_criteria>
- tasks/schedules/task_categories 三表 + pm_write kind 就绪(schema_version 12)
- kv JSON → 关系表搬移幂等、kv 保留、门闩防重
- pm_store CRUD 帮助函数带测试,29-02 工具层可直接调用
</success_criteria>

<output>
After completion, create `.planning/phases/29-pm-crud/29-01-SUMMARY.md`
</output>
