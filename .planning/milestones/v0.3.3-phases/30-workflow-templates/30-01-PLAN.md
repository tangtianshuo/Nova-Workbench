---
phase: 30-workflow-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/data/deliverables-catalog.json, src/data/deliverableCatalog.ts, tsconfig.json, src/data/mockRndData.ts, src/ai/confirmations.ts, src/stores/rndStore.ts, src/components/product/FullDeliverablesTab.tsx, src/ai/tools/generateDeliverable.ts, src-tauri/src/engine/tools.rs]
autonomous: true
requirements: [SC-4]
must_haves:
  truths:
    - "产研各 tab 与 generate_deliverable 的 catalog 全部来自单一 JSON 文件(TS import 与 Rust include_str! 同一文件)"
    - "mockRndData.ts 中 FULL_LIFECYCLE_DELIVERABLES_CATALOG 硬编码退役,grep 零残留"
    - "Rust 与 TS 对 18 个 DEL-* code 的认知由 Rust 单测 + 既有双侧测试锁定"
  artifacts:
    - path: "src/data/deliverables-catalog.json"
      provides: "18 交付物 catalog 唯一真相源"
      contains: "DEL-REQ-01"
    - path: "src/data/deliverableCatalog.ts"
      provides: "带类型的 TS 侧 catalog 导出"
      exports: ["DELIVERABLES_CATALOG", "CatalogEntry"]
  key_links:
    - from: "src-tauri/src/engine/tools.rs"
      to: "src/data/deliverables-catalog.json"
      via: "include_str! + LazyLock serde_json 解析"
      pattern: "include_str!"
    - from: "src/ai/tools/generateDeliverable.ts"
      to: "src/data/deliverableCatalog.ts"
      via: "import DELIVERABLES_CATALOG"
      pattern: "DELIVERABLES_CATALOG"
---

<objective>
catalog 数据化(SC-4 / D-01 / D-02):把 18 交付物 catalog 从双侧硬编码(TS `SLOT_BY_CODE` + Rust `SLOT_BY_CODE` parity)抽成单一 JSON 文件,TS 与 Rust 同读一个文件。这是 999.4 隔离投资:换垂类只换 JSON,Rust 零改动。

Pitfall #1(最高风险)约束:严禁「JSON 归 TS、Rust 保留硬编码」——那只是把双源换了位置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-workflow-templates/30-CONTEXT.md
@.planning/phases/30-workflow-templates/30-RESEARCH.md

<interfaces>
现状消费方(全部要改到 JSON):
- src/ai/tools/generateDeliverable.ts:27 — TS `SLOT_BY_CODE`(prd 别名 + 18 DEL-*),:33 zod enum
- src/ai/confirmations.ts:298 — `DeliverableCode` 类型由 catalog 推导
- src/stores/rndStore.ts:56 — buildInitialDeliverables map catalog
- src/components/product/FullDeliverablesTab.tsx:43,112 — 列表渲染 + 多步 prompt
- src-tauri/src/engine/tools.rs:72 — Rust `const SLOT_BY_CODE: &[(&str,&str)]`,:93 slot_for_code,:274 registry code enum

已验证:`defaultContent: (product)=>string` 在 src/ 下除 mockRndData.ts 自身定义外零消费方(grep 实证)——直接退役,不做字符串模板降级。

tsconfig.json 当前无 `resolveJsonModule`(grep 实证)——Task 1 加。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 catalog JSON 单源 + TS 侧切换</name>
  <files>src/data/deliverables-catalog.json, src/data/deliverableCatalog.ts, tsconfig.json, src/data/mockRndData.ts, src/ai/confirmations.ts, src/stores/rndStore.ts, src/components/product/FullDeliverablesTab.tsx, src/ai/tools/generateDeliverable.ts</files>
  <read_first>src/data/mockRndData.ts (414-930 catalog 全文——JSON 条目逐字段照抄), src/ai/tools/generateDeliverable.ts, src/ai/confirmations.ts:290-305, src/stores/rndStore.ts:50-70, src/components/product/FullDeliverablesTab.tsx:40-125, tsconfig.json</read_first>
  <action>
1. `tsconfig.json` compilerOptions 加 `"resolveJsonModule": true`(Vite 层无碍,`npm run lint` tsc 需要)。
2. 创建 `src/data/deliverables-catalog.json`:JSON 数组,18 条,每条从 mockRndData.ts:424-930 逐条照抄字段 `code/phase/phaseName/title/category/format/icon/summary`(字符串原样,**丢弃 defaultContent 函数**)。第一条必须是 `{"code":"DEL-REQ-01","phase":"requirement",...}`。
3. 创建 `src/data/deliverableCatalog.ts`:
```ts
import catalogJson from './deliverables-catalog.json';
export interface CatalogEntry {
  code: string;
  phase: 'requirement' | 'design' | 'dev' | 'test' | 'release';
  phaseName: string; title: string; category: string;
  format: 'markdown' | 'json' | 'sql' | 'typescript' | 'table';
  icon: string; summary: string;
}
export const DELIVERABLES_CATALOG = catalogJson as CatalogEntry[];
```
4. 四个消费方切到 `DELIVERABLES_CATALOG`:
   - `generateDeliverable.ts`:SLOT_BY_CODE 改为 `{ prd: 'DEL-REQ-01', ...Object.fromEntries(DELIVERABLES_CATALOG.map(c => [c.code, c.code])) }`;zod enum 同理改。
   - `confirmations.ts:298`:`typeof FULL_LIFECYCLE_DELIVERABLES_CATALOG` 改 `typeof DELIVERABLES_CATALOG`。
   - `rndStore.ts:56`、`FullDeliverablesTab.tsx:43,112`:import 换源。
5. `mockRndData.ts`:删除 `FULL_LIFECYCLE_DELIVERABLES_CATALOG` 导出及其 18 条定义(其余 mock 导出不动);全仓 grep `FULL_LIFECYCLE_DELIVERABLES_CATALOG` 清零。
  </action>
  <verify>
    <automated>npm run lint && npm test && (grep -rn "FULL_LIFECYCLE_DELIVERABLES_CATALOG" src/ | wc -l) | grep -q "^0" && node -e "const c=require('./src/data/deliverables-catalog.json');console.assert(c.length===18&&c[0].code==='DEL-REQ-01')"</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json 含 `"resolveJsonModule": true`
    - src/data/deliverables-catalog.json 存在且为 18 元素数组,首条 code 为 DEL-REQ-01,无 defaultContent 键
    - grep `FULL_LIFECYCLE_DELIVERABLES_CATALOG` 在 src/ 下零命中
    - grep `defaultContent` 在 src/ 下零命中
    - `npm run lint` 退出码 0;`npm test` 全绿
  </acceptance_criteria>
  <done>TS 侧 catalog 唯一来源为 deliverables-catalog.json,mockRndData catalog 与 defaultContent 退役</done>
</task>

<task type="auto">
  <name>Task 2: Rust include_str! 单源 + 单测锁 parity</name>
  <files>src-tauri/src/engine/tools.rs</files>
  <read_first>src-tauri/src/engine/tools.rs (60-120 SLOT_BY_CODE/slot_for_code 区域、270-280 registry code enum、generate_deliverable ~1108), src-tauri/src/lib.rs:28 (include_str! 先例)</read_first>
  <action>
1. tools.rs 顶部:
```rust
use std::sync::LazyLock;
#[derive(serde::Deserialize)]
struct CatalogEntry { code: String }
static CATALOG: LazyLock<Vec<CatalogEntry>> = LazyLock::new(|| {
    serde_json::from_str(include_str!("../../../src/data/deliverables-catalog.json"))
        .expect("deliverables-catalog.json invalid")
});
```
2. 删除 `const SLOT_BY_CODE: &[(&str,&str)]`(tools.rs:72);`slot_for_code(code)`(:93)改为:`if code == "prd" { return Some("DEL-REQ-01") } CATALOG.iter().find(|e| e.code == code).map(|e| e.code.as_str())`。
3. registry 内 ToolSpec 的 code enum(:274)改为 `[&["prd"], CATALOG.iter().map(|e| e.code.as_str()).collect::<Vec<_>>().as_slice()].concat()` 形式——从解析结果生成,不得保留任何硬编码 DEL-* 字符串。
4. 加 `#[cfg(test)]` 单测:`assert CATALOG.len() == 18`、codes 去重后长度不变、包含 "DEL-REQ-01" 且不包含 "prd" 之外的别名键。
5. 跑 `cargo test`(既有 178+ 测试含 generate_deliverable 用例,验证 JSON 路径不回归)。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - tools.rs 含 `include_str!("../../../src/data/deliverables-catalog.json")`
    - tools.rs 不再有 `const SLOT_BY_CODE`
    - tools.rs 有 `LazyLock` 的 CATALOG static 与断言 18 条的单测
    - `cargo test` 全绿
  </acceptance_criteria>
  <done>Rust 与 TS 读同一 JSON,catalog 单源成立,双源漂移风险关闭</done>
</task>

</tasks>

<verification>
npm run lint + npm test + cargo test 全绿;两侧均无 catalog 硬编码残留。
</verification>

<success_criteria>
SC-4 的数据化部分完成(catalog JSON 只读层 + mockRndData 退役;用户层 SQLite 表在 30-02)。
</success_criteria>

<output>
After completion, create `.planning/phases/30-workflow-templates/30-01-SUMMARY.md`
</output>
