---
phase: 31-doc-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/MarkdownEditorInner.tsx, src/components/ui/MarkdownToolbar.tsx, src/components/ui/MarkdownEditor.tsx, src/index.css, package.json, patches/@mdxeditor+editor+4.2.0.patch]
autonomous: false
requirements: [DOC-05, DOC-06]

must_haves:
  truths:
    - "3 个既有使用点(KnowledgeBaseView/ProductKnowledgeTab/PrdDraftDialog)的 MarkdownEditor 外观与操作正常,底层已是 Milkdown"
    - "编辑器 toolbar 全 Nova tokens + Phosphor,无外部编辑器主题 CSS"
    - "仓库 grep 零 mdxeditor 残留"
  artifacts:
    - path: "src/components/ui/MarkdownEditorInner.tsx"
      provides: "Milkdown 受控编辑器(value/onChange/readOnly/placeholder/className/minHeight 契约不变)"
      contains: "useEditor"
    - path: "src/components/ui/MarkdownToolbar.tsx"
      provides: "tokens+Phosphor toolbar"
      contains: "callCommand"
  key_links:
    - from: "src/components/ui/MarkdownEditorInner.tsx"
      to: "@milkdown/kit/plugin/listener"
      via: "markdownUpdated -> onChange"
      pattern: "markdownUpdated"
    - from: "src/components/ui/MarkdownToolbar.tsx"
      to: "Milkdown instance"
      via: "useInstance + callCommand"
      pattern: "callCommand"
---

<objective>
Milkdown core headless 编辑器落地 + MDXEditor 全量退役(D-01/D-02/D-03/SC-5/SC-6)。

重写 `MarkdownEditorInner.tsx` 为 Milkdown 实现(保持受控 props 契约,3 个使用点零改动),自建 tokens+Phosphor toolbar,删除 @mdxeditor 依赖、vendored patch 与 token 覆盖 CSS 层。含真机 IME/round-trip PoC checkpoint(research Pitfall 2 前置)。

Output: Milkdown 编辑器组件 + toolbar + 退役收口
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-doc-workspace/31-CONTEXT.md
@.planning/phases/31-doc-workspace/31-RESEARCH.md
@.planning/phases/31-doc-workspace/31-UI-SPEC.md
@src/components/ui/MarkdownEditor.tsx
@src/components/ui/MarkdownEditorInner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Milkdown 受控编辑器重写 + toolbar</name>
  <files>src/components/ui/MarkdownEditorInner.tsx, src/components/ui/MarkdownToolbar.tsx, src/index.css</files>
  <read_first>
  - src/components/ui/MarkdownEditorInner.tsx (现 MDXEditor 实现 + MarkdownEditorProps/MarkdownEditorHandle 契约 + token 覆盖 CSS)
  - src/components/ui/MarkdownEditor.tsx (lazy 壳,零改动,确认契约消费方式)
  - .planning/phases/31-doc-workspace/31-RESEARCH.md (Pattern 1 受控封装 / Pattern 3 toolbar 命令 / Anti-Patterns)
  - src/index.css (样式层挂载点)
  </read_first>
  <action>
  1. `npm install @milkdown/kit@7.22.1 @milkdown/react@7.22.1`
  2. 重写 `MarkdownEditorInner.tsx`,**保持导出的 `MarkdownEditorProps`(value/onChange/readOnly/placeholder/className/minHeight)与 `MarkdownEditorHandle` 契约不变**,结构:
     - `MilkdownProvider > InnerEditor(useEditor)`,plugins: `commonmark` + `gfm` + `history` + `listener` + `clipboard`(全部 `@milkdown/kit/...` 子路径导入,禁止直接 npm i prosemirror-*)
     - 受控同步(Pattern 1,防回环):`lastEmitted` ref;`markdownUpdated` 回调更新 lastEmitted 并调 onChange(onChange 用 ref 桥接,不进 useEditor deps);`useEffect([value])` 中 `value !== lastEmitted.current` 时 `get()?.action(replaceAll(value, true))`(flush:true 清撤销栈,仅换文档时发生)
     - `useEditor` deps 仅 `[readOnly]`;placeholder 经 Milkdown 顶层 props 或 ProseMirror plugin 实现(以 7.22.1 .d.ts 为准,若需 plugin 则用最简 attributes 方案)
  3. 新建 `MarkdownToolbar.tsx`(仅编辑器内部,32px 条 `bg-bg-secondary border-b border-border-subtle`):按钮 `Button variant="ghost" size="sm"` h-7 + 图标 14 duotone + Tooltip,基线按钮集:Undo(ArrowCounterClockwise)/Redo(ArrowClockwise)、H1/H2/H3(text 按钮,turnIntoHeadingCommand 1/2/3)、Bold(TextB)/Italic(TextItalic)/Strikethrough(TextStrikethrough)/Code inline(Code)、Bullet(ListBullets)/Ordered(ListNumbers)/Quote(Quotes)/CodeBlock(CodeBlock)、Link(LinkSimple)/Table(Table,insertTableCommand 3x3)。命令执行 = `useInstance()` 取实例 → `ed.action(callCommand(key, payload))`;toggleLinkCommand 传 payload `{href: ''}`。**命令 key 名以 node_modules 内 7.22.1 .d.ts 实际导出为准**(research MEDIUM 项,发现名字不同以 .d.ts 为准)。toolbar 需在 readOnly 时禁用整条。组间用 `Separator` vertical。
  4. `src/index.css` 加 `.milkdown-editor` 样式层(唯一编辑器专属 CSS,预期 <80 行,UI-SPEC §3 排版契约):正文 `text-base text-text-primary` line-height 1.5、标题 600 weight(H1 20px/H2 17px/H3 15px)、`pre/code` `bg-bg-tertiary rounded-[var(--radius-md)] p-3 font-mono text-sm`、blockquote `border-l-2 border-border-subtle pl-3 text-text-secondary`、表格 `border-border-subtle` 分隔线、链接 `text-accent`。**禁引入任何外部主题 CSS(nord/prism),禁 hex 色值**。删除原 MarkdownEditorInner.tsx 中 MDXEditor 的 token 覆盖 CSS 层。
  </action>
  <verify>
    <automated>npm run lint && grep -c "milkdown" src/components/ui/MarkdownEditorInner.tsx</automated>
  </verify>
  <acceptance_criteria>
  - MarkdownEditorInner.tsx 包含 `useEditor`、`markdownUpdated`、`replaceAll`、`lastEmitted`
  - MarkdownEditorInner.tsx 导出 `MarkdownEditorProps` 与 `MarkdownEditorHandle`,props 含 value/onChange/readOnly/placeholder/className/minHeight
  - MarkdownEditorInner.tsx 无 `@mdxeditor` import
  - MarkdownToolbar.tsx 包含 `callCommand`、`useInstance`、`insertTableCommand`(或 .d.ts 实际等价 key)
  - src/index.css 包含 `.milkdown-editor`,不包含 `nord`/`prism`
  - `npm run lint` exit 0;`npm run build` 成功且输出 chunk 报表中新编辑器 chunk min+gzip 不超过 300KB(对齐 research PoC 项④)
  </acceptance_criteria>
  <done>Milkdown 编辑器以旧契约工作,toolbar 全 tokens+Phosphor,lint/build 绿</done>
</task>

<task type="auto">
  <name>Task 2: MDXEditor 全量退役收口</name>
  <files>package.json, patches/@mdxeditor+editor+4.2.0.patch</files>
  <read_first>
  - package.json (dependencies + postinstall script)
  - patches/ 目录(确认仅此一个 patch)
  </read_first>
  <action>
  1. `npm uninstall @mdxeditor/editor`
  2. 删除 `patches/@mdxeditor+editor+4.2.0.patch`;`ls patches/` 后:目录为空 → 删 package.json `postinstall` script 并 `npm uninstall patch-package`;若仍有其它 patch → 保留 postinstall
  3. 全仓 grep `@mdxeditor|mdxeditor|patch-package`(排除 node_modules/.planning)确认零残留;若 PrdDraftDialog 等直接 import 了 MDXEditor 类型/组件,改为经 MarkdownEditor 契约(research 确认三使用点均走单一入口,预期零改动,若有漏网直连点一并迁移)
  </action>
  <verify>
    <automated>grep -ri "mdxeditor" --include="*.ts" --include="*.tsx" --include="*.json" src/ server.ts package.json | grep -v node_modules; npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - grep `mdxeditor` 于 src/ 与 package.json 零命中(命令输出为空)
  - `patches/` 无 `@mdxeditor+editor+4.2.0.patch`
  - package.json dependencies 无 `@mdxeditor/editor`,dependencies/devDependencies 含 `@milkdown/kit` 与 `@milkdown/react` 7.22.1
  - `npm run lint` exit 0
  </acceptance_criteria>
  <done>仓库零双编辑器,依赖与 patch 全清(D-02/SC-5)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task: 真机 PoC / UAT 人工验证</name>
  <action>人工验证 checkpoint(自动化已由前置任务完成)</action>
  <what-built>Milkdown 编辑器 + toolbar,MDXEditor 已退役;3 个使用点(KnowledgeBaseView / ProductKnowledgeTab / PrdDraftDialog)底层换轨</what-built>
  <how-to-verify>
  `npm run dev` 后逐项验证(research Pitfall 2 PoC 四场景 + round-trip):
  1. 知识库任一文档点编辑:中文输入法四场景 — 空行首字不吞、选中后重输正常、composition 中粘贴不破坏输入法、表格内拼音输入正常
  2. 插入 3x3 表格填中文、插入代码块粘贴代码,保存后重新打开:表格结构与代码块 round-trip 无损
  3. toolbar 各按钮(H1-H3/粗斜删/列表/引用/链接/表格/代码/undo/redo)逐一生效
  4. 外观:编辑器无 MDXEditor 痕迹、无外部主题 CSS,全 tokens 风格
  5. 切换文档后 undo 不会回到上一文档内容(flush:true 生效)
  若 IME 四场景任一失败:停,回报现象(research 裁定:不过则降级排期再议)
  </how-to-verify>
  <resume-signal>Type "approved" 或描述失败场景</resume-signal>
</task>

</tasks>

<verification>
- npm run lint / npm run build 绿
- grep mdxeditor 零残留
- 编辑器 chunk 体积记录到 SUMMARY(PoC 项④)
</verification>

<success_criteria>
Milkdown 以受控契约运行,IME/round-trip 真机 PoC 通过,MDXEditor 依赖+patch+CSS 覆盖层全删(SC-5/SC-6)
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-01-SUMMARY.md`
</output>
