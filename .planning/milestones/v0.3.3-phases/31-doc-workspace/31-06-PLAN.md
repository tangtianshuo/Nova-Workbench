---
phase: 31-doc-workspace
plan: 06
type: execute
wave: 2
depends_on: [31-03, 31-04]
files_modified: [src/components/workspace/DocWorkspaceShell.tsx, src/App.tsx, src/stores/uiStore.ts, src/components/workspace/DocWorkspaceContent.tsx, src/views/KnowledgeBaseView.tsx, src/components/product/ProductKnowledgeTab.tsx, src/components/PrdDraftDialog.tsx, src/components/workspace/DocList.tsx]
autonomous: true
requirements: [DOC-01, DOC-03, DOC-04]

must_haves:
  truths:
    - "主工作区点击文档,右侧面板以 overlay 滑出,主工作区窗体大小不变(不被挤压)"
    - "overlay 面板保持打开直到手动收起,点击主工作区不收起、不抢焦点"
    - "禅模式隐藏主工作区内容区,左侧导航保留,编辑区占满主工作区位置"
    - "全部文档预览/编辑入口(知识库/产品知识 tab/PRD 草稿)移交右侧工作区,面板内无文档列表"
    - "新建笔记入口在知识库视图 header,点击即在右侧面板打开空白笔记"
  artifacts:
    - path: "src/components/workspace/DocWorkspaceShell.tsx"
      provides: "overlay 面板 + 禅模式布局"
      contains: "zen"
    - path: "src/views/KnowledgeBaseView.tsx"
      provides: "主工作区联动入口 + 新建笔记按钮"
      contains: "新建笔记"
  key_links:
    - from: "src/views/KnowledgeBaseView.tsx"
      to: "src/stores/docWorkspaceStore.ts"
      via: "点击文档 openDoc 右侧弹出"
      pattern: "openDoc"
    - from: "src/components/product/ProductKnowledgeTab.tsx"
      to: "src/stores/docWorkspaceStore.ts"
      via: "编辑按钮改 openDoc"
      pattern: "openDoc"
---

<objective>
UAT 反馈驱动的 UX 调整(D-09..D-13,2026-09-03 用户裁定):文档工作区改 overlay 联动模式 + 禅模式 + 全入口移交。

核心变化:右侧面板从 flex 布局挤压改为向右滑出的 overlay(主工作区大小不变);面板瘦身只留预览+编辑(+SC-3 确认卡);主工作区所有文档点击行为联动弹出面板;禅模式隐藏主工作区。

Output: 主工作区 ↔ 文档工作区联动 + 禅模式可用,面板零列表
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-doc-workspace/31-CONTEXT.md
@.planning/phases/31-doc-workspace/31-UI-SPEC.md
@.planning/phases/31-doc-workspace/31-03-SUMMARY.md
@.planning/phases/31-doc-workspace/31-04-SUMMARY.md
</context>

<decisions>
用户 2026-09-03 UAT 中段裁定(追加为 CONTEXT D-09..D-13):
- D-09 入口移交:全部文档类预览/编辑移交右侧工作区(知识库视图、产品知识 tab、PRD 草稿对话框)
- D-10 面板瘦身:移除面板内文档列表/文档名索引,只保留预览与编辑(SC-3 确认卡保留)
- D-11 overlay 弹出:向右滑出不挤压主工作区;保持打开,手动收起;点击主工作区不收起不抢焦点;拖宽保留
- D-12 禅模式:隐藏主工作区内容区,左侧导航 Sidebar 保留,编辑区占主工作区位置;工具栏切换按钮进出
- D-13 新建笔记入口:知识库视图 header 按钮(非面板内)
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Shell overlay 化 + 禅模式</name>
  <files>src/components/workspace/DocWorkspaceShell.tsx, src/App.tsx, src/stores/uiStore.ts</files>
  <read_first>
  - src/components/workspace/DocWorkspaceShell.tsx (31-03 现状:flex aside + 拖宽 clamp 360–60vw + 收起 rail)
  - src/App.tsx (MainLayout 挂载点 135 行附近 DocWorkspaceShell 包裹结构)
  - src/stores/uiStore.ts (docWorkspaceOpen/docWorkspaceWidth persist 字段)
  - src/components/ui/Drawer.tsx (side/modal passthrough 先例,motion 滑入动画模式)
  </read_first>
  <action>
  1. `DocWorkspaceShell.tsx` 重构为 overlay:
     - 定位改为绝对覆盖右侧(`absolute inset-y-0 right-0` 或 fixed,以 App 布局容器为准),不再参与 flex 分配 — 主工作区宽度恒定不挤压
     - 滑入动画沿用 motion(`initial={{ x: '100%' }} animate={{ x: 0 }}` 先例参考 Drawer),z-index 高于主内容、低于全局 Dialog
     - 左缘阴影区分层次(`shadow-*` token 或等价 border);拖宽 clamp 保留(360–60vw),收起按钮与收起 rail 保留(D-11 手动收起)
     - 不拦截主工作区点击:面板容器不铺全屏遮罩,无 backdrop
  2. `uiStore.ts` 新增 `docZenMode: boolean`(persist)+ `toggleDocZenMode()`。
  3. 禅模式(D-12):`docZenMode === true` 时主工作区内容区不渲染(或 `hidden`),DocWorkspaceShell 面板宽度占满主工作区区域(`w-full` 或 left: 导航栏右缘起);左侧导航 Sidebar 保留;App.tsx 布局相应分支。
  4. 面板工具栏加禅模式切换按钮(Phosphor 图标,如 ArrowsInLineHorizontal/ArrowsOutLineHorizontal 或 CornersOut,size 14,aria-label「禅模式」/「退出禅模式」)。
  </action>
  <verify>
    <automated>npm run lint && grep -q "docZenMode" src/stores/uiStore.ts && grep -q "absolute\|fixed" src/components/workspace/DocWorkspaceShell.tsx</automated>
  </verify>
  <acceptance_criteria>
  - DocWorkspaceShell 不再用 flex aside 挤压主内容;grep 含定位类(absolute/fixed)
  - uiStore 含 docZenMode + toggleDocZenMode,且 persist 字段列表包含 docZenMode
  - 面板打开时主工作区宽度不变(无 flex 收缩);收起 rail 与拖宽仍可用
  - 禅模式开:主工作区内容隐藏、编辑区占满(导航保留);关:恢复;状态重启保持
  - `npm run lint` exit 0
  </acceptance_criteria>
  <done>overlay 联动 + 禅模式布局成立,主工作区零挤压</done>
</task>

<task type="auto">
  <name>Task 2: 面板瘦身 + 全入口移交</name>
  <files>src/components/workspace/DocWorkspaceContent.tsx, src/views/KnowledgeBaseView.tsx, src/components/product/ProductKnowledgeTab.tsx, src/components/PrdDraftDialog.tsx, src/components/workspace/DocList.tsx</files>
  <read_first>
  - src/components/workspace/DocWorkspaceContent.tsx (现状:确认卡 + 折叠 DocList + 编辑器)
  - src/views/KnowledgeBaseView.tsx (472 行附近「编辑」按钮 + 506 行内联 MarkdownEditor)
  - src/components/product/ProductKnowledgeTab.tsx (379 行「编辑知识条目」+ 420/516 两处内联 MarkdownEditor)
  - src/components/PrdDraftDialog.tsx + 其调用方(src/components/AgentConsole.tsx, src/components/rnd/TabRunPanel.tsx, src/components/workspace/IngestionPanel.tsx)
  - src/stores/docWorkspaceStore.ts (openDoc/createNote 签名与行为)
  - src/stores/uiStore.ts (docWorkspaceOpen,展开面板所需)
  </read_first>
  <action>
  1. 面板瘦身(D-10):`DocWorkspaceContent.tsx` 移除文档列表区块(含折叠按钮/DocList 引用),保留 WorkspaceConfirmCard(顶部)+ 编辑器 + 保存状态指示;未打开文档空态文案改为「在主工作区点击文档打开,或在知识库新建笔记」;`DocList.tsx` 删除(grep 确认无引用残留)。
  2. 知识库视图(KnowledgeBaseView):
     - 文档行点击(或现有「编辑」按钮)改为 `openDoc(doc)` + 展开面板(uiStore docWorkspaceOpen=true);移除内联 MarkdownEditor 编辑区(预览摘要可保留在视图内,完整预览/编辑走面板)
     - header 加「新建笔记」`Button variant="primary" size="sm"`(Plus 14):调 `createNote('无标题笔记')` 并展开面板(D-13)
  3. 产品知识 tab(ProductKnowledgeTab):两处内联 MarkdownEditor(420/516)同款处理 — 「编辑知识条目」按钮改 openDoc + 展开面板,移除内联编辑区。
  4. PrdDraftDialog(D-09):读调用方确认对话框实际结构 — 若其主体即文档查看/编辑,则调用方点击改 openDoc(草稿 doc 存在 knowledge_docs 时直接 open;若是内存草稿先落库再 open,以现有数据流为准),PrdDraftDialog 删除;若含独立工作流动作(非文档编辑),保留动作但内容预览移交面板,并在 SUMMARY 记录裁定。
  5. CONTEXT.md 追加 D-09..D-13(标注 2026-09-03 UAT 裁定)。
  </action>
  <verify>
    <automated>npm run lint && ! test -f src/components/workspace/DocList.tsx && grep -c "openDoc" src/views/KnowledgeBaseView.tsx && grep -c "新建笔记" src/views/KnowledgeBaseView.tsx && grep -c "openDoc" src/components/product/ProductKnowledgeTab.tsx</automated>
  </verify>
  <acceptance_criteria>
  - DocList.tsx 已删除且全仓 grep 无 `DocList` 引用残留(除 .planning 文档)
  - KnowledgeBaseView 含「新建笔记」按钮与 openDoc 调用;内联 MarkdownEditor import 移除
  - ProductKnowledgeTab 含 openDoc 调用;内联 MarkdownEditor import 移除
  - 面板内无文档列表/文档名索引区块;空态文案为「在主工作区点击文档打开,或在知识库新建笔记」
  - PrdDraftDialog 处置有明确裁定且调用方行为不 broken(删除或保留的依据写入 SUMMARY)
  - `npm run lint` exit 0;`npm run build` exit 0
  </acceptance_criteria>
  <done>全应用文档预览/编辑统一收敛到右侧工作区,主工作区成为入口面</done>
</task>

</tasks>

<verification>
- npm run lint 绿 + npm run build 绿
- 真机:知识库点文档 → 面板右滑 overlay 主界面不挤压;面板保持打开,点主工作区不收起;禅模式进出;知识库新建笔记 → 面板打开空白笔记;产品知识 tab 编辑按钮 → 面板打开
</verification>

<success_criteria>
D-09..D-13 全部成立:overlay 联动不挤压、面板零列表、禅模式可用、全入口移交、笔记入口在知识库
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-06-SUMMARY.md`
</output>
