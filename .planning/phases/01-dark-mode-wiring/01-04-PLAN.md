---
phase: 01-dark-mode-wiring
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 03
files_modified:
  - src/components/ui/Card.tsx
  - src/components/product/AIRequirementsTab.tsx
  - src/components/product/CodeManagementTab.tsx
  - src/components/product/CompetitorAnalysisTab.tsx
  - src/components/product/FullDeliverablesTab.tsx
  - src/components/product/ProductGovernanceTab.tsx
  - src/components/product/ProductKnowledgeTab.tsx
  - src/components/product/ProductMilestonesTab.tsx
  - src/components/product/ProductSkillsTab.tsx
  - src/components/product/TestManagementTab.tsx
  - src/components/product/UIPrototypeTab.tsx
autonomous: false
requirements:
  - DARK-05
  - DARK-06

must_haves:
  truths:
    - "All 5 Card variants (default, elevated, glass, interactive, dark) render with correct contrast in dark mode"
    - "Card variant='dark' (hero panel) is visually distinct from the surrounding dark --bg-app — does NOT vanish"
    - "All 11 views render with correct text contrast in dark mode (no white-on-white, no invisible borders)"
    - "All 10 Card dark variant consumers in src/components/product/ have alpha overlays swapped to semantic tokens"
    - "3 product tabs with literal gradient overrides (CompetitorAnalysis amber, TestManagement teal, ProductSkills blue) retain per-tab accent identity while surviving dark mode"
    - "All P0 (unreadable/invisible) and P1 (WCAG AA fail) audit findings are fixed"
  artifacts:
    - path: "src/components/ui/Card.tsx"
      provides: "Reworked dark variant — accent-tinted gradient replacing literal slate-950"
      contains: "from-accent/20 via-accent-hover/10 to-bg-tertiary"
    - path: "src/components/product/AIRequirementsTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
    - path: "src/components/product/CodeManagementTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
    - path: "src/components/product/CompetitorAnalysisTab.tsx"
      provides: "Hero with literal slate-950 replaced by bg-bg-tertiary + amber accent retained"
      contains: "bg-bg-tertiary"
    - path: "src/components/product/FullDeliverablesTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
    - path: "src/components/product/ProductGovernanceTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
    - path: "src/components/product/ProductKnowledgeTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
    - path: "src/components/product/ProductMilestonesTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
    - path: "src/components/product/ProductSkillsTab.tsx"
      provides: "Hero with literal blue-600/indigo-600 reworked to bg-bg-tertiary + blue accent retained"
      contains: "bg-bg-tertiary"
    - path: "src/components/product/TestManagementTab.tsx"
      provides: "Hero with literal slate-950 replaced by bg-bg-tertiary + teal accent retained"
      contains: "bg-bg-tertiary"
    - path: "src/components/product/UIPrototypeTab.tsx"
      provides: "Card dark variant consumer with semantic-token overlays"
  key_links:
    - from: "src/components/ui/Card.tsx"
      to: "src/styles/tokens.css"
      via: "accent token references in dark variant class"
      pattern: "from-accent/20"
    - from: "src/components/product/*.tsx"
      to: "src/components/ui/Card.tsx"
      via: "Card variant=\"dark\" consumer with semantic overlays"
      pattern: "variant=\"dark\""
---

<objective>
Rework the Card `dark` variant from literal `from-slate-950 via-indigo-950/90` (which vanishes against dark `--bg-app`) to an accent-tinted gradient that survives both modes, then swap alpha overlays to semantic tokens in all 10 verified Card dark variant consumers (in `src/components/product/`), then run a manual dark-mode audit across all 47 components (20 UI primitives + 11 views + 16 product sub-components) and fix every P0/P1 finding. P2 items go to backlog.

Purpose: Satisfy DARK-05 (all Card variants correct in dark) and DARK-06 (all views + product components free of token gaps). The Card `dark` variant is the highest-risk hotspot (Pitfall 4) — fixing it cascades to consumer fixes in 10 product sub-components (Pitfall 5). Three of those sub-components have additional literal gradient overrides that bypass Card's variant class and need explicit per-tab handling.

Output: Card.tsx edited + 10 product sub-component consumer fixes (alpha overlay swaps + 3 literal-override reworks) + a documented audit trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-dark-mode-wiring/01-CONTEXT.md
@.planning/phases/01-dark-mode-wiring/01-RESEARCH.md
@.planning/phases/01-dark-mode-wiring/01-01-SUMMARY.md
@.planning/phases/01-dark-mode-wiring/01-03-SUMMARY.md

@src/components/ui/Card.tsx
@src/styles/tokens.css

<interfaces>
<!-- Current Card.tsx variantStyles (line 12-23): -->
```typescript
type Variant = 'default' | 'elevated' | 'glass' | 'interactive' | 'dark';
const variantStyles: Record<Variant, string> = {
  default:     'bg-bg-primary border border-border-subtle shadow-shadow-sm',
  elevated:    'bg-bg-primary border border-border-subtle shadow-shadow-md',
  glass:       'glass border border-white/20 shadow-shadow-glass',
  interactive: 'bg-bg-primary border border-border-subtle shadow-shadow-sm hover:shadow-shadow-md hover:border-border transition-all duration-normal cursor-pointer',
  dark:        'bg-gradient-to-r from-slate-950 via-indigo-950/90 to-slate-900 text-white border border-indigo-500/20 shadow-shadow-lg',
};
```

<!-- tokens.css dark palette (relevant tokens, lines 116-156): -->
```css
.dark {
  --accent: 211 100% 55%;
  --accent-hover: 211 100% 60%;
  --bg-app: 220 16% 8%;        /* near-black */
  --bg-tertiary: 220 12% 16%;  /* visibly lighter than bg-app */
  --text-primary: 0 0% 95%;
  --text-secondary: 220 6% 62%;
  --text-tertiary: 220 4% 46%;
  --border-subtle: (alias for border-secondary, 220 10% 14%);
}
```

<!-- Verified consumers (grep confirmed — 10 files in src/components/product/, ZERO in src/views/): -->
<!--
  AIRequirementsTab.tsx
  CodeManagementTab.tsx
  CompetitorAnalysisTab.tsx   — ALSO has literal gradient override (line 83)
  FullDeliverablesTab.tsx
  ProductGovernanceTab.tsx
  ProductKnowledgeTab.tsx
  ProductMilestonesTab.tsx
  ProductSkillsTab.tsx        — ALSO has literal gradient override (line 77)
  TestManagementTab.tsx       — ALSO has literal gradient override (line 109)
  UIPrototypeTab.tsx
-->

<!-- Literal gradient overrides (verified lines): -->
<!--
  CompetitorAnalysisTab.tsx:83:
    <Card variant="dark" className="bg-gradient-to-r from-amber-950 via-slate-900 to-slate-950 border-amber-500/20 p-6 md:p-8 space-y-6">
  TestManagementTab.tsx:109:
    <Card variant="dark" className="bg-gradient-to-r from-teal-950 via-slate-900 to-slate-950 border-teal-500/20 p-6 md:p-8 space-y-6">
  ProductSkillsTab.tsx:77:
    <Card variant="dark" className="bg-gradient-to-r from-blue-600 to-indigo-600 p-6 flex flex-col md:flex-row md:items-center justify-between gap-4">
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rework Card dark variant to accent-tinted gradient (fixes Pitfall 4)</name>
  <files>src/components/ui/Card.tsx</files>

  <read_first>
    - src/components/ui/Card.tsx (full 82 lines — variantStyles at line 12-23, focus on line 22 the `dark` entry)
    - src/styles/tokens.css (dark palette at lines 116-156 — confirm --accent, --bg-tertiary, --text-primary values)
    - .planning/phases/01-dark-mode-wiring/01-RESEARCH.md §5 (full Card dark variant rework reference + alternative, lines 609-651)
    - .planning/phases/01-dark-mode-wiring/01-RESEARCH.md Pitfall 4 (root cause — literal slate-950 against dark bg-app, lines 322-328)
    - .planning/phases/01-dark-mode-wiring/01-RESEARCH.md Pitfall 5 (text-white/alpha overlay vanishing — informs downstream consumer fixes in Task 2)
    - .planning/codebase/CONVENTIONS.md §"Styling" (semantic tokens are the rule — Card dark was the documented exception, this task retires that exception)
  </read_first>

  <action>
    Edit `src/components/ui/Card.tsx`. Replace ONLY the `dark` entry in the `variantStyles` map (line 22). Leave the other 4 variants and all other code unchanged.

    **Before (line 22):**
    ```typescript
    dark:
      'bg-gradient-to-r from-slate-950 via-indigo-950/90 to-slate-900 text-white border border-indigo-500/20 shadow-shadow-lg',
    ```

    **After:**
    ```typescript
    dark:
      'bg-gradient-to-r from-accent/20 via-accent-hover/10 to-bg-tertiary text-text-primary border border-accent/20 shadow-shadow-lg',
    ```

    **Why this works (per RESEARCH §5):**
    - Light mode: `from-accent/20` is subtle blue tint, `to-bg-tertiary` blends to warm-gray surface — hero panel retains premium feel.
    - Dark mode: `--accent: 211 100% 55%` (brighter in dark) at 20% opacity over dark surface = glowing hero panel. `to-bg-tertiary` (`220 12% 16%`) is visibly lighter than `--bg-app` (`220 16% 8%`) — restores visual separation that literal slate-950 lost.
    - `text-text-primary` (instead of `text-white`) — semantic token, swaps automatically.
    - `border-accent/20` (instead of `border-indigo-500/20`) — uses accent token, consistent across modes.

    **Key correctness rules:**
    - This is a 1-line change. DO NOT refactor anything else in Card.tsx.
    - DO NOT touch the `default`, `elevated`, `glass`, `interactive` variants — RESEARCH §1 confirms they already use semantic tokens correctly.
    - DO NOT remove `CardHover`, `CardHeader`, `CardContent`, `CardFooter` exports.
    - The literal-slate-950 exception in CONVENTIONS.md §"Styling" is now retired — the comment "Card dark variant uses literal gradients... special-case hero panels only" is no longer accurate. (Optional: leave the CONVENTIONS.md note alone — it's documentation, not load-bearing. Ponytail: skip the doc edit.)
  </action>

  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/ui/Card.tsx` line for `dark:` variant contains `from-accent/20`
    - `src/components/ui/Card.tsx` line for `dark:` variant contains `via-accent-hover/10`
    - `src/components/ui/Card.tsx` line for `dark:` variant contains `to-bg-tertiary`
    - `src/components/ui/Card.tsx` line for `dark:` variant contains `text-text-primary` (NOT `text-white`)
    - `src/components/ui/Card.tsx` line for `dark:` variant contains `border-accent/20` (NOT `border-indigo-500/20`)
    - `src/components/ui/Card.tsx` no longer contains `from-slate-950` (verify grep returns zero matches for `slate-950` in Card.tsx)
    - `src/components/ui/Card.tsx` no longer contains `via-indigo-950`
    - The other 4 variant lines (`default`, `elevated`, `glass`, `interactive`) are byte-identical to before (verify by reading lines 13-20)
    - `npm run lint` passes
  </acceptance_criteria>

  <done>
    Card dark variant swapped to accent-tinted gradient. Other variants untouched. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix all 10 Card dark variant consumers in src/components/product/ — alpha overlay swap + 3 literal-override reworks</name>
  <files>src/components/product/AIRequirementsTab.tsx, src/components/product/CodeManagementTab.tsx, src/components/product/CompetitorAnalysisTab.tsx, src/components/product/FullDeliverablesTab.tsx, src/components/product/ProductGovernanceTab.tsx, src/components/product/ProductKnowledgeTab.tsx, src/components/product/ProductMilestonesTab.tsx, src/components/product/ProductSkillsTab.tsx, src/components/product/TestManagementTab.tsx, src/components/product/UIPrototypeTab.tsx</files>

  <read_first>
    - Each of the 10 files listed in <files> — locate the `<Card variant="dark">` block (grep `variant="dark"` to find the exact line in each)
    - .planning/phases/01-dark-mode-wiring/01-RESEARCH.md §5 "Downstream consumer fixes" (full mapping table, lines 640-651)
    - .planning/phases/01-dark-mode-wiring/01-RESEARCH.md Pitfall 5 (why alpha overlays on literal-dark break on the new lighter accent gradient)
    - src/styles/tokens.css dark palette (confirm --text-secondary, --text-tertiary, --bg-secondary, --border-subtle values for the swap)
    - src/components/ui/Card.tsx (after Task 1 — verify the new dark variant class to understand what overlays will/won't work against it)
    - For the 3 literal-override files, the exact lines verified by grep:
      - CompetitorAnalysisTab.tsx:83 — `bg-gradient-to-r from-amber-950 via-slate-900 to-slate-950 border-amber-500/20`
      - TestManagementTab.tsx:109 — `bg-gradient-to-r from-teal-950 via-slate-900 to-slate-950 border-teal-500/20`
      - ProductSkillsTab.tsx:77 — `bg-gradient-to-r from-blue-600 to-indigo-600`
  </read_first>

  <action>
    This task edits 10 files. Work through them in three passes:

    **PASS A — Standard alpha-overlay swap (7 files):**

    Apply to: `AIRequirementsTab.tsx`, `CodeManagementTab.tsx`, `FullDeliverablesTab.tsx`, `ProductGovernanceTab.tsx`, `ProductKnowledgeTab.tsx`, `ProductMilestonesTab.tsx`, `UIPrototypeTab.tsx`.

    **Scope the edits to children of `<Card variant="dark">` ONLY.** DO NOT touch `text-white` elsewhere (e.g., `text-white` on `bg-accent` Button primary is correct in both modes per RESEARCH §4 "Acceptable text-white cases").

    Inside the Card variant="dark" block, apply these swaps:

    | Current class | New class | Why |
    |---------------|-----------|-----|
    | `text-white` (large headings, primary text) | `text-text-primary` | Token purity; new gradient is mid-tone, white still works but text-text-primary swaps correctly in both modes |
    | `text-white/60` (secondary labels, stat captions) | `text-text-secondary` | The /60 alpha was tuned for slate-950; on accent-tinted gradient it becomes too faint |
    | `text-white/40` (tertiary hints, metadata) | `text-text-tertiary` | Same reason, more aggressive fade |
    | `bg-white/10` (subtle highlight backgrounds, dividers) | `bg-bg-secondary` | Token-based surface tint |
    | `border-white/15` (subtle separators) | `border-border-subtle` | Token-based border |
    | `bg-white/5` (very subtle backgrounds) | `bg-bg-secondary/50` | Token with reduced opacity |

    Process per file:
    1. Read the file, grep `variant="dark"` to locate the Card block.
    2. For each className inside that block matching the table above, apply the swap.
    3. Verify edits compile (`npm run lint` after each file or in a batch at the end of Pass A).

    **PASS B — Literal-override reworks (3 files, retain per-tab accent identity):**

    Per Ponytail + RESEARCH D-10 intent: keep the per-tab accent (amber/teal/blue) so tabs retain visual identity, but replace the slate-950/indigo-600 base with `bg-bg-tertiary` so the hero survives dark mode. Then apply the alpha overlay swap table inside each block.

    **B1. `src/components/product/CompetitorAnalysisTab.tsx` (line 83):**
    Before:
    ```tsx
    <Card variant="dark" className="bg-gradient-to-r from-amber-950 via-slate-900 to-slate-950 border-amber-500/20 p-6 md:p-8 space-y-6">
    ```
    After:
    ```tsx
    <Card variant="dark" className="bg-gradient-to-r from-amber-500/20 via-amber-500/10 to-bg-tertiary border-amber-500/20 p-6 md:p-8 space-y-6">
    ```
    Why: amber-500 at 20% opacity over `bg-bg-tertiary` keeps the amber identity in both modes (light: subtle amber tint; dark: glowing amber hero). The slate-950 base is what vanished against dark `--bg-app`. Border stays amber to reinforce identity.
    Then apply the Pass A swap table to all child elements inside this Card (text-white/40, text-white/60, bg-white/10, border-white/15).

    **B2. `src/components/product/TestManagementTab.tsx` (line 109):**
    Before:
    ```tsx
    <Card variant="dark" className="bg-gradient-to-r from-teal-950 via-slate-900 to-slate-950 border-teal-500/20 p-6 md:p-8 space-y-6">
    ```
    After:
    ```tsx
    <Card variant="dark" className="bg-gradient-to-r from-teal-500/20 via-teal-500/10 to-bg-tertiary border-teal-500/20 p-6 md:p-8 space-y-6">
    ```
    Why: same pattern as B1 — teal-500 at 20% opacity over `bg-bg-tertiary` keeps teal identity in both modes. Then apply Pass A swap table to children.

    **B3. `src/components/product/ProductSkillsTab.tsx` (line 77):**
    Before:
    ```tsx
    <Card variant="dark" className="bg-gradient-to-r from-blue-600 to-indigo-600 p-6 flex flex-col md:flex-row md:items-center justify-between gap-4">
    ```
    After:
    ```tsx
    <Card variant="dark" className="bg-gradient-to-r from-blue-500/20 via-indigo-500/10 to-bg-tertiary border-blue-500/20 p-6 flex flex-col md:flex-row md:items-center justify-between gap-4">
    ```
    Why: blue-600/indigo-600 are full-saturation mid-tones that look fine in light mode but in dark mode they're overpowering + clash with surrounding dark surfaces. Switching to 20%/10% opacity over `bg-bg-tertiary` retains the blue→indigo identity while matching the new Card dark variant aesthetic. Added `border-blue-500/20` to keep a subtle edge (was missing before).
    Then apply Pass A swap table to children.

    **PASS C — Verify + SUMMARY:**

    1. Run `npm run lint` to confirm all 10 files compile.
    2. In the SUMMARY, enumerate EVERY edit by file + before/after line so post-phase verifier can grep-confirm (this is also the requirement for Task 3's audit-trail completeness).

    **Key correctness rules:**
    - DO NOT touch `text-white` on `<Button variant="primary">` or `<Button variant="danger">` — those are correct (white on accent/danger in both modes).
    - DO NOT change any structural/layout JSX — only className strings.
    - DO NOT introduce new imports.
    - The literal-override reworks (Pass B) intentionally keep per-tab accent (amber/teal/blue) — this is per Ponytail + D-10's "accent-tinted gradient" intent. Do NOT collapse them all to the global `--accent` blue; the per-tab color identity is a feature, not debt.
  </action>

  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Each of the 10 listed files: inside any Card variant="dark" block, zero occurrences of `text-white/60`, `text-white/40`, `bg-white/10`, `border-white/15` (verify by reading the Card block range in each file)
    - `text-white` (without alpha) MAY remain on large hero headings — it is acceptable on the new accent gradient (mid-tone, white still readable). The strict requirement is the alpha-overlay swaps.
    - CompetitorAnalysisTab.tsx:83 no longer contains `from-amber-950`, `via-slate-900`, `to-slate-950`
    - TestManagementTab.tsx:109 no longer contains `from-teal-950`, `via-slate-900`, `to-slate-950`
    - ProductSkillsTab.tsx:77 no longer contains `from-blue-600`, `to-indigo-600`
    - All 3 Pass B files: hero Card className now contains `bg-bg-tertiary` (either via `to-bg-tertiary` or as a base)
    - All 3 Pass B files: still reference their accent color (amber/teal/blue) at reduced opacity — identity preserved
    - All edits are inside Card variant="dark" blocks — Button primary/danger `text-white` is untouched
    - `npm run lint` passes
  </acceptance_criteria>

  <done>
    All 10 Card variant="dark" consumers in src/components/product/ updated. 7 standard swaps + 3 literal-override reworks complete. Per-tab accent identity preserved. No invisible alpha overlays on the new gradient. tsc passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Manual dark-mode audit across 47 components (D-09, D-11)</name>
  <action>Human verification checkpoint — no code action. See <what-built>, <how-to-verify>, and <resume-signal> below.</action>
  <what-built>
    Card dark variant reworked to accent-tinted gradient + 10 product sub-component consumer fixes (7 standard alpha-overlay swaps + 3 literal-override reworks retaining per-tab accent identity).
    What remains: a manual dark-mode audit across all 47 components per D-09, fixing every P0 (unreadable/invisible) and P1 (WCAG AA fail) finding.
  </what-built>

  <how-to-verify>
    Run `npm run dev` (web dev mode). Toggle dark mode via the new Header button (from Plan 02) or via Settings → 外观主题 → 深色.

    **Pre-audit: confirm the Card dark variant itself is fixed (DARK-05)**
    1. Navigate to ProductManagement view, select any product, click through each R&D-style tab (the ones that show hero banners — Overview, Skills, Milestones, Governance, AI Requirements, Full Deliverables, UI Prototype, Code Management, Test Management, Product Knowledge, Competitor Analysis).
    2. For each hero banner, verify: visible against dark background (not a black hole), text is readable, accent tint (blue for most, amber for CompetitorAnalysis, teal for TestManagement, blue→indigo for ProductSkills) is present.
    3. Toggle to light mode — verify the same hero banners still look acceptable.
    4. Toggle back to dark.

    **Audit pass — 20 UI primitives** (D-09). For each, navigate to a view that uses it, toggle dark mode, verify:
    - 1. Card (5 variants) — already covered above + default/elevated/glass/interactive in any view
    - 2. Button (4 variants) — Settings view (primary 保存修改, secondary 更换头像, ghost 删除, danger in 删除)
    - 3. Badge (6 variants) — ProductManagement or TaskManagement
    - 4. Dialog — Header → 新增任务 button
    - 5. Input — Settings account form
    - 6. Textarea — Settings 个人简介
    - 7. Select — Header 新增任务 dialog 任务分类
    - 8. Tabs — ProductManagement detail tabs
    - 9. Switch — Settings 桌面通知
    - 10. Checkbox — TaskManagement if present
    - 11. Tooltip — hover any item with tooltip
    - 12. Popover — find a popover trigger
    - 13. Toast — trigger a success/error toast (e.g., complete a task)
    - 14. Avatar — Settings account
    - 15. DropdownMenu — find a dropdown trigger
    - 16. ProgressBar — find a progress indicator
    - 17. SegmentedControl — Settings 外观主题 (from Plan 02)
    - 18. Separator — Settings view dividers
    - 19. Skeleton — verify loading state if any
    - 20. ScrollArea — main content scroll

    For each, check: text readable, borders visible, no white-on-white, no invisible elements.

    **Audit pass — 11 views** (D-09): Click through each sidebar tab — AgentWorkspace, TaskManagement, ProductManagement, RndCenter, Schedule, FileArchive, KnowledgeBase, Settings. Verify each renders correctly in dark mode.

    **Audit pass — 16 product sub-components** (D-09): In ProductManagement, select a product, click through each tab (Overview, Documents, Skills, Milestones, Governance, Analytics). In RndCenter, click through AI Requirements, Full Deliverables, UI Prototype, Code Management, Test Management, Product Knowledge, Competitor Analysis.

    **Triage findings (D-11):**
    - P0 (text unreadable / border invisible / all-black region): FIX in this task — record the file, the offending class, the fix applied.
    - P1 (contrast < WCAG AA): FIX in this task — same recording.
    - P2 (minor hue drift, hero aesthetic rework): document in the resume signal — goes to backlog.

    Resume with: either "approved" (zero P0/P1 findings or all fixed inline) OR a list of findings grouped by severity.
  </how-to-verify>

  <resume-signal>Type "approved" or list findings: P0: [file: class → fix], P1: [...], P2 (backlog): [...]</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply P0/P1 audit fixes surfaced by the checkpoint</name>
  <files>(dynamic — depends on checkpoint findings)</files>

  <read_first>
    - The list of P0/P1 findings from the checkpoint resume signal
    - For each finding: the offending file (Read it), the offending className, and the surrounding JSX context
    - src/styles/tokens.css (semantic token reference — pick the right replacement token)
    - .planning/phases/01-dark-mode-wiring/01-CONTEXT.md D-09 (audit scope), D-11 (P0/P1/P2 triage rules)
  </read_first>

  <action>
    For each P0/P1 finding from the checkpoint:

    1. **Read the offending file** at the specified line range.
    2. **Identify the bad class** — usually a literal color (`text-gray-500`, `bg-white`, `text-slate-900`, hex color) or a missing token (`text-black`, `bg-black`).
    3. **Pick the semantic replacement** from tokens.css:
       - `text-gray-500/600` → `text-text-secondary`
       - `text-gray-400/700/800` → `text-text-tertiary` or `text-text-primary` (depends on context)
       - `bg-white` (surface) → `bg-bg-primary`
       - `bg-gray-50/100` (subtle surface) → `bg-bg-secondary`
       - `bg-gray-200` → `bg-bg-tertiary`
       - `text-black` → `text-text-primary`
       - `border-gray-200` → `border-border-subtle`
       - hex colors → match HSL value to nearest token
    4. **Apply the edit** — only the className string, no structural changes.
    5. **Re-verify in dev mode** — toggle dark mode, confirm the fix resolves the issue.

    **Key rules:**
    - DO NOT touch `text-white` on `bg-accent` / `bg-danger` / `bg-success` backgrounds — those are correct in both modes (RESEARCH §4 "Acceptable text-white cases").
    - DO NOT refactor layouts, only className strings.
    - DO NOT add new design tokens — the existing palette is complete.
    - If a P0/P1 finding reveals a systemic issue (same bad pattern in many files), fix ALL occurrences — don't patch one and leave siblings broken (Ponytail root-cause rule).
    - Document each fix in the SUMMARY: file, line, before/after class, severity.
    - If the checkpoint returns "approved" with zero P0/P1 findings, skip this task entirely (mark as no-op in SUMMARY).
  </action>

  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - Every P0 finding from the checkpoint has a corresponding edit (verify by re-reading the file at the flagged line — the bad class is gone)
    - Every P1 finding has the same
    - All fixes use semantic token classes (no new literal colors introduced — verify: grep for `text-gray-`, `bg-gray-`, `text-black`, `bg-white ` in modified files shows no new occurrences)
    - P2 findings are documented in `.planning/phases/01-dark-mode-wiring/01-04-SUMMARY.md` backlog section
    - **SUMMARY must enumerate every fix's file path + line number + before/after className** so post-phase verifier can grep-confirm (non-blocking but required for audit trail completeness)
    - `npm run lint` passes
    - `npm run build` succeeds (full production build)
  </acceptance_criteria>

  <done>
    All P0/P1 findings from the manual audit are fixed and verified. P2 backlog documented. Every fix recorded in SUMMARY with file:line + before/after for post-phase grep verification. Build + tsc pass.
  </done>
</task>

</tasks>

<verification>
1. `npm run lint` passes after every task.
2. `npm run build` succeeds at the end of Task 3 (full production build).
3. Manual audit per the checkpoint covers all 47 components per D-09.
4. Re-verify the originally-flagged hotspots from RESEARCH §4 (the 10 product sub-components — especially AIRequirementsTab, UIPrototypeTab, FullDeliverablesTab, ProductGovernanceTab, plus the 3 literal-override tabs) — these are the highest-risk files per the 143 literal-palette grep result.
5. Grep-confirm Task 2 sweep: `grep -rn "from-slate-950\|via-indigo-950\|from-blue-600\|from-amber-950\|from-teal-950\|via-slate-900\|to-slate-950" src/components/product/` returns ZERO matches after Task 2.
6. Grep-confirm alpha-overlay sweep: `grep -rn "text-white/40\|text-white/60\|bg-white/10\|border-white/15" src/components/product/` returns ZERO matches inside Card variant="dark" blocks after Task 2.
</verification>

<success_criteria>
- Card `dark` variant uses accent-tinted gradient (Pitfall 4 resolved).
- All 10 Card dark variant consumers in src/components/product/ render correctly in both modes after overlay swaps + 3 literal-override reworks (Pitfall 5 resolved).
- Per-tab accent identity preserved (amber for CompetitorAnalysis, teal for TestManagement, blue→indigo for ProductSkills).
- Zero P0/P1 findings remain in dark mode across all 47 components.
- P2 backlog captured in SUMMARY for future polish.
- Every Task 3 fix recorded in SUMMARY with file:line + before/after.
- Build + lint pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dark-mode-wiring/01-04-SUMMARY.md`.
The SUMMARY MUST include:
- Card dark variant before/after class string.
- List of ALL 10 consumer files edited (the 7 standard swaps + the 3 literal-override reworks), each with the specific edits applied.
- For the 3 literal-override reworks: explicit before/after className on the Card line + which alpha-overlay swaps were applied to children.
- Audit findings table: Component | Severity | File:Line | Before → After | Status (fixed/backlogged).
- P2 backlog section (for future milestone).
- Post-phase grep commands + their results (zero matches for literal slate-950/blue-600/amber-950/teal-950 in src/components/product/; zero matches for alpha overlays inside Card variant="dark" blocks).
</output>
