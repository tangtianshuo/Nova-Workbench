---
phase: 03-tauri-ipc-migration-security-baseline
plan: 04
type: execute
wave: 4
depends_on: ["03-02"]
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/llm.json
  - server.ts
  - .planning/phases/03-tauri-ipc-migration-security-baseline/03-HUMAN-UAT.md
autonomous: false
requirements: [IPC-09, IPC-10, SEC-01, SEC-02, SEC-03, SEC-04]
must_haves:
  truths:
    - "tauri.conf.json has app.security.csp set to the corrected string with 'connect-src ... ipc: http://ipc.localhost ...' (NOT CONTEXT.md D-17 verbatim — RESEARCH.md Pattern 6 wins)"
    - "tauri.conf.json has app.security.devCsp set with Vite HMR compat (script-src 'unsafe-inline', connect-src localhost:3000 ws://localhost:3000)"
    - "capabilities/llm.json exists with permissions for all 4 Phase 3 commands"
    - "server.ts listen() binds to '127.0.0.1' explicitly (not '0.0.0.0' implicit)"
    - "npm run tauri:build completes without error (full production build)"
    - "Installed production build launches with NO CSP violations in DevTools console across all 7 views + all modals"
    - "Each of the 4 Phase 3 commands can be invoked from production DevTools console without silent permission rejection"
    - "03-HUMAN-UAT.md captures the manual smoke test checklist + results"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Production CSP + devCsp + beforeBuildCommand verified"
      contains: "ipc: http://ipc.localhost"
    - path: "src-tauri/capabilities/llm.json"
      provides: "Capability ACL for 4 Phase 3 commands"
      contains: "permissions"
    - path: "server.ts"
      provides: "Express bound to 127.0.0.1 (D-23)"
      contains: "'127.0.0.1'"
    - path: ".planning/phases/03-tauri-ipc-migration-security-baseline/03-HUMAN-UAT.md"
      provides: "Manual smoke test checklist + signed results"
      contains: "smoke"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/capabilities/llm.json"
      via: "Tauri loads all capability files at build time"
      pattern: "capabilities"
    - from: "src-tauri/capabilities/llm.json"
      to: "src-tauri/src/commands.rs"
      via: "permissions must match command identifiers"
      pattern: "permissions"
---

<objective>
Close the Phase 3 security perimeter in one coordinated landing:
1. Set production CSP (verbatim from RESEARCH.md §Pattern 6 — includes critical `ipc: http://ipc.localhost` connect-src that CONTEXT.md D-17 missed)
2. Set devCsp for Vite HMR compatibility
3. Create capabilities/llm.json with permissions for all 4 Phase 3 commands
4. Bind Express to 127.0.0.1 (D-23, one-line change)
5. Verify beforeBuildCommand — D-22 may already be satisfied (single command)
6. Run full production build (`npm run tauri:build`), install, and verify CSP doesn't break any view/modal (SEC-02 explicit)
7. Write 03-HUMAN-UAT.md with the smoke test checklist and signed results

Purpose: This wave MUST come last because CSP verification requires the commands from Wave 2 to exist (capabilities reference them) and the adapter from Wave 2 to be in place (smoke test invokes them). Doing CSP earlier would break prod build before commands exist.

Output: Production-grade Tauri app with real security perimeter; manual UAT artifacts documenting verification.
</objective>

<execution_context>
@.planning/phases/03-tauri-ipc-migration-security-baseline/03-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tauri-ipc-migration-security-baseline/03-02-PLAN.md
@src-tauri/tauri.conf.json
@src-tauri/capabilities/sql.json
@src-tauri/capabilities/default.json
@server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set production CSP + devCsp + verify beforeBuildCommand (D-22)</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>
    - D:\Projects\Nova\pm-workspace\src-tauri\tauri.conf.json (current state: csp is null, beforeBuildCommand is already "bunx vite build" — verify before changing)
    - D:\Projects\Nova\pm-workspace\.planning\phases\03-tauri-ipc-migration-security-baseline\03-RESEARCH.md (§Pattern 6 — corrected CSP VERBATIM with ipc: connect-src; §Pattern 7 — full tauri.conf.json snippet; §Open Question 4 — D-22 may already be satisfied)
    - D:\Projects\Nova\pm-workspace\.planning\research\PITFALLS.md (§Pitfall 7 — CSP tightening breaks Tailwind/Radix/motion inline styles → must keep style-src 'unsafe-inline'; must verify in tauri build, NOT dev)
  </read_first>
  <behavior>
    - tauri.conf.json app.security.csp = RESEARCH.md §Pattern 6 production string VERBATIM (must contain "ipc: http://ipc.localhost" — NOT D-17 verbatim)
    - tauri.conf.json app.security.devCsp = RESEARCH.md §Pattern 6 dev string VERBATIM (Vite HMR compat)
    - beforeBuildCommand: if already "bunx vite build", no change (D-22 zero-effort per RESEARCH.md Open Question 4); if anything else, change to "bunx vite build"
    - All other fields in tauri.conf.json preserved (productName, identifier, windows, plugins.sql, bundle, macOSPrivateApi)
  </behavior>
  <action>
Open src-tauri/tauri.conf.json. Make these EXACT changes:

=== 1. Replace the security block ===

FIND:
```json
"security": {
  "csp": null
},
```

REPLACE WITH (these are the EXACT strings from RESEARCH.md §Pattern 6 — production CSP has ipc: connect-src, devCsp has Vite HMR + ipc: connect-src):

```json
"security": {
  "csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost https://generativelanguage.googleapis.com; frame-src 'none'; object-src 'none';",
  "devCsp": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost http://localhost:3000 ws://localhost:3000 https://generativelanguage.googleapis.com; frame-src 'none'; object-src 'none';"
},
```

DO NOT use D-17 from CONTEXT.md verbatim — it is missing `ipc: http://ipc.localhost` in connect-src. Without this directive, `invoke()` silently fails CSP in production (works in dev because devCsp is permissive). RESEARCH.md §Pattern 6 + §Critical Finding document this correction.

=== 2. Verify beforeBuildCommand (D-22 zero-effort check) ===

FIND the `build` block. If it reads:
```json
"beforeBuildCommand": "bunx vite build",
```
→ D-22 is already satisfied. NO CHANGE NEEDED.

If it reads anything else (e.g. includes "bunx tsx server.ts"), change to:
```json
"beforeBuildCommand": "bunx vite build",
```

The current file (per Wave 0 read during planning) already shows `"beforeBuildCommand": "bunx vite build"` — so D-22 is zero-effort. Document this in 03-HUMAN-UAT.md.

=== 3. Leave everything else alone ===

Do NOT touch: productName, version, identifier, build.beforeDevCommand, build.devUrl, build.frontendDist, app.windows, app.macOSPrivateApi, plugins.sql, bundle.

Reference: RESEARCH.md §Pattern 6 (corrected CSP verbatim — NOT D-17), §Pattern 7 (full tauri.conf.json snippet), §Open Question 4 (D-22 verification), §Pitfall 1 (CSP works in dev, breaks in prod — ipc: connect-src is the fix), §Pitfall 6 (Tailwind v4 inline styles need style-src 'unsafe-inline').
  </action>
  <verify>
    <automated>grep "ipc: http://ipc.localhost" src-tauri/tauri.conf.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ipc: http://ipc.localhost" src-tauri/tauri.conf.json | wc -l` returns 2 (once in csp, once in devCsp)
    - `grep -c "csp.*null" src-tauri/tauri.conf.json` returns 0 (no more null CSP)
    - `grep "style-src 'self' 'unsafe-inline'" src-tauri/tauri.conf.json | wc -l` returns 2 (both csp and devCsp have it — Pitfall 7)
    - `grep "script-src 'self'" src-tauri/tauri.conf.json | wc -l` returns 1 (production has strict script-src)
    - `grep "script-src 'self' 'unsafe-inline'" src-tauri/tauri.conf.json | wc -l` returns 1 (devCsp has unsafe-inline for Vite HMR)
    - `grep "ws://localhost:3000" src-tauri/tauri.conf.json | wc -l` returns 1 (devCsp has Vite WS HMR)
    - `grep "https://generativelanguage.googleapis.com" src-tauri/tauri.conf.json | wc -l` returns 2 (both csp and devCsp allow Gemini)
    - `grep "beforeBuildCommand" src-tauri/tauri.conf.json` matches the line and shows "bunx vite build"
    - `grep -c "productName.*Nova" src-tauri/tauri.conf.json` returns 1 (other fields preserved)
    - `grep -c "macOSPrivateApi" src-tauri/tauri.conf.json` returns 1 (preserved)
    - `grep -c "frontendDist" src-tauri/tauri.conf.json` returns 1 (preserved)
  </acceptance_criteria>
  <done>Production CSP + devCsp set verbatim from RESEARCH.md §Pattern 6 (with ipc: connect-src correction); beforeBuildCommand verified single-command (D-22); all other tauri.conf.json fields preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create capabilities/llm.json + bind Express to 127.0.0.1</name>
  <files>src-tauri/capabilities/llm.json, server.ts</files>
  <read_first>
    - D:\Projects\Nova\pm-workspace\src-tauri\capabilities\sql.json (Phase 2 capability file pattern — same shape, same $schema, windows: ["main"])
    - D:\Projects\Nova\pm-workspace\src-tauri\capabilities\default.json (Phase 0 capability — core:default + window perms + shell:allow-open)
    - D:\Projects\Nova\pm-workspace\server.ts (line 259 — current `app.listen(PORT, "0.0.0.0", ...)` — D-23 single-line change)
    - D:\Projects\Nova\pm-workspace\.planning\phases\03-tauri-ipc-migration-security-baseline\03-RESEARCH.md (§Code Examples §Capability File llm.json; §Pitfall 2 — capability silent rejection, must verify against gen/schemas/desktop-schema.json post-build)
  </read_first>
  <behavior>
    - capabilities/llm.json exists with windows: ["main"], identifier: "llm", permissions array including all 4 Phase 3 commands
    - server.ts listen() binds to '127.0.0.1' explicitly
    - server.ts gets a one-line console warning at startup: "Dev-only server, not for production"
  </behavior>
  <action>
=== Create src-tauri/capabilities/llm.json ===

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "llm",
  "description": "AI generation IPC commands (Phase 3) — streaming + cancellation + keyring access",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "generate-project:allow",
    "cancel-generate-project:allow",
    "has-api-key:allow",
    "set-api-key:allow"
  ]
}
```

WARNING (RESEARCH.md §Pitfall 2 + §Open Question 3): The exact permission identifier format (`generate-project:allow` vs `core:allow-generate-project` vs other) MUST be verified against `src-tauri/gen/schemas/desktop-schema.json` after the first `npm run tauri:dev` post-change. That schema is auto-generated and authoritative. If the chosen identifiers don't match, capabilities silently reject at runtime. The Task 3 checkpoint verifies this — fix the identifiers in llm.json based on what desktop-schema.json actually contains before declaring done.

=== Modify server.ts line 259 ===

FIND:
```ts
app.listen(PORT, "0.0.0.0", () => {
  console.log(`Server running on http://localhost:${PORT}`);
});
```

REPLACE WITH:
```ts
// D-23: bind 127.0.0.1 only — dev fallback, never expose on LAN (CONCERNS.md HIGH).
app.listen(PORT, "127.0.0.1", () => {
  console.log(`Dev-only server running at http://127.0.0.1:${PORT} (not for production)`);
});
```

Do NOT touch any other line in server.ts. The 5 endpoints remain unchanged — they are dev fallback per D-21.

Reference: RESEARCH.md §Code Examples §Capability File (verbatim shape — windows: ["main"], $schema path matches sql.json), §Pitfall 2 (capability silent rejection), §Pattern 7 (server.ts bind change one-line), D-23.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; test -f src-tauri/capabilities/llm.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src-tauri/capabilities/llm.json` succeeds
    - `grep '"identifier": "llm"' src-tauri/capabilities/llm.json` matches
    - `grep '"windows": \["main"\]' src-tauri/capabilities/llm.json` matches
    - `grep 'generate-project:allow' src-tauri/capabilities/llm.json` matches
    - `grep 'cancel-generate-project:allow' src-tauri/capabilities/llm.json` matches
    - `grep 'has-api-key:allow' src-tauri/capabilities/llm.json` matches
    - `grep 'set-api-key:allow' src-tauri/capabilities/llm.json` matches
    - `grep "127.0.0.1" server.ts` matches (D-23 bind change)
    - `grep -c "0.0.0.0" server.ts` returns 0 (old bind removed)
    - `grep "not for production" server.ts` matches (dev warning added)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>capabilities/llm.json grants the 4 Phase 3 commands to the main window; Express dev server bound to 127.0.0.1 with explicit dev warning (D-21, D-23); all 4 Express endpoints preserved as dev fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify production build CSP + capability smoke test</name>
  <action>
Human verification of the production build with new CSP + capabilities — see <how-to-verify> for exact steps. This task pauses for user approval.
  </action>
  <what-built>
    Full Phase 3 security perimeter landed:
    1. Production CSP with ipc: connect-src fix (RESEARCH.md §Pattern 6)
    2. Dev CSP for Vite HMR
    3. Capability file for 4 Phase 3 commands
    4. Express bound to 127.0.0.1
    5. beforeBuildCommand verified (D-22 zero-effort confirmed or applied)
  </what-built>
  <how-to-verify>
    **Phase 3 production build smoke test (SEC-02 + SEC-04):**

    1. Verify capabilities/llm.json permission identifiers against the auto-generated schema:
       - Run `npm run tauri:dev` once (let it boot, then close)
       - Read `src-tauri/gen/schemas/desktop-schema.json` (or whatever filename Tauri generates in src-tauri/gen/schemas/)
       - Find the 4 command permission identifiers — confirm they match what's in capabilities/llm.json
       - If MISMATCH: edit capabilities/llm.json to use the actual identifiers from desktop-schema.json, then re-run `npm run tauri:dev` to confirm

    2. Run full production build:
       ```
       npm run tauri:build
       ```
       - Verify build completes without error (Pitfall 7 warning: first build after adding rig-core + tokio may take 5-10 minutes due to lto=true + opt-level=s — this is one-time cost)
       - Verify output `.exe` (or `.app`/AppImage) is created in `src-tauri/target/release/bundle/`

    3. Install and launch the built artifact:
       - Run the installer
       - Launch Nova from the Start Menu / Applications folder (NOT `npm run tauri:dev` — must be the prod build to exercise CSP)
       - Open DevTools (right-click → Inspect Element, or F12)

    4. CSP verification (SEC-02):
       - DevTools Console: NO red CSP violations during boot
       - Visit every view: Product / Tasks / R&D / Schedule / Files / Knowledge / Settings
       - At each view: DevTools Console shows NO CSP errors
       - Open every modal: Create Product (ProjectCreateModal), Workspace Summary, Add Document
       - At each modal: NO CSP errors

    5. IPC reachability smoke test (SEC-04):
       - In production DevTools console, paste each line one at a time and verify response:
         ```
         await window.__TAURI__.core.invoke('has_api_key')
         ```
         Expected: `true` (if key was set in Phase 3 Wave 3) or `false` (fresh install)
         ```
         await window.__TAURI__.core.invoke('set_api_key', { key: 'prod-test' })
         ```
         Expected: undefined (no throw)
         ```
         await window.__TAURI__.core.invoke('has_api_key')
         ```
         Expected: `true`
         ```
         await window.__TAURI__.core.invoke('cancel_generate_project', { requestId: 'nope' })
         ```
         Expected: null/undefined (idempotent, no throw — confirms capability granted)
       - If ANY invoke throws "permission denied" or returns nothing: capability file is wrong (Pitfall 2). Fix identifier, rebuild.

    6. Streaming works in production (CSP connect-src ipc: verified):
       - In production Nova: ProjectCreateModal → fill prompt → 生成项目计划
       - Verify tokens stream into 实时生成预览 (this confirms `connect-src ipc:` is allowing invoke in prod)

    7. Express bind verification (D-23, dev-only):
       - Stop prod build
       - Run `npm run dev`
       - Server log should say "Dev-only server running at http://127.0.0.1:3000 (not for production)"
       - From another machine on LAN: `curl http://<this-machine-ip>:3000/` → connection refused

    8. Document results in `.planning/phases/03-tauri-ipc-migration-security-baseline/03-HUMAN-UAT.md` (create the file if it doesn't exist). For each numbered step above, record PASS/FAIL with the actual DevTools output. Sign and date at the bottom.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass, OR describe which step failed + the actual DevTools/console output. Capability identifier mismatches (step 1) are easy to fix — just report what desktop-schema.json actually contains.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run lint` exits 0
2. `npm run tauri:build` completes without error
3. Installed production build launches without CSP violations
4. All 4 Phase 3 commands invoke from production DevTools without permission errors
5. Streaming works in production (CSP connect-src ipc: verified)
6. Express dev server bound to 127.0.0.1 only
7. 03-HUMAN-UAT.md exists with signed PASS/FAIL for each verification step
</verification>

<success_criteria>
- Production CSP set verbatim from RESEARCH.md §Pattern 6 (with ipc: connect-src fix — NOT D-17 verbatim)
- devCsp set for Vite HMR compat
- capabilities/llm.json grants 4 Phase 3 commands to main window (identifiers verified against desktop-schema.json)
- Express bound to 127.0.0.1 with explicit dev warning (D-23)
- beforeBuildCommand verified or changed to single "bunx vite build" (D-22)
- `npm run tauri:build` succeeds, no CSP violations in installed app
- All 4 commands reachable from production webview (no silent capability rejection — SEC-04)
- 03-HUMAN-UAT.md captures signed verification results
</success_criteria>

<output>
After completion, create `.planning/phases/03-tauri-ipc-migration-security-baseline/03-04-SUMMARY.md` documenting:
- Final CSP string used
- Final capability identifiers (matching desktop-schema.json)
- Production build artifact path
- Any deviations from RESEARCH.md
- Smoke test results summary
</output>
