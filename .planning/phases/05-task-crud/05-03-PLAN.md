---
phase: 05-task-crud
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/TaskDialog.tsx
  - src/views/TaskManagementView.tsx
autonomous: true
requirements: [TASK-02, TASK-03, TASK-04]

must_haves:
  truths:
    - "TaskDialog 支持创建 + 编辑双模式,通过 mode='create'|'edit' prop 区分 (D-08)"
    - "用户可以在 TaskDialog 中编辑任务的全部字段(标题/描述/优先级/截止日期/分类/状态/关联产品)"
    - "用户可以从 TaskDialog 删除任务,触发二次确认嵌套 Dialog,确认后任务从看板消失 (TASK-03)"
    - "用户可以在 TaskDialog 编辑模式把 '已完成' 任务改回 '未开始' (TASK-04)"
    - "产品选择器使用 Combobox (Popover + Input + 列表筛选),不是 Radix Select (D-09)"
    - "创建模式新建任务 ID 使用 crypto.randomUUID() (TASK-09)"
    - "更新关联产品时,setTaskProject 自动镜像 task.project = product.name (D-10, store 已实现)"
    - "TaskDialog open 转 true 时,所有字段从 task prop 同步重置 (Pitfall P13)"
  artifacts:
    - path: "src/components/TaskDialog.tsx"
      provides: "TaskDialog 创建/编辑双模式 + Combobox 产品选择器 + 嵌套删除确认"
      contains: "TaskDialog"
      min_lines: 200
    - path: "src/views/TaskManagementView.tsx"
      provides: "新建任务按钮触发 TaskDialog + 编辑入口"
      contains: "TaskDialog"
  key_links:
    - from: "src/components/TaskDialog.tsx"
      to: "src/stores/taskStore.ts"
      via: "useTaskStore 直接调用 addTask/updateTask/deleteTask/setTaskProject"
      pattern: "useTaskStore\\(\\(s\\) => s\\.(addTask|updateTask|deleteTask|setTaskProject)\\)"
    - from: "src/components/TaskDialog.tsx"
      to: "src/stores/productStore.ts"
      via: "useProductStore 读取产品列表供 Combobox"
      pattern: "useProductStore"
---

<objective>
新建 TaskDialog 组件,支持创建/编辑双模式 + Combobox 产品选择器 + 嵌套删除确认对话框。在 TaskManagementView 接入"新建任务"按钮和编辑入口。

Purpose: 交付 TASK-02 (对话框编辑)、TASK-03 (删除带确认)、TASK-04 (重新打开)。Combobox 是 D-09 锁定的产品选择器交互。
Output: TaskDialog.tsx 新建 + TaskManagementView 接入
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-task-crud/05-CONTEXT.md
@.planning/phases/05-task-crud/05-UI-SPEC.md

@src/components/TaskKanban.tsx
@src/views/TaskManagementView.tsx
@src/components/ui/Dialog.tsx
@src/components/ui/Popover.tsx
@src/components/ui/Input.tsx
@src/components/ui/Select.tsx
@src/components/ui/DatePickerInput.tsx
@src/components/ui/DropdownMenu.tsx
@src/components/ui/Button.tsx
@src/stores/taskStore.ts
@src/stores/productStore.ts

<interfaces>
<!-- Plan 01 完成后的 taskStore actions (直接 useTaskStore 调用) -->
```typescript
interface TaskState {
  categories: TaskCategory[];
  addTask: (task: Task, categoryId?: string) => void;
  updateTask: (taskId: string, updates: Partial<Task>) => void;
  deleteTask: (taskId: string) => void;
  reopenTask: (taskId: string) => void;
  setTaskProject: (taskId: string, projectId: string | undefined) => void;
  // ...
}
```

<!-- Plan 01 完成后的 Task 接口 -->
```typescript
export interface Task {
  id: string;
  title: string;
  priority: 'high' | 'medium' | 'low';
  time?: string;
  status: string;          // '未开始' | '进行中' | '已完成' (中文 literal,与 mock 数据一致)
  description: string;
  project: string;         // legacy,与 projectId 镜像 (D-10)
  projectId?: string;      // 新增 (Plan 01)
  scheduledEventId?: string; // 新增 (Plan 01,Phase 5 内不写入,Phase 7 使用)
  assignee: string;
  assigneeAvatar: string;
  deadline: string;        // 现有格式 'YYYY-MM-DD HH:mm'
  aiSuggestions: string[];
}
export interface TaskCategory {
  id: string;
  name: string;
  color: string;
  tasks: Task[];
}
```

<!-- 现有 Popover API (Combobox 基座) -->
```typescript
export const Popover = PopoverPrimitive.Root;
export const PopoverTrigger = PopoverPrimitive.Trigger;
export function PopoverContent({ className, align, sideOffset, ...props }) { ... }
// 默认 z-tooltip w-72 p-3,可 override className
```

<!-- 现有 Dialog API -->
```typescript
export const Dialog = DialogPrimitive.Root;
export function DialogContent({ className, children, ...props }) { ... }  // max-w-lg 默认
export function DialogHeader({ title, description, className, ...props }) { ... }
export function DialogBody({ className, ...props }) { ... }   // px-5 py-4 space-y-4
export function DialogFooter({ className, ...props }) { ... }
```

<!-- 现有 DatePickerInput API -->
```typescript
interface DatePickerInputProps {
  value?: string;        // YYYY-MM-DD
  onChange?: (date: string) => void;
  label?: string;
  placeholder?: string;
}
```

<!-- UI-SPEC 文案契约 (TaskDialog) -->
- 新建任务 / 编辑任务 (title, mode 切换)
- 快速记录一个新任务 / 编辑 {task.title} (description)
- 创建任务 / 保存修改 (CTA, mode 切换)
- 删除 (edit mode only, mr-auto)
- 搜索产品... / 未找到匹配的产品 (Combobox placeholder + empty)
- 任务标题 / 详细描述... (input placeholder)
- 取消 (cancel button)
- 删除任务? / 任务 "{title}" 将被永久删除,此操作无法撤销。 (删除确认)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 TaskDialog.tsx (创建/编辑双模式 + Combobox + 嵌套删除确认)</name>
  <files>src/components/TaskDialog.tsx</files>
  <read_first>
    - src/components/ui/Dialog.tsx (DialogContent/Header/Body/Footer API)
    - src/components/ui/Popover.tsx (PopoverTrigger/Content API, Combobox 基座)
    - src/components/ui/Input.tsx (Input/Textarea + label/icon/error API)
    - src/components/ui/Select.tsx (SelectTrigger/Content/Item API, Combobox trigger 复用样式)
    - src/components/ui/DatePickerInput.tsx (value/onChange/label API)
    - src/components/ui/Button.tsx (variant: primary/secondary/danger)
    - src/components/ui/Toast.tsx (useToast API,删除成功后 toast)
    - src/stores/taskStore.ts (Plan 01 后 5 个新 action 签名)
    - src/stores/productStore.ts (useProductStore selector + Product 类型)
    - src/data/mockTasks.ts (Task/TaskCategory 接口)
    - .planning/phases/05-task-crud/05-UI-SPEC.md §4 TaskDialog 章节 (字段顺序 + Combobox 实现)
  </read_first>
  <behavior>
    - Props: { open: boolean, onOpenChange: (open: boolean) => void, mode: 'create' | 'edit', task?: Task, defaultCategoryId?: string }
    - create 模式: 所有字段重置为默认值(title='', priority='medium', status='未开始', description='', deadline='', categoryId=defaultCategoryId ?? categories[0]?.id, projectId=undefined);autoFocus 在标题 Input
    - edit 模式: 所有字段从 task prop 同步
    - useEffect([open, mode, task?.id]): 当 open 从 false→true,重置所有字段 (Pitfall P13)
    - Combobox 实现: Popover + Input(搜索)+ 列表(button rows);case-insensitive substring filter on product.name
    - Combobox trigger 样式与 SelectTrigger 一致 (h-9 px-3 rounded-[var(--radius-md)] bg-bg-input border border-border)
    - Combobox trigger 显示已选产品名或 placeholder "搜索产品..."
    - Combobox trigger 内右侧有 X 清除按钮(仅当 projectId 已选),aria-label="清除产品关联"
    - Combobox PopoverContent: w-72 max-h-[240px] overflow-y-auto p-1,button rows "px-2.5 py-1.5 text-sm rounded-[var(--radius-sm)] hover:bg-bg-secondary",空状态 "未找到匹配的产品" 居中 text-xs text-text-tertiary py-6
    - 提交校验: title.trim() 非空,否则主按钮 disabled
    - create 模式提交: addTask({ id: crypto.randomUUID(), title, priority, status, description, deadline, project: projectId ? product.name : '', projectId, assignee: '', assigneeAvatar: '', aiSuggestions: [] }, categoryId)
    - edit 模式提交: updateTask(task.id, { title, priority, status, description, deadline, projectId });若 projectId 变了,setTaskProject(task.id, projectId) (store 处理 project 镜像)
    - 删除按钮(edit mode only): 放在 DialogFooter 左侧 mr-auto,variant="danger" size="sm" "删除",触发 showDeleteConfirm=true
    - 嵌套删除确认 Dialog: title="删除任务?" body=`任务 "${task.title}" 将被永久删除,此操作无法撤销。` 取消按钮 + 确认按钮(variant="danger" "删除"),确认后 deleteTask(task.id) + 关闭外层 TaskDialog + Toast success "任务已删除"
    - reopen 入口: edit 模式下 status Select 直接可选 '未开始'/'进行中'/'已完成',用户改 status='未开始' 即实现 reopen (TASK-04);不需要单独按钮
  </behavior>
  <action>
1. 创建 `src/components/TaskDialog.tsx`。完整骨架(注意所有 className / 文案 / 字段顺序严格按 UI-SPEC §4):

   ```tsx
   import { useState, useEffect, useMemo } from 'react';
   import { Trash, X, MagnifyingGlass } from '@phosphor-icons/react';
   import { Task, TaskCategory } from '@/src/data/mockTasks';
   import { useTaskStore } from '@/src/stores/taskStore';
   import { useProductStore } from '@/src/stores/productStore';
   import { useToast } from '@/src/components/ui/Toast';
   import {
     Dialog, DialogContent, DialogHeader, DialogBody, DialogFooter,
   } from '@/src/components/ui/Dialog';
   import { Popover, PopoverTrigger, PopoverContent } from '@/src/components/ui/Popover';
   import { Input, Textarea } from '@/src/components/ui/Input';
   import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/src/components/ui/Select';
   import { DatePickerInput } from '@/src/components/ui/DatePickerInput';
   import { Button } from '@/src/components/ui/Button';
   import { cn } from '@/src/lib/utils';

   interface TaskDialogProps {
     open: boolean;
     onOpenChange: (open: boolean) => void;
     mode: 'create' | 'edit';
     task?: Task;
     defaultCategoryId?: string;
   }

   const DEFAULT_TASK: Omit<Task, 'id'> = {
     title: '',
     priority: 'medium',
     status: '未开始',
     description: '',
     project: '',
     assignee: '',
     assigneeAvatar: '',
     deadline: '',
     aiSuggestions: [],
   };

   export function TaskDialog({ open, onOpenChange, mode, task, defaultCategoryId }: TaskDialogProps) {
     const categories = useTaskStore((s) => s.categories);
     const addTask = useTaskStore((s) => s.addTask);
     const updateTask = useTaskStore((s) => s.updateTask);
     const deleteTask = useTaskStore((s) => s.deleteTask);
     const setTaskProject = useTaskStore((s) => s.setTaskProject);
     const products = useProductStore((s) => s.products);
     const { toast } = useToast();

     // Form state
     const [title, setTitle] = useState('');
     const [description, setDescription] = useState('');
     const [priority, setPriority] = useState<'high' | 'medium' | 'low'>('medium');
     const [status, setStatus] = useState('未开始');
     const [categoryId, setCategoryId] = useState('');
     const [deadlineDate, setDeadlineDate] = useState<string | undefined>(undefined);
     const [projectId, setProjectId] = useState<string | undefined>(undefined);
     const [showDeleteConfirm, setShowDeleteConfirm] = useState(false);
     const [comboboxOpen, setComboboxOpen] = useState(false);
     const [productSearch, setProductSearch] = useState('');

     // P13: reset form when dialog opens or task changes
     useEffect(() => {
       if (!open) return;
       if (mode === 'edit' && task) {
         setTitle(task.title);
         setDescription(task.description);
         setPriority(task.priority);
         setStatus(task.status);
         setCategoryId(categories.find((c) => c.tasks.some((t) => t.id === task.id))?.id ?? '');
         setDeadlineDate(task.deadline ? task.deadline.split(' ')[0] : undefined);
         setProjectId(task.projectId);
       } else {
         setTitle('');
         setDescription('');
         setPriority('medium');
         setStatus('未开始');
         setCategoryId(defaultCategoryId ?? categories[0]?.id ?? '');
         setDeadlineDate(undefined);
         setProjectId(undefined);
       }
       setProductSearch('');
       setShowDeleteConfirm(false);
     }, [open, mode, task?.id]);  // eslint-disable-line react-hooks/exhaustive-deps

     const selectedProduct = products.find((p) => p.id === projectId);
     const filteredProducts = useMemo(() => {
       const q = productSearch.trim().toLowerCase();
       if (!q) return products;
       return products.filter((p) => p.name.toLowerCase().includes(q));
     }, [products, productSearch]);

     const handleSubmit = () => {
       if (!title.trim()) return;
       const deadline = deadlineDate ? `${deadlineDate} 18:00` : '';
       if (mode === 'create') {
         addTask(
           {
             ...DEFAULT_TASK,
             id: crypto.randomUUID(),
             title: title.trim(),
             description: description.trim(),
             priority,
             status,
             deadline,
             projectId,
             project: selectedProduct?.name ?? '',
           },
           categoryId,
         );
         toast({ type: 'success', title: '任务已创建' });
       } else if (task) {
         updateTask(task.id, {
           title: title.trim(),
           description: description.trim(),
           priority,
           status,
           deadline,
         });
         if ((task.projectId ?? undefined) !== projectId) {
           setTaskProject(task.id, projectId);
         }
         toast({ type: 'success', title: '已保存' });
       }
       onOpenChange(false);
     };

     const handleDelete = () => {
       if (!task) return;
       deleteTask(task.id);
       setShowDeleteConfirm(false);
       onOpenChange(false);
       toast({ type: 'success', title: '任务已删除' });
     };

     return (
       <>
         <Dialog open={open} onOpenChange={onOpenChange}>
           <DialogContent className="max-w-md">
             <DialogHeader
               title={mode === 'create' ? '新建任务' : '编辑任务'}
               description={mode === 'create' ? '快速记录一个新任务' : `编辑 ${task?.title ?? ''}`}
             />
             <DialogBody>
               {/* 1. 标题 */}
               <Input
                 placeholder="任务标题"
                 value={title}
                 onChange={(e) => setTitle(e.target.value)}
                 autoFocus={mode === 'create'}
               />

               {/* 2. 描述 */}
               <Textarea
                 placeholder="详细描述..."
                 value={description}
                 onChange={(e) => setDescription(e.target.value)}
                 className="min-h-[80px]"
               />

               {/* 3. 优先级 / 分类 (grid 2 cols) */}
               <div className="grid grid-cols-2 gap-2">
                 <Select value={priority} onValueChange={(v) => setPriority(v as 'high' | 'medium' | 'low')}>
                   <SelectTrigger className="h-9"><SelectValue placeholder="优先级" /></SelectTrigger>
                   <SelectContent>
                     <SelectItem value="high">高</SelectItem>
                     <SelectItem value="medium">中</SelectItem>
                     <SelectItem value="low">低</SelectItem>
                   </SelectContent>
                 </Select>
                 <Select value={categoryId} onValueChange={setCategoryId}>
                   <SelectTrigger className="h-9"><SelectValue placeholder="分类" /></SelectValue>
                   <SelectContent>
                     {categories.map((c) => (
                       <SelectItem key={c.id} value={c.id}>{c.name}</SelectItem>
                     ))}
                   </SelectContent>
                 </Select>
               </div>

               {/* 4. 截止日期 / 状态 (grid 2 cols) */}
               <div className="grid grid-cols-2 gap-2">
                 <DatePickerInput
                   value={deadlineDate}
                   onChange={setDeadlineDate}
                   placeholder="截止日期"
                 />
                 <Select value={status} onValueChange={setStatus}>
                   <SelectTrigger className="h-9"><SelectValue placeholder="状态" /></SelectTrigger>
                   <SelectContent>
                     <SelectItem value="未开始">未开始</SelectItem>
                     <SelectItem value="进行中">进行中</SelectItem>
                     <SelectItem value="已完成">已完成</SelectItem>
                   </SelectContent>
                 </Select>
               </div>

               {/* 5. 关联产品 Combobox */}
               <div className="space-y-1.5">
                 <label className="block text-sm font-medium text-text-primary">关联产品 (可选)</label>
                 <Popover open={comboboxOpen} onOpenChange={setComboboxOpen}>
                   <PopoverTrigger asChild>
                     <button
                       type="button"
                       className={cn(
                         'w-full h-9 px-3 text-sm rounded-[var(--radius-md)]',
                         'bg-bg-input border border-border text-text-primary',
                         'flex items-center justify-between gap-2 text-left',
                         'cursor-pointer transition-colors duration-fast',
                         'hover:border-text-tertiary/30',
                         'focus:outline-none focus:ring-2 focus:ring-accent/20 focus:border-accent',
                       )}
                     >
                       <span className={cn(!selectedProduct && 'text-text-placeholder')}>
                         {selectedProduct ? selectedProduct.name : '搜索产品...'}
                       </span>
                       {selectedProduct ? (
                         <button
                           type="button"
                           aria-label="清除产品关联"
                           onClick={(e) => { e.stopPropagation(); setProjectId(undefined); }}
                           className="text-text-tertiary hover:text-text-primary"
                         >
                           <X size={14} />
                         </button>
                       ) : (
                         <MagnifyingGlass size={14} className="text-text-tertiary" />
                       )}
                     </button>
                   </PopoverTrigger>
                   <PopoverContent className="w-72 p-1" align="start">
                     <Input
                       placeholder="搜索产品..."
                       value={productSearch}
                       onChange={(e) => setProductSearch(e.target.value)}
                       className="mb-1"
                       autoFocus
                     />
                     <div className="max-h-[200px] overflow-y-auto">
                       {filteredProducts.length === 0 ? (
                         <p className="text-xs text-text-tertiary py-6 text-center">未找到匹配的产品</p>
                       ) : (
                         filteredProducts.map((p) => (
                           <button
                             key={p.id}
                             type="button"
                             onClick={() => { setProjectId(p.id); setComboboxOpen(false); setProductSearch(''); }}
                             className="w-full text-left px-2.5 py-1.5 text-sm rounded-[var(--radius-sm)] hover:bg-bg-secondary text-text-primary"
                           >
                             {p.name}
                           </button>
                         ))
                       )}
                     </div>
                   </PopoverContent>
                 </Popover>
               </div>
             </DialogBody>

             <DialogFooter>
               {mode === 'edit' && (
                 <Button
                   variant="danger"
                   size="sm"
                   className="mr-auto"
                   onClick={() => setShowDeleteConfirm(true)}
                 >
                   <Trash size={14} />
                   删除
                 </Button>
               )}
               <Button variant="secondary" onClick={() => onOpenChange(false)}>取消</Button>
               <Button
                 variant="primary"
                 disabled={!title.trim()}
                 onClick={handleSubmit}
               >
                 {mode === 'create' ? '创建任务' : '保存修改'}
               </Button>
             </DialogFooter>
           </DialogContent>
         </Dialog>

         {/* 嵌套删除确认 (top-level Dialog, z-modal 自动叠加) */}
         <Dialog open={showDeleteConfirm} onOpenChange={setShowDeleteConfirm}>
           <DialogContent className="max-w-sm">
             <DialogHeader title="删除任务?" />
             <DialogBody>
               <p className="text-sm text-text-secondary">
                 任务 "{task?.title}" 将被永久删除,此操作无法撤销。
               </p>
             </DialogBody>
             <DialogFooter>
               <Button variant="secondary" onClick={() => setShowDeleteConfirm(false)}>取消</Button>
               <Button variant="danger" onClick={handleDelete}>删除</Button>
             </DialogFooter>
           </DialogContent>
         </Dialog>
       </>
     );
   }
   ```

   注意:确保 `task.projectId` 引用在 Plan 01 完成后已合法(Task 接口已加可选字段)。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TaskDialog.tsx 文件存在
    - 包含 `interface TaskDialogProps` 含 mode/task/defaultCategoryId/open/onOpenChange
    - 包含 `mode === 'create' ? '新建任务' : '编辑任务'` 标题分支 (D-08)
    - 包含 `mode === 'create' ? '创建任务' : '保存修改'` CTA 分支 (D-08)
    - 包含 `crypto.randomUUID()` (TASK-09)
    - 包含 `Popover` 和 `PopoverTrigger` import (Combobox 实现基座, D-09)
    - 包含 "搜索产品..." 字符串 (Combobox placeholder)
    - 包含 "未找到匹配的产品" 字符串 (Combobox empty state)
    - 包含 `aria-label="清除产品关联"` (Combobox clear button)
    - 包含 `<Button variant="danger" size="sm" className="mr-auto">` + "删除" 文案 (edit mode delete left)
    - 包含嵌套删除确认 Dialog,title="删除任务?",body 含 task.title
    - 包含 `useEffect(... [open, mode, task?.id])` 表单重置 (Pitfall P13)
    - 包含 `useTaskStore((s) => s.addTask)` / `updateTask` / `deleteTask` / `setTaskProject` (direct store hooks, REQUIREMENTS Key Decision #2)
    - 包含 `useProductStore((s) => s.products)` (Combobox 数据源)
    - DialogContent 主对话框 className 含 `max-w-md` (UI-SPEC)
    - `npm run lint` 通过
  </acceptance_criteria>
  <done>
    TaskDialog 双模式 + Combobox + 嵌套删除确认就绪。下一步在 TaskManagementView 接入新建任务按钮。
  </done>
</task>

<task type="auto">
  <name>Task 2: TaskManagementView 接入 "新建任务" 按钮 + TaskDialog 状态</name>
  <files>src/views/TaskManagementView.tsx</files>
  <read_first>
    - src/views/TaskManagementView.tsx (现有结构)
    - src/components/TaskDialog.tsx (Task 1 完成后的 API)
    - src/components/StatsRow.tsx (顶部 StatsRow 不动)
  </read_first>
  <behavior>
    - 顶部加 "新建任务" 按钮 (Plus 图标 + 文案),触发 TaskDialog mode='create'
    - 状态: const [dialogOpen, setDialogOpen] = useState(false); const [editingTask, setEditingTask] = useState<Task | undefined>()
    - 不在 TaskManagementView 触发 edit 模式 (edit 入口在 TaskKanban DotsMenu,Plan 04 处理);TaskManagementView 仅负责 create 入口
    - TaskDialog open + mode + onClose 接入
  </behavior>
  <action>
1. 编辑 `src/views/TaskManagementView.tsx`:

   a. 顶部加 imports:
   ```tsx
   import { useState } from 'react';
   import { Plus } from '@phosphor-icons/react';
   import { Button } from '@/src/components/ui/Button';
   import { TaskDialog } from '@/src/components/TaskDialog';
   import type { Task } from '../data/mockTasks';
   ```

   b. 在组件内加状态:
   ```tsx
   const [dialogOpen, setDialogOpen] = useState(false);
   const [editingTask, setEditingTask] = useState<Task | undefined>();
   ```

   c. 在 `<StatsRow />` 之后、`<div className="flex gap-6 ...">` 之前,加一行操作栏:
   ```tsx
   <div className="flex items-center justify-between">
     <h1 className="text-lg font-semibold text-text-primary">任务管理</h1>
     <Button variant="primary" size="sm" onClick={() => { setEditingTask(undefined); setDialogOpen(true); }}>
       <Plus size={14} weight="duotone" />
       新建任务
     </Button>
   </div>
   ```

   d. 在组件 return 末尾(`</>` 之前)加 TaskDialog 渲染:
   ```tsx
   <TaskDialog
     open={dialogOpen}
     onOpenChange={setDialogOpen}
     mode={editingTask ? 'edit' : 'create'}
     task={editingTask}
   />
   ```

   e. (现有 useApp 引用保留不动;此 view 仍是 legacy consumer,迁移推到后续 phase)
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/views/TaskManagementView.tsx 包含 `import { TaskDialog }` (grep)
    - src/views/TaskManagementView.tsx 包含 `<TaskDialog` JSX (grep)
    - src/views/TaskManagementView.tsx 包含 "新建任务" 按钮文案
    - src/views/TaskManagementView.tsx 包含 `mode={editingTask ? 'edit' : 'create'}` (双模式切换)
    - src/views/TaskManagementView.tsx 包含 `useState(false)` 用于 dialogOpen
    - `npm run lint` 通过
  </acceptance_criteria>
  <done>
    "新建任务" 按钮可见可点击,触发 TaskDialog create 模式。edit 模式入口由 Plan 04 在 TaskKanban DotsMenu 接入。
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 通过
- TaskDialog 在新建模式下打开字段为空
- TaskDialog 在 edit 模式下字段从 task prop 同步 (Pitfall P13 fix)
- 删除确认嵌套 Dialog 可正常打开关闭
- Combobox 可搜索筛选产品,清除按钮可清空
</verification>

<success_criteria>
1. TaskDialog 创建/编辑双模式完整 (TASK-02 ✓, D-08)
2. 删除任务触发嵌套确认 Dialog,确认后从 store 移除 (TASK-03 ✓)
3. 编辑模式可改 status 为 '未开始' 实现 reopen (TASK-04 ✓)
4. Combobox 产品选择器替代 Select (D-09 ✓)
5. crypto.randomUUID() 生成新任务 ID (TASK-09 ✓)
6. setTaskProject 在 projectId 变更时镜像 project 字段 (D-10 ✓, store 已实现)
7. TaskManagementView "新建任务" 按钮接入
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-crud/05-03-SUMMARY.md`
</output>
