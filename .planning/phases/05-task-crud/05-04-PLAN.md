---
phase: 05-task-crud
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - src/components/TaskKanban.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [TASK-01, TASK-05, TASK-06]

must_haves:
  truths:
    - "用户展开任务卡片后,标题/描述/优先级/截止日期/分类直接渲染为可编辑控件,无额外点击进入编辑态 (D-01)"
    - "字段变动 400ms 后自动调用 updateTask,无保存按钮 (D-02)"
    - "卡片右上角 DotsThree DropdownMenu 承载 '在对话框中编辑' / '复制 ID' / '重新打开' / '删除' 四个操作 (D-03)"
    - "底部主操作按钮保留(标记完成/已完成),与 DotsMenu 并存 (D-04)"
    - "用户可在看板列之间拖拽任务卡片,松开后分类自动更新 (TASK-05)"
    - "拖拽时 DnD activation threshold 8px,保护 click-to-expand 行为 (D-05)"
    - "拖拽中实时更新两列的计数 Badge (D-06)"
    - "拖拽使用专用 moveTask action,不构造完整 task 对象 (D-07)"
    - "卡片上显示关联产品的 Badge,点击触发 ProductSummaryDrawer (TASK-06 + D-11)"
  artifacts:
    - path: "src/components/TaskKanban.tsx"
      provides: "inline 编辑 + DotsMenu + DnD + 产品徽章 + ProductSummaryDrawer 接入"
      contains: "DndContext"
      min_lines: 350
    - path: "package.json"
      provides: "@dnd-kit/core@6.3.1 依赖"
      contains: "@dnd-kit/core"
  key_links:
    - from: "src/components/TaskKanban.tsx"
      to: "@dnd-kit/core"
      via: "DndContext + useSensor + DragOverlay 实现拖拽"
      pattern: "@dnd-kit/core"
    - from: "src/components/TaskKanban.tsx"
      to: "src/stores/taskStore.ts"
      via: "useTaskStore 调用 updateTask/moveTask/reopenTask/deleteTask"
      pattern: "useTaskStore\\(\\(s\\) => s\\.(updateTask|moveTask|reopenTask|deleteTask)\\)"
    - from: "src/components/TaskKanban.tsx"
      to: "src/components/ProductSummaryDrawer.tsx"
      via: "产品徽章点击触发 Drawer"
      pattern: "ProductSummaryDrawer"
    - from: "src/components/TaskKanban.tsx"
      to: "src/components/TaskDialog.tsx"
      via: "DotsMenu '在对话框中编辑' 触发 TaskDialog mode=edit"
      pattern: "TaskDialog"
---

<objective>
重写 TaskKanban.tsx,集成 4 大能力:1) 展开卡片 inline 编辑(全字段 input + 400ms debounced autosave);2) DotsThree DropdownMenu 承载次级操作;3) @dnd-kit/core 跨列拖拽;4) 关联产品徽章 + ProductSummaryDrawer 接入。

Purpose: 交付 TASK-01 (inline 编辑)、TASK-05 (DnD)、TASK-06 (徽章 + Drawer 接入)。这是 Phase 5 体量最大的 UI plan,前置 plan 01-03 已就绪所有 store action 与辅助组件。
Output: 重写后的 TaskKanban.tsx + @dnd-kit/core@6.3.1 安装
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-task-crud/05-CONTEXT.md
@.planning/phases/05-task-crud/05-UI-SPEC.md

@src/components/TaskKanban.tsx
@src/components/ui/DropdownMenu.tsx
@src/components/ui/Input.tsx
@src/components/ui/Select.tsx
@src/components/ui/DatePickerInput.tsx
@src/components/ui/Badge.tsx
@src/components/ui/Button.tsx
@src/components/ui/SegmentedControl.tsx
@src/stores/taskStore.ts
@src/data/mockTasks.ts
@package.json

<interfaces>
<!-- Plan 01 完成后的 taskStore actions -->
```typescript
updateTask: (taskId: string, updates: Partial<Task>) => void;
deleteTask: (taskId: string) => void;
reopenTask: (taskId: string) => void;
moveTask: (taskId: string, fromCatId: string, toCatId: string) => void;
setTaskProject: (taskId: string, projectId: string | undefined) => void;
```

<!-- Plan 02 完成后的 ProductSummaryDrawer API -->
```typescript
interface ProductSummaryDrawerProps {
  productId: string | undefined;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
```

<!-- Plan 03 完成后的 TaskDialog API -->
```typescript
interface TaskDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  mode: 'create' | 'edit';
  task?: Task;
  defaultCategoryId?: string;
}
```

<!-- 现有 TaskKanbanProps (需扩展) -->
```typescript
interface TaskKanbanProps {
  className?: string;
  categories: TaskCategory[];
  selectedTaskId: string;
  onSelectTask: (id: string) => void;
}
```

<!-- @dnd-kit/core API (待安装) -->
```typescript
import { DndContext, DragOverlay, PointerSensor, useSensor, useSensors, closestCorners } from '@dnd-kit/core';
import type { DragStartEvent, DragEndEvent } from '@dnd-kit/core';
// useDroppable({ id: catId }) → setNodeRef, isOver
// useDraggable({ id: taskId }) → attributes, listeners, setNodeRef, transform
```

<!-- UI-SPEC 文案契约 (TaskKanban 部分) -->
- 任务操作 (DotsThree aria-label)
- 在对话框中编辑 / 复制 ID / 重新打开 / 删除 (DotsMenu 4 项)
- 已复制任务 ID (复制成功 Toast)
- 标记完成 / 已完成 (底部按钮)
- 添加描述... (inline Textarea placeholder)
- 任务标题 (inline title placeholder)

<!-- DnD 视觉契约 -->
- Dragging 卡片: opacity-60 scale-[1.02] shadow-shadow-lg cursor-grabbing, spring {stiffness:500, damping:30}
- Drop target column: ring-2 ring-accent/40 rounded-[var(--radius-md)] bg-bg-tertiary/50
- DragOverlay clone: opacity-90 rotate-2 shadow-shadow-xl
- Column header Badge count 用 motion.span key=count,AnimatePresence 出入动画

<!-- PointerSensor 配置 -->
- activationConstraint: { distance: 8 }  // D-05
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 @dnd-kit/core@6.3.1 + 在 TaskKanban 接入 DnD + DotsMenu + Inline 编辑 + 产品徽章</name>
  <files>package.json, package-lock.json, src/components/TaskKanban.tsx</files>
  <read_first>
    - src/components/TaskKanban.tsx (现有结构 — collapsed 卡片、展开预览、底部按钮、Add Category、SegmentedControl)
    - src/components/ui/DropdownMenu.tsx (DropdownMenu primitives API)
    - src/components/ui/Input.tsx (Input + Textarea,带 label API)
    - src/components/ui/Select.tsx (SelectTrigger/Content/Item API)
    - src/components/ui/DatePickerInput.tsx (value/onChange API)
    - src/components/ui/Badge.tsx (variant: accent/neutral/danger/warning)
    - src/components/ui/Toast.tsx (useToast,复制 ID 成功 Toast)
    - src/components/TaskDialog.tsx (Plan 03 完成,接入 edit 模式入口)
    - src/components/ProductSummaryDrawer.tsx (Plan 02 完成,产品徽章 Drawer)
    - src/stores/taskStore.ts (Plan 01 后 5 个新 action)
    - src/data/mockTasks.ts (Task 接口扩展字段)
    - package.json (验证当前依赖)
    - .planning/phases/05-task-crud/05-UI-SPEC.md §1-3 (TaskKanban 展开卡片 + DotsMenu + DnD 视觉契约)
  </read_first>
  <behavior>
    安装依赖:
    - 运行 `npm install @dnd-kit/core@6.3.1` (D-05, REQUIREMENTS Key Decision #5)
    - 不安装 @dnd-kit/sortable(本 phase 只做跨列拖拽,无需列内排序)

    Inline 编辑 (D-01, D-02):
    - 展开卡片内: title 用 Input (borderless 样式 h-7 px-1 -mx-1 text-sm font-medium);description 用 Textarea (auto-resize min-h-[60px] placeholder "添加描述...");grid-cols-2 gap-2 优先级 Select + 截止日期 DatePickerInput (h-8);分类 Select (h-8)
    - 状态字段不渲染 Select (D-04),仅底部按钮控制
    - 每个字段 onChange 触发 debounced(400ms) → updateTask(task.id, {字段: value});立刻本地 state 同步保证 input 响应
    - debounce 实现: 用 useRef 存 timer,change 时 clearTimeout + setTimeout(400ms);unmount 时 flush
    - 字段层 "justSaved" 状态: border 闪 200ms border-accent (UI-SPEC §Inline Save),可选,实现简单的话省略
    - 关联产品 Badge: 仅在 task.projectId 存在时渲染,variant="accent" + 项目图标,点击 → 打开 ProductSummaryDrawer;onClick e.stopPropagation() 防止触发卡片 collapse
    - AI 建议块保留不动(原有代码)
    - 底部按钮: variant 跟随 status (已完成 → secondary disabled 显示 "已完成"; 未完成 → primary 显示 "标记完成"); 已完成任务额外显示 "重新打开" 按钮(同时与 DotsMenu 中的"重新打开"等价)

    DotsThree DropdownMenu (D-03):
    - 触发器: Button variant="ghost" size="xs" aria-label="任务操作",含 DotsThree icon (size 16 weight="duotone")
    - 位置: 卡片右上角 (top-3 right-3);已完成态时与 CheckCircle 共存 (CheckCircle 移到 top-3 right-10)
    - Menu items 顺序: 1) 在对话框中编辑 (PencilSimple) → 触发 TaskDialog mode=edit open;2) 复制 ID (Copy) → navigator.clipboard.writeText(task.id) + Toast "已复制任务 ID";3) separator;4) 重新打开 (ArrowCounterClockwise) → reopenTask(task.id),disabled when status !== '已完成';5) 删除 (Trash text-danger) → 触发 showDeleteConfirm
    - min-w-[10rem]
    - DropdownMenu 内 onSelect 必须 e.stopPropagation() 防触发卡片 click

    DnD (D-05, D-06, D-07):
    - DndContext 包裹整个 columns 容器;collisionDetection=closestCorners
    - PointerSensor activationConstraint: { distance: 8 }
    - 每个列用 useDroppable({ id: cat.id }) 接收 setNodeRef + isOver
    - 每个卡片用 useDraggable({ id: task.id, data: { fromCatId: cat.id } }) 接收 attributes/listeners/setNodeRef/transform;listeners 套在卡片 motion.div 上(transform 应用 + style)
    - onDragStart: setActiveDragId(task.id) + setActiveDragTask(task)
    - onDragOver: setOverCatId(over.id as string) (用于实时 Badge 计数)
    - onDragEnd: 调用 moveTask(task.id, fromCatId, toCatId);清空 activeDragId/overCatId/activeDragTask
    - DragOverlay: portal-rendered 卡片克隆,opacity-90 rotate-2 shadow-shadow-xl
    - 实时 Badge 计数 (D-06): 每列 derivedCount = cat.tasks.length + (thisColumnIsOver && activeDragId ? +1 : 0) - (activeDragFromThisColumn && activeDragId ? -1 : 0);用 motion.span key={derivedCount} 在 AnimatePresence 内,y:-4 exit / y:4 enter,spring {stiffness:400, damping:25}

    状态:
    - 卡片本地编辑 state: 用 useState 存 [title, description, priority, deadline, categoryId] 每张展开卡片;展开时 useEffect 同步 task prop → local state
    - DotsMenu 状态由 TaskKanban 顶层管理 (openEditDialogTask: Task | undefined, confirmDeleteTask: Task | undefined, drawerProductId: string | undefined)

    onSelectTask 兼容:
    - 点击卡片(非 input/menu 区域)继续触发 onSelectTask(展开/收起),onClick 上判断 (e.target as HTMLElement).closest('input,textarea,button,[role=menuitem]') 早返回

    重要现有行为保留:
    - SegmentedControl category/date 切换保留
    - dateGroups useMemo 保留(只在 viewMode==='date' 时生效)
    - Add Category 流程保留
    - DnD 仅在 viewMode==='category' 时启用(date view 模式不拖拽,UI-SPEC deferred)
  </behavior>
  <action>
1. 先运行 `cd D:/Projects/Nova/pm-workspace && npm install @dnd-kit/core@6.3.1`。验证 package.json dependencies 包含 "@dnd-kit/core": "^6.3.1"。

2. 重写 `src/components/TaskKanban.tsx`。完整骨架(根据 UI-SPEC §1-3 严格落地):

   顶部 import 区:
   ```tsx
   import { useState, useMemo, useEffect, useRef, useCallback } from 'react';
   import {
     DndContext, DragOverlay, PointerSensor, useSensor, useSensors, closestCorners,
     useDroppable, useDraggable,
     type DragStartEvent, DragEndEvent, DragMoveEvent,
   } from '@dnd-kit/core';
   import {
     CaretDown, Funnel, ArrowsDownUp, CheckCircle, Clock, Sparkle, Plus, DotsThree,
     PencilSimple, Copy, ArrowCounterClockwise, Trash, FolderSimple,
   } from '@phosphor-icons/react';
   import { motion, AnimatePresence } from 'motion/react';
   import { TaskCategory, Task } from '../data/mockTasks';
   import { useTaskStore } from '@/src/stores/taskStore';
   import { useProductStore } from '@/src/stores/productStore';
   import { useToast } from '@/src/components/ui/Toast';
   import { Card } from '@/src/components/ui/Card';
   import { Button } from '@/src/components/ui/Button';
   import { Badge } from '@/src/components/ui/Badge';
   import { Input, Textarea } from '@/src/components/ui/Input';
   import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/src/components/ui/Select';
   import { DatePickerInput } from '@/src/components/ui/DatePickerInput';
   import { Separator } from '@/src/components/ui/Separator';
   import { SegmentedControl } from '@/src/components/ui/SegmentedControl';
   import {
     DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator,
   } from '@/src/components/ui/DropdownMenu';
   import { TaskDialog } from './TaskDialog';
   import { ProductSummaryDrawer } from './ProductSummaryDrawer';
   import { cn } from '@/src/lib/utils';
   ```

   组件签名:保留原 props 不变,内部状态扩展:
   ```tsx
   interface TaskKanbanProps {
     className?: string;
     categories: TaskCategory[];
     selectedTaskId: string;
     onSelectTask: (id: string) => void;
   }

   export function TaskKanban({ className = '', categories, selectedTaskId, onSelectTask }: TaskKanbanProps) {
     const {
       addCategory, completeTask, updateTask, deleteTask, reopenTask, moveTask,
     } = useTaskStore();
     const products = useProductStore((s) => s.products);
     const { toast } = useToast();

     // View state
     const [isAddingCategory, setIsAddingCategory] = useState(false);
     const [newCategoryName, setNewCategoryName] = useState('');
     const [viewMode, setViewMode] = useState('category');

     // DotsMenu / Dialog state
     const [editDialogTask, setEditDialogTask] = useState<Task | undefined>();
     const [editDialogOpen, setEditDialogOpen] = useState(false);
     const [confirmDeleteTask, setConfirmDeleteTask] = useState<Task | undefined>();
     const [drawerProductId, setDrawerProductId] = useState<string | undefined>();
     const [drawerOpen, setDrawerOpen] = useState(false);

     // DnD state
     const [activeDragId, setActiveDragId] = useState<string | null>(null);
     const [activeDragTask, setActiveDragTask] = useState<Task | null>(null);
     const [overCatId, setOverCatId] = useState<string | null>(null);
     const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }));
   ```

   抽出 KanbanCard 子组件(便于 useDraggable hook 复用 + 本地编辑 state 隔离):

   ```tsx
   function KanbanCard({
     task, cat, isExpanded, onToggleExpand,
   }: {
     task: Task; cat: TaskCategory; isExpanded: boolean; onToggleExpand: () => void;
   }) {
     const {
       updateTask, completeTask, reopenTask, deleteTask,
     } = useTaskStore();
     const products = useProductStore((s) => s.products);
     const { toast } = useToast();
     const categories = useTaskStore((s) => s.categories);

     // DnD
     const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({
       id: task.id,
       data: { fromCatId: cat.id },
     });

     // Inline edit local state
     const [title, setTitle] = useState(task.title);
     const [description, setDescription] = useState(task.description);
     const [priority, setPriority] = useState(task.priority);
     const [deadlineDate, setDeadlineDate] = useState<string | undefined>(
       task.deadline ? task.deadline.split(' ')[0] : undefined,
     );
     const [categoryId, setCategoryId] = useState(cat.id);

     // Sync when expand toggles open
     useEffect(() => {
       if (isExpanded) {
         setTitle(task.title);
         setDescription(task.description);
         setPriority(task.priority);
         setDeadlineDate(task.deadline ? task.deadline.split(' ')[0] : undefined);
         setCategoryId(cat.id);
       }
     }, [isExpanded, task.id]);  // eslint-disable-line

     // Debounced autosave per field (D-02)
     const timers = useRef<Record<string, ReturnType<typeof setTimeout>>>({});
     const scheduleSave = useCallback((key: string, updates: Partial<Task>) => {
       if (timers.current[key]) clearTimeout(timers.current[key]);
       timers.current[key] = setTimeout(() => {
         updateTask(task.id, updates);
       }, 400);
     }, [task.id, updateTask]);

     // Flush on unmount
     useEffect(() => () => {
       Object.values(timers.current).forEach(clearTimeout);
     }, []);

     const product = task.projectId ? products.find((p) => p.id === task.projectId) : undefined;

     const handleCopyId = () => {
       navigator.clipboard.writeText(task.id);
       toast({ type: 'success', title: '已复制任务 ID' });
     };

     const dragStyle = transform
       ? { transform: `translate3d(${transform.x}px, ${transform.y}px, 0)` }
       : undefined;

     return (
       <motion.div
         ref={setNodeRef}
         style={dragStyle}
         {...attributes}
         {...listeners}
         layout
         initial={{ opacity: 0, y: 6 }}
         animate={{ opacity: isDragging ? 0.4 : 1, y: 0, scale: isDragging ? 1.02 : 1 }}
         transition={{ type: 'spring', stiffness: 500, damping: 30 }}
         onClick={(e) => {
           // click-vs-drag: PointerSensor handles activation. Stop if click hit interactive el.
           const target = e.target as HTMLElement;
           if (target.closest('input, textarea, button, [role="menuitem"], [data-no-expand]')) return;
           onToggleExpand();
         }}
         className={cn(
           'bg-bg-primary rounded-[var(--radius-md)] p-3 transition-all cursor-grab active:cursor-grabbing border relative',
           isExpanded
             ? 'border-accent shadow-md ring-1 ring-accent/15'
             : 'border-border-subtle shadow-xs hover:border-border hover:shadow-sm',
         )}
       >
         {/* Header row */}
         <div className="flex items-center gap-2 pr-8">
           <span className="text-[10px] font-mono text-text-tertiary shrink-0">{task.id}</span>
           {isExpanded ? (
             <Input
               value={title}
               onChange={(e) => { setTitle(e.target.value); scheduleSave('title', { title: e.target.value }); }}
               className="h-7 px-1 -mx-1 text-sm font-medium border-transparent hover:border-border-subtle focus:border-accent"
               data-no-expand
             />
           ) : (
             <h4 className={cn(
               'text-sm font-medium leading-tight flex-1 min-w-0 truncate',
               task.status === '已完成' ? 'text-text-tertiary line-through' : 'text-text-primary',
             )}>
               {task.title}
             </h4>
           )}
         </div>

         {/* Status indicator (collapsed + done) */}
         {task.status === '已完成' && !isExpanded && (
           <CheckCircle size={14} weight="fill" className="absolute top-3 right-10 text-success" />
         )}

         {/* DotsMenu — always present, top-right */}
         <div className="absolute top-3 right-3" data-no-expand>
           <DropdownMenu>
             <DropdownMenuTrigger asChild>
               <Button variant="ghost" size="xs" aria-label="任务操作" className="h-6 w-6 p-0">
                 <DotsThree size={16} weight="duotone" />
               </Button>
             </DropdownMenuTrigger>
             <DropdownMenuContent className="min-w-[10rem]" align="end">
               <DropdownMenuItem onSelect={() => onToggleExpandToDialog()}>
                 <PencilSimple size={14} weight="duotone" /> 在对话框中编辑
               </DropdownMenuItem>
               <DropdownMenuItem onSelect={handleCopyId}>
                 <Copy size={14} weight="duotone" /> 复制 ID
               </DropdownMenuItem>
               <DropdownMenuSeparator />
               <DropdownMenuItem
                 disabled={task.status !== '已完成'}
                 onSelect={() => reopenTask(task.id)}
               >
                 <ArrowCounterClockwise size={14} weight="duotone" /> 重新打开
               </DropdownMenuItem>
               <DropdownMenuItem
                 className="text-danger"
                 onSelect={() => onRequestDelete()}
               >
                 <Trash size={14} weight="duotone" /> 删除
               </DropdownMenuItem>
             </DropdownMenuContent>
           </DropdownMenu>
         </div>

         {/* Collapsed preview */}
         {!isExpanded && (
           <div className="mt-2.5 flex items-center justify-between pr-6">
             <Badge
               variant={task.priority === 'high' ? 'danger' : task.priority === 'medium' ? 'warning' : 'neutral'}
               className="text-[10px]"
             >
               {task.priority === 'high' ? '高' : task.priority === 'medium' ? '中' : '低'}
             </Badge>
             <div className="flex items-center gap-2">
               {product && (
                 <Badge variant="accent" className="text-[10px] gap-1">
                   <FolderSimple size={10} weight="fill" />
                   {product.name}
                 </Badge>
               )}
               <span className="text-[11px] text-text-tertiary">{task.time || task.status}</span>
             </div>
           </div>
         )}

         {/* Expanded body — inline editable */}
         {isExpanded && (
           <div className="mt-3 pt-3 border-t border-border-subtle space-y-3" data-no-expand>
             <Textarea
               placeholder="添加描述..."
               value={description}
               onChange={(e) => { setDescription(e.target.value); scheduleSave('description', { description: e.target.value }); }}
               className="min-h-[60px] resize-none"
             />

             <div className="grid grid-cols-2 gap-2">
               <Select
                 value={priority}
                 onValueChange={(v) => {
                   setPriority(v as 'high' | 'medium' | 'low');
                   scheduleSave('priority', { priority: v as 'high' | 'medium' | 'low' });
                 }}
               >
                 <SelectTrigger className="h-8"><SelectValue placeholder="优先级" /></SelectTrigger>
                 <SelectContent>
                   <SelectItem value="high">高</SelectItem>
                   <SelectItem value="medium">中</SelectItem>
                   <SelectItem value="low">低</SelectItem>
                 </SelectContent>
               </Select>
               <DatePickerInput
                 value={deadlineDate}
                 onChange={(d) => { setDeadlineDate(d); scheduleSave('deadline', { deadline: d ? `${d} 18:00` : '' }); }}
               />
             </div>

             <Select
               value={categoryId}
               onValueChange={(v) => {
                 setCategoryId(v);
                 // 移动到新分类 = moveTask
                 if (v !== cat.id) moveTask(task.id, cat.id, v);
               }}
             >
               <SelectTrigger className="h-8"><SelectValue placeholder="分类" /></SelectTrigger>
               <SelectContent>
                 {categories.map((c) => (
                   <SelectItem key={c.id} value={c.id}>{c.name}</SelectItem>
                 ))}
               </SelectContent>
             </Select>

             {product && (
               <div>
                 <button
                   type="button"
                   onClick={(e) => { e.stopPropagation(); onRequestOpenProductDrawer(task.projectId!); }}
                   className="inline-flex"
                 >
                   <Badge variant="accent" className="text-[10px] gap-1 cursor-pointer hover:bg-accent-subtle/80">
                     <FolderSimple size={10} weight="fill" />
                     {product.name}
                   </Badge>
                 </button>
               </div>
             )}

             {task.aiSuggestions && task.aiSuggestions.length > 0 && (
               <div className="bg-accent-subtle/50 rounded-[var(--radius-sm)] p-2.5 border border-accent/10">
                 <h4 className="text-[11px] font-semibold text-text-primary flex items-center gap-1.5 mb-2">
                   <Sparkle size={12} weight="duotone" className="text-accent" />
                   AI 建议
                 </h4>
                 <ul className="space-y-1.5">
                   {task.aiSuggestions.map((s, i) => (
                     <li key={i} className="flex items-start gap-1.5 text-[11px] text-text-secondary">
                       <span className="w-1 h-1 rounded-full mt-1.5 shrink-0 bg-accent" />
                       <span>{s}</span>
                     </li>
                   ))}
                 </ul>
               </div>
             )}

             <Button
               variant={task.status === '已完成' ? 'secondary' : 'primary'}
               size="sm"
               className="w-full"
               disabled={task.status === '已完成'}
               onClick={(e) => { e.stopPropagation(); completeTask(task.id); }}
             >
               <CheckCircle size={14} weight="duotone" />
               {task.status === '已完成' ? '已完成' : '标记完成'}
             </Button>
           </div>
         )}
       </motion.div>
     );
   }
   ```

   注:`onToggleExpandToDialog` / `onRequestDelete` / `onRequestOpenProductDrawer` 通过 props 传入 KanbanCard(由父级 TaskKanban 管理状态)。在父级实现:
   ```tsx
   const handleRequestDialogEdit = (task: Task) => {
     setEditDialogTask(task);
     setEditDialogOpen(true);
   };
   const handleRequestDelete = (task: Task) => {
     setConfirmDeleteTask(task);
   };
   const handleOpenProductDrawer = (productId: string) => {
     setDrawerProductId(productId);
     setDrawerOpen(true);
   };
   ```

   Columns 渲染(useDroppable per column):
   ```tsx
   function KanbanColumn({
     cat, tasks, isDateView,
   }: { cat: TaskCategory; tasks: Task[]; isDateView: boolean }) {
     const { setNodeRef, isOver } = useDroppable({ id: cat.id });
     // D-06 实时计数
     const { activeDragId, activeDragTask, overCatId } = useDnDContext(); // 或通过 props 传入
     const isSource = activeDragTask && tasks.some((t) => t.id === activeDragTask.id);
     const isOverThis = overCatId === cat.id && activeDragId;
     const derivedCount = tasks.length + (isOverThis ? 1 : 0) - (isSource && activeDragId ? 1 : 0);

     return (
       <div
         ref={!isDateView ? setNodeRef : undefined}
         className={cn(
           'min-w-[clamp(240px,22vw,300px)] w-[clamp(240px,22vw,300px)] bg-bg-secondary/60 rounded-[var(--radius-md)] p-3 flex flex-col max-h-full border transition-colors',
           isOver && !isDateView
             ? 'ring-2 ring-accent/40 bg-bg-tertiary/50 border-accent/30'
             : 'border-border-subtle',
         )}
       >
         <div className="flex items-center justify-between py-1 mb-2 px-1">
           <div className="flex items-center gap-2">
             <div className={cn('w-2 h-2 rounded-full', cat.color)} />
             <h3 className="text-sm font-semibold text-text-primary">{cat.name}</h3>
             <AnimatePresence mode="popLayout">
               <motion.span
                 key={derivedCount}
                 initial={{ opacity: 0, y: -4 }}
                 animate={{ opacity: 1, y: 0 }}
                 exit={{ opacity: 0, y: 4 }}
                 transition={{ type: 'spring', stiffness: 400, damping: 25 }}
               >
                 <Badge variant="neutral" className="text-[10px]">{derivedCount}</Badge>
               </motion.span>
             </AnimatePresence>
           </div>
           <Button variant="ghost" size="xs"><Plus size={14} weight="bold" /></Button>
         </div>
         <div className="flex-1 overflow-y-auto pr-1 space-y-2 pb-2">
           {tasks.length === 0 ? (
             <div className="text-center py-6 text-xs text-text-tertiary">
               <p>暂无任务</p>
               <p className="mt-1">点击 + 新建一个任务</p>
             </div>
           ) : (
             tasks.map((task) => (
               <KanbanCard
                 key={task.id}
                 task={task}
                 cat={cat}
                 isExpanded={selectedTaskId === task.id}
                 onToggleExpand={() => onSelectTask(selectedTaskId === task.id ? '' : task.id)}
                 onToggleExpandToDialog={() => handleRequestDialogEdit(task)}
                 onRequestDelete={() => handleRequestDelete(task)}
                 onRequestOpenProductDrawer={handleOpenProductDrawer}
               />
             ))
           )}
         </div>
       </div>
     );
   }
   ```

   DnD handlers in parent:
   ```tsx
   const handleDragStart = (e: DragStartEvent) => {
     setActiveDragId(String(e.active.id));
     const task = categories.flatMap((c) => c.tasks).find((t) => t.id === e.active.id) ?? null;
     setActiveDragTask(task);
   };
   const handleDragMove = (e: DragMoveEvent) => {
     if (e.over) setOverCatId(String(e.over.id));
   };
   const handleDragEnd = (e: DragEndEvent) => {
     const taskId = String(e.active.id);
     const fromCatId = (e.active.data.current as any)?.fromCatId;
     const toCatId = e.over ? String(e.over.id) : null;
     if (fromCatId && toCatId && fromCatId !== toCatId) {
       moveTask(taskId, fromCatId, toCatId);
     }
     setActiveDragId(null);
     setActiveDragTask(null);
     setOverCatId(null);
   };
   ```

   Render 主结构:
   ```tsx
   return (
     <>
       <Card className={cn('flex flex-col', className)}>
         {/* Header (保留现有) */}
         {/* ... */}

         {/* Columns */}
         <div className="flex-1 overflow-x-auto overflow-y-hidden p-4 flex items-start gap-4 h-full">
           <DndContext
             sensors={sensors}
             collisionDetection={closestCorners}
             onDragStart={handleDragStart}
             onDragMove={handleDragMove}
             onDragEnd={handleDragEnd}
           >
             {displayGroups.map((group) => (
               <KanbanColumn
                 key={group.id}
                 cat={group}
                 tasks={group.tasks}
                 isDateView={viewMode === 'date'}
                 activeDragId={activeDragId}
                 activeDragTask={activeDragTask}
                 overCatId={overCatId}
               />
             ))}
             <DragOverlay>
               {activeDragTask ? (
                 <div className="opacity-90 rotate-2 shadow-shadow-xl rounded-[var(--radius-md)] bg-bg-primary border border-border-subtle p-3 max-w-[280px]">
                   <span className="text-[10px] font-mono text-text-tertiary">{activeDragTask.id}</span>
                   <h4 className="text-sm font-medium text-text-primary truncate">{activeDragTask.title}</h4>
                 </div>
               ) : null}
             </DragOverlay>
           </DndContext>

           {/* Add Category (保留现有, viewMode === 'category' 时显示) */}
         </div>
       </Card>

       {/* Dialogs/Drawer wired to top-level state */}
       <TaskDialog
         open={editDialogOpen}
         onOpenChange={setEditDialogOpen}
         mode={editDialogTask ? 'edit' : 'create'}
         task={editDialogTask}
       />
       <Dialog open={!!confirmDeleteTask} onOpenChange={(o) => !o && setConfirmDeleteTask(undefined)}>
         <DialogContent className="max-w-sm">
           <DialogHeader title="删除任务?" />
           <DialogBody>
             <p className="text-sm text-text-secondary">
               任务 "{confirmDeleteTask?.title}" 将被永久删除,此操作无法撤销。
             </p>
           </DialogBody>
           <DialogFooter>
             <Button variant="secondary" onClick={() => setConfirmDeleteTask(undefined)}>取消</Button>
             <Button
               variant="danger"
               onClick={() => {
                 if (confirmDeleteTask) {
                   deleteTask(confirmDeleteTask.id);
                   toast({ type: 'success', title: '任务已删除' });
                 }
                 setConfirmDeleteTask(undefined);
               }}
             >删除</Button>
           </DialogFooter>
         </DialogContent>
       </Dialog>
       <ProductSummaryDrawer
         productId={drawerProductId}
         open={drawerOpen}
         onOpenChange={setDrawerOpen}
       />
     </>
   );
   ```

   注意修正:import 路径补齐 `Dialog, DialogContent, DialogHeader, DialogBody, DialogFooter` 来自 `@/src/components/ui/Dialog`(用于 delete 确认嵌套)。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies 包含 "@dnd-kit/core": "^6.3.1" (grep)
    - src/components/TaskKanban.tsx 包含 `import { DndContext` (DnD 接入)
    - src/components/TaskKanban.tsx 包含 `useSensor(PointerSensor` 且 `activationConstraint: { distance: 8 }` (D-05)
    - src/components/TaskKanban.tsx 包含 `useDraggable` 用于卡片 (DnD)
    - src/components/TaskKanban.tsx 包含 `useDroppable` 用于列 (DnD)
    - src/components/TaskKanban.tsx 包含 `moveTask(` 调用 (D-07 专用 action)
    - src/components/TaskKanban.tsx 包含 `setTimeout(` 和 `400` 用于 debounced autosave (D-02)
    - src/components/TaskKanban.tsx 包含 "在对话框中编辑" DotsMenu item (D-03)
    - src/components/TaskKanban.tsx 包含 "复制 ID" DotsMenu item (D-03)
    - src/components/TaskKanban.tsx 包含 "重新打开" DotsMenu item (D-03)
    - src/components/TaskKanban.tsx 包含 "删除" DotsMenu item (D-03)
    - src/components/TaskKanban.tsx 包含 `aria-label="任务操作"` (DotsThree trigger)
    - src/components/TaskKanban.tsx 包含 "已复制任务 ID" toast 文案
    - src/components/TaskKanban.tsx 包含 `<DragOverlay>` (portal-rendered 克隆)
    - src/components/TaskKanban.tsx 包含 `<TaskDialog` 接入 (DotsMenu "在对话框中编辑")
    - src/components/TaskKanban.tsx 包含 `<ProductSummaryDrawer` 接入 (产品徽章)
    - src/components/TaskKanban.tsx 包含 `motion.span key={derivedCount}` (D-06 实时计数)
    - src/components/TaskKanban.tsx 包含 "暂无任务" + "点击 + 新建一个任务" 空状态文案
    - `npm run lint` 通过
  </acceptance_criteria>
  <done>
    TaskKanban 4 大能力全部就绪:inline 编辑 + DotsMenu + DnD + 产品徽章 Drawer 接入。Phase 5 全部代码完成,准备 UAT。
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 通过
- @dnd-kit/core@6.3.1 出现在 package.json dependencies
- 展开卡片后所有字段为 input/select,400ms 后 store 数据更新(可在 React DevTools / Zustand inspector 验证)
- 拖动卡片到另一列,PointerSensor 8px 阈值生效(短距离移动仍是 click,长距离触发 DnD),drop 后任务出现在新列
- DotsMenu 4 项可正常触发
- 产品徽章点击触发 Drawer
- 空列显示"暂无任务 / 点击 + 新建一个任务"
</verification>

<success_criteria>
1. 用户展开卡片可内联编辑 title/description/priority/deadline/category,400ms 自动保存 (TASK-01 ✓, D-01, D-02)
2. DotsMenu 在卡片右上角,4 个 item 功能完整 (D-03)
3. 底部主操作按钮保留 (D-04)
4. 跨列拖拽工作,8px 阈值保护 click-to-expand (TASK-05 ✓, D-05)
5. 拖拽中两列计数 Badge 实时更新 (D-06)
6. 使用 moveTask 专用 action (D-07)
7. 卡片显示产品徽章,点击触发 ProductSummaryDrawer (TASK-06 ✓, D-11)
8. 空列空状态文案完整
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-crud/05-04-SUMMARY.md`
</output>
