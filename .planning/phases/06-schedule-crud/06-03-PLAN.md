---
phase: 06-schedule-crud
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/views/ScheduleView.tsx
autonomous: true
requirements: [SCHED-04]

must_haves:
  truths:
    - "用户可以点击月历头部 '新建日程' 按钮触发 ScheduleDialog create 模式 (SCHED-01 入口)"
    - "用户可以点击月历上的事件 chip 触发 ScheduleDialog edit 模式,字段预填 (SCHED-02 入口)"
    - "用户可以在月历头部点击上/下月按钮切换月份,日历网格实时重新渲染 (SCHED-04)"
    - "用户可以点击 '今天' 按钮回到今天所在月份 (SCHED-04)"
    - "月历不再写死 2025-5 (Pitfall P20 修复),currentMonth state 默认今天所在月"
    - "事件按 date 字符串 (YYYY-MM-DD) 过滤显示在对应日期格"
    - "今天日期格有 accent 高亮"
    - "事件 chip 按 type 显示不同颜色 (meeting/deadline/task/reminder/review/sync)"
    - "Agenda 侧栏按 currentMonth 内日期升序展示本月事件"
    - "ScheduleView 直接 useScheduleStore selector (不通过 AppContext,Key Decision #2)"
  artifacts:
    - path: "src/views/ScheduleView.tsx"
      provides: "真实月历 + 月份切换 state + ScheduleDialog 接入"
      contains: "currentMonth"
      min_lines: 200
  key_links:
    - from: "src/views/ScheduleView.tsx"
      to: "src/stores/scheduleStore.ts"
      via: "useScheduleStore((s) => s.events)"
      pattern: "useScheduleStore"
    - from: "src/views/ScheduleView.tsx"
      to: "src/components/ScheduleDialog.tsx"
      via: "<ScheduleDialog ... /> JSX"
      pattern: "<ScheduleDialog"
---

<objective>
完全重写 ScheduleView 月历:去掉写死的 2025-5 计算 (Pitfall P20),改用 currentMonth state 驱动真实日历网格;事件按 date 字符串正确归位;接入 ScheduleDialog 创建/编辑入口。

Purpose: 交付 SCHED-04 (月份切换 + 真实渲染),并把 Plan 02 的 ScheduleDialog 接到视图上,使整个 schedule CRUD 闭环可用。
Output: ScheduleView.tsx 单文件重写
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-schedule-crud/06-CONTEXT.md

@src/views/ScheduleView.tsx
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
@src/components/ui/Badge.tsx
@src/components/ScheduleDialog.tsx
@src/stores/scheduleStore.ts

<interfaces>
<!-- Plan 01 完成后的 scheduleStore (直接 useScheduleStore,不经 AppContext) -->
```typescript
events: ScheduleEvent[];
// ScheduleEvent.date: string (YYYY-MM-DD)
// ScheduleEvent.type: 'meeting' | 'deadline' | 'task' | 'reminder' | 'review' | 'sync'
```

<!-- Plan 02 完成后的 ScheduleDialog API -->
```typescript
interface ScheduleDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  mode: 'create' | 'edit';
  event?: ScheduleEvent;
  defaultDate?: string;  // YYYY-MM-DD, create 模式预填
}
```

<!-- 现有 UI primitives API -->
```typescript
// Card: <Card className="...">...</Card>
// Button: variant='primary'|'secondary'|'ghost'|'danger', size='xs'|'sm'|'md'|'lg'
// Badge: variant='default'|'accent'|'success'|'warning'|'danger'|'neutral'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScheduleView 完全重写 (currentMonth state + 真实日历 + 事件归位 + Dialog 接入)</name>
  <files>src/views/ScheduleView.tsx</files>
  <read_first>
    - src/views/ScheduleView.tsx (现有写死 2025-5 的实现,要替换)
    - src/stores/scheduleStore.ts (Plan 01 后 events + ScheduleEvent 类型)
    - src/components/ScheduleDialog.tsx (Plan 02 后 API)
    - src/components/ui/Card.tsx / Button.tsx / Badge.tsx (现有 primitives)
  </read_first>
  <behavior>
    - 状态: `const [currentMonth, setCurrentMonth] = useState(() => { const d = new Date(); return { year: d.getFullYear(), month: d.getMonth() }; })` (0-based month)
    - 状态: `const [dialogOpen, setDialogOpen] = useState(false)`, `const [editingEvent, setEditingEvent] = useState<ScheduleEvent | undefined>()`, `const [createDefaultDate, setCreateDefaultDate] = useState<string | undefined>()`
    - 月份切换: prev → setCurrentMonth(m => month===0 ? {year: year-1, month: 11} : {year, month: month-1});next 反之;"今天"按钮 → setCurrentMonth(今天所在月)
    - 日历网格计算: 用 `new Date(year, month, 1).getDay()` 得本月 1 号是周几(0=周日),用 `new Date(year, month+1, 0).getDate()` 得本月天数;渲染 42 格(6 行 × 7 列),前补上月尾、后补下月头
    - 日期格 key: 用 YYYY-MM-DD 字符串(便于和 event.date 直接比较);本月格 isCurrentMonth=true,非本月格 opacity-25
    - 今天判断: `dateStr === todayISO()`(todayISO = new Date().toISOString().slice(0,10));isToday → accent 圆形高亮
    - 事件归位: `const dayEvents = events.filter(e => e.date === dateStr)`(string 直接比较,无需转换)
    - 点击事件 chip → setEditingEvent(event) + setDialogOpen(true) (edit 模式)
    - 点击日期格空白处(可选,Claude's discretion) → setCreateDefaultDate(dateStr) + setEditingEvent(undefined) + setDialogOpen(true) (create 模式预填该日期)
    - "新建日程"按钮(Agenda 侧栏底部,沿用现有位置) → setEditingEvent(undefined) + setCreateDefaultDate(undefined) + setDialogOpen(true)
    - EVENT_COLORS 扩展为 6 个 type 颜色:meeting=accent(蓝)、deadline=danger(红)、task=purple(紫)、reminder=warning(黄)、review=purple、sync=success(绿);无匹配时 fallback 到 meeting 颜色
    - 月历标题: `${year}年 ${month+1}月`(动态)
    - Agenda 侧栏: 改为展示 currentMonth 内事件(本月 1 号到月末,用 date string 范围 filter),按 date+time 升序,展示前 8 条;每条 Badge 显示 "MM月DD日" 格式(从 date string 解析)
    - 直接 `import { useScheduleStore } from '@/src/stores/scheduleStore';` + `const events = useScheduleStore((s) => s.events);`(去掉 useApp)
    - SegmentedControl 月视图/周视图保留(周视图不实现功能,仅 UI 占位;切换不影响 currentMonth)
  </behavior>
  <action>
1. 完全替换 `src/views/ScheduleView.tsx` 内容:

   ```tsx
   import { CaretLeft, CaretRight, Clock, MapPin, VideoCamera, Plus } from '@phosphor-icons/react';
   import { useState } from 'react';
   import { Card } from '@/src/components/ui/Card';
   import { Button } from '@/src/components/ui/Button';
   import { Badge } from '@/src/components/ui/Badge';
   import { SegmentedControl } from '@/src/components/ui/SegmentedControl';
   import { ScheduleDialog } from '@/src/components/ScheduleDialog';
   import { useScheduleStore } from '@/src/stores/scheduleStore';
   import { ScheduleEvent } from '@/src/stores/scheduleStore';
   import { cn } from '@/src/lib/utils';

   const EVENT_COLORS: Record<string, { bg: string; text: string; dot: string }> = {
     meeting:  { bg: 'bg-accent/10',     text: 'text-accent',     dot: 'bg-accent' },
     deadline: { bg: 'bg-danger/10',     text: 'text-danger',     dot: 'bg-danger' },
     task:     { bg: 'bg-purple-500/10', text: 'text-purple-600', dot: 'bg-purple-500' },
     reminder: { bg: 'bg-warning/10',    text: 'text-warning',    dot: 'bg-warning' },
     review:   { bg: 'bg-purple-500/10', text: 'text-purple-600', dot: 'bg-purple-500' },
     sync:     { bg: 'bg-success/10',    text: 'text-success',    dot: 'bg-success' },
   };

   const todayISO = () => new Date().toISOString().slice(0, 10);
   const pad = (n: number) => String(n).padStart(2, '0');
   const toISO = (y: number, m: number, d: number) => `${y}-${pad(m + 1)}-${pad(d)}`;
   const parseISO = (iso: string) => { const [y, m, d] = iso.split('-').map(Number); return { y, m, d }; };

   export function ScheduleView() {
     const events = useScheduleStore((s) => s.events);
     const [viewMode, setViewMode] = useState('month');
     const now = new Date();
     const [currentMonth, setCurrentMonth] = useState(() => ({ year: now.getFullYear(), month: now.getMonth() }));
     const [dialogOpen, setDialogOpen] = useState(false);
     const [editingEvent, setEditingEvent] = useState<ScheduleEvent | undefined>();
     const [createDefaultDate, setCreateDefaultDate] = useState<string | undefined>();

     const { year, month } = currentMonth;
     const today = todayISO();

     const firstDayOfMonth = new Date(year, month, 1).getDay();        // 0=周日
     const daysInMonth = new Date(year, month + 1, 0).getDate();
     const prevMonthDays = new Date(year, month, 0).getDate();

     // 42 格: 前补上月尾,中本月,后补下月头
     const cells: { dateStr: string; dayNum: number; isCurrentMonth: boolean; isToday: boolean; dayEvents: ScheduleEvent[] }[] = [];
     for (let i = 0; i < 42; i++) {
       let dayNum: number, y = year, m = month, isCurrentMonth = true;
       if (i < firstDayOfMonth) {
         dayNum = prevMonthDays - firstDayOfMonth + i + 1;
         m = month - 1;
         if (m < 0) { m = 11; y = year - 1; }
         isCurrentMonth = false;
       } else if (i >= firstDayOfMonth + daysInMonth) {
         dayNum = i - firstDayOfMonth - daysInMonth + 1;
         m = month + 1;
         if (m > 11) { m = 0; y = year + 1; }
         isCurrentMonth = false;
       } else {
         dayNum = i - firstDayOfMonth + 1;
       }
       const dateStr = toISO(y, m, dayNum);
       cells.push({
         dateStr,
         dayNum,
         isCurrentMonth,
         isToday: dateStr === today,
         dayEvents: events.filter((e) => e.date === dateStr),
       });
     }

     // Agenda: 本月事件,按 date+time 升序,top 8
     const monthStart = toISO(year, month, 1);
     const monthEnd = toISO(year, month, daysInMonth);
     const agendaEvents = events
       .filter((e) => e.date >= monthStart && e.date <= monthEnd)
       .sort((a, b) => a.date.localeCompare(b.date) || a.time.localeCompare(b.time))
       .slice(0, 8);

     const goPrev = () => setCurrentMonth(({ year, month }) => month === 0 ? { year: year - 1, month: 11 } : { year, month: month - 1 });
     const goNext = () => setCurrentMonth(({ year, month }) => month === 11 ? { year: year + 1, month: 0 } : { year, month: month + 1 });
     const goToday = () => { const d = new Date(); setCurrentMonth({ year: d.getFullYear(), month: d.getMonth() }); };

     const openCreate = (defaultDate?: string) => {
       setEditingEvent(undefined);
       setCreateDefaultDate(defaultDate);
       setDialogOpen(true);
     };
     const openEdit = (event: ScheduleEvent) => {
       setEditingEvent(event);
       setCreateDefaultDate(undefined);
       setDialogOpen(true);
     };

     return (
       <div className="flex gap-5 h-full min-h-[700px]">
         <Card className="flex-1 flex flex-col p-5">
           <div className="flex items-center justify-between mb-6">
             <div className="flex items-center gap-3">
               <h2 className="text-xl font-bold text-text-primary">{year}年 {month + 1}月</h2>
               <div className="flex items-center gap-0.5">
                 <Button variant="ghost" size="xs" onClick={goPrev}><CaretLeft size={14} weight="bold" /></Button>
                 <Button variant="ghost" size="xs" className="text-xs font-medium px-2" onClick={goToday}>今天</Button>
                 <Button variant="ghost" size="xs" onClick={goNext}><CaretRight size={14} weight="bold" /></Button>
               </div>
             </div>
             <SegmentedControl
               segments={[{ id: 'month', label: '月视图' }, { id: 'week', label: '周视图' }]}
               value={viewMode}
               onChange={setViewMode}
             />
           </div>

           <div className="grid grid-cols-7 gap-px bg-border-subtle rounded-[var(--radius-md)] overflow-hidden flex-1 border border-border-subtle">
             {['周日', '周一', '周二', '周三', '周四', '周五', '周六'].map((d) => (
               <div key={d} className="bg-bg-secondary py-2.5 text-center text-xs font-semibold text-text-tertiary">{d}</div>
             ))}
             {cells.map((cell, i) => (
               <div
                 key={i}
                 className={cn(
                   'bg-bg-primary min-h-[90px] p-1.5 transition-colors hover:bg-bg-secondary/50 cursor-pointer',
                   !cell.isCurrentMonth && 'opacity-25'
                 )}
                 onClick={() => cell.isCurrentMonth && openCreate(cell.dateStr)}
               >
                 <div className={cn(
                   'w-6 h-6 flex items-center justify-center rounded-full text-xs font-medium mb-0.5',
                   cell.isToday ? 'bg-accent text-white' : 'text-text-primary'
                 )}>
                   {cell.dayNum}
                 </div>
                 {cell.isCurrentMonth && cell.dayEvents.length > 0 && (
                   <div className="space-y-0.5">
                     {cell.dayEvents.map((e) => {
                       const c = EVENT_COLORS[e.type] || EVENT_COLORS.meeting;
                       return (
                         <div
                           key={e.id}
                           onClick={(ev) => { ev.stopPropagation(); openEdit(e); }}
                           className={cn('px-1 py-px text-[10px] rounded font-medium truncate', c.bg, c.text)}
                         >
                           {e.time.split(' ')[0]} {e.title}
                         </div>
                       );
                     })}
                   </div>
                 )}
               </div>
             ))}
           </div>
         </Card>

         <Card className="w-72 p-5 flex flex-col shrink-0">
           <h3 className="text-base font-bold text-text-primary mb-4 flex items-center justify-between">
             本月日程
             <Badge variant="neutral">{agendaEvents.length} 个事件</Badge>
           </h3>
           <div className="flex-1 space-y-3 overflow-y-auto pr-1">
             {agendaEvents.length === 0 ? (
               <p className="text-xs text-text-tertiary py-6 text-center">本月暂无日程</p>
             ) : agendaEvents.map((event) => {
               const c = EVENT_COLORS[event.type] || EVENT_COLORS.meeting;
               const { m, d } = parseISO(event.date);
               return (
                 <div key={event.id} className="relative pl-5 pb-4 border-l-2 border-border-subtle last:border-transparent last:pb-0">
                   <div className={cn('absolute -left-[5px] top-0 w-2 h-2 rounded-full', c.dot)} />
                   <div
                     className="bg-bg-secondary rounded-[var(--radius-md)] p-3 hover:bg-bg-tertiary transition-colors cursor-pointer group"
                     onClick={() => openEdit(event)}
                   >
                     <div className="flex justify-between items-start mb-1.5">
                       <h4 className="text-sm font-semibold text-text-primary group-hover:text-accent transition-colors">{event.title}</h4>
                       <Badge variant={event.date === today ? 'accent' : 'neutral'} className="text-[10px]">
                         {event.date === today ? '今天' : `${m}月${d}日`}
                       </Badge>
                     </div>
                     <div className="flex items-center gap-1.5 text-xs text-text-tertiary mb-2">
                       <Clock size={12} weight="duotone" />
                       {event.time}
                     </div>
                     {event.location && (
                       <div className="flex items-center gap-1.5 text-xs text-text-secondary bg-bg-primary px-2 py-1 rounded-[var(--radius-sm)] border border-border-subtle w-fit">
                         {event.location.includes('线上') || event.location.includes('Meeting')
                           ? <VideoCamera size={12} weight="duotone" className="text-accent" />
                           : <MapPin size={12} weight="duotone" className="text-danger" />}
                         {event.location}
                       </div>
                     )}
                   </div>
                 </div>
               );
             })}
           </div>
           <Button variant="primary" className="w-full mt-3" onClick={() => openCreate()}>
             <Plus size={14} weight="duotone" />
             新建日程
           </Button>
         </Card>

         <ScheduleDialog
           open={dialogOpen}
           onOpenChange={setDialogOpen}
           mode={editingEvent ? 'edit' : 'create'}
           event={editingEvent}
           defaultDate={createDefaultDate}
         />
       </div>
     );
   }
   ```

   注意点:
   - `parseISO` 返回 `{ y, m, d }` 但 Agenda 只用 `m, d`,所以解构时只取需要的(`const { m, d } = parseISO(...)`)。lint 若警告未用 y,把 parseISO 改为只返回 `{ m, d }` 或在解构里省略。
   - 所有 date 字符串比较都是字符串字典序(YYYY-MM-DD 字典序 = 时间序,正确)。
   - `new Date()` 在 useState initializer 里取一次,避免重渲染时"今天"漂移;`today` 常量每次 render 取,可接受(用户不会跨午夜使用)。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    ScheduleView 真实月历就绪:currentMonth state 驱动、月份切换、今天高亮、事件按 date 字符串归位、点击事件进编辑、新建按钮 + 日期格点击都能调起 ScheduleDialog。Pitfall P20 修复。
  </done>
</task>

</tasks>

<verification>
- `npm run lint` 通过
- 月历标题动态显示 `${year}年 ${month+1}月`,不再是写死 "2025年 5月"
- 上/下月/今天按钮都能改变 currentMonth,日历重渲染
- 今天日期格有 accent 圆形高亮
- 事件 chip 出现在其 date 字符串对应的日期格
- 点击事件 chip 触发 ScheduleDialog edit 模式,字段预填
- 点击日期格空白触发 ScheduleDialog create 模式,日期预填为该格
- Agenda 侧栏 "新建日程" 按钮触发 ScheduleDialog create 模式
- Agenda 侧栏显示本月事件(切月份后内容跟着变)
</verification>

<success_criteria>
1. 月历不再写死 2025-5 (Pitfall P20 修复) — SCHED-04 ✓
2. 月份切换 (上/下/今天) 正确,日历实时重渲染 — SCHED-04 ✓
3. 事件按 date 字符串 (YYYY-MM-DD) 正确归位到日期格 — SCHED-04 ✓
4. ScheduleDialog 创建/编辑入口接入 (新建按钮 + 日期格点击 + 事件 chip 点击 + Agenda 点击)
5. ScheduleView 直接 useScheduleStore selector (不经 AppContext,Key Decision #2)
6. Agenda 侧栏展示 currentMonth 内事件(切月份后内容更新)
</success_criteria>

<output>
After completion, create `.planning/phases/06-schedule-crud/06-03-SUMMARY.md`
</output>
