---
phase: 07-cross-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/scheduleStore.ts
  - src/stores/rndStore.ts
  - src/stores/taskStore.ts
  - src/data/mockProducts.ts
  - src/store/AppContext.tsx
autonomous: true
requirements: [CROSS-01, CROSS-02, CROSS-03, CROSS-07, L5, L6, L7]

must_haves:
  truths:
    - "ScheduleEvent 接口包含可选 projectId?/taskId? 字段、type 联合类型包含 'task'、status 字段默认 '未开始'"
    - "scheduleStore 暴露 createEvent / updateEvent / deleteEvent / setEventStatus 4 个新 action,persist v2 migrate 为旧数据补字段"
    - "taskStore.completeTask 在标记任务完成后,若 task.scheduledEventId 存在,同步调用 scheduleStore.setEventStatus(eventId, '已完成') (CROSS-07)"
    - "taskStore.deleteTask 在删除任务时,若 task.scheduledEventId 存在,调用 scheduleStore.clearTaskLink(eventId) 双向清理"
    - "rndStore 暴露 cleanupProduct(productId) 和 getDeliverableStatusForPhase(productId, phase) 两个新 action (L7 + L6)"
    - "ProductMilestone 接口新增 deliverableCodes?: string[] 字段 (L5),deliverables 保留向后兼容"
    - "AppContext 暴露 arrangeOnCalendar(taskId) wrapper (CROSS-01/D-01/D-02/D-03) 和 deleteProductWrapped 增强版 (CROSS-03/D-04/D-05/D-06)"
  artifacts:
    - path: "src/stores/scheduleStore.ts"
      provides: "ScheduleEvent 扩展 + 4 个新 action + persist v2"
      contains: "createEvent"
    - path: "src/stores/rndStore.ts"
      provides: "cleanupProduct + getDeliverableStatusForPhase"
      contains: "cleanupProduct"
    - path: "src/stores/taskStore.ts"
      provides: "completeTask + deleteTask 跨 store 同步副作用"
      contains: "useScheduleStore.getState()"
    - path: "src/data/mockProducts.ts"
      provides: "ProductMilestone.deliverableCodes 可选字段"
      contains: "deliverableCodes"
    - path: "src/store/AppContext.tsx"
      provides: "arrangeOnCalendar + deleteProductWrapped (enhanced) + 新 action delegate"
      contains: "arrangeOnCalendar"
  key_links:
    - from: "src/store/AppContext.tsx"
      to: "src/stores/scheduleStore.ts + taskStore + rndStore"
      via: "arrangeOnCalendar 和 deleteProductWrapped 编排多 store actions"
      pattern: "useScheduleStore\\.getState\\(\\)|useRndStore\\.getState\\(\\)"
    - from: "src/stores/taskStore.ts"
      to: "src/stores/scheduleStore.ts"
      via: "completeTask 触发 scheduleStore.setEventStatus (CROSS-07)"
      pattern: "useScheduleStore\\.getState\\(\\)\\.setEventStatus"
---

<objective>
扩展四个 store 的契约层,补齐 Phase 7 所有跨模块联动所需的字段、actions、和 AppContext 编排 wrappers。本 plan 只交付契约,不涉及 UI (UI 在 02/03/04 plan)。

Purpose: Phase 7 所有 UI 联动 (安排到日历 / 关联徽章 / 删除清理 / 任务完成联动 / L5/L6 展示) 都依赖这些 store action 和扩展后的类型。先交付契约层,后续 UI plan 才能直接消费,避免 store/UI 交错修改。
Output: scheduleStore 4 个新 action + rndStore 2 个新 action + taskStore 副作用 + ProductMilestone 字段 + AppContext 2 个 wrapper
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-module/07-CONTEXT.md
@.planning/research/PRODUCT-RND-LINKAGE.md

@src/stores/scheduleStore.ts
@src/stores/taskStore.ts
@src/stores/rndStore.ts
@src/stores/productStore.ts
@src/store/AppContext.tsx
@src/data/mockProducts.ts
@src/data/mockRndData.ts
@src/data/mockTasks.ts

<interfaces>
<!-- 现有 ScheduleEvent (src/stores/scheduleStore.ts) — Phase 6 已扩展? 需先检查 -->
```typescript
export interface ScheduleEvent {
  id: string;
  title: string;
  time: string;
  date: number;       // ⚠️ 这是 day-of-month number, 不是 ISO date
  type: string;       // 当前是 open string,需要扩展为 union 含 'task'
  location: string;
}
```

**注意:** 当前沿途的 scheduleStore.date 是 `number` (月份中的日期),没有 projectId/taskId/status。本 plan 需扩展。

<!-- 现有 taskStore.completeTask (src/stores/taskStore.ts) -->
```typescript
completeTask: (taskId) =>
  set((state) => ({
    categories: state.categories.map((cat) => ({
      ...cat,
      tasks: cat.tasks.map((task) =>
        task.id === taskId ? { ...task, status: '已完成' } : task
      ),
    })),
  })),
```

<!-- 现有 productStore.deleteProduct (无级联) -->
```typescript
deleteProduct: (id) =>
  set((state) => ({
    products: state.products.filter((p) => p.id !== id),
  })),
```

<!-- 现有 AppContext.deleteProductWrapped (仅清 selectedProductId) -->
```typescript
const deleteProductWrapped = (id: string) => {
  deleteProduct(id);
  if (selectedProductId === id) setSelectedProductId(null);
};
```

<!-- ProductMilestone.deliverables 当前是 string[] 自由文本 -->
```typescript
export interface ProductMilestone {
  id?: string;
  title: string;
  date: string;
  stage?: string;
  status: 'pending' | 'in-progress' | 'completed';
  owner?: string;
  deliverables?: string[];     // 自由字符串,如 ['PRD v3.0']
  description?: string;
}
```

<!-- FullLifecycleDeliverable (src/data/mockRndData.ts) -->
```typescript
export interface FullLifecycleDeliverable {
  id: string;
  productId: string;
  phase: 'requirement' | 'design' | 'dev' | 'test' | 'release';
  code: string;        // 'DEL-REQ-01' ...
  title: string;
  status: 'ready' | 'generating' | 'draft';
  // ...其他字段
}
```

<!-- Product.stage (mockProducts.ts) -->
```typescript
stage: '规划中' | '研发中' | '公测灰度' | '商业化运营' | '已发布';
```

**stage → deliverable.phase 映射 (L6,本 plan 定义):**
- '规划中' → 'requirement'
- '研发中' → 'design' + 'dev' (累加)
- '公测灰度' → 'test'
- '商业化运营' → 'release'
- '已发布' → 'release' (全部)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 ScheduleEvent 类型 + 4 个新 action + persist v2</name>
  <files>src/stores/scheduleStore.ts</files>
  <read_first>
    - src/stores/scheduleStore.ts (现有 ScheduleEvent + INITIAL_EVENTS + persist 配置)
    - src/data/mockTasks.ts (Task.scheduledEventId 已定义,确认契约一致)
    - .planning/phases/07-cross-module/07-CONTEXT.md (D-01/D-11/D-12 决策)
  </read_first>
  <behavior>
    - ScheduleEvent 接口扩展:新增 `projectId?: string`、`taskId?: string`、`status?: '未开始' | '进行中' | '已完成'` (默认 '未开始',optional 以保旧数据兼容)、`type` 从 `string` 改为 `'meeting' | 'review' | 'sync' | 'task' | string` (union 扩展,允许 'task')
    - createEvent(event: ScheduleEvent): 旧 addEvent 改名,行为不变 (去重 + 按 date 排序)
    - updateEvent(eventId, updates): 找到 event 合并 updates,找不到 no-op
    - deleteEvent(eventId): filter 移除
    - setEventStatus(eventId, status): 设置 status 字段 (CROSS-07 用)
    - clearTaskLink(eventId): 设置 taskId = undefined (双向清理用,等同于 updateEvent(eventId, { taskId: undefined }) 但语义明确)
    - persist version 1 → 2,migrate 为旧 event 补 projectId/taskId/status undefined
  </behavior>
  <action>
1. 编辑 `src/stores/scheduleStore.ts`:

   a. 扩展 `ScheduleEvent` 接口:
   ```typescript
   export interface ScheduleEvent {
     id: string;
     title: string;
     time: string;
     date: number;
     type: 'meeting' | 'review' | 'sync' | 'task' | string;  // 扩展 union
     location: string;
     projectId?: string;   // 🆕 弱关联产品 (CROSS-04)
     taskId?: string;      // 🆕 反向引用任务 (CROSS-01)
     status?: '未开始' | '进行中' | '已完成';  // 🆕 D-11/D-12
   }
   ```

   b. `ScheduleState` 接口扩展 — 把 `addEvent` 改名为 `createEvent`,加 4 个新 action:
   ```typescript
   interface ScheduleState {
     events: ScheduleEvent[];
     setEvents: (events: ScheduleEvent[]) => void;
     // 🆕 Phase 7
     createEvent: (event: ScheduleEvent) => void;       // 旧 addEvent 改名
     updateEvent: (eventId: string, updates: Partial<ScheduleEvent>) => void;
     deleteEvent: (eventId: string) => void;
     setEventStatus: (eventId: string, status: '未开始' | '进行中' | '已完成') => void;
     clearTaskLink: (eventId: string) => void;
     // Persistence
     _hasHydrated: boolean;
     _setHydrated: () => void;
   }
   ```
   **注意:** `addEvent` 删除并改名为 `createEvent`。需要检查调用方 — 若有外部代码用 `addEvent`,在本 plan Task 3 (AppContext) 中保留 `addEvent` alias 委托到 `createEvent` 以兼容。

   c. 实现 5 个 action (在 `create<ScheduleState>()(persist(...))` 内):
   ```typescript
   createEvent: (event) =>
     set((state) => {
       if (state.events.some((e) => e.id === event.id)) return state;
       const newEvent: ScheduleEvent = { ...event, status: event.status ?? '未开始' };
       return { events: [...state.events, newEvent].sort((a, b) => a.date - b.date) };
     }),

   updateEvent: (eventId, updates) =>
     set((state) => ({
       events: state.events.map((e) => (e.id === eventId ? { ...e, ...updates } : e)),
     })),

   deleteEvent: (eventId) =>
     set((state) => ({
       events: state.events.filter((e) => e.id !== eventId),
     })),

   setEventStatus: (eventId, status) =>
     set((state) => ({
       events: state.events.map((e) => (e.id === eventId ? { ...e, status } : e)),
     })),

   clearTaskLink: (eventId) =>
     set((state) => ({
       events: state.events.map((e) => (e.id === eventId ? { ...e, taskId: undefined } : e)),
     })),
   ```

   d. persist 升级到 v2:
   ```typescript
   {
     name: 'nova-schedule',
     version: 2,
     storage: sqliteStorage,
     partialize: (s) => ({ events: s.events }),
     migrate: (persisted: any, version: number) => {
       if (!persisted || !persisted.events) return persisted as Partial<ScheduleState>;
       if (version < 2) {
         persisted.events = persisted.events.map((e: any) => ({
           ...e,
           projectId: e.projectId ?? undefined,
           taskId: e.taskId ?? undefined,
           status: e.status ?? '未开始',
           // type 兼容: 旧 string 值保留, 新增 'task' 由调用方传入
         }));
       }
       return persisted as Partial<ScheduleState>;
     },
     onRehydrateStorage: () => (state) => { state?._setHydrated(); },
   }
   ```

   e. **不修改 INITIAL_EVENTS** — mock 数据保持精简,新字段由 migrate 兜底。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    ScheduleEvent 扩展完成,5 个新 action (createEvent/updateEvent/deleteEvent/setEventStatus/clearTaskLink) 实现并类型安全,persist v2 migration 兜底旧数据。
  </done>
</task>

<task type="auto">
  <name>Task 2: rndStore 新增 cleanupProduct + getDeliverableStatusForPhase + ProductMilestone.deliverableCodes</name>
  <files>src/stores/rndStore.ts, src/data/mockProducts.ts</files>
  <read_first>
    - src/stores/rndStore.ts (现有 7 个 Record + actions + getProd helper)
    - src/data/mockProducts.ts (ProductMilestone + mock milestones 的 deliverables 字段格式)
    - src/data/mockRndData.ts (FULL_LIFECYCLE_DELIVERABLES_CATALOG 的 phase/code 字段)
    - .planning/research/PRODUCT-RND-LINKAGE.md (§5.1 接口设计 + §6 字段映射)
  </read_first>
  <behavior>
    - ProductMilestone 接口加 `deliverableCodes?: string[]` (可选,L5),保留 `deliverables?: string[]` 向后兼容
    - rndStore.cleanupProduct(productId): 清理 requirements[productId]、prototypes[productId]、knowledgeBase[productId]、codeScaffolds[productId]、testCases[productId]、competitorData[productId]、deliverables[productId] 七个 key (用 `delete` 操作符或解构 omit)
    - rndStore.getDeliverableStatusForPhase(productId, phase): 返回 `{ total, ready, generating, draft }`,基于 deliverables[productId].filter(d => d.phase === phase);若 deliverables[productId] 不存在,先调 getDeliverablesForProduct 懒初始化
  </behavior>
  <action>
1. 编辑 `src/data/mockProducts.ts`:
   - `ProductMilestone` 接口加一行 (deliverables 之后):
   ```typescript
   deliverableCodes?: string[];  // 🆕 L5: 引用 FullLifecycleDeliverable.code
   ```
   - **不修改 mock milestones 数据** — 现有 `deliverables: ['PRD v3.0', ...]` 保留;L5 UI 层在 plan 04 中处理 fallback (优先读 deliverableCodes,无则用 deliverables)

2. 编辑 `src/stores/rndStore.ts`:
   - `RndState` 接口在 `syncDeliverableToDocs` 之后、`initDeliverablesForProduct` 之前,加:
   ```typescript
   // ── Phase 7: product-rnd linkage (L6/L7) ────────────────────────────────
   cleanupProduct: (productId: string) => void;
   getDeliverableStatusForPhase: (
     productId: string,
     phase: 'requirement' | 'design' | 'dev' | 'test' | 'release'
   ) => { total: number; ready: number; generating: number; draft: number };
   ```

   - 在 `syncDeliverableToDocs` 实现 (约 line 503-515) 之后、`initDeliverablesForProduct` (约 line 520) 之前,加两个 action:
   ```typescript
   cleanupProduct: (productId) =>
     set((state) => {
       const omit = <T extends Record<string, any>>(rec: T, key: string) => {
         const { [key]: _removed, ...rest } = rec;
         return rest;
       };
       return {
         requirements: omit(state.requirements, productId),
         prototypes: omit(state.prototypes, productId),
         knowledgeBase: omit(state.knowledgeBase, productId),
         codeScaffolds: omit(state.codeScaffolds, productId),
         testCases: omit(state.testCases, productId),
         competitorData: omit(state.competitorData, productId),
         deliverables: omit(state.deliverables, productId),
       };
     }),

   getDeliverableStatusForPhase: (productId, phase) => {
     const list = get().getDeliverablesForProduct(productId);
     const phaseList = list.filter((d) => d.phase === phase);
     return {
       total: phaseList.length,
       ready: phaseList.filter((d) => d.status === 'ready').length,
       generating: phaseList.filter((d) => d.status === 'generating').length,
       draft: phaseList.filter((d) => d.status === 'draft').length,
     };
   },
   ```
   **注意:** `getDeliverableStatusForPhase` 调用 `get().getDeliverablesForProduct(productId)` 触发懒初始化 (若 deliverables[productId] 不存在,会写入 state),这是 rndStore 既有模式,无需特殊处理。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    ProductMilestone 含 deliverableCodes?,rndStore 暴露 cleanupProduct (L7) 和 getDeliverableStatusForPhase (L6),后续 AppContext.deleteProductWrapped 和 UI plan 可直接调用。
  </done>
</task>

<task type="auto">
  <name>Task 3: taskStore.completeTask/deleteTask 跨 store 副作用 + AppContext wrappers</name>
  <files>src/stores/taskStore.ts, src/store/AppContext.tsx</files>
  <read_first>
    - src/stores/taskStore.ts (现有 completeTask + deleteTask + import)
    - src/store/AppContext.tsx (现有 deleteProductWrapped + addProject + 5 个 Phase 5 delegate + value 对象)
    - src/stores/scheduleStore.ts (Task 1 完成后的 createEvent/setEventStatus/clearTaskLink)
    - .planning/phases/07-cross-module/07-CONTEXT.md (D-01..D-12 所有决策)
  </read_first>
  <behavior>
    - taskStore 顶部 import: `import { useScheduleStore } from './scheduleStore';`
    - completeTask(taskId): 在 set() 标记 '已完成' **之后**,通过 get() 读到 task.scheduledEventId,若存在则 `useScheduleStore.getState().setEventStatus(eventId, '已完成')` (CROSS-07)
    - deleteTask(taskId): 在 set() 删除 **之前**,先从 get().categories 找到 task.scheduledEventId,若存在则 `useScheduleStore.getState().clearTaskLink(eventId)` (CROSS-05 双向清理)
    - AppContext.arrangeOnCalendar(taskId): wrapper 编排 — 读 task → 构造 ScheduleEvent → scheduleStore.createEvent → taskStore.updateTask 写回 scheduledEventId → 返回 { success, event?, reason? } 给调用方做 toast
    - AppContext.deleteProductWrapped(id): 增强 — 计算关联计数 (tasks with projectId === id, events with projectId === id, rndStore 该 productId 是否有数据) → 返回 { taskCount, eventCount, hasRndData } 给调用方展示确认对话框;**实际清理在 plan 03 中由调用方二次确认后调用** doDeleteProduct(id) wrapper (本 task 实现 doDeleteProduct,内部调用 taskStore.unlinkProjectTasks、scheduleStore 清理、rndStore.cleanupProduct、deleteProduct)
    - AppContext 新增 delegate: createEvent / updateEvent / deleteEvent / setEventStatus / clearTaskLink / getDeliverableStatusForPhase
  </behavior>
  <action>
1. 编辑 `src/stores/taskStore.ts`:

   a. 顶部加 import (在 `import { useProductStore }` 之后):
   ```typescript
   import { useScheduleStore } from './scheduleStore';
   ```

   b. 重写 `completeTask` 加 CROSS-07 副作用:
   ```typescript
   completeTask: (taskId) => {
     // 先读取 task.scheduledEventId (在 set 之前)
     const before = get().categories.flatMap((c) => c.tasks).find((t) => t.id === taskId);
     const eventId = before?.scheduledEventId;
     set((state) => ({
       categories: state.categories.map((cat) => ({
         ...cat,
         tasks: cat.tasks.map((task) =>
           task.id === taskId ? { ...task, status: '已完成' } : task
         ),
       })),
     }));
     // CROSS-07: 同步标记关联日程完成 ( ponytail: store action 副作用,不走 React effect)
     if (eventId) {
       useScheduleStore.getState().setEventStatus(eventId, '已完成');
     }
   },
   ```

   c. 重写 `deleteTask` 加 CROSS-05 双向清理:
   ```typescript
   deleteTask: (taskId) => {
     // 先读取 task.scheduledEventId (在 set 之前)
     const before = get().categories.flatMap((c) => c.tasks).find((t) => t.id === taskId);
     const eventId = before?.scheduledEventId;
     // CROSS-05: 清除日程的反向 taskId 引用
     if (eventId) {
       useScheduleStore.getState().clearTaskLink(eventId);
     }
     set((state) => ({
       categories: state.categories.map((cat) => ({
         ...cat,
         tasks: cat.tasks.filter((t) => t.id !== taskId),
       })),
     }));
   },
   ```

   d. 新增 `unlinkProjectTasks(projectId)`: 把 task.projectId 为该值的任务的 projectId/project 清空 (CROSS-03 删除产品用):
   ```typescript
   // 加入 TaskState 接口 (Phase 7 新增)
   unlinkProjectTasks: (projectId: string) => void;

   // 实现 (在 setTaskProject 之后):
   unlinkProjectTasks: (projectId) =>
     set((state) => ({
       categories: state.categories.map((cat) => ({
         ...cat,
         tasks: cat.tasks.map((t) =>
           t.projectId === projectId ? { ...t, projectId: undefined, project: '' } : t
         ),
       })),
     })),
   ```

2. 编辑 `src/store/AppContext.tsx`:

   a. AppContextType 接口加 (在 Phase 5 delegate 之后):
   ```typescript
   // Phase 7 schedule delegates
   createEvent: (event: ScheduleEvent) => void;
   updateEvent: (eventId: string, updates: Partial<ScheduleEvent>) => void;
   deleteEvent: (eventId: string) => void;
   setEventStatus: (eventId: string, status: '未开始' | '进行中' | '已完成') => void;
   clearTaskLink: (eventId: string) => void;

   // Phase 7 rnd delegates
   cleanupProduct: (productId: string) => void;
   getDeliverableStatusForPhase: (
     productId: string,
     phase: 'requirement' | 'design' | 'dev' | 'test' | 'release'
   ) => { total: number; ready: number; generating: number; draft: number };

   // Phase 7 task delegate
   unlinkProjectTasks: (projectId: string) => void;

   // Phase 7 跨 store 编排 wrappers
   arrangeOnCalendar: (taskId: string) => { success: boolean; event?: ScheduleEvent; reason?: 'no-deadline' | 'already-arranged' | 'task-not-found' };
   getDeleteProductImpact: (productId: string) => { taskCount: number; eventCount: number; hasRndData: boolean };
   doDeleteProduct: (productId: string) => void;
   ```

   b. AppProvider 内加 selector 订阅 (在 Phase 5 delegate 之后):
   ```typescript
   // Phase 7 schedule
   const createEvent = useScheduleStore((s) => s.createEvent);
   const updateEvent = useScheduleStore((s) => s.updateEvent);
   const deleteEvent = useScheduleStore((s) => s.deleteEvent);
   const setEventStatus = useScheduleStore((s) => s.setEventStatus);
   const clearTaskLink = useScheduleStore((s) => s.clearTaskLink);
   // Phase 7 rnd
   const cleanupProduct = useRndStore((s) => s.cleanupProduct);
   const getDeliverableStatusForPhase = useRndStore((s) => s.getDeliverableStatusForPhase);
   // Phase 7 task
   const unlinkProjectTasks = useTaskStore((s) => s.unlinkProjectTasks);
   ```

   c. 在 `deleteProductWrapped` 声明 (约 line 234) **之前** 加新 wrapper 函数:
   ```typescript
   // Phase 7: 安排任务到日历 (CROSS-01, D-01/D-02)
   const arrangeOnCalendar = (taskId: string) => {
     const task = useTaskStore.getState().categories.flatMap((c) => c.tasks).find((t) => t.id === taskId);
     if (!task) return { success: false, reason: 'task-not-found' as const };
     if (task.scheduledEventId) return { success: false, reason: 'already-arranged' as const };
     // 解析 task.deadline → day-of-month (mock 月份中的日期);若 deadline 缺失则用今天
     const deadlineDay = task.deadline ? parseInt(task.deadline.split('-').pop() || '15', 10) : 15;
     if (!task.deadline) return { success: false, reason: 'no-deadline' as const };
     const event: ScheduleEvent = {
       id: crypto.randomUUID(),
       title: task.title,            // D-02: 标题同步任务
       time: '全天',                 // 默认,用户可在 ScheduleDialog 编辑
       date: isNaN(deadlineDay) ? 15 : Math.max(1, Math.min(31, deadlineDay)),
       type: 'task',
       location: '',
       projectId: task.projectId,    // 顺带继承产品的弱关联
       taskId: task.id,
       status: '未开始',
     };
     useScheduleStore.getState().createEvent(event);
     useTaskStore.getState().updateTask(taskId, { scheduledEventId: event.id });
     return { success: true, event };
   };

   // Phase 7: 删除产品前的影响评估 (CROSS-03, D-04)
   const getDeleteProductImpact = (productId: string) => {
     const taskCount = useTaskStore.getState().categories
       .flatMap((c) => c.tasks)
       .filter((t) => t.projectId === productId).length;
     const eventCount = useScheduleStore.getState().events
       .filter((e) => e.projectId === productId).length;
     const rnd = useRndStore.getState();
     const hasRndData = !!rnd.deliverables[productId] || !!rnd.requirements[productId]
       || !!rnd.prototypes[productId] || !!rnd.knowledgeBase[productId]
       || !!rnd.codeScaffolds[productId] || !!rnd.testCases[productId]
       || !!rnd.competitorData[productId];
     return { taskCount, eventCount, hasRndData };
   };

   // Phase 7: 真正执行删除产品 + 级联清理 (CROSS-03, D-05/D-06, L7)
   const doDeleteProduct = (productId: string) => {
     // 1. 清空任务的 projectId (CROSS-03)
     useTaskStore.getState().unlinkProjectTasks(productId);
     // 2. 清空日程的 projectId (CROSS-03)
     useScheduleStore.getState().events
       .filter((e) => e.projectId === productId)
       .forEach((e) => useScheduleStore.getState().updateEvent(e.id, { projectId: undefined }));
     // 3. 清理 rndStore (L7)
     useRndStore.getState().cleanupProduct(productId);
     // 4. 删除产品本体
     deleteProduct(productId);
     // 5. 清选中态
     if (selectedProductId === productId) setSelectedProductId(null);
   };
   ```

   d. 把原 `deleteProductWrapped` **替换**为返回 impact 的两段式 (用于 UI 二次确认)。`deleteProduct: deleteProductWrapped` 改为 `deleteProduct: getDeleteProductImpact` 不行 — 类型不匹配。**保留** `deleteProduct` 委托到 store 的 `deleteProduct` (无级联,纯删除),UI 调用 `getDeleteProductImpact` + 用户确认后调用 `doDeleteProduct`。

   ```typescript
   // 替换原 deleteProductWrapped:
   // (删除 line 234-237 的 const deleteProductWrapped,doDeleteProduct 已经包含其逻辑)
   ```

   在 `value` 对象中,把 `deleteProduct: deleteProductWrapped` 改回 `deleteProduct` (delegate 到 productStore,纯删除,无副作用)。新增字段:
   ```typescript
   // 在 deleteProduct 之后加:
   createEvent, updateEvent, deleteEvent, setEventStatus, clearTaskLink,
   cleanupProduct, getDeliverableStatusForPhase,
   unlinkProjectTasks,
   arrangeOnCalendar, getDeleteProductImpact, doDeleteProduct,
   ```

   e. 兼容 `addEvent` 调用方:在 value 对象加 `addEvent: createEvent` alias (旧代码可能仍调 addEvent)。
   ```typescript
   // 保留 value 中的 addEvent: createEvent (alias) 直到 plan 02 替换完毕
   ```
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    taskStore.completeTask/deleteTask 触发 scheduleStore 同步副作用 (CROSS-07/CROSS-05);AppContext 暴露 arrangeOnCalendar (CROSS-01/02)、getDeleteProductImpact + doDeleteProduct (CROSS-03/L7)、5 个 schedule delegate、2 个 rnd delegate、unlinkProjectTasks。后续 plan 02/03/04 UI 可直接消费这些 wrapper。
  </done>
</task>

</tasks>

<verification>
- `npm run lint` (tsc --noEmit) 通过,无类型错误
- grep 验证: src/stores/scheduleStore.ts 包含 `createEvent:`、`updateEvent:`、`deleteEvent:`、`setEventStatus:`、`clearTaskLink:`、`version: 2`、`projectId?`、`taskId?`、`status?`
- grep 验证: src/stores/rndStore.ts 包含 `cleanupProduct:`、`getDeliverableStatusForPhase:`
- grep 验证: src/data/mockProducts.ts 包含 `deliverableCodes?`
- grep 验证: src/stores/taskStore.ts 包含 `useScheduleStore.getState().setEventStatus`、`useScheduleStore.getState().clearTaskLink`、`unlinkProjectTasks:`
- grep 验证: src/store/AppContext.tsx 包含 `arrangeOnCalendar`、`getDeleteProductImpact`、`doDeleteProduct`、`createEvent`、`cleanupProduct`、`getDeliverableStatusForPhase`
</verification>

<success_criteria>
1. ScheduleEvent 扩展 projectId?/taskId?/status?,scheduleStore 5 个新 action + persist v2 migration 兜底 (CROSS-01/02/05/07 contract ✓)
2. rndStore.cleanupProduct(productId) 清理 7 个 Record key (L7 contract ✓)
3. rndStore.getDeliverableStatusForPhase(productId, phase) 返回 ready/draft/generating 统计 (L6 contract ✓)
4. ProductMilestone 接口加 deliverableCodes? (L5 contract ✓)
5. taskStore.completeTask 触发 scheduleStore.setEventStatus (CROSS-07 ✓)
6. taskStore.deleteTask 触发 scheduleStore.clearTaskLink (CROSS-05 ✓)
7. AppContext.arrangeOnCalendar wrapper 编排 task → event → 写回 scheduledEventId (CROSS-01/02 ✓)
8. AppContext.getDeleteProductImpact + doDeleteProduct 编排跨 store 清理 (CROSS-03/L7 ✓)
9. AppContext 暴露所有新 action 给 legacy useApp() 调用方
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-module/07-01-SUMMARY.md`
</output>
