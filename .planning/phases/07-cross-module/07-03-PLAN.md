---
phase: 07-cross-module
plan: 03
type: execute
wave: 3
depends_on: [07-01]
files_modified:
  - src/components/product/ProductManagementView.tsx
  - src/components/CreateProductModal.tsx
autonomous: false
requirements: [CROSS-03, CROSS-05, CROSS-07, L7]

must_haves:
  truths:
    - "用户点击删除产品按钮时,弹出确认对话框显示动态文案 'X 个任务、Y 个日程将失去关联,Z 个研发数据将被清理' (D-04)"
    - "确认后:任务 projectId 清空、日程 projectId 清空、rndStore 该 productId 数据全部清理、产品从列表移除 (D-05/D-06/L7)"
    - "任务完成时,若有关联日程,日程自动同步标记完成 (CROSS-07 — taskStore.completeTask 已在 07-01 实现,本 plan 仅做 UAT 验证)"
    - "删除关联日程时,任务的 scheduledEventId 自动清空;删除已关联任务时,日程的 taskId 自动清空 (CROSS-05 — store 层已在 07-01 实现,本 plan 仅做 UAT 验证)"
  artifacts:
    - path: "src/components/product/ProductManagementView.tsx"
      provides: "删除产品前调用 getDeleteProductImpact + 弹 Dialog 显示影响 + 确认后调用 doDeleteProduct"
      contains: "getDeleteProductImpact"
  key_links:
    - from: "src/components/product/ProductManagementView.tsx (或实际承载删除产品的组件)"
      to: "AppContext.getDeleteProductImpact + doDeleteProduct"
      via: "删除按钮 → getDeleteProductImpact → Dialog → 确认 → doDeleteProduct"
      pattern: "getDeleteProductImpact|doDeleteProduct"
---

<objective>
把 07-01 的 store/AppContext 删除清理契约接入产品管理 UI。CROSS-07 (任务完成联动日程标记) 和 CROSS-05 (双向清理) 的 store 层已在 07-01 实现,本 plan 仅补 UI + UAT 验证。

Purpose: Phase 7 删除产品的 UX 安全感 — 用户在删除前看到"会失去什么",确认后无残留。CROSS-07/CROSS-05 的 store 副作用已经在 07-01 落地 (completeTask 触发 setEventStatus、deleteTask 触发 clearTaskLink),不需要额外 UI 代码,本 plan 通过 UAT 验证联动正确。
Output: 产品删除二次确认对话框 (动态影响文案) + UAT 验证
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-cross-module/07-CONTEXT.md
@.planning/phases/07-cross-module/07-01-SUMMARY.md

@src/store/AppContext.tsx
@src/components/ui/Dialog.tsx
@src/components/ui/Button.tsx

<interfaces>
<!-- 来自 07-01 的契约 -->

```typescript
// AppContext 暴露 (07-01 Task 3 完成):
getDeleteProductImpact(productId: string): {
  taskCount: number;       // 该产品关联的任务数
  eventCount: number;      // 该产品关联的日程数
  hasRndData: boolean;     // 是否有研发数据 (rndStore)
};

doDeleteProduct(productId: string): void;
// 内部: unlinkProjectTasks → updateEvents 清 projectId → cleanupProduct → deleteProduct → setSelectedProductId(null)

// taskStore.completeTask 已增强 (07-01 Task 3): 自动调用 scheduleStore.setEventStatus(eventId, '已完成')
// taskStore.deleteTask 已增强 (07-01 Task 3): 自动调用 scheduleStore.clearTaskLink(eventId)
```

**注意:** 找到当前调用 `deleteProduct` 的 UI 位置 (executor 需 grep `deleteProduct` 用法),通常在 ProductManagementView 列表项或产品详情的删除按钮。本 plan 把直接调用改为"先 getDeleteProductImpact + Dialog + 确认后 doDeleteProduct"。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 产品删除前显示影响确认 Dialog (CROSS-03, D-04/D-05/D-06, L7)</name>
  <files>src/components/product/ProductManagementView.tsx, src/components/CreateProductModal.tsx</files>
  <read_first>
    - src/components/product/ProductManagementView.tsx (找到现有删除产品的入口)
    - 用 Grep 找 `deleteProduct` 调用方 (Bash: `grep -rn "deleteProduct" src/components/product/ src/views/`)
    - src/store/AppContext.tsx (07-01 完成后的 getDeleteProductImpact + doDeleteProduct 契约)
    - .planning/phases/07-cross-module/07-CONTEXT.md (D-04/D-05/D-06)
    - src/components/ui/Dialog.tsx (现有 Dialog 组件)
  </read_first>
  <behavior>
    - 找到当前直接调用 deleteProduct(id) 的 UI 位置 (列表项 DotsMenu / 详情删除按钮)
    - 改为先打开影响确认 Dialog:调用 getDeleteProductImpact(id) → 动态生成文案:
      - "此操作将解除 X 个任务、Y 个日程的关联"
      - 若 hasRndData: 追加 ",研发中心的交付物/里程碑/原型/需求/知识/代码/测试/竞品将被清理"
      - 结尾:"是否继续?"
    - Dialog 按钮: 取消 (secondary) + 确认删除 (danger)
    - 确认后:调用 doDeleteProduct(id) → toast 成功 → 关闭 Dialog
    - **不修改** taskStore.completeTask / deleteTask (07-01 已实现 CROSS-07/CROSS-05 store 副作用)
  </behavior>
  <action>
1. 用 Grep 找到当前 `deleteProduct` 的 UI 调用位置。基于项目结构猜测在 `src/components/product/ProductManagementView.tsx` 或产品列表/卡片组件中。executor 应先定位确切位置再编辑。

2. 在该组件内:
   a. 引入 useApp 的 getDeleteProductImpact + doDeleteProduct + useToast:
   ```typescript
   const { getDeleteProductImpact, doDeleteProduct } = useApp();
   const { toast } = useToast();
   ```

   b. 新增 confirm delete state:
   ```typescript
   const [confirmDeleteProductId, setConfirmDeleteProductId] = useState<string | undefined>();
   ```

   c. 把原来的 `onClick={() => deleteProduct(id)}` 改为 `onClick={() => setConfirmDeleteProductId(id)}` (打开 Dialog 而非直接删)

   d. 计算 impact (在 Dialog 渲染时):
   ```typescript
   const deleteImpact = confirmDeleteProductId
     ? getDeleteProductImpact(confirmDeleteProductId)
     : null;
   ```

   e. 在 JSX 末尾加 Dialog:
   ```tsx
   <Dialog open={!!confirmDeleteProductId} onOpenChange={(o) => !o && setConfirmDeleteProductId(undefined)}>
     <DialogContent className="max-w-md">
       <DialogHeader
         title="确认删除产品?"
         description={deleteImpact
           ? `此操作将解除 ${deleteImpact.taskCount} 个任务、${deleteImpact.eventCount} 个日程的关联${
               deleteImpact.hasRndData ? '。研发中心的交付物/里程碑/原型/需求/知识/代码/测试/竞品将被清理' : ''
             }。是否继续?`
           : ''
         }
       />
       <DialogFooter>
         <Button variant="secondary" onClick={() => setConfirmDeleteProductId(undefined)}>取消</Button>
         <Button
           variant="danger"
           onClick={() => {
             if (confirmDeleteProductId) {
               doDeleteProduct(confirmDeleteProductId);
               toast({ type: 'success', title: '产品已删除', description: '关联数据已清理' });
             }
             setConfirmDeleteProductId(undefined);
           }}
         >
           确认删除
         </Button>
       </DialogFooter>
     </DialogContent>
   </Dialog>
   ```

3. CreateProductModal.tsx 可能不需要改 — 仅当 executor 发现删除入口在这里时才编辑。**默认只编辑 ProductManagementView.tsx**,若发现 deleteProduct 在其他文件 (如 ProductCard 或详情页 Header),在那里改。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <done>
    产品删除前弹出影响确认 Dialog,动态显示任务/日程/rndStore 影响计数,确认后 doDeleteProduct 完成级联清理。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 7 Plan 03 UAT - 删除清理 + 任务完成联动 + 双向清理</name>
  <action>启动 dev server,按 how-to-verify 16 步动线验证:CROSS-03 删除产品级联清理 (9 步)、CROSS-07 任务完成联动日程标记 (3 步)、CROSS-05 双向清理 (4 步)。</action>
  <what-built>
    产品删除二次确认 (CROSS-03);CROSS-07 任务完成联动日程标记 (07-01 store 层实现);CROSS-05 双向清理 (07-01 store 层实现)。
  </what-built>
  <how-to-verify>
    **CROSS-03 删除清理:**
    1. 启动 `npm run dev`
    2. 在任务 tab 给若干任务关联同一个产品 (例如 'Nova Agent Workspace') 的 projectId
    3. 在日程 tab 创建若干日程,设置 projectId 为同一产品 (或用 plan 02 的 "安排到日历" 间接给事件加上 projectId)
    4. 进入产品管理,点击删除该产品
    5. 预期: 弹 Dialog 显示"X 个任务、Y 个日程将失去关联。研发中心... 将被清理"
    6. 点确认删除 → 产品从列表消失
    7. 回任务 tab,预期: 之前关联的任务还在,但 projectId 已清空 (产品徽章消失)
    8. 回日程 tab,预期: 之前关联的日程还在,但产品徽章消失
    9. 进研发中心,切换到该产品 (若仍可选则刷新),预期: rndStore 中该 productId 数据全清 (交付物列表重新初始化或为空)

    **CROSS-07 任务完成联动:**
    10. 创建任务 → 设置截止日期 → 安排到日历 (plan 02 流程)
    11. 在任务 tab 把该任务标记完成
    12. 切到日程 tab → 预期:对应日程 chip 视觉降饱和 + 删除线 + CheckCircle icon (status === '已完成')

    **CROSS-05 双向清理:**
    13. 创建新任务 → 安排到日历
    14. 在任务 tab 删除该任务
    15. 切到日程 tab → 预期:对应日程仍在,但 taskId 已清空 (任务徽章消失,产品徽章若 inherited 仍在)
    16. 反向: 创建任务 → 安排到日历 → 在日程中删除该 event → 任务卡片上的 scheduledEventId 应清空 (日程徽章消失)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` 通过
- 手动 UAT: 上述 16 步全部通过
</verification>

<success_criteria>
1. 删除产品弹影响确认 Dialog,文案动态显示任务/日程计数和 rndStore 提示 (CROSS-03 ✓, D-04 ✓)
2. 确认后任务 projectId 清空、日程 projectId 清空、rndStore 数据清理 (D-05/D-06 ✓, L7 ✓)
3. 任务完成联动日程视觉降饱和 (CROSS-07 ✓ — store 层 07-01 已实现,UAT 验证)
4. 删除关联日程/任务双向清理,无孤儿 (CROSS-05 ✓ — store 层 07-01 已实现,UAT 验证)
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-module/07-03-SUMMARY.md`
</output>
