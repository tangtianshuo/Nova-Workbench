---
phase: 08-mdx-editor
plan: 03
type: execute
wave: 3
depends_on: [08-01]
files_modified:
  - src/views/KnowledgeBaseView.tsx
autonomous: false
requirements: [EDITOR-03, EDITOR-05]

must_haves:
  truths:
    - "KnowledgeBaseView 的 '编辑' 按钮 (line 125) 点击后切换到编辑模式,显示 MarkdownEditor"
    - "编辑模式下 '保存' 按钮将 MarkdownEditor 的 Markdown 原文写回 DOCS (local state 或 store)"
    - "编辑模式下 '取消' 按钮丢弃编辑内容,恢复只读视图"
    - "只读模式下仍用 whitespace-pre-wrap 渲染 (现状, 不强制改 ReactMarkdown — 文档多为短文本)"
    - "Phase 8 全量 UAT: 三处替换 (组件 + ProductKnowledgeTab + KnowledgeBaseView) 通过用户验证"
    - "Tailwind v4 样式共存: KnowledgeBaseView 编辑模式 MarkdownEditor 与 view 现有样式不冲突"
  artifacts:
    - path: "src/views/KnowledgeBaseView.tsx"
      provides: "编辑模式切换 + MarkdownEditor 集成"
      contains: "MarkdownEditor"
  key_links:
    - from: "src/views/KnowledgeBaseView.tsx (编辑按钮)"
      to: "isEditing local state"
      via: "setIsEditing(true) + 初始化 editContent"
      pattern: "setIsEditing\\(true\\)"
    - from: "src/views/KnowledgeBaseView.tsx (编辑模式)"
      to: "src/components/ui/MarkdownEditor.tsx"
      via: "import { MarkdownEditor } from '@/src/components/ui'"
      pattern: "MarkdownEditor"
---

<objective>
为 KnowledgeBaseView 接入完整编辑流程:点击"编辑" → 切换编辑模式 (MarkdownEditor) → 保存/取消。然后进入 Phase 8 全量 UAT 关卡,验证 5 条 Success Criteria 全部 TRUE。

Purpose: EDITOR-03 要求实现"编辑"按钮功能 (当前 line 125 的 Button 无 onClick,点击无反应)。完成此 plan 后 Phase 8 全部交付,进入 UAT。
Output: src/views/KnowledgeBaseView.tsx 编辑模式 + Phase 8 UAT 结果
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-mdx-editor/08-CONTEXT.md
@.planning/phases/08-mdx-editor/08-01-SUMMARY.md
@.planning/phases/08-mdx-editor/08-02-SUMMARY.md

@src/views/KnowledgeBaseView.tsx
@src/components/ui/MarkdownEditor.tsx

<interfaces>
<!-- Wave 1 交付的 MarkdownEditor API (与 plan 02 相同) -->
```typescript
interface MarkdownEditorProps {
  value: string;
  onChange: (markdown: string) => void;
  readOnly?: boolean;
  placeholder?: string;
  className?: string;
  minHeight?: string;
}
import { MarkdownEditor } from '@/src/components/ui';
```

<!-- KnowledgeBaseView 关键现有结构 (Wave 3 替换目标) -->
```typescript
// DOCS 是模块级 const (line 15-28),编辑目前无路径写回
// ponytail: 用 local state DOCS_PROXY (deep copy of DOCS) 或直接 useState<Record<string, {title, content}>>
// 考虑到 KnowledgeBaseView 是 demo view (mock 数据), 写回 local state 即可 (不接 store)

// 编辑按钮 (line 124-127):
<div className="flex items-center gap-2">
  <Button variant="ghost" size="sm">编辑</Button>  // ← 无 onClick
  <Button variant="primary" size="sm">分享</Button>
</div>

// 文档正文渲染 (line 130-137):
<div className="flex-1 overflow-y-auto p-10">
  <div className="max-w-2xl mx-auto">
    <h1 className="text-3xl font-bold text-text-primary mb-6 tracking-tight">{currentDoc.title}</h1>
    <div className="text-sm text-text-secondary whitespace-pre-wrap leading-relaxed">
      {currentDoc.content}
    </div>
  </div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KnowledgeBaseView 编辑模式 + MarkdownEditor 集成</name>
  <files>src/views/KnowledgeBaseView.tsx</files>
  <read_first>
    - src/views/KnowledgeBaseView.tsx (完整文件, 特别 line 9-28 DOCS / line 30-49 state / line 115-138 viewer)
    - .planning/phases/08-mdx-editor/08-01-SUMMARY.md (MarkdownEditor API)
  </read_first>
  <behavior>
    - 点击"编辑"按钮 → 切换到编辑模式 (isEditing=true)
    - 编辑模式: 文档正文区由 whitespace-pre-wrap 渲染切换为 MarkdownEditor,value 绑定 editContent,onChange 更新 editContent
    - 编辑模式: 顶部按钮组变为 "取消" + "保存" (替换原 "编辑" + "分享")
    - 点击"保存": 把 editContent 写回 local docs (useState Record),setIsEditing(false),Toast 或视觉反馈确认
    - 点击"取消": 丢弃 editContent,setIsEditing(false),恢复只读
    - 切换文档 (activeDoc 改变) 时自动退出编辑模式 (避免 editContent 残留)
    - 编辑模式 MarkdownEditor 用 minHeight='480px' (知识库文章通常较长)
    - 只读模式保持 whitespace-pre-wrap (现状不变 — ponytail: 不强制切 ReactMarkdown,KnowledgeBaseView 是 demo view,后续 Phase 11 AI 接入时再升级)
  </behavior>
  <action>
1. 编辑 `src/views/KnowledgeBaseView.tsx`:

   a. **imports 区**: 添加:
   ```typescript
   import { MarkdownEditor } from '@/src/components/ui';
   import { PencilSimple, Check, X } from '@phosphor-icons/react';
   // (现有 BookOpen/Folder/FileText/MagnifyingGlass/CaretRight/Star 保留)
   ```

   b. **DOCS 改为可变 state** (line 15-28): 把 `const DOCS = {...}` 改为组件内 `useState` 初始化. 或更简单: 保持 DOCS 为初始常量, 加 `const [docs, setDocs] = useState(DOCS);` 然后所有 `DOCS[activeDoc]` 改为 `docs[activeDoc]`. 这样切换文档时持久化编辑结果 (本 view 内).
   - ponytail: 若用户切走再切回 view (lazy unmount), state 会丢失 — 这是 demo view 可接受行为. 若要持久化需接 store,留到 Phase 11.

   c. **新增 state** (在 useState expandedFolders 附近):
   ```typescript
   const [isEditing, setIsEditing] = useState(false);
   const [editContent, setEditContent] = useState('');
   ```

   d. **切换文档时退出编辑模式** (在 toggleFolder 或 setActiveDoc 调用处):
   修改 line 95 `onClick={() => setActiveDoc(child.id)}` 为:
   ```typescript
   onClick={() => { setActiveDoc(child.id); setIsEditing(false); }}
   ```

   e. **添加 handlers** (在 getFolderForDoc 函数后):
   ```typescript
   const handleStartEdit = () => {
     setEditContent(currentDoc.content);
     setIsEditing(true);
   };
   const handleSaveEdit = () => {
     setDocs(prev => ({ ...prev, [activeDoc]: { ...prev[activeDoc], content: editContent } }));
     setIsEditing(false);
   };
   const handleCancelEdit = () => {
     setEditContent('');
     setIsEditing(false);
   };
   ```

   f. **替换顶部按钮组** (line 124-127):
   ```tsx
   <div className="flex items-center gap-2">
     {isEditing ? (
       <>
         <Button variant="ghost" size="sm" onClick={handleCancelEdit}>
           <X size={14} weight="duotone" />
           <span className="ml-1">取消</span>
         </Button>
         <Button variant="primary" size="sm" onClick={handleSaveEdit}>
           <Check size={14} weight="duotone" />
           <span className="ml-1">保存</span>
         </Button>
       </>
     ) : (
       <>
         <Button variant="ghost" size="sm" onClick={handleStartEdit}>
           <PencilSimple size={14} weight="duotone" />
           <span className="ml-1">编辑</span>
         </Button>
         <Button variant="primary" size="sm">分享</Button>
       </>
     )}
   </div>
   ```

   g. **替换正文渲染** (line 130-137):
   ```tsx
   <div className="flex-1 overflow-y-auto p-10">
     <div className="max-w-2xl mx-auto">
       <h1 className="text-3xl font-bold text-text-primary mb-6 tracking-tight">{currentDoc.title}</h1>
       {isEditing ? (
         <MarkdownEditor
           value={editContent}
           onChange={setEditContent}
           placeholder="编辑文档内容 (支持 Markdown)..."
           minHeight="480px"
         />
       ) : (
         <div className="text-sm text-text-secondary whitespace-pre-wrap leading-relaxed">
           {currentDoc.content}
         </div>
       )}
     </div>
   </div>
   ```

2. **dev 验证**:
   - 启动 `npm run dev`,切到"知识库" tab
   - 选一个文档 (如 "前端开发规范 V3.0") → 看到 whitespace-pre-wrap 渲染 (现状)
   - 点 "编辑" 按钮 → 切换到 MarkdownEditor,预填当前内容
   - 修改内容 (加个 `## 新章节` 标题) → 实时渲染
   - 点 "取消" → 退回只读,内容未变
   - 再点 "编辑" → 修改 → 点 "保存" → 退回只读,看到新内容
   - 切换到另一个文档 → 自动退出编辑模式
   - 中文输入法测试
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/pm-workspace && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/views/KnowledgeBaseView.tsx 包含 `import { MarkdownEditor } from '@/src/components/ui'`
    - src/views/KnowledgeBaseView.tsx 包含 `isEditing` state + `handleStartEdit` / `handleSaveEdit` / `handleCancelEdit` handlers
    - 编辑按钮 (原 line 125) 现在 onClick={handleStartEdit}
    - 编辑模式顶部按钮组显示 "取消" + "保存"
    - 编辑模式正文区渲染 MarkdownEditor,minHeight="480px"
    - 只读模式正文区保持 whitespace-pre-wrap (现状不变)
    - 切换文档 (setActiveDoc) 触发 setIsEditing(false)
    - `npm run lint` 通过
    - 运行时验证: 编辑流程完整 (编辑 → 保存/取消),Markdown 实时渲染,中文 IME 不卡顿
  </acceptance_criteria>
  <done>
    KnowledgeBaseView 编辑流程完整. Phase 8 三处交付 (组件 + ProductKnowledgeTab + KnowledgeBaseView) 全部完成. 进入 Task 2 UAT 关卡.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 8 全量 UAT 验证 (EDITOR-01..05 + 5 条 Success Criteria)</name>
  <action>启动 dev server,用户按使用动线批量验证 Phase 8 全部 5 个 EDITOR-* 需求与 5 条 Success Criteria,重点确认 Markdown WYSIWYG 编辑、AI 润色注入、Tailwind v4 共存、bundle 增量、纯渲染场景不变。</action>
  <what-built>
    Phase 8 完整交付:
    1. MarkdownEditor 组件 (src/components/ui/MarkdownEditor.tsx) — 封装 @mdxeditor/editor,React.lazy + Suspense,按需 plugins (toolbar/headings/lists/tables/link/quote/markdownShortcut/codeMirror),Nova tokens 样式,barrel 导出
    2. ProductKnowledgeTab 编辑面板 + 新建模态框 Textarea → MarkdownEditor 替换,实时渲染 Markdown 格式化效果,AI 润色结果注入路径保留
    3. KnowledgeBaseView 编辑按钮接入,实现完整编辑流程 (编辑 → 保存/取消)
    4. 6 处 react-markdown 纯渲染场景保持不变
  </what-built>
  <how-to-verify>
    **前置准备:**
    - 启动 dev: `cd D:/Projects/Nova/pm-workspace && npm run dev` (或 `npm run tauri:dev`)
    - 浏览器 devtools Network tab 打开 (观察 chunk 加载)

    **使用动线 (UAT, 按 MEMORY.md 偏好批量跑):**

    1. **ProductKnowledgeTab 编辑模式 (EDITOR-02, SC-2)**:
       - 切到 "产品管理" → 选一个产品 → 切到 "知识库" tab
       - 选一个知识词条 → 点编辑按钮 (铅笔图标)
       - **首次加载**: Skeleton 显示 ~1s,然后 MarkdownEditor 渲染 (Network tab 应看到独立 chunk 拉取,记录大小)
       - 清空内容,输入 `# 测试标题` 回车 → 实时渲染为 H1 (无需切换预览)
       - 输入 `**粗体**` 和 `*斜体*` → 实时渲染
       - 输入 `- 列表项 1\n- 列表项 2` → 渲染为无序列表
       - 点 toolbar 表格按钮 → 插入表格骨架,可编辑单元格
       - 点 toolbar 代码块按钮 → 插入代码块,可选语言
       - 点 "AI 排版润色" → 等待 → 内容刷新 (editContent 注入 MarkdownEditor)
       - 点 "保存词条" → 退回只读,ReactMarkdown 渲染保存的内容
       - 刷新页面 → 内容持久化 (通过 store persist)

    2. **ProductKnowledgeTab 新建模态框 (EDITOR-02)**:
       - 点 "新建知识词条" → 模态框打开
       - MarkdownEditor 在模态框中渲染正常 (z-index 正确,toolbar 不被遮挡)
       - 输入内容 → 实时渲染
       - 点 "立即收录" → 模态框关闭,新词条出现在列表中

    3. **KnowledgeBaseView 编辑流程 (EDITOR-03, SC-3)**:
       - 切到 "知识库" tab (全局, 不是产品内的)
       - 选一个文档 (如 "前端开发规范 V3.0")
       - 看到 whitespace-pre-wrap 渲染 (现状)
       - 点 "编辑" 按钮 → 切换到 MarkdownEditor,预填当前内容
       - 修改内容,加 `## 新章节` 标题 → 实时渲染
       - 点 "取消" → 退回只读,内容未变
       - 再点 "编辑" → 修改 → 点 "保存" → 退回只读,新内容显示
       - 切换到另一个文档 → 自动退出编辑模式

    4. **bundle + lazy 验证 (EDITOR-04, SC-4)**:
       - devtools Network tab,刷新首页 (产品管理或任务管理)
       - 观察首屏 main bundle,**不应**包含 @mdxeditor/editor chunk
       - 进入知识库编辑模式 → 此时才拉取 mdxeditor chunk (独立文件, gzip 目标 ~250KB,容忍 400KB)
       - 记录实际 chunk 大小到 UAT 反馈

    5. **纯渲染场景不变 (D-04, SC-5)**:
       - 切到 "研发中心" → 选产品 → 切到 "全生命周期交付物" → 点查看任一交付物 → `<ReactMarkdown>` 渲染正常 (未替换)
       - 切到 "产品管理" → 选产品 → 切到 "PRD/需求" → `<ReactMarkdown>` 渲染正常
       - 切到 "工作区" → 触发 AI 总结 → `<Markdown>` (WorkspaceSummaryModal) 渲染正常

    6. **Tailwind v4 共存验证 (D-07, SC-5)**:
       - 在 MarkdownEditor 渲染区检查:
         - 文字颜色使用 Nova text-primary token (不是 MDXEditor 默认黑)
         - 背景使用 bg-primary token
         - border 使用 border-subtle token
         - toolbar 按钮不被 Tailwind preflight 重置 (可见,有 hover 态)
       - 检查 MDXEditor 内部样式不污染外部 (退出编辑模式后其他 view 样式正常)

    7. **中文输入法 (IME) 验证**:
       - 切换到中文输入法
       - 在 MarkdownEditor 中输入 "测试中文输入法是否卡顿" → 流畅,不丢字
       - 在列表项中输入中文 → 回车后正确生成新列表项

    **预期结果**: 5 条 Success Criteria 全部 TRUE,无 crash,无样式污染,中文 IME 流畅.
  </how-to-verify>
  <resume-signal>回复 "approved" 或描述发现的缺陷 (如 "toolbar 按钮被 Tailwind 覆盖"、"中文输入法丢字"、"chunk 大小超 500KB" 等)</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` (tsc --noEmit) 通过
- grep 验证: src/views/KnowledgeBaseView.tsx 包含 MarkdownEditor + isEditing + handleStartEdit/handleSaveEdit/handleCancelEdit
- 用户在 UAT 中确认 5 条 Success Criteria 通过,或列出待修缺陷
</verification>

<success_criteria>
1. KnowledgeBaseView 编辑按钮接入 MarkdownEditor,实现编辑 → 保存/取消流程 (EDITOR-03 ✓)
2. Phase 8 全量 UAT 通过: 5 条 Success Criteria 全部 TRUE
3. Markdown WYSIWYG 实时渲染 (粗体/标题/列表/表格/代码块)
4. AI 润色结果正确注入 MarkdownEditor (ProductKnowledgeTab)
5. bundle lazy chunk 在容忍范围内 (< 400KB gzip)
6. Tailwind v4 样式无冲突
7. 中文输入法流畅
8. 6 处 react-markdown 纯渲染场景保持不变 (D-04 ✓, EDITOR-05 ✓)
</success_criteria>

<output>
完成后:
1. 创建 `.planning/phases/08-mdx-editor/08-03-SUMMARY.md`
2. 在 `.planning/STATE.md` 更新 Phase 8 状态为 completed
3. 在 `.planning/HUMAN-UAT.md` (若存在) 中标记 Phase 8 区段
SUMMARY 必须包含:
- KnowledgeBaseView 编辑模式实现细节 (state 设计 / handler 流程)
- Phase 8 全量 UAT 结果 (5 条 SC 通过 / 缺陷列表)
- bundle chunk 实际大小
- 中文 IME 测试结果
- Tailwind v4 共存最终结论
</output>
