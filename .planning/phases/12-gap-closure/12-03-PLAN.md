---
phase: 12-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
gap_closure: true
closes_audit_ids: [security.medium.1, security.medium.2, security.medium.3, traceability.CROSS-04, traceability.CROSS-05, traceability.CROSS-06]

must_haves:
  truths:
    - "server.ts 的 express.json() 设置了 limit:'1mb'"
    - "server.ts 的 /api/chat endpoint 受 Origin allowlist 中间件保护(允许 localhost variants + tauri://)"
    - "server.ts 错误日志不输出完整 provider error.message(改输出 error.name + 脱敏 message)"
    - "REQUIREMENTS.md traceability 表中 CROSS-04/05/06 三行 Status 改为 Complete,checkbox 从 [ ] 改为 [x]"
  artifacts:
    - path: "server.ts"
      provides: "Express 3 项 MEDIUM 安全加固"
      contains_grep: ["limit: '1mb'", "Origin", "tauri://", "error.name"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "修正 traceability 表 stale 状态"
      contains_grep: ["CROSS-04 | Phase 7 | Complete", "CROSS-05 | Phase 7 | Complete", "CROSS-06 | Phase 7 | Complete"]
  key_links: []
---

<objective>
关闭 v0.2.0-MILESTONE-AUDIT 的 3 个 Express MEDIUM 安全 finding + REQUIREMENTS traceability 表 stale 状态修正。

**3 个 MEDIUM(audit lines 67-70):**
1. `server.ts:12` `express.json()` 无 body size limit → 加 `{limit: '1mb'}` 防 DoS
2. `server.ts:16` `/api/chat` 无 Origin 校验 → DNS rebinding 风险,加 allowlist middleware
3. `server.ts:59` `console.error` 输出完整 Gemini error.message → 可能含 API key,改输出 `error.name` + 脱敏 message

**Traceability stale(audit lines 51-63):**
- CROSS-04/05/06 在 `.planning/REQUIREMENTS.md` 标 `Pending` / `[ ]`,实际代码已实现并验证。

Purpose: Express 暴露的攻击面虽小(只 Web 模式 fallback 才暴露,Tauri 桌面包不暴露外部),但 audit 标记 MEDIUM 需在里程碑内修复;traceability 表的 stale 状态影响下一次 audit 的判断准确度。
Output: server.ts 3 处加固 + REQUIREMENTS.md 3 行状态修正。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v0.2.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@server.ts

<audit_findings>
<!-- Verbatim from v0.2.0-MILESTONE-AUDIT.md -->

security.medium (audit lines 67-70):
  - "[MEDIUM] express: server.ts:12 express.json() 无 body size limit — 加 {limit:'1mb'} 防 DoS"
  - "[MEDIUM] express: server.ts:16 /api/chat 无 Origin 校验 — DNS rebinding 风险,加 Origin allowlist"
  - "[MEDIUM] express: server.ts:59 console.error 输出完整 Gemini error.message — 可能含 API key,改输出 error.name + 脱敏 message"

traceability (audit lines 51-63):
  - req: CROSS-04, table_status: Pending, actual_status: satisfied
    evidence: src/views/ScheduleView.tsx:114-133 (任务徽章渲染 + 跳转)
  - req: CROSS-05, table_status: Pending, actual_status: satisfied
    evidence: src/components/TaskKanban.tsx:596-604 (scheduledEventId 徽章 + "跳转到日程")
  - req: CROSS-06, table_status: Pending, actual_status: satisfied
    evidence: 同 CROSS-04 + CROSS-05 (徽章点击 → setSelectedProductId/setSelectedTaskId → 切 tab)

audit context note (line 171):
  "这 3 条仅在 `npm start`(Web 模式 fallback)下暴露风险;Tauri 桌面包不暴露 Express 到外部。"
</audit_findings>

<current_server_state>
<!-- Current state of server.ts relevant lines (read from disk 2026-08-11) -->

Line 10-12:
```ts
  const app = express();
  const PORT = 3000;
  app.use(express.json());      // ← MEDIUM #1: no limit
```

Line 16-17:
```ts
  app.post('/api/chat', async (req, res) => {     // ← MEDIUM #2: no Origin check
    try {
```

Line 57-62:
```ts
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error('Chat proxy error:', message);   // ← MEDIUM #3: full message (may contain key)
      if (!res.headersSent) return res.status(500).json({ error: message });
      res.write(JSON.stringify({ kind: 'error', data: { message } }) + '\n');
      res.end();
    }
```

Line 75-77:
```ts
  app.listen(PORT, '127.0.0.1', () => {
    console.log(`Dev-only server running at http://127.0.0.1:${PORT} (not for production)`);
  });
```
Note: 已经 bind 127.0.0.1(audit 也认可),所以 Origin allowlist 是 defense-in-depth。
</current_server_state>

<design_decisions>
**Origin allowlist 范围(MEDIUM #2):**
- `http://localhost:3000` / `http://127.0.0.1:3000`(dev / Web fallback)
- `http://localhost:5173` / `http://127.0.0.1:5173`(Vite frontend-only 测试时,见 9-06 SUMMARY)
- `tauri://localhost` / `https://tauri.localhost`(Tauri v2 Windows production)
- 空 Origin / 同源请求允许(POSTMAN/curl 通常不发 Origin header,这种 case 不应被误杀;非浏览器客户端不受 DNS rebinding 威胁)
- middleware 位置:`app.use(express.json(...))` 之后,`/api/chat` route 之前;`if (req.path === '/api/chat' && req.method === 'POST')` 守卫,避免影响 Vite middleware(dev HMR)和静态文件 fallback

**Error sanitization 策略(MEDIUM #3):**
- `error.name`(类名,如 'Error'、'GoogleGenAIError'):安全,完整输出
- `error.message`:可能含 key 引用或 provider 内部信息;截断到 200 字符 + 过滤 `GEMINI_API_KEY` / `AIza` 开头的 token 字符串
- 不传 raw `message` 给 client response,改传 generic "Chat proxy error"(client 看不到内部细节,UI 已有 try/catch 显示错误)

**Body limit(MEDIUM #1):**
- `1mb`:audit 指定值。LLM chat 请求正常 <50KB(tools 数组 + messages);1mb 给充足余量又防滥用。

**Traceability 修正:**
- 3 行表格行修改 `Pending → Complete`
- v1 requirements checkbox 列表(line 38-40):`[ ] → [x]` 三处
- 不动其他行
- 在文件底部更新 `*Last updated*` 时间戳并注明原因
</design_decisions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: server.ts 3 项 MEDIUM 加固(body limit + Origin allowlist + error 脱敏)</name>
  <files>server.ts</files>

  <read_first>
    - server.ts (全文,81 行;目标 3 处改动分别在 line 12 / 16 / 59)
    - .planning/v0.2.0-MILESTONE-AUDIT.md (lines 67-70 + 155-171 安全段)
    - src-tauri/tauri.conf.json (确认 production 模式下 Tauri 加载的 Origin scheme;Tauri v2 在 Windows 上是 `https://tauri.localhost`,macOS/Linux 是 `tauri://localhost`)
  </read_first>

  <behavior>
    - 行为 1: POST /api/chat 发送 >1mb body,期望收到 413 Payload Too Large(而非进入 handler)
    - 行为 2: 用 Origin: http://evil.com 发 POST /api/chat,期望收到 403 Forbidden;用 Origin: http://localhost:3000 发同样请求,期望正常进入 handler
    - 行为 3: 触发 Gemini error(api key 故意错误),期望 console.error 输出形如 "Chat proxy error: GoogleGenAIError — <truncated message>" 而非完整 message
  </behavior>

  <action>
    **改动 1 — Body size limit(line 12):**
    替换:
    ```ts
    app.use(express.json());
    ```
    为:
    ```ts
    app.use(express.json({ limit: '1mb' }));
    ```

    **改动 2 — Origin allowlist middleware(在 line 12 之后、line 16 `/api/chat` route 之前插入):**
    ```ts
    // MEDIUM #2: Origin allowlist — DNS rebinding defense-in-depth.
    // Server already binds 127.0.0.1 (audit acknowledged); this guards against
    // browser-driven DNS rebinding when an attacker resolves localhost to a
    // public IP and posts from a malicious page.
    const ALLOWED_ORIGINS = new Set([
      'http://localhost:3000',
      'http://127.0.0.1:3000',
      'http://localhost:5173',
      'http://127.0.0.1:5173',
      'tauri://localhost',
      'https://tauri.localhost',
    ]);
    app.use((req, res, next) => {
      // Only enforce on /api/chat POST; Vite HMR + static fallback untouched.
      if (req.path !== '/api/chat' || req.method !== 'POST') return next();
      const origin = req.headers.origin;
      // No Origin header = non-browser client (curl/postman) — allow through,
      // DNS rebinding is a browser-vector attack.
      if (!origin) return next();
      if (ALLOWED_ORIGINS.has(origin)) {
        res.setHeader('Access-Control-Allow-Origin', origin);
        return next();
      }
      return res.status(403).json({ error: 'Origin not allowed' });
    });
    ```

    **改动 3 — Error sanitization(line 57-62 catch block):**
    替换:
    ```ts
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      console.error('Chat proxy error:', message);
      if (!res.headersSent) return res.status(500).json({ error: message });
      res.write(JSON.stringify({ kind: 'error', data: { message } }) + '\n');
      res.end();
    }
    ```
    为:
    ```ts
    } catch (error) {
      // MEDIUM #3: Sanitize — provider error.message may include API key fragments.
      // Log name + truncated/redacted message; client gets generic text.
      const errName = error instanceof Error ? error.name : 'UnknownError';
      let rawMessage = error instanceof Error ? error.message : String(error);
      // Strip common API key patterns (Gemini keys start with AIza, plus generic key=...).
      rawMessage = rawMessage.replace(/AIza[0-9A-Za-z_-]{35}/g, '[REDACTED_KEY]');
      rawMessage = rawMessage.replace(/key=[^\s&]+/gi, 'key=[REDACTED]');
      rawMessage = rawMessage.slice(0, 200);
      console.error(`Chat proxy error: ${errName} — ${rawMessage}`);
      const safeClientMessage = 'Chat proxy error';
      if (!res.headersSent) return res.status(500).json({ error: safeClientMessage });
      res.write(JSON.stringify({ kind: 'error', data: { message: safeClientMessage } }) + '\n');
      res.end();
    }
    ```

    **保留不动:**
    - 全部其他 endpoint 逻辑(Gemini SDK 调用、streaming NDJSON 写出)
    - `app.listen(PORT, '127.0.0.1', ...)` 已 bind 127.0.0.1,不动
    - Vite middleware / static fallback 配置

    **TypeScript:** 错误处理保持 `error instanceof Error` 收窄;无新依赖;无 type assertion 滥用。
  </action>

  <verify>
    <automated>npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -n "limit: '1mb'" server.ts` 命中(MEDIUM #1 修复)
    - [ ] `grep -n "ALLOWED_ORIGINS" server.ts` 命中(MEDIUM #2 修复)
    - [ ] `grep -n "tauri://localhost" server.ts` 命中(Tauri macOS/Linux origin)
    - [ ] `grep -n "https://tauri.localhost" server.ts` 命中(Tauri Windows origin)
    - [ ] `grep -n "Origin not allowed" server.ts` 命中(403 响应路径)
    - [ ] `grep -n "REDACTED_KEY\|REDACTED" server.ts` 命中(MEDIUM #3 脱敏)
    - [ ] `grep -n "errName" server.ts` 命中(改用 error.name)
    - [ ] `grep -nE "console\.error\('Chat proxy error:', message\)" server.ts` 0 处命中(旧调用已删)
    - [ ] `npm run lint` exit 0,无类型错误
    - [ ] `npm run build` 成功
    - [ ] 手动验证:`curl -X POST http://localhost:3000/api/chat -H "Origin: http://evil.com" -H "Content-Type: application/json" -d '{}'` 返回 403
    - [ ] 手动验证:`curl -X POST http://localhost:3000/api/chat -H "Origin: http://localhost:3000" -H "Content-Type: application/json" -d '{"messages":[],"systemPrompt":"","provider":"gemini"}'` 不被 Origin middleware 拦截(进入 handler 后可能 400/500 但不是 403)
    - [ ] 手动验证:发送 >1mb body 期望 413 Payload Too Large
  </acceptance_criteria>

  <done>
    server.ts 完成 3 项 MEDIUM 加固:body size 限制 1mb、Origin allowlist 6 条规则(localhost variants + tauri:// + tauri.localhost)、catch 块用 error.name + 截断 + 关键词 redact 脱敏。npm run lint / build 全绿。v0.2.0-MILESTONE-AUDIT 的 security.medium 三条全部可标记为 closed。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REQUIREMENTS.md traceability 表修正(CROSS-04/05/06 Pending → Complete)</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (全文;目标行:line 38-40 checkbox 列表 + line 134-136 traceability 表 + line 157 last_updated 时间戳)
    - .planning/v0.2.0-MILESTONE-AUDIT.md (lines 51-63 traceability section,包含三行的 actual_status + evidence)
  </read_first>

  <behavior>
    - REQUIREMENTS.md 中 CROSS-04/05/06 三行的 checkbox 列变为 `[x]`
    - Traceability 表中 CROSS-04/05/06 三行的 Status 列变为 `Complete`
    - 文件底部 last_updated 时间戳更新,注明改因
  </behavior>

  <action>
    **改动 1 — v1 Requirements checkbox 列表(line 38-40):**
    替换:
    ```markdown
    - [ ] **CROSS-04**: 日程视图上,关联任务的日程显示任务徽章;点击徽章跳转/高亮任务
    - [ ] **CROSS-05**: 任务卡片上显示关联日程的徽章(如有 scheduledEventId);点击徽章跳转到日程视图
    - [ ] **CROSS-06**: 用户点击关联徽章可跳转到对应模块并定位(产品→ProductManagementView,任务→TaskManagementView,日程→ScheduleView)
    ```
    为:
    ```markdown
    - [x] **CROSS-04**: 日程视图上,关联任务的日程显示任务徽章;点击徽章跳转/高亮任务
    - [x] **CROSS-05**: 任务卡片上显示关联日程的徽章(如有 scheduledEventId);点击徽章跳转到日程视图
    - [x] **CROSS-06**: 用户点击关联徽章可跳转到对应模块并定位(产品→ProductManagementView,任务→TaskManagementView,日程→ScheduleView)
    ```

    **改动 2 — Traceability 表(line 134-136):**
    替换:
    ```markdown
    | CROSS-04 | Phase 7 | Pending |
    | CROSS-05 | Phase 7 | Pending |
    | CROSS-06 | Phase 7 | Pending |
    ```
    为:
    ```markdown
    | CROSS-04 | Phase 7 | Complete |
    | CROSS-05 | Phase 7 | Complete |
    | CROSS-06 | Phase 7 | Complete |
    ```

    **改动 3 — 文件底部时间戳(line 157):**
    替换:
    ```markdown
    *Last updated: 2026-08-10 after roadmap creation (traceability validated)*
    ```
    为:
    ```markdown
    *Last updated: 2026-08-11 — Phase 12 Plan 12-03 corrected CROSS-04/05/06 traceability (stale Pending → Complete per v0.2.0-MILESTONE-AUDIT.md lines 51-63)*
    ```

    **不动:**
    - TASK-* / SCHED-* / CROSS-01/02/03/07 行(已正确为 Complete)
    - EDITOR-* / LINKAGE-* 前置调研行(状态正确)
    - v2 Requirements / Out of Scope / Key Decisions / Coverage 统计 段落
  </action>

  <verify>
    <automated>grep -n "CROSS-04\|CROSS-05\|CROSS-06" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -c "^- \[x\] \*\*CROSS-04\|^- \[x\] \*\*CROSS-05\|^- \[x\] \*\*CROSS-06" .planning/REQUIREMENTS.md` 命中数 ≥ 3(checkbox 已勾选)
    - [ ] `grep -c "^| CROSS-04 | Phase 7 | Complete \|^| CROSS-05 | Phase 7 | Complete \|^| CROSS-06 | Phase 7 | Complete " .planning/REQUIREMENTS.md` 命中数 = 3(traceability 表更新)
    - [ ] `grep -E "CROSS-04.*Pending\|CROSS-05.*Pending\|CROSS-06.*Pending" .planning/REQUIREMENTS.md` 0 处命中(无残留 Pending)
    - [ ] `grep -E "^- \[ \] \*\*CROSS-04\|^- \[ \] \*\*CROSS-05\|^- \[ \] \*\*CROSS-06" .planning/REQUIREMENTS.md` 0 处命中(无残留未勾选)
    - [ ] `grep "2026-08-11" .planning/REQUIREMENTS.md` 命中 last_updated 时间戳已更新
    - [ ] 文件其他段落(TASK-* / SCHED-* / 其他 CROSS-* / EDITOR-* / LINKAGE-* / Key Decisions)未被改动
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md traceability 表 CROSS-04/05/06 三行从 Pending → Complete、checkbox 从 [ ] → [x];时间戳更新到 2026-08-11 并注明改因(Phase 12 Plan 12-03 + audit lines 51-63)。v0.2.0-MILESTONE-AUDIT 的 traceability stale gap 关闭。
  </done>
</task>

</tasks>

<verification>
**自动化:**
- `npm run lint` (tsc --noEmit) exit 0
- `npm run build` 成功

**grep 验证(server.ts):**
- 必须命中:`limit: '1mb'`、`ALLOWED_ORIGINS`、`tauri://localhost`、`https://tauri.localhost`、`Origin not allowed`、`REDACTED`、`errName`
- 必须未命中:`console.error('Chat proxy error:', message)`(原始调用)

**grep 验证(REQUIREMENTS.md):**
- 必须命中:`| CROSS-04 | Phase 7 | Complete |`(×3 for 04/05/06)、`- [x] **CROSS-04**`(×3)、`2026-08-11`
- 必须未命中:`CROSS-04.*Pending`(任何 CROSS-0X Pending)

**手动 curl 验证(server.ts):**
- Origin deny:`curl -X POST http://localhost:3000/api/chat -H "Origin: http://evil.com" -H "Content-Type: application/json" -d '{}'` → 403
- Origin allow:`curl -X POST http://localhost:3000/api/chat -H "Origin: http://localhost:3000" -H "Content-Type: application/json" -d '{"messages":[],"systemPrompt":"","provider":"gemini"}'` → 非 403(进入 handler)
- Body size:`head -c 1100000 /dev/zero | tr '\0' 'a' > /tmp/big.json && curl -X POST http://localhost:3000/api/chat -H "Content-Type: application/json" -d "$(cat /tmp/big.json)"` → 413
</verification>

<success_criteria>
1. server.ts 3 处 MEDIUM 全部修复(body limit 1mb + Origin allowlist 6 规则 + 错误脱敏 redact)
2. REQUIREMENTS.md CROSS-04/05/06 三行 traceability 表更新到 Complete + checkbox [x]
3. npm run lint / build 全绿
4. 手动 curl 验证 Origin allowlist 工作
5. v0.2.0-MILESTONE-AUDIT 的 security.medium 三条 + traceability stale 三条全部可标记为 closed
</success_criteria>

<output>
After completion, create `.planning/phases/12-gap-closure/12-03-SUMMARY.md` documenting:
- server.ts 3 处改动 diff 摘要
- REQUIREMENTS.md 改动行号
- curl 验证结果(403/non-403/413)
- 已关闭的 audit gap IDs
</output>
