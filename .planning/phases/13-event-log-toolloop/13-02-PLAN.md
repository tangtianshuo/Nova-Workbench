---
phase: 13-event-log-toolloop
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/ai/chatSession.ts
  - src/ai/__tests__/phase13ChatSessionProjection.test.ts
autonomous: true
requirements:
  - EVT-01
  - EVT-03
  - EVT-06
  - EVT-07

must_haves:
  truths:
    - "ChatSession.addMessage 在追加内存消息的同时追加对应事件(user_message / assistant_message / tool_call / tool_result),事件按会话串行落序"
    - "getMessagesForLLM 的投影把同一模型响应的多个 tool_call assistant 消息折叠为一条,tool 消息内容原样透传(单一派生格式)"
    - "ChatSession.fromEvents 能从事件流重建会话,重建结果不再发射事件;同一事件流重建两次,getMessagesForLLM 完全一致"
    - "session_created 事件在会话创建时发射一次;turn 内事件携带同一 correlation_id(setCorrelationId 设置)"
    - "token 预算裁剪改用 CJK 感知 estimateTokens — 中文超长轮次被正确裁掉(旧 length/4 会误留)"
    - "phase10PromptContext.test.ts 的既有 ChatSession 断言(8 轮窗口、tool 分组、预算裁剪)不改一行仍然通过"
  artifacts:
    - path: "src/ai/chatSession.ts"
      provides: "事件日志投影版 ChatSession(保留类与公开 API,addMessage 双写事件,新增 fromEvents/flushEvents/setCorrelationId/recordTurnEnd)"
      contains: "static fromEvents("
    - path: "src/ai/__tests__/phase13ChatSessionProjection.test.ts"
      provides: "投影/重建/相关性/中文预算测试"
  key_links:
    - from: "src/ai/chatSession.ts"
      to: "src/ai/events/eventStore.ts"
      via: "addMessage → getEventStore().append(单一写入路径)"
      pattern: "getEventStore\\(\\)\\.append"
    - from: "src/ai/chatSession.ts"
      to: "src/ai/tokenEstimate.ts"
      via: "预算裁剪使用 estimateTokens"
      pattern: "estimateTokens"
    - from: "ChatSession.fromEvents"
      to: "getMessagesForLLM"
      via: "重建会话与活会话派生出相同 LLM messages(replay parity 前提)"
      pattern: "fromEvents"
---

<objective>
把 ChatSession 重构为事件日志的投影:保留类与公开 API;`addMessage` 在更新内存消息的同时追加事件(经 Plan 01 的 EventStore 按会话串行);新增 `ChatSession.fromEvents()` 重建路径与 `flushEvents()`;`getMessagesForLLM` 成为唯一派生格式(tool_call assistant 折叠、tool 内容透传);token 预算改用 CJK 感知估算。本 plan 不改 toolLoop(Plan 03)。

Purpose: EVT-03 的核心 — 建立"消息列表只是日志投影"的单一真相;为 Plan 03 消灭 toolLoop 双历史提供派生函数。
Output: 重构后的 chatSession.ts + 投影测试套件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-event-log-toolloop/13-CONTEXT.md
@.planning/phases/13-event-log-toolloop/13-01-SUMMARY.md
@src/ai/chatSession.ts
@src/ai/events/eventStore.ts
@src/ai/events/types.ts
@src/ai/tokenEstimate.ts
@src/ai/__tests__/phase10PromptContext.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 chatSession.ts 为事件日志投影</name>
  <files>src/ai/chatSession.ts</files>
  <read_first>
    - src/ai/chatSession.ts(当前实现全文 — 构造函数重载、groupIntoTurns、trimToBudget、trimOversizedTurn 必须保留语义)
    - src/ai/__tests__/phase10PromptContext.test.ts(既有契约:addMessage 位置参数签名、getMessagesForLLM 内容序列、getAllMessages()[1].toolCallId === 'call-1'、tokenBudget: 5 用例 — 全部必须继续通过)
    - src/ai/events/eventStore.ts(getEventStore/append 接口)
    - src/ai/events/types.ts(AgentEvent/AgentEventInput/AgentEventType)
    - src/ai/tokenEstimate.ts(estimateTokens)
    - .planning/phases/13-event-log-toolloop/13-CONTEXT.md(Locked:保留类与 API;addMessage 双写;fromEvents;事件类型映射)
  </read_first>
  <action>
重写 `src/ai/chatSession.ts`。保持现有导出(`ChatSessionRole`, `ChatMessage`, `ChatSessionMessageInput`, `ChatSessionOptions`, `ChatSession`)与构造函数重载签名不变,具体变更如下:

1. **导入**:顶部新增
```ts
import { getEventStore } from './events/eventStore';
import type { AgentEvent } from './events/types';
import { estimateTokens } from './tokenEstimate';
```

2. **token 估算替换(EVT-07 接线)**:删除文件内 `function estimateMessageTokens(...)`,把所有调用点(`trimToBudget`、`trimOversizedTurn`、`estimateTokens()` 方法)替换为 `estimateTokens(message.content)`。

3. **ChatSessionMessageInput 扩展(可选字段,向后兼容)**:
```ts
export interface ChatSessionMessageInput {
  role: ChatSessionRole;
  content: string;
  toolCallId?: string;
  toolName?: string;
  /** Structured payload recorded on the emitted event (args / ok / error / artifactId / ...). */
  payload?: Record<string, unknown>;
}
```

4. **ChatSessionOptions**:公开字段不变(`sessionId?`, `tokenBudget?`);仅新增第 6 点所述的内部可选字段 `__emitEvents`。

5. **ChatSession 类新增私有状态与方法**:
```ts
private currentCorrelationId: string | null = null;
private readonly emitEvents: boolean;
private lastEventPromise: Promise<unknown> = Promise.resolve();

setCorrelationId(correlationId: string | null): void {
  this.currentCorrelationId = correlationId;
}

/** Await pending event writes for this session (tests + turn-end audits). */
async flushEvents(): Promise<void> {
  await this.lastEventPromise;
}

/** Emit turn_ended (no in-memory message). payload: { outcome, iterations, toolCallsExecuted }. */
recordTurnEnd(payload: Record<string, unknown>): void {
  if (!this.emitEvents) return;
  this.lastEventPromise = getEventStore().append({
    sessionId: this.sessionId,
    eventType: 'turn_ended',
    payload,
    correlationId: this.currentCorrelationId,
  }).then(() => undefined, () => undefined);
}
```

6. **构造函数**:新增实例字段 `private readonly emitEvents: boolean;`(默认 true),两个现有重载路径的末尾在 `this.emitEvents === true` 时调用私有方法 `this.emitSessionCreated()`:
```ts
private emitSessionCreated(): void {
  this.lastEventPromise = getEventStore().append({
    sessionId: this.sessionId,
    eventType: 'session_created',
    payload: { sessionId: this.sessionId, tokenBudget: this.tokenBudget },
  }).then(() => undefined, () => undefined);
}
```
`emitEvents` 默认 true。重建实例必须从不发射任何事件(包括构造时的 session_created),通过内部选项字段 `/** internal: rebuilt sessions never re-emit */ __emitEvents?: boolean` 实现(双下划线标记内部使用,不属于公开语义)。构造函数解析:
```ts
constructor(sessionIdOrOptions?: string | ChatSessionOptions, tokenBudget = DEFAULT_TOKEN_BUDGET) {
  // ...existing overload resolution...
  this.emitEvents = resolvedOptions?.__emitEvents ?? true;   // 字符串重载路径下为 true
  if (this.emitEvents) this.emitSessionCreated();
}
```
fromEvents 使用 `{ sessionId, tokenBudget, __emitEvents: false }` 构造,因此重建实例从不发射事件(包括 session_created)。

7. **addMessage 双写事件(EVT-01)**:在现有 `this.messages.push({ ...message, timestamp: Date.now() })` 之后追加:
```ts
if (this.emitEvents) {
  this.lastEventPromise = getEventStore().append(this.toEventInput(message)).then(() => undefined, () => undefined);
}
```
私有映射方法(事件词表,逐字实现):
```ts
private toEventInput(message: ChatSessionMessageInput): AgentEventInput {
  const correlationId = this.currentCorrelationId;
  if (message.role === 'user') {
    return { sessionId: this.sessionId, eventType: 'user_message', payload: { content: message.content }, correlationId };
  }
  if (message.role === 'assistant' && !message.toolCallId) {
    return { sessionId: this.sessionId, eventType: 'assistant_message', payload: { content: message.content }, correlationId };
  }
  if (message.role === 'assistant' && message.toolCallId) {
    return {
      sessionId: this.sessionId,
      eventType: 'tool_call',
      payload: { toolCallId: message.toolCallId, toolName: message.toolName ?? null, args: message.payload?.args ?? null, content: message.content },
      correlationId,
    };
  }
  // role 'tool' — model-visible text is the content itself (composed by toolLoop)
  return {
    sessionId: this.sessionId,
    eventType: 'tool_result',
    payload: { toolCallId: message.toolCallId ?? null, toolName: message.toolName ?? null, modelText: message.content, ...(message.payload ?? {}) },
    correlationId,
  };
}
```
(需要 `import type { AgentEventInput } from './events/types';`)。两个 addMessage 重载都归一到对象形式后走同一 push+emit 路径(现状已如此)。

8. **fromEvents 静态重建(EVT-03)**:
```ts
/** Rebuild a session as a pure projection of its event stream. Never re-emits events. */
static fromEvents(events: AgentEvent[], options?: { sessionId?: string; tokenBudget?: number }): ChatSession {
  const sessionId = options?.sessionId ?? events[0]?.sessionId ?? crypto.randomUUID();
  const session = new ChatSession({ sessionId, tokenBudget: options?.tokenBudget, __emitEvents: false });
  for (const event of events) {
    const payload = event.payload;
    const timestamp = Date.parse(event.createdAt) || Date.now();
    switch (event.eventType) {
      case 'user_message':
        session.pushRebuilt({ role: 'user', content: String(payload.content ?? '') }, timestamp);
        break;
      case 'assistant_message':
        session.pushRebuilt({ role: 'assistant', content: String(payload.content ?? '') }, timestamp);
        break;
      case 'tool_call':
        session.pushRebuilt({
          role: 'assistant',
          content: String(payload.content ?? '[requesting tools]'),
          toolCallId: typeof payload.toolCallId === 'string' ? payload.toolCallId : undefined,
          toolName: typeof payload.toolName === 'string' ? payload.toolName : undefined,
        }, timestamp);
        break;
      case 'tool_result':
        session.pushRebuilt({
          role: 'tool',
          content: String(payload.modelText ?? ''),
          toolCallId: typeof payload.toolCallId === 'string' ? payload.toolCallId : undefined,
          toolName: typeof payload.toolName === 'string' ? payload.toolName : undefined,
        }, timestamp);
        break;
      default:
        break; // session_created / turn_ended / future types carry no message
    }
  }
  return session;
}

private pushRebuilt(message: Omit<ChatMessage, 'timestamp'>, timestamp: number): void {
  this.messages.push({ ...message, timestamp });
}
```

9. **getMessagesForLLM 投影折叠**:在现有 `groupIntoTurns(this.messages).slice(-maxTurns)` → `trimToBudget(turns, this.tokenBudget)` 之后、返回 map 之前,对展平结果应用新私有函数:
```ts
/** One model response may request N tools => N assistant-with-toolCallId rows in
 * the session; the LLM must see that response once. Collapse consecutive rows. */
function collapseToolCallAssistants(messages: ChatMessage[]): ChatMessage[] {
  const out: ChatMessage[] = [];
  for (const message of messages) {
    const prev = out[out.length - 1];
    if (message.role === 'assistant' && message.toolCallId && prev && prev.role === 'assistant' && prev.toolCallId) {
      continue;
    }
    out.push(message);
  }
  return out;
}
```
getMessagesForLLM 返回 `collapseToolCallAssistants(selected).map(({ role, content }) => ({ role, content }))`(返回类型签名不变)。

10. **clear()**:保留(只清内存投影;事件日志 append-only,不提供删除 — 加注释说明)。

11. **groupIntoTurns / trimToBudget / trimOversizedTurn / DEFAULT_MAX_TURNS=8 / DEFAULT_TOKEN_BUDGET=8000**:语义与常量值不变。
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint && npx tsx --test src/ai/__tests__/phase13ChatSessionProjection.test.ts src/ai/__tests__/phase10PromptContext.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/ai/chatSession.ts 包含 `import { getEventStore } from './events/eventStore';`
    - src/ai/chatSession.ts 包含 `import { estimateTokens } from './tokenEstimate';` 且不再包含 `length / 4` 或 `length/4`
    - src/ai/chatSession.ts 包含 `static fromEvents(events: AgentEvent[]`
    - src/ai/chatSession.ts 包含 `async flushEvents(): Promise<void>`
    - src/ai/chatSession.ts 包含 `setCorrelationId(` 与 `recordTurnEnd(`
    - src/ai/chatSession.ts 包含 `'user_message'`、`'assistant_message'`、`'tool_call'`、`'tool_result'`、`'session_created'`、`'turn_ended'` 六个事件类型字符串
    - src/ai/chatSession.ts 包含 `collapseToolCallAssistants`
    - src/ai/chatSession.ts 保留 `constructor(sessionId?: string, tokenBudget?: number);` 与 `constructor(options?: ChatSessionOptions);` 两个重载声明
    - `npx tsx --test src/ai/__tests__/phase10PromptContext.test.ts` 退出码 0(既有契约零回归)
  </acceptance_criteria>
  <done>ChatSession 成为事件投影:addMessage 双写、fromEvents 重建、投影折叠、CJK 预算,phase10 测试零改动通过</done>
</task>

<task type="auto">
  <name>Task 2: phase13ChatSessionProjection.test.ts — 投影与重建测试</name>
  <files>src/ai/__tests__/phase13ChatSessionProjection.test.ts</files>
  <read_first>
    - src/ai/chatSession.ts(重构后的实际 API — 先读 Task 1 产物再写测试)
    - src/ai/__tests__/phase10PromptContext.test.ts(node:test 约定)
    - src/ai/events/eventStore.ts(getMemoryEventStore/resetMemoryEventStore/listEvents)
    - src/ai/__tests__/phase13EventLog.test.ts(Plan 01 测试的辅助函数写法)
  </read_first>
  <action>
新建 `src/ai/__tests__/phase13ChatSessionProjection.test.ts`,node:test + node:assert/strict,文件头 `// Phase 13 Plan 02 — ChatSession as event-log projection.`,每个测试开头 `resetMemoryEventStore()`。必须包含以下测试(名称逐字):

1. `test('new session emits session_created then user/assistant events with contiguous seq', async () => ...)`:
`const session = new ChatSession({ sessionId: 'proj-1', tokenBudget: 8000 });`
`session.addMessage('user', '帮我查任务');` `session.addMessage('assistant', '好的,正在查询');`
`await session.flushEvents();` `const events = await getMemoryEventStore().listEvents('proj-1');`
断言 events.map(e => e.eventType) deepEqual ['session_created', 'user_message', 'assistant_message'];seq deepEqual [1, 2, 3];events[1].payload.content === '帮我查任务'。

2. `test('tool_call and tool_result messages map to paired events with structured payload', async () => ...)`:
session 'proj-2';`session.setCorrelationId('corr-turn-1');`
`session.addMessage('user', '安排任务');`
`session.addMessage('assistant', '我先查询', 'tc-uuid-1', 'listTasks', { args: { status: 'open' } });`
`session.addMessage('tool', '[tool_result listTasks] {"ok":true}', 'tc-uuid-1', 'listTasks', { ok: true });`
flush 后取事件:tool_call 事件 payload.toolCallId === 'tc-uuid-1'、payload.args.status === 'open'、payload.content === '我先查询';tool_result 事件 payload.modelText === '[tool_result listTasks] {"ok":true}'、payload.ok === true;两条事件 correlationId 均为 'corr-turn-1'。

3. `test('projection collapses consecutive tool-call assistant rows into one LLM message', () => ...)`:
session 'proj-3'(emitEvents 无关紧要,纯内存断言):
addMessage('user', '查两个工具');
addMessage('assistant', 'thinking', 'tc-a', 'listTasks');
addMessage('assistant', 'thinking', 'tc-b', 'listProducts');
addMessage('tool', '[tool_result listTasks] {"ok":true,"data":[]}', 'tc-a', 'listTasks');
addMessage('tool', '[tool_result listProducts] {"ok":true,"data":[]}', 'tc-b', 'listProducts');
addMessage('assistant', '都查完了');
断言 getMessagesForLLM().map(m => m.content) deepEqual ['查两个工具', 'thinking', '[tool_result listTasks] {"ok":true,"data":[]}', '[tool_result listProducts] {"ok":true,"data":[]}', '都查完了'](5 条,第二个 'thinking' 被折叠);且除最后一条外 role 序列为 ['user', 'assistant', 'tool', 'tool']。

4. `test('fromEvents rebuilds an identical projection and never re-emits events', async () => ...)`:
session 'proj-4' 走一遍完整序列:user → assistant(toolCallId tc-1, args {a:1}) → tool(modelText '[tool_result listTasks] {"ok":true}') → assistant('完成') → recordTurnEnd({ outcome: 'completed', iterations: 1, toolCallsExecuted: 1 }) → flush。
`const events = await getMemoryEventStore().listEvents('proj-4');`
断言 events 末尾 eventType === 'turn_ended' 且 payload.outcome === 'completed'。
`const rebuilt = ChatSession.fromEvents(events);` `const rebuiltAgain = ChatSession.fromEvents(events);`
断言 rebuilt.getMessagesForLLM() deepEqual session.getMessagesForLLM()(活会话与重建会话同一次派生格式);
断言 rebuiltAgain.getMessagesForLLM() deepEqual rebuilt.getMessagesForLLM()(回放两次一致 — replay parity 投影层锚点);
断言 rebuilt.getAllMessages()[1].toolCallId === 'tc-1';
再 `await rebuilt.flushEvents();` 后 listEvents('proj-4') 数量与之前相等(重建不发射事件,含 session_created)。

5. `test('CJK-aware budget drops oversized Chinese turns that length/4 would keep', () => ...)`:
`const session = new ChatSession({ sessionId: 'proj-5', tokenBudget: 20 });`
addMessage('user', '中文'.repeat(30))(60 CJK 字符 → 60 token);addMessage('assistant', '好的');(第二轮)addMessage('user', '新问题');addMessage('assistant', '回答');
断言 getMessagesForLLM().map(m => m.content) deepEqual ['新问题', '回答']。
(旧 length/4 估算:第一轮仅 15+1=16 token ≤ 20,会被错误保留 — 本断言失败即证明 EVT-07 未接线。)

6. `test('phase10 contract: eight-turn window, tool grouping and tiny budgets still hold', () => ...)`:
复制 phase10PromptContext.test.ts 中两个 ChatSession 用例的断言序列(10 轮取最新 8 轮;toolSession 的 6 条内容序列与 getAllMessages()[1].toolCallId === 'call-1'),确保重构后契约双重锚定。
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npx tsx --test src/ai/__tests__/phase13ChatSessionProjection.test.ts src/ai/__tests__/phase10PromptContext.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test src/ai/__tests__/phase13ChatSessionProjection.test.ts` 退出码 0,6 个具名测试全部通过
    - 测试文件包含字符串 'session_created'、'tool_call'、'tool_result'、'turn_ended'、'ChatSession.fromEvents('
    - 测试文件包含 `['session_created', 'user_message', 'assistant_message']` 序列断言
    - 测试文件包含 replay parity 断言:`rebuiltAgain.getMessagesForLLM()` 与 `rebuilt.getMessagesForLLM()` deepEqual
    - `npx tsx --test src/ai/__tests__/phase10PromptContext.test.ts` 退出码 0
  </acceptance_criteria>
  <done>投影/重建/相关性/中文预算/phase10 契约全部有测试锚点且全绿</done>
</task>

</tasks>

<verification>
1. `npm run lint` — 0 类型错误
2. `npx tsx --test src/ai/__tests__/phase13ChatSessionProjection.test.ts` — 6/6
3. `npx tsx --test src/ai/__tests__/phase10PromptContext.test.ts` — 既有 ChatSession 契约零回归(金丝雀之一)
4. 回归:`npx tsx --test src/ai/__tests__/registry.test.ts src/ai/__tests__/knowledgeWrite.test.ts src/ai/__tests__/phase11Plan03.test.ts src/ai/__tests__/scheduleAdvanced.test.ts src/ai/__tests__/taskAdvanced.test.ts src/ai/__tests__/phase13EventLog.test.ts src/stores/__tests__/rndStore.test.ts src/stores/__tests__/settingsProvider.test.ts` — 全部通过(ChatPanel 走 npm run dev 手动冒烟不在本 plan 范围,toolLoop 未改)
5. grep 确认 chatSession.ts 中无 `length / 4`、无对 messages 数组的删除式操作(clear 只清内存)
</verification>

<success_criteria>
- addMessage 追加消息的同时追加正确类型的事件,user/assistant/tool_call/tool_result 映射与 §3 词表一致,事件携带 correlation_id
- fromEvents(events) 重建的会话与活会话派生出相同 getMessagesForLLM 输出;重建不发射事件
- getMessagesForLLM 折叠连续 tool_call assistant 行、tool 内容透传 — 派生格式唯一
- 中文 60-token 轮次在 budget 20 下被裁掉(EVT-07 接线生效)
- phase10PromptContext.test.ts 零改动通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-event-log-toolloop/13-02-SUMMARY.md`
</output>
