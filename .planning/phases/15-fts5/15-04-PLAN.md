---
phase: 15-fts5
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - src/components/ChatPanel.tsx
  - src/views/KnowledgeBaseView.tsx
autonomous: false
requirements: [MEM-01, MEM-02, MEM-03, MEM-06, MEM-07]
must_haves:
  truths:
    - "模型提出记忆候选后，ChatPanel 出现「待确认的记忆」卡片（仅 model_inferred），点「记住」入库点「忽略」消失，被忽略的不再出现"
    - "用户显式说「记住 X」→ 不出现待确认卡片，出现「已记住」信息卡（无记住/忽略按钮，不可拒绝），内容即时进入长期记忆（锁定决策后半句）"
    - "知识库侧栏搜索框输入 2 字中文词（如'需求'）出现扁平结果列表，每条带 {产品} · v{版本} · {时间} 来源行"
    - "标签/产品/时间三个过滤器任意激活即进入搜索模式，无关键词也可过滤"
    - "知识库侧栏「长期记忆」入口可查看全部记忆（含'已被取代'标记），删除有 danger 确认弹窗"
    - "会话恢复后 pending 记忆候选重新出现（与 Phase 14 knowledge banner 同款恢复行为）"
  artifacts:
    - path: "src/components/ChatPanel.tsx"
      provides: "记忆候选内联确认卡片（Surface 1）+ user_directed「已记住」信息卡"
      contains: "待确认的记忆"
    - path: "src/views/KnowledgeBaseView.tsx"
      provides: "FTS5 搜索 + 过滤（Surface 2）与记忆列表（Surface 3）"
      contains: "清除筛选"
  key_links:
    - from: "src/components/ChatPanel.tsx"
      to: "src/ai/memoryStore.ts"
      via: "confirm/reject 消费 + listPending 恢复刷新 + listRecentUserDirected 信息卡"
      pattern: "getMemoryStore\\(\\)\\.(confirm|reject|listPending|listRecentUserDirected)"
    - from: "src/views/KnowledgeBaseView.tsx"
      to: "src/ai/knowledgeRepo.ts"
      via: "debounce 300ms 调 repo.search"
      pattern: "getKnowledgeRepo\\(\\)\\.search"
---

<objective>
Phase 15 三块 UI 落地：ChatPanel 记忆候选确认卡片（含 user_directed「已记住」信息卡分支）、KnowledgeBaseView FTS5 搜索 + 过滤 + 记忆管理列表，末尾人工 UAT 检查点。

Purpose: MEM-01/02/03 的用户触点（含锁定决策后半句：用户显式指令直接入库不可拒）、MEM-06/07 的检索界面（15-UI-SPEC.md 三 surfaces，零新依赖零新 token）。
Output: ChatPanel 双卡片、KnowledgeBaseView 双 pane 改造、UAT PASS。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-fts5/15-UI-SPEC.md
@.planning/phases/15-fts5/15-02-SUMMARY.md
@.planning/phases/15-fts5/15-03-SUMMARY.md

<interfaces>
<!-- 来自 15-02/15-03（本 plan 依赖） -->

From src/ai/memoryStore.ts:
```typescript
getMemoryStore().listPending(): Promise<MemoryCandidate[]>   // { candidateToken, content, origin, scope, productId, createdAt, expiresAt }
// 仅 model_inferred 候选 — user_directed 已在 propose 内自动 confirm+consume（15-01 T3 锁定决策），不经此列表
getMemoryStore().listRecentUserDirected(limit=5): Promise<MemoryCandidate[]>  // status='consumed' AND origin='user_directed'，按 consumed_at DESC — 「已记住」信息卡数据源
getMemoryStore().confirm(token): Promise<MemoryCandidate>
getMemoryStore().reject(token): Promise<boolean>
getMemoryStore().consumeIntoMemories(token): Promise<MemoryRecord>  // confirm 后由 UI 直接触发入库（或 confirm 即含 — 以 15-02 实现为准：UI 调 confirm + consumeIntoMemories 链）
getMemoryStore().listAllMemories(): Promise<MemoryRecord[]>  // 含 supersededAt 标记，供 Surface 3
```

From src/ai/knowledgeRepo.ts:
```typescript
getKnowledgeRepo().search(query: string, filters?: { productId?: string; tag?: string; since?: string; limit?: number }): Promise<KnowledgeHit[]>
// KnowledgeHit = { docId, version, title, category, tags, summary, productId, sourceType, updatedAt }
```

<!-- 既有 UI 契约（照抄源） -->

From src/components/ChatPanel.tsx:
```
:383-396  knowledge write pending banner（Surface 1 卡片的逐字模板）
:124-127  会话恢复时 pendingKnowledgeWrites 恢复逻辑（记忆候选同款恢复）
```

From src/views/KnowledgeBaseView.tsx:
```
:100-105  搜索 Input 壳（MagnifyingGlass icon，改 functional）
categories memo / products（productStore）— 过滤器数据源
setActiveItemId — 结果行点击复用既有选中流
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChatPanel 记忆候选卡片 + user_directed「已记住」信息卡（Surface 1）</name>
  <files>src/components/ChatPanel.tsx</files>
  <read_first>
    - src/components/ChatPanel.tsx（整文件 — 重点 :124-127 恢复逻辑、:383-410 两个 pending banner 模板、bottomRef 位置）
    - src/ai/memoryStore.ts（listPending/listRecentUserDirected/confirm/reject/consumeIntoMemories 签名）
    - .planning/phases/15-fts5/15-UI-SPEC.md（Surface 1 契约 + Copywriting Contract — JSX 逐字模板已给出）
  </read_first>
  <action>
在 ChatPanel 中，`pendingDestructiveAction` banner 之后、`bottomRef` 之前插入两张卡片（15-UI-SPEC Surface 1 模板 + origin 分支渲染，锁定决策后半句：user_directed 已直接入库，只展示不可拒）：

**卡片 A — 待确认（仅 model_inferred 候选出现在 listPending）：**

```tsx
{pendingMemory && (
  <div className="rounded-[var(--radius-lg)] border border-accent/30 bg-accent-subtle px-3.5 py-3 text-sm text-text-primary">
    <div className="font-medium">待确认的记忆</div>
    <div className="mt-1 text-xs text-text-secondary">{pendingMemory.content}</div>
    <div className="mt-1 text-xs text-text-tertiary">来源: 本次对话 · {formatTime(pendingMemory.createdAt)}</div>
    <div className="mt-2 flex gap-2">
      <Button type="button" variant="primary" size="sm" onClick={confirmMemory} disabled={memoryBusy}>记住</Button>
      <Button type="button" variant="secondary" size="sm" onClick={rejectMemory} disabled={memoryBusy}>忽略</Button>
    </div>
  </div>
)}
```

**卡片 B — 已记住（origin='user_directed'，无确认/拒绝按钮，不可拒 — store 已自动入库）：**

```tsx
{autoRemembered && (
  <div className="rounded-[var(--radius-lg)] border border-accent/30 bg-accent-subtle px-3.5 py-3 text-sm text-text-primary">
    <div className="font-medium">已记住</div>
    <div className="mt-1 text-xs text-text-secondary">{autoRemembered.content}</div>
    <div className="mt-1 text-xs text-text-tertiary">来源: 你的指令 · {formatTime(autoRemembered.createdAt)}</div>
    <div className="mt-2">
      <Button type="button" variant="ghost" size="sm" onClick={() => setAutoRemembered(null)}>知道了</Button>
    </div>
  </div>
)}
```

逻辑：
- state：`pendingMemory: MemoryCandidate | null`、`autoRemembered: MemoryCandidate | null`、`memoryBusy: boolean`。各卡一次只显示一条（UI spec 锁定，无堆叠）。
- 刷新点：每轮 `onToolEnd('proposeMemory', ...)` 后 + 组件 mount 恢复时（照 :124-127 pendingKnowledgeWrites 同款）调两个查询：`getMemoryStore().listPending()` 取队头 → `setPendingMemory`；`getMemoryStore().listRecentUserDirected(1)` 取最新一条 → 若与已展示 token 不同则 `setAutoRemembered`。
- `confirmMemory`：`memoryBusy=true` → `confirm(token)` → `consumeIntoMemories(token)`（或 15-02 实现的合并入口）→ `toast({ type: 'success', title: '已记住' })` → 刷新队头（下一条候选自动渲染同 slot）。
- `rejectMemory`：`reject(token)` → 静默（UI spec：忽略无 toast）→ 刷新队头。被拒候选因 listPending 过滤 status 永不回显（MEM-02）。
- 卡片 B「知道了」只清本地展示状态，不触碰 store（记忆已入库，不可撤回 — 删除走 Surface 3）。
- 错误路径 catch → `toast({ type: 'error', title: '检索失败,请稍后重试;若持续失败请重启应用。' })`（UI spec error copy）。
- 禁用/无 expiry UI、无 queue-full 提示（UI spec 锁定 YAGNI）。
  </action>
  <verify>
    <automated>grep -q "待确认的记忆" src/components/ChatPanel.tsx && grep -q "已记住" src/components/ChatPanel.tsx && grep -qE "listPending|listRecentUserDirected" src/components/ChatPanel.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "待确认的记忆\|记住\|忽略" src/components/ChatPanel.tsx` ≥ 3
    - 卡片容器 class 含 `border-accent/30 bg-accent-subtle`（与 Phase 14 banner 逐字同款）
    - `grep -q "listPending" 与 `grep -q "listRecentUserDirected"` 均命中（mount 恢复 + tool 后刷新）
    - 卡片 A 两个按钮均有 `disabled={memoryBusy}`；卡片 B 无「记住/忽略」按钮（仅 ghost「知道了」— user_directed 不可拒，锁定决策）
    - 卡片 B 渲染 `来源: 你的指令`（origin 分支文案）
    - 无 expiry/queue-full 相关 UI 字符串
    - `npm run lint` 退出码 0
  </acceptance_criteria>
  <done>两张卡片出现在 pending banner 与 bottomRef 之间：model_inferred 确认/忽略/恢复三链路接通；user_directed 只展示「已记住」不可拒；copy 与 UI spec 逐字一致（origin 分支为锁定决策修订）。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: KnowledgeBaseView 搜索 + 过滤（Surface 2）</name>
  <files>src/views/KnowledgeBaseView.tsx</files>
  <read_first>
    - src/views/KnowledgeBaseView.tsx（整文件 — :100-105 搜索壳、categories memo、setActiveItemId 流、sidebar 布局）
    - src/ai/knowledgeRepo.ts（search 签名与 KnowledgeHit）
    - src/components/ui/index.ts（Input/Select/Button 导入面）
    - .planning/phases/15-fts5/15-UI-SPEC.md（Surface 2 契约 — 行 JSX/过滤项/清除按钮逐字）
  </read_first>
  <action>
Sidebar（w-64 列）自上而下改造（15-UI-SPEC Surface 2 逐字）：

1. **搜索框**：现有 :100-105 Input 变受控 `query` state，`placeholder="搜索文档..."`（不变），debounce 300ms。query ≥ 1 字即触发（中文 2 字是后端的事，UI 不做长度门槛）。
2. **过滤行**（常驻，垂直堆叠 gap-2）：三个 `Select`（h-8 trigger）— 标签（categories memo，placeholder `全部标签`）、产品（productStore products，`全部产品`）、时间（`全部时间`/`最近 7 天`/`最近 30 天` → since = now-7d/30d ISO）。
3. **结果区双模式**：
   - Browse 模式（query 空 且 三过滤全空）：既有分类树原样。
   - Search 模式（query 非空 **或** 任一过滤激活 — UI spec 锁定）：扁平结果列表替换树。每行（UI spec JSX 逐字）：
     ```tsx
     <button className="w-full flex flex-col gap-0.5 px-3 py-2 rounded-[var(--radius-sm)] text-sm text-text-secondary hover:bg-bg-secondary text-left" onClick={() => setActiveItemId(hit.docId)}>
       <span className="truncate font-medium text-text-primary">{hit.title}</span>
       <span className="text-xs text-text-tertiary">{productName(hit.productId)} · v{hit.version} · {hit.updatedAt}</span>
     </button>
     ```
   - 列表顶部：`{n} 条结果`（text-xs text-text-tertiary）+ `清除筛选` ghost sm 按钮（清 query + 三过滤回树模式）。
4. **检索调用**：debounce 后 `const hits = await getKnowledgeRepo().search(query, { productId, tag, since, limit: 50 })`；空 query 时 search 仍走结构过滤路径（15-03 已实现 filters-only）。catch → `toast({ type: 'error', title: '检索失败,请稍后重试;若持续失败请重启应用。' })`。
5. **空态**：搜索模式 0 结果 → `没有匹配的文档` / `换个关键词试试,或清除筛选条件查看全部文档。`
6. 结果行 tags 含过滤 tag 的语义由 repo 保证；UI 只渲染。

点击结果行走既有 `setActiveItemId` 选中流（零新路由）。tag 过滤值用 Select 直选单个 tag（UI spec：标签即 category Select）。
  </action>
  <verify>
    <automated>grep -q "清除筛选" src/views/KnowledgeBaseView.tsx && grep -q "getKnowledgeRepo" src/views/KnowledgeBaseView.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "全部标签\|全部产品\|全部时间" src/views/KnowledgeBaseView.tsx` = 3
    - `grep -q "清除筛选" 与 `grep -q "条结果"` 均命中
    - `grep -q "v{hit.version}" 或等价 version 渲染` 命中（MEM-07 来源行）
    - debounce 常量 300（grep `300` 命中 debounce 上下文）
    - 结果行 class 含 `hover:bg-bg-secondary`（无 raw hex / text-gray）
    - `npm run lint` 退出码 0
  </acceptance_criteria>
  <done>搜索 + 三过滤 + 双模式 + 空态 + 清除全部接通，检索走 knowledgeRepo 单一通道。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 记忆管理列表 pane（Surface 3）</name>
  <files>src/views/KnowledgeBaseView.tsx</files>
  <read_first>
    - src/views/KnowledgeBaseView.tsx（Task 2 改造后的当前状态 — pane 切换插入点）
    - src/ai/memoryStore.ts（listAllMemories/deleteMemory 签名）
    - src/components/ui/index.ts（Card/Badge/Dialog/Button）
    - .planning/phases/15-fts5/15-UI-SPEC.md（Surface 3 卡片 JSX + 删除确认 Dialog copy 逐字）
  </read_first>
  <action>
1. 侧栏「快速访问」区新增入口 `长期记忆`（`Brain` icon, `size={16} weight="duotone" className="text-accent"`），与 我的收藏/所有文档 并列。pane state：`activePane: 'docs' | 'memories'`，点击切换主 pane。
2. 记忆 pane（UI spec 结构逐字）：header `长期记忆 · N 条`（h-12 对齐既有 viewer header；**不建「删除全部」**）；列表区 `space-y-2 p-10 max-w-2xl mx-auto`；每条：

```tsx
<Card className="p-4">
  <div className="flex items-start justify-between gap-2">
    <p className="text-sm text-text-primary leading-6">{memory.content}</p>
    <Button variant="ghost" size="sm" onClick={() => askDelete(memory)}>删除</Button>
  </div>
  <div className="mt-2 flex items-center gap-2 text-xs text-text-tertiary">
    <Badge variant="neutral">{memory.scope === 'product' ? '产品' : '全局'}</Badge>
    <span>确认于 {memory.confirmedAt}</span>
    {memory.supersededAt && <Badge variant="warning">已被取代</Badge>}
  </div>
</Card>
```
3. 删除确认：`Dialog`（max-w-md）— title `删除这条记忆?`，body `删除后 AI 将不再使用这条信息,且无法恢复。`，confirm `Button variant="danger"` `删除记忆` → `deleteMemory(memoryRowid)` + `toast({ type: 'success', title: '记忆已删除' })` + 刷新列表；cancel `variant="secondary"` `取消`。
4. 数据：mount 进 pane 时 `getMemoryStore().listAllMemories()`（含 superseded，不隐藏 — UI spec 锁定）。空态：`还没有长期记忆` / `在对话中让 AI 记住你的偏好和工作习惯,确认后会出现在这里。`
5. 无编辑无新增按钮（锁定：本期最小列表）。字段缺失省略元素不渲染占位。
  </action>
  <verify>
    <automated>grep -q "长期记忆" src/views/KnowledgeBaseView.tsx && grep -q "listAllMemories" src/views/KnowledgeBaseView.tsx && grep -q "删除这条记忆" src/views/KnowledgeBaseView.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Brain" src/views/KnowledgeBaseView.tsx` 命中（icon import 自 @phosphor-icons/react）
    - `grep -q "listAllMemories"` 与 `grep -q "deleteMemory"` 命中
    - `grep -q "已被取代"` 命中且为 `Badge variant="warning"`
    - `grep -q "删除这条记忆?"` 与 `grep -q "variant=\"danger\""` 命中
    - `grep -c "删除全部"` 输出 0（OUT OF SCOPE）
    - `npm run lint` 退出码 0
  </acceptance_criteria>
  <done>记忆查看 + 删除最小闭环落地，superseded 可审计可见，全部 copy 与 UI spec 一致。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 15 UAT 人工验收（FTS5 运行时 probe + 全链路）</name>
  <files>无文件修改 — 人工验证检查点</files>
  <action>按 how-to-verify 逐步人工验证 Phase 15 全链路（详细步骤见下）。tauri:dev 启动本身就是 FTS5 运行时 probe：迁移 0004 的 CREATE VIRTUAL TABLE 在缺 FTS5 的打包 SQLite 上失败 → initializeDatabase 拒启。</action>
  <what-built>Phase 15 全链路：记忆候选确认流（proposeMemory → ChatPanel 卡片 → 入库/拒绝，含 user_directed「已记住」直接入库）、五段上下文注入（context_injected 事件）、FTS5 知识检索 UI（中文 2 字 + 过滤 + 来源行）、记忆管理列表、产品删除级联。</what-built>
  <how-to-verify>
    前置：`npm run tauri:dev`（迁移 0004 即 FTS5 运行时 probe — 启动失败 = 打包 SQLite 缺 FTS5，立即上报）。
    1. **FTS5 中文命中（MEM-06）**：知识库搜索框输入 `需求` → 结果列表出现含"需求"文档，每条带 `{产品} · v1 · {时间}` 来源行；再单独选"最近 7 天"过滤（无关键词）→ 只剩近期文档。
    2. **记忆候选流（MEM-01/02/03）**：a) 在 ChatPanel 对话让模型推断一条偏好（如"我喜欢简洁的周报"）→ 出现「待确认的记忆」卡片 → 点「忽略」→ 卡片消失；再让模型提同一条 → 不再出现（防重提）；再提一条新的 → 点「记住」→ toast「已记住」。b) **user_directed 直接入库（锁定决策后半句）**：对 AI 说「记住我用深色主题」→ 不出现待确认卡片，出现「已记住」信息卡（无记住/忽略按钮，仅「知道了」）→ 知识库「长期记忆」列表立即可见该条。
    3. **记忆列表（Surface 3）**：知识库侧栏点「长期记忆」→ 刚确认的记忆可见（全局 Badge + 确认时间）；点删除 → danger 弹窗 → 确认 → 列表刷新。
    4. **上下文注入审计（MEM-08）**：完成步骤 2 后查 nova.db `agent_events` 表（或既有 dev 工具）：本轮 turn 内有 `context_injected` 事件，payload.segments 含 5 段（含 fts_topk 段真实命中数 — 同时闭环确认 15-02/15-03 软前向引用已生效）。
    5. **产品删除级联**：删除一个测试产品 → 知识库搜该产品文档 0 命中；`agent_events` 无删除。
    6. **回归**：Phase 14 UAT-A（knowledge write 确认 → 重启 → banner 重现）仍通过；重启后 pending 记忆候选卡片重现（Surface 1 恢复行为）。
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues per numbered step</resume-signal>
  <verify>人工验收：上述 6 步全部通过，用户回复 "approved"</verify>
  <done>UAT 6 步全过：FTS5 中文命中、候选确认流（含 user_directed 直接入库）、记忆列表、context_injected 审计、级联删除、Phase 14 回归。</done>
</task>

</tasks>

<verification>
- `npm test` 全绿（本 plan 不新增测试，但不得回归）
- `npm run lint` 通过
- 上述 UAT 6 步全过
</verification>

<success_criteria>
- 三个 UI surface 与 15-UI-SPEC 逐字一致（copy/class/token）；Surface 1 的 origin 分支为锁定决策修订（user_directed「已记住」不可拒）
- MEM-01/02/03/06/07 的用户可观察行为全部成立
- Phase 15 全部 8 需求（MEM-01..08）端到端可用
</success_criteria>

<output>
After completion, create `.planning/phases/15-fts5/15-04-SUMMARY.md`
</output>
