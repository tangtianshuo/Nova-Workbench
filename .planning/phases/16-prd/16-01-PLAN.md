---
phase: 16-prd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/0005_deliverable_source_event.sql
  - src-tauri/src/lib.rs
  - src/ai/agentScope.ts
  - src/ai/confirmationStore.ts
  - src/ai/confirmations.ts
  - src/ai/knowledgeRepo.ts
  - src/ai/tools/generateDeliverable.ts
  - src/ai/toolLoop.ts
  - src/ai/index.ts
  - src/ai/contextAssembler.ts
  - src/data/mockRndData.ts
  - src/stores/rndStore.ts
  - src/ai/__tests__/phase16DeliverableCandidates.test.ts
  - src/ai/__tests__/phase16GenerateDeliverable.test.ts
  - src/ai/__tests__/phase16ContextAssembler.test.ts
autonomous: true
requirements: [DELIV-01, DELIV-03, DELIV-04]

must_haves:
  truths:
    - "generateDeliverable 首次调用只登记候选不写库,turn 不中断(不抛 ConfirmationRequiredError)"
    - "未选中产品时 generateDeliverable 报错「请先选择一个产品,再生成 PRD。」且不产生候选"
    - "确认后携带(编辑后)draft 的二次调用原子消费候选,写入 knowledge_docs 新版本并同步 FTS5,搜索立即命中"
    - "落槽同步更新研发中心 DEL-REQ-01 卡槽,携带 aiSource(sessionId/eventId/generatedAt)"
    - "同一卡槽重复落槽产生新版本,supersede 旧版本,版本链完整"
    - "被忽略(拒绝)的 PRD 草稿进入系统提示防重复段,模型不再重复生成"
  artifacts:
    - path: "src/ai/tools/generateDeliverable.ts"
      provides: "两段式 PRD 草稿 tool(登记候选 / 消费+落槽写)"
      exports: ["generateDeliverableSchema"]
      min_lines: 60
    - path: "src/ai/confirmations.ts"
      provides: "DeliverableDraftCandidate 全生命周期 API"
      contains: "createDeliverableDraftCandidate"
    - path: "src/ai/agentScope.ts"
      provides: "ambient session scope(sessionId + correlationId)供 tool 溯源"
      exports: ["setActiveAgentScope", "getActiveAgentScope"]
    - path: "src-tauri/migrations/0005_deliverable_source_event.sql"
      provides: "knowledge_docs.source_event_id 列(AI 溯源事件指针)"
      contains: "ALTER TABLE knowledge_docs"
  key_links:
    - from: "src/ai/toolLoop.ts"
      to: "src/ai/agentScope.ts"
      via: "runToolLoop 入口 setActiveAgentScope"
      pattern: "setActiveAgentScope\\(\\{ sessionId: session\\.sessionId, correlationId \\}\\)"
    - from: "src/ai/tools/generateDeliverable.ts"
      to: "src/ai/knowledgeRepo.ts"
      via: "upsertDoc 单一写 API(doc + FTS5 同事务路径)"
      pattern: "getKnowledgeRepo\\(\\)\\.upsertDoc"
    - from: "src/ai/tools/generateDeliverable.ts"
      to: "src/stores/rndStore.ts"
      via: "commitDeliverableDraft 卡槽投影"
      pattern: "commitDeliverableDraft"
    - from: "src/ai/tools/generateDeliverable.ts"
      to: "src/ai/confirmations.ts"
      via: "consumeDeliverableDraftConfirmation 原子消费"
      pattern: "consumeDeliverableDraftConfirmation"
    - from: "src/ai/contextAssembler.ts"
      to: "src/ai/confirmations.ts"
      via: "pending + rejected deliverable 注入防重复段"
      pattern: "listRejectedDeliverableDrafts"
---

<objective>
PRD 生产线后端管线:generateDeliverable tool(两段式候选确认)+ 落槽写路径(knowledgeRepo 版本链 + rndStore 卡槽投影 + AI 溯源)+ FTS5 立即命中审计 + 系统提示防重复。

Purpose: 打通「agent 生成 → 登记 → 原子消费 → 版本化落槽 → 立即可检索」的后端闭环。Plan 02 的 ChatPanel UI 与 Plan 03 的徽章全部建立在本计划的导出接口之上。
Output: generateDeliverable tool、DeliverableDraftCandidate API、migration 0005、rndStore.commitDeliverableDraft、21 个新自动化测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-prd/16-CONTEXT.md

<interfaces>
<!-- 本计划创建的契约(Plan 02/03 直接消费,不再探索代码库) -->

```typescript
// src/ai/agentScope.ts (new)
export interface AgentScope { sessionId: string; correlationId: string | null; }
export function setActiveAgentScope(scope: AgentScope | null): void;
export function getActiveAgentScope(): AgentScope | null;

// src/ai/confirmationStore.ts (extend)
export type ConfirmationKind = 'knowledge_write' | 'destructive_action' | 'deliverable_draft';
// ConfirmationStore interface 新增:
listRejected(kind: ConfirmationKind, limit?: number): Promise<PersistedConfirmation[]>;

// src/ai/confirmations.ts (new API,同文件既有 knowledge API 风格)
export type DeliverableCode = 'prd';
export interface DeliverableDraftCandidate {
  confirmationToken: string;
  productId: string;
  code: DeliverableCode;
  title: string;
  draft: string;
  sessionId: string | null;
  eventId: string | null;   // 生成 turn 的 correlation_id(agent_events 可查)
  createdAt: string;
}
export function createDeliverableDraftCandidate(input: {
  productId: string; code: DeliverableCode; title: string; draft: string;
  sessionId: string | null; eventId: string | null;
}): Promise<DeliverableDraftCandidate>;
export function getDeliverableDraftCandidate(token: string): Promise<DeliverableDraftCandidate | undefined>;
export function confirmDeliverableDraft(token: string): Promise<DeliverableDraftCandidate>;
export function rejectDeliverableDraft(token: string): Promise<boolean>;
export function listPendingDeliverableDrafts(): Promise<DeliverableDraftCandidate[]>;
export function listRejectedDeliverableDrafts(limit?: number): Promise<DeliverableDraftCandidate[]>;
export function consumeDeliverableDraftConfirmation(candidate: DeliverableDraftCandidate): Promise<DeliverableDraftCandidate>;

// src/ai/tools/generateDeliverable.ts (new) — tool 'generateDeliverable'
// 首次调用返回(不抛错,turn 继续):
{ ok: true, candidateQueued: true, confirmationToken, code, title,
  note: 'PRD 草稿已进入待确认队列;用户将在对话面板中确认并编辑,不要重复生成同一份草稿。' }
// 二次调用(携带 confirmationToken,由 ChatPanel 直接 executeTool)返回:
{ ok: true, docId, version, slotCode: 'DEL-REQ-01', ftsImmediateHit: boolean, ftsHitCount: number,
  aiSource: { sessionId: string; eventId: string; generatedAt: string } }

// src/ai/knowledgeRepo.ts (extend)
export interface KnowledgeDocInput { /* 既有字段 */ sourceEventId?: string; }
export interface KnowledgeDoc { /* 既有字段 */ sourceEventId: string | null; }

// src/stores/rndStore.ts (new action)
commitDeliverableDraft: (productId: string, slotCode: string, content: string,
  aiSource: { sessionId: string; eventId: string; generatedAt: string }) => void;

// src/data/mockRndData.ts (extend)
export interface FullLifecycleDeliverable { /* 既有字段 */
  aiSource?: { sessionId: string; eventId: string; generatedAt: string };
}
```

<!-- 既有契约(直接复用,勿改) -->

```typescript
// src/ai/confirmationStore.ts — store.create 已支持任意 kind + sessionId 列:
create(input: { kind: ConfirmationKind; params: Record<string, unknown>; summary: string | null;
  sessionId: string | null; ttlMs?: number }): Promise<PersistedConfirmation>;
consume(confirmationToken: string, paramsHash: string): Promise<PersistedConfirmation>; // 原子条件 UPDATE
listActive(kind: ConfirmationKind): Promise<PersistedConfirmation[]>; // pending+confirmed,TTL 内

// src/ai/paramsHash.ts
export function computeParamsHash(params: Record<string, unknown>): Promise<string>; // canonical JSON SHA-256

// src/ai/knowledgeRepo.ts — 单一写 API,同 docId 再写 = 新版本 supersede:
getKnowledgeRepo().upsertDoc(input: KnowledgeDocInput): Promise<KnowledgeDoc>;
getKnowledgeRepo().search(query, { productId?, tag?, since?, limit? }): Promise<KnowledgeHit[]>;
resetMemoryKnowledgeRepo(): void; // 测试复位

// src/ai/registry.ts
registerTool({ name, description, schema, execute });
executeTool(name, args);

// src/stores/uiStore.ts — useUIStore.getState().selectedProductId
// src/stores/productStore.ts — useProductStore.getState().products
// FULL_LIFECYCLE_DELIVERABLES_CATALOG: 18 条目,PRD 卡槽 code = 'DEL-REQ-01'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 候选存储底座 — migration 0005 + agentScope + deliverable_draft 候选 API</name>
  <files>src-tauri/migrations/0005_deliverable_source_event.sql, src-tauri/src/lib.rs, src/ai/agentScope.ts, src/ai/confirmationStore.ts, src/ai/confirmations.ts, src/ai/knowledgeRepo.ts, src/ai/__tests__/phase16DeliverableCandidates.test.ts</files>
  <read_first>
  - src-tauri/src/lib.rs (sql_migrations 注册格式,0004 条目在 :41)
  - src-tauri/migrations/0004_memories_knowledge_fts.sql (迁移文件头注释风格 + knowledge_docs 表结构)
  - src/ai/confirmationStore.ts (全文 — ConfirmationKind 联合类型 :11、ConfirmationStore 接口 :48-61、MemoryConfirmationStore :92-181、SqliteConfirmationStore :217-348)
  - src/ai/confirmations.ts (全文 — knowledge API 的一切形状:candidateFromRow/isRowAlive/knowledgeParams 模式,新 API 同构复制)
  - src/ai/knowledgeRepo.ts (KnowledgeDocInput :16-29、KnowledgeDoc :31-47、MemoryKnowledgeRepo.upsertDoc :104-129、KnowledgeDocRow :215-231、mapRow :233-251、SqliteKnowledgeRepo.upsertDoc :254-296)
  - src/ai/__tests__/phase14ConfirmationStore.test.ts (测试风格:node:test + assert/strict + resetMemoryConfirmationStore)
  </read_first>
  <behavior>
  - Test: createDeliverableDraftCandidate 后 listPendingDeliverableDrafts()[0] 携带全部字段(code/title/draft/sessionId/eventId/confirmationToken/createdAt)
  - Test: confirmDeliverableDraft → consumeDeliverableDraftConfirmation 成功;再次 consume 同一 token 抛错(already_settled 路径)
  - Test: rejectDeliverableDraft 后不在 listPendingDeliverableDrafts,出现在 listRejectedDeliverableDrafts(5)
  - Test: kind 判别 — knowledge_write 候选不出现在 deliverable 列表(复用 createKnowledgeWriteCandidate 造一条)
  - Test: paramsHash 去重 — 同 {code, productId, title, draft} 二次 createDeliverableDraftCandidate 返回**同一** confirmationToken,且 listPendingDeliverableDrafts() 仍只有一条;draft 不同则产生新 token
  - Test: MemoryKnowledgeRepo.upsertDoc 携带 sourceEventId 'corr-1' → 返回 doc.sourceEventId === 'corr-1';未传 → null
  </behavior>
  <action>
1. **migration 0005** — 新建 `src-tauri/migrations/0005_deliverable_source_event.sql`:
   ```sql
   -- src-tauri/migrations/0005_deliverable_source_event.sql
   -- Phase 16 (DELIV-03). Forward-only additive; no DROP / ALTER DROP ever in this directory.
   -- AI 溯源事件指针:落槽 doc 记录生成 turn 的 correlation_id,
   -- SELECT * FROM agent_events WHERE correlation_id = source_event_id 可重建完整生成回合。
   ALTER TABLE knowledge_docs ADD COLUMN source_event_id TEXT;
   ```
   在 `src-tauri/src/lib.rs` 的 `sql_migrations()` 中 0004 条目后追加(version 5, name "0005_deliverable_source_event", `include_str!("../migrations/0005_deliverable_source_event.sql")`, `MigrationKind::Up` — 与既有条目同构)。

2. **agentScope** — 新建 `src/ai/agentScope.ts`(约 15 行,同 setEventScopeProvider 先例的 ambient 模式):
   ```typescript
   // src/ai/agentScope.ts — ambient agent session scope. toolLoop 在每个 turn
   // 入口设置;tool(如 generateDeliverable)读取它做 AI 溯源(sessionId +
   // correlationId)。直接 executeTool(ChatPanel 确认后重放)时保持上一次
   // 的值 — 同一 ChatPanel 内即同一 session,语义正确。
   export interface AgentScope { sessionId: string; correlationId: string | null; }
   let current: AgentScope | null = null;
   export function setActiveAgentScope(scope: AgentScope | null): void { current = scope; }
   export function getActiveAgentScope(): AgentScope | null { return current; }
   ```

3. **confirmationStore.ts** — (a) `ConfirmationKind` 联合加 `'deliverable_draft'`(kind 是 TEXT 列,无迁移);(b) `ConfirmationStore` 接口加 `listRejected(kind: ConfirmationKind, limit?: number): Promise<PersistedConfirmation[]>`;(c) Memory 实现:遍历 rows,`row.kind === kind && row.status === 'rejected' && row.expiresAt > now`,按 rejectedAt 降序,`slice(0, limit ?? 5)`,返回 deepCopyRow;(d) Sqlite 实现:
   ```sql
   SELECT * FROM agent_confirmation_candidates
    WHERE kind = $1 AND status = 'rejected' AND expires_at > $2
    ORDER BY rejected_at DESC LIMIT $3
   ```
   (rejected 随行 TTL 24h 自然过期 — 防重复注入有界,不永久封禁未来生成)。

4. **confirmations.ts** — 追加 deliverable 候选 API(同文件 knowledge API 同构):
   - `export type DeliverableCode = 'prd';`
   - `DeliverableDraftCandidate` 接口见上方 interfaces 块(逐字段一致)。
   - `createDeliverableDraftCandidate(input)`: **先去重再插入**(CONTEXT 锁定决策:paramsHash 去重防重复候选;agent_confirmation_candidates.params_hash 无 UNIQUE 约束,去重必须在 API 层做):计算 `const params = { code: input.code, productId: input.productId, title: input.title, draft: input.draft };` + `const hash = await computeParamsHash(params);` 遍历 `await getConfirmationStore().listActive('deliverable_draft')` 找 `row.paramsHash === hash` — 命中则直接返回该行的 deliverableFromRow(同 token,不新增行);未命中才 `store.create({ kind: 'deliverable_draft', params, summary: input.title, sessionId: input.sessionId })`,返回 `{ code, productId, title, draft, sessionId: row.sessionId, eventId: input.eventId, confirmationToken: row.confirmationToken, createdAt: row.createdAt }`。
   - `deliverableFromRow(row: PersistedConfirmation)`: params 解构 + `sessionId: row.sessionId, eventId: (row.params as { eventId?: string | null }).eventId ?? null, createdAt: row.createdAt, confirmationToken: row.confirmationToken`。
   - `getDeliverableDraftCandidate(token)`: row 不存在 / `row.kind !== 'deliverable_draft'` / isRowAlive(row)(consumed/rejected/expired)→ undefined;否则 deliverableFromRow。
   - `confirmDeliverableDraft(token)`: `store.confirm(token)` + deliverableFromRow;ConfirmationStoreError 包装为 `DeliverableDraftConfirmationError`(新 error class,message 映射同 knowledgeErrorMessage 三态文案)。
   - `rejectDeliverableDraft(token)`: `store.reject(token)`。
   - `listPendingDeliverableDrafts()`: `store.listActive('deliverable_draft')` → map deliverableFromRow。
   - `listRejectedDeliverableDrafts(limit = 5)`: `store.listRejected('deliverable_draft', limit)` → map。
   - `consumeDeliverableDraftConfirmation(candidate)`: **关键设计** — 锁定决策「确认→编辑→落槽」使内容 hash 匹配不可能(draft 被用户编辑)。消费 hash 从 candidate 的**原始字段**重算,必然等于行内 paramsHash:`const hash = await computeParamsHash({ code: candidate.code, productId: candidate.productId, title: candidate.title, draft: candidate.draft });` 然后 `store.consume(candidate.confirmationToken, hash)`。防护来自:① store 原子条件 UPDATE(双并发恰一成功,Phase 14 不变量);② Task 2 的 tool 内 args↔candidate 身份校验。代码注释写明此差异与原因。

5. **knowledgeRepo.ts** — (a) `KnowledgeDocInput` 加 `sourceEventId?: string;`;(b) `KnowledgeDoc` 加 `sourceEventId: string | null;`;(c) MemoryKnowledgeRepo.upsertDoc 的 doc 字面量加 `sourceEventId: input.sourceEventId ?? null,`;(d) `KnowledgeDocRow` 加 `source_event_id: string | null;`,`mapRow` 加 `sourceEventId: row.source_event_id,`;(e) SqliteKnowledgeRepo.upsertDoc 的 INSERT 列表与 VALUES 加该列(`$13` 位置,参数 `input.sourceEventId ?? null`)。SELECT 用 `*` 自动带出。
   注意 repo 头注释声明「无事务」— 落槽的 doc+FTS 单语句序列性由既有 upsertDoc 语义承担,不新增事务代码。

6. **测试** `src/ai/__tests__/phase16DeliverableCandidates.test.ts` — behavior 块 5 条逐条落 test;`import '../index'` 不需要(无 tool);`resetMemoryConfirmationStore()` 每测试开头;knowledgeRepo 用 `resetMemoryKnowledgeRepo()`。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase16DeliverableCandidates.test.ts && npm run lint</automated>
  </verify>
  <done>新测试全绿且既有 96+ 测试不回归;grep 'deliverable_draft' 在 confirmationStore.ts/confirmations.ts 各出现 ≥2 次;0005 迁移已注册进 lib.rs。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateDeliverable tool + 落槽写路径 + 卡槽投影</name>
  <files>src/data/mockRndData.ts, src/stores/rndStore.ts, src/ai/tools/generateDeliverable.ts, src/ai/toolLoop.ts, src/ai/index.ts, src/ai/__tests__/phase16GenerateDeliverable.test.ts</files>
  <read_first>
  - src/ai/tools/proposeMemory.ts (全文 — 「登记候选不中断 turn」返回形状的参照)
  - src/ai/tools/knowledgeWrite.ts (全文 — 两段式 execute + resolveDraft + 写后校验模式)
  - src/ai/confirmations.ts (Task 1 之后的 deliverable API)
  - src/stores/rndStore.ts (deliverables 区段 :511-588、hydrateKnowledgeFromRepo :391-399、RndState 接口 Deliverables 区段 :128-135)
  - src/data/mockRndData.ts (FullLifecycleDeliverable :168-184、FULL_LIFECYCLE_DELIVERABLES_CATALOG :834+ 确认 'DEL-REQ-01' 是 PRD)
  - src/ai/toolLoop.ts (:107-111 runToolLoop 入口 correlationId 处、:17 side-effect import)
  - src/ai/index.ts (tool 注册 import 列表)
  - src/ai/__tests__/phase15ProposeMemory.test.ts (tool 测试模式:executeTool + useUIStore.setState)
  </read_first>
  <behavior>
  - Test: 未选产品(useUIStore.setState({ selectedProductId: null }))→ executeTool('generateDeliverable', {...}) 抛 Error,message === '请先选择一个产品,再生成 PRD。',且 listPendingDeliverableDrafts() 为空
  - Test: 选定 'p1' 首次调用 → result.candidateQueued === true;listPendingDeliverableDrafts() 长度 1;getCurrentDocs('p1') 无 category 'deliverable' 文档(未写库)
  - Test: confirm → 携带编辑后 draft('EDITED') 二次调用 → result.ok true、result.slotCode 'DEL-REQ-01'、ftsImmediateHit true;knowledgeRepo.getCurrentDocs('p1') 含该 doc(sourceType 'agent'、sourceSessionId 'test-session'、version 1)
  - Test: rndStore deliverables['p1'] 中 code 'DEL-REQ-01' → status 'ready'、content 含 'EDITED'、aiSource.sessionId 'test-session'、aiSource.eventId 'corr-16'
  - Test: 同一 token 二次消费 → 抛错(DeliverableDraftConfirmationError 或 already_settled)
  - Test: 再生成 + 再落槽(新候选)→ listVersions(docId) 长度 2,version 2 为 current,version 1 supersededAt 非 null
  - Test: code 'api' 传入 → ToolArgError(zod enum 拒绝)
  - Test: args.title 与 candidate.title 不一致的消费调用 → 抛 Error('确认的草稿与候选不一致,请重新生成。')
  </behavior>
  <action>
1. **mockRndData.ts** — `FullLifecycleDeliverable` 接口追加:
   ```typescript
   aiSource?: { sessionId: string; eventId: string; generatedAt: string };
   ```
   (18 目录条目与 buildInitialDeliverables 不动 — mock 无溯源。)

2. **rndStore.ts** —
   (a) RndState 接口 Deliverables 区段加:
   ```typescript
   commitDeliverableDraft: (productId: string, slotCode: string, content: string,
     aiSource: { sessionId: string; eventId: string; generatedAt: string }) => void;
   ```
   (b) 实现(放 syncDeliverableToDocs 前):
   ```typescript
   commitDeliverableDraft: (productId, slotCode, content, aiSource) => {
     const list = get().getDeliverablesForProduct(productId);
     if (!list.some((d) => d.code === slotCode)) {
       throw new Error(`[rndStore] deliverable slot not found: ${slotCode}`);
     }
     set((state) => ({
       deliverables: {
         ...state.deliverables,
         [productId]: state.deliverables[productId].map((d) => d.code === slotCode
           ? { ...d, content, status: 'ready' as const, generatedAt: aiSource.generatedAt, wordCount: `${content.length} 字`, aiSource }
           : d),
       },
     }));
   },
   ```
   (c) `hydrateKnowledgeFromRepo` 循环体内加跳过过滤 + 注释:
   ```typescript
   // Phase 16: 'deliverable' 类文档归属研发中心卡槽 + KnowledgeBaseView,
   // 不进知识文章投影(避免 ProductKnowledgeItem category 联合类型撒谎)。
   if (doc.category === 'deliverable') continue;
   ```

3. **generateDeliverable tool** — 新建 `src/ai/tools/generateDeliverable.ts`:
   ```typescript
   import { z } from 'zod';
   import { useUIStore } from '@/src/stores/uiStore';
   import { useProductStore } from '@/src/stores/productStore';
   import { useRndStore } from '@/src/stores/rndStore';
   import { registerTool } from '../registry';
   import { getKnowledgeRepo } from '../knowledgeRepo';
   import { getActiveAgentScope } from '../agentScope';
   import {
     confirmDeliverableDraft, consumeDeliverableDraftConfirmation,
     createDeliverableDraftCandidate, getDeliverableDraftCandidate,
     type DeliverableCode, type DeliverableDraftCandidate,
   } from '../confirmations';

   // 本期只验证 PRD 路径(DELIV-01 锁定决策);DELIV-05 推广时在此表加行即可。
   const SLOT_BY_CODE: Record<DeliverableCode, string> = { prd: 'DEL-REQ-01' };

   const generateDeliverableSchema = z.object({
     code: z.enum(['prd']),
     title: z.string().min(1),
     draft: z.string().min(1),
     confirmationToken: z.string().min(1).optional(),
   }).strict();
   ```
   execute 逻辑:
   - **落槽分支**(args.confirmationToken 存在):`const candidate = await getDeliverableDraftCandidate(args.confirmationToken)` → undefined 则 throw `Error('PRD 草稿候选已失效,请重新生成。')`;身份校验 `args.code !== candidate.code || args.title !== candidate.title` → throw `Error('确认的草稿与候选不一致,请重新生成。')`;`await confirmDeliverableDraft(token)`(幂等 confirm);`await consumeDeliverableDraftConfirmation(candidate)`(原子消费,Phase 14 不变量:绝不双写);然后写路径:
     ```typescript
     const slotCode = SLOT_BY_CODE[candidate.code];
     const scope = { sessionId: candidate.sessionId ?? '', eventId: candidate.eventId ?? '', generatedAt: new Date().toISOString() };
     const doc = await getKnowledgeRepo().upsertDoc({
       docId: `deliverable-${candidate.productId}-${slotCode}`,   // 稳定 docId:重复落槽 = 新版本 supersede
       productId: candidate.productId,
       title: candidate.title,
       category: 'deliverable',
       tags: ['prd'],                                            // CONTEXT 锁定:category 'deliverable' + tag 'prd'
       summary: args.draft.slice(0, 100),
       content: args.draft,                                      // 用户编辑后的最终稿
       author: 'AI 助手',
       sourceType: 'agent',
       sourceSessionId: candidate.sessionId,
       sourceEventId: candidate.eventId,
     });
     useRndStore.getState().commitDeliverableDraft(candidate.productId, slotCode, args.draft, scope);
     // DELIV-04 可审计的「索引立即可检索」:落槽后立即查一次,命中数进返回值
     // (Plan 02 把它写进 deliverable_committed 事件 payload)。
     const hits = await getKnowledgeRepo().search(candidate.title, { productId: candidate.productId, limit: 50 });
     const ftsHitCount = hits.filter((h) => h.docId === doc.docId).length;
     return { ok: true, docId: doc.docId, version: doc.version, slotCode, ftsImmediateHit: ftsHitCount > 0, ftsHitCount, aiSource: scope };
     ```
   - **登记分支**(无 token):`const productId = useUIStore.getState().selectedProductId`;`if (!productId) throw new Error('请先选择一个产品,再生成 PRD。');`(UI-SPEC 文案逐字);校验产品存在(`useProductStore.getState().products.some(p => p.id === productId)`,不存在同文案报错);`const scope = getActiveAgentScope()`;`const candidate = await createDeliverableDraftCandidate({ productId, code: args.code, title: args.title, draft: args.draft, sessionId: scope?.sessionId ?? null, eventId: scope?.correlationId ?? null })`;返回 `{ ok: true, candidateQueued: true, confirmationToken: candidate.confirmationToken, code: args.code, title: args.title, note: 'PRD 草稿已进入待确认队列;用户将在对话面板中确认并编辑,不要重复生成同一份草稿。' }`。**绝不抛 ConfirmationRequiredError**(proposeMemory 红线同款:中断 turn 会杀对话)。
   - registerTool name `'generateDeliverable'`,description 写明:「Generate a deliverable draft (currently PRD only) for the currently selected product. You produce the full draft content yourself in the `draft` parameter. The first call only queues a candidate for user confirmation — the user will review and edit it in the chat panel; do not call again for the same deliverable.」
   - 文件底部 `import { generateDeliverableSchema }` 如 knowledgeWrite 惯例导出 schema。

4. **toolLoop.ts** — import `setActiveAgentScope` from './agentScope';在 `session.setCorrelationId(correlationId);`(:111)后一行加:
   ```typescript
   setActiveAgentScope({ sessionId: session.sessionId, correlationId });
   ```

5. **index.ts** — import 列表加 `import './tools/generateDeliverable';`

6. **测试** `src/ai/__tests__/phase16GenerateDeliverable.test.ts` — behavior 块 9 条逐条落 test。测试骨架要点:`import '../index';` + `import { executeTool, ToolArgError } from '../registry';`;每 test 前 `useUIStore.setState({ selectedProductId: 'p1' })`(afterEach 恢复 null)、`setActiveAgentScope({ sessionId: 'test-session', correlationId: 'corr-16' })`、`resetMemoryConfirmationStore(); resetMemoryKnowledgeRepo();`;rndStore 复位用 `useRndStore.setState({ deliverables: { p1: buildInitialDeliverables({ id: 'p1', name: 'P1' } as any) } })` 或直接读默认 mock 态后断言差异;调用 helper:
   ```typescript
   async function callGen(args: Record<string, unknown>) { return await executeTool('generateDeliverable', args); }
   ```
   落槽序列:callGen(无 token)→ confirmDeliverableDraft(token) → callGen({ code:'prd', title, draft:'EDITED…', confirmationToken: token })。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase16GenerateDeliverable.test.ts && npm test</automated>
  </verify>
  <done>新测试全绿、npm test 全量绿(96+ 旧测试零回归);grep "generateDeliverable" 命中 toolLoop 无(agentScope 已接)、index.ts 1 次、tool 文件 ≥5 次;rndStore 含 commitDeliverableDraft。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: contextAssembler 防重复注入段(MEM-02 同构)</name>
  <files>src/ai/contextAssembler.ts, src/ai/__tests__/phase16ContextAssembler.test.ts</files>
  <read_first>
  - src/ai/contextAssembler.ts (全文 — segment 2「pending」区段 :91-104 是唯一改动点)
  - src/ai/confirmations.ts (Task 1 之后的 listPendingDeliverableDrafts / listRejectedDeliverableDrafts)
  - src/ai/__tests__/phase15ContextAssembler.test.ts (既有断言风格 — 改动必须保持这些测试原样通过)
  </read_first>
  <behavior>
  - Test: 无 deliverable 候选时 assembled.coreContext 不含「交付物草稿」(既有输出零变化)
  - Test: 一条 pending PRD 候选 → coreContext 含 `- （待用户确认）PRD 草稿《<title>》`
  - Test: 一条 rejected PRD 候选 → coreContext 含 `不要再生成以下交付物草稿（用户已忽略）: PRD《<title>》`,且该候选不在待确认行
  - Test: audit segments 中 name 'pending' 的 items = memory pending 数 + deliverable pending 数
  </behavior>
  <action>
1. import 顶部加:
   ```typescript
   import { listPendingDeliverableDrafts, listRejectedDeliverableDrafts } from './confirmations';
   ```
2. segment 2(:92)的 Promise.all 扩为四路:
   ```typescript
   const [pending, rejected, deliverablePending, deliverableRejected] = await Promise.all([
     store.listPending(), store.listRejected(REJECTED_LIMIT),
     listPendingDeliverableDrafts(), listRejectedDeliverableDrafts(REJECTED_LIMIT),
   ]);
   ```
   deliverable 查询失败不得阻塞注入 — 若担心 store 异常,包 try/catch 退化为 `[[], []]`(与 fts_topk 段 error 降级同哲学);memoryStore 两路维持原样不动。
3. pendingLines 组装(顺序:拒绝清单先行,同 MEM-02):
   ```typescript
   if (deliverableRejected.length > 0) {
     pendingLines.push(`不要再生成以下交付物草稿（用户已忽略）: ${deliverableRejected.map((c) => `${c.code.toUpperCase()}《${c.title}》`).join('；')}`);
   }
   if (deliverablePending.length > 0) {
     pendingLines.push(...deliverablePending.map((c) => `- （待用户确认）${c.code.toUpperCase()} 草稿《${c.title}》`));
   }
   ```
   (插在既有 rejected 记忆行之后、pending 记忆行之前;段标题与 clamp 逻辑不动。)
4. audit items 改为 `items: pending.length + deliverablePending.length`(deliverable 空时与旧值恒等 — 既有 phase15 测试零影响)。
5. 新测试文件按 behavior 4 条;用 createDeliverableDraftCandidate + confirmDeliverableDraft/rejectDeliverableDraft 造状态,`resetMemoryConfirmationStore()` + `resetMemoryMemoryStore()` 复位;assembleInjectedContext 直接调用(参照 phase15 测试的 opts 形状)。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase16ContextAssembler.test.ts src/ai/__tests__/phase15ContextAssembler.test.ts && npm run lint</automated>
  </verify>
  <done>新 4 测试绿 + phase15 既有 assembler 测试原样绿;grep 'listRejectedDeliverableDrafts' 命中 contextAssembler.ts 1 次。</done>
</task>

</tasks>

<verification>
1. `npm test` — 全量绿(96+ 既有 + 21 新 ≈ 117+)。
2. `npm run lint` — tsc --noEmit 零错误。
3. `grep -c "deliverable_draft" src/ai/confirmationStore.ts src/ai/confirmations.ts` — 各 ≥2。
4. `ls src-tauri/migrations/` 含 0005;`grep -n "0005" src-tauri/src/lib.rs` 命中。
5. 抽查心智模型:generateDeliverable 首调不写库、不中断 turn;消费在写入之前(Phase 14 不变量);FTS 命中数可审计。
</verification>

<success_criteria>
- DELIV-01: tool 存在、携带当前产品上下文、未选产品有明确报错(自动化覆盖)
- DELIV-03(写侧): 落槽 doc sourceType 'agent' + sessionId + eventId(correlation_id)+ generatedAt;卡槽 aiSource 齐
- DELIV-04: upsertDoc 单一写 API 复用,落槽后 search 立即命中(ftsImmediateHit 断言)
- DELIV-02(后端半边): 确认→编辑→落槽 原子消费链 + 编辑后 draft 为最终落槽内容(测试断言 'EDITED')
</success_criteria>

<output>
After completion, create `.planning/phases/16-prd/16-01-SUMMARY.md`
</output>
