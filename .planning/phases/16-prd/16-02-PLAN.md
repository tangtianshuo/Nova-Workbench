---
phase: 16-prd
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/PrdDraftDialog.tsx
  - src/components/ChatPanel.tsx
autonomous: true
requirements: [DELIV-02]

must_haves:
  truths:
    - "用户让 agent 生成 PRD 后,ChatPanel 出现「待确认的 PRD 草稿」卡片(产品名 + line-clamp-3 预览 + 来源时间)"
    - "点「确认并编辑」打开预填 MDXEditor Dialog;取消/关闭不丢候选,重新打开恢复原始草稿"
    - "点「落槽至研发中心」后 PRD(编辑后内容)落入研发中心卡槽,卡片消失,出现成功 toast 与助手消息"
    - "点「忽略」静默拒绝候选,卡片消失,拒绝进入系统提示防重复段"
    - "落槽成功后 agent_events 出现 deliverable_committed 事件(payload 含 ftsHitCount)"
    - "重启后未处理的 PRD 候选卡片恢复(confirmationStore 持久化)"
  artifacts:
    - path: "src/components/PrdDraftDialog.tsx"
      provides: "PRD 编辑 Dialog(MDXEditor 预填 + 落槽/取消)"
      min_lines: 40
    - path: "src/components/ChatPanel.tsx"
      provides: "Surface 1 确认卡片 + refresh/restore/reject/commit 全部接线"
      contains: "pendingPrdDraft"
  key_links:
    - from: "src/components/ChatPanel.tsx"
      to: "src/ai/confirmations.ts"
      via: "listPendingDeliverableDrafts / confirmDeliverableDraft / rejectDeliverableDraft"
      pattern: "listPendingDeliverableDrafts"
    - from: "src/components/ChatPanel.tsx"
      to: "src/ai/registry.ts"
      via: "落槽 = executeTool('generateDeliverable', { code, title, draft: 编辑稿, confirmationToken })"
      pattern: "executeTool\\('generateDeliverable'"
    - from: "src/components/ChatPanel.tsx"
      to: "src/ai/chatSession.ts"
      via: "落槽成功 appendAuxEvent('deliverable_committed', payload) + flushEvents"
      pattern: "deliverable_committed"
    - from: "src/components/ChatPanel.tsx onToolEnd"
      to: "refreshPrdCard"
      via: "name === 'generateDeliverable' 时刷新卡片(与 refreshMemoryCards 同构)"
      pattern: "name === 'generateDeliverable'"
---

<objective>
ChatPanel HITL 全流程 UI:PRD 草稿确认卡片(Surface 1)+ MDXEditor 编辑 Dialog(Surface 2)+ 落槽消费链与 deliverable_committed 审计事件。

Purpose: DELIV-02 的「HITL 确认 → MDXEditor 编辑 → 落槽」人机闭环 — 全程无手动复制粘贴。
Output: PrdDraftDialog 组件、ChatPanel 卡片与全部接线、事件审计。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-prd/16-CONTEXT.md
@.planning/phases/16-prd/16-UI-SPEC.md
@.planning/phases/16-prd/16-01-SUMMARY.md

<interfaces>
<!-- 来自 16-01 的契约(已存在,直接消费) -->

```typescript
// src/ai/confirmations.ts
export type DeliverableCode = 'prd';
export interface DeliverableDraftCandidate {
  confirmationToken: string; productId: string; code: DeliverableCode;
  title: string; draft: string; sessionId: string | null; eventId: string | null; createdAt: string;
}
export function listPendingDeliverableDrafts(): Promise<DeliverableDraftCandidate[]>;
export function confirmDeliverableDraft(token: string): Promise<DeliverableDraftCandidate>;
export function rejectDeliverableDraft(token: string): Promise<boolean>;

// 落槽调用(ChatPanel 直接 executeTool,与 handleConfirmKnowledgeWrite 同形):
await executeTool('generateDeliverable', {
  code: candidate.code,            // 'prd'
  title: candidate.title,          // 必须与候选一致(tool 身份校验)
  draft: editedDraft,              // 用户编辑后的最终稿
  confirmationToken: candidate.confirmationToken,
});
// 返回:
{ ok: true; docId: string; version: number; slotCode: 'DEL-REQ-01';
  ftsImmediateHit: boolean; ftsHitCount: number;
  aiSource: { sessionId: string; eventId: string; generatedAt: string } }
```

<!-- 既有 UI 契约 -->

```typescript
// src/components/ui/MarkdownEditor.tsx
<MarkdownEditor value={string} onChange={(v: string) => void} minHeight="320px" />

// src/components/ui/Dialog.tsx — Dialog/DialogContent/DialogHeader(title, description)/DialogFooter
// src/components/ui/Button.tsx — variant primary/secondary, size sm, loading 属性使按钮禁用+spinner
// src/components/ui/Tooltip.tsx — 略(Plan 03 用)

// src/ai/chatSession.ts (公开方法)
session.appendAuxEvent(eventType: string, payload: Record<string, unknown>): void;
session.flushEvents(): Promise<void>;

// src/stores/productStore.ts — useProductStore((s) => s.products): Product[] (有 .name, .id)
// ChatPanel 现有同构参照:
//   refreshMemoryCards :114-130(refresh 模式 + 错误 toast 文案)
//   记忆卡片 JSX :483-493(Surface 1 的克隆母版)
//   handleConfirmKnowledgeWrite :309-341(confirm→executeTool→消息→清理 的消费链母版)
//   restore effect :136-169(重启恢复候选)
//   onToolEnd 'proposeMemory' 刷新 :224
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrdDraftDialog 组件(MDXEditor 编辑 Dialog)</name>
  <files>src/components/PrdDraftDialog.tsx</files>
  <read_first>
  - .planning/phases/16-prd/16-UI-SPEC.md (Surface 2 结构与交互契约 — JSX 逐字母版)
  - src/components/ui/MarkdownEditor.tsx (props: value/onChange/minHeight)
  - src/components/ui/Dialog.tsx (Dialog/DialogContent/DialogHeader/DialogFooter 组合)
  - src/components/ui/Button.tsx (loading/disabled 行为)
  - src/views/KnowledgeBaseView.tsx:400-430 (Dialog + MarkdownEditor 既有组合用法参照)
  </read_first>
  <action>
新建 `src/components/PrdDraftDialog.tsx`,具名导出 `PrdDraftDialog`,props:
```typescript
interface PrdDraftDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  title: string;            // 候选标题;空串由调用方兜底为 'PRD 草稿'
  description: string;      // `{产品名} · 编辑后落槽至研发中心,并同步知识库索引`
  initialDraft: string;     // 候选原始草稿(唯一真相,直到落槽)
  busy: boolean;            // 落槽进行中
  onCommit: (editedDraft: string) => void;
}
```
结构(UI-SPEC Surface 2 逐字):
```tsx
<Dialog open={open} onOpenChange={onOpenChange}>
  <DialogContent className="max-w-3xl">
    <DialogHeader title={title} description={description} />
    <MarkdownEditor value={editedDraft} onChange={setEditedDraft} minHeight="320px" />
    <DialogFooter>
      <Button variant="secondary" onClick={() => onOpenChange(false)} disabled={busy}>取消</Button>
      <Button variant="primary" onClick={() => onCommit(editedDraft)} loading={busy}>落槽至研发中心</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```
行为契约:
- 本地 `const [editedDraft, setEditedDraft] = useState(initialDraft);` + `useEffect(() => { if (open) setEditedDraft(initialDraft); }, [open, initialDraft]);` — **每次打开重置为原始草稿**:取消的编辑绝不持久(UI-SPEC:候选拿到落槽前是唯一真相)。
- 取消/X/onOpenChange(false) 只关 Dialog,不动候选。
- busy 时两按钮禁用(loading 已含 disabled)。
- 纯展示组件:不 import 任何 ai/store 模块。
  </action>
  <verify>
    <automated>npm run lint && grep -c "MarkdownEditor" src/components/PrdDraftDialog.tsx</automated>
  </verify>
  <done>文件存在且 lint 通过;grep 'initialDraft' 命中 ≥3(props/useState/useEffect);无 ai/store import。</done>
</task>

<task type="auto">
  <name>Task 2: ChatPanel 接线 — 卡片 / 刷新 / 拒绝 / 落槽消费链 / 审计事件 / 恢复</name>
  <files>src/components/ChatPanel.tsx</files>
  <read_first>
  - .planning/phases/16-prd/16-UI-SPEC.md (Surface 1 卡片 JSX 逐字母版 + 交互契约 + Copywriting Contract 全表)
  - src/components/ChatPanel.tsx (全文 — 改动点:imports、state、refreshPrdCard、restore effect、onToolEnd、scroll deps、卡片 JSX 插入点、Dialog 渲染)
  - src/components/PrdDraftDialog.tsx (Task 1 产物,props 契约)
  - src/ai/confirmations.ts (16-01 deliverable API 段)
  - src/ai/chatSession.ts (:159-201 appendAuxEvent/flushEvents 签名)
  </read_first>
  <action>
1. **imports** — 从 `'@/src/ai/confirmations'` 加 `confirmDeliverableDraft, listPendingDeliverableDrafts, rejectDeliverableDraft, type DeliverableDraftCandidate`(executeTool 已在既有 `'@/src/ai'` import 中);加 `import { PrdDraftDialog } from '@/src/components/PrdDraftDialog';` 与 `import { useProductStore } from '@/src/stores/productStore';`。

2. **state**(pendingMemory 组旁):
   ```tsx
   const [pendingPrdDraft, setPendingPrdDraft] = useState<DeliverableDraftCandidate | null>(null);
   const [prdBusy, setPrdBusy] = useState(false);
   const [prdDialogOpen, setPrdDialogOpen] = useState(false);
   const products = useProductStore((s) => s.products);
   ```

3. **refreshPrdCard**(refreshMemoryCards 正下方,同构):
   ```tsx
   const refreshPrdCard = async () => {
     try {
       const pending = await listPendingDeliverableDrafts();
       setPendingPrdDraft(pending[0] ?? null);
     } catch (error) {
       console.error('[prd-card] refresh failed', error);
       toast({ type: 'error', title: '检索失败,请稍后重试;若持续失败请重启应用。' });
     }
   };
   ```
   (错误文案 = UI-SPEC 指定的 Phase 15 复用文案,逐字。)

4. **restore effect** — 在 `void refreshMemoryCards();` 行后加 `void refreshPrdCard();`(重启后卡片恢复,confirmationStore 持久化路径)。

5. **onToolEnd** — 在 `if (name === 'proposeMemory') void refreshMemoryCards();` 后加:
   ```tsx
   if (name === 'generateDeliverable') void refreshPrdCard();
   ```

6. **scroll effect deps** — 依赖数组追加 `pendingPrdDraft`。

7. **Surface 1 卡片 JSX** — 插入位置:`pendingDestructiveAction` 块之后、`pendingMemory` 块之前(UI-SPEC 指定)。JSX 逐字:
   ```tsx
   {pendingPrdDraft && (
     <div className="rounded-[var(--radius-lg)] border border-accent/30 bg-accent-subtle px-3.5 py-3 text-sm text-text-primary">
       <div className="font-medium">待确认的 PRD 草稿</div>
       <div className="mt-1 text-xs text-text-secondary">产品: {products.find((p) => p.id === pendingPrdDraft.productId)?.name ?? pendingPrdDraft.productId}</div>
       <div className="mt-1 text-xs text-text-secondary line-clamp-3 whitespace-pre-wrap">{pendingPrdDraft.draft}</div>
       <div className="mt-1 text-xs text-text-tertiary">来源: 本次对话 · {formatMemoryTime(pendingPrdDraft.createdAt)}</div>
       <div className="mt-2 flex gap-2">
         <Button type="button" variant="primary" size="sm" onClick={() => setPrdDialogOpen(true)} disabled={prdBusy}>确认并编辑</Button>
         <Button type="button" variant="secondary" size="sm" onClick={() => void rejectDraft()} disabled={prdBusy}>忽略</Button>
       </div>
     </div>
   )}
   ```
   (卡片保持显示直到落槽成功或忽略 — Dialog 取消绝不移除卡片。)

8. **rejectDraft**(静默,MEM-02 同款):
   ```tsx
   const rejectDraft = async () => {
     if (!pendingPrdDraft || prdBusy) return;
     setPrdBusy(true);
     try {
       await rejectDeliverableDraft(pendingPrdDraft.confirmationToken);
       await refreshPrdCard();
     } catch (error) {
       console.error('[prd-card] reject failed', error);
     } finally {
       setPrdBusy(false);
     }
   };
   ```
   (无 toast、无助手消息 — UI-SPEC: 忽略 silent。)

9. **commitToSlot**(落槽消费链,handleConfirmKnowledgeWrite 母版):
   ```tsx
   const commitToSlot = async (editedDraft: string) => {
     if (!pendingPrdDraft || prdBusy) return;
     setPrdBusy(true);
     try {
       await confirmDeliverableDraft(pendingPrdDraft.confirmationToken);
       const result = await executeTool('generateDeliverable', {
         code: pendingPrdDraft.code,
         title: pendingPrdDraft.title,
         draft: editedDraft,
         confirmationToken: pendingPrdDraft.confirmationToken,
       }) as { docId: string; version: number; slotCode: string; ftsImmediateHit: boolean; ftsHitCount: number; aiSource: { sessionId: string; eventId: string; generatedAt: string } };
       // DELIV-04 可审计:落槽事件 payload 记录 FTS 命中数(CONTEXT 锁定)。
       sessionRef.current.appendAuxEvent('deliverable_committed', {
         docId: result.docId, version: result.version, slotCode: result.slotCode, code: 'prd',
         ftsImmediateHit: result.ftsImmediateHit, ftsHitCount: result.ftsHitCount,
         sessionId: result.aiSource.sessionId, eventId: result.aiSource.eventId,
       });
       await sessionRef.current.flushEvents();
       toast({ type: 'success', title: 'PRD 已落槽' });
       setMessages((current) => [...current, {
         id: nextIdRef.current++, role: 'assistant',
         content: 'PRD 已落槽至研发中心,知识库立即可检索。',
       }]);
       setPrdDialogOpen(false);
       await refreshPrdCard();
     } catch (error) {
       // ponytail: 消费先于写入(Phase 14 不变量:绝不双写)。若消费成功但写入失败,
       // 候选已耗尽 — refreshPrdCard 会移除卡片,Dialog 留开供用户复制编辑稿。
       toast({ type: 'error', title: '落槽失败,请稍后重试;草稿仍保留在对话中。', description: error instanceof Error ? error.message : undefined });
       await refreshPrdCard();
     } finally {
       setPrdBusy(false);
     }
   };
   ```
   (文案逐字取自 UI-SPEC Copywriting Contract。)

10. **Dialog 渲染**(卡片 JSX 之后):
    ```tsx
    {pendingPrdDraft && (
      <PrdDraftDialog
        open={prdDialogOpen}
        onOpenChange={setPrdDialogOpen}
        title={pendingPrdDraft.title || 'PRD 草稿'}
        description={`${products.find((p) => p.id === pendingPrdDraft.productId)?.name ?? ''} · 编辑后落槽至研发中心,并同步知识库索引`}
        initialDraft={pendingPrdDraft.draft}
        busy={prdBusy}
        onCommit={(draft) => void commitToSlot(draft)}
      />
    )}
    ```
    (pendingPrdDraft 为 null 时 Dialog 随卡片一起卸载 — Dialog 关闭态不需要孤儿渲染。)
  </action>
  <verify>
    <automated>npm run lint && grep -c "pendingPrdDraft" src/components/ChatPanel.tsx</automated>
  </verify>
  <done>lint 零错误;grep 'pendingPrdDraft' ≥10、'deliverable_committed' 1 次、'generateDeliverable' 2 次(onToolEnd + executeTool)、'PRD 已落槽' 2 次(toast+消息);卡片 JSX 含 'line-clamp-3 whitespace-pre-wrap' 与 '待确认的 PRD 草稿'。</done>
</task>

</tasks>

<verification>
1. `npm run lint` 零错误;`npm test` 全量绿(16-01 测试不受 UI 改动影响)。
2. UI-SPEC Surface 1/2 逐项对照:卡片类名/文案/按钮变体/尺寸、Dialog max-w-3xl、编辑器 minHeight 320px、按钮 loading 态。
3. 消费链顺序正确:confirm → executeTool(内部 consume → upsertDoc → commitDeliverableDraft → FTS 查询)→ appendAuxEvent → flushEvents → toast/消息/关 Dialog/refresh。
4. 取消语义:Dialog onOpenChange(false) 不触碰候选;重开恢复原始草稿(initialDraft effect)。
</verification>

<success_criteria>
- DELIV-02: 确认→编辑→落槽全链路 UI 就绪(可视化验收在 16-03 UAT checkpoint 执行)
- 重启恢复:restore effect 调 refreshPrdCard,候选卡片随 pendingKnowledgeWrites 同路径恢复
- 审计:每次成功落槽产生 deliverable_committed 事件(payload 含 docId/version/ftsHitCount/sessionId/eventId)
</success_criteria>

<output>
After completion, create `.planning/phases/16-prd/16-02-SUMMARY.md`
</output>
