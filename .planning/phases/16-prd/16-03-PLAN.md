---
phase: 16-prd
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - src/components/product/FullDeliverablesTab.tsx
autonomous: false
requirements: [DELIV-03]

must_haves:
  truths:
    - "研发中心 DEL-REQ-01 卡片在 aiSource 存在时显示 AI 徽章(Sparkle + AI),悬停 tooltip 显示「AI 生成 · {yyyy-MM-dd HH:mm} · 会话 {前8位}」"
    - "mock/手动交付物卡片不渲染 AI 徽章"
    - "用户可端到端走通:对话生成 PRD → 卡片确认 → MDXEditor 编辑 → 落槽 → 研发中心徽章 → 知识库立即检索命中(UAT)"
  artifacts:
    - path: "src/components/product/FullDeliverablesTab.tsx"
      provides: "Surface 3 AI 溯源徽章 + tooltip"
      contains: "d.aiSource"
  key_links:
    - from: "src/components/product/FullDeliverablesTab.tsx"
      to: "FullLifecycleDeliverable.aiSource"
      via: "条件渲染 {d.aiSource && (...)}"
      pattern: "d\\.aiSource"
    - from: "src/components/product/FullDeliverablesTab.tsx"
      to: "src/components/ui/Tooltip.tsx"
      via: "Tooltip content 悬停溯源"
      pattern: "import \\{ Tooltip \\}"
---

<objective>
研发中心卡槽 AI 溯源徽章(Surface 3,DELIV-03 展示侧)+ Phase 16 全流程 UAT 人工验收。

Purpose: 让「这份 PRD 是 AI 在哪次会话生成的」在研发中心一眼可见;并以人工验收闭合整个 phase 的成功标准。
Output: FullDeliverablesTab 徽章 + UAT 通过记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-prd/16-CONTEXT.md
@.planning/phases/16-prd/16-UI-SPEC.md
@.planning/phases/16-prd/16-01-SUMMARY.md
@.planning/phases/16-prd/16-02-SUMMARY.md

<interfaces>
```typescript
// src/data/mockRndData.ts (16-01 已扩展)
export interface FullLifecycleDeliverable {
  /* 既有字段 */
  aiSource?: { sessionId: string; eventId: string; generatedAt: string; docId: string; version: number };
  // generatedAt ISO;docId/version = knowledge_docs 真相源显式指针(16-01,CONTEXT「卡槽只存指针」)
}

// src/components/ui/Tooltip.tsx
export function Tooltip({ content, children, side = 'top', align = 'center' }: {
  content: ReactNode; children: ReactNode; side?: 'top' | 'right' | 'bottom' | 'left';
  align?: 'start' | 'center' | 'end';
}): JSX.Element;  // children 经 Radix Trigger asChild — Badge 是 forwardRef,可直接作 child

// FullDeliverablesTab 徽章行锚点(:278-286):
//   <div className="flex items-center gap-2">
//     <Badge variant="neutral" ...>{d.code}</Badge>
//     <Badge variant="accent">{d.phaseName}</Badge>   ← 徽章插此后
//   </div>
// Sparkle 已在 :11 导入;Tooltip 需新导入。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FullDeliverablesTab AI 溯源徽章</name>
  <files>src/components/product/FullDeliverablesTab.tsx</files>
  <read_first>
  - .planning/phases/16-prd/16-UI-SPEC.md (Surface 3 结构与交互契约 — JSX 逐字母版 + AI badge tooltip 文案)
  - src/components/product/FullDeliverablesTab.tsx (:1-60 imports 与组件头部、:276-290 徽章行锚点、:343+ 预览 Modal — 本期不改 Modal)
  - src/components/ui/Tooltip.tsx (props 签名)
  - src/components/ui/Badge.tsx (variant 与 forwardRef)
  </read_first>
  <action>
1. import 区加 `import { Tooltip } from '@/src/components/ui/Tooltip';`。

2. 组件文件内加时间格式化 helper(模块级,getStatusBadge 旁):
   ```tsx
   function formatAiSourceTime(iso: string): string {
     const d = new Date(iso);
     const pad = (n: number) => String(n).padStart(2, '0');
     return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
   }
   ```

3. 徽章行:在 `<Badge variant="accent">{d.phaseName}</Badge>` 之后插入(UI-SPEC Surface 3 逐字,仅时间格式化调 helper):
   ```tsx
   {d.aiSource && (
     <Tooltip content={`AI 生成 · ${formatAiSourceTime(d.aiSource.generatedAt)} · 会话 ${d.aiSource.sessionId.slice(0, 8)}`}>
       <Badge variant="accent">
         <Sparkle size={12} weight="fill" />
         AI
       </Badge>
     </Tooltip>
   )}
   ```
   交互约束:纯悬停展示,无点击跳转;deliverable 预览 Dialog(:344+)与 getStatusBadge 不改;mock 交付物(aiSource undefined)渲染无徽章。
  </action>
  <verify>
    <automated>npm run lint && grep -c "d.aiSource" src/components/product/FullDeliverablesTab.tsx</automated>
  </verify>
  <done>lint 零错误;grep 'd.aiSource' ≥1、'Sparkle size={12} weight="fill"' 1 次、"import { Tooltip }" 1 次;预览 Modal(:344 区段)零 diff。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 16 全流程 UAT 人工验收(DELIV-01..04)</name>
  <action>人工验收 <what-built> 所述全链路,按 <how-to-verify> 8 步执行并记录结果;任一步失败按现象回传,不得跳过。</action>
  <what-built>Phase 16 全链路:generateDeliverable tool(候选登记 → 原子消费 → knowledgeRepo 版本链落槽 + FTS5 同步 → rndStore 卡槽 + aiSource 溯源)、ChatPanel 确认卡片 + MDXEditor 编辑 Dialog、研发中心 AI 徽章、知识库立即检索。</what-built>
  <how-to-verify>
前置:`npm run tauri:dev`(SQLite 真路径;若 web 模式 `npm run dev` 验证流程亦通,但持久化为内存镜像,步骤 7 重启验证须用 tauri:dev)。

1. **生成**:侧栏选中任一产品 → 打开 AI 助手 ChatPanel → 输入「帮我为当前产品生成一份 PRD 草稿」→ 期待:模型调用 generateDeliverable 后出现「待确认的 PRD 草稿」卡片(产品名 + 3 行预览 + 来源时间),对话不中断。
2. **取消无损**:点「确认并编辑」→ Dialog 打开预填草稿 → 随意改动文字 → 点「取消」→ 卡片仍在;再次「确认并编辑」→ 内容为**原始**草稿(编辑未持久)。
3. **落槽**:编辑若干内容 → 点「落槽至研发中心」→ 期待:toast「PRD 已落槽」、Dialog 关闭、卡片消失、追加助手消息「PRD 已落槽至研发中心,知识库立即可检索。」。
4. **溯源徽章**:研发中心 → 产品详情 → 全生命周期交付物 → 找到 DEL-REQ-01 卡片 → 期待:阶段徽章后出现 AI 徽章(Sparkle+AI),悬停显示「AI 生成 · yyyy-MM-dd HH:mm · 会话 <8位>」;相邻 mock 卡片无徽章。
5. **立即检索**:知识库 → 搜索 PRD 标题关键词(如标题里的 2 字词)→ 期待:**搜索结果**命中该 PRD(来源标注 AI/agent),无等待窗口(搜索直连 knowledgeRepo FTS5,不经 store 投影)。注意:deliverable 类文档**不进**知识库浏览列表与分类侧栏 — 它归属研发中心卡槽,16-01 Task 2c 在 hydrateKnowledgeFromRepo 投影中跳过该类(侧栏 categories 只由该投影派生)。侧栏**不**出现 deliverable 分类是预期行为,不是缺陷;UI-SPEC Surface 4 的「appears automatically」按此口径理解为仅 FTS5 搜索命中。
6. **版本链**:回到 ChatPanel 再生成一份(内容改动)→ 确认编辑落槽 → 知识库再搜 → 命中的是新版内容;研发中心 DEL-REQ-01 内容更新、徽章仍在。
7. **重启恢复**(tauri:dev):生成一份新草稿但**不**确认 → 关闭应用 → 重启 → ChatPanel 打开 → 期待:「待确认的 PRD 草稿」卡片恢复(confirmationStore 持久化);确认落槽一次后重启,卡片不再出现(原子消费)。
8. **审计(可选)**:查 nova.db agent_events — 每次成功落槽有一条 deliverable_committed 事件,payload 含 docId/version/ftsHitCount(session_id = 卡片 tooltip 里的会话)。
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述未通过的步骤与现象</resume-signal>
</task>

</tasks>

<verification>
1. `npm run lint` 零错误、`npm test` 全量绿(回归确认)。
2. UAT 8 步(步骤 8 可选)全部通过或明确记录偏差。
3. 成功标准对照 ROADMAP Phase 16:① 携带产品上下文的 PRD 草稿 ② HITL 确认→编辑→落槽无复制粘贴 ③ AI 溯源标记可见 ④ 落槽即检索命中(FTS5 搜索命中;deliverable 不进浏览列表/侧栏,归属研发中心卡槽)。
</verification>

<success_criteria>
- DELIV-03 展示侧:AI 徽章仅出现在 agent 落槽的卡槽,tooltip 含生成时间与会话 ID
- Phase 16 四条成功标准全部经人工验收为 TRUE
- UAT 结果记录进 16-03-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/16-prd/16-03-SUMMARY.md`
</output>
