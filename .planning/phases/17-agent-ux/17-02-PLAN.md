---
phase: 17-agent-ux
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/stores/uiStore.ts
  - src/ai/context.ts
  - src/hooks/useCmdK.ts
  - src/components/TaskKanban.tsx
  - src/components/AgentConsole.tsx
  - src/ai/__tests__/phase17ContextCarry.test.ts
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "在产品/任务/日程视图按裸 ⌘K/Ctrl+K 唤起 agent，携带当前选中产品/任务（或当前列表过滤器——视图模式+活动分类名，如「任务 · 设计」）/今日日程上下文"
    - "输入区上方显示「已携带: {名称}」chip，点 × 移除后下一次发送不再携带该项"
    - "携带项进入 buildCoreContext 业务事实段（同一 core segment，同一 600-token clamp），无第二组装路径"
  artifacts:
    - path: "src/ai/__tests__/phase17ContextCarry.test.ts"
      provides: "carry 注入与 refreshAgentCarry 的行为测试"
      contains: "buildCoreContext"
    - path: "src/stores/uiStore.ts"
      provides: "transient agentContextCarry 状态 + taskKanbanView/taskKanbanCategory 提升"
      contains: "agentContextCarry"
    - path: "src/ai/context.ts"
      provides: "refreshAgentCarry + buildCoreContext carry 段"
      contains: "refreshAgentCarry"
  key_links:
    - from: "src/hooks/useCmdK.ts"
      to: "src/ai/context.ts"
      via: "⌘K 分支调用 refreshAgentCarry() 再开面板"
      pattern: "refreshAgentCarry"
    - from: "src/ai/context.ts"
      to: "src/stores/uiStore.ts"
      via: "buildCoreContext 读取 agentContextCarry"
      pattern: "agentContextCarry"
    - from: "src/components/AgentConsole.tsx"
      to: "src/stores/uiStore.ts"
      via: "chip 行渲染 carry 并 removeCarriedItem"
      pattern: "已携带:"
---

<objective>
裸 ⌘K/Ctrl+K 唤起 agent 并携带当前视图上下文（选中产品/任务/今日日程），chip 行可视化且可移除，注入走 buildCoreContext 同一段 — UX-02（17-UI-SPEC Surface 2，含 carry 状态模型决策）。

Purpose: 全局 agent 入口「懂你所在之处」；与 Phase 15 五段投影同构，不开第二路径。
Output: uiStore carry 状态、context.ts 扩展、useCmdK 裸绑定、AgentConsole chip 行、TaskKanban viewMode+活动分类提升。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-agent-ux/17-CONTEXT.md (⌘K 决策段)
@.planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 2)

@src/ai/context.ts
@src/stores/uiStore.ts
@src/hooks/useCmdK.ts
@src/components/TaskKanban.tsx
@src/components/AgentConsole.tsx (17-01 产物)

<interfaces>
From src/ai/context.ts (现状，扩展点):
```typescript
export function buildCoreContext(): string;
// 现有 sections: # Current Context / ## Selected Product / ## Active Tasks (top 10)
//                / ## Upcoming Events (next 7 days, top 5) / ## User Preferences
// 经 toolLoop.ts:127 buildCore 传入 assembleInjectedContext（Phase 15 五段投影第 1 段，
// SEGMENT_QUOTAS.core clamp）— 扩展 buildCoreContext 即自动流经既有组装器。
```

From src/stores/uiStore.ts: `useUIStore` (persist, partialize 白名单 — 新字段**不得**加入 partialize)；既有 `activeTab/selectedProductId/selectedTaskId/isChatPanelOpen/setChatPanelOpen`。

From src/stores (context.ts 已有读取先例): `useProductStore.getState().products`、`useTaskStore.getState().categories`（Task: title/priority/deadline/status/project/id；Category: id/name/tasks）、`useScheduleStore.getState().events`（ScheduleEvent: id/title/time/date/type）。

From src/components/TaskKanban.tsx:59: `const [viewMode, setViewMode] = useState('category')` — 本 plan 提升入 uiStore。:123 `displayGroups = viewMode === 'category' ? categories : dateGroups`（groups 有 `name` 字段 — 活动分类名数据源）。注意：分类视图无既有「单一活动分类」交互，本 plan 以列头点击补齐（UI-SPEC Surface 2 DECISION 锁定提升活动分类名）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uiStore carry 状态 + refreshAgentCarry + buildCoreContext 扩展（测试先行）</name>
  <read_first>src/ai/context.ts, src/stores/uiStore.ts, src/stores/taskStore.ts, src/stores/scheduleStore.ts, .planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 2 carry 表格), src/ai/__tests__/phase15ToolLoopInjection.test.ts (测试风格参考)</read_first>
  <files>src/ai/__tests__/phase17ContextCarry.test.ts, src/stores/uiStore.ts, src/ai/context.ts</files>
  <behavior>
- Test 1: `useUIStore.setState({ activeTab: 'tasks', selectedTaskId: 't1' })` 且 taskStore 含 id 't1' → `refreshAgentCarry()` 后 `useUIStore.getState().agentContextCarry` 为 `[{ kind: 'task', id: 't1', label: 含任务 title 与 priority 的字符串 }]`
- Test 2: `agentContextCarry = [{ kind: 'task', id: 't1', label: '[高] 写周报' }]` → `buildCoreContext()` 输出含 `- Selected Task: [高] 写周报`
- Test 3: `agentContextCarry = [{ kind: 'schedule', label: '今日日程', count: 2 }]` 且 scheduleStore 有今日 2 条事件 → 输出含 `## Carried Context` 且含两条 `- Today:` 事件行（date time title）
- Test 4: `agentContextCarry = []` → 输出**不含** `## Carried Context`（空 carry 零新行）
- Test 5: schedule carry 但今日无事件 → 不输出该段（count 为 0 时 refreshAgentCarry 不产生 schedule 项）
- Test 6: `activeTab: 'product-management'` + selectedProductId 指向存在产品 → carry 含 `{ kind: 'product', id, label: 产品名 }`；buildCoreContext 不为 product 重复输出新行（已有 ## Selected Product 段覆盖）
- Test 7（任务未选中 fallback — CONTEXT「无选中则当前列表过滤器」）: `activeTab: 'tasks'`, `selectedTaskId: null` — (a) `taskKanbanView: 'category'` + `taskKanbanCategory: '设计'` → carry 为 `[{ kind: 'task', label: '任务 · 设计' }]`；(b) `taskKanbanView: 'category'` + `taskKanbanCategory: null` → label `'任务 · 全部分类'`；(c) `taskKanbanView: 'date'` → label `'任务 · 按日期'`
- Test 8: `setTaskKanbanView('date')` 在 `taskKanbanCategory: '设计'` 时 → `taskKanbanCategory` 被置回 null（store setter 联动清理）
  </behavior>
  <action>
RED: 先写 `src/ai/__tests__/phase17ContextCarry.test.ts`（node:test + assert，风格对照 phase15 测试；`useUIStore.setState(...)` / `useTaskStore.setState(...)` 直接播种，无 Tauri 依赖）→ `npm test` 确认失败。

GREEN:
1. `src/stores/uiStore.ts` 增 transient 状态（不进 partialize）：
```typescript
export interface CarriedContextItem { kind: 'product' | 'task' | 'schedule'; id?: string; label: string; count?: number }
// state: agentContextCarry: CarriedContextItem[] = [];
//        taskKanbanView: 'category' | 'date' = 'category';     // TaskKanban viewMode 提升
//        taskKanbanCategory: string | null = null;             // 分类视图活动分类名（UI-SPEC Surface 2 DECISION）
// actions: setAgentContextCarry(items: CarriedContextItem[])
//          removeCarriedItem(kind, id?)  // 过滤掉首个 kind 匹配（id 匹配若给）
//          setTaskKanbanView(v)          // v === 'date' 时同时置 taskKanbanCategory = null
//          setTaskKanbanCategory(name: string | null)
```
2. `src/ai/context.ts` 增：
```typescript
export function refreshAgentCarry(): void
```
按 17-UI-SPEC Surface 2 表格派生 — 读 `useUIStore.getState()`：
- `product-management`: products.find(selectedProductId) → `{kind:'product', id, label: product.name}`（未选中 → 该视图 carry 为空）
- `tasks`: selectedTaskId 命中 → `{kind:'task', id, label: \`[${task.priority}] ${task.title}\`}`；未选中 fallback（CONTEXT「无选中则当前列表过滤器」，UI-SPEC DECISION 提升活动分类名）→ `{kind:'task', label: taskKanbanView === 'date' ? '任务 · 按日期' : taskKanbanCategory ? \`任务 · ${taskKanbanCategory}\` : '任务 · 全部分类'}` — chip 显示实际过滤器名（如「已携带:任务 · 设计」），不是视图粒度泛称
- `schedule`: events 过滤 `date === 今日Key`，n>0 → `{kind:'schedule', label:'今日日程', count:n}`；n=0 → 不产生
- 其它 tab → `[]`。结果 `setAgentContextCarry(items)`。
3. `buildCoreContext()` 末尾（User Preferences 前）追加：carry 中 task/schedule 项输出 `## Carried Context` 段 — task 项 `- Selected Task: ${label}`；schedule 项从 scheduleStore 读今日事件输出 `- Today: ${date} ${time} ${title}` 行；product 项不输出（## Selected Product 已覆盖）。空则不加段。同一函数同一 clamp，无第二路径。

`CarriedContextItem` 类型定义放 context.ts 并 export，uiStore import（或反向 — 保持单一来源：定义在 uiStore.ts，context.ts import）。
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `npm test` 全绿，phase17ContextCarry.test.ts ≥ 8 个用例全过（含 Test 7 分类名 fallback 三分支）
  - grep `agentContextCarry` 命中 uiStore.ts 与 context.ts；grep `refreshAgentCarry` 命中 context.ts
  - grep `taskKanbanCategory` 命中 uiStore.ts 与 context.ts（活动分类名已提升并被派生读取）
  - grep -A5 partialize src/stores/uiStore.ts 输出不含 agentContextCarry / taskKanbanView / taskKanbanCategory（transient）
  - grep `Carried Context` 命中 context.ts
  </acceptance_criteria>
  <done>carry 派生与注入经测试锁定；空 carry 零污染；任务 fallback 携带实际过滤器名</done>
</task>

<task type="auto">
  <name>Task 2: 裸 ⌘K 绑定 + TaskKanban viewMode/活动分类提升 + chip 行</name>
  <read_first>src/hooks/useCmdK.ts, src/components/TaskKanban.tsx ([:59 viewMode, :123 displayGroups, :215 SegmentedControl), src/components/AgentConsole.tsx, src/ai/context.ts, .planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 2 ⌘K 绑定与 chip JSX)</read_first>
  <files>src/hooks/useCmdK.ts, src/components/TaskKanban.tsx, src/components/AgentConsole.tsx</files>
  <action>
1. `src/hooks/useCmdK.ts` — 在现有 shift 分支**之前**加裸 ⌘K 分支（17-UI-SPEC 逐字），并给既有 Shift+K 分支同样补 `refreshAgentCarry()`：
```typescript
if (meta && !event.shiftKey && key === 'k') {
  event.preventDefault();
  event.stopPropagation();
  refreshAgentCarry();
  const s = useUIStore.getState();
  s.setChatPanelOpen(!s.isChatPanelOpen);
} else if (meta && event.shiftKey && key === 'k') {
  event.preventDefault();
  event.stopPropagation();
  refreshAgentCarry();
  const s = useUIStore.getState();
  s.setChatPanelOpen(!s.isChatPanelOpen);
}
```
（顶部注释更新：注明 Tauri webview 无 URL 栏冲突、Shift+K 留作 web dev fallback。）

2. `src/components/TaskKanban.tsx` — 删除 :59 `useState('category')`，改 `const viewMode = useUIStore((s) => s.taskKanbanView); const setViewMode = useUIStore((s) => s.setTaskKanbanView);`（:215 SegmentedControl onChange 不变，切 date 由 store setter 联动清空 taskKanbanCategory）。

   活动分类名提升（checker 修订：与 viewMode 同一 transient 字段结构）：displayGroups.map 渲染的分类列**列头**（group.name 所在标题元素）加 `onClick={() => setTaskKanbanCategory(taskKanbanCategory === group.name ? null : group.name)}`（点击设为活动分类，再点同列清除；date 视图列头不挂 — `viewMode === 'category'` 条件内）。`taskKanbanCategory`/`setTaskKanbanCategory` 来自 useUIStore selectors。零视觉改动：不加选中高亮（纯 carry 数据源；视觉反馈即 ⌘K 后 chip 显示「任务 · {分类名}」）。

3. `src/components/AgentConsole.tsx` — footer 表单上方插 chip 行（17-UI-SPEC Surface 2 JSX 逐字）：
```tsx
{carry.length > 0 && (
  <div className="mb-2 flex flex-wrap items-center gap-1.5">
    <span className="text-xs text-text-tertiary">已携带:</span>
    {carry.map((item) => (
      <span key={`${item.kind}-${item.id ?? 'default'}`}
        className="inline-flex items-center gap-1 rounded-full border border-accent/20 bg-accent-subtle px-2 py-0.5 text-xs text-accent">
        {item.kind === 'schedule' ? `今日日程 (${item.count})` : item.label}
        <button type="button" onClick={() => removeCarriedItem(item.kind, item.id)}
          aria-label={`移除 ${item.label}`} className="shrink-0 hover:text-accent-hover">
          <X size={10} />
        </button>
      </span>
    ))}
  </div>
)}
```
carry/removeCarriedItem 来自 useUIStore selectors（`X` 从 @phosphor-icons/react import）。移除即 session-scoped，无 toast、无持久化（发送时 buildCoreContext 读 store 现值，天然满足「移除影响下一次发送」）。
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm test &amp;&amp; grep -c "refreshAgentCarry" src/hooks/useCmdK.ts &amp;&amp; grep -c "已携带:" src/components/AgentConsole.tsx &amp;&amp; grep -c "setTaskKanbanCategory" src/components/TaskKanban.tsx</automated>
  </verify>
  <acceptance_criteria>
- useCmdK.ts: grep `!event.shiftKey && key === 'k'` 命中（裸 ⌘K 分支）；grep `refreshAgentCarry` 命中 ≥ 2 处
- TaskKanban.tsx: grep `useState('category')` 零命中；grep `taskKanbanView` 与 `setTaskKanbanCategory` 均命中（viewMode + 活动分类提升）
- AgentConsole.tsx: grep `已携带:` 与 `removeCarriedItem` 命中；chip class 串 `border-accent/20 bg-accent-subtle` 命中
- npm run lint + npm test 全绿
  </acceptance_criteria>
  <done>裸 ⌘K 携带上下文唤起；chip 行在两宿主 footer 均可见可移除；任务 fallback chip 显示实际分类名（如「任务 · 设计」）</done>
</task>

</tasks>

<verification>
- npm test（含新 phase17ContextCarry.test.ts）+ npm run lint 全绿
- 三条链路 grep 打通：useCmdK→refreshAgentCarry→uiStore.agentContextCarry→buildCoreContext；AgentConsole chip 行→removeCarriedItem；TaskKanban 列头→setTaskKanbanCategory→refreshAgentCarry label
</verification>

<success_criteria>
UX-02 达成：⌘K 携带视图上下文唤起 agent，chip 可视化、可移除、影响下一次发送，无第二组装路径；任务未选中时携带实际列表过滤器名（视图模式+活动分类）。
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-ux/17-02-SUMMARY.md`
</output>
