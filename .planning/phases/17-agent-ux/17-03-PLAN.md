---
phase: 17-agent-ux
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/stores/reportSelectors.ts
  - src/stores/__tests__/morningReport.test.ts
  - src/components/MorningReport.tsx
  - src/views/AgentWorkspaceView.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "每日首次进入 Agent 工作区呈现结构化晨报卡片（今日日程/过期任务/待确认记忆候选），数据来自本地查询零 LLM"
    - "同一天内再次进入不重复出现（localStorage 日期戳）；全空的一天不渲染晨报区"
    - "条目可点击跳转（日程→日程视图、任务→任务管理、记忆候选→打开 ChatPanel）；会话开始后晨报折叠为顶部横条"
  artifacts:
    - path: "src/stores/reportSelectors.ts"
      provides: "selectTodayEvents / isOverdueDeadline / selectOverdueTasks 纯函数"
      contains: "selectOverdueTasks"
    - path: "src/components/MorningReport.tsx"
      provides: "晨报卡片（expanded hero + collapsed bar + 每日一次戳）"
      contains: "morning-report:last-shown"
    - path: "src/views/AgentWorkspaceView.tsx"
      provides: "晨报挂载点（glass Card 之上）"
      contains: "MorningReport"
  key_links:
    - from: "src/components/MorningReport.tsx"
      to: "src/stores/reportSelectors.ts"
      via: "selectors 派生三段数据"
      pattern: "selectOverdueTasks"
    - from: "src/components/MorningReport.tsx"
      to: "src/ai/memoryStore.ts"
      via: "getMemoryStore().listPending()（异步 effect）"
      pattern: "listPending"
    - from: "src/views/AgentWorkspaceView.tsx"
      to: "src/components/MorningReport.tsx"
      via: "晨报区渲染"
      pattern: "<MorningReport"
---

<objective>
Agent 工作区每日首次进入呈现结构化晨报（纯数据查询零 LLM），同日去重、空内容不渲染、会话开始折叠为横条 — UX-03（17-UI-SPEC Surface 3，含每日一次语义决策）。

Purpose: agent 从被动应答升级为主动建议；复述 SQLite 已有数据是被禁止的失败模式，结构化卡片是正解。
Output: reportSelectors.ts（纯函数 + 测试）、MorningReport.tsx、AgentWorkspaceView 挂载。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-agent-ux/17-CONTEXT.md (晨报决策段)
@.planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 3 + Copywriting Contract)

@src/stores/scheduleStore.ts
@src/stores/taskStore.ts
@src/views/AgentWorkspaceView.tsx (17-01 产物)
@src/components/AgentConsole.tsx (17-01 产物 — messages.length 判据)

<interfaces>
From src/stores/scheduleStore.ts:
```typescript
export interface ScheduleEvent { id: string; title: string; time: string /* 'HH:mm - HH:mm' 或 'HH:mm' */; date: string /* YYYY-MM-DD */; type: ScheduleEventType; location?: string }
useScheduleStore.getState().events: ScheduleEvent[];
```

From src/stores/taskStore.ts:
```typescript
// Task 字段: id, title, status ('进行中'|'已完成'|'done'|...), priority, deadline: string
// deadline 为自由文本: '2025-05-16' / '明天 12:00' / '' — 过滤必须跳过不可解析值，绝不抛错
useTaskStore.getState().categories.flatMap((c) => c.tasks): Task[];
```

From src/ai/memoryStore.ts: `getMemoryStore().listPending(): Promise<MemoryCandidate[]>`（MemoryCandidate: content/createdAt/candidateToken）— 双实现，web/Node 内存版可跑。

From src/stores/uiStore.ts: `setActiveTab(tab)`, `setSelectedTaskId(id)`, `setChatPanelOpen(open)`。

From 17-01 chatConsoleStore: `useChatConsoleStore((s) => s.messages)` — "会话开始" = messages.length > 0。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reportSelectors 纯函数（测试先行）</name>
  <read_first>src/stores/taskStore.ts (Task.deadline 实际取值), src/stores/scheduleStore.ts, src/stores/__tests__/taskAdvanced.test.ts (测试风格)</read_first>
  <files>src/stores/reportSelectors.ts, src/stores/__tests__/morningReport.test.ts</files>
  <behavior>
- Test 1: `isOverdueDeadline('2025-05-16', new Date('2026-08-17'))` → true；`isOverdueDeadline('2099-01-01', ...)` → false
- Test 2: `isOverdueDeadline('明天 12:00', ...)` → false；`isOverdueDeadline('', ...)` → false（不可解析/空 → 永不过期，不抛错）
- Test 3: 今天日期等于 today → false（仅严格早于今天算过期）
- Test 4: `selectOverdueTasks(tasks, today)` 排除 status '已完成' 与 'done'；排除 deadline 不可解析项；保留过期项
- Test 5: `selectTodayEvents(events, '2026-08-17')` 只含 date === '2026-08-17' 且按 time 升序
- Test 6: deadline 支持带时间的 ISO 前缀（'2026-08-16 18:00' 类字符串按日期部分比较）
  </behavior>
  <action>
RED: 先写 `src/stores/__tests__/morningReport.test.ts`（node:test + assert，直接构造 Task/ScheduleEvent 字面量，不碰真实 store 数据）→ `npm test` 确认失败。

GREEN: 新建 `src/stores/reportSelectors.ts`（纯函数，零 React/零 zustand 依赖）：
```typescript
export function isOverdueDeadline(deadline: string, today: Date): boolean {
  // 取 deadline 头部 YYYY-MM-DD（/^(\d{4})-(\d{2})-(\d{2})/）构造本地 Date(早于今日零点)；
  // 不匹配（'明天 12:00' / '' / 任意自由文本）→ false。绝不 throw。
}
export function selectOverdueTasks(tasks: Task[], today: Date): Task[] {
  // status !== '已完成' && status !== 'done' && isOverdueDeadline(...)
}
export function selectTodayEvents(events: ScheduleEvent[], todayKey: string): ScheduleEvent[] {
  // date === todayKey，time localeCompare 升序
}
```
Task/ScheduleEvent 类型从各自 store 文件 import（type-only）。
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
- `npm test` 全绿，morningReport.test.ts ≥ 6 用例
- grep `isOverdueDeadline\|selectOverdueTasks\|selectTodayEvents` 在 reportSelectors.ts 各命中 export
- reportSelectors.ts 无 react/zustand import（grep 零命中）
- 不可解析 deadline 用例存在且断言 false（grep `明天` 命中测试文件）
  </acceptance_criteria>
  <done>晨报三段数据的派生逻辑被测试锁定，自由文本 deadline 安全</done>
</task>

<task type="auto">
  <name>Task 2: MorningReport 组件 + AgentWorkspaceView 挂载</name>
  <read_first>src/stores/reportSelectors.ts, src/views/AgentWorkspaceView.tsx, .planning/phases/17-agent-ux/17-UI-SPEC.md (Surface 3 全部 JSX), src/stores/chatConsoleStore.ts, src/components/ChatPanel.tsx (空态参考)</read_first>
  <files>src/components/MorningReport.tsx, src/views/AgentWorkspaceView.tsx</files>
  <action>
新建 `src/components/MorningReport.tsx`，`export function MorningReport()`，按 17-UI-SPEC Surface 3 实现：

**每日一次语义（锁定）**：mount 时读 `localStorage['morning-report:last-shown']`；不等于今日 `yyyy-MM-dd` → 本次为当日首次：**立即写戳**（无论内容是否为空）并进入渲染判定；等于今日 → `return null`（整天不再出现）。戳只从 View mount 写（⌘K 进 ChatPanel 不消耗晨报 — 组件仅挂载在 AgentWorkspaceView，天然满足）。

**数据**：`today = new Date()`；events = selectTodayEvents(scheduleStore.events, todayKey)；overdue = selectOverdueTasks(taskStore 全量 tasks, today)；memory 候选 = useEffect 内 `getMemoryStore().listPending()`（异步，初始 []）。三段全空 → `return null`（空报告也已完成写戳）。

**折叠判定**：`hasConversation = useChatConsoleStore((s) => s.messages.length > 0)`；本地 state `collapsed` 初始 = hasConversation；会话从无到有时自动折叠（effect 监听 hasConversation 翻转 → setCollapsed(true)）。折叠态渲染横条（17-UI-SPEC JSX 逐字）：
```tsx
<button type="button" onClick={expand}
  className="flex w-full cursor-pointer items-center gap-3 rounded-[var(--radius-lg)] border border-border-subtle bg-bg-primary px-4 py-2 text-xs text-text-secondary transition-colors hover:bg-bg-secondary">
  <Sparkle size={14} weight="duotone" className="text-accent" />
  <span className="font-medium text-text-primary">晨报</span>
  <span className="text-text-tertiary">今日日程 {n1} · 过期任务 {n2} · 待确认 {n3}</span>
  <CaretDown size={12} className="ml-auto shrink-0" />
</button>
```

**展开态**（17-UI-SPEC JSX 逐字）：`<Card variant="default" className="p-5">`，header（Sparkle text-accent + `晨报` text-sm font-semibold + `formatChineseDate(today)` text-xs）+ 收起 ghost 按钮（`aria-label="收起晨报"`，CaretUp 14）；`mt-4 space-y-4` 三段，**空段省略不显示**。段 header 模板（今日日程例）：
```tsx
<div className="flex items-center gap-1.5 text-xs font-medium text-text-secondary">
  <CalendarDots size={14} weight="duotone" />
  今日日程
  <span className="text-text-tertiary">({events.length})</span>
</div>
```
段图标：今日日程 `CalendarDots`；过期任务 `Warning` 加 `className="text-warning"`；待确认记忆候选 `Brain`。条目行（17-UI-SPEC row 模板逐字）：`flex w-full cursor-pointer items-center gap-2 rounded-[var(--radius-md)] px-2 py-1.5 text-left text-sm text-text-primary transition-colors hover:bg-bg-secondary`；schedule 条目前缀 `{ev.time}` tabular-nums；task 条目次行文本 `{deadline} · {priority}`；memory 条目单行 truncate `{content}`。

**跳转**：schedule 条目 → `setActiveTab('schedule')`；task 条目 → `setActiveTab('tasks')` + `setSelectedTaskId(id)`；memory 条目 → `setChatPanelOpen(true)`（确认卡片已在 console 内渲染，无新面）。无批量操作（锁定）。

`formatChineseDate(d)` 本地 helper：`${d.getMonth()+1}月${d.getDate()}日 星期${['日','一','二','三','四','五','六'][d.getDay()]}`（Copywriting Contract）。

**AgentWorkspaceView.tsx**：在 glass Card 之上插 `<MorningReport />`（17-01 预留位）。
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm test &amp;&amp; grep -c "morning-report:last-shown\|收起晨报\|今日日程" src/components/MorningReport.tsx</automated>
  </verify>
  <acceptance_criteria>
- MorningReport.tsx: grep `morning-report:last-shown`、`晨报`、`收起晨报`、`text-warning`、`listPending`、`setChatPanelOpen(true)` 均命中
- grep `今日日程 {n1}` 形态的折叠横条文案 `· 待确认 {n3}` 或其 JSX 等价物命中
- AgentWorkspaceView.tsx: grep `<MorningReport` 命中且位于 glass Card 之前
- 无 LLM 调用：grep `runToolLoop\|fetch\|generate` 零命中
- npm run lint + npm test 全绿
  </acceptance_criteria>
  <done>晨报三段卡片 + 折叠横条 + 每日一戳 + 点击跳转全部落地，纯数据查询</done>
</task>

</tasks>

<verification>
- npm test（含新 morningReport.test.ts）+ npm run lint 全绿
- 空数据安全：selectors 测试覆盖不可解析 deadline 与空事件
</verification>

<success_criteria>
UX-03 达成：晨报结构化卡片、每日一次、数据查询零 LLM、条目可跳转、空内容不渲染。
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-ux/17-03-SUMMARY.md`
</output>
