---
phase: 17-agent-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/ADR-0001-architecture-switch.md
  - docs/adr/ADR-0002-harness-mit-attribution.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "docs/ARCHITECTURE.md 描述「事件日志 + tool loop + FTS5 + HITL + Tauri 壳」现行架构，GraphFlow/Rig/LanceDB 正式出局"
    - "AGENT_MEMORY_REFERENCE.md 被 ARCHITECTURE.md 纳入真相源索引"
    - "ADR-0001 记录架构切换决策，ADR-0002 记录 deepseek-harness 纯函数复用的 MIT 归属，ARCHITECTURE.md 索引两 ADR"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "新架构真相源文档"
      contains: "AGENT_MEMORY_REFERENCE"
    - path: "docs/adr/ADR-0001-architecture-switch.md"
      provides: "架构切换 ADR"
      contains: "GraphFlow"
    - path: "docs/adr/ADR-0002-harness-mit-attribution.md"
      provides: "harness MIT 归属 ADR"
      contains: "MIT"
  key_links:
    - from: "docs/ARCHITECTURE.md"
      to: "docs/AGENT_MEMORY_REFERENCE.md"
      via: "真相源索引链接"
      pattern: "AGENT_MEMORY_REFERENCE"
    - from: "docs/ARCHITECTURE.md"
      to: "docs/adr/"
      via: "ADR 索引"
      pattern: "ADR-000[12]"
---

<objective>
docs/ARCHITECTURE.md 全文重写为「事件日志 + tool loop + FTS5」新真相源叙事；新建 docs/adr/ 含 ADR-0001（架构切换）与 ADR-0002（harness 纯函数复用 MIT 归属）— ARCH-01/ARCH-02。

Purpose: 旧 GraphFlow/Rig/LanceDB 蓝图与现行代码严重脱节；文档对齐后后续里程碑不再引用旧蓝图。
Output: 重写的 ARCHITECTURE.md + 两个新 ADR（docs/adr/ 目录当前不存在，需创建）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md (Accumulated Context — 各 phase 架构决策摘要)
@.planning/phases/17-agent-ux/17-CONTEXT.md (ARCH-01/02 决策段)

@docs/AGENT_MEMORY_REFERENCE.md
@docs/ARCHITECTURE.md

<interfaces>
文档事实锚点（写作时必须对照，不得凭记忆）：
- 事件日志: src/ai/events/（append-only agent_events 表，迁移 src-tauri/migrations/0002_agent_events.sql，seq 连续 + correlation_id + 配对不变量 + artifacts >4KB 外置）
- 确认持久化: src/ai/confirmationStore.ts + 迁移 0003（paramsHash 规范化 JSON SHA-256、原子条件 UPDATE 消费）
- 会话投影: src/ai/chatSession.ts（ChatSession = 事件日志投影，LLM messages 单一真相派生）
- 恢复: src/ai/sessionRestore.ts（崩溃尾切到最后完整 turn、孤儿 tool_call 标记 interrupted 绝不重试）
- 压缩: src/ai/compaction.ts（≥0.8×窗口触发，配对平衡处切分，事件无损）
- 记忆/知识: src/ai/memoryStore.ts + src/ai/knowledgeRepo.ts + 迁移 0004（memories/memory_candidates/knowledge_docs + knowledge_fts FTS5，ftsTokens 同源切分）
- 上下文: src/ai/contextAssembler.ts（五段优先级投影 + context_injected 审计事件）
- 交付物: src/ai/tools/generateDeliverable（两段式候选 + 版本化落槽 + FTS 同事务）
- 运行时: src/ai/toolLoop.ts（单历史，事件驱动）
- 壳: Tauri v2（src-tauri/）+ React 19 + zustand；零 sidecar 约束
- harness 参考: D:\Projects\Nova\deepseek-harness\deepseek-harness-master（Node.js/Cordis，dev preview — 只抄设计不引入框架）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 docs/adr/ 与两个 ADR</name>
  <read_first>docs/AGENT_MEMORY_REFERENCE.md, .planning/ROADMAP.md (Out of Scope 表与 GraphFlow 否决理由), .planning/STATE.md (Decisions 段)</read_first>
  <files>docs/adr/ADR-0001-architecture-switch.md, docs/adr/ADR-0002-harness-mit-attribution.md</files>
  <action>
创建 `docs/adr/` 目录与两个 ADR（每篇 60-120 行，中文，标准 ADR 骨架：Status/Date/Context/Decision/Consequences）。

**ADR-0001-architecture-switch.md**（Status: Accepted，Date: 2026-08-17）：
- Context: v0.2.0 蓝图规划 GraphFlow（工作流引擎）+ Rig（Rust LLM 框架）+ LanceDB（向量库）。v0.3.0 实际演进选择了 TS 侧「append-only 事件日志 + 增强 tool loop + SQLite FTS5」：pre-1.0 crate 风险、AGENT_MEMORY_REFERENCE §9 不采用其插件树、事件日志已在 Phase 13-16 全部落地并被验证。
- Decision: GraphFlow/Rig/LanceDB 正式出局；新架构 = 事件日志（真相源）+ tool loop（执行）+ FTS5（检索）+ HITL 确认队列 + Tauri 壳；SQLite 为唯一持久层；零 sidecar 约束保持。
- Consequences: 文档/后续里程碑不得再引用旧蓝图；向量检索（SEM-01/02）仅作派生索引评估，永不做事实源；Rust 原生后端目标降级为远期可选。

**ADR-0002-harness-mit-attribution.md**（Status: Accepted，Date: 2026-08-17）：
- Context: Phase 13/14 的 agent_events 事件词表、tool_call/tool_result 配对算法、ChatSession 投影（deriveMessages 纯函数）设计参考了 DeepSeek Harness（`deepseek-harness-master`，MIT License）的 session event log 设计。
- Decision: 复用范围严格限定为**设计思想与纯函数算法**（事件配对、投影派生、崩溃恢复切分点）；不引入其框架/运行时（Node.js/Cordis 违反零 sidecar，且 dev preview）。在 ADR 中注明 MIT 归属与上游路径。
- Consequences: 涉及复用算法的源文件（src/ai/events/ 不变量检查、chatSession 投影）的衍生关系可追溯；未来若引入更多 harness 概念须先补 ADR。
  </action>
  <verify>
    <automated>grep -l "GraphFlow" docs/adr/*.md &amp;&amp; grep -l "MIT" docs/adr/ADR-0002-harness-mit-attribution.md &amp;&amp; grep -c "Status" docs/adr/ADR-0001-architecture-switch.md</automated>
  </verify>
  <acceptance_criteria>
- docs/adr/ADR-0001-architecture-switch.md 存在，含 `Status: Accepted`、`GraphFlow`、`Rig`、`LanceDB`、`事件日志` 字样
- docs/adr/ADR-0002-harness-mit-attribution.md 存在，含 `MIT`、`deepseek-harness` 字样
- 两文件均含 Context/Decision/Consequences 三节标题
  </acceptance_criteria>
  <done>两个 ADR 落盘，架构切换与 MIT 归属均有正式决策记录</done>
</task>

<task type="auto">
  <name>Task 2: ARCHITECTURE.md 全文重写</name>
  <read_first>docs/ARCHITECTURE.md (旧文全文,确认哪些叙事作废), docs/AGENT_MEMORY_REFERENCE.md, docs/adr/ADR-0001-architecture-switch.md, docs/adr/ADR-0002-harness-mit-attribution.md, src/ai/index.ts</read_first>
  <files>docs/ARCHITECTURE.md</files>
  <action>
整文件重写（旧 v1.0 2026-08-07 蓝图全文作废，不保留章节）。新文档骨架（版本 2.0，日期 2026-08-17）：

1. **产品定位** — 一段（沿用 CLAUDE.md 的 AI native PM 桌面工作台定位，含零 sidecar/本地优先约束）
2. **架构总览** — 「事件日志 + tool loop + FTS5 + HITL + Tauri 壳」一图一段说明数据流：用户消息 → toolLoop（单历史）→ agent_events 追加 → ChatSession 投影 → LLM messages 派生；上下文五段投影注入；HITL 候选经确认队列原子消费
3. **分层结构** — Tauri 壳 / React 19 + zustand 前端（含 AppContext 兼容层现状）/ TS agent 运行时（src/ai/ 各模块，对照 context 段锚点逐模块一小段）/ SQLite 持久层（migrations 0002-0004 表清单：agent_events、confirmation_candidates、memories、memory_candidates、knowledge_docs、knowledge_fts）
4. **关键设计决策** — append-only 事件日志为唯一真相源；投影可重建；孤儿 tool_call 绝不重试；>4KB artifact 外置；压缩无损原始事件；FTS5 索引与文档写入同事务
5. **真相源索引** — docs/AGENT_MEMORY_REFERENCE.md（agent 记忆/知识架构权威参考）、src-tauri/migrations/（schema 真相）、CLAUDE.md（工程约定）
6. **ADR 索引** — 链接 docs/adr/ADR-0001、ADR-0002
7. **已否决方向** — GraphFlow 工作流引擎 / LanceDB 作事实源 / 静默自动写记忆 / 动态脚本工作流（引 REQUIREMENTS.md Out of Scope 的一句话理由）

写作纪律：只描述**已落地**的系统（对照 context 段锚点与实际源码），不写规划中的能力（SEM-01 等只在「已否决/后续」一句话提及）。全文不得出现将 GraphFlow/Rig/LanceDB 描述为现行组件的语句（仅在否决/历史语境出现）。
  </action>
  <verify>
    <automated>grep -c "事件日志" docs/ARCHITECTURE.md &amp;&amp; grep -c "AGENT_MEMORY_REFERENCE" docs/ARCHITECTURE.md &amp;&amp; grep -c "ADR-0001\|ADR-0002" docs/ARCHITECTURE.md &amp;&amp; grep -n "FTS5" docs/ARCHITECTURE.md | head -3</automated>
  </verify>
  <acceptance_criteria>
- docs/ARCHITECTURE.md 版本标记为 2.0，日期 2026-08-17
- grep `事件日志`、`tool loop`（或 toolLoop）、`FTS5`、`HITL`、`Tauri` 均命中
- grep `AGENT_MEMORY_REFERENCE` 命中（真相源索引）
- grep `ADR-0001` 与 `ADR-0002` 均命中（ADR 索引）
- GraphFlow/Rig/LanceDB 仅出现在「已否决/ADR/历史」语境（grep -n 人工核对每一处出现均伴随 否决/出局/历史 字样）
- 旧文档的「Rust 原生后端 / GraphFlow pipeline / LanceDB 第二大脑」章节不再作为现行架构描述存在
  </acceptance_criteria>
  <done>ARCHITECTURE.md 与现行代码对齐，旧蓝图正式出局且留有 ADR 痕迹</done>
</task>

</tasks>

<verification>
- 三个文件落盘；ARCHITECTURE.md 索引 ADR 与 AGENT_MEMORY_REFERENCE
- grep 全 docs/ARCHITECTURE.md：GraphFlow 出现次数 ≤ 3 且全部在否决/历史语境
</verification>

<success_criteria>
ARCH-01/ARCH-02 达成：新真相源文档 + 双 ADR，后续里程碑无旧蓝图引用。
</success_criteria>

<output>
After completion, create `.planning/phases/17-agent-ux/17-04-SUMMARY.md`
</output>
