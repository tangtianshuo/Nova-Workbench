---
phase: 18-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/migrations/0007_sessions.sql
  - src-tauri/src/lib.rs
  - src/stores/storage/initializeDatabase.ts
  - src/ai/__tests__/phase18MigrationUpgrade.test.ts
autonomous: true
requirements: [SESS-01, SESS-06]
must_haves:
  truths:
    - "A v0.3.0 database upgraded with 0007 retains every event byte-identically and every historical session_id has a sessions row"
    - "Running 0007 twice produces identical final state (idempotent backfill)"
    - "Historical agent_events rows carry workspace_id from kv_store 'nova-workspace' blob after migration"
  artifacts:
    - path: "src-tauri/migrations/0007_sessions.sql"
      provides: "sessions table + idempotent backfill + workspace_id backfill + schema_version 7"
      contains: "CREATE TABLE IF NOT EXISTS sessions"
    - path: "src/ai/__tests__/phase18MigrationUpgrade.test.ts"
      provides: "fixture-DB upgrade test"
      min_lines: 60
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/migrations/0007_sessions.sql"
      via: "add_migrations include_str! version 7"
      pattern: "0007_sessions\\.sql"
---

<objective>
Migration 0007: sessions 元数据表 + 幂等回填 + agent_events workspace_id 回填,fixture-DB 升级测试锁定「旧数据不丢失」(P-A 最高风险项)。

Purpose: SESS-01/SESS-06 数据基础。全部 DDL + 回填在一个 .sql 文件内(sqlx per-file 事务,天然原子)。
Output: 0007_sessions.sql、lib.rs 注册、APP_SCHEMA_VERSION=7、升级测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session/18-RESEARCH.md
@src/ai/__tests__/sqlSchemaCheckConstraints.test.ts
@src-tauri/migrations/0006_confirmation_kind_deliverable.sql
@src/stores/storage/initializeDatabase.ts
@src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture-DB 升级测试(RED — 先于 0007 编写)</name>
  <files>src/ai/__tests__/phase18MigrationUpgrade.test.ts</files>
  <read_first>
    - src/ai/__tests__/sqlSchemaCheckConstraints.test.ts (proven pattern: node:sqlite DatabaseSync + migrationsDir resolution)
    - src-tauri/migrations/0001_init.sql, 0002_agent_events.sql, 0003_confirmation_candidates.sql (fixture schema: kv_store, agent_events columns, confirmation_candidates)
    - src/stores/workspaceStore.ts:195-210 (kv_store 'nova-workspace' blob shape `{state:{activeWorkspaceId},version}`)
  </read_first>
  <behavior>
    - Test 1 (idempotent backfill): build v0.3.0 DB (exec 0001..0006), insert fixture: kv_store row `('nova-workspace', '{"state":{"activeWorkspaceId":"ws-1"},"version":1}')`, 3 distinct session_ids x 4 events each with workspace_id NULL, 1 pending confirmation candidate. Run 0007 → every session_id has a sessions row with workspace_id='ws-1', created_at=MIN(created_at), last_active_at=MAX(created_at), title/parent_session_id/fork_cut_seq NULL; event count unchanged (12); per-event (session_id, seq, payload) checksums unchanged; all agent_events workspace_id='ws-1'; candidate row untouched. Run 0007 again → snapshot deepEqual identical.
    - Test 2 (empty kv_store): same fixture without the kv_store row → sessions rows exist with workspace_id NULL, no error (NULL = visible everywhere, acceptable-by-design).
    - Test 3 (schema_version): meta table value = '7' after 0007.
  </behavior>
  <action>
    Create test file following sqlSchemaCheckConstraints.test.ts setup verbatim (fs readFileSync + path.join to src-tauri/migrations, node:sqlite DatabaseSync, node:test + node:assert). Helper `buildV030Db(kvBlob?: string)` runs 0001..0006 then inserts fixture via prepared statements matching real column names (read 0002 for agent_events columns). Helper `snapshot(db)` returns `{sessions: rows sorted by session_id, eventCount, eventChecksums: sorted SHA or string join of (session_id,seq,type), metaVersion}`. Run 0007 via db.exec(fullFileSql). This task ships the test; it will fail until Task 2 creates 0007 — commit RED then proceed.
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase18MigrationUpgrade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/ai/__tests__/phase18MigrationUpgrade.test.ts and imports `node:sqlite`
    - Contains `buildV030Db` and runs migrations 0001-0006 by filename
    - Contains double-exec of 0007 with deepEqual snapshot comparison
    - Asserts workspace_id backfill from kv_store json path `$.state.activeWorkspaceId`
  </acceptance_criteria>
  <done>Test file committed; fails only because 0007_sessions.sql does not exist yet (RED).</done>
</task>

<task type="auto">
  <name>Task 2: 0007_sessions.sql + lib.rs 注册 + APP_SCHEMA_VERSION=7(GREEN)</name>
  <files>src-tauri/migrations/0007_sessions.sql, src-tauri/src/lib.rs, src/stores/storage/initializeDatabase.ts</files>
  <read_first>
    - src-tauri/migrations/0006_confirmation_kind_deliverable.sql (header comment style, INSERT OR IGNORE meta + UPDATE meta version pair)
    - src-tauri/src/lib.rs:19-56 (sql_migrations add_migrations + include_str! pattern)
    - src/stores/storage/initializeDatabase.ts:1-50 (APP_SCHEMA_VERSION usage)
    - 18-RESEARCH.md Pattern 2 (exact SQL to copy)
  </read_first>
  <action>
    Create 0007_sessions.sql with header comment `-- 0007: sessions 元数据表 + 历史会话回填 + agent_events workspace_id 回填 (v0.3.1 Phase 18, SESS-01/SESS-06)`, then EXACTLY the SQL from 18-RESEARCH.md Pattern 2:
    1. `CREATE TABLE IF NOT EXISTS sessions (session_id TEXT PRIMARY KEY, workspace_id TEXT, title TEXT, title_source TEXT, parent_session_id TEXT, fork_cut_seq INTEGER, created_at TEXT NOT NULL, last_active_at TEXT NOT NULL);`
    2. `CREATE INDEX IF NOT EXISTS idx_sessions_workspace ON sessions (workspace_id, last_active_at DESC);`
    3. `CREATE INDEX IF NOT EXISTS idx_agent_events_workspace ON agent_events (workspace_id, created_at);`
    4. `INSERT OR IGNORE INTO sessions (session_id, workspace_id, created_at, last_active_at) SELECT e.session_id, (SELECT json_extract(v.value, '$.state.activeWorkspaceId') FROM kv_store v WHERE v.key = 'nova-workspace'), MIN(e.created_at), MAX(e.created_at) FROM agent_events e GROUP BY e.session_id;`
    5. `UPDATE agent_events SET workspace_id = (SELECT json_extract(v.value, '$.state.activeWorkspaceId') FROM kv_store v WHERE v.key = 'nova-workspace') WHERE workspace_id IS NULL;`
    6. `INSERT OR IGNORE INTO meta (key, value) VALUES ('schema_version', '7');` + `UPDATE meta SET value = '7' WHERE key = 'schema_version';`
    Verify kv_store column names against 0001_init.sql before writing (research says `key, value`).
    In src-tauri/src/lib.rs sql_migrations(): add `Migration { version: 7, description: "sessions metadata + backfill", sql: include_str!("../migrations/0007_sessions.sql"), kind: MigrationKind::Up },` matching sibling entries.
    In initializeDatabase.ts change `export const APP_SCHEMA_VERSION = 6;` → `= 7;`.
    All backfill logic stays in SQL — zero TypeScript backfill (Pitfall 2). Never edit shipped 0001-0006 (Pitfall 3).
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase18MigrationUpgrade.test.ts && cd src-tauri && cargo check</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/migrations/0007_sessions.sql contains `CREATE TABLE IF NOT EXISTS sessions`, both CREATE INDEX statements, the INSERT OR IGNORE backfill with `json_extract`, and the workspace_id UPDATE
    - src-tauri/src/lib.rs contains `include_str!` referencing `0007_sessions.sql` with `version: 7`
    - initializeDatabase.ts contains `APP_SCHEMA_VERSION = 7`
    - phase18MigrationUpgrade.test.ts exits 0 (all tests pass)
    - cargo check exits 0
  </acceptance_criteria>
  <done>Fixture test green; migration registered Rust-side; schema version bumped to 7.</done>
</task>

</tasks>

<verification>
- `npm test` full suite passes (no regression in 161 existing tests)
- `cd src-tauri && cargo check` passes
</verification>

<success_criteria>
- 0007 applied to a v0.3.0 fixture DB: zero event loss, all sessions backfilled, idempotent on re-run, workspace_id stamped from kv_store
</success_criteria>

<output>
After completion, create `.planning/phases/18-session/18-01-SUMMARY.md`
</output>
