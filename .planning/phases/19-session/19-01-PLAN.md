---
phase: 19-session
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/sessionRestore.ts
  - src/ai/__tests__/phase14SessionRestore.test.ts
  - src/ai/__tests__/phase19SessionSwitch.test.ts
autonomous: true
requirements: [SESS-03]
must_haves:
  truths:
    - "调用 restoreSession(sessionId) 可恢复任意指定 session,投影与原会话逐字一致"
    - "restoreSession() 无参保持现行为(恢复最近 session,崩溃恢复路径兼容)"
    - "有参路径同样走 orphan settlement + crash tail cut"
    - "sessions[0] 假设(P-B)被移除,按显式 sessionId 查询"
  artifacts:
    - path: "src/ai/sessionRestore.ts"
      provides: "restoreSession(sessionId?) parameterized restore"
      contains: "export function restoreSession"
    - path: "src/ai/__tests__/phase19SessionSwitch.test.ts"
      provides: "per-session restore + replay parity tests"
  key_links:
    - from: "src/ai/sessionRestore.ts"
      to: "store.listEvents(sessionId)"
      via: "explicit sessionId query"
      pattern: "listEvents\\("
---

<objective>
Parameterize session restore: restoreLatestSession → restoreSession(sessionId?). No-arg keeps current behavior (crash-recovery startup compat); with sessionId restores ANY session by explicit query, removing the sessions[0] assumption (pitfall P-B). This is the SESS-03 engine that Plan 19-02 wires into switchSession.

Purpose: enable safe multi-session switching with verbatim history recovery.
Output: restoreSession(sessionId?) + replay-parity tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-session/19-CONTEXT.md
@src/ai/sessionRestore.ts
@src/ai/chatSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: restoreSession(sessionId?) — parameterized restore with per-session dedupe</name>
  <files>src/ai/sessionRestore.ts</files>
  <read_first>
    - src/ai/sessionRestore.ts (entire file — you are modifying doRestoreLatestSession)
    - src/ai/events/eventStore.ts (listSessions / listEvents signatures)
    - src/ai/chatSession.ts lines 280-320 (fromEvents signature: ChatSession.fromEvents(events, { sessionId, tokenBudget }))
  </read_first>
  <action>
    1. Rename public entry: `export function restoreSession(sessionId?: string): Promise<RestoredSession | null>`.
       Keep `export function restoreLatestSession()` as a thin alias calling `restoreSession()` (existing importers in chatConsoleStore.ts must keep compiling; Plan 19-02 will migrate callers).
    2. Replace module dedupe `let activeRestore: Promise<...> | null` with a per-sessionId Map: `const activeRestores = new Map<string, Promise<RestoredSession | null>>()`. Key: `sessionId ?? '__latest__'`. Dedupe promise is stored per key; `resetRestoreForTesting()` clears the whole Map.
    3. Extract `doRestoreLatestSession` body into `async function doRestore(sessionId?: string)`:
       - If `sessionId` provided: query that session's events directly via `store.listEvents(sessionId)` — do NOT call `store.listSessions()` and do NOT use `sessions[0]`. If the store exposes a session-exists check (getSessionRepo / sessions table), use it; otherwise treat `events.length === 0` as "not found → return null" (current behavior).
       - If `sessionId` omitted: keep current path (`store.listSessions()`, take `sessions[0]` — this no-arg path is the ONLY remaining sessions[0] use, and it is the documented "latest" semantic, not an assumption).
       - Both paths run the SAME settlement pipeline: orphan tool_result append (findOrphanToolCallEvents), crash tail cut (findCrashTailCutSeq), tokenBudget from session_created, ChatSession.fromEvents(projectionEvents, { sessionId, tokenBudget }), resumeEventEmission(), pending confirmations listing.
    4. Update file header comment to document the two paths.
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npm run lint && npx tsx --test src/ai/__tests__/phase14SessionRestore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/sessionRestore.ts` contains `export function restoreSession(sessionId?: string)`
    - `src/ai/sessionRestore.ts` contains `restoreLatestSession` (compat alias)
    - `src/ai/sessionRestore.ts` contains `new Map` for per-session dedupe (grep `activeRestores`)
    - `sessions[0]` appears at most once and only on the no-arg latest path
    - Existing phase14SessionRestore tests pass unchanged (no-arg behavior preserved)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>restoreSession(sessionId?) exported; no-arg behavior byte-identical; dedupe is per-session; lint + existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: switch-restore tests (replay parity + explicit sessionId)</name>
  <files>src/ai/__tests__/phase19SessionSwitch.test.ts</files>
  <read_first>
    - src/ai/__tests__/phase14SessionRestore.test.ts (existing fixtures/helpers — reuse its DB setup pattern)
    - src/ai/sessionRestore.ts (post-Task-1 state)
    - src/ai/__tests__/phase13ReplayParity.test.ts (parity assertion pattern)
  </read_first>
  <behavior>
    - Test 1 (verbatim restore): build session A with 2+ complete turns, persist events, call restoreSession(A.sessionId) → restored.getAllMessages() filtered to user/assistant(non-tool) matches the original session's message list exactly (role + content, element by element).
    - Test 2 (independent sessions): build sessions A and B with distinct messages; restoreSession(B.sessionId) returns B's messages, not A's — no cross-session bleed.
    - Test 3 (unknown sessionId): restoreSession('nonexistent') resolves to null, does not throw.
    - Test 4 (dedupe per session): two concurrent restoreSession(A) calls share one underlying promise (spy or event-count), but restoreSession(A) and restoreSession(B) run independently.
    - Test 5 (orphan settlement on explicit path): session with a dangling tool_call (no tool_result) restored via restoreSession(id) gets an appended interrupted tool_result marker; second restore of same id finds zero orphans (idempotent).
  </behavior>
  <action>
    Create the test file following the existing phase14 test DB fixture pattern (node:sqlite temp DB, getEventStore initialization). Write all five tests FIRST (they must fail to compile/pass against the old API), then confirm they pass against Task 1's restoreSession. Do not weaken Test 1: assert per-message `content` equality, not just count.
  </action>
  <verify>
    <automated>cd "D:\Projects\Nova\nova-pm-workspace" && npx tsx --test src/ai/__tests__/phase19SessionSwitch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/__tests__/phase19SessionSwitch.test.ts` exists and contains 5 test cases covering: verbatim restore, cross-session isolation, unknown id → null, per-session dedupe, orphan settlement idempotency
    - `npx tsx --test src/ai/__tests__/phase19SessionSwitch.test.ts` exits 0
    - Full suite `npm test` exits 0
  </acceptance_criteria>
  <done>All 5 switch-restore tests green; full test suite green.</done>
</task>

</tasks>

<verification>
npm test passes; restoreSession(sessionId?) restores any session verbatim; no-arg path unchanged.
</verification>

<success_criteria>
- SESS-03 engine ready: explicit-session restore is tested for verbatim parity and cross-session isolation.
- P-B (sessions[0] assumption) removed from all explicit-session paths.
</success_criteria>

<output>
After completion, create `.planning/phases/19-session/19-01-SUMMARY.md`
</output>
