---
phase: 21-session-list
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - src/stores/uiStore.ts
  - src/hooks/useCmdK.ts
  - src/components/ChatPanel.tsx
  - src/views/AgentWorkspaceView.tsx
autonomous: false
requirements: [LIST-01, LIST-02, QUICK-01, QUICK-02, QUICK-03]
must_haves:
  truths:
    - "Agent 页「最近任务」显示真实 session 列表(标题 + 相对时间 + 消息数),按当前工作区过滤,最近活动倒序,点击恢复该 session"
    - "空列表显示引导文案;streaming 时非当前行禁用"
    - "Ctrl+Shift+K 打开带工作区 + session 双下拉的 ChatPanel;工作区切换后 session 下拉联动过滤;+ 新对话 项创建新 session"
    - "Ctrl+K 打开的 ChatPanel 无任何选择器 DOM(QUICK-03)"
    - "标题生成后列表静默更新(sessionListVersion 订阅)"
  artifacts:
    - path: "src/views/AgentWorkspaceView.tsx"
      provides: "real recent-session list"
      contains: "listSessionsByWorkspace"
    - path: "src/components/ChatPanel.tsx"
      provides: "scoped selector row"
      contains: "chatPanelMode"
    - path: "src/stores/uiStore.ts"
      provides: "chatPanelMode state"
      contains: "chatPanelMode"
    - path: "src/hooks/useCmdK.ts"
      provides: "shortcut split"
      contains: "setChatPanelMode"
  key_links:
    - from: "AgentWorkspaceView list row"
      to: "switchSession"
      via: "onClick"
      pattern: "switchSession\\("
    - from: "AgentWorkspaceView"
      to: "sessionRepo.listSessionsByWorkspace + countMessagesBySession"
      via: "effect on [activeWorkspaceId, sessionListVersion]"
      pattern: "countMessagesBySession"
    - from: "useCmdK"
      to: "uiStore.setChatPanelMode"
      via: "Ctrl+Shift+K → 'scoped', Ctrl+K → 'pure'"
      pattern: "setChatPanelMode\\('scoped'\\)"
---

<objective>
All Phase 21 UI: real recent-session list on Agent page, ChatPanel scoped mode with dual Selects, and the Ctrl+K / Ctrl+Shift+K shortcut split. Visual spec is locked in 21-UI-SPEC.md.
</objective>

<context>
@.planning/phases/21-session-list/21-CONTEXT.md
@.planning/phases/21-session-list/21-UI-SPEC.md
@.planning/phases/21-session-list/21-01-SUMMARY.md
@.planning/phases/21-session-list/21-02-SUMMARY.md
@src/views/AgentWorkspaceView.tsx
@src/components/ChatPanel.tsx
@src/components/ui/Select.tsx
</context>

<interfaces>
From src/ai/sessionRepo.ts:
```typescript
listSessionsByWorkspace(workspaceId: string | null): Promise<SessionMeta[]>  // SessionMeta: { sessionId, workspaceId, title, titleSource, parentSessionId, forkCutSeq, parentTitle, createdAt, lastActiveAt }
countMessagesBySession(sessionIds: string[]): Promise<Map<string, number>>
```
From src/stores/chatConsoleStore.ts:
```typescript
activeSessionId: string;
isStreaming / loading: boolean; // check actual field name — loading gates streaming
switchSession(sessionId: string): Promise<{ success: boolean; reason?: string }>;
startNewSession: () => { success: boolean; reason?: string };
sessionListVersion: number;
```
From src/stores/workspaceStore.ts:
```typescript
workspaces: Workspace[]; activeWorkspaceId: string | null;
setActiveWorkspaceId: (id: string) => { success: boolean; reason?: string };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: chatPanelMode state + shortcut split</name>
  <files>src/stores/uiStore.ts, src/hooks/useCmdK.ts</files>
  <read_first>src/stores/uiStore.ts, src/hooks/useCmdK.ts</read_first>
  <action>
    uiStore: add `chatPanelMode: 'pure' | 'scoped'` (interface + initial `'pure'`) and `setChatPanelMode: (mode: 'pure' | 'scoped') => void` (`set({ chatPanelMode: mode })`), mirroring setChatPanelOpen style. Optionally fold mode into setChatPanelOpen — plain separate setter is fine.
    useCmdK.ts: in the Ctrl+Shift+K branch add `s.setChatPanelMode('scoped');` before `s.setChatPanelOpen(...)`; in the Ctrl+K branch add `s.setChatPanelMode('pure');`. Update the header comment (Ctrl+Shift+K is no longer "same panel, web fallback" — it is scoped mode; keep Shift+K also serving web fallback).
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - uiStore.ts contains `chatPanelMode: 'pure' | 'scoped'` and `setChatPanelMode`
    - useCmdK.ts contains `setChatPanelMode('scoped')` in the shift branch and `setChatPanelMode('pure')` in the non-shift branch
  </acceptance_criteria>
  <done>Two shortcuts now distinguish panel modes</done>
</task>

<task type="auto">
  <name>Task 2: Real recent-session list in AgentWorkspaceView</name>
  <files>src/views/AgentWorkspaceView.tsx</files>
  <read_first>src/views/AgentWorkspaceView.tsx, .planning/phases/21-session-list/21-UI-SPEC.md, src/ai/sessionRepo.ts, src/lib/utils.ts</read_first>
  <action>
    Per UI-SPEC section 1-2 (follow it exactly):
    1. Delete the `recentTasks` mock array (lines ~27-33).
    2. Add state `sessions: (SessionMeta & { messageCount: number })[]` via useEffect keyed `[activeWorkspaceId, sessionListVersion, activeSessionId]`: `const metas = await getSessionRepo().listSessionsByWorkspace(activeWorkspaceId); const counts = await getSessionRepo().countMessagesBySession(metas.map(m => m.sessionId));` merge, cap `.slice(0, 10)`. Read sessionListVersion + activeSessionId from useChatConsoleStore subscriptions.
    3. Render rows reusing the existing motion.div skeleton but keyed `key={session.sessionId}`, layout per spec: `[Clock 12px] [time text-[11px] w-16 shrink-0] [GitBranch 12px text-accent when parentSessionId, title attr "分支自: {parentTitle}"] [title truncate flex-1 text-sm font-medium] [Badge "{messageCount} 条" variant="neutral" opacity-0 group-hover:opacity-100]`.
    - Title text: `session.title ?? '新对话'` (untitled sessions).
    - Time: `formatRelativeTime(session.lastActiveAt)`.
    - Active row (session.sessionId === activeSessionId): add `bg-accent-subtle/50`, click no-op.
    - Other rows while streaming (loading === true in chatConsoleStore): `opacity-50 pointer-events-none`.
    - onClick: `onClick={() => { void switchSession(session.sessionId); }}`.
    4. Empty state (recent tab, sessions.length === 0): `<div className="text-center text-sm text-text-tertiary py-6">还没有对话，在左侧开始第一句吧</div>` and hide the "查看全部" footer ghost button.
    5. Keep "查看全部" button unchanged (inert placeholder) when list non-empty; keep scheduled tab as-is.
  </action>
  <verify>npm run lint && npm run build</verify>
  <acceptance_criteria>
    - AgentWorkspaceView.tsx contains `listSessionsByWorkspace`, `countMessagesBySession`, `formatRelativeTime`, `switchSession(`
    - No `recentTasks` mock remains (grep 'recentTasks' returns nothing)
    - Row uses `key={session.sessionId}`; time column class `w-16`
    - Empty-state string `还没有对话，在左侧开始第一句吧` present
    - `bg-accent-subtle/50` active-row tint present
  </acceptance_criteria>
  <done>Real list renders with all LIST-01/02 behaviors</done>
</task>

<task type="auto">
  <name>Task 3: ChatPanel scoped selector row</name>
  <files>src/components/ChatPanel.tsx</files>
  <read_first>src/components/ChatPanel.tsx, src/components/ui/Select.tsx, .planning/phases/21-session-list/21-UI-SPEC.md</read_first>
  <action>
    Per UI-SPEC section 3 (follow it exactly):
    1. Subscribe `chatPanelMode` from uiStore; only when `chatPanelMode === 'scoped'` render, directly below DrawerHeader:
       `<div className="flex gap-2 px-4 py-2 border-b border-border-subtle">` containing workspace Select (`className="flex-1 min-w-0"`) and session Select (`className="flex-[1.4] min-w-0"`), triggers h-9, values truncate (min-w-0 + truncate wrapper).
    2. Workspace Select: placeholder `工作区`, items = workspaces names, value = activeWorkspaceId, onValueChange → `useWorkspaceStore.getState().setActiveWorkspaceId(id)` (already streaming-guarded, starts a new session).
    3. Session Select: placeholder `会话`, value = activeSessionId; items = sessions of selected workspace via the same repo query (listSessionsByWorkspace + local sort last_active_at DESC — SQL already returns DESC), item label `{title ?? '新对话'} · {formatRelativeTime(lastActiveAt)}`. First item pinned above a SelectSeparator: `+ 新对话` with Plus icon (12px duotone), value `'__new__'`, onValueChange `'__new__'` → `void startNewSession()` else `void switchSession(v)`. Empty workspace: disabled item `该工作区暂无会话`.
    4. Both Select triggers get `disabled` while chatConsoleStore `loading` is true.
    5. Pure mode: zero added DOM — the row is inside `{chatPanelMode === 'scoped' && (...)}`.
    6. Re-query sessions in an effect keyed `[isChatPanelOpen, chatPanelMode, activeWorkspaceId, sessionListVersion]` so title updates reflect on next open/render.
  </action>
  <verify>npm run lint && npm run build</verify>
  <acceptance_criteria>
    - ChatPanel.tsx contains `chatPanelMode === 'scoped'`, `startNewSession`, `switchSession`, placeholder strings `工作区` and `会话`, `+ 新对话`, `该工作区暂无会话`
    - Selector row is conditional (pure mode adds no DOM)
    - Select triggers have `disabled` tied to streaming/loading state
  </acceptance_criteria>
  <done>Dual-dropdown scoped panel + pure Ctrl+K unchanged</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — session list + scoped panel + auto title</name>
  <action>Human verifies the end-to-end behaviors below; no code changes unless issues reported.</action>
  <what-built>Session list, scoped ChatPanel, auto-titling end-to-end (21-01..21-03)</what-built>
  <how-to-verify>
    Run `npm run tauri:dev` (or web dev). 1) Agent 页最近任务 tab shows real sessions with title/time/count; click a row restores that conversation. 2) Send a first message in a new session; after the turn completes the list row title silently becomes an LLM-generated short title (or truncated first message if API key absent). 3) Press Ctrl+Shift+K: panel shows 工作区+会话 dropdowns; switch workspace in dropdown → session list filters; `+ 新对话` starts a new session. 4) Press Ctrl+K: no dropdowns visible. 5) During streaming: list rows (non-active) and both Selects disabled.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npm run lint && npm run build && npx tsx --test src/ai/__tests__/phase21SessionList.test.ts src/stores/__tests__/phase21Title.test.ts
</verification>

<success_criteria>
All five phase success criteria observable in the running app; QUICK-03 verified by DOM absence in pure mode.
</success_criteria>

<output>
Create `.planning/phases/21-session-list/21-03-SUMMARY.md`
</output>
