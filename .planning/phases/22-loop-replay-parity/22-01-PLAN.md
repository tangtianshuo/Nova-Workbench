---
phase: 22-loop-replay-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/adr/ADR-0003-rust-run-engine.md, src/ai/sessionRestore.ts, src/ai/registry.ts, src-tauri/Cargo.toml, src-tauri/src/engine/mod.rs, src-tauri/src/engine/db.rs, src/ai/__tests__/fixtures/.gitkeep, src/ai/__tests__/fixtures/golden-tokenEstimate.json, src/ai/__tests__/fixtures/golden-paramsHash.json]
autonomous: true
requirements: [PORT-01, ENG-03]
must_haves:
  truths:
    - "PORT-01 协议三要素(幂等分类字段/孤儿第三态/unknown 先验证再重跑)在 ADR-0003 附则定稿且 TS 侧同步实现"
    - "Rust 引擎模块骨架可编译,依赖就位,DB 路径 probe 有可执行验证"
    - "fixture 单源目录存在,含 TS 生成的算法金样本"
  artifacts:
    - path: "docs/adr/ADR-0003-rust-run-engine.md"
      provides: "PORT-01 协议附则"
      contains: "idempotency"
    - path: "src-tauri/src/engine/db.rs"
      provides: "rusqlite 连接 + schema_version 断言"
      contains: "schema_version"
    - path: "src/ai/__tests__/fixtures/"
      provides: "双侧共享 fixture 单源"
  key_links:
    - from: "src/ai/sessionRestore.ts"
      to: "PORT-01 协议"
      via: "interrupted marker payload 含 status:'unknown'"
      pattern: "\"status\":\"unknown\""
---

<objective>
PORT-01 协议定稿(SC-1 硬性要求:引擎动手前锁协议)+ Wave 0 基建(Rust 依赖、engine 模块骨架、DB 路径 probe、fixture 单源目录与算法金样本)。

Purpose: 协议改在引擎建好后最贵;基石金样本决定后续逐位复刻的验收基准。
Output: ADR-0003 附则 + TS 同步改动 + engine/ 骨架 + fixtures/ 目录
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@22-CONTEXT.md
@22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PORT-01 协议定稿 — ADR 附则 + TS sessionRestore 第三态 + 工具描述约定</name>
  <files>docs/adr/ADR-0003-rust-run-engine.md, src/ai/sessionRestore.ts, src/ai/registry.ts</files>
  <read_first>
  - docs/adr/ADR-0003-rust-run-engine.md(现状与「协议决策」节)
  - src/ai/sessionRestore.ts(规格源:孤儿 marker 现有 payload)
  - src/ai/registry.ts(工具 schema/description 生成处)
  - 22-RESEARCH.md §PORT-01 协议草案
  </read_first>
  <action>
  1. ADR-0003 追加「附则 A:孤儿 tool_result 第三态与幂等分类协议(PORT-01)」,内容:
     - tool_call payload 新增字段 `idempotency: 'rerunnable' | 'verify_first'`(随 tool_call 事件 payload_json 落盘,不建表);旧事件无该字段时读取侧一律视为 `verify_first`。
     - 孤儿 tool_result 第三态:崩溃恢复追加的 marker payload 为 `{ok:false, interrupted:true, status:'unknown', reason:'app-restart'}`,modelText 为 `[tool_result <name>] {"ok":false,"status":"unknown","interrupted":true,"reason":"app restarted before tool completion"}`(键序即此序,ok 不再前置 — unknown 语义区别于 error)。模型不得假定命令失败。
     - 工具描述约定:工具 description 生成模板追加一句:「若 tool_result 状态为 unknown,先验证(如查看文件/状态)再决定是否重跑;verify_first 类命令禁止未验证直接重跑」。
     - Phase 22 工具集边界(orchestrator 裁决):PM CRUD 工具在 Phase 22 期间从模型 schema 消失(TOOL-04 降级先例),Phase 23 经 TS 桥恢复。
  2. src/ai/sessionRestore.ts:孤儿 tool_call marker 改为上述 unknown 语义(同步改,保双侧 parity;reason 文案 'app-restart' 保持)。受影响的既有 TS 测试(sessionRestore 相关)同步更新期望值。
  3. src/ai/registry.ts:工具 description 生成处加 PORT-01 约定文案(中文或与现有 description 语言一致,一句话)。
  </action>
  <verify>
    <automated>node --test src/ai/__tests__/ && grep -c '"status":"unknown"' src/ai/sessionRestore.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep "idempotency" docs/adr/ADR-0003-rust-run-engine.md` 命中 ≥3 处
  - `grep '"status":"unknown"' src/ai/sessionRestore.ts` 命中(modelText 内嵌 JSON)
  - `grep -c "verify_first" docs/adr/ADR-0003-rust-run-engine.md` ≥2
  - `node --test src/ai/__tests__/` 全绿(TS 规格源测试未破坏)
  </acceptance_criteria>
  <done>PORT-01 三要素定稿落 ADR,TS 侧 unknown 第三态与工具描述约定同步实现,TS 测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 基建 — Rust 依赖 + engine 骨架 + DB probe + fixture 金样本</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/engine/mod.rs, src-tauri/src/engine/db.rs, src/ai/__tests__/fixtures/golden-tokenEstimate.json, src/ai/__tests__/fixtures/golden-paramsHash.json</files>
  <read_first>
  - src-tauri/Cargo.toml(现有依赖)
  - src-tauri/src/lib.rs(builder/setup 结构,plugin-sql 注册点)
  - src/ai/tokenEstimate.ts + src/ai/paramsHash.ts(金样本规格源)
  - 22-RESEARCH.md §SQLite 并发结论、§fixture 机制
  </read_first>
  <action>
  1. Cargo.toml [dependencies] 新增:`rusqlite = { version = "0.32", features = ["bundled"] }`、`sha2 = "0.10"`、`chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }`、`unicode-normalization = "0.1"`。
  2. 建 `src-tauri/src/engine/mod.rs`(空模块声明:pub mod db; 后续 plan 逐个加)与 `engine/db.rs`:
     ```rust
     pub fn open(path: &std::path::Path) -> rusqlite::Result<Connection> // open + busy_timeout(5000) + PRAGMA journal_mode=WAL + foreign_keys(true)
     pub fn assert_schema(conn: &Connection) -> Result<()> // SELECT schema_version FROM meta,断言 >= 7,否则返回错误 "engine: schema_version {v} < 7"
     ```
     lib.rs 暂不接线(接线在 22-06)。
  3. DB 路径 probe:`#[test] fn probe_db_path()` 用 `std::env::var("NOVA_DB")`(开发机 app config dir 下 nova.db 绝对路径)打开连接并 assert_schema 通过;`#[ignore]` 标注(需真实 DB,UAT 跑)。同时在 db.rs 顶部注释记录 probe 结论:tauri-plugin-sql `"sqlite:nova.db"` 实际解析目录(app_config vs app_data,执行时用 tauri app handle 打日志或手动确认 dev DB 位置,结论写死为常量 `pub fn db_path(app: &tauri::AppHandle) -> PathBuf`)。
  4. fixture 单源:建 `src/ai/__tests__/fixtures/`。生成两个金样本 JSON(用一次性 node 脚本从 TS 函数产出后固化):
     - `golden-tokenEstimate.json`:`[{ "text": "...", "expectedTokens": n }, ...]`,至少 8 例:纯 ASCII、纯 CJK(含 U+3400-U+4DBF 扩展A)、CJK 标点(U+3000-303F)、全角(U+FF00-FFEF)、emoji(👑 BMP 外,锁定 UTF-16 vs char 决策)、混合、空串、长文本。
     - `golden-paramsHash.json`:`[{ "params": {...}, "expectedHash": "hex" }, ...]`,至少 6 例:键乱序嵌套对象、数组保序、null、整数/浮点(含 1.0 边界)、空对象、TS 既有存量 hash 一例(从开发 DB agent_confirmation_candidates.params_hash 取一条真实值配其 params_json)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && ls src/ai/__tests__/fixtures/</automated>
  </verify>
  <acceptance_criteria>
  - `cargo test` 编译通过且 `probe_db_path` 出现在测试列表(ignored)
  - `grep "rusqlite" src-tauri/Cargo.toml` 命中,features 含 bundled
  - `src/ai/__tests__/fixtures/golden-tokenEstimate.json` 存在且 ≥8 条,含 emoji 用例
  - `src/ai/__tests__/fixtures/golden-paramsHash.json` 存在且 ≥6 条,含一条真实存量 hash
  - `grep "schema_version" src-tauri/src/engine/db.rs` 命中
  </acceptance_criteria>
  <done>依赖就位、engine 骨架可编译、DB probe 有可执行验证、双侧共享金样本落盘</done>
</task>

</tasks>

<verification>
- ADR-0003 附则含 idempotency/unknown/verify 三要素
- cargo test 编译通过;TS 测试全绿
- fixtures/ 目录与金样本就位
</verification>

<success_criteria>
- PORT-01 协议文档化 + TS 实现,SC-1 达成
- Wave 0 缺口(依赖/fixture/probe)全部关闭
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-01-SUMMARY.md`
</output>
