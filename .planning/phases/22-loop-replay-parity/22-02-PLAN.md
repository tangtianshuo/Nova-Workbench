---
phase: 22-loop-replay-parity
plan: 02
type: execute
wave: 1
depends_on: ["22-01"]
files_modified: [src-tauri/src/engine/mod.rs, src-tauri/src/engine/token_estimate.rs, src-tauri/src/engine/fts_tokens.rs, src-tauri/src/engine/params_hash.rs]
autonomous: true
requirements: [ENG-03]
must_haves:
  truths:
    - "tokenEstimate / ftsTokens / paramsHash 三个算法函数与 TS 输出逐位一致(金样本锁定)"
  artifacts:
    - path: "src-tauri/src/engine/token_estimate.rs"
      provides: "CJK 感知 token 估算"
      exports: ["estimate_tokens"]
    - path: "src-tauri/src/engine/fts_tokens.rs"
      provides: "CJK 切分 + FTS match 串"
      exports: ["fts_tokens", "fts_match_string"]
    - path: "src-tauri/src/engine/params_hash.rs"
      provides: "canonical JSON + SHA-256"
      exports: ["canonical_json", "params_hash"]
  key_links:
    - from: "src-tauri/src/engine/params_hash.rs"
      to: "src/ai/__tests__/fixtures/golden-paramsHash.json"
      via: "#[test] 遍历金样本断言 hex 相等"
      pattern: "golden-paramsHash"
---

<objective>
三个「逐位复刻」算法基石移植 Rust 并用 22-01 金样本锁定:tokenEstimate(CJK 感知估算)、ftsTokens(FTS5 切分)、paramsHash(canonical JSON + SHA-256)。

Purpose: 这三个函数被 loop/compaction/assembler/confirmations 全线依赖,是 parity 的底座。
Output: engine/ 下三个纯函数模块 + 金样本测试全绿
</objective>

<context>
@22-RESEARCH.md
@src/ai/tokenEstimate.ts
@src/ai/ftsTokens.ts
@src/ai/paramsHash.ts
<interfaces>
金样本(22-01 已建,直接读):
- src/ai/__tests__/fixtures/golden-tokenEstimate.json: [{ text, expectedTokens }]
- src/ai/__tests__/fixtures/golden-paramsHash.json: [{ params, expectedHash }]
fixture 路径常量: const FIXTURES: &str = concat!(env!("CARGO_MANIFEST_DIR"), "/../src/ai/__tests__/fixtures");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: token_estimate.rs + fts_tokens.rs 逐位复刻</name>
  <files>src-tauri/src/engine/token_estimate.rs, src-tauri/src/engine/fts_tokens.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/tokenEstimate.ts:9-15(规格源)
  - src/ai/ftsTokens.ts:7-24(规格源)
  - src/ai/__tests__/fixtures/golden-tokenEstimate.json(期望值)
  - 22-RESEARCH.md §算法逐位复刻 #1 #2
  </read_first>
  <behavior>
  - golden-tokenEstimate.json 每例: estimate_tokens(text) == expectedTokens(含 emoji 用例 — 按 chars() 计;若金样本 emoji 例失败,改 text.encode_utf16().count() 对齐并在代码注释记录)
  - fts_tokens("Hello 世界abc") 提取 ["hello","abc","世","界"](保序去重,lowercase+NFKC 先小写后归一)
  - fts_match_string 每个 token 双引号包裹、内部 " 转 ""、空格 join
  </behavior>
  <action>
  1. `token_estimate.rs`:
     ```rust
     fn is_cjk(c: char) -> bool // matches!: '\u{2E80}'..='\u{2EFF}' | '\u{3000}'..='\u{303F}' | '\u{3040}'..='\u{30FF}' | '\u{3400}'..='\u{4DBF}' | '\u{4E00}'..='\u{9FFF}' | '\u{F900}'..='\u{FAFF}' | '\u{FF00}'..='\u{FFEF}'
     pub fn estimate_tokens(text: &str) -> i64 // ceil(cjk + non_cjk as f64 / 4.0)
     ```
  2. `fts_tokens.rs`:
     - normalize: `text.to_lowercase().nfkc()`(unicode-normalization crate,先小写后归一,顺序复刻 TS)
     - 提取:连续 `[a-z0-9]+` 段 whole + `'\u{3400}'..='\u{9FFF}'` 逐字;去重保序(Vec + contains 即可)
     - `pub fn fts_tokens(text: &str) -> Vec<String>`;`pub fn fts_match_string(tokens: &[String]) -> String`
  3. mod.rs 注册 `pub mod token_estimate; pub mod fts_tokens;`
  4. 测试:金样本逐例断言 + behavior 中的手工用例 + 一个含 NFKC 敏感字符(如全角 `Ａ`)用例。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml token_ fts_</automated>
  </verify>
  <acceptance_criteria>
  - `grep "fn estimate_tokens" src-tauri/src/engine/token_estimate.rs` 命中
  - `grep "nfkc" src-tauri/src/engine/fts_tokens.rs` 命中
  - `cargo test token_estimate` 通过,金样本 ≥8 例全断言
  - `cargo test fts` 通过
  </acceptance_criteria>
  <done>两个算法函数与 TS 金样本逐位一致</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: params_hash.rs(canonical JSON + SHA-256)</name>
  <files>src-tauri/src/engine/params_hash.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/paramsHash.ts:9-36(规格源:canonical 规则 + hash)
  - src/ai/__tests__/fixtures/golden-paramsHash.json(期望值)
  - 22-RESEARCH.md §算法逐位复刻 #3
  </read_first>
  <behavior>
  - golden-paramsHash.json 每例: params_hash(&Value) 输出小写 hex == expectedHash(含真实存量 hash 例 — 跨引擎 dedup 兼容的生命线)
  - canonical_json: 对象键字典序、数组保序、null 保留
  </behavior>
  <action>
  1. `params_hash.rs`:按 RESEARCH Code Examples 实现
     ```rust
     pub fn canonical_json(v: &serde_json::Value) -> String // 递归:Null/Bool/Number/String 用 v.to_string();Array join ",";Object 按 Map 迭代序(serde_json 默认 BTreeMap = 字典序,勿启用 preserve_order)
     pub fn params_hash(v: &serde_json::Value) -> String // sha2::Sha256(canonical_json) 小写 hex
     ```
  2. 测试:金样本逐例 + 键序无关断言(同一对象两种插入序的 Value canonical 后相同 — 构造时用不同顺序的两个 json! 字面量)。
  3. mod.rs 注册。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml params_hash</automated>
  </verify>
  <acceptance_criteria>
  - `grep "fn canonical_json" src-tauri/src/engine/params_hash.rs` 命中
  - `cargo test params_hash` 通过,含真实存量 hash 金样本例
  </acceptance_criteria>
  <done>paramsHash 与 TS 逐位兼容,存量候选 dedup 跨引擎可用</done>
</task>

</tasks>

<verification>
`cargo test` 全绿;三个基石模块就位,后续 plan 直接依赖
</verification>

<success_criteria>
算法基石逐位复刻完成,金样本永久锁定
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-02-SUMMARY.md`
</output>
