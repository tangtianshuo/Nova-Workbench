---
phase: 22-loop-replay-parity
plan: 04
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified: [src-tauri/src/engine/mod.rs, src-tauri/src/engine/chat_session.rs, src-tauri/src/engine/compaction.rs, src-tauri/src/engine/fork.rs, src/ai/__tests__/fixtures/projection-cases.json]
autonomous: true
requirements: [ENG-03]
must_haves:
  truths:
    - "Rust ChatSession 投影(getMessagesForLLM)对同一事件流输出与 TS 逐位一致"
    - "压缩决策(tokenPressure/findSplitPoint)与 fork remap(seq 归一化)语义与 TS 一致"
  artifacts:
    - path: "src-tauri/src/engine/chat_session.rs"
      provides: "groupIntoTurns/trim/rebuildMessages/fromEvents/getMessagesForLLM 投影"
      exports: ["ChatSession", "from_events", "get_messages_for_llm"]
    - path: "src-tauri/src/engine/compaction.rs"
      provides: "maybeCompactSession + forkOffset child-space 落盘"
      exports: ["maybe_compact_session", "token_pressure", "find_split_point"]
    - path: "src-tauri/src/engine/fork.rs"
      provides: "fork 事件流拼接 + seq remap"
      exports: ["resolve_session_events", "fork_cut_seq"]
    - path: "src/ai/__tests__/fixtures/projection-cases.json"
      provides: "投影 parity 用例(事件流 + 期望 getMessagesForLLM 输出)"
  key_links:
    - from: "src-tauri/src/engine/chat_session.rs"
      to: "src-tauri/src/engine/compaction.rs"
      via: "fromEvents compaction-aware 重建 + formatCompactionSummary 前置"
      pattern: "format_compaction_summary"
---

<objective>
投影与压缩移植:chat_session(纯投影函数群)、compaction(0.8× 窗口配对边界压缩 + forkOffset child space)、fork(seq remap 递归 resolve),并落首批投影 parity 用例 fixture。

Purpose: 投影是 parity 测试的对比对象(锁定决策:只比投影不比事件序列)。
Output: 三个纯函数模块 + projection-cases.json 双侧用例
</objective>

<context>
@22-RESEARCH.md
@src/ai/chatSession.ts
@src/ai/compaction.ts
@src/ai/fork.ts
<interfaces>
来自 22-02: engine::token_estimate::estimate_tokens(&str) -> i64
AgentEvent 类型在 22-03 event_log.rs 定义(payload: serde_json::Value)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chat_session.rs — 投影函数群 1:1 移植</name>
  <files>src-tauri/src/engine/chat_session.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/chatSession.ts 全文(规格源,重点 45-47/54-111/166-201/252-317/330-343)
  - 22-RESEARCH.md §编码映射表 chatSession 行
  </read_first>
  <behavior>
  - groupIntoTurns/trimToBudget/trimOversizedTurn/collapseToolCallAssistants 对既有 TS 测试用例输出一致
  - DEFAULT_MAX_TURNS=8、budget=8000
  - formatCompactionSummary 输出串与 chatSession.ts:45-47 模板逐字一致
  - get_messages_for_llm:compaction 摘要前置
  - lazy session_created:链序 session_created 先于首条消息(once-guard)
  </behavior>
  <action>
  1. 逐函数移植:常量 DEFAULT_MAX_TURNS=8、消息预算 8000;formatCompactionSummary 格式串**逐字复刻**(执行时从 chatSession.ts:45-47 抄,含空格/换行)。
  2. `ChatSession` struct + `from_events(&[AgentEvent]) -> ChatSession`(compaction-aware rebuildMessages)+ `get_messages_for_llm() -> Vec<LlmMessage { role, content }>`。
  3. addMessage 双写 / appendAuxEvent 语义并入 loop 的事件队列顺序保证(本 task 只留接口注释,实现在 22-05)。
  4. 测试:从 TS chatSession 测试(src/ai/__tests__/ 下相关文件)逐用例翻成 cargo test;执行时先跑 `node --test` 确认期望值,再照抄。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml chat_session</automated>
  </verify>
  <acceptance_criteria>
  - `grep "8000" src-tauri/src/engine/chat_session.rs` 与 `grep "DEFAULT_MAX_TURNS" src-tauri/src/engine/chat_session.rs`(或常量=8)命中
  - `grep "fn get_messages_for_llm" src-tauri/src/engine/chat_session.rs` 命中
  - `cargo test chat_session` 通过,用例数 ≥ TS chatSession 测试用例数
  </acceptance_criteria>
  <done>投影函数群与 TS 语义一致,为 parity harness 提供对比对象</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compaction.rs + fork.rs + 首批 projection-cases.json</name>
  <files>src-tauri/src/engine/compaction.rs, src-tauri/src/engine/fork.rs, src/ai/__tests__/fixtures/projection-cases.json, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/compaction.ts:15-181(规格源,重点 145/151/171 forkOffset 注释)
  - src/ai/fork.ts:27-83(规格源:SEQ_PAYLOAD_FIELDS remap)
  - src/ai/__tests__/ 下 compaction/fork 相关测试(用例源)
  - 22-RESEARCH.md §算法 #4 + 风险 #8
  </read_first>
  <behavior>
  - 常量 0.8 / 0.5 / 12000 / 2000 与 TS 一致;clampLines「首行必保」(kept.length>0 才 break)
  - fork 后再压缩:payload 字段在 child space(减 forkOffset)落盘 — fixture 必含此用例
  - fork remap:仅 splitSeq/coveredSeqStart/coveredSeqEnd 为数字时 remap,prefix identity、child += prefix.length;递归 resolve;invalid 三因与 TS 一致
  </behavior>
  <action>
  1. `compaction.rs`:`token_pressure` / `find_split_point` / `build_transcript` / `maybe_compact_session`;forkOffset = 非本 session 事件数;compaction_started/completed payload 字段在 child space 减 offset 落盘。
  2. `fork.rs`:`const SEQ_PAYLOAD_FIELDS: [&str; 3] = ["splitSeq", "coveredSeqStart", "coveredSeqEnd"]`;`resolve_session_events`(递归)、`fork_cut_seq`。
  3. `projection-cases.json`:数组,每项 `{ name, fork: {parentSessionId, cutSeq} | null, budget?: number, events: [...], expectedMessages: [{role, content}] }`。用 node 一次性脚本调 TS ChatSession.fromEvents(...).getMessagesForLLM() 生成 expectedMessages 后固化。至少 8 例:基础对话、tool 配对、HITL pending、压缩触发(小 budget)、fork、**fork 后再压缩**、恢复(interrupted/unknown marker,与 22-01 新语义一致)、幂等分类 payload 用例。
  4. 测试:cargo test 遍历 projection-cases.json,`from_events(...).get_messages_for_llm()` 与 expectedMessages 逐字段断言(role/content 字符串相等比较)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml projection</automated>
  </verify>
  <acceptance_criteria>
  - `grep "12000" src-tauri/src/engine/compaction.rs` 命中;`grep "splitSeq" src-tauri/src/engine/fork.rs` 命中
  - `src/ai/__tests__/fixtures/projection-cases.json` 存在且 ≥8 例,含 "fork 后再压缩" 用例(name 含 fork-compaction)
  - `cargo test projection` 通过
  </acceptance_criteria>
  <done>压缩与 fork 语义移植完成,投影 parity 用例双侧锁定</done>
</task>

</tasks>

<verification>
`cargo test` 全绿;projection-cases.json 可被后续 TS 侧 parity 测试复用(单源)
</verification>

<success_criteria>
投影/压缩/fork 三个纯函数域与 TS 逐位一致,ENG-03 对比对象就绪
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-04-SUMMARY.md`
</output>
