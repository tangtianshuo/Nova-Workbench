---
phase: 22-loop-replay-parity
plan: 05
type: execute
wave: 3
depends_on: ["22-03", "22-04"]
files_modified: [src-tauri/src/engine/mod.rs, src-tauri/src/engine/context_assembler.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs, src-tauri/src/engine/channel.rs]
autonomous: true
requirements: [ENG-01, ENG-04]
must_haves:
  truths:
    - "contextAssembler 五段注入产出与 TS 相同结构的 context_injected 事件 payload"
    - "最小 Rust 工具集:knowledge_search(只读 FTS5)+ knowledge_write/destructive 候选生成(触发 HITL)"
    - "loop_runner 完整 run_tool_loop 语义:MAX_ITERATIONS=5、四种 tool 落库 payload 逐字段、endTurn 四种 outcome、turn-end audit"
  artifacts:
    - path: "src-tauri/src/engine/context_assembler.rs"
      provides: "五段注入 + FTS5/memory 只读查询"
      exports: ["assemble_context"]
    - path: "src-tauri/src/engine/tools.rs"
      provides: "Rust 工具注册表 + 最小工具集"
      exports: ["schemas", "execute"]
    - path: "src-tauri/src/engine/loop_runner.rs"
      provides: "run_tool_loop 主循环"
      exports: ["run_tool_loop"]
    - path: "src-tauri/src/engine/channel.rs"
      provides: "EngineEvent Channel 消息 enum"
      exports: ["EngineEvent"]
  key_links:
    - from: "src-tauri/src/engine/loop_runner.rs"
      to: "src-tauri/src/llm.rs"
      via: "直接调用 llm::chat_with_tools(已支持 tool-call 聚合)"
      pattern: "chat_with_tools"
    - from: "src-tauri/src/engine/tools.rs"
      to: "src-tauri/src/engine/confirmations.rs"
      via: "knowledge_write/destructive 工具产生候选"
      pattern: "create_candidate"
---

<objective>
引擎运行时组装:context_assembler(五段注入 + FTS5 检索 Rust 化)、最小 Rust 工具集(orchestrator 裁决:不提前建 TS 桥)、loop_runner(toolLoop 主循环移植 + EngineEvent channel)。

Purpose: ENG-01 主体 — Rust 完成整轮 agent loop 的执行核心。
Output: 四个模块,loop 可被 Tauri command 驱动(接线在 22-06)
</objective>

<context>
@22-RESEARCH.md
@src/ai/contextAssembler.ts
@src/ai/toolLoop.ts
@src/ai/registry.ts
@src/ai/memoryStore.ts
@src/ai/knowledgeRepo.ts
@src-tauri/src/llm.rs
<interfaces>
来自 22-02/03/04:
- engine::{token_estimate, fts_tokens, params_hash, event_log, confirmations}
- engine::chat_session::ChatSession / from_events / get_messages_for_llm
- engine::compaction::maybe_compact_session
来自 llm.rs(已存在,直接调): llm::chat_with_tools(...) -> ChatResult{ content, tool_calls }; ToolCallInfo{ name, arguments }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: context_assembler.rs — 五段注入 + 只读检索 Rust 化</name>
  <files>src-tauri/src/engine/context_assembler.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/contextAssembler.ts:37-160(规格源:五段配额与文案)
  - src/ai/memoryStore.ts:360-...(规格源:listActiveMemories/listPending/listRejected SQL)
  - src/ai/knowledgeRepo.ts:327-416(规格源:searchKnowledgeHybrid SQL)
  - 22-RESEARCH.md §编码映射表 + §算法 #5(clampToTokens char 边界)
  </read_first>
  <behavior>
  - 常量 RECENT_DIALOG_RESERVED=1200、FTS_TOP_K=5、REJECTED_LIMIT=5、段配额 core600/pending200/memories500/fts400/recent300
  - 中文标题文案逐字:如 `## 待确认记忆候选`(执行时从 contextAssembler.ts 全部标题串照抄)
  - clampToTokens 二分在 char 边界;fixture 锁一个 CJK+emoji 用例
  - 检索失败(knowledge FTS 报错)降级为空段(Result 降级,不 panic)
  </behavior>
  <action>
  1. `assemble_context(conn, session_id, user_message) -> Value`:产出与 TS 相同形状的 context_injected payload(段结构 + audit 字段集与 TS 逐字段一致 — 执行时以 contextAssembler.ts 输出构造处为准照抄)。
  2. SQL 照抄:memoryStore listActiveMemories(newest-first,overflow drop tail)/listPending/listRejected;knowledgeRepo searchKnowledgeHybrid(knowledge_fts MATCH + superseded_at IS NULL + filters),MATCH 串用 engine::fts_tokens::fts_match_string。
  3. clampToTokens:char 边界二分;补 fixture 用例(CJK+emoji)进 projection-cases 或独立 golden。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml context_assembler</automated>
  </verify>
  <acceptance_criteria>
  - `grep "1200" src-tauri/src/engine/context_assembler.rs` 与 `grep "待确认记忆候选" src-tauri/src/engine/context_assembler.rs` 命中
  - `grep "knowledge_fts" src-tauri/src/engine/context_assembler.rs` 命中
  - `cargo test context_assembler` 通过(内存库 + 0004/0005 migration)
  </acceptance_criteria>
  <done>组 prompt 的读路径全 Rust 化,parity 可过</done>
</task>

<task type="auto">
  <name>Task 2: tools.rs — 最小 Rust 工具集(orchestrator 裁决边界)</name>
  <files>src-tauri/src/engine/tools.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/registry.ts(规格源:schema 生成形态与 PORT-01 描述约定落点)
  - src/ai/toolLoop.ts 中 knowledge 工具调用点(候选生成语义参考)
  - 22-CONTEXT.md 裁决:Phase 22 工具集 = 最小 Rust 原生,PM CRUD 从 schema 消失,Phase 23 桥恢复
  </read_first>
  <action>
  1. 注册表:静态表 `[(name, description, parameters_schema, kind)]` + `pub fn schemas() -> Vec<Value>`(LLM tools 形态与 TS registry.ts schema 生成一致 — name/description/parameters 三键)。
  2. 注册工具(全部 Rust 原生执行,无 TS 桥):
     - `knowledge_search`(只读):参数 `{ query: string, limit?: number }`;执行 = context_assembler 的 searchKnowledgeHybrid 查询,返回结果列表 JSON。idempotency='rerunnable'。
     - `knowledge_write`(写类,触发 HITL):执行 = confirmations::create_candidate(kind='knowledge_write'),返回 WAIT 形态(由 loop_runner 落 `{ok:false,awaitingConfirmation:true,...}` payload)。idempotency='verify_first'。
     - `memory_write` 同型(kind 走 memory_candidates 写者)。idempotency='verify_first'。
  3. idempotency 字段进 tool_call 事件 payload(PORT-01,22-01 协议);所有 description 尾部带 PORT-01 约定文案(与 22-01 Task1 registry.ts 文案一致)。
  4. **PM CRUD 工具(createTask/scheduleCrud 等)不注册** — schema 列表消失即能力降级,Phase 23 恢复(裁决照办,代码注释标记)。
  5. `pub fn execute(conn, name, args, ctx) -> ToolOutcome`,ToolOutcome = Executed(Value) | AwaitConfirmation { candidate } 。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml tools</automated>
  </verify>
  <acceptance_criteria>
  - `grep "knowledge_search" src-tauri/src/engine/tools.rs` 命中
  - `grep "verify_first" src-tauri/src/engine/tools.rs` 命中 ≥2
  - `grep -c "createTask\|scheduleCrud" src-tauri/src/engine/tools.rs` = 0(PM CRUD 未注册)
  - `cargo test tools` 通过(候选生成断言)
  </acceptance_criteria>
  <done>Phase 22 最小工具集就位:1 只读 + HITL 写类,可验证 ENG-04 候选推送</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: loop_runner.rs + channel.rs — runToolLoop 主循环移植</name>
  <files>src-tauri/src/engine/loop_runner.rs, src-tauri/src/engine/channel.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/toolLoop.ts:110-282(规格源:主循环)
  - src/ai/toolLoop.ts:47-58(knowledge lazy + 降级 [])
  - src/ai/toolLoop.ts:200/210/247-248/264-267(规格源:四种落库 payload 逐字段)
  - src-tauri/src/llm.rs:337-410(ToolCallDeltaAccumulator — 已支持,勿重建)
  - src-tauri/src/commands.rs(StreamChunk 模式参考)
  - 22-RESEARCH.md §Channel 消息协议设计
  </read_first>
  <behavior>
  - MAX_ITERATIONS=5;超出 truncated=true
  - 四种 tool 落库 payload 逐字段:
    tool_call: `{toolCallId, toolName, args, content}`(含 idempotency — PORT-01)
    WAIT: `{ok:false, awaitingConfirmation:true[, summary|error]}`,modelText 前缀 `[tool_result <name>] `
    错误: `{ok:false, error, retryAvailable}` + errorText 重试提示文案逐字(从 toolLoop.ts 抄)
    成功: toolLoop.ts:200 形态照抄
  - endTurn outcome 四种;turn-end 时 check_event_stream 审计失败即返回 error
  - endTurn 后 fire-and-forget session upsert(同步落库即可)
  - cancellation token 取消后:停止迭代,已有事件保持一致状态(不写半截 payload)
  </behavior>
  <action>
  1. `channel.rs`:按 RESEARCH §Channel 协议逐字定义 `EngineEvent` enum(token/tool_start/tool_end/event/confirmation/done/error 七变体,serde tag="kind" content="data")。
  2. `loop_runner.rs`:`pub async fn run_tool_loop(ctx: LoopContext, cancel: CancellationToken, on_event: impl Fn(EngineEvent)) -> Result<EngineRunResult>`。
     - LoopContext{ conn, session_id, user_message, workspace_id, product_id, provider, model }
     - EngineRunResult 与 TS ToolLoopResult 同形:{ content, iterations, tool_calls_executed, truncated, pending_confirmation }
     - 流程:user_message 落库 → assemble_context 注入 → llm::chat_with_tools(流式 token 经 on_event 发 Token)→ tool_calls 逐个:落 tool_call 事件 → tools::execute → 按四形态落 tool_result → maybe_compact_session → 循环;无 tool_calls 即 endTurn。
     - 每次 append 后发 `EventCommitted { seq, event_type }`;候选生成发 `Confirmation { candidate }`;工具开始/结束发 ToolStart/ToolEnd(confirmation 时 ok=true)。
  3. 测试:mock LLM(LoopContext 注入 LlmFn trait 或 fn 指针,测试返回固定 tool_calls)覆盖:纯回复轮、单工具轮、WAIT 轮(payload 断言)、超迭代、审计失败、取消。payload 形状逐字段断言(serde_json::json! 对比)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml loop_runner</automated>
  </verify>
  <acceptance_criteria>
  - `grep "MAX_ITERATIONS" src-tauri/src/engine/loop_runner.rs` 命中(=5)
  - `grep "awaitingConfirmation" src-tauri/src/engine/loop_runner.rs` 命中
  - `grep "enum EngineEvent" src-tauri/src/engine/channel.rs` 命中,含七个变体
  - `cargo test loop_runner` 通过,含 WAIT payload 与取消测试
  </acceptance_criteria>
  <done>整轮 agent loop 可在 Rust 内完成(不经 TS toolLoop),ENG-01 核心就绪待接线</done>
</task>

</tasks>

<verification>
`cargo test` 全绿;引擎核心(上下文/工具/循环)完整,只差 Tauri command 接线
</verification>

<success_criteria>
ENG-01 主体完成;最小工具集验证 ENG-04 候选路径
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-05-SUMMARY.md`
</output>
