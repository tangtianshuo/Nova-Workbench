---
phase: 22-loop-replay-parity
plan: 06
type: execute
wave: 4
depends_on: ["22-05"]
files_modified: [src-tauri/src/engine/mod.rs, src-tauri/src/engine/restore.rs, src-tauri/src/engine/commands.rs, src-tauri/src/state.rs, src-tauri/src/lib.rs, src/stores/chatConsoleStore.ts, src/components/CmdKPalette.tsx, src/ai/api.ts]
autonomous: true
requirements: [ENG-01, ENG-02, ENG-04, ENG-05]
must_haves:
  truths:
    - "用户在现有 ChatPanel 发起对话走 engine_run,全程不经 TS runToolLoop"
    - "确认卡片确认后工具重执行落库走 engine_append_tool_result(最细接缝,不漏)"
    - "启动崩溃恢复:latest session 尾切 + 孤儿 tool_call 追加 unknown marker,绝不重执行"
    - "runToolLoop 运行时调用点归零(grep 断言)"
  artifacts:
    - path: "src-tauri/src/engine/restore.rs"
      provides: "尾切/孤儿 unknown marker/预算重建/pending 重现"
      exports: ["restore_session"]
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_run/engine_cancel/engine_confirm_candidate/engine_reject_candidate/engine_append_tool_result"
      exports: ["engine_run", "engine_cancel", "engine_confirm_candidate", "engine_reject_candidate", "engine_append_tool_result"]
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "engine_run command"
      via: "invoke('engine_run', ...) + Channel<EngineEvent>"
      pattern: "engine_run"
    - from: "src/stores/chatConsoleStore.ts confirm 流"
      to: "engine_append_tool_result command"
      via: "executeTool(留 TS)后事件落库走 Rust 唯一写者"
      pattern: "engine_append_tool_result"
---

<objective>
接线切换与崩溃恢复:engine_* 五个 Tauri command、run 注册表、启动恢复;TS 侧 ChatPanel/CmdK 两个调用点直接切换(无配置开关),确认后重执行落库走 Rust,TS 运行时写路径归零。

Purpose: SC-2/4/5 的用户可观察交付点。
Output: 切换完成的应用 + restore 模块 + grep 断言
</objective>

<context>
@22-RESEARCH.md
@22-CONTEXT.md(锁定:直接切换、读路径不动、Channel 模式、cancel 语义)
@src/stores/chatConsoleStore.ts
@src/ai/sessionRestore.ts
@src-tauri/src/commands.rs
@src-tauri/src/state.rs
@src-tauri/src/lib.rs
<interfaces>
来自 22-05:
- engine::loop_runner::run_tool_loop(LoopContext, CancellationToken, on_event) -> EngineRunResult
- engine::channel::EngineEvent(token/tool_start/tool_end/event/confirmation/done/error)
- engine::confirmations::{confirm, consume, reject, create_candidate}
- engine::event_log::{append, list_events, check_event_stream}
现 TS 侧(保留只读):
- chatConsoleStore fromEvents 投影刷新函数(读路径不动)
- confirm 流 executeTool(chatConsoleStore.ts:527-537)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: restore.rs + engine commands + run 注册表 + setup 接线</name>
  <files>src-tauri/src/engine/restore.rs, src-tauri/src/engine/commands.rs, src-tauri/src/state.rs, src-tauri/src/lib.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src/ai/sessionRestore.ts:36-166(规格源:尾切/孤儿 marker/预算重建/pending 重现)
  - src-tauri/src/commands.rs(generate_project 的 Channel + 注册/清理模式)
  - src-tauri/src/state.rs(AppState 先例)
  - src-tauri/src/lib.rs(builder/setup/plugin-sql 注册顺序)
  - 22-RESEARCH.md §关键技术结论 #7、§Channel 协议
  </read_first>
  <behavior>
  - restore:不完整 turn 尾切(删到 turn 边界)、孤儿 tool_call 追加 unknown marker(payload = 22-01 定稿形状 `{ok:false,interrupted:true,status:'unknown',reason:'app-restart',modelText:...}`)
  - 启动仅对 latest session 做 marker append(对齐 TS no-arg 路径),其余 session 懒恢复(switchSession 时)
  - marker append 后事件流通过 check_event_stream
  - engine_cancel 对已结束 run 返回 Ok(幂等);确认等待态取消走候选 reject 语义
  </behavior>
  <action>
  1. `restore.rs`:`restore_session(conn, session_id) -> Result<RestoreReport>`;sessionRestore.ts 逐函数映射;marker modelText 键序 = `{"ok":false,"status":"unknown","interrupted":true,"reason":"app restarted before tool completion"}`(22-01 协议,手写 format!)。
  2. `engine/commands.rs` 五个 command:
     - `engine_run(user_message, session_id, provider, ollama_model, workspace_id, product_id, on_event: Channel<EngineEvent>) -> EngineRunResult`:State 取 Mutex<Connection>,run_id = correlation_id(uuid)注册进 AppState.runs,cancel token 传播,结束清理(照抄 commands.rs generate_project 模式)。
     - `engine_cancel(run_id)`:remove + cancel。
     - `engine_confirm_candidate(token)` / `engine_reject_candidate(token)`:调 confirmations 原子 UPDATE。
     - `engine_append_tool_result(session_id, tool_call_id, tool_name, ok, payload_json)`:确认后重执行的落库接缝 — append tool_result 事件(配对 tool_call 须存在,否则 error)。
  3. lib.rs:`.setup()` 内解析 db_path(22-01 probe 结论)→ open → assert_schema → manage(Mutex<Connection>) → tokio::spawn 对 latest session 执行 restore_session(不阻塞窗口)。engine 模块全部注册进 invoke_handler。
  4. 测试:内存库 restore 用例(完整 turn 不动/尾切/孤儿 marker 形状断言/双孤儿);commands 层用最小 tauri mock 或抽薄到可测函数。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml restore</automated>
  </verify>
  <acceptance_criteria>
  - `grep "fn engine_run" src-tauri/src/engine/commands.rs` 与 `grep "engine_append_tool_result" src-tauri/src/engine/commands.rs` 命中
  - `grep '"status":"unknown"' src-tauri/src/engine/restore.rs` 命中
  - `grep "CancellationToken" src-tauri/src/state.rs` 命中(run 注册表)
  - `cargo test restore` 通过
  </acceptance_criteria>
  <done>引擎全量接线,崩溃恢复语义(ENG-05)与 PORT-01 第三态在 Rust 落地</done>
</task>

<task type="auto">
  <name>Task 2: TS 切换 — ChatPanel/CmdK 调用点 + 确认接缝 + 写路径归零断言</name>
  <files>src/stores/chatConsoleStore.ts, src/components/CmdKPalette.tsx, src/ai/api.ts</files>
  <read_first>
  - src/stores/chatConsoleStore.ts:456 附近(submit → runToolLoop 调用点)与 527-537(confirm 流 executeTool)
  - src/components/CmdKPalette.tsx:69(第二个 runToolLoop 调用点)
  - src/ai/api.ts(Tauri invoke/Channel 封装先例)
  - src-tauri/src/engine/channel.rs(EngineEvent 七变体 — webview 侧 switch 的对端)
  - 22-RESEARCH.md §风险 #4 #5(调用点穷举 + 确认接缝)
  </read_first>
  <action>
  1. api.ts 新增 `engineRun(params, onEvent: (e: EngineEventMsg) => void): Promise<EngineRunResult>`(Channel 封装,与现有 llm 流式 invoke 同构)+ `engineCancel/engineConfirmCandidate/engineRejectCandidate/engineAppendToolResult` 薄封装。
  2. chatConsoleStore submit 路径:改调 engineRun。onEvent 处理:
     - `token` → 现有流式增量逻辑(复用现有 streaming 守卫)
     - `event` → refreshProjection(sessionId)(现有 fromEvents 读路径,不动)
     - `confirmation` → 现有 pending 卡片入列路径
     - `tool_start/tool_end` → 现有 onToolStart/onToolEnd 等价 UI 更新
     - `done` → 结束态(对应现 ToolLoopResult 消费处)
     - `error` → 现有错误呈现
  3. CmdKPalette.tsx:69 同构切换。
  4. **确认接缝(最细)**:chatConsoleStore confirm 流中 executeTool(留 TS)执行成功后,原落库调用改调 `engineAppendToolResult`(token 已 consume 由 engine_confirm_candidate 完成 — 顺序:confirm → executeTool → append)。
  5. 收尾断言:`grep -rn "runToolLoop" src/ --include="*.ts" --include="*.tsx"` 仅剩 src/ai/toolLoop.ts 定义与 src/ai/__tests__/ 测试引用,零运行时调用点;TS 写路径(confirmationStore/memoryStore SQLite 分支写函数)无运行时触达(代码保留)。
  6. TS 工具 schema 侧:PM CRUD 从 registry 导出的模型可见列表消失与 Rust tools.rs 一致(模型视角单源 = Rust schemas;TS registry 仅供测试/Phase 23 桥参考)。
  </action>
  <verify>
    <automated>npm run lint && grep -rn "runToolLoop" src/store src/stores src/components src/views 2>/dev/null; echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "engine_run\|engineRun" src/stores/chatConsoleStore.ts` ≥1;`grep "engineRun\|engine_run" src/components/CmdKPalette.tsx` 命中
  - `grep "engineAppendToolResult\|engine_append_tool_result" src/stores/chatConsoleStore.ts` 命中(确认接缝)
  - `grep -rn "runToolLoop(" src/store src/stores src/components src/views` 零命中
  - `npm run lint`(tsc)通过
  - `node --test src/ai/__tests__/` 全绿(217 规格测试不动)
  </acceptance_criteria>
  <done>用户可观察:ChatPanel 发起对话全程 Rust 引擎,流式/HITL/取消/恢复行为与切换前一致(SC-2/4/5)</done>
</task>

</tasks>

<verification>
- cargo test 全绿 + npm run lint + TS 测试全绿
- grep 断言:runToolLoop 运行时调用点 = 0
- 人工 UAT 项只留档(用户指令:延后 milestone 收口)
</verification>

<success_criteria>
ENG-01/02/04/05 用户可观察达成;唯一写者切换完成
</success_criteria>

<output>
创建 `.planning/phases/22-loop-replay-parity/22-06-SUMMARY.md`
</output>
