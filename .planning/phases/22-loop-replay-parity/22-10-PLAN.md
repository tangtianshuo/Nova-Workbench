---
phase: 22-loop-replay-parity
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/engine/tools.rs
autonomous: true
gap_closure: true
requirements: [PARITY-KW-HASH]

must_haves:
  truths:
    - "knowledge_write 卡片出现 → 确认 → 重放落库成功(无 params_mismatch toast)"
    - "拒绝路径不受影响(无孤儿事件)"
  artifacts:
    - path: "src-tauri/src/engine/tools.rs"
      provides: "execute_knowledge_write 将 effective_args 规整为 TS knowledgeParams(resolveDraft) 同构的 10 字段形状"
      contains: "TS parity boundary"
  key_links:
    - from: "src-tauri/src/engine/tools.rs (create_candidate)"
      to: "src/ai/confirmations.ts (consumeKnowledgeWriteConfirmation)"
      via: "两侧对同一规整对象计算 params_hash"
      pattern: "params_hash"
---

<objective>
Close UAT Test 7 (major): knowledge_write HITL confirm fails with `params_mismatch` on EVERY confirm.

Root cause (22-UAT.md Gaps entry 1): Rust candidate `params_hash` is computed over raw model args + productId/category/tags injections, while TS consume recomputes over `knowledgeParams(resolveDraft(args))` — a canonical 10-field draft with operation/summary/author/readTime always filled. The two hash domains never match. This is round 3 of the same bug class (productId fallback → category enum → params shape).

Fix: Rust normalizes `effective_args` at candidate creation into the EXACT shape TS `knowledgeParams(resolveDraft(...))` produces at replay, so the normalized object is a fixed point of TS re-normalization.

Purpose: make the knowledge_write HITL confirm chain structurally sound.
Output: patched `execute_knowledge_write` + hash-parity regression test. No TS code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-loop-replay-parity/22-UAT.md

Source of truth for the TS-side hash shape (do NOT modify these files):
- @src/ai/confirmations.ts (knowledgeParams :65-78 = canonical 10-field hash object)
- @src/ai/tools/knowledgeWrite.ts (resolveDraft :48-78 = TS normalizer to mirror)
- @src/ai/paramsHash.ts (canonical JSON: keys sorted at every depth, undefined dropped, array order preserved)
- @src/stores/chatConsoleStore.ts (:933-985 replay seam — picks ONLY known fields for the .strict() zod args, `operation` is NOT passed, so `operation` in Rust card args is harmless)

Rust side:
- @src-tauri/src/engine/tools.rs (:330-446 current execute_knowledge_write; :690-800 existing tests to update)
- @src-tauri/src/engine/confirmations.rs (:164-196 create_candidate computes params_hash over the passed Value)
- @src-tauri/src/engine/params_hash.rs (canonicalization — already parity-locked by golden fixture, no changes expected)
- @src-tauri/src/engine/commands.rs (:1098-1103 memory hash-parity test precedent — hash constant computed by TS, asserted in Rust)

<interfaces>
TS canonical hash object (confirmations.ts:65-78) — Rust normalization MUST produce exactly this, as serde_json::Value:

{
  productId:  string,           // str_arg(args,"productId") ?? ctx.product_id  (22-08 logic, keep)
  itemId:     string,           // ONLY if model provided non-empty; OMIT the key entirely when absent
                                //   (TS knowledgeParams includes itemId but canonical JSON drops undefined)
  operation:  "created"|"updated", // "updated" iff itemId exists in Rust knowledge tables (SQLite lookup), else "created"
  title:      string,
  category:   string,           // 22-09 enum validation, keep
  tags:       string[],         // default [] (22-09 logic, keep); each tag must be a non-empty string (zod array(z.string().min(1)))
  content:    string,
  summary:    string,           // str_arg(args,"summary") ?? first 100 chars of content
                                //   NOTE: current Rust code falls back to TITLE — that is wrong, TS falls back to content.slice(0,100)
  author:     string,           // str_arg(args,"author") ?? "AI 助手"
  readTime:   string,           // str_arg(args,"readTime") ?? "待阅读"
}

Why the fixed point holds on replay: chatConsoleStore.ts:941-951 passes the normalized summary/author/readTime (all non-empty strings) back as replay args, so resolveDraft's `??` fallbacks never re-trigger, and `operation` is excluded from the zod args (recomputed by TS from webview rndStore — matches Rust's "created" whenever itemId is absent).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize effective_args to the TS knowledgeParams shape in execute_knowledge_write</name>
  <files>src-tauri/src/engine/tools.rs</files>
  <behavior>
    - Test: execute("knowledge_write", {productId:"p1", title:"T", content:C(>100 chars), category:"最佳实践"}) → AwaitConfirmation candidate whose stored params are EXACTLY the 10-field normalized object (itemId key absent, operation="created", summary=content first 100 chars, author="AI 助手", readTime="待阅读", tags=[]) — no extra keys, no missing keys.
    - Test: hash parity — params_hash(normalized args) equals a TS-precomputed SHA-256 constant. Compute the constant by running node against the real TS functions (see action step 6). Assert in Rust, following the commands.rs:1099 memory precedent.
    - Test: model-provided summary/author/readTime/tags/itemId are preserved verbatim (no defaulting over explicit values); itemId present → operation computed from Rust knowledge tables.
    - Test (regression, must keep passing): invalid category → Failed{arg_error} no candidate; no productId + no ctx → Failed{arg_error}; tags defaulted to [] when absent; explicit productId wins over ctx.
    - Test: tags containing an empty string → Failed{arg_error:true} (zod array(z.string().min(1)) would reject AFTER confirm — same bug class, fail pre-card).
  </behavior>
  <action>
    Replace the block at tools.rs:427-432 (`let mut effective_args = args.clone(); ...`) with ONE clearly-delimited normalization block, commented `// 22-10: TS parity boundary — must stay field-identical to knowledgeParams(resolveDraft(...)) in src/ai/confirmations.ts:65-78`. Steps:

    1. `let content = str_arg(args, "content").unwrap();` (already validated non-empty).
    2. itemId: `let item_id = str_arg(args, "itemId").map(|s| s.to_string());` — only include the key in the output object when Some.
    3. operation: if itemId is Some, query the knowledge tables via the existing connection (`conn`) for that itemId (same tables `execute_knowledge_search` reads — reuse the existing query helper in tools.rs / its module; if lookup errors, treat as not-found → "created"). Else "created". Add a test comment: `// ponytail: update-path operation is computed from Rust SQLite here and from webview rndStore on TS replay — if the two stores disagree the hashes drift; known edge, create-path (no itemId) is parity-locked by the constant test below. Upgrade path: single source of truth for knowledge tables.`
    4. Build the normalized object as a fresh `serde_json::json!({...})` — do NOT clone raw model args and patch (that's the old bug: leftover unknown keys would enter the hash). Construct explicitly:
       ```rust
       let mut normalized = json!({
           "productId": product_id,
           "operation": operation,
           "title": title,
           "category": category,
           "tags": tags,
           "content": content,
           "summary": str_arg(args, "summary").map(String::from)
               .unwrap_or_else(|| content.chars().take(100).collect()),
           // ponytail: JS content.slice(0,100) counts UTF-16 units; chars().take(100) counts
           // Unicode scalars. Identical for BMP (all Chinese); diverges for astral chars (emoji).
           // Upgrade path: UTF-16-aware truncate if emoji-prefixed summaries ever matter.
           "author": str_arg(args, "author").unwrap_or("AI 助手"),
           "readTime": str_arg(args, "readTime").unwrap_or("待阅读"),
       });
       if let Some(id) = &item_id { normalized["itemId"] = json!(id); }
       ```
    5. tags: if `args.get("tags")` is an array, validate every element is a non-empty string AND `tags.len() <= 20` (zod `max(20)`, knowledgeWrite.ts:30 — checker round-1 warning; else `Failed{arg_error:true}` with message naming the constraint — pre-card, mirrors zod); pass through verbatim. If absent/not-array → `json!([])` (keep 22-09 comment).
    6. Delete the old `summary` title-fallback local (tools.rs:423-426). Card summary for `create_candidate`'s `Some(&summary)` param: keep using the normalized summary string (it's the card display text; only the hash shape is load-bearing — pass the normalized summary slice).
    7. Feed the SAME `normalized` value to both `create_candidate(conn, "knowledge_write", &normalized, ...)` (hash source) and the AwaitConfirmation `candidate.args` payload (replace `effective_args` there). `operation` in card args is harmless — chatConsoleStore.ts:941 only picks known fields.
    8. Generate the hash constant: write a one-off node script (or node -e) importing `computeParamsHash` from src/ai/paramsHash.ts and building knowledgeParams(resolveDraft-equivalent) for a fixed input, e.g. args `{productId:"p1",title:"T",content:"C".repeat(120),category:"最佳实践"}` (no summary/author/readTime/itemId/tags). Run with the project's tsx (`npx tsx`). Paste the hex constant into the Rust test.
    9. Update existing tests in tools.rs (:691-800) that assert on candidate args / effective_args shape to the new normalized shape; add the new tests from `<behavior>`.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test knowledge_write && cargo test</automated>
  </verify>
  <done>cargo test all green; candidate params are exactly the 10-field TS-parity shape; Rust params_hash equals the TS-computed constant for the create path; all 22-08/22-09 regression tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate + TS-side fixed-point sanity check</name>
  <files>(no source files — verification only, plus test fixture if needed)</files>
  <action>
    1. Run the full gates: `cd src-tauri && cargo test`, then `npm test` and `npm run lint` (tsc). All must pass.
    2. TS-side fixed-point sanity (no production change): confirm by reading/grepping that chatConsoleStore.ts:941-951 replay args (productId, itemId, title, category, tags, content, summary, author, readTime, confirmationToken — no `operation`) flow through resolveDraft idempotently given the Rust-normalized values. If a TS test file already covers knowledgeWrite confirm replay (search src/ai/__tests__ / *.test.ts for consumeKnowledgeWriteConfirmation), extend it minimally: feed a candidate whose params are the Rust-normalized shape and assert consume succeeds. If none exists, skip — the Rust constant test in Task 1 locks the parity; do not build new TS scaffolding for this.
    3. Sanity-check loop_runner.rs:637-672 / :745-761 tests still pass (they assert card kind/pending counts, not args shape — should be unaffected; update only if they break).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test && cd .. && npm test && npm run lint</automated>
  </verify>
  <done>cargo test + npm test + tsc all green; no TS production code touched; parity locked by Rust constant test.</done>
</task>

</tasks>

<verification>
- `cargo test` (all), `npm test` (all), `npm run lint` (tsc) pass.
- New Rust test asserts params_hash(normalized) == TS-computed constant (memory-precedent style).
- Invalid category / empty-string tag / missing productId arg_error paths still fail pre-card.
</verification>

<success_criteria>
- knowledge_write confirm chain: card → confirm → replay → 落库, no `params_mismatch` (structurally: Rust hash domain == TS consume hash domain, locked by constant test).
- Reject path unchanged. 22-08/22-09 behaviors preserved. Minimal diff confined to tools.rs (+ its test module).
- Human UAT re-test of Test 7 remains the final gate (non-blocking for plan completion; note in SUMMARY).
</success_criteria>

<output>
After completion, create `.planning/phases/22-loop-replay-parity/22-10-SUMMARY.md`
</output>
