---
phase: 23-tools-native
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified: [src-tauri/src/engine/tools.rs, src-tauri/src/engine/exec.rs, src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/commands.rs, src-tauri/src/lib.rs, src/stores/chatConsoleStore.ts, src/lib/api.ts]
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "exec 工具注册于 registry,argv 数组无 shell,idempotency=verify_first + PORT_01_SUFFIX"
    - "白名单(command+只读子命令二元组)命中直接执行;白名单外产生 exec_approval 候选进入 WAIT"
    - "子进程具备 kill_on_drop、timeout(默认 120s)、CancellationToken 取消、Windows taskkill /T 树杀"
    - "stdout/stderr 逐行经 EngineEvent::ToolOutput 流式回传"
    - "确认卡支持「仅本次允许」与「永久加入白名单」(kv_store key agent.exec.whitelist);确认后由 Rust command 重执行并 [confirmed rerun] settle"
    - "exec 全程无 webview invoke 反向调用(TOOL-04)"
  artifacts:
    - path: "src-tauri/src/engine/exec.rs"
      provides: "子进程 spawn/树杀/超时/取消/流式核心 + 白名单匹配"
      contains: "taskkill"
    - path: "src-tauri/src/engine/tools.rs"
      provides: "exec 工具 ToolSpec 注册"
      contains: "PORT_01_SUFFIX"
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_exec_confirmed + engine_whitelist_add commands"
      contains: "engine_exec_confirmed"
  key_links:
    - from: "src-tauri/src/engine/loop_runner.rs"
      to: "exec::run"
      via: "execute_async 内 cancel/on_event 直传子进程 select!"
      pattern: "cancel"
    - from: "src/stores/chatConsoleStore.ts"
      to: "engine_exec_confirmed"
      via: "确认卡按钮 invoke"
      pattern: "engineExecConfirmed|engine_exec_confirmed"
---

<objective>
exec 原生工具全集:进程管理(tokio::process + taskkill /T 树杀 + timeout + cancel + 流式)、只读白名单(command+子命令二元组)、白名单外 HITL(仅本次/永久加入)、确认后 Rust 重执行。

Purpose: TOOL-02 核心;TOOL-04 前置(零 webview 依赖)。
Output: engine/exec.rs 模块 + exec 工具注册 + 白名单持久化 + 两个新 command + 确认卡 UI 选项
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@23-CONTEXT.md
@23-RESEARCH.md
@.planning/phases/23-tools-native/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: exec 核心模块(进程管理 + 白名单)+ 单元测试</name>
  <files>src-tauri/src/engine/exec.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src-tauri/src/engine/tools.rs(ToolSpec/registry()/idempotency/PORT_01_SUFFIX :23)
  - src-tauri/src/engine/confirmations.rs(create_candidate :163、params_hash dedup :170-185)
  - src-tauri/Cargo.toml(tokio full :38、tokio-util :42 已就位,零新依赖)
  - 23-RESEARCH.md §1、§2、Don't Hand-Roll 表、Pitfalls 2/3/5/6
  </read_first>
  <action>
  1. 新建 engine/exec.rs(mod.rs 注册 `pub mod exec;`):
     - `pub struct WhitelistEntry { command: String, subcommands: Option<Vec<String>> }`;默认白名单常量(10 条,command 级 + git 子命令限定):`git[status,diff,log,show,branch]`, `dir`, `ls`, `type`, `cat`, `rg`, `grep`, `findstr`, `where`, `pwd`。匹配:argv[0] basename + to_lowercase 归一;subcommands=Some 时要求 args[0] ∈ 子命令集(堵 `git push`)。
     - `pub async fn run(conn, args_json, ctx, cancel, on_event) -> ToolOutcome`:
       a. workspace_root 为 None → `Failed { message: "no workspace root", arg_error: false }`(Pitfall 3)。
       b. schema: `{ command: String, args: string[] (默认[]), timeoutMs?: u64 (max 600_000) }`;argv 数组直接传 tokio::process::Command,**无 shell**。
       c. 白名单命中(kv_store 读 `agent.exec.whitelist` 合并默认清单,读失败只用默认)→ 执行;未命中 → `create_candidate(conn, "exec_approval", params={command,args,cwd}, ...)`(加入 params_hash dedup,confirmations.rs:170-185 补一行 kind 匹配)→ AwaitConfirmation,wait_value 类比 knowledge 文案。
       d. 执行:`kill_on_drop(true)`、cwd=workspace_root、piped stdout/stderr;`tokio::select!` 三路 — wait_with_output 风格逐行流式(`BufReader::lines` → `EngineEvent::ToolOutput { name:"exec", stream, is_stderr }`)、`cancel.cancelled()` → kill_tree、`sleep(timeout 默认 120_000ms)` → kill_tree 返回 Failed("timeout")。取消返回 `Failed { message: "cancelled", arg_error: false }`(下轮 loop LLM 前取消检查自然命中,23-RESEARCH §1 裁定)。
       e. kill_tree:Windows `std::process::Command "taskkill" ["/PID", pid, "/T", "/F"]`;非 Windows `child.kill()`。留 `// ponytail: taskkill /T 有竞态窗口,Job Object 当出现真实漏杀` 注释。
     - 结果 ≤4KB 直接返回;>4KB 走既有 prepare_tool_result artifact 化(零改动)。
  2. tools.rs registry 注册 `exec`:description 含「argv 数组、无 shell、需 workspace、超时默认 120s」+ PORT_01_SUFFIX;idempotency = VerifyFirst。
  3. execute_async 的 exec 分支接入(启用 cancel/on_event 参数)。
  4. 单元测试(可移植:`cfg(windows)` 用 `cmd /c echo hi`,else 用 `echo hi`):白名单命中执行成功;白名单外产生候选;取消中断返回 Failed("cancelled");timeout 触发;git push 被子命令限定拦截;`git.exe` 大写归一匹配。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml exec</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "taskkill" src-tauri/src/engine/exec.rs` ≥1(含 ponytail 注释)
  - `grep "exec_approval" src-tauri/src/engine/exec.rs` 命中
  - cargo test exec 子集全绿;全量 cargo test 不减
  </acceptance_criteria>
  <done>exec 核心可执行可取消可流式,白名单/ HITL 候选逻辑有测试锁定</done>
</task>

<task type="auto">
  <name>Task 2: engine_exec_confirmed / engine_whitelist_add commands + 确认卡 UI</name>
  <files>src-tauri/src/engine/commands.rs, src-tauri/src/engine/exec.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts</files>
  <read_first>
  - src-tauri/src/engine/commands.rs(append_tool_result_inner :284-367 模式、invoke_handler 注册表)
  - src-tauri/src/engine/confirmations.rs(consume 原子条件 UPDATE)
  - src/stores/chatConsoleStore.ts(既有 exec/工具确认卡渲染与 engineAppendToolResult 调用流)
  - 23-RESEARCH.md §2「HITL 候选流」差异点段
  </read_first>
  <action>
  1. commands.rs 新增:
     - `engine_exec_confirmed(session_id, token, allow_permanently: bool) -> Result<Value, AppError>`:原子 `consume(token, expected_params=Some(candidate params))` → 若 allow_permanently:白名单 kv UPSERT 追加该 command(仅 command 级,不加子命令)→ 在本 command 内 tokio spawn 子进程执行(与 exec::run 同核心,复用函数)→ 结果走 `append_tool_result_inner`(自动 mint fresh tool_call_id + `[confirmed rerun]`)→ 返回执行结果。
     - `engine_whitelist_add(command: String) -> Result<(), AppError>`:独立 command(备用;主路径是 allow_permanently)。
     - lib.rs invoke_handler 注册两个。
  2. api.ts:engineExecConfirmed / engineWhitelistAdd 包装。
  3. chatConsoleStore.ts:exec_approval 确认卡(kind 分支)三选项:「拒绝」「仅本次允许」→ engineExecConfirmed(token, false);「永久加入白名单」→ engineExecConfirmed(token, true)。拒绝走既有候选拒绝路径。注意:本 phase 不做白名单设置页(deferred)。
  4. ToolOutput 事件前端渲染:ChatPanel 工具卡内追加 stdout/stderr 行(is_stderr 弱化色),无头一致性无关、纯展示。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && npm test && npm run lint && grep -rn "invoke" src-tauri/src/engine/exec.rs src-tauri/src/engine/tools.rs</automated>
  </verify>
  <acceptance_criteria>
  - `grep "engine_exec_confirmed" src-tauri/src/engine/commands.rs src-tauri/src/lib.rs src/lib/api.ts` 均命中
  - grep invoke 于 engine/exec.rs、tools.rs **零命中**(TOOL-04 结构性锁定)
  - chatConsoleStore 存在 exec_approval 分支与「永久加入白名单」文案
  - 全部测试绿
  </acceptance_criteria>
  <done>确认后重执行完全在 Rust 内,白名单学习持久化,前端三选项确认卡可用</done>
</task>

</tasks>

<verification>
- cargo test / npm test / npm run lint 全绿
- `grep -rn "invoke" src-tauri/src/engine/` 仅 commands.rs 出现(Tauri command 定义本身),执行路径(exec.rs/tools.rs)零命中
</verification>

<success_criteria>
- TOOL-02 五要素(树杀/超时/取消/流式/白名单 HITL)各有测试或 grep 证据
- 确认后执行者为 Rust,无头可用
</success_criteria>

<output>
完成后创建 `.planning/phases/23-tools-native/23-02-SUMMARY.md`;每 task 一次 commit。
</output>
