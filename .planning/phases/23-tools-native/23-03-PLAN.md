---
phase: 23-tools-native
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified: [src-tauri/src/file_ops.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/fs_ops.rs, src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/commands.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts]
autonomous: true
requirements: [TOOL-01, TOOL-04]
must_haves:
  truths:
    - "fs_list/fs_read 工作区内读自由,直接执行返回结果"
    - "fs_write/fs_mkdir/fs_delete/fs_move 产生 fs_write 候选进入 WAIT;确认后由 Rust command 执行并 [confirmed rerun] settle"
    - "workspace 外路径直接 error(arg_error)拒绝,不给 HITL 选项"
    - "全部 fs 工具路径安全走 resolve_in_root(canonicalize + starts_with),.. 逃逸被拒"
    - "fs 工具零 webview 依赖"
  artifacts:
    - path: "src-tauri/src/engine/fs_ops.rs"
      provides: "agent fs 工具执行核心(复用 file_ops 路径安全)"
      contains: "resolve_in_root"
    - path: "src-tauri/src/engine/tools.rs"
      provides: "六个 fs 工具 ToolSpec 注册"
      contains: "fs_read"
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_fs_apply command(消费+执行+settle)"
      contains: "engine_fs_apply"
  key_links:
    - from: "src-tauri/src/engine/fs_ops.rs"
      to: "file_ops::resolve_in_root"
      via: "路径解析复用"
      pattern: "resolve_in_root"
    - from: "src/stores/chatConsoleStore.ts"
      to: "engine_fs_apply"
      via: "fs 写确认卡确认按钮 invoke"
      pattern: "engine_fs_apply|engineFsApply"
---

<objective>
fs 原生工具六件套(list/read 直接执行;write/mkdir/delete/move 走 HITL),路径安全复用 file_ops.rs 先例,越界直接拒绝。确认后执行在 Rust command 内(TOOL-04)。

Purpose: TOOL-01 fs 读写两类;Phase 24 无头 run 前置。
Output: engine/fs_ops.rs + 六个 ToolSpec + engine_fs_apply command + 确认卡分支
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@23-CONTEXT.md
@23-RESEARCH.md
@.planning/phases/23-tools-native/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fs 工具核心 + 注册 + 单元测试</name>
  <files>src-tauri/src/file_ops.rs, src-tauri/src/engine/fs_ops.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/mod.rs, src-tauri/src/engine/confirmations.rs</files>
  <read_first>
  - src-tauri/src/file_ops.rs(sanitize_file_name :8-16、resolve_in_root :21-45)
  - src-tauri/src/engine/tools.rs(registry、knowledge 工具先例)
  - 23-CONTEXT.md「fs 工具边界」决策
  - 23-RESEARCH.md §3、Pitfall 3
  </read_first>
  <action>
  1. file_ops.rs:`resolve_in_root` 从私有改 `pub(crate)`(sanitize 已是 pub(crate))。
  2. 新建 engine/fs_ops.rs(mod.rs 注册),每工具一个函数,统一前置:workspace_root None → Failed(no workspace root, arg_error=false);路径经 resolve_in_root 解析,越界 → `Failed { message: "path escapes workspace", arg_error: true }`(直接 error,无 HITL — CONTEXT 锁定):
     - `fs_list { path = "" }` → 目录条目列表(name/is_dir/size)。Readonly, rerunnable。
     - `fs_read { path }` → 文件内容(>1MB arg_error;≤4KB 内联,超出走既有 artifact 化)。Readonly, rerunnable。
     - `fs_write { path, content }` → create_candidate(kind "fs_write", params={operation:"write", path, content}) → AwaitConfirmation。VerifyFirst。
     - `fs_mkdir { path }` / `fs_delete { path }` / `fs_move { src, dest }` → 同 kind "fs_write" 候选,operation 区分(mkdir/delete/move)。VerifyFirst。
     - fs_write 类候选加入 params_hash dedup(23-02 已为 exec_approval 加过 kind 匹配,复用同一机制)。
  3. tools.rs registry 注册六个 fs 工具,description 说明 workspace 根相对路径 + 写需确认 + PORT_01_SUFFIX;同步工具走 execute 原路(fs 全同步,不进 execute_async 特殊分支 — 写路径只是入候选)。
  4. 单元测试(tempdir 作 workspace_root):读自由命中;`../escape` 与绝对路径越界被拒(arg_error=true);写产生候选且 WAIT;dedup 同 path+content 二次调用不重复入队。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml fs</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "fs_" src-tauri/src/engine/tools.rs` ≥6
  - `grep "pub(crate) fn resolve_in_root" src-tauri/src/file_ops.rs` 命中
  - cargo test fs 子集绿,全量不减
  </acceptance_criteria>
  <done>六 fs 工具注册可执行,边界策略(内读自由/内写 HITL/越界 error)测试锁定</done>
</task>

<task type="auto">
  <name>Task 2: engine_fs_apply command + 确认卡</name>
  <files>src-tauri/src/engine/commands.rs, src-tauri/src/engine/fs_ops.rs, src-tauri/src/lib.rs, src/lib/api.ts, src/stores/chatConsoleStore.ts</files>
  <read_first>
  - src-tauri/src/engine/commands.rs(engine_exec_confirmed — 23-02 同构先例,直接照抄结构)
  - src/stores/chatConsoleStore.ts(exec_approval 卡分支 — 23-02 产物)
  </read_first>
  <action>
  1. commands.rs:`engine_fs_apply(session_id, token) -> Result<Value, AppError>`:原子 consume(expected_params=Some)→ 按 candidate params.operation 执行写(write/mkdir/delete/move)→ append_tool_result_inner([confirmed rerun] settle)→ 返回结果。与 engine_exec_confirmed 同构。
  2. lib.rs 注册;api.ts 包装 engineFsApply。
  3. chatConsoleStore.ts:fs_write kind 确认卡分支(展示 operation/path/content 摘要),确认 → engineFsApply(token),拒绝走既有路径。无「永久加入」概念(边界即策略)。
  4. cargo 测试:consume→apply→settle 链路一条 happy path + 重复 consume 恰一成功。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - `grep "engine_fs_apply" src-tauri/src/commands.rs src-tauri/src/lib.rs src/lib/api.ts` 均命中(路径按实际)
  - 全部测试绿
  </acceptance_criteria>
  <done>fs 写确认后 Rust 执行 + settle,前端确认卡可用</done>
</task>

</tasks>

<verification>
- cargo test / npm test / npm run lint 全绿
- `grep -rn "invoke" src-tauri/src/engine/fs_ops.rs` 零命中
</verification>

<success_criteria>
- fs 边界三态(自由读/HITL 写/越界拒)全部有测试
- 确认后执行者为 Rust(TOOL-04)
</success_criteria>

<output>
完成后创建 `.planning/phases/23-tools-native/23-03-SUMMARY.md`;每 task 一次 commit。
</output>
