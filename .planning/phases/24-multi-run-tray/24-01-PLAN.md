---
phase: 24-multi-run-tray
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/engine/scheduler.rs, src-tauri/src/engine/commands.rs, src-tauri/src/engine/channel.rs, src-tauri/src/engine/mod.rs, src-tauri/src/lib.rs, src/stores/chatConsoleStore.ts, src/components/AgentConsole.tsx]
autonomous: true
requirements: [SCHED-01]
must_haves:
  truths:
    - "两个 session 同时发起 run,两个 run 并行流式输出,事件与确认卡互不串扰(集成测试锁定)"
    - "并发上限 3:第 4 个 run 进入 queued 状态,前序完成后自动开始(FIFO)"
    - "排队状态对 webview 可见(ChatPanel 显示排队中),开始执行时转为流式"
    - "取消 queued run 立即生效;取消 running run 行为与 23-02 一致"
  artifacts:
    - path: "src-tauri/src/engine/scheduler.rs"
      provides: "RunStatus 枚举 + FIFO 队列 + 并发上限 3 闸门 + 生命周期广播"
      contains: "queued"
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_run 走调度器 + 每 run 独立 Connection"
      contains: "acquire"
  key_links:
    - from: "src-tauri/src/engine/commands.rs"
      to: "scheduler::acquire"
      via: "engine_run 入口排队闸门"
      pattern: "scheduler"
    - from: "src/stores/chatConsoleStore.ts"
      to: "EngineEvent run_status"
      via: "on_event channel 分支,排队/开始状态写入 per-session run state"
      pattern: "run_status|queued"
---

<objective>
调度器核心:per-run Connection(解除单 slot "engine busy" 锁死)+ run 状态枚举(queued/running)+ FIFO 队列 + 并发上限 3 + 生命周期事件推 webview + SCHED-01 双 run 并行集成测试。

Purpose: SCHED-01 的真阻塞点不是调度而是 `EngineDb(Mutex<Option<Connection>>)` 单 slot(commands.rs:151 "engine busy")——第二个 run 直接报错。先解它,再加排队。
Output: engine/scheduler.rs 模块 + engine_run 改造 + queued 状态 UI + 并行/排队/取消集成测试
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@24-CONTEXT.md
@.planning/phases/23-tools-native/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 调度器模块 + per-run Connection 改造 + 单元/集成测试</name>
  <files>src-tauri/src/engine/scheduler.rs, src-tauri/src/engine/commands.rs, src-tauri/src/engine/channel.rs, src-tauri/src/engine/mod.rs</files>
  <read_first>
  - src-tauri/src/engine/commands.rs(engine_run :126-213 单 slot take/restore、engine_cancel :227)
  - src-tauri/src/engine/db.rs(open :28-33 WAL+busy_timeout、open_second :91 多连接先例)
  - src-tauri/src/engine/channel.rs(EngineEvent :30-45)
  - src-tauri/src/engine/mod.rs、src-tauri/src/lib.rs(EngineDb managed 状态从哪来 —— setup 时如何填充,取 db 路径)
  - 既有双连接测试模式:commands.rs:999、confirmations.rs:670
  </read_first>
  <behavior>
  - Test 1: 4 个 run 同时 acquire,第 4 个状态为 Queued;完成 1 个后第 4 个变 Running(FIFO 顺序)
  - Test 2: 两个 run 各持独立 Connection 并发写 event_log,事件按 (session_id, seq) 各自连续,无交叉(复用 db.rs temp file DB 测试基建)
  - Test 3: cancel 一个 queued run → 立即出队,不占并发槽;cancel running run → 槽释放
  - Test 4: engine busy 错误路径消失(第 2 个 run 不再被拒)
  </behavior>
  <action>
  1. 新建 engine/scheduler.rs(mod.rs 注册):
     - `pub enum RunStatus { Queued, Running }`;`pub struct Scheduler` 内部 `Mutex<SchedulerState>`,SchedulerState 含 `active: usize`(上限常量 `MAX_CONCURRENT: usize = 3`,per 24-CONTEXT 并发决策)+ `queue: VecDeque<run_id>`。
     - 实现:async `acquire(run_id, cancel, on_start: callback) -> Result<Permit, Cancelled>` —— 槽满则入队并挂起(每 run 一个 `tokio::sync::oneshot` 或 Notify;discretion:semaphore vs 显式队列,选显式 VecDeque 因为需要 FIFO 顺序 + 队列内容供托盘菜单读取),取消时从队列移除。Permit Drop 时槽减一并唤醒队首。
     - `pub fn snapshot() -> Vec<(run_id, RunStatus)>` — 供托盘菜单(24-02)与测试读取,挂在 AppState(`state.scheduler`)。
  2. channel.rs:EngineEvent 加变体 `#[serde(rename = "run_status")] RunStatus { run_id: String, status: String }`(status ∈ "queued"|"running";借用 serde 命名小心与类型名冲突,可命名 `RunStatusChange`)。
  3. commands.rs engine_run 改造:
     - **per-run Connection**:不再 `db.0.lock().take()`。改为从 AppState/EngineDb 持有的 db 路径(读 lib.rs setup 看 EngineDb 填充处,把路径存进 AppState 或 EngineDb 变体)每次 `db::open(path)` 新开连接(WAL+busy_timeout 已保证跨连接写安全,23-04/confirmations 测试先例)。**删除 "engine busy" 错误路径,并删除 take 与 restore 两处**——`take`(约 :151-157)与 run 完成时的 `*db.0.lock() = Some(conn)` restore(约 :209,drop 前)都要删,只删 take 不删 restore 会让 run 完成时覆盖托管连接。managed 单连接保留给非 run 命令(confirm/append 等)——它们已在用,不动。
     - **同步更新 event_log.rs 过时注释**(:100 附近"单连接 Mutex 保证 seq 无竞争"):该不变量随本 task 删除。新事实:多连接并发写下 seq 唯一性靠 (session_id, 单写者) 保证 —— per-session 单写者由 TS streaming guard(v0.3.1 SESS-04)保证,跨 run 并发写不同 session 经 WAL + busy_timeout 串行化,MAX(seq)+1 子查询(event_log.rs:84-86)在单 session 内无竞争。注释按此改写,勿留失效声明。
     - 入口:`scheduler.acquire(run_id, cancel, ...)` 先排队;排队期间向 on_event 发 `RunStatus{queued}`;获得 permit 后发 `RunStatus{running}` 再走现有 spawn_blocking 逻辑。engine_run 的 await 全程保持(调用方仍等完成)。
     - engine_cancel:cancel token 照旧;若 run 在队列中,由 acquire 的取消分支出队(Permit 未发出)。
  4. 测试:scheduler 单元测试(cap/FIFO/取消)+ 集成测试(两个 fake-LLM run 并行写 temp DB,事件不串扰 —— fake Llm 已有,loop_runner 测试模式)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml scheduler && cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
  - `grep "engine busy" src-tauri/src/engine/commands.rs` 零命中(单 slot 锁死已除)
  - `grep "MAX_CONCURRENT" src-tauri/src/engine/scheduler.rs` 命中且 = 3
  - 集成测试证明两 run 并行、四 run 排队 FIFO、queued 可取消,全绿
  </acceptance_criteria>
  <done>多 run 真并行 + 排队语义有测试锁定,调度器 snapshot 就绪供托盘消费</done>
</task>

<task type="auto">
  <name>Task 2: webview 排队状态接收 + ChatPanel 排队中 UI</name>
  <files>src/stores/chatConsoleStore.ts, src/components/AgentConsole.tsx, src-tauri/src/lib.rs</files>
  <read_first>
  - src/stores/chatConsoleStore.ts(per-session streaming guard :v0.3.1 SESS-04、on_event 分发处、run state 结构)
  - src/components/AgentConsole.tsx / ChatPanel(run 状态指示渲染点)
  </read_first>
  <action>
  1. chatConsoleStore:engineRun 的 on_event 分支加 `run_status` 处理 —— `queued` 时置该 session 的 run state 为排队中(isQueued 标志 + 禁重复发起,guard 已有);`running` 时清除排队标志(流式开始自然接管)。
  2. ChatPanel/AgentConsole:排队中的 session 显示「排队中(第 N 位)」状态条(N 可选,executor discretion;至少显示"排队中")。样式走现有 token 体系。
  3. 确认 lib.rs invoke_handler 无需新 command(本任务零新 command;若 Task 1 有注册遗漏在此补)。
  </action>
  <verify>
    <automated>npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - chatConsoleStore 有 run_status/queued 分支;ChatPanel 有排队中文案
  - npm test / tsc 全绿
  </acceptance_criteria>
  <done>排队状态用户可见,UI 不串扰 guard 保持</done>
</task>

</tasks>

<verification>
- cargo test / npm test / npm run lint 全绿
- SCHED-01 结构性证据:两 run 并行集成测试 + 单 slot busy 错误已删除
</verification>

<success_criteria>
- 两 session 并行 run 流式互不串扰(测试锁定);第 4 个 run 排队可见、FIFO 自动开始;queued 可取消
</success_criteria>

<output>
完成后创建 `.planning/phases/24-multi-run-tray/24-01-SUMMARY.md`;每 task 一次 commit。
</output>
