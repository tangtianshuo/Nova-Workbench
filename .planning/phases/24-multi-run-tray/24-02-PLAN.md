---
phase: 24-multi-run-tray
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: [src-tauri/Cargo.toml, src-tauri/src/tray.rs, src-tauri/src/lib.rs, src-tauri/src/engine/scheduler.rs, src-tauri/src/state.rs, src/stores/chatConsoleStore.ts, src/components/AgentConsole.tsx]
autonomous: true
requirements: [SCHED-02]
must_haves:
  truths:
    - "关闭窗口 = hide(不退出);run 继续执行;仅托盘「退出」真正退出"
    - "托盘常驻,菜单 = 动态运行中 run 列表(session 标题 + running/queued 状态)+ 分隔线 + 显示 Nova + 退出"
    - "run 生命周期变化(start/finish/queue)时菜单重建;tooltip 显示运行计数"
    - "点击 run 菜单项 = 显示窗口 + 跳转对应 session"
  artifacts:
    - path: "src-tauri/src/tray.rs"
      provides: "托盘初始化 + 菜单动态重建 + on_menu_event 跳转 emit + hide-on-close"
      contains: "TrayIconBuilder"
    - path: "src-tauri/Cargo.toml"
      provides: "tauri tray-icon feature"
      contains: "tray-icon"
  key_links:
    - from: "src-tauri/src/engine/scheduler.rs"
      to: "tray.rs rebuild"
      via: "生命周期变化回调(AppState 内 channel 或回调注册)"
      pattern: "rebuild|menu"
    - from: "src-tauri/src/tray.rs"
      to: "chatConsoleStore"
      via: "app.emit(\"tray-open-session\", session_id) + webview listen"
      pattern: "tray-open-session"
    - from: "src-tauri/src/lib.rs"
      to: "window.hide"
      via: "on_window_event CloseRequested → api.prevent_close() + hide"
      pattern: "prevent_close"
---

<objective>
托盘常驻 + hide-on-close + 动态 run 列表菜单 + 点击跳 session。

Purpose: SCHED-02(关窗 run 继续 + 重开状态一致——重开投影本身已由 SQLite 投影保证,本 plan 落的是关窗不杀)。
Output: tray.rs 模块 + Cargo tray-icon feature + CloseRequested 拦截 + webview 跳转监听
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@24-CONTEXT.md
@.planning/phases/24-multi-run-tray/24-01-SUMMARY.md
</context>

<context_notes>
- 标题来源:菜单项显示 session 标题 —— engine_run payload 无 session 标题;executor discretion:chatConsoleStore 发起 run 时把 title 随 payload 传(engine_run 加可选 `session_title: Option<String>` 参数,存 scheduler snapshot),或 snapshot 只显示 run/session id 前缀。优先前者(一行改动)。
- 图标:src-tauri/icons/icon.ico 已存在,直接用;运行态换图为 discretion,默认不换。
</context_notes>

<tasks>

<task type="auto">
  <name>Task 1: tray-icon feature + hide-on-close + 托盘骨架(显示/退出)</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/tray.rs, src-tauri/src/lib.rs</files>
  <read_first>
  - src-tauri/src/lib.rs(run() builder :107-150、window label "main"、setup 结构)
  - src-tauri/Cargo.toml(:16 tauri features 现 ["macos-private-api"])
  - src/components/layout/TitleBar.tsx:148-152(handleClose = win.close() —— TS 不改,Rust 拦截)
  </read_first>
  <action>
  1. Cargo.toml:tauri features 加 `"tray-icon"`。
  2. 新建 src-tauri/src/tray.rs:
     - `pub fn init(app: &tauri::AppHandle) -> tauri::Result<()>`:TrayIconBuilder + 默认菜单(「显示 Nova」+「退出」)+ icon.ico;`on_tray_icon_event` 双击/左键 = 显示+聚焦窗口。
     - `pub fn rebuild(app: &AppHandle, runs: &[(run_id, session_id, session_title, status)])`:tray.set_menu 重建 —— run 项(MenuItem id = run_id)+ 分隔线 + 显示 Nova + 退出;tooltip = `Nova — N 个运行中`。
     - `on_menu_event`:id == "show" → show+focus;id == "quit" → app.exit(0);id 命中 run_id → show+focus + `app.emit("tray-open-session", session_id)`。
  3. lib.rs:
     - `.on_window_event` CloseRequested → `api.prevent_close(); window.hide();`(一律 hide,per 24-CONTEXT)。
     - setup 中 `tray::init`。
  4. 手动 smoke 无法在 CI —— 用 `cargo build` 编译门 + 结构 grep(见 acceptance)。
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
  - `grep "prevent_close" src-tauri/src/lib.rs` 命中(hide-on-close 结构锁定)
  - `grep "tray-icon" src-tauri/Cargo.toml` 命中
  - tray.rs 含 TrayIconBuilder / set_menu / tray-open-session;cargo build 零错
  </acceptance_criteria>
  <done checked>关窗隐藏不退出,托盘常驻可显示/退出</done>
</task>

<task type="auto">
  <name>Task 2: run 列表动态重建 + webview 跳转 session</name>
  <files>src-tauri/src/engine/scheduler.rs, src-tauri/src/engine/commands.rs, src-tauri/src/tray.rs, src/stores/chatConsoleStore.ts, src/components/AgentConsole.tsx</files>
  <read_first>
  - src-tauri/src/engine/scheduler.rs(24-01 snapshot/生命周期点)
  - src-tauri/src/engine/commands.rs(engine_run 签名 —— 加 session_title 参数处)
  - src/stores/chatConsoleStore.ts(selectSession/切换 session 既有函数、事件 listen 先例:工作区切换通知)
  </read_first>
  <action>
  1. scheduler:run 注册时带 (session_id, session_title);生命周期每次变化(enqueue/start/finish/cancel)触发 tray rebuild —— 实现:Scheduler 持 `AppHandle`(或回调 Vec),变化处调 `tray::rebuild(app, &self.snapshot())`。节流 discretion:每次变化直接重建即可(Tauri set_menu 开销小,run 生命周期事件低频);若抖动再加 200ms debounce。
  2. engine_run 加 `session_title: Option<String>` 参数(默认 None 兼容);chatConsoleStore 发起时传入当前 session 标题。
  3. chatConsoleStore:`listen('tray-open-session')` → selectSession(sessionId) + 确保 activeTab 切到 Agent 控制台(uiStore)。卸载时 unlisten。
  4. cargo test + npm test 回归(scheduler 测试若因 AppHandle 耦合需注入 trait/Option<AppHandle>,测试传 None)。
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - scheduler snapshot 含 session 标题与状态;rebuild 在 enqueue/start/finish/cancel 四点触发(grep 或测试断言)
  - chatConsoleStore 有 tray-open-session 监听 + selectSession 调用
  - 全部测试绿
  </acceptance_criteria>
  <done checked>托盘 run 列表实时反映 queued/running,点击回 app 并跳对应 session</done>
</task>

</tasks>

<verification>
- cargo test / cargo build / npm test / npm run lint 全绿
- 人工 smoke(交 24-04 gates 或 UAT):关窗 → run 继续 → 托盘列表 → 点击跳 session → 托盘退出
</verification>

<success_criteria>
- SCHED-02:hide-on-close 后 run 继续;重开窗口投影完整(既有 SQLite 投影 + restore);托盘可跳转
</success_criteria>

<output>
完成后创建 `.planning/phases/24-multi-run-tray/24-02-SUMMARY.md`;每 task 一次 commit。
</output>
