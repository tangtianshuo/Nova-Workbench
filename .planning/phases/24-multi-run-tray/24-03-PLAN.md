---
phase: 24-multi-run-tray
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified: [src-tauri/Cargo.toml, src-tauri/src/lib.rs, src-tauri/src/notify.rs, src-tauri/src/engine/scheduler.rs, src-tauri/src/engine/commands.rs, src-tauri/capabilities/default.json, src/stores/chatConsoleStore.ts, src/ai/sessionRestore.ts]
autonomous: true
requirements: [SCHED-03]
must_haves:
  truths:
    - "窗口隐藏时,run 完成或等待 HITL 确认触发 OS 通知;窗口可见时不通知"
    - "通知点击/托盘回 app 可定位到对应 session"
    - "sessionRestore 恢复未过期的 exec/fs 确认卡(carry-in,24h TTL 候选)"
  artifacts:
    - path: "src-tauri/src/notify.rs"
      provides: "后台门控通知(notify_if_background)+ plugin 注册"
      contains: "notification"
    - path: "src-tauri/Cargo.toml"
      provides: "tauri-plugin-notification 依赖"
      contains: "tauri-plugin-notification"
  key_links:
    - from: "src-tauri/src/engine/commands.rs"
      to: "notify.rs"
      via: "run Done / Confirmation 事件处调 notify_if_background"
      pattern: "notify_if_background"
    - from: "src/ai/sessionRestore.ts"
      to: "确认卡渲染"
      via: "restore 输出补 pending exec/fs 候选(TTL 过滤)"
      pattern: "exec_approval|fs_"
---

<objective>
后台通知(仅隐藏态;run 完成 / 等待 HITL 确认)+ carry-in:确认卡 sessionRestore 重现。

Purpose: SCHED-03 + 23-VERIFICATION carry-in(后台 run 窗口重开场景确认卡必须回来,否则 HITL 死锁)。
Output: notify.rs + plugin 依赖/权限 + 门控接线 + sessionRestore 确认卡恢复
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@24-CONTEXT.md
@.planning/phases/24-multi-run-tray/24-02-SUMMARY.md
@.planning/phases/23-tools-native/23-VERIFICATION.md
</context>

<context_notes>
- tauri-plugin-notification 通知点击 payload 回传在 Windows 上支持有限(executor 验证):若 click-to-session 不可靠,fallback = 点击仅激活窗口(默认行为)+ 跳最近一条通知的 session(记录 last_notified_session);不足则靠托盘列表跳转(24-02 已交付)。此 fallback 为已授权 discretion,不算降标 —— SC 原文"可一键回到对应 session"由托盘 + 通知激活窗口共同满足。
- capabilities/default.json 需加 `"notification:default"`。
</context_notes>

<tasks>

<task type="auto">
  <name>Task 1: notification plugin + 后台门控通知</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/lib.rs, src-tauri/src/notify.rs, src-tauri/src/engine/commands.rs, src-tauri/capabilities/default.json</files>
  <read_first>
  - src-tauri/src/lib.rs(builder plugin 注册处 :108-112)
  - src-tauri/src/engine/commands.rs(engine_run 完成/Done 事件处、Confirmation 事件转发处)
  - src-tauri/capabilities/default.json
  </read_first>
  <action>
  1. Cargo.toml 加 `tauri-plugin-notification = "2"`;lib.rs `.plugin(tauri_plugin_notification::init())`;capabilities 加 `"notification:default"`。
  2. 新建 src-tauri/src/notify.rs:
     - `pub fn notify_if_background(app: &AppHandle, title: &str, body: &str, session_id: &str)`:仅当 main window `is_visible() == false` 时发送(`app.notification().builder().title(..).body(..).show()`);同时记录 `LAST_NOTIFIED_SESSION`(Atomic/Mutex in AppState)供点击 fallback 跳转。
     - 点击跳转:若 plugin 支持点击事件/action payload 则直接 emit tray-open-session;否则 fallback:窗口激活事件(tauri RunEvent::WindowEvent Focused / Reopen)时若 LAST_NOTIFIED_SESSION 有值则 emit 并清空。// ponytail: Windows toast 点击回调受限,focused-fallback 够用;需要精确点击语义再换 Windows AppNotification API
  3. 接线(commands.rs 或 tray.rs 统一事件出口处,executor 选最收口的一点):
     - run Done(engine_run 返回 Ok 处)→ notify「run 完成: {session_title}」。
     - run Error → notify 同理(与"完成"同级可见性,discretion,建议通知)。
     - EngineEvent::Confirmation 推 webview 处 → notify「等待确认: {session_title}」。
     - 取消不通知(用户自己点的)。
  4. 单元测试:门控逻辑(window visible 时 no-op)若可测则测;plugin 本身依赖系统,编译门 + grep 锁定接线点。
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml && grep -c "notify_if_background" src-tauri/src/engine/commands.rs</automated>
  </verify>
  <acceptance_criteria>
  - Cargo.toml / capabilities 含 notification;lib.rs 注册 plugin
  - notify_if_background 在 Done 与 Confirmation 两处被调(grep ≥2 或经由统一出口 1 处 + 出口内两分支)
  - cargo build/test 全绿
  </acceptance_criteria>
  <done checked="true">后台 run 完成/待确认有 OS 通知,前台不打扰,可回到对应 session(通知点击或托盘)</done>
</task>

<task type="auto">
  <name>Task 2: carry-in — exec/fs 确认卡 sessionRestore 恢复</name>
  <files>src/ai/sessionRestore.ts, src/stores/chatConsoleStore.ts</files>
  <read_first>
  - src/ai/sessionRestore.ts(restoreSession 输出结构、现有候选拉取)
  - src/stores/chatConsoleStore.ts(:468-479 restore 接线、:272 候选卡渲染、:479 只留本 session 卡的过滤)
  - src-tauri/src/engine/confirmations.rs(候选表 kind:exec_approval / fs_*、24h TTL 语义)
  - .planning/phases/23-tools-native/23-VERIFICATION.md(carry-in note 原文)
  </read_first>
  <action>
  1. sessionRestore.ts:restore 输出补 pending exec/fs 确认候选 —— 查候选表 kind ∈ {exec_approval, fs_*} 且未消费且未过 TTL(created_at 24h 内);随既有 memory/deliverable 候选管线一起返回。若 Rust 侧已有列候选的 command/query 则直接用;没有则加一个只读 Tauri command `engine_list_pending_candidates(session_id)`(Rust 侧 SQL select,只读不占写者)。
  2. chatConsoleStore:restore 后把 exec/fs 候选渲染为对应确认卡(渲染分支已有 —— 23-02 exec 三选项、23-03 fs 确认;确保 restore 数据 shape 与实时推送一致)。
  3. 测试:sessionRestore 单测加一例 —— DB 中有未过期 exec_approval 候选时 restore 输出含该卡;过期(>24h)不含。
  </action>
  <verify>
    <automated>npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
  - restoreSession 输出含 pending exec/fs 候选(未过期),过期被滤(有测试)
  - npm test / tsc 全绿
  </acceptance_criteria>
  <done checked="true">后台 run 等确认 → 关窗 → 重开,确认卡重现,可确认/拒绝(原子消费既有)</done>
</task>

</tasks>

<verification>
- cargo build/test + npm test + npm run lint 全绿
- 门控结构性证据:notify_if_background 存在且只在隐藏态发
</verification>

<success_criteria>
- SCHED-03(后台通知 + 回 session)+ carry-in(确认卡 restore)达成
</success_criteria>

<output>
完成后创建 `.planning/phases/24-multi-run-tray/24-03-SUMMARY.md`;每 task 一次 commit。
</output>
