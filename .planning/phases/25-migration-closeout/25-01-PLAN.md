---
phase: 25-migration-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ai/toolLoop.ts, src/ai/compaction.ts, src/ai/contextAssembler.ts, src/ai/index.ts, src/ai/__tests__/phase15ToolLoopInjection.test.ts, src/ai/__tests__/phase14Compaction.test.ts, src/ai/__tests__/phase15ContextAssembler.test.ts, src/ai/__tests__/phase16ContextAssembler.test.ts, src/ai/__tests__/fork.test.ts, src/ai/__tests__/phase14Integration.test.ts, docs/adr/ADR-0003-rust-run-engine.md, docs/ARCHITECTURE.md, CLAUDE.md]
autonomous: true
requirements: [PORT-02, PORT-03]
must_haves:
  truths:
    - "src/ai/toolLoop.ts 不存在;grep runToolLoop 生产代码零命中(注释/测试除外为零容忍:全 src 零命中)"
    - "compaction.ts / contextAssembler.ts(TS 算法副本)不存在;Rust 侧 parity 测试仍全绿(fixture 单源)"
    - "npm test 243+ 全绿、cargo test 165 pass/2 ignored、tsc clean(基线不减一)"
    - "ADR-0003 状态为 Accepted 且含实施结果注记;ARCHITECTURE.md 呈现 Rust 引擎分层 + webview=投影/HITL,无双引擎叙述;CLAUDE.md Architecture Overview 与现状一致"
  artifacts:
    - path: "docs/adr/ADR-0003-rust-run-engine.md"
      provides: "Accepted 状态 + 实施结果注记"
      contains: "Accepted"
    - path: "docs/ARCHITECTURE.md"
      provides: "Rust 引擎分层架构(engine 模块图)"
      contains: "engine"
    - path: "CLAUDE.md"
      provides: "同步后的 Architecture Overview"
      contains: "engine"
  key_links:
    - from: "src/ai/index.ts"
      to: "toolLoop"
      via: "barrel export 已移除"
      pattern: "!(toolLoop)"
    - from: "src-tauri parity tests"
      to: "src/ai/__tests__/fixtures/*.json"
      via: "双侧 fixture 单源回放(删除 TS 副本后 Rust parity 不受影响)"
      pattern: "projection-cases"
---

<objective>
迁移收口:删除 TS toolLoop 与零调用方的 TS 算法副本(双引擎并存窗口关闭),全量测试保持绿;ADR-0003 转 Accepted、ARCHITECTURE.md 重写为 Rust 引擎分层、CLAUDE.md 同步(PORT-02 + PORT-03)。

Purpose: 双引擎并存是 v0.3.2 迁移期的临时状态;引擎已在 Phase 22-24 全量落地并测试锁定,死代码与过时文档继续存在只会误导后续开发。
Output: 删除后的 src/ai(仅活路径)、更新后的 ADR/ARCHITECTURE/CLAUDE.md、全绿测试基线
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 TS toolLoop + 零调用方算法副本 + 关联测试清理</name>
  <files>src/ai/toolLoop.ts, src/ai/compaction.ts, src/ai/contextAssembler.ts, src/ai/index.ts, src/ai/__tests__/phase15ToolLoopInjection.test.ts, src/ai/__tests__/phase14Compaction.test.ts, src/ai/__tests__/phase15ContextAssembler.test.ts, src/ai/__tests__/phase16ContextAssembler.test.ts, src/ai/__tests__/fork.test.ts, src/ai/__tests__/phase14Integration.test.ts</files>
  <read_first>
  - src/ai/index.ts(:21 `export * from './toolLoop'` — 删此行)
  - src/stores/chatConsoleStore.ts(:578 retired 注释、:603 engineRun 无条件 — 证据:runToolLoop 生产调用方为零)
  - src/ai/__tests__/parity.rust.test.ts(双侧 fixture 单源:仅消费 fixtures/*.json + ChatSession.fromEvents;**不 import** compaction/contextAssembler — 因此删算法副本不伤 parity)
  - src/ai/chatSession.ts(:37 CompactionSummaryRecord 定义于此文件自身,compaction.ts 只是消费方 — 删 compaction.ts 不破坏投影)
  - src/ai/__tests__/fork.test.ts(:14 import maybeCompactSession — 需修剪)
  - src/ai/__tests__/phase14Integration.test.ts(3 个测试中 1 个用 maybeCompactSession — 修剪该测试,其余两个测 sessionRestore/eventStore 保留)
  - src/ai/__tests__/phase13ToolLoopEvents.test.ts(只 import events/* 活模块,文件名仅历史 — **保留不改**)
  </read_first>
  <action>
  Planner 已核实的调用方证据与裁定(planner grep 于 2026-08-24,executor 直接执行,不必重侦察;若与实际冲突以 grep 为准):

  **DELETE(生产调用方为零):**
  1. `src/ai/toolLoop.ts` — runToolLoop 仅自身 + 测试引用;chatConsoleStore :603 无条件 engineRun
  2. `src/ai/compaction.ts` — 唯一生产 import 方是 toolLoop.ts;CompactionSummaryRecord 类型定义在 chatSession.ts(:37),删文件不影响投影/parity
  3. `src/ai/contextAssembler.ts` — 唯一生产 import 方是 toolLoop.ts;context.ts/api.ts/store 无引用
  4. `src/ai/index.ts`:删 `export * from './toolLoop'` 行(:21)
  5. 整文件删除的测试:`phase15ToolLoopInjection.test.ts`(直接测 runToolLoop)、`phase14Compaction.test.ts`、`phase15ContextAssembler.test.ts`、`phase16ContextAssembler.test.ts`
  6. 修剪:`fork.test.ts` 删 compaction 相关 import 与测试用例(保留 fork 语义用例);`phase14Integration.test.ts` 删使用 maybeCompactSession 的那个测试,保留 restore/crash-tail 两个

  **KEEP(活路径,勿动):**
  - `src/ai/tools/*` + registry.ts(executeTool 活接缝:chatConsoleStore :763/:1040、ProductKnowledgeTab、CmdKPalette、WorkspaceSummaryModal、ProjectCreateModal)
  - `agentScope.ts`(confirmations.ts、tools/generateDeliverable.ts 活引用)
  - `tokenEstimate.ts`(chatSession.ts 活引用)、`ftsTokens.ts`(knowledgeRepo.ts 活引用)、`paramsHash.ts`(confirmations/confirmationStore/memoryStore 活引用)
  - chatSession / sessionRepo / sessionRestore / fork / events/* / confirmations / memoryStore / api / context(chatConsoleStore、ChatPanel、AgentWorkspaceView、useCmdK 活引用)
  - `parity.rust.test.ts` 与 fixtures(fixture 单源,Rust parity 不依赖被删副本)

  删除后运行 npm test,若有遗漏的引用编译/运行报错,按调用方证据逐个处理(仍以"生产调用方为零才删"为界)。
  </action>
  <verify>
    <automated>! grep -rnq "runToolLoop" src/ && ! test -f src/ai/toolLoop.ts && ! test -f src/ai/compaction.ts && ! test -f src/ai/contextAssembler.ts && npm test && npm run lint && cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
  - `grep -rn "runToolLoop" src/` 零命中;`grep -rn "from '.*compaction'\|from '.*contextAssembler'" src/ --include="*.ts" --include="*.tsx"` 零命中
  - npm test 全绿(数量允许因删除死测试减少,不允许出现失败)、tsc clean、cargo test 165 pass/2 ignored
  - src/ai/tools/*、agentScope、tokenEstimate、ftsTokens、paramsHash、parity fixtures 全部原样保留
  </acceptance_criteria>
  <done>TS loop 死代码与算法副本删除,单引擎现实与代码一致,基线绿</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0003 转 Accepted + ARCHITECTURE.md 引擎分层重写 + CLAUDE.md 同步</name>
  <files>docs/adr/ADR-0003-rust-run-engine.md, docs/ARCHITECTURE.md, CLAUDE.md</files>
  <read_first>
  - docs/adr/ADR-0003-rust-run-engine.md(含 2026-08-24 无桥修订注记 — 在其上补 Accepted)
  - docs/ARCHITECTURE.md(现状结构,判断改写范围)
  - CLAUDE.md(Architecture Overview / Directory Structure / Tauri Integration 段 — v0.1 期描述已过时)
  - src-tauri/src/engine/(mod.rs 模块清单:scheduler/loop_runner/tools/exec/fs_ops/notify/tray/event_log/confirmations/channel/commands 等)
  - .planning/phases/24-multi-run-tray/24-VERIFICATION.md(deferred UAT 4 项 — 仅作 milestone 收口引用素材,本 phase 不安排执行)
  </read_first>
  <action>
  1. **ADR-0003**:Status `Proposed` → `Accepted`;文末补「实施结果」注记,要点:Phase 22(loop 语义移植 + replay parity fixture 单源)、Phase 23(原生工具,无桥修订兑现)、Phase 24(scheduler 多 run 并行 + 托盘裁定兑现)、Phase 25(TS toolLoop 删除,双引擎窗口关闭);TS 工具注册表(executeTool)保留服务 webview 用户动作与 HITL 后重放,PM CRUD 原生化留 v0.3.3。
  2. **ARCHITECTURE.md**:更新为 Rust 引擎分层现实 — engine 模块图(scheduler / loop_runner / tools / exec / fs_ops / notify / tray / event_log / confirmations / channel / commands,各自一句职责);webview 定位 = 投影(ChatSession 投影 + engine_* command)+ HITL UI;删除双引擎并存叙述;保留仍成立的 TS 侧模块说明(tools registry / stores / projection)。形式 discretion:文字分层图即可,不引入绘图工具。
  3. **CLAUDE.md**:同步 Architecture Overview 与相关段落 — Rust 引擎是 agent 运行时(非 "Tauri commands currently minimal");src-tauri/src/engine/ 目录结构;webview=React 投影 + HITL;server.ts 定位不变(dev web fallback);确认无残留 "双引擎"/v0.1 期失实表述。
  4. README.md 仅在 grep 命中"双引擎/toolLoop"过时表述时顺带修正(预期无需)。

  注:里程碑级人工 UAT(SC-3)已由用户裁定 defer 到 complete-milestone,本 task 与本 phase 均不安排;仅在 ADR 注记或 ARCHITECTURE 验收段落可引用 24-VERIFICATION deferred 清单作为 milestone gate 输入。
  </action>
  <verify>
    <automated>grep -n "Accepted" docs/adr/ADR-0003-rust-run-engine.md && grep -c "engine" docs/ARCHITECTURE.md && ! grep -rnq "runToolLoop" docs/ ARCHITECTURE.md CLAUDE.md 2>/dev/null; grep -n "engine" CLAUDE.md | head -5</automated>
  </verify>
  <acceptance_criteria>
  - ADR-0003 状态 Accepted + 实施结果注记覆盖 Phase 22-25 四要点
  - ARCHITECTURE.md 含 engine 模块分层与 webview=投影/HITL 定位,无双引擎并存叙述
  - CLAUDE.md Architecture Overview 反映 Rust 引擎现实(无 "Tauri commands currently minimal" 类过时句)
  </acceptance_criteria>
  <done>决策记录与架构文档与单引擎代码现实对齐(PORT-03)</done>
</task>

</tasks>

<verification>
- 全量基线:cargo test 165 pass/2 ignored、npm test 全绿、npm run lint(tsc)clean
- PORT-02 结构性证据:src/ 无 runToolLoop / compaction import / contextAssembler import 零命中;parity fixture 双侧回放仍绿(agent 语义无退化的测试锁定)
- PORT-03 结构性证据:ADR-0003 Accepted、ARCHITECTURE.md 引擎分层、CLAUDE.md 同步
</verification>

<success_criteria>
- SC-1:TS toolLoop 与死代码删除,测试基线全绿,agent 语义由既有 parity/集成测试锁定无退化
- SC-2:三份文档与 Rust 引擎现实对齐
- SC-3(里程碑 UAT)不在本 phase — 由 complete-milestone 消费 24-VERIFICATION deferred 清单
</success_criteria>

<output>
完成后创建 `.planning/phases/25-migration-closeout/25-01-SUMMARY.md`;每 task 一次 commit。
</output>
