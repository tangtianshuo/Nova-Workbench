---
phase: 26-mock-tab
plan: 04
type: execute
wave: 3
depends_on: ["26-03"]
files_modified:
  - src/components/rnd/PrototypeTab.tsx
  - src/components/rnd/CodeScaffoldTab.tsx
  - src/components/rnd/TestCasesTab.tsx
  - src/components/rnd/CompetitorTab.tsx
  - src/components/product/FullDeliverablesTab.tsx
  - src/components/product/SkillsTab.tsx
  - src/stores/rndStore.ts
  - src/stores/productStore.ts
  - src/store/AppContext.tsx
  - src/data/mockRndData.ts
autonomous: false
requirements: [TAB-01, TAB-02, TAB-04, TAB-06]

must_haves:
  truths:
    - "六个 tab + 一键交付物 + product skill 全部触发真实 engine_run"
    - "一键十八份交付物 = 单 run 多步(1 个 runId),候选逐份出、逐份 HITL"
    - "grep 全 src 零 generate*AI mock / fabricate 兜底残留,UI 无死路径"
    - "runProductSkill 走 startTabRun,调用方错误分支被审计(P13)"
  artifacts:
    - path: "src/stores/rndStore.ts"
      provides: "mock 全清后的 store(六 generate*AI 删除、fabricate 兜底删除)"
    - path: "src/components/product/FullDeliverablesTab.tsx"
      provides: "一键十八份单 run 多步触发"
  key_links:
    - from: "src/components/product/FullDeliverablesTab.tsx"
      to: "useTabRunStore.startTabRun"
      via: "kind='deliverable-batch' 单次调用"
      pattern: "deliverable-batch"
    - from: "src/stores/productStore.ts"
      to: "useTabRunStore.startTabRun"
      via: "runProductSkill 重写"
      pattern: "startTabRun"
---

<objective>
其余五个 tab + 一键交付物(单 run 多步)+ runProductSkill 接线,mock 全删,人工验证收口。

Purpose: TAB-04 mock 零残留;TAB-06 一键交付物单 run 多步;26-03 模式机械复制。
Output: 全 tab 真实化 + mock 清除 + 人工 UAT checkpoint。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-mock-tab/26-CONTEXT.md
@.planning/phases/26-mock-tab/26-UI-SPEC.md
@.planning/phases/26-mock-tab/26-03-SUMMARY.md(已验证的接线模式——本 plan 复制)

<interfaces>
26-03 交付的接线模式(每个 tab 复制):
```typescript
useTabRunStore.getState().startTabRun({
  tabId, kind, productId, userMessage,
  coreContext: buildCoreContext({ kind, instruction }),
  sessionTitle,
}); // tabRunStore 内 engineRun 带 priority:'batch'
<TabRunPanel tabId={...} />;  // 运行中按钮 disabled + tooltip;web fallback disabled
// 候选走全局确认队列 + PrdDraftDialog 编辑落槽(单链,勿另造)
```
rndStore 待删 mock(现文件行号):generateRequirementAI(242)、generatePrototypeAI(297)、generateCodeScaffoldAI(423)、generateTestCasesAI(459)、generateCompetitorAnalysisAI(499)、generateDeliverableAI(534)、generateAllDeliverablesBatchAI(561);fabricate 兜底:getRequirementForProduct(201-231)、getPrototypeForProduct(273-289)、getCompetitorDataForProduct(491 INITIAL_COMPETITOR_DATA 引用)。productStore.runProductSkill(102-136)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 五 tab + FullDeliverablesTab + runProductSkill 接线(复制 26-03 模式)</name>
  <files>src/components/rnd/PrototypeTab.tsx, src/components/rnd/CodeScaffoldTab.tsx, src/components/rnd/TestCasesTab.tsx, src/components/rnd/CompetitorTab.tsx, src/components/product/FullDeliverablesTab.tsx, src/components/product/SkillsTab.tsx</files>
  <read_first>
    - .planning/phases/26-mock-tab/26-03-SUMMARY.md + 26-03 试点代码(RequirementTab 接线为参照模板)
    - 本 task 全部目标文件(找各自现有 AI 按钮与 generate*AI 调用点)
    - src/data/mockRndData.ts 的 FULL_LIFECYCLE_DELIVERABLES_CATALOG(十八份 code 清单,喂 batch instruction)
    - src/stores/productStore.ts runProductSkill 调用方(grep runProductSkill 全 src,P13 错误分支审计)
  </read_first>
  <action>
按 26-03 已验证模式逐 tab 接线(每 tab:按钮→startTabRun、TabRunPanel 挂载、运行中 disabled+tooltip「本 tab 已有生成任务进行中」、!isTauri() disabled+tooltip「此功能需要桌面引擎,请使用桌面版 Nova。」、删除对旧 mock action 的调用):

1. **PrototypeTab**:tabId/kind='prototype',instruction 指示产出原型方案文档(Markdown,含界面分区说明),userMessage 带设备/主题偏好。
2. **CodeScaffoldTab**:kind='code',instruction 指示产出代码脚手架方案文档(文档级,v0.4 才真写文件——Out of Scope 边界)。
3. **TestCasesTab**:kind='test',instruction 指示产出测试用例文档(结构化用例表 Markdown)。
4. **CompetitorTab**:kind='competitor',instruction 指示产出竞品分析文档。
5. **FullDeliverablesTab 一键生成**:kind='deliverable-batch',**单次** startTabRun(1 个 runId,绝不循环 18 次)。userMessage:`一键生成全部十八份交付物`。instruction 里枚举 FULL_LIFECYCLE_DELIVERABLES_CATALOG 的全部 code+title(catalog 可 import,运行时拼接进 instruction),明确指示:依次对每份交付物调用一次 generateDeliverable(每份一个 code),候选逐份产出等待用户确认,不要合并为一份文档。旧 onProgress 百分比 UI 删除,进度由 TabRunPanel 的 currentStep/events 呈现(逐份 tool_start 即天然进度)。单份卡槽的单独生成按钮(若有)kind='deliverable-single'。
6. **SkillsTab / runProductSkill 调用方**:产品页 skill 运行按钮改为 `startTabRun({ tabId: \`skill-${skillId}\`, kind: 'product-skill', ... })`,instruction = skill 的 prompt 模板(associatedSkills 里既有字段,read SkillsTab 确认字段名),产物同候选管线;调用方按 startTabRun 返回的 runId 处理(去假 `{success,timestamp}` 形状;错误经 TabRunPanel error 态呈现,P13)。
7. 每 tab 空态文案按 UI-SPEC:「还没有{交付物名}。点击上方生成按钮,AI 将生成候选供你确认。」
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -rn "generatePrototypeAI\|generateCodeScaffoldAI\|generateTestCasesAI\|generateCompetitorAnalysisAI\|generateDeliverableAI\|generateAllDeliverablesBatchAI\|runProductSkill" src/components/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 上述 grep 在 src/components/ 返回 0 行(组件不再调任何 mock action)
    - grep -n "deliverable-batch" src/components/product/FullDeliverablesTab.tsx 命中,且该文件内 startTabRun 调用仅 1 处 batch 入口(单 run;单份按钮另计)
    - grep -rn "startTabRun" src/components/ 命中 ≥ 7 处(五 tab + batch + skill)
    - grep -rn "for.*deliverable.*startTabRun\|list.map.*startTabRun" src/components/product/FullDeliverablesTab.tsx 为 0 行(无 18-run 循环)
    - grep -c "此功能需要桌面引擎" src/components/ -r ≥ 7
    - npx tsc --noEmit 零错误
  </acceptance_criteria>
  <done>全部入口真实化:一键十八份是单 run 多步,批量 run 标 batch 优先级,web 模式全部禁用无死路径</done>
</task>

<task type="auto">
  <name>Task 2: mock 全删 + fabricate 兜底清除 + 空态显式化</name>
  <files>src/stores/rndStore.ts, src/stores/productStore.ts, src/store/AppContext.tsx, src/data/mockRndData.ts</files>
  <read_first>
    - src/stores/rndStore.ts 全文
    - src/stores/productStore.ts runProductSkill 区段 + ProductSkill 相关类型
    - src/store/AppContext.tsx(rndStore/productStore action 的转发点)
    - src/data/mockRndData.ts(INITIAL_* 种子与 catalog;catalog 保留,种子删)
    - .planning/research/PITFALLS.md Pitfall 12/13(半清理反模式)
  </read_first>
  <action>
1. **rndStore.ts 删除**:`generateRequirementAI`、`generatePrototypeAI`、`generateCodeScaffoldAI`、`generateTestCasesAI`、`generateCompetitorAnalysisAI`、`generateDeliverableAI`、`generateAllDeliverablesBatchAI` 七个 action(RndState 接口 + 实现全删)。
2. **fabricate 兜底删除(P12)**:`getRequirementForProduct`/`getPrototypeForProduct`/`getCompetitorDataForProduct` 的「不存在→现场编造」分支删除,未知/缺失 productId 一律返回对应 EMPTY_*(EMPTY_PROTOTYPE/EMPTY_REQUIREMENT/EMPTY_COMPETITOR 已存在,直接用);`getCompetitorDataForProduct` 中 `INITIAL_COMPETITOR_DATA.p1` 引用清除。
3. **mock 种子删除**:rndStore 初始 state 与 mockRndData.ts 中 INITIAL_REQUIREMENTS / INITIAL_PROTOTYPES / INITIAL_CODE_SCAFFOLDS / INITIAL_TEST_CASES / INITIAL_COMPETITOR_DATA 改为空对象 `{}`(类型定义与 FULL_LIFECYCLE_DELIVERABLES_CATALOG 保留——catalog 是目录定义,锁定决策);persist migrate 保持 v3 剔除历史 mock 桶(26-01 已做 deliverables;这里对其余桶同样:persisted 桶若来自 mock 种子清空,简单做法 = migrate 直接将这些桶置 `{}`,不迁移——锁定决策「直接删除,不迁移」)。`runTestCase`/`runAllTestCases` 的假 setTimeout 通过改为同步置 passed?不——它们属测试执行 mock,不在本 phase 六个 generate*AI 清单内,保留原样(显式留待 coding 工具 v0.4),仅在 SUMMARY 记 debt。
4. **productStore.ts**:`runProductSkill` action 删除(调用方 Task 1 已改走 startTabRun);`polishKnowledgeArticleAI`(rndStore)若为纯字符串拼接 mock 也一并删除并改其调用方走 startTabRun(kind='product-skill' 或 polish instruction)——调用方用 grep 定位。
5. **AppContext.tsx / 其他转发层**:删除被删 action 的转发与类型引用;grep 每个被删名字清零。
6. **空态显式化**:相关 tab 视图对空数据渲染 UI-SPEC 空态文案(Task 1 已做组件侧;此处确保 getter 返回空不再被 fabricate 掩盖)。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -rn "generateRequirementAI\|generatePrototypeAI\|generateCodeScaffoldAI\|generateTestCasesAI\|generateCompetitorAnalysisAI\|generateDeliverableAI\|generateAllDeliverablesBatchAI\|runProductSkill\|INITIAL_REQUIREMENTS\|INITIAL_PROTOTYPES\|INITIAL_CODE_SCAFFOLDS\|INITIAL_TEST_CASES\|INITIAL_COMPETITOR_DATA" src/ --include="*.ts" --include="*.tsx" | grep -v mockRndData | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 上述 grep(排除 mockRndData.ts 中的类型/常量定义行)返回 0 行;mockRndData.ts 内 INITIAL_* 仅剩 `= {}` 空定义或已删除
    - grep -n "setTimeout(r, 1" src/stores/rndStore.ts 为 0 行;grep -n "setTimeout(r, 1400)" src/stores/productStore.ts 为 0 行
    - grep -n "INITIAL_COMPETITOR_DATA.p1" src/stores/rndStore.ts 为 0 行
    - grep -n "FULL_LIFECYCLE_DELIVERABLES_CATALOG" src/data/mockRndData.ts 命中(catalog 保留)
    - npx tsc --noEmit 零错误;npm run build 成功
  </acceptance_criteria>
  <done>TAB-04 成立:代码零 mock/fabricate 残留,空态显式,catalog 作为目录定义保留</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证 — 全 tab 真实引擎端到端</name>
  <action>人工验证 26-01..26-04 全部交付(见 how-to-verify 八步)</action>
  <what-built>产研中心全 tab 真实引擎接线 + mock 全清(26-01..26-04 全部交付)</what-built>
  <how-to-verify>
桌面模式(npm run tauri:dev,需已配置 provider key):
1. 需求 tab:点「生成需求文档」→ TabRunPanel 展开、流式步骤滚动 → 候选出现,确认队列可打开 → PrdDraftDialog 编辑 → 确认落槽 → 卡槽变 ready + AI 溯源徽章,知识库 FTS 可搜到
2. 运行中再点生成按钮:disabled + tooltip「本 tab 已有生成任务进行中」
3. 一次生成途中点「取消生成」:面板显示已取消文案,已落槽产物仍在
4. 三个 tab 各起一个批量生成(占满 cap-3),随后去聊天发一句话:聊天 run 应先于排队中的批量 run 执行(TAB-06)
5. 一键生成十八份交付物:托盘/tray 或 TabRunPanel 确认只有 1 个 run,事件列表逐份 tool_start,候选逐份出
6. 聊天会话列表:不出现任何 tab run 的 session(TAB-05)
7. 重启 app:已落槽交付物经投影恢复,未落槽槽位为空态
8. web 模式(npm run dev 浏览器):全部 AI 按钮 disabled + tooltip
  </how-to-verify>
  <verify>用户对 8 步桌面/web 验证逐项确认通过</verify>
  <done>用户回复 "approved",Phase 26 五条 success criteria 人工确认成立</done>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
- npx tsc --noEmit、npm run build 全绿
- grep 审计:src/ 内 generate*AI / runProductSkill / INITIAL_*(rnd 域)零残留
- 人工 checkpoint 覆盖 5 条 roadmap success criteria
</verification>

<success_criteria>
- TAB-01/02/04/06 端到端成立;Phase 26 六条 success criteria 全部可验证
</success_criteria>

<output>
完成后创建 .planning/phases/26-mock-tab/26-04-SUMMARY.md
</output>
