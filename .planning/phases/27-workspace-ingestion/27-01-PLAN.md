---
phase: 27-workspace-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/engine/ingest.rs
  - src-tauri/src/engine/mod.rs
  - src-tauri/src/engine/tools.rs
  - src-tauri/src/engine/db.rs
  - src-tauri/migrations/0010_ingested_documents.sql
  - src-tauri/tests/fixtures/ingest/README.md
autonomous: false
requirements: [ING-01, ING-02, ING-05]
must_haves:
  truths:
    - "纯 Rust 提取 docx/pdf 文本，无外部进程"
    - "扫描件/解析错误产出 failed 态 + reason，绝不建档"
    - "同一文件重扫以 sha2 内容 hash 命中跳过，不重复提取"
  artifacts:
    - path: "src-tauri/src/engine/ingest.rs"
      provides: "extract docx/pdf, three-state mapping, sha2 hash, truncation"
      contains: "pub fn extract_pdf"
    - path: "src-tauri/migrations/0010_ingested_documents.sql"
      provides: "idempotency table"
      contains: "content_hash TEXT NOT NULL UNIQUE"
    - path: "src-tauri/src/engine/tools.rs"
      provides: "ingest_scan tool registration"
      contains: "ingest_scan"
  key_links:
    - from: "src-tauri/src/engine/tools.rs"
      to: "src-tauri/src/engine/ingest.rs"
      via: "execute dispatch"
      pattern: "ingest::"
---

<objective>
纯 Rust 文档提取地基（27-RESEARCH Pattern 1/2/5）：三 crate + ingest.rs（提取/三态/hash/截断）+ 0010 migration + ingest_scan 确定性工具 + 中文 PDF PoC。

Purpose: ING-01/02/05 的全部机械层；后续 plan 的编排 run 与 HITL 都消费这里的输出。
Output: ingest.rs + migration + 注册的工具 + cargo test 全绿的提取测试（真实中文 fixture）。
</objective>

<context>
@.planning/phases/27-workspace-ingestion/27-RESEARCH.md
@.planning/phases/27-workspace-ingestion/27-CONTEXT.md
@src-tauri/src/engine/tools.rs
@src-tauri/src/engine/mod.rs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 准备中文 PDF 测试 fixtures</name>
  <what-built>fixture 目录与说明</what-built>
  <how-to-verify>
    研究已确认无法用代码可靠生成带 CJK 文本层的真实 PDF（需嵌入 CID 字体）。请用 Word/WPS 手工导出 2 份含中文正文的 PDF + 1 份扫描件（打印后拍照/扫描，或导出为图片型 PDF），放入 `src-tauri/tests/fixtures/ingest/`，命名为 `chinese-1.pdf`、`chinese-2.pdf`、`scanned.pdf`。每份中文 PDF 请在回复中列出 3 个正文里确定出现的中文短语（如"产品需求文档"），供断言使用。
  </how-to-verify>
  <resume-signal>文件已放入目录并附预期中文子串列表</resume-signal>
  <files>src-tauri/tests/fixtures/ingest/</files>
  <action>人工准备 fixtures：2 份中文 PDF + 1 份扫描件放入 src-tauri/tests/fixtures/ingest/（chinese-1.pdf / chinese-2.pdf / scanned.pdf），并附 3 个预期中文子串。</action>
  <verify>ls src-tauri/tests/fixtures/ingest/ 含 chinese-1.pdf、chinese-2.pdf、scanned.pdf</verify>
  <done>三份 fixture 存在且预期中文子串已记录</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest.rs — 提取 + 三态 + hash + 截断</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/engine/ingest.rs, src-tauri/src/engine/mod.rs, src-tauri/tests/fixtures/ingest/README.md</files>
  <read_first>
    - src-tauri/src/engine/mod.rs
    - src-tauri/Cargo.toml
    - 27-RESEARCH.md §Code Examples（docx 定式 + pdf_oxide 语义示意 + sha2 样例）与 §Pitfalls 1/2/3/4
  </read_first>
  <behavior>
    - chinese-1.pdf / chinese-2.pdf → status=extracted，text 含用户提供的预期中文子串
    - scanned.pdf → status=failed，reason="scanned_pdf_no_text_layer"，text 为空
    - 用 Rust 程序生成的最小 .docx（zip 内 word/document.xml，含 w:p/w:t/w:tab）→ extracted 且段落以 \n 分隔
    - 空 zip（无 word/document.xml）→ failed("not_a_docx")
    - 部分页空文本的 PDF → partial + pages_empty 列表（可用单页仅图 PDF 构造，若难构造则以单元级逻辑测试覆盖三态映射函数）
    - file_sha256 同字节 → 同 hash；truncate_for_llm(15000字文本) → 长度 ≤ 12000 + 尾部含"…(全文已存,共 15000 字)"
  </behavior>
  <action>
    1. `cd src-tauri && cargo add pdf_oxide zip quick-xml`（sha2 0.10 已在 Cargo.toml:50，勿重复加）。若 cargo 报 pdf_oxide 传递依赖含 cc/C 构建，停下报告（违反纯 Rust 约束）。
    2. 新建 `src-tauri/src/engine/ingest.rs`，mod.rs 加 `pub mod ingest;`：
       - `pub fn file_sha256(path: &Path) -> Result<String>`（sha2 流式，格式 `{:x}`）
       - `pub fn extract_docx(path: &Path) -> Result<String, String>`：zip + quick-xml 事件流解析 word/document.xml，`w:p`→\n、`w:t` 文本 unescape、`w:tab`→空格、`w:ins` 保留、`w:del` 跳过、`w:tbl` 逐行拼接（按 27-RESEARCH Code Example 定式）；无 word/document.xml → Err("not_a_docx")
       - `pub fn extract_pdf(path: &Path) -> Result<Vec<String>, String>`：pdf_oxide 按页 extract_text，每页收集文本；open/parse Err → Err("parse_error: ...")。以 docs.rs 实际 API 校准签名（vendor 示例 `PdfDocument::open` / `extract_text(i)` 可能不符，以编译为准）
       - 三态映射（纯函数，便于单测）：`pub fn classify_pdf(per_page: &[String]) -> IngestStatus`：空页判定 = trim 后字符数 < 10；全部空 → Failed{reason:"scanned_pdf_no_text_layer"}；部分空 → Partial{pages_empty}；否则 Extracted。docx 提取成功即 Extracted，Err → Failed{reason}
       - `pub fn truncate_for_llm(text: &str) -> String`：> 12000 chars 取首 12000 + `…(全文已存,共 X 字)`（X 为原文字符数）
       - 隔离 trait `pub trait PdfExtractor`（PoC 失败时换实现的接缝，27-RESEARCH Pitfall 1）
    3. 若 pdf_oxide 对真实中文 fixture 提取出乱码/`\u{fffd}`/PUA 码位（PoC 失败信号）：在 ingest.rs 保持 PdfExtractor 抽象并在 SUMMARY 记录，改 trait 默认实现评估 parangi —— 不得静默通过测试。
    4. 测试放 `src-tauri/src/engine/ingest.rs` 内 `#[cfg(test)]`，fixture 路径用 `env!("CARGO_MANIFEST_DIR")/tests/fixtures/ingest/...`，文件缺失时 `eprintln! + return`（不 fail，避免阻塞无 fixture 环境），但本任务执行时 fixture 必须存在且断言生效。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test ingest -- --nocapture</automated>
  </verify>
  <done>cargo test 全绿；中文子串断言真实跑过；扫描件 failed 不产出文本</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: migration 0010 + ingest_scan 工具注册</name>
  <files>src-tauri/migrations/0010_ingested_documents.sql, src-tauri/src/engine/tools.rs, src-tauri/src/engine/db.rs, src-tauri/src/engine/ingest.rs</files>
  <read_first>
    - src-tauri/migrations/0004_memories_knowledge_fts.sql
    - src-tauri/src/engine/db.rs（migration runner 如何发现 .sql 文件）
    - src-tauri/src/engine/tools.rs（execute 同步分发与 ToolOutcome 形状、KNOWLEDGE_CATEGORIES 附近）
  </read_first>
  <behavior>
    - ingest_scan({workspaceRoot}) 对目录内 .docx/.pdf 逐个：sha256 → SELECT ingested_documents WHERE content_hash 命中且 status != 'failed' → skip（返回 skipped 计数）；未命中 → 提取 + 三态
    - 同 path 不同 hash → 视为文档变更，可重新摄取（不 skip）
    - failed 文档不写 hash 记录（D-04：每次重试）
    - 返回 items：[{path, contentHash, status, reason?, pagesEmpty?, text(truncated)}]，text 经 truncate_for_llm
  </behavior>
  <action>
    1. `0010_ingested_documents.sql`：
       ```sql
       CREATE TABLE IF NOT EXISTS ingested_documents (
         id TEXT PRIMARY KEY,
         workspace_id TEXT NOT NULL,
         path TEXT NOT NULL,
         content_hash TEXT NOT NULL UNIQUE,
         status TEXT NOT NULL,           -- extracted/partial/failed
         reason TEXT,
         doc_id TEXT,                    -- consume 后回填 knowledge_docs.id
         extracted_at INTEGER NOT NULL
       );
       CREATE INDEX IF NOT EXISTS idx_ingested_documents_workspace ON ingested_documents(workspace_id);
       ```
       确认 db.rs migration runner 会自动加载新文件（0009 为参照）。
    2. tools.rs `execute` 分发注册 `"ingest_scan"`：args `{workspaceRoot: string}`（必填非空）；调 ingest::scan_workspace(root, conn)：fs 递归枚举 .docx/.pdf（忽略隐藏目录），按上述 behavior 逐文件处理，写库用 INSERT OR REPLACE（hash UNIQUE 冲突=同内容同记录）。失败返回 ToolOutcome::Failed（arg_error 仅参数缺失）。工具非 HITL、确定性（loop_runner 工具白名单若存在则同步加入，grep `knowledge_search` 在 tools.rs/loop_runner.rs 的注册链照抄）。
    3. 单测（内存 SQLite conn）：①同 hash 二次 scan → 第二次全部 skip；②改字节后重扫 → 重新提取；③failed PDF 重扫 → 再次尝试（不 skip）。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test ingest && cargo build</automated>
  </verify>
  <done>ingest_scan 在引擎工具表中可调；hash 幂等三场景测试通过</done>
</task>

</tasks>

<verification>
- `cargo test` 全绿（含中文 fixture 断言）
- grep tools.rs 含 `"ingest_scan"` 分发
- 0010 migration 被 runner 加载（启动日志或单测建表断言）
</verification>

<success_criteria>
ING-01 纯 Rust 提取成立；ING-02 三态映射硬编码且 failed 不建档；ING-05 hash diff 语义有测试锁定。
</success_criteria>

<output>
Create `.planning/phases/27-workspace-ingestion/27-01-SUMMARY.md`
</output>
