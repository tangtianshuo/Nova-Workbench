---
phase: 27-workspace-ingestion
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src-tauri/src/engine/tools.rs
  - src-tauri/src/engine/confirmations.rs
  - src-tauri/src/engine/commands.rs
  - src/ai/tools/knowledgeWrite.ts
  - src-tauri/src/engine/parity.rs
autonomous: true
requirements: [ING-04, ING-05, ING-06]
must_haves:
  truths:
    - "ingestion_batch 单候选可携带 items 数组并原子 consume"
    - "consume 事务内写 knowledge_docs + knowledge_fts，写入后 FTS5 中文检索命中"
    - "重复 consume 按 items[].id 幂等，不重复建档"
    - "PM 四类目双侧可校验通过"
  artifacts:
    - path: "src-tauri/src/engine/commands.rs"
      provides: "engine_consume_ingestion_batch 事务命令"
      contains: "engine_consume_ingestion_batch"
    - path: "src-tauri/src/engine/tools.rs"
      provides: "ingest_submit HITL 工具 + 13 类目"
      contains: "ingest_submit"
  key_links:
    - from: "engine_consume_ingestion_batch"
      to: "knowledge_docs/knowledge_fts"
      via: "单事务 INSERT"
      pattern: "INSERT INTO knowledge_(docs|fts)"
---

<objective>
批量 HITL 后端 + 写路径：PM 四类目扩展（D-06 双侧 parity）、ingest_submit 工具（emit ingestion_batch 候选，D-14 cap 校验）、engine_consume_ingestion_batch 事务命令（knowledge Rust 写 + task/schedule 审计事件，D-15）、consume 幂等（Pitfall 6）。

Purpose: ING-04 后端、ING-05 consume 侧幂等、ING-06 FTS 命中的引擎侧闭环。
Output: 可注册候选、可事务消费、cargo test + npm 测试 + tsc 全绿。
</objective>

<context>
@.planning/phases/27-workspace-ingestion/27-RESEARCH.md
@.planning/phases/27-workspace-ingestion/27-CONTEXT.md
@src-tauri/src/engine/confirmations.rs
@src-tauri/src/engine/commands.rs
@src-tauri/src/engine/tools.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KNOWLEDGE_CATEGORIES +4 PM 类目（D-06 双侧）</name>
  <files>src-tauri/src/engine/tools.rs, src/ai/tools/knowledgeWrite.ts, src-tauri/src/engine/parity.rs</files>
  <read_first>
    - src-tauri/src/engine/tools.rs:351-356（KNOWLEDGE_CATEGORIES 与 parity 注释）
    - src/ai/tools/knowledgeWrite.ts:13-29（TS knowledgeCategories zod enum）
    - grep -rn "架构设计" src src-tauri --include=*test*（既有常量互锁测试位置）
  </read_first>
  <behavior>
    - Rust const 变为 13 类：原 9 类 + "会议纪要", "竞品分析", "需求文档", "项目周报"
    - TS zod enum 同步 13 类
    - 常量互锁测试（若为枚举行数断言则更新为 13）
  </behavior>
  <action>
    1. tools.rs `KNOWLEDGE_CATEGORIES: [&str; 9]` → `[&str; 13]`，追加 "会议纪要", "竞品分析", "需求文档", "项目周报"（顺序照此，D-06）。
    2. knowledgeWrite.ts knowledgeCategories 数组同步追加相同四项（顺序一致）。
    3. 更新两侧常量互锁测试期望（9→13）。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test knowledge && npm run lint && npx vitest run src/ai 2>/dev/null || npm test</automated>
  </verify>
  <done>两侧各 13 类、顺序一致、测试绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest_submit 工具（HITL）+ D-14 cap 校验</name>
  <files>src-tauri/src/engine/tools.rs, src-tauri/src/engine/confirmations.rs</files>
  <read_first>
    - src-tauri/src/engine/tools.rs 中 execute_knowledge_write 的候选创建链（create_candidate 调用形状、ToolOutcome::NeedsConfirmation 返回形状）
    - src-tauri/src/engine/confirmations.rs（create_candidate 签名、params_json 任意 Value、:171 dedup kind 列表——确认 ingestion_batch 不在其中，不改）
  </read_first>
  <behavior>
    - ingest_submit({workspaceId, items:[...]})：items 非空数组；type ∈ {knowledge, task_draft, schedule_draft}；knowledge 项 category 必须 ∈ 13 类；title 非空
    - 单文档 task_draft+schedule_draft 合计 > 5 → arg_error（D-14 Rust 侧校验，按 sourcePath 分组计数）
    - 通过 → 创建 kind="ingestion_batch" 候选，params_json = {workspaceId, productId, items}
    - items[].id 格式 ing-{hash8}（hash8 = contentHash 前 8 位，由编排 prompt 约定）
  </behavior>
  <action>
    1. tools.rs 新增 `execute_ingest_submit(conn, args, ctx)`：校验如 behavior；productId 必填（ctx.product_id fallback，与 knowledge_write 同式，D-05）；cap 校验：对 items 按 sourcePath（或 contentHash）分组，每组 task_draft+schedule_draft 计数 >5 → ToolOutcome::Failed{arg_error:true, message 含 "drafts exceed cap 5"}。
    2. 复用既有 NeedsConfirmation/create_candidate 通路（照抄 knowledge_write 的候选 ID/token 生成方式），kind 用自由字符串 "ingestion_batch"；候选创建后返回确认请求，模型停轮（同其他 HITL 工具语义）。
    3. 单测：合法 payload 建候选成功；6 条 task_draft 同 sourcePath → 拒；category 非法 → 拒；空 items → 拒。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test ingest</automated>
  </verify>
  <done>ingest_submit 可产出 ingestion_batch 候选；cap 双保险的 Rust 侧生效</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: engine_consume_ingestion_batch 事务命令（D-15 写路径）</name>
  <files>src-tauri/src/engine/commands.rs</files>
  <read_first>
    - src-tauri/src/engine/commands.rs（engine_commit_deliverable 的命令形状：invoke 参数、事务、事件写入、返回 JSON）
    - src-tauri/src/engine/event_log.rs（审计事件写入 API）
    - src-tauri/migrations/0004_memories_knowledge_fts.sql（knowledge_docs/knowledge_fts 列）
    - src-tauri/src/engine/fts_tokens.rs（fts_match_string/插入 token 化入口）
  </read_first>
  <behavior>
    - consume(token, editedItems)：原子条件 UPDATE 候选为 consumed（照抄既有 consume 语义，重复 consume 返回幂等成功）
    - 事务内：selected 且 type=knowledge 的项 → INSERT knowledge_docs（product_id 必填, source 溯源字段记 ingested path/contentHash）+ INSERT knowledge_fts（fts_tokens 分词）；回填 ingested_documents.doc_id
    - selected 且 type=task_draft/schedule_draft → 不写业务表，写 agent_events 审计事件（kind 如 "ingestion_task_applied"/"ingestion_schedule_applied"，payload 带 item 全文）——webview applier 消费（D-15，Plan 03 接）
    - failed 项防御：consume 遇 failed 项直接跳过不建档（Pitfall 2 二道闸）
    - 写侧幂等：按 items[].id 查重（knowledge_docs.source_item_id 或查 ingested_documents），重复项跳过（Pitfall 6）
    - consume 后立即 FTS5 检索 knowledge 标题/内容中文子串可命中（ING-06）
  </behavior>
  <action>
    1. commands.rs 新增 `#[tauri::command] pub async fn engine_consume_ingestion_batch(state, token: String, items: Vec<Value>) -> Result<Value, String>`：单 SQLite 事务（BEGIN/COMMIT，照 engine_commit_deliverable 的事务模式）。editedItems 覆盖候选 params 的 items（selected/title/content 可被 UI 改过，Phase 26 editedDraft 同构）。
    2. knowledge 写入复用 knowledge_write 提交路径的 INSERT 语句（含 version/summary 列；summary 取 content 前 120 字符）；FTS 用 fts_tokens 同一分词器。
    3. 审计事件：event_log 写 `{kind:"ingestion_task_applied"|"ingestion_schedule_applied", payload:{item, workspaceId, productId, sessionId}}`。
    4. lib.rs/commands 注册进 invoke_handler。
    5. 单测（内存 DB）：①consume 后 knowledge_docs 有行且 fts 查询中文字串命中；②同 token 二次 consume 幂等（不抛错、不重复行）；③仅 task_draft 项 → 只产生审计事件，knowledge_docs 零新增。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test ingest && cargo test consume && npm run lint</automated>
  </verify>
  <done>事务命令可用且注册；三场景测试绿；FTS 命中断言真实跑过</done>
</task>

</tasks>

<verification>
- cargo test 全绿；npm run lint 绿
- ingestion_batch 不在 dedup 列表（未改动 confirmations.rs:171 即为正确）
</verification>

<success_criteria>
ING-04 后端（单候选 items + 原子 consume + 逐项覆盖）；ING-05 consume 幂等；ING-06 Rust 侧 FTS 命中。
</success_criteria>

<output>
Create `.planning/phases/27-workspace-ingestion/27-02-SUMMARY.md`
</output>
