---
phase: 27-workspace-ingestion
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
files_modified:
  - src/stores/tabRunStore.ts
  - src/stores/ingestionStore.ts
  - src/stores/taskStore.ts
  - src/stores/scheduleStore.ts
  - src/views/FileArchiveView.tsx
  - src/components/workspace/IngestionPanel.tsx
  - src/components/workspace/IngestionBatchCard.tsx
  - src/ai/api.ts
  - src/App.tsx
autonomous: false
requirements: [ING-03, ING-04, ING-06]
must_haves:
  truths:
    - "用户在 FileArchiveView 勾选文件/文件夹发起摄取，编排 run 进度可见"
    - "用户在聚合卡全选/全不选、逐项编辑、一次提交，确认后才落业务数据"
    - "全局队列只出精简入口卡，点击跳 FileArchiveView 聚合视图"
    - "确认后 toast 汇总，任务/日程立即可见，知识可 FTS 中文检索"
    - "无选中产品时摄取入口禁用并提示"
  artifacts:
    - path: "src/stores/ingestionStore.ts"
      provides: "摄取结果投影、三态列表、批量卡状态"
      contains: "export const useIngestionStore"
    - path: "src/components/workspace/IngestionPanel.tsx"
      provides: "范围勾选 + 三态列表 + 聚合确认视图挂载"
      contains: "extracted"
    - path: "src/components/workspace/IngestionBatchCard.tsx"
      provides: "全局队列精简入口卡"
      contains: "摄取批次待确认"
  key_links:
    - from: "src/stores/tabRunStore.ts"
      to: "engineRun"
      via: "kind 'ingestion' 复用 startTabRun"
      pattern: "'ingestion'"
    - from: "src/stores/ingestionStore.ts"
      to: "engine_consume_ingestion_batch"
      via: "invoke 提交 editedItems"
      pattern: "engine_consume_ingestion_batch"
    - from: "webview applier"
      to: "taskStore/scheduleStore"
      via: "consume 返回的 task/schedule 项调既有 action"
      pattern: "(addTask|addEvent)"
---

<objective>
前端编排与批量 HITL UI（D-01..D-16 全部体验面）：ingestion TabRunKind + ingestionStore + FileArchiveView 摄取区（勾选/三态列表/聚合编辑视图）+ 全局队列入口卡 + webview applier（task/schedule 落库 + Rust 审计事件已由 consume 记）+ 人工验证。

Purpose: ING-03/04 的用户可见闭环 + ING-06 端到端验证。
Output: 可用的摄取 UI 流 + 人工 UAT 通过。
</objective>

<context>
@.planning/phases/27-workspace-ingestion/27-CONTEXT.md
@.planning/phases/27-workspace-ingestion/27-RESEARCH.md
@src/stores/tabRunStore.ts
@src/views/FileArchiveView.tsx
@src/ai/api.ts
</context>

<interfaces>
From src/stores/tabRunStore.ts:
```typescript
export type TabRunKind = 'requirement' | 'prototype' | 'code' | 'test' | 'competitor'
  | 'deliverable-single' | 'deliverable-batch' | 'product-skill';   // + 'ingestion' (this plan)
export interface StartTabRunParams { tabId: string; kind: TabRunKind; productId: string;
  userMessage: string; coreContext: string; sessionTitle?: string;
  workspaceId?: string | null; workspaceRoot?: string | null; }
startTabRun(params: StartTabRunParams): string
```
From src-tauri (27-02): `engine_consume_ingestion_batch(token: string, items: Value[])` →
`{ knowledgeWritten, taskDrafts, scheduleDrafts, skipped }`
ingestion_batch candidate params: `{ workspaceId, productId, items: [{ id: "ing-{hash8}", type: 'knowledge'|'task_draft'|'schedule_draft', title, content?, category?, dueHint?, selected, sourcePath, contentHash, status }] }`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: tabRunKind 'ingestion' + ingestionStore + API 桥</name>
  <files>src/stores/tabRunStore.ts, src/stores/ingestionStore.ts, src/ai/api.ts</files>
  <read_first>
    - src/stores/tabRunStore.ts（全文：startTabRun、candidate 路由 routeEngineCandidateToConsole、pendingDeliverables 模式）
    - src/ai/api.ts（engineRun/engineCommitDeliverable 的 invoke 封装式样）
    - src/stores/workspaceStore.ts:184-200（scan_workspace_folder 现有调用）
    - src/stores/uiStore.ts（selectedProductId 读法）
  </read_first>
  <acceptance_criteria>
    - tabRunStore.ts TabRunKind 联合含 `'ingestion'`
    - src/stores/ingestionStore.ts 导出 `useIngestionStore`，state 含 `scanResults`（三态项数组）、`batchCandidate`、`batchItems`（selected/title/content 可变副本）、actions `startIngestion(workspaceId, root, selectedPaths)`、`submitBatch()`、`dismissScan()`、`refreshBadge()`
    - src/ai/api.ts 导出 `engineConsumeIngestionBatch(token: string, items: unknown[])`，invoke cmd 名为 `engine_consume_ingestion_batch`
    - startIngestion 调 startTabRun({ tabId: 'workspace-ingest', kind: 'ingestion', priority 走 interactive 默认, userMessage: '摄取工作区文档', coreContext: JSON.stringify({workspaceId, productId, selectedPaths}) , workspaceRoot: root })
    - 无选中产品（!useUIStore.getState().selectedProductId）时 startIngestion 返回错误提示（D-05），不发起 run
    - ingestion run 的 ingestion_batch 候选路由进 ingestionStore（不进 console PRD 卡链），沿用 routeEngineCandidateToConsole 的分流点加分支
  </acceptance_criteria>
  <action>
    按 acceptance_criteria 实现。编排 run 的 userMessage 提示词约定（LLM 在 run 内依次调 ingest_scan → 分类产 knowledge 内容 → 抽草稿（D-13 保守 + 每文档 ≤5）→ ingest_submit）写进 userMessage 模板常量 `INGESTION_PROMPT`，内容含：每文档一条知识文档、LLM 生成语义标题（D-08）、task/schedule 仅明确行动项、ing-{hash8} 命名规则。web dev fallback：无引擎时 startIngestion 提示禁用（Phase 26 同款，不留 mock）。tabRunStore 对 'ingestion' kind 的候选分流：kind==='ingestion_batch' → ingestionStore.setBatchCandidate(candidate)，其余照旧。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>类型/桥/store 就绪，候选可路由进摄取面</done>
</task>

<task type="auto">
  <name>Task 2: FileArchiveView 摄取区 + 聚合卡 + 队列入口卡</name>
  <files>src/views/FileArchiveView.tsx, src/components/workspace/IngestionPanel.tsx, src/components/workspace/IngestionBatchCard.tsx, src/App.tsx</files>
  <read_first>
    - src/views/FileArchiveView.tsx（现有结构、workspace 文件列表来源）
    - src/components/product/ 中 TabRunPanel 与 PrdDraftDialog（进度面板与编辑 Dialog 同构参照）
    - src/stores/chatConsoleStore.ts 或 confirmationStore 的全局队列渲染入口（入口卡挂载点）
    - src/components/ui/index.ts（Card/Button/Dialog/Badge/Input 可用组合）
  </read_first>
  <acceptance_criteria>
    - IngestionPanel.tsx 含：文件/文件夹勾选列表 + "全选整个工作区"快捷（D-02）、摄取按钮（无选中产品 disabled + 提示文案"请先选择产品"）、TabRunPanel 同构进度区、三态列表（Badge variant: success/extracted、warning/partial+pages_empty、danger/failed+reason，D-01/ING-02）、摄取按钮徽章计数（refreshBadge：枚举+hash 对比，无提取、进入页面时触发一次，D-03）
    - IngestionPanel 内嵌聚合确认视图（展开态）：按 type 三组分组，组级全选/全不选（D-10）；每项 selected 开关 + 标题行内改 + 知识 content 编辑 Dialog（复用 PrdDraftDialog/MDXEditor 模式，D-11）；每项显示源文件名徽章；一次提交按钮 → submitBatch()
    - IngestionBatchCard.tsx：精简入口卡文案「摄取批次待确认（N 项）」，点击 set activeTab='files' 并展开聚合视图（D-09），确认动作不在卡内
    - submitBatch()：调 engineConsumeIngestionBatch(token, batchItems) → 成功后 toast `已落库：N 知识 + M 任务草稿 + K 日程草稿`（useToast，D-12）→ 刷新三态列表，留在 FileArchiveView
    - webview applier：consume 返回的 task/schedule 项逐个调 taskStore.addTask / scheduleStore.addEvent（既有 action，D-15/16，正式落库无 draft 态）——若 consume 返回即含审计事件 id 则随行传入溯源字段
    - App.tsx 懒加载与 FileArchiveView 挂载 IngestionPanel 不新增 tab（D-01）
    - 全代码 grep 无新增 mock/fabricate 路径
  </acceptance_criteria>
  <action>
    按 acceptance_criteria 实现，样式走 design tokens（bg-bg-primary/text-text-*/border-border-subtle，禁 raw hex）。部分 UI 状态（如 waiting-for-confirmation 时自动展开聚合视图）复用 tabRunStore runs 记录驱动。扫描后 failed 项重扫允许重试（自然发生：D-04 failed 不进 hash 跳过）。
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>tsc/build 绿；D-01..D-05、D-09..D-12 的 UI 语义全部落地</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 端到端人工验证（ING-03/04/06）</name>
  <what-built>完整摄取流：勾选→编排 run→三态列表→聚合确认→落库→检索</what-built>
  <how-to-verify>
    前置：Tauri dev（npm run tauri:dev）、已选产品、工作区含中文 docx + 中文 pdf + 扫描件（27-01 fixtures 可复制进工作区）。
    1. FileArchiveView：未选产品时摄取按钮禁用且提示 → 选中产品后可点
    2. 勾选 2 个文档发起摄取：进度可见（扫描→分类→草稿），结束后三态列表正确（扫描件显示 failed+原因）
    3. 全局队列出现「摄取批次待确认（N 项）」入口卡，点击跳回文件区并展开聚合视图
    4. 聚合视图：全不选→提交 无落库；恢复全选、改一个标题、点开编辑一个知识 content→提交；toast 汇总数字正确
    5. 知识库 FTS 中文检索：搜刚摄取文档的中文短语可命中（ING-06）
    6. 任务页/日历立即可见确认的任务/日程草稿（ING/D-16）
    7. 对同一批文件再次发起摄取：已摄取项被跳过（diff），扫描件 failed 仍重试（ING-05）
  </how-to-verify>
  <resume-signal>Type "approved" 或描述问题</resume-signal>
  <files>（无代码改动）</files>
  <action>人工按 how-to-verify 7 步执行端到端验证；失败项反馈给执行 agent 修复后复验。</action>
  <verify>7 步全部通过，用户回复 approved</verify>
  <done>ING-03/04/06 用户视角确认达成</done>
</task>

</tasks>

<verification>
- npm run lint && npm run build 绿
- 人工验证 7 步全过
</verification>

<success_criteria>
ING-03 进度全程可见；ING-04 批量 HITL 聚合确认闭环；ING-06 端到端 FTS 命中。Phase 27 全部 6 需求达成（ING-01/02/05 由 27-01/27-02 + 步骤 7 复验）。
</success_criteria>

<output>
Create `.planning/phases/27-workspace-ingestion/27-03-SUMMARY.md`
</output>
