---
phase: 27-workspace-ingestion
plan: 04
type: execute
wave: 1
depends_on: ["27-01", "27-02", "27-03"]
files_modified:
  - src-tauri/src/lib.rs
gap_closure: true
autonomous: false
requirements: [ING-01, ING-02, ING-05, ING-06]
must_haves:
  truths:
    - "真机启动后 nova.db 的 _sqlx_migrations 含 version 10/11，ingested_documents 表存在"
    - "摄取 run 的 ingest_scan 不再报 'no such table: ingested_documents'，产出候选并进入 ingestion_batch HITL"
    - "ingest_submit 写入 candidate 不被 kind CHECK 拒绝（ingestion_batch 合法 kind）"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "sql_migrations() 注册 0010 + 0011"
      contains: "0011_confirmation_kind_ingestion.sql"
  key_links:
    - from: "src-tauri/src/lib.rs sql_migrations()"
      to: "tauri-plugin-sql add_migrations"
      via: "include_str! 注册表"
      pattern: "include_str!\\!\\(\"../migrations/001[01]_"
---

<objective>
修复 UAT-2 major gap：sql_migrations() 注册表漏注册 0010/0011，真机 nova.db 缺 ingested_documents 表与 ingestion_batch kind CHECK，导致摄取 run 0 候选。

Purpose: 打通 Phase 27 摄取全链路的最后断点；已存的迁移 SQL（0010 纯 CREATE TABLE IF NOT EXISTS、0011 copy→drop→rename 重建）对已有库安全，仅缺接线。
Output: lib.rs 两行注册 + 注册表完整性防复发测试；随后人工回归 UAT-2 全链路与被跳过的 UAT-3/4/5/6/7。
</objective>

<context>
@.planning/debug/ingestion-zero-candidates.md
@.planning/phases/27-workspace-ingestion/27-HUMAN-UAT.md
@src-tauri/src/lib.rs
@src-tauri/src/engine/db.rs

根因（已诊断，勿再排查）：lib.rs sql_migrations() Vec 止于 0009；Rust 单测走 db.rs testing::run_migrations 直扫 migrations 目录（全量），与生产注册表是两条路径 → 测试全绿但真机断档。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 注册 0010/0011 + 注册表完整性断言测试</name>
  <files>src-tauri/src/lib.rs</files>
  <behavior>
    - sql_migrations() 返回的 Migration 数量 == src-tauri/migrations 目录 .sql 文件数量（新增迁移漏注册时测试立刻失败）
    - 注册表 version 与文件名前缀（0001..0011）一一对应且升序
  </behavior>
  <action>
    1. 在 sql_migrations() Vec 末尾（0009 条目之后）追加两个条目，写法与 0008/0009 完全一致：
       - `version: 10, description: "ingested_documents idempotency", sql: include_str!("../migrations/0010_ingested_documents.sql"), kind: MigrationKind::Up`
       - `version: 11, description: "confirmation kind ingestion_batch", sql: include_str!("../migrations/0011_confirmation_kind_ingestion.sql"), kind: MigrationKind::Up`
    2. 在 lib.rs 加 `#[cfg(test)] mod migration_registry_tests`：
       - 用 std::fs::read_dir 扫 `concat!(env!("CARGO_MANIFEST_DIR"), "/migrations")` 数 .sql 文件（复用 db.rs testing::run_migrations 的扫描写法）
       - `assert_eq!(sql_migrations().len(), file_count, "每个 migrations/*.sql 必须注册进 sql_migrations() — 漏注册=真机断档（27-04 根因）")`
       - 再断言 versions 严格递增且最大 version == 文件名前缀最大值
       - 注意：sql_migrations() 当前是私有 fn，测试同 crate 可直接调用，无需改可见性。
    3. 前向迁移安全性确认（读文件即可，无需改动）：0010 全部 CREATE TABLE/INDEX IF NOT EXISTS；0011 是 copy→drop→rename 重建（与 0006/0008/0009 同款），对已有数据的 nova.db 安全。tauri-plugin-sql 按 _sqlx_migrations 版本自动应用 0010/0011。
    4. 运行测试验证：先临时注释掉一个新条目确认测试会 RED（防复发测试真的能抓），再恢复确认 GREEN。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test migration_registry && cargo build</automated>
  </verify>
  <done>注册表完整性测试通过且能抓漏注册；cargo build 通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 真机回归 — UAT-2 全链路 + 跳过项 UAT-3/4/5/6/7</name>
  <what-built>0010/0011 已注册，重启后真机 DB 自动前向迁移，摄取链路应完整可用</what-built>
  <how-to-verify>
    前置：`npm run tauri:dev` 重启（首次启动 tauri-plugin-sql 自动应用 0010/0011）。
    1. UAT-2 回归：FileArchiveView → 选中产品 → 勾选中文 docx+pdf+扫描件发起摄取 → 预期「生成完成 — N 份候选已提交确认」（N>0），扫描件显示 failed+原因。
    2. UAT-3：全局队列出现「摄取批次待确认」入口卡，点击跳回文件区并展开聚合视图。
    3. UAT-4：批量 HITL——全不选提交无落库；全选+改标题+编辑 content 提交，toast 汇总数字正确。
    4. UAT-5：知识库搜刚摄取文档的中文短语可命中（FTS）。
    5. UAT-6：确认的任务/日程草稿在任务页与日历立即出现。
    6. UAT-7：同一批文件再次摄取——已摄取项 skip（diff 生效），扫描件 failed 仍重试。
    可选验证：`%APPDATA%\com.nova.pm-workspace\nova.db` 的 _sqlx_migrations 现含 version 10、11。
  </how-to-verify>
  <resume-signal>逐项报 pass/issue；全部 pass 则 gap 关闭</resume-signal>
  <files>.planning/phases/27-workspace-ingestion/27-HUMAN-UAT.md</files>
  <action>用户重启 tauri:dev 后按 how-to-verify 逐项回归 UAT-2/3/4/5/6/7，结果回填 27-HUMAN-UAT.md</action>
  <verify>27-HUMAN-UAT.md 中 UAT-2..7 全部标记 pass</verify>
  <done>真机摄取产出 N>0 候选进入 ingestion_batch HITL，全部回归项 pass</done>
</task>

</tasks>

<verification>
- cargo test migration_registry 通过（且 RED 验证过能抓漏注册）
- 真机摄取产出 N>0 候选并进入 ingestion_batch HITL
- UAT-2..7 全部回归 pass
</verification>

<success_criteria>
UAT-2 major gap 关闭：真机 DB schema 到 v11，摄取→候选→批量确认→落库全链路可用；注册表完整性测试杜绝同类「测试绿、真机断档」复发。
</success_criteria>

<output>
Create `.planning/phases/27-workspace-ingestion/27-04-SUMMARY.md`
</output>
