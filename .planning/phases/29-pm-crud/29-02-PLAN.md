---
phase: 29-pm-crud
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src-tauri/src/engine/tools.rs
  - src-tauri/src/engine/loop_runner.rs
autonomous: true
requirements: [PM-01, PM-02]

must_haves:
  truths:
    - "模型可见的 9 个新工具 schema(task_create/update/complete/delete/search + schedule_create/update/delete/search)出现在 registry"
    - "search 直读免确认;create/update/complete 直写免确认(落 tasks/schedules 表)"
    - "delete 返回 pm_write AwaitConfirmation 候选,不直接删"
    - "单 run 累计免确认写 >5 条后,后续轻写也升级为 pm_write 候选"
  artifacts:
    - path: "src-tauri/src/engine/tools.rs"
      provides: "9 个 PM 工具注册 + 分发 + 三档风险执行"
      contains: "task_create"
    - path: "src-tauri/src/engine/loop_runner.rs"
      provides: "cap-5 免确认写计数升级"
      contains: "pm_writes_used"
  key_links:
    - from: "src-tauri/src/engine/tools.rs"
      to: "src-tauri/src/engine/pm_store.rs"
      via: "工具执行调 SQL CRUD"
      pattern: "pm_store::(insert_task|list_tasks|delete_task|upsert_schedule)"
    - from: "src-tauri/src/engine/tools.rs"
      to: "src-tauri/src/engine/confirmations.rs"
      via: "delete/cap 升级创建 pm_write 候选"
      pattern: "create_candidate\\(conn, \"pm_write\""
---

<objective>
9 工具 + 三档风险(PM-01/PM-02):按 CONTEXT 工具面裁定注册 task/schedule 四件套 ×2 + search,读免确认、轻写免确认直落库、delete 走 pm_write HITL;loop_runner 实现 cap-5 免确认写升级护栏。

Purpose: 助手真实替用户写任务/日程(SC-1),三档风险分级生效(SC-2)。
Output: tools.rs 9 工具 + ToolKind::Pm + loop_runner 写计数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-pm-crud/29-CONTEXT.md
@.planning/phases/29-pm-crud/29-01-SUMMARY.md

<interfaces>
From src-tauri/src/engine/pm_store.rs (29-01 产出):
```rust
pub fn insert_task(conn: &Connection, t: &Value) -> Result<String>;
pub fn update_task(conn: &Connection, id: &str, updates: &Value) -> Result<bool>;
pub fn delete_task(conn: &Connection, id: &str) -> Result<bool>;   // 含 schedules.task_id 反向清理
pub fn list_tasks(conn: &Connection, filters: &Value) -> Result<Vec<Value>>;  // camelCase Task 形状
pub fn upsert_schedule_from_json(conn: &Connection, e: &Value) -> Result<String>;
pub fn update_schedule(conn: &Connection, id: &str, updates: &Value) -> Result<bool>;
pub fn delete_schedule(conn: &Connection, id: &str) -> Result<bool>;
pub fn list_schedules(conn: &Connection, filters: &Value) -> Result<Vec<Value>>;
```

From src-tauri/src/engine/tools.rs 既有契约:
- `ToolOutcome::{Executed(Value), AwaitConfirmation{candidate, wait_key, wait_value}, Failed{message, arg_error}}`
- `ToolCtx { session_id, product_id, workspace_root }`
- registry() 静态表 + schemas() 自动投影 + idempotency() 查表
- 既有测试 `schemas_three_tools_with_port01_suffix` 断言工具名列表 — 新工具追加进去

From src-tauri/src/engine/loop_runner.rs:
- `execute_async` 分发(313 行附近);`ToolOutcome::Executed` 分支 tool_calls_executed += 1
- 确认等待文案先例:`CONFIRMATION_REQUIRED_INGEST` + wait_key="error"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools.rs — 9 个 PM 工具注册 + 执行(读/轻写直执行,delete 走 pm_write)</name>
  <files>src-tauri/src/engine/tools.rs</files>
  <read_first>
    - src-tauri/src/engine/tools.rs (全文,重点 registry()/execute()/execute_ingest_submit/arg_fail 先例与 tests)
    - src-tauri/src/engine/pm_store.rs (29-01 产出的函数签名)
    - src/stores/scheduleStore.ts (ScheduleEvent type/status 枚举 — 工具参数枚举照抄)
  </read_first>
  <behavior>
    - Test 1: `schemas_three_tools_with_port01_suffix` 更新后的名字列表含全部 9 个新名字且顺序追加在 ingest_submit 之后
    - Test 2: execute("task_create", {title:"写 PRD", priority:"high", deadline:"2026-09-03"}) → Executed,返回 {"taskId": "<uuid>", "created": true};SELECT tasks 含该行
    - Test 3: task_create 缺 title / priority 非法枚举 → Failed { arg_error: true, message 含 "arg validation failed" }
    - Test 4: task_update({taskId, updates:{status:"进行中"}}) → Executed {"updated": true};未知 taskId → Failed non-arg_error
    - Test 5: task_complete({taskId}) → 等价 update status='已完成',Executed(合并实现 per CONTEXT 自裁:单独工具名,内部走 update_task)
    - Test 6: task_delete({taskId}) → AwaitConfirmation,candidate.kind=="pm_write",candidate.params=={action:"task_delete", taskId, title};DB 中 tasks 行仍在;重复同参调用 dedup 同 token
    - Test 7: schedule_create({title, date:"2026-09-03", time:"10:00", type:"meeting"}) → Executed {"eventId","created":true};type 非法枚举 → arg_error;schedule_update/schedule_delete 对称(schedule_delete → pm_write action:"schedule_delete")
    - Test 8: task_search({status:"未开始"}) / schedule_search({date:"2026-09-03"}) → Executed matches 数组;limit 上限 50(学 MAX_ARTICLES),超限 arg_error
    - Test 9: idempotency 表 — search 工具 "rerunnable",写工具与 delete "verify_first"
  </behavior>
  <action>
tools.rs 修改:

1. `ToolKind` 加 `Pm` 变体。

2. registry() 追加 9 个 ToolSpec(参数 schema 对齐 TS 字段名,id 服务端生成不给模型):

- `task_create`: properties { title: string minLength 1, priority: enum [high,medium,low], deadline: string (YYYY-MM-DD), description: string, projectId: string }, required [title]; kind: Pm; idempotency "verify_first"。执行:组装 task JSON {title, priority ?? "medium", status "未开始", deadline ?? "", description ?? "", projectId, assignee "AI 助手", assigneeAvatar "AI", categoryId ""} → pm_store::insert_task → Executed json!({"taskId": id, "created": true, "title": title})
- `task_update`: { taskId: string, updates: object (任意 Task 可写字段) }, required [taskId, updates];执行 update_task(updateTask 列白名单);命中 Executed {"updated": true},未命中 Failed{arg_error:false}
- `task_complete`: { taskId }, required [taskId];执行 = update_task(id, {"status":"已完成"});返回 {"completed": true}
- `task_delete`: { taskId }, required [taskId];先 SELECT title(供卡片摘要),行不存在 Failed{arg_error:false};否则 `confirmations::create_candidate(conn, "pm_write", &json!({"action":"task_delete","taskId":id,"title":title}), Some(&format!("删除任务「{title}」")), Some(ctx.session_id))` → AwaitConfirmation { candidate: json!({"kind":"pm_write","confirmationToken":…,"summary":…,"action":"task_delete","taskId":id,"title":title}), wait_key: "error", wait_value: "Explicit confirmation is required before deleting." }(新常量 CONFIRMATION_REQUIRED_PM_WRITE)
- `task_search`: { status: string, priority: string, projectId: string, deadline: string, limit: integer 1..=50 } 全可选;执行 pm_store::list_tasks → Executed {"matches": rows, "count": n}
- `schedule_create`: { title minLength 1, date (YYYY-MM-DD), time (HH:mm), type: enum [meeting,deadline,task,reminder,review,sync], location, projectId, taskId }, required [title, date];默认 time ""、type "reminder"、location ""、status "未开始" → upsert_schedule_from_json → Executed {"eventId", "created": true, "title"}
- `schedule_update`: { eventId: string, updates: object }, required [eventId, updates]
- `schedule_delete`: { eventId } → pm_write candidate {action:"schedule_delete", eventId, title},摘要「删除日程「X」」
- `schedule_search`: { date, projectId, type, limit 1..=50 } → list_schedules

参数校验失败一律走既有 `arg_fail("task_create", "…")`(arg_error: true)。每个 description 明确写「创建任务并立即生效,无需确认」「删除任务,需用户确认」等三档语义,末尾自动拼 PORT_01_SUFFIX(schemas() 已做)。

3. `execute()` match 加 9 个分支(全部同步,不进 execute_async):`"task_create" => execute_pm_task_create(conn, args, ctx)` 等;删除类与轻写类共用 `fn pm_write_escalated(conn, ctx, action, params, summary) -> ToolOutcome`(cap 升级由 Task 2 的 ctx 门闩复用同一函数)。

4. ToolCtx 加字段:
```rust
pub pm_writes_used: u32,  // 本 run 已落库的免确认 PM 写条数(cap-5 门闩输入)
```
所有既有构造点(loop_runner、tests)补 `pm_writes_used: 0`。轻写工具(create/update/complete)在执行前检查 `ctx.pm_writes_used >= PM_WRITE_CAP`(新常量 `pub const PM_WRITE_CAP: u32 = 5;`,注释注明学 ingest INGEST_DRAFT_CAP 先例):超限则不直写,改为 create_candidate(kind "pm_write", params {action:"<工具名>", args: 原始 args, reason:"cap-5 escalation"}, summary「本 run 写入已超 5 条,需确认」) → AwaitConfirmation(候选 params.action 用原工具名,29-03 consume 按原语义执行)。

5. 更新既有测试 `schemas_three_tools_with_port01_suffix` 的名字断言向量与 idempotency 断言;<behavior> 九条测试写进 mod tests(mem_conn 起库,直接 INSERT 或调 pm_store 造数据)。

6. 删除 tools.rs 第 298-301 行「PM CRUD tools NOT registered」注释块(它已被本任务实现取代)。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test engine::tools exit 0</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "\"task_create\"\|\"task_update\"\|\"task_complete\"\|\"task_delete\"\|\"task_search\"\|\"schedule_create\"\|\"schedule_update\"\|\"schedule_delete\"\|\"schedule_search\"" src-tauri/src/engine/tools.rs ≥ 18(registry + dispatch 各一处)
    - grep -n "PM_WRITE_CAP\|pm_writes_used\|Pm" src-tauri/src/engine/tools.rs 命中
    - grep -n "create_candidate(conn, \"pm_write\"" src-tauri/src/engine/tools.rs 命中
    - 「NOT registered」旧注释已删(grep "are NOT registered" 无命中)
    - cargo test engine::tools exit 0;cargo test --lib 全绿
  </acceptance_criteria>
  <done>9 工具注册+分发+三档执行全落地,cargo 测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: loop_runner — cap-5 写计数传递</name>
  <files>src-tauri/src/engine/loop_runner.rs</files>
  <read_first>
    - src-tauri/src/engine/loop_runner.rs (tool_calls_executed 计数区、ToolCtx 构造点 308-312)
    - src-tauri/src/engine/tools.rs (Task 1 加的 PM_WRITE_CAP 与 ToolCtx.pm_writes_used)
  </read_first>
  <action>
loop_runner.rs:

1. run loop 起始(tool_calls_executed 声明旁)加 `let mut pm_writes_used: u32 = 0;` — run 内局部变量即 per-run 状态(调度器/scheduler 不动,写计数生命周期 = 单次 engine_run,重启 run 重新计,符合「单 run 累计」语义)。

2. ToolCtx 构造(约 308 行)传 `pm_writes_used`。

3. `ToolOutcome::Executed` 分支:若 `call.name` 属于 PM 轻写集合(task_create/task_update/task_complete/schedule_create/schedule_update,提取为 tools.rs `pub fn is_pm_light_write(name: &str) -> bool`),`pm_writes_used += 1`(注意在 execute_async 返回后判定,不依赖 outcome 内容 — AwaitConfirmation 的升级写不计入免确认数)。

4. 测试:在 loop_runner tests(或 tools tests)加一条:构造 ToolCtx { pm_writes_used: 5 } 调 task_create → AwaitConfirmation(kind pm_write, params.action=="task_create", reason 含 "cap");pm_writes_used: 4 → Executed 正常直写。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test loop_runner exit 0 && cargo test pm_write_cap exit 0</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "pm_writes_used" src-tauri/src/engine/loop_runner.rs 命中(声明 + ToolCtx 传递 + 自增)
    - grep -n "is_pm_light_write" src-tauri/src/engine/tools.rs 与 loop_runner.rs 均命中
    - cargo test --lib 全绿;npm run lint 不受影响(纯 Rust)
  </acceptance_criteria>
  <done>cap-5 护栏 per-run 生效:第 6 条免确认写起升级 HITL</done>
</task>

</tasks>

<verification>
- cd src-tauri && cargo test --lib 全绿(含既有 parity/fixture 测试 — 无新事件种类本 plan 不动 fixture)
- 工具 schema 经 schemas() 投影自动带 PORT_01_SUFFIX
</verification>

<success_criteria>
- 模型可调 9 工具真实读写 tasks/schedules 表(非 mock)
- delete/cap 升级返回 pm_write AwaitConfirmation,不盲写
- 读免确认 + 轻写免确认 + 删除确认 三档齐备
</success_criteria>

<output>
After completion, create `.planning/phases/29-pm-crud/29-02-SUMMARY.md`
</output>
