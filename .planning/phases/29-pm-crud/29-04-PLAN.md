---
phase: 29-pm-crud
plan: 04
type: execute
wave: 3
depends_on: [29-01, 29-03]
files_modified:
  - src/stores/storage/pmRepo.ts
  - src/stores/taskStore.ts
  - src/stores/scheduleStore.ts
  - src/stores/storage/initializeDatabase.ts
  - src/stores/chatConsoleStore.ts
  - src/stores/tabRunStore.ts
autonomous: false
requirements: [PM-03, PM-04]

must_haves:
  truths:
    - "Tauri 模式下任务页/日历的 UI 手动 CRUD 直接读写 tasks/schedules 关系表(kv persist 退役)"
    - "agent 免确认写入完成后任务页/日历立即出现新数据(事件驱动 refresh)"
    - "重启后任务/日程不丢(关系表为唯一真相源)"
    - "对话驱动端到端可用:「建 3 个任务」→ 免确认直落库立即可见 →「删掉」→ HITL 卡确认删除 → 重启不丢"
  artifacts:
    - path: "src/stores/storage/pmRepo.ts"
      provides: "webview 侧 SQL 读写适配"
      exports: ["loadTasks", "upsertTaskRow", "deleteTaskRow", "loadSchedules", "upsertScheduleRow", "deleteScheduleRow"]
    - path: "src/stores/taskStore.ts"
      provides: "公开 API 不变,Tauri 下内部走 SQL"
      contains: "refreshFromSql"
  key_links:
    - from: "src/stores/chatConsoleStore.ts"
      to: "src/stores/taskStore.ts + scheduleStore.ts"
      via: "onEvent tool_end(task_*/schedule_*) → refreshFromSql"
      pattern: "refreshFromSql"
    - from: "src/stores/taskStore.ts"
      to: "src/stores/storage/pmRepo.ts"
      via: "actions 分支 isTauri() SQL 写"
      pattern: "pmRepo"
---

<objective>
Store 换轨 + 写后可见(PM-03/PM-04):taskStore/scheduleStore 在 Tauri 下退役 persist、SQL 单真相源读写;initializeDatabase 首帧前 hydration;chat/tab-run 的 task_/schedule_ tool_result 触发即时 refresh;端到端 UAT。

Purpose: 写入立即可见于对应视图、重启不丢(SC-3);双真相源(kv persist vs 引擎直写)拆除。
Output: pmRepo.ts + 两 store 换轨 + refresh 接线 + 人工 UAT。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-pm-crud/29-CONTEXT.md
@.planning/phases/29-pm-crud/29-01-SUMMARY.md
@.planning/phases/29-pm-crud/29-03-SUMMARY.md

<interfaces>
From 29-01 (Rust, tasks/schedules 表列名 — pmRepo SQL 必须逐列对齐):
tasks(id, title, status, priority, deadline, description, project, project_id, assignee, assignee_avatar, time, category_id, scheduled_event_id, created_at, updated_at)
schedules(id, title, time, date, type, location, project_id, task_id, status, created_at, updated_at)
task_categories(id, name, color, sort)
启动时 Rust 已把 kv JSON 幂等搬入这些表(kv 原样保留)。

From src/stores/storage/lazySqlite.ts:
`import { lazySqlite } from './lazySqlite'` → `db.select<T[]>(sql, params)` / `db.execute(sql, params)`(tauri-plugin-sql,与 kv_store 同库)

约束(CONTEXT Area 4 + 计划说明):
- taskStore/scheduleStore 公开 API 形状不变(views 零改动)
- hydration 在首帧前完成(initializeDatabase 由 main.tsx 在 render 前 await)
- 事件驱动 refresh:既有 EngineEvent Channel 的 tool_end/tool_result 已送 webview
- web dev 模式(非 Tauri)维持现状:persist(localStorage)— sqliteStorage 本身已按 isTauri 分支
</context>

<tasks>

<task type="auto">
  <name>Task 1: pmRepo + 两 store 换轨 + hydration</name>
  <files>src/stores/storage/pmRepo.ts, src/stores/taskStore.ts, src/stores/scheduleStore.ts, src/stores/storage/initializeDatabase.ts</files>
  <read_first>
    - src/stores/taskStore.ts + src/stores/scheduleStore.ts (全文 — 每个 action 的语义都要保留:completeTask 的 setEventStatus 联动、deleteTask 的 clearTaskLink、moveTask、setTaskProject 的 project.name 镜像)
    - src/stores/storage/lazySqlite.ts (db.select/execute 签名)
    - src/stores/storage/initializeDatabase.ts (hydration 插入点 + rndStore hydrate 先例)
    - src/stores/storage/sqliteStorage.ts (isTauri 分支惯例)
    - src/data/mockTasks.ts (Task/TaskCategory 类型与 aiSuggestions 字段)
  </read_first>
  <action>
1. **新建 src/stores/storage/pmRepo.ts**:
```ts
// Phase 29 (29-04) — task/schedule 关系表 webview 适配层。
// 引擎(29-02 工具)与本层写同一组表;列名与 migration 0012 逐列对齐。
import { lazySqlite } from './lazySqlite';

type Db = Awaited<ReturnType<typeof lazySqlite>>;

// 行 → Task camelCase(deadline/date/time 全字符串零转换;aiSuggestions 不入库,回读时回 [])
export async function loadTasks(): Promise<{ categories: import('../../data/mockTasks').TaskCategory[]; tasks: import('../../data/mockTasks').Task[] }>
export async function upsertTaskRow(task: Task): Promise<void>   // INSERT INTO tasks(...) VALUES(...) ON CONFLICT(id) DO UPDATE SET 全列, updated_at=now ISO
export async function deleteTaskRow(id: string): Promise<void>   // DELETE + UPDATE schedules SET task_id=NULL WHERE task_id=$1
export async function loadSchedules(): Promise<ScheduleEvent[]>  // ORDER BY date, time
export async function upsertScheduleRow(e: ScheduleEvent): Promise<void>
export async function deleteScheduleRow(id: string): Promise<void>
```
loadTasks 实现:SELECT task_categories ORDER BY sort → SELECT tasks ORDER BY created_at DESC;按 category_id 分组成 TaskCategory.tasks;aiSuggestions: []、projectId/scheduledEventId null→undefined。categories 空时给一个默认组(id 'cat-default', name '待办', color 'bg-blue-500')兜底分组。

2. **taskStore.ts 换轨**(API 不变):
- 把现 base creator 提为 `const taskBase = (set, get) => ({ …现有实现… })`
- 每个 mutation action 内部加 `if (isTauri()) void pmRepo.upsertTaskRow(…)/deleteTaskRow(…)`(fire-and-forget SQL 写 + 本地 set 乐观更新;SQL 错误 console.error 不回滚 — 关系表与内存短期漂移由 refreshFromSql 自愈)
- 模块尾部:
```ts
export const useTaskStore = create<TaskState>()(
  isTauri() ? taskBase : persist(taskBase, { name: 'nova-task', version: 2, /* 现有 config 原样搬 */ })
);
```
  (Tauri 下 persist 退役 — 不再写 kv;web dev 维持 localStorage。isTauri 来自 '@/src/lib/api',模块加载时同步判定。)
- 新增 action `refreshFromSql: async () => { if (!isTauri()) return; const { categories } = await pmRepo.loadTasks(); set({ categories }); }`
- addCategory 补 SQL 写(task_categories INSERT OR IGNORE);_hasHydrated 语义保留(set 由 hydration 调用)

3. **scheduleStore.ts 换轨**:同构 — scheduleBase + persist 条件化(name 'nova-schedule', version 3)+ mutations 加 pmRepo 写 + `refreshFromSql`。INITIAL_EVENTS 仅 web 模式默认生效(Tauri 下 hydration 会覆盖)。

4. **initializeDatabase.ts**(首帧前,在 hydrateKnowledgeFromRepo 之后):
```ts
// Phase 29 (29-04): task/schedule 关系表 hydration(Rust 启动搬移已把 kv 数据落表)。
const { useTaskStore } = await import('../taskStore');
const { useScheduleStore } = await import('../scheduleStore');
await useTaskStore.getState().refreshFromSql();
await useScheduleStore.getState().refreshFromSql();
useTaskStore.getState()._setHydrated();
useScheduleStore.getState()._setHydrated();
```
(竞态说明:Rust kv 搬移在 lib.rs setup 异步块执行,若 hydration 先跑读到空表且 kv 尚未搬完,用户旧数据会在下一次 refresh 自愈;真机 UAT Task 3 验证重启不丢。)

5. **联动语义保持**:completeTask 的 `useScheduleStore.getState().setEventStatus(eventId,…)` 与 deleteTask 的 clearTaskLink 照旧跑内存态 + 各自 SQL 写(setEventStatus/clearTaskLink 内部也加 pmRepo.upsertScheduleRow — 联动即再写一次该行,ON CONFLICT 幂等)。
  </action>
  <verify>
    <automated>npm run lint exit 0</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/storage/pmRepo.ts 存在,grep -n "FROM tasks\|FROM schedules\|FROM task_categories\|ON CONFLICT" 全命中
    - grep -n "refreshFromSql" src/stores/taskStore.ts 与 src/stores/scheduleStore.ts 各 ≥ 1;src/stores/storage/initializeDatabase.ts 命中(hydration)
    - grep -n "isTauri() ? taskBase : persist" src/stores/taskStore.ts 命中(或等价条件 persist 结构);scheduleStore 同
    - grep -n "pmRepo" src/stores/taskStore.ts src/stores/scheduleStore.ts 命中
    - grep -rn "useApp()\|categories\|events" src/views/TaskManagementView.tsx 的调用面无签名变化(公开 API 未动:npm run lint 是门禁)
    - npm run lint exit 0
  </acceptance_criteria>
  <done>Tauri 下两 store SQL 单真相源 + 首帧 hydration;web dev 模式不变;views 零改动</done>
</task>

<task type="auto">
  <name>Task 2: 事件驱动 refresh — agent 写 → 视图即时刷新</name>
  <files>src/stores/chatConsoleStore.ts, src/stores/tabRunStore.ts</files>
  <read_first>
    - src/stores/chatConsoleStore.ts (onEvent tool_end 分支 689-704 行;confirmPmWrite(29-03 加的)成功路径)
    - src/stores/tabRunStore.ts (onEvent 处理器 199 行附近 — tab-run 也可能产生 pm 写,如 ingestion run 内)
  </read_first>
  <action>
1. chatConsoleStore onEvent 的 `tool_end` 分支加(在 memory_write 特判旁):
```ts
if (name.startsWith('task_')) void useTaskStore.getState().refreshFromSql();
else if (name.startsWith('schedule_')) void useScheduleStore.getState().refreshFromSql();
```
(tool_end 在写已落库后触发,时序正确;refresh 拉全表,量级 <千行可接受 — ponytail: 全表 refresh,增量投影当任务量真的大再做。)

2. confirmPmWrite 成功路径(set pendingPmWrite:null 处)追加:
```ts
void useTaskStore.getState().refreshFromSql();
void useScheduleStore.getState().refreshFromSql();
```
(删除落在两表都可能(task/schedule),两个都刷。)

3. tabRunStore onEvent 的 tool_end 处理(找到对应分支,同款加前缀判定 refresh)。

4. import:两文件顶部从对应 store 模块引入(lazy import 若文件有惯例则跟随)。
  </action>
  <verify>
    <automated>npm run lint exit 0 && grep -c "refreshFromSql" src/stores/chatConsoleStore.ts src/stores/tabRunStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "startsWith('task_')\|startsWith('schedule_')" src/stores/chatConsoleStore.ts 与 src/stores/tabRunStore.ts 均命中
    - grep -c "refreshFromSql" src/stores/chatConsoleStore.ts ≥ 3(tool_end + confirmPmWrite 两处);tabRunStore ≥ 1
    - npm run lint exit 0
  </acceptance_criteria>
  <done>任何 run(聊天/tab)的 PM 写与确认删除后,任务页/日历即时反映</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 端到端 UAT — 对话驱动写路径主脚本</name>
  <files>(无代码 — 人工验证)</files>
  <read_first>
    - .planning/phases/29-pm-crud/29-CONTEXT.md (收口 UAT 主脚本原文)
  </read_first>
  <what-built>PM CRUD agent 写路径全链路:9 工具 + 三档风险 + 关系表 + store 换轨 + 事件驱动刷新</what-built>
  <how-to-verify>
前提:npm run tauri:dev,已配置 API key,已选一个产品。
1. **升级迁移**:用旧版本数据(或手动在 kv_store 造 nova-task JSON)启动 → 任务页显示旧任务;重启再进,任务仍在(Rust 搬移 + 关系表生效);sqlite 里 kv 'nova-task' 仍存在(备份未删)。
2. **免确认创建**:聊天输入「帮我建 3 个任务,明天截止,高优先级」→ 无确认卡,助手直接调 task_create ×3 → 不切页,打开任务页:3 条新任务立即可见(高优先级、明天 deadline)。
3. **日程**:「明天上午 10 点安排一个评审会」→ 日历出现该日程。
4. **HITL 删除**:「删掉刚才的任务」→ 聊天出现 pm_write 确认卡(标题「确认删除任务」+ 任务名)→ 点确认 → 任务页该任务消失;点取消路径另测一次(数据不动)。
5. **重启不丢**:全部操作后重启 app → 任务/日程全部仍在(关系表持久)。
6. **UI 手动 CRUD**:任务页手动新增/勾完成/删除一条 → 重启不丢;与 agent 写共存不互相覆盖。
7. **cap-5**:让助手一口气建 7 条任务 → 前 5 条直落,第 6 条起出现确认卡。
8. cargo test --lib 全绿、npm run lint 全绿(执行者先自跑并贴结果)。
  </how-to-verify>
  <resume-signal>全过回复 "approved";有问题逐条描述(标注步骤号)</resume-signal>
  <action>执行者先自跑 cd src-tauri && cargo test --lib 与 npm run lint 并贴结果,然后引导用户按 how-to-verify 8 步操作,记录结果。</action>
  <verify>用户回复 approved,或逐条问题清单</verify>
  <done>UAT 8 步全过(升级迁移/免确认建任务/日程/HITL 删除/重启不丢/UI 手动 CRUD/cap-5/测试全绿)</done>
</task>

</tasks>

<verification>
- npm run lint exit 0;cd src-tauri && cargo test --lib 全绿
- 端到端 UAT 8 步全过
</verification>

<success_criteria>
- SC-3:写入立即可见 + 重启不丢
- SC-4 完成:task/schedule 关系化单真相源,persist 退役(Tauri)
- 29-CONTEXT 收口主脚本全过
</success_criteria>

<output>
After completion, create `.planning/phases/29-pm-crud/29-04-SUMMARY.md`
</output>
