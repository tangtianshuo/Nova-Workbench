---
phase: 30-workflow-templates
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified: [src-tauri/migrations/0013_workflow_templates.sql, src-tauri/src/engine/workflow_store.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs, src-tauri/src/lib.rs, src/ai/registry.ts, src/ai/api.ts, src/stores/workflowStore.ts, src/stores/tabRunStore.ts, src/ai/__tests__/fixtures/workflow-pm-write.json]
autonomous: true
requirements: [SC-2, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "agent 对话中可创建/修改/查询模板真实落库;删除走 pm_write HITL 确认卡"
    - "轻写工具计入 cap-5,模板不豁免 29 三档风险分级(SC-5)"
    - "模板表含 skill_trigger/skill_tools_json 预留字段,单 DSL(SC-6)"
    - "用户层交付物种类表存在,generate_deliverable 校验 = 内置 JSON ∪ 用户表"
  artifacts:
    - path: "src-tauri/migrations/0013_workflow_templates.sql"
      provides: "workflow_templates + deliverable_catalog_user 表"
      contains: "workflow_templates"
    - path: "src-tauri/src/engine/workflow_store.rs"
      provides: "模板 SQL CRUD(pm_store 模式)"
      contains: "pub fn insert_workflow"
    - path: "src/stores/workflowStore.ts"
      provides: "TS 模板状态 + refreshFromSql"
      exports: ["useWorkflowStore"]
  key_links:
    - from: "src-tauri/src/engine/tools.rs"
      to: "workflow_store.rs"
      via: "workflow_ 工具执行器调用 store"
      pattern: "workflow_(create|update|delete|search)"
    - from: "src/stores/tabRunStore.ts"
      to: "workflowStore.refreshFromSql"
      via: "tool_end name 以 workflow_ 前缀触发"
      pattern: "startsWith\\('workflow_'\\)"
---

<objective>
模板数据层 + 引擎 CRUD 工具(SC-2/SC-4 用户层/SC-5/SC-6):migration 0013(workflow_templates + deliverable_catalog_user)、workflow_store.rs、4 个 workflow_ 工具(29 三档风险模式)、TS 侧 workflowStore + registry + 刷新接线、pm_write 复用的双侧 parity fixture。

Planner 裁定(research 遗留问题):
- workflow_delete **复用 pm_write kind**,不加新 confirmation kind(Pitfall #4)
- system prompt 注入模板名短清单(≤30 条,超过截断并留 TODO v0.4 FTS5)
- 内置模板不入 SQLite,呈现层/search 时由 Rust include_str 合并(30-03 建文件;本 plan 先留接口,Task 3 中 include 若文件不存在会编译失败——因此 Task 1 同时创建含 2 个占位模板的 builtin JSON,30-03 扩充内容)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-workflow-templates/30-CONTEXT.md
@.planning/phases/30-workflow-templates/30-RESEARCH.md
@.planning/phases/29-pm-crud/29-02-SUMMARY.md
@.planning/phases/29-pm-crud/29-03-SUMMARY.md

<interfaces>
29 模式直接复制(pm-crud 已验证):
- src-tauri/src/engine/pm_store.rs — insert/update/delete/list + update_row 泛型 helper
- src-tauri/src/engine/tools.rs — ToolKind::Pm 注册、三档风险、pm_write 候选产出
- src-tauri/src/engine/loop_runner.rs:233 — pm_writes_used cap-5
- src/ai/registry.ts — executeTool TS 侧工具(webview 发起路径)
- src/stores/tabRunStore.ts:253 — tool_end 前缀触发 refreshFromSql
- 0012_pm_crud.sql + lib.rs sql_migrations() 注册 + APP_SCHEMA_VERSION 互锁(07e44fd 教训:注册与版本号必须同步)
- parity fixture 双侧 glob:.planning/phases/29-pm-crud/29-03-SUMMARY.md(projection-cases-pm-write.json 前缀约定)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 0013 + workflow_store.rs + builtin 占位 JSON</name>
  <files>src-tauri/migrations/0013_workflow_templates.sql, src-tauri/src/engine/workflow_store.rs, src-tauri/src/lib.rs, src/data/workflow-templates-builtin.json</files>
  <read_first>src-tauri/migrations/0012_pm_crud.sql, src-tauri/src/engine/pm_store.rs, src-tauri/src/lib.rs (sql_migrations 注册 + APP_SCHEMA_VERSION), 30-RESEARCH.md「模板 schema」节</read_first>
  <action>
1. `0013_workflow_templates.sql`:
```sql
CREATE TABLE IF NOT EXISTS workflow_templates (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  steps_json TEXT NOT NULL,
  manifest_version INTEGER NOT NULL DEFAULT 1,
  skill_trigger TEXT,
  skill_tools_json TEXT,
  source TEXT NOT NULL DEFAULT 'user' CHECK (source IN ('user','distilled')),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS deliverable_catalog_user (
  code TEXT PRIMARY KEY,
  phase TEXT NOT NULL, phase_name TEXT NOT NULL, title TEXT NOT NULL,
  category TEXT NOT NULL DEFAULT '', format TEXT NOT NULL DEFAULT 'markdown',
  icon TEXT NOT NULL DEFAULT 'FileText', summary TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL
);
```
(source CHECK 不含 'builtin'——内置不入 SQLite,呈现层合并;skill_trigger/skill_tools_json 为 D-08 预留。)
2. lib.rs:`sql_migrations()` 注册 0013,**同步 bump APP_SCHEMA_VERSION**(照 07e44fd 先例,数值与 migration 内 schema_version 一致)。
3. `workflow_store.rs` 抄 pm_store.rs 结构:`insert_workflow(conn,&Value)->Result<String>`(id 缺省 uuid v4,steps_json 整存 `serde_json::to_string`)、`update_workflow(conn,id,&Value)->Result<bool>`(update_row 泛型,columns = name/description/steps_json/skill_trigger/skill_tools_json)、`delete_workflow(conn,id)->Result<bool>`、`list_workflows(conn)->Result<Vec<Value>>`(steps_json 解析回数组);`list_catalog_user(conn)` / `insert_catalog_user`。
4. `src/data/workflow-templates-builtin.json`:数组,含 2 个占位模板(30-03 扩充为 4-5 个正式内容),条目:
```json
{ "id": "builtin-competitor-analysis", "name": "竞品深度分析", "description": "扫描竞品信息并产出对比报告", "steps": [ { "name": "生成竞品分析报告", "prompt": "对当前产品的核心竞品做对比分析,调用 generate_deliverable(code=DEL-REL-03) 产出报告草稿", "expectedSlotCode": "DEL-REL-03", "toolHint": "generate_deliverable" } ], "source": "builtin" }
```
  </action>
  <verify>
    <automated>cd src-tauri && cargo build && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - 0013 文件存在,含 `workflow_templates` 与 `deliverable_catalog_user` 两张表与 skill_trigger 列
    - lib.rs sql_migrations 含 "0013",APP_SCHEMA_VERSION 已 bump
    - workflow_store.rs 导出 insert_workflow/update_workflow/delete_workflow/list_workflows/list_catalog_user
    - src/data/workflow-templates-builtin.json 存在且为非空数组,首条含 "source": "builtin"
    - cargo test 全绿
  </acceptance_criteria>
  <done>表 + SQL 层 + builtin JSON 就绪,引擎工具可挂</done>
</task>

<task type="auto">
  <name>Task 2: 4 个 workflow_ 引擎工具 + generate_deliverable 用户层校验 + system prompt 模板清单</name>
  <files>src-tauri/src/engine/tools.rs, src-tauri/src/engine/loop_runner.rs</files>
  <read_first>src-tauri/src/engine/tools.rs (registry 全貌、pm_write 候选产出路径、generate_deliverable code 校验 ~1108), src-tauri/src/engine/loop_runner.rs (ROLE_AND_TOOL_RULES:97、build_system_prompt:99、pm_writes_used cap-5:233), src-tauri/src/engine/workflow_store.rs</read_first>
  <action>
1. tools.rs registry 注册 4 工具(ToolKind::Pm 同款风险模式):
   - `workflow_search`(读,免确认):args `{ query?: string }`,返回内置(include_str! workflow-templates-builtin.json,LazyLock 同 CATALOG 模式)∪ SQLite 用户层,含 name/description/steps 摘要/step 数/source。
   - `workflow_create`(轻写,免确认+计 cap-5):args `{ name, description?, steps: [{name,prompt,expectedSlotCode?,toolHint?}] }` → workflow_store::insert_workflow(source='user')。steps schema 宽松(serde Value 透传),prompt 层负责语义。
   - `workflow_update`(轻写,cap-5):args `{ id, name?, description?, steps? }` → update_workflow(steps 整量替换)。
   - `workflow_delete`(delete 档):**产出 pm_write 候选**,params_json `{ entity: "workflow_template", id, name }`,复用 29 的 engine_consume_pm_write 确认路径(确认后调 delete_workflow)——若 29 的 consume 分发表按 entity 路由,加 workflow_template 分支;照 29-03 事务收口模式。
2. cap-5:workflow_create/workflow_update 与 task/schedule 轻写共用 `pm_writes_used` 计数(loop_runner.rs:233 处的工具名判断扩到 workflow_ 前缀轻写)。
3. generate_deliverable code 校验改为:内置 CATALOG ∪ `workflow_store::list_catalog_user(conn)` codes(每次调用查一次,无缓存——量级 18+N,无需)。
4. system prompt 两处:
   - ROLE_AND_TOOL_RULES 追加一句:`workflow_search / workflow_create / workflow_update apply immediately (counted toward the light-write budget); workflow_delete requires user confirmation. Templates are reference playbooks the user runs on demand.`
   - loop_runner run 启动处(build_system_prompt 调用点旁,conn 可得):查 workflow 名清单,≤30 条时在 system prompt 末尾追加 `## 可用工作流模板\n- name1 / name2 / ...`(用户可说「帮我跑 X」;>30 条截断 30 并注释 `// TODO v0.4: FTS5 retrieval (D-08)`)。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test</automated>
  </verify>
  <acceptance_criteria>
    - tools.rs registry 含 workflow_search/workflow_create/workflow_update/workflow_delete 四个 ToolSpec
    - workflow_delete 走 pm_write 候选路径(grep 候选产出代码含 workflow)
    - generate_deliverable 校验路径含 list_catalog_user
    - loop_runner system prompt 注入含「可用工作流模板」
    - cargo test 全绿
  </acceptance_criteria>
  <done>agent 对话可 CRUD 模板,风险分级与 cap-5 生效,模板可发现</done>
</task>

<task type="auto">
  <name>Task 3: TS 侧 registry/workflowStore/刷新接线 + parity fixture</name>
  <files>src/ai/registry.ts, src/ai/api.ts, src/stores/workflowStore.ts, src/stores/tabRunStore.ts, src/ai/__tests__/fixtures/workflow-pm-write.json</files>
  <read_first>src/ai/registry.ts (executeTool 现有 pm 工具注册), src/ai/api.ts (engine invoke 封装 + pm 读命令先例), src/stores/tabRunStore.ts:245-265 (tool_end 刷新分支), src/stores/taskStore.ts (refreshFromSql 模式), 29-03-SUMMARY.md (fixture 命名前缀约定)</read_first>
  <action>
1. `src/ai/api.ts`:加 `engineListWorkflows()`(照 29 pm 读 invoke 先例,命令名与 Rust commands.rs 注册一致——Task 2 若未加读命令,此处同 plan 内补 commands.rs 注册并纳入 files_modified)。
2. `src/stores/workflowStore.ts`:`WorkflowTemplate` 接口(id/name/description/steps{name,prompt,expectedSlotCode?,toolHint?}/source:'builtin'|'user'|'distilled'/updatedAt);state `{ templates: WorkflowTemplate[] }`;action `refreshFromSql()` = builtin JSON import + engineListWorkflows 合并(builtin 只读在前);`createTemplate/renameTemplate/deleteTemplate` 走 executeTool(webview 手动路径,UI 用)。
3. `src/ai/registry.ts`:注册 workflowSearch/Create/Update/Delete 到 executeTool(镜像 29 pm 工具的 TS 侧实现,经 api invoke)。
4. `src/stores/tabRunStore.ts` onEvent tool_end 分支(tabRunStore.ts:253 task_/schedule_ 旁)加:`else if (toolName.startsWith('workflow_')) void useWorkflowStore.getState().refreshFromSql();`
5. 双侧 parity fixture `src/ai/__tests__/fixtures/workflow-pm-write.json`:workflow_delete 的 pm_write 候选→确认→tool_result 事件序列(照 projection-cases-pm-write 现有 fixture 结构复制改 params);Rust 侧同 glob 前缀读取,锁双侧。
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - workflowStore.ts 导出 useWorkflowStore,含 refreshFromSql 与 templates state
    - tabRunStore.ts 含 `startsWith('workflow_')` 分支
    - fixtures/workflow-pm-write.json 存在且双侧测试跑到它(cargo test + npm test 全绿)
    - npm run lint 退出码 0
  </acceptance_criteria>
  <done>TS 投影/刷新/手动路径闭环,parity 锁定</done>
</task>

</tasks>

<verification>
cargo test + npm test + npm run lint 全绿;对话中让 agent 建模板→删模板(确认卡)端到端可跑(留 30-04 UAT 覆盖)。
</verification>

<success_criteria>
SC-2/SC-5/SC-6 达成(引擎+TS 双侧);SC-4 用户层部分达成。
</success_criteria>

<output>
After completion, create `.planning/phases/30-workflow-templates/30-02-SUMMARY.md`
</output>
