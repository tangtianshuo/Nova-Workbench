---
phase: 30-workflow-templates
plan: 03
type: execute
wave: 3
depends_on: [30-02]
files_modified: [src/data/workflow-templates-builtin.json, src/stores/tabRunStore.ts, src/views/WorkflowView.tsx, src/components/layout/Sidebar.tsx, src/App.tsx]
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "用户从侧边栏「工作流」顶层入口浏览模板库(内置参考 + 自建),点「运行工作流」一键发起,无参数表单无逐步确认"
    - "模板 run = 单 run 多步、参考剧本措辞在 userMessage 与卡片元信息双出现、进度可见可取消(TabRunPanel 复用)"
    - "1-run-per-tab:运行中再点运行 → toast「已有工作流在运行」"
    - "内置模板仅可复制;用户模板可复制/改名/删除(删除走本地 Dialog 确认)"
  artifacts:
    - path: "src/views/WorkflowView.tsx"
      provides: "工作流视图(模板库 + 运行区)"
      exports: ["WorkflowView"]
    - path: "src/data/workflow-templates-builtin.json"
      provides: "4-5 个内置参考模板"
      contains: "builtin"
  key_links:
    - from: "src/views/WorkflowView.tsx"
      to: "useTabRunStore.startTabRun"
      via: "运行按钮 → tabId:'workflows', kind:'workflow'"
      pattern: "startTabRun"
    - from: "src/views/WorkflowView.tsx"
      to: "TabRunPanel"
      via: "tabId=\"workflows\" 零改动复用"
      pattern: "TabRunPanel"
---

<objective>
「工作流」顶层视图 + 模板 run(SC-1 / D-04 / D-05):Sidebar 顶层入口、WorkflowView(模板库 Tabs + TabRunPanel 运行区)、TabRunKind 加 'workflow'、内置参考模板正式内容(4-5 个)、运行 = 模板渲染多步 userMessage 一键发起。

硬约束(UI-SPEC 措辞红线):「参考剧本,可微调」必须同时出现在模板卡元信息与 run userMessage;严禁参数表单/逐步确认/刚性进度 UI。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-workflow-templates/30-CONTEXT.md
@.planning/phases/30-workflow-templates/30-UI-SPEC.md
@.planning/phases/30-workflow-templates/30-02-SUMMARY.md

<interfaces>
- src/stores/tabRunStore.ts:19 TabRunKind union(加 'workflow');StartTabRunParams {tabId,kind,productId,userMessage,coreContext,sessionTitle?}
- src/components/rnd/TabRunPanel.tsx — props `{tabId, className}`(零改动复用)
- src/stores/workflowStore.ts(30-02)— useWorkflowStore:templates / refreshFromSql / createTemplate / renameTemplate / deleteTemplate
- src/components/layout/Sidebar.tsx:177 MENU_ITEMS(knowledge 后、settings 前插 workflows)
- src/components/product/FullDeliverablesTab.tsx:110-122 — 多步 prompt 拼装先例
- UI 契约:30-UI-SPEC.md §1-2/§4-5 + Copywriting Contract(逐字照抄)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 内置模板正式内容 + TabRunKind 'workflow' + run 发起</name>
  <files>src/data/workflow-templates-builtin.json, src/stores/tabRunStore.ts, src/views/WorkflowView.tsx</files>
  <read_first>src/data/workflow-templates-builtin.json (30-02 占位), src/stores/tabRunStore.ts:19 (TabRunKind), src/components/product/FullDeliverablesTab.tsx:100-130 (多步 prompt 先例), src/stores/ingestionStore.ts:55-70 (INGESTION_PROMPT), src/views/KnowledgeBaseView.tsx (顶层视图骨架先例)</read_first>
  <action>
1. builtin JSON 扩为 5 个正式模板(discretion:PM 高频,质量优先),每个 2-4 步、步骤引用真实工具(generate_deliverable 的 DEL-* code、knowledge_write、task_create 等):
   - `builtin-prd-draft`「PRD 起草」:知识检索 → generate_deliverable(DEL-REQ-01)
   - `builtin-competitor-analysis`「竞品深度分析」:knowledge_search 竞品 → generate_deliverable(DEL-REL-03 竞品对比报告 code 以 catalog 实际为准)
   - `builtin-weekly-review`「周报汇总」:task/schedule 检索 → knowledge_write 周报
   - `builtin-weekend-scan`「周末工作区扫描」:ingest_scan → 知识归档(knowledge_write)
   - `builtin-release-checklist`「发版前检查」:generate_deliverable(DEL-REL-01/DEL-TST-04 类)→ 汇总
   step 字段:{name, prompt, expectedSlotCode?, toolHint?}。
2. tabRunStore.ts:19 TabRunKind 加 `| 'workflow'`。
3. WorkflowView 内 `startWorkflowRun(t: WorkflowTemplate)`:
```ts
const steps = t.steps.map((s, i) => `${i + 1}. ${s.name}:${s.prompt}`).join('\n');
useTabRunStore.getState().startTabRun({
  tabId: 'workflows',
  kind: 'workflow',
  productId: useUIStore.getState().selectedProductId ?? '',
  userMessage: `执行工作流「${t.name}」。以下为参考剧本,可按当前上下文合理微调(顺序、跳过明显不适用的步骤),但每步产物都走候选确认:\n${steps}\n完成后简要汇总每步结果。`,
  coreContext: '',
  sessionTitle: `工作流 · ${t.name}`,
});
```
(coreContext 组装照 FullDeliverablesTab 现有做法取产品上下文;1-run-per-tab guard 由 startTabRun 既有逻辑承担,重复点击 UI 侧 toast。)
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - workflow-templates-builtin.json 为 5 元素数组,每个含 steps 且 steps 非空
    - tabRunStore.ts TabRunKind 含 'workflow'
    - WorkflowView.tsx 存在,含 startWorkflowRun 与字符串「参考剧本」(userMessage 中)
    - npm run lint 退出码 0
  </acceptance_criteria>
  <done>模板数据与 run 链路就绪</done>
</task>

<task type="auto">
  <name>Task 2: WorkflowView 视图 + Sidebar/App 接线 + 卡片操作</name>
  <files>src/views/WorkflowView.tsx, src/components/layout/Sidebar.tsx, src/App.tsx</files>
  <read_first>30-UI-SPEC.md 全文(布局/文案/颜色契约逐字执行), src/components/layout/Sidebar.tsx (MENU_ITEMS + active 态), src/App.tsx (lazy 先例), src/components/ui/index.ts (Card/Button/Badge/Dialog/Tabs/Toast), src/stores/workflowStore.ts</read_first>
  <action>
1. Sidebar MENU_ITEMS:`knowledge` 后插 `{ id: 'workflows', icon: Flow, label: '工作流', subtitle: '模板库 / 参考剧本 / 一次发起', isNew: true }`(import `Flow` from '@phosphor-icons/react')。
2. App.tsx lazy:`React.lazy(() => import('./views/WorkflowView').then(m => ({ default: m.WorkflowView })))`,activeTab switch 加 'workflows' 分支。
3. WorkflowView 按 UI-SPEC §2:根 `cn('space-y-6', className)`;标题「工作流」副标题「参考剧本,按需微调 — Nova 给参考,你来组合」;运行区(`runsByTab['workflows']` 存在时渲染 `<TabRunPanel tabId="workflows" />`);模板库 Tabs(内置参考/我的模板,source 过滤);Card 网格 `grid gap-4`,卡内:20px duotone 图标 + 名称(Heading)+ source Badge(内置=accent/自建=neutral/沉淀=success)+ 描述两行截断 + 元信息「N 步 · 参考剧本(agent 可按上下文微调顺序与取舍)」+ 按钮组(primary sm「运行工作流」;用户模板加 ghost 复制/改名、danger ghost 删除;内置仅复制)。
4. 操作交互(UI-SPEC §4-5):复制=createTemplate(name + ' 副本')不弹窗 toast「已复制为「<name> 副本」」;改名=单 Input Dialog(title="重命名模板");删除=Dialog 确认(title="删除模板",description="「<name>」将被删除,此操作不可撤销。",danger 主按钮)→ deleteTemplate;重复运行 toast「已有工作流在运行,先等它完成或取消」。空态文案照 UI-SPEC Copywriting Contract。
5. 视觉红线:只用语义 token;accent 仅 UI-SPEC 穷尽清单 5 处;禁刚性措辞/进度强制清单。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx MENU_ITEMS 含 `id: 'workflows'` 与 `Flow`
    - App.tsx 含 `import('./views/WorkflowView')`
    - WorkflowView.tsx 含 TabRunPanel、Badge、「运行工作流」、「参考剧本」字样
    - WorkflowView.tsx 无 `bg-white`/`text-gray-`/hex 颜色类
    - npm run lint 退出码 0
  </acceptance_criteria>
  <done>工作流视图端到端可浏览/发起/管理(真机 UAT 在 30-04)</done>
</task>

</tasks>

<verification>
npm run lint + npm test 全绿;grep 措辞红线通过。
</verification>

<success_criteria>
SC-1 UI 侧达成(真机验证在 30-04 UAT)。
</success_criteria>

<output>
After completion, create `.planning/phases/30-workflow-templates/30-03-SUMMARY.md`
</output>
