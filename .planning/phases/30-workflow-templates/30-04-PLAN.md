---
phase: 30-workflow-templates
plan: 04
type: execute
wave: 4
depends_on: [30-03]
files_modified: [src/ai/distill.ts, src/components/workflow/DistillDialog.tsx, src/components/rnd/TabRunPanel.tsx, src/views/WorkflowView.tsx, .planning/phases/30-workflow-templates/30-HUMAN-UAT.md]
autonomous: false
requirements: [SC-3, SC-1, SC-2, SC-5]
must_haves:
  truths:
    - "用户对跑完的 run 说「把这次沉淀成模板」:TS 确定性提取白名单 tool_call → 可编辑草稿 Dialog → 确认落库(source='distilled'),不烧 LLM、不进 candidates 表"
    - "无可提取产物步骤时入口 disabled + Tooltip「这次运行没有可沉淀的产物步骤」"
    - "phase UAT 全过:视图发起 run/对话建删模板/沉淀/风险分级/审计"
  artifacts:
    - path: "src/ai/distill.ts"
      provides: "事件日志 → 步骤草稿确定性提取"
      exports: ["distillSessionToSteps"]
    - path: "src/components/workflow/DistillDialog.tsx"
      provides: "沉淀确认卡(纯 Dialog)"
  key_links:
    - from: "src/ai/distill.ts"
      to: "resolveSessionEvents"
      via: "读 agent_events 过滤 tool_call 白名单"
      pattern: "resolveSessionEvents"
    - from: "DistillDialog"
      to: "executeTool('workflowCreate')"
      via: "确认保存 → 落库 + refreshFromSql"
      pattern: "workflowCreate"
---

<objective>
沉淀(SC-3 / D-07)+ phase 收口 UAT。沉淀链路 planner 裁定(遵循 UI-SPEC):**确定性链路 + 纯 Dialog,不进 candidates 表**——TS 侧从事件日志提取,人工编辑后直接落库,不为确定性数据变换烧 LLM、不加 confirmation kind。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-workflow-templates/30-CONTEXT.md
@.planning/phases/30-workflow-templates/30-UI-SPEC.md
@.planning/phases/30-workflow-templates/30-03-SUMMARY.md

<interfaces>
- src/ai/fork.ts — resolveSessionEvents(sessionId) 拿 agent_events(tabRunStore rehydrate 已用)
- 事件 payload:tool_call 事件 payload 含 name/args/toolCallId(event_log.rs:281 校验依赖;执行时以实际 payload 结构为准核对)
- src/ai/registry.ts(30-02)— executeTool('workflowCreate', {name, description, steps, source:'distilled'})
- src/stores/workflowStore.ts — templates/refreshFromSql
- UI-SPEC §3 沉淀确认卡契约(逐字执行)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: distill 提取器 + 沉淀 Dialog + 入口接线</name>
  <files>src/ai/distill.ts, src/components/workflow/DistillDialog.tsx, src/components/rnd/TabRunPanel.tsx, src/views/WorkflowView.tsx</files>
  <read_first>src/ai/fork.ts (resolveSessionEvents + 事件类型), src/stores/tabRunStore.ts (TabRunRecord.done 态 + sessionId), src/components/rnd/TabRunPanel.tsx (done 态渲染区), src/ai/registry.ts (workflowCreate 参数), 30-RESEARCH.md「沉淀提取」节 + Code Examples, 30-UI-SPEC.md §3</read_first>
  <action>
1. `src/ai/distill.ts`:
```ts
export const DISTILL_KEEP = new Set(['generate_deliverable','knowledge_write','task_create','task_update','task_complete','schedule_create','schedule_update','ingest_scan','ingest_submit']);
export interface DistilledStep { name: string; prompt: string; toolHint?: string; expectedSlotCode?: string; }
export async function distillSessionToSteps(sessionId: string): Promise<DistilledStep[]>
```
实现:resolveSessionEvents → filter `eventType==='tool_call' && DISTILL_KEEP.has(payload.name)` → map { name: args.title ?? args.name ?? payload.name, prompt: JSON.stringify(args), toolHint: payload.name, expectedSlotCode: args.code }。**失败调用剔除**:若后续同 toolCallId 的 tool_result `ok===false`(或 status 字段,以实际事件结构为准)则丢弃该步。空数组原样返回。
2. `src/components/workflow/DistillDialog.tsx` 照 UI-SPEC §3:Dialog max-w-md,DialogHeader title="沉淀为模板" description="从这次运行的产物步骤提取,可编辑后保存";Input 模板名(默认「<原 sessionTitle> 模板」);Textarea 描述;步骤列表(每步 bg-bg-secondary rounded-md p-2:序号 + name Input + toolHint Badge + 删除该步 ghost);Footer 取消(secondary)/保存模板(primary)。保存 → executeTool('workflowCreate', {name, description, steps, source:'distilled'}) → toast「模板已保存,可在工作流视图找到」+ refreshFromSql + 关闭。
3. 入口:TabRunPanel done 态区域加 ghost 按钮「把这次沉淀成模板」(BookmarkSimple 14px duotone)打开 DistillDialog;distillSessionToSteps 空结果时 disabled + Tooltip「这次运行没有可沉淀的产物步骤」。WorkflowView 运行区同款入口(TabRunPanel 若为共享组件则以 slot/条件渲染最小改动接入,避免复制)。
4. 沉淀写库走确定性 executeTool,不发起 LLM run,不进 candidates 表。
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - distill.ts 导出 DISTILL_KEEP 与 distillSessionToSteps,DISTILL_KEEP 含 generate_deliverable/knowledge_write/task_create,不含 knowledge_search/fs_list/exec
    - DistillDialog.tsx 存在,含「沉淀为模板」「保存模板」,无 candidates/confirmationToken 引用
    - TabRunPanel.tsx 含「把这次沉淀成模板」按钮
    - npm run lint + npm test 全绿
  </acceptance_criteria>
  <done>沉淀链路闭环:提取→草稿→编辑→落库</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 30 端到端 UAT</name>
  <files>.planning/phases/30-workflow-templates/30-HUMAN-UAT.md</files>
  <read_first>.planning/phases/29-pm-crud/29-04-SUMMARY.md (UAT 文档先例), 30-UI-SPEC.md Copywriting Contract</read_first>
  <action>
先写 `.planning/phases/30-workflow-templates/30-HUMAN-UAT.md` 清单,再请用户 `npm run tauri:dev` 真机执行:
1. 侧边栏出现「工作流」顶层入口;模板库显示 5 个内置参考模板(accent Badge)
2. 点「竞品深度分析」→「运行工作流」:单 run 启动,TabRunPanel 进度/事件流可见,agent 按参考剧本执行且可中途取消
3. 每步产物走候选确认卡,确认后落 knowledge_docs 版本化槽,产研 tab 可见
4. 对话说「建个周五收尾工作流:汇总本周任务并写知识库」→ agent workflow_create 落库,工作流视图「我的模板」出现
5. 对话说「删掉它」→ pm_write 确认卡 → 确认后消失;UI 手动删除内置模板不可用(仅复制),用户模板删除走 Dialog 确认
6. 跑完的 run 点「把这次沉淀成模板」→ 草稿卡可编辑步骤 → 保存 → 「我的模板」出现(Badge success)
7. 运行中重复点运行 → toast「已有工作流在运行,先等它完成或取消」
8. 哲学红线:全程无参数表单、无逐步确认发起、无刚性进度 UI;模板卡与 run prompt 均含「参考剧本/可微调」语义
  </action>
  <verify>
    <automated>grep -c "^- \[ \]" .planning/phases/30-workflow-templates/30-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 30-HUMAN-UAT.md 存在,8 项检查各一行
    - 用户回报全过(或列出 gap 待修)
  </acceptance_criteria>
  <resume-signal>Type "approved" 或描述失败项</resume-signal>
  <done>30-HUMAN-UAT.md 存在且用户确认 8 项检查通过(或 gap 已修复复验)</done>
</task>

</tasks>

<verification>
UAT 8/8 通过 = SC-1..SC-6 全部可观察成立;gap 则修复后复验。
</verification>

<success_criteria>
SC-3 达成;phase 30 全部 Success Criteria 经真机 UAT 验证。
</success_criteria>

<output>
After completion, create `.planning/phases/30-workflow-templates/30-04-SUMMARY.md`
</output>
