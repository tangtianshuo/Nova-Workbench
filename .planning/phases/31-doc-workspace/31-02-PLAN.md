---
phase: 31-doc-workspace
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/migrations/0014_doc_kind_note.sql, src/ai/knowledgeRepo.ts, src/stores/docWorkspaceStore.ts]
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "可写入无产品归属的全局笔记(doc_kind=note,product_id='__global__'),FTS5 可检索命中"
    - "既有 document 行为零变化(default doc_kind='document')"
  artifacts:
    - path: "src-tauri/src/migrations/0014_doc_kind_note.sql"
      provides: "doc_kind 列"
      contains: "doc_kind"
    - path: "src/stores/docWorkspaceStore.ts"
      provides: "工作区状态:文档列表/当前文档/笔记 CRUD/保存链"
      exports: ["useDocWorkspaceStore"]
  key_links:
    - from: "src/stores/docWorkspaceStore.ts"
      to: "src/ai/knowledgeRepo.ts"
      via: "upsert/listDocs 读写"
      pattern: "knowledgeRepo\\.(upsert|list)"
---

<objective>
数据层:knowledge_docs 扩展 doc_kind=note(D-06/SC-1 数据部分/SC-2)。

migration 0014 加 doc_kind 列(哨兵 `__global__` 方案,不重建表);knowledgeRepo 加 docKind 入参与 listDocs/listNotes 过滤;新建 docWorkspaceStore(工作区文档列表/当前文档/笔记创建/保存动作),为 31-04 面板接线备好数据与状态层。

Output: migration 0014 + repo 扩展 + docWorkspaceStore
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-doc-workspace/31-RESEARCH.md
@src/ai/knowledgeRepo.ts
@src/stores/rndStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 0014 + Rust 侧确认</name>
  <files>src-tauri/src/migrations/0014_doc_kind_note.sql</files>
  <read_first>
  - src-tauri/src/migrations/ (0004_memories_knowledge_fts.sql 的 knowledge_docs 定义与 FTS join;0012/0013 的 schema_version 处理方式)
  - src-tauri/src/engine/db.rs 或 migrate 入口(migration 如何被发现/执行、版本如何记账)
  </read_first>
  <action>
  1. 新建 `0014_doc_kind_note.sql`:
     ```sql
     -- 0014: doc workspace — doc_kind for global notes (Phase 31, D-06)
     ALTER TABLE knowledge_docs ADD COLUMN doc_kind TEXT NOT NULL DEFAULT 'document';
     -- 全局归属哨兵方案(不重建表,FTS/版本链零改动;语义债:product_id='__global__' 表示无归属)
     ```
     schema_version 记账方式**复刻 0012/0013 的既有写法**(若既有 migration 文件内含 `UPDATE meta`,照抄 +1 到 14;若由 initializeDatabase 代码驱动,则不改 SQL、确认注册点覆盖新文件)。
  2. grep `src-tauri` 中 `SELECT` knowledge_docs 的读取点:确认 Rust struct 反序列化(serde 默认忽略未知字段,若任一处 `deny_unknown_fields` 则为该 struct 补 `doc_kind: Option<String>` 字段,默认 `#[serde(default)]`)。引擎写路径不写 doc_kind(保持 'document' 默认),零改动。
  </action>
  <verify>
    <automated>grep -rn "deny_unknown_fields" src-tauri/src/ | head -20; grep -n "0014" src-tauri/src/migrations.rs src-tauri/src/engine/db.rs 2>/dev/null || ls src-tauri/src/migrations/ | tail -3</automated>
  </verify>
  <acceptance_criteria>
  - `src-tauri/src/migrations/0014_doc_kind_note.sql` 存在且包含 `ADD COLUMN doc_kind TEXT NOT NULL DEFAULT 'document'`
  - migration 文件中**不出现** `DROP NOT NULL`(research Pitfall 4 warning sign)
  - `npm run tauri:dev` 或既有 db init 启动后 `PRAGMA table_info(knowledge_docs)` 含 doc_kind(在 SUMMARY 记录验证方式);若 Rust 有 deny_unknown_fields 命中 knowledge_docs 读取 struct,已补字段
  </acceptance_criteria>
  <done>doc_kind 列落地,旧行默认 'document',Rust 读路径不受新列影响</done>
</task>

<task type="auto">
  <name>Task 2: knowledgeRepo docKind 扩展 + docWorkspaceStore</name>
  <files>src/ai/knowledgeRepo.ts, src/stores/docWorkspaceStore.ts</files>
  <read_first>
  - src/ai/knowledgeRepo.ts (KnowledgeDocInput / upsert 版本链 / list / search 的现签名与 FTS insert 路径)
  - src/stores/rndStore.ts (hydrateKnowledgeFromRepo 投影 — **不改它**,Pitfall 5:note 不进产品分桶)
  - src/stores/uiStore.ts (persist partialize 先例,31-03 会加面板字段)
  </read_first>
  <action>
  1. `knowledgeRepo.ts`:`KnowledgeDocInput` 加 `docKind?: 'document' | 'note'`(缺省 'document');upsert INSERT 列表加 doc_kind;新增 `listDocs(filter?: { docKind?: 'document' | 'note'; productId?: string; limit?: number })` 按更新时间倒序返回(复用既有 SELECT 列 + doc_kind);note 的 FTS insert 走既有 `INSERT INTO knowledge_fts` 路径零改动(research Pitfall 5:FTS 行级 insert 自动覆盖新 kind)。
  2. 新建 `src/stores/docWorkspaceStore.ts`(Zustand,不 persist —— 面板开合/宽度归 uiStore,31-03):
     - state: `docs: KnowledgeDoc[]`(混合列表)、`currentDocId: string | null`、`saveStatus: 'idle' | 'editing' | 'saving' | 'saved' | 'error'`、`lastError: string | null`
     - actions: `loadDocs()`(listDocs 全量)、`openDoc(docId)`、`createNote(title)`(productId 写哨兵 `'__global__'`,docKind:'note',repo.upsert 后 openDoc)、`saveDoc(docId, content)`(repo.upsert 保持 docId/productId/docKind,title 不变,成功后刷新投影 `useRndStore.getState().hydrateKnowledgeFromRepo?.()` 若该方法存在,失败置 saveStatus='error' + lastError)
     - 常量 `export const GLOBAL_OWNER = '__global__'`
     - web dev fallback:若 knowledgeRepo 有 memory/web 分支,按既有模式补齐;没有则沿用 Tauri-only 路径
  </action>
  <verify>
    <automated>npm run lint && grep -c "docKind" src/ai/knowledgeRepo.ts src/stores/docWorkspaceStore.ts</automated>
  </verify>
  <acceptance_criteria>
  - knowledgeRepo.ts 含 `docKind` 入参、`listDocs`、upsert 写入 doc_kind 列
  - docWorkspaceStore.ts 导出 `useDocWorkspaceStore` 与 `GLOBAL_OWNER = '__global__'`,含 loadDocs/openDoc/createNote/saveDoc 与 saveStatus 状态机
  - rndStore.ts **零改动**(Pitfall 5:note 走独立查询)
  - `npm run lint` exit 0
  </acceptance_criteria>
  <done>笔记可落库(版本链复用)、可按 docKind 过滤列出,工作区状态层就绪(31-04 消费)</done>
</task>

</tasks>

<verification>
- npm run lint 绿;migration 0014 前向注册;dev 启动无 SQLite 错误
</verification>

<success_criteria>
doc_kind 列 + 哨兵全局归属 + listDocs 过滤 + docWorkspaceStore 就绪,旧行为零变化(SC-1 数据部分/SC-2 数据部分)
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-02-SUMMARY.md`
</output>
