---
phase: 31-doc-workspace
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/Drawer.tsx, src/components/ai/ChatPanel.tsx, src/App.tsx, src/stores/uiStore.ts, src/components/workspace/DocWorkspaceShell.tsx]
autonomous: true
requirements: [DOC-04]

must_haves:
  truths:
    - "⌘K AI Drawer 从左侧滑出,非模态:开着时主内容与右侧面板完全可交互,外点不关、Esc 可关"
    - "右侧常驻工作区面板可收起/展开/拖宽,宽度 persist 重启不丢"
  artifacts:
    - path: "src/components/ui/Drawer.tsx"
      provides: "side + modal 透传"
      contains: "side"
    - path: "src/components/workspace/DocWorkspaceShell.tsx"
      provides: "右侧 aside 面板壳(拖宽/收起)"
    - path: "src/stores/uiStore.ts"
      provides: "docWorkspaceOpen/docWorkspaceWidth persist"
      contains: "docWorkspaceWidth"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/workspace/DocWorkspaceShell.tsx"
      via: "MainLayout 内容区右缘 aside 挂载"
      pattern: "DocWorkspaceShell"
---

<objective>
布局层(D-04/SC-4):⌘K Drawer 左滑非模态 + 右侧常驻工作区面板壳。

Drawer.tsx 加 `side` prop + Root 透传 `modal`;ChatPanel 用 `<Drawer side="left" modal={false}>`;uiStore 加 docWorkspaceOpen/docWorkspaceWidth(persist);新建 DocWorkspaceShell(flex aside 非 Drawer,拖宽手柄 ~30 行,收起/展开),挂到 MainLayout。本 plan 只做壳,业务内容(列表/编辑器/确认卡)由 31-04 填充。

Output: 左右并存布局成立
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-doc-workspace/31-RESEARCH.md
@.planning/phases/31-doc-workspace/31-UI-SPEC.md
@src/components/ui/Drawer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drawer 左滑非模态化</name>
  <files>src/components/ui/Drawer.tsx, src/components/ai/ChatPanel.tsx</files>
  <read_first>
  - src/components/ui/Drawer.tsx (全读:Radix Dialog 结构、定位类、motion 动画)
  - src/components/ai/ChatPanel.tsx (Drawer 消费方式;若路径不同以 grep `<Drawer` 找到 ⌘K 消费点)
  </read_first>
  <action>
  1. `Drawer.tsx`:`DrawerProps` 加 `side?: 'left' | 'right'`(默认 'right')与 `modal?: boolean`(默认 true,透传 Radix `Dialog.Root` 的 `modal`);Content 定位按 side 切换:`side === 'left'` 时 `fixed left-0 top-0 bottom-0` + motion x-slide 方向反转(-100% → 0);`modal={false}` 时不渲染 Overlay(Radix 非 modal 本身无 overlay,确认现有 Overlay 条件渲染)。Esc 关闭验证 Radix 非 modal 默认行为(应仍关;若不关,在 Content 加 `onEscapeKeyDown={(e) => e.preventDefault()}` 的反面 — 即直接允许默认或显式调用 onOpenChange(false))。
  2. ChatPanel(⌘K AI Drawer 消费点):改为 `<Drawer side="left" modal={false}>`。⌘K 快捷键逻辑零改动。
  3. 自测要点(research Pitfall 1):打开 ⌘K 后,主内容区与右侧区域可点击/可输入、Drawer 不因外点关闭。
  </action>
  <verify>
    <automated>npm run lint && grep -n "side" src/components/ui/Drawer.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
  - Drawer.tsx 包含 `side?: 'left' | 'right'` 与 `modal` 透传
  - ChatPanel(或实际 ⌘K 消费文件)包含 `side="left"` 与 `modal={false}`
  - `npm run lint` exit 0
  </acceptance_criteria>
  <done>⌘K 左滑非模态,与右侧同屏并存互不遮挡</done>
</task>

<task type="auto">
  <name>Task 2: 右侧常驻面板壳 + uiStore 状态</name>
  <files>src/stores/uiStore.ts, src/components/workspace/DocWorkspaceShell.tsx, src/App.tsx</files>
  <read_first>
  - src/stores/uiStore.ts (persist partialize 现状)
  - src/App.tsx (MainLayout 结构,内容区 flex 布局,aside 挂载点)
  - .planning/phases/31-doc-workspace/31-UI-SPEC.md §1(面板结构契约)
  </read_first>
  <action>
  1. `uiStore.ts` 加 `docWorkspaceOpen: boolean`(默认 true)与 `docWorkspaceWidth: number`(默认 420),加 `toggleDocWorkspace()`、`setDocWorkspaceWidth(n)`;两个 key 加入 persist partialize(重启不丢)。
  2. 新建 `src/components/workspace/DocWorkspaceShell.tsx`:
     - 根:`<aside className="bg-bg-primary border-l border-border-subtle shrink-0 relative flex flex-col" style={{ width: docWorkspaceWidth }}>`,**普通 flex 元素而非 Drawer**(D-04,非模态并存)
     - 左缘拖宽手柄:`<div onMouseDown={onResize} className="w-1 cursor-col-resize hover:bg-accent shrink-0 absolute left-0 top-0 bottom-0" />`,onResize = research 代码示例的 mousedown/mousemove/mouseup handler,clamp `Math.min(Math.max(w, 360), window.innerWidth * 0.6)`,调 setDocWorkspaceWidth
     - 顶部工具条(32px):标题「文档工作区」(text-md 600)+ 折叠按钮(CaretRight 14,Tooltip「收起文档工作区(右侧按钮展开)」)调 toggleDocWorkspace
     - 主体:预留 `<div className="flex-1 overflow-hidden">{children}</div>`(31-04 填充)
     - 收起态:由 MainLayout/App 侧控制 —— docWorkspaceOpen=false 时 aside 不渲染,右缘渲染 28px 竖条按钮(CaretLeft 14 + NotePencil duotone 16)调 toggleDocWorkspace
  3. `App.tsx` MainLayout 内容区 flex 容器右缘挂 `<DocWorkspaceShell />`(内容与 aside 为 flex 兄弟)。
  </action>
  <verify>
    <automated>npm run lint && grep -n "docWorkspaceWidth" src/stores/uiStore.ts src/components/workspace/DocWorkspaceShell.tsx | head</automated>
  </verify>
  <acceptance_criteria>
  - uiStore.ts 含 docWorkspaceOpen/docWorkspaceWidth 且出现在 persist partialize 列表
  - DocWorkspaceShell.tsx 含 `cursor-col-resize` 手柄、`aside`、clamp 360/0.6vw 逻辑、收起按钮
  - App.tsx 挂载 DocWorkspaceShell
  - `npm run lint` exit 0;`npm run dev` 下:拖宽流畅、收起/展开正常、刷新页面宽度保持
  </acceptance_criteria>
  <done>右侧常驻面板壳成立(可收/可拖宽/persist),布局左右并存(D-04/SC-4)</done>
</task>

</tasks>

<verification>
- npm run lint 绿
- dev 真机:⌘K 开着时右栏可点可输入、Drawer 不关;面板宽度刷新保持
</verification>

<success_criteria>
⌘K 左滑非模态 + 右侧常驻可收/拖宽面板,同屏并存互不遮挡(SC-4)
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-03-SUMMARY.md`
</output>
