---
phase: 31-doc-workspace
plan: 04
type: execute
wave: 2
depends_on: [31-01, 31-02, 31-03]
files_modified: [src/components/workspace/DocWorkspaceContent.tsx, src/components/workspace/DocList.tsx, src/components/workspace/WorkspaceConfirmCard.tsx]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-05]

must_haves:
  truths:
    - "用户在工作区打开 AI 产出/知识库/笔记文档,编辑后自动保存落 knowledge_docs 版本化,重启不丢"
    - "用户可新建全局笔记并在知识库 FTS5 中文检索命中"
    - "待确认 KnowledgeWrite 卡在工作区内直接确认/拒绝,不离开工作区;队列真相源仍是 chatConsoleStore"
  artifacts:
    - path: "src/components/workspace/DocList.tsx"
      provides: "三过滤文档列表 + 新建笔记"
      contains: "新建笔记"
    - path: "src/components/workspace/WorkspaceConfirmCard.tsx"
      provides: "确认卡第三宿主"
      contains: "确认写入"
  key_links:
    - from: "src/components/workspace/DocWorkspaceContent.tsx"
      to: "src/stores/docWorkspaceStore.ts"
      via: "openDoc/saveDoc 自动保存链"
      pattern: "saveDoc"
    - from: "src/components/workspace/WorkspaceConfirmCard.tsx"
      to: "src/stores/chatConsoleStore.ts"
      via: "confirm/reject actions 直调"
      pattern: "chatConsoleStore|useChatConsoleStore"
---

<objective>
工作区业务内容(D-05 三场景/SC-1/2/3):文档列表、编辑器接线、自动保存、新建笔记、确认卡第三宿主。

填充 31-03 的 DocWorkspaceShell 主体:三过滤文档列表(SegmentedControl)+ Milkdown 编辑器 + 800ms 防抖自动保存 + WorkspaceConfirmCard(订阅 chatConsoleStore pending,动作直调既有 confirm/reject,真相源不动)。

Output: 工作区三场景端到端可用
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-doc-workspace/31-CONTEXT.md
@.planning/phases/31-doc-workspace/31-UI-SPEC.md
@.planning/phases/31-doc-workspace/31-02-SUMMARY.md
@.planning/phases/31-doc-workspace/31-03-SUMMARY.md
@src/ai/pendingCount.ts
@src/components/rnd/DeliverableDocCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 文档列表 + 编辑器接线 + 自动保存</name>
  <files>src/components/workspace/DocList.tsx, src/components/workspace/DocWorkspaceContent.tsx</files>
  <read_first>
  - src/components/workspace/DocWorkspaceShell.tsx (31-03 的 children 挂载点)
  - src/stores/docWorkspaceStore.ts (31-02 的 docs/currentDocId/saveDoc/saveStatus)
  - src/components/rnd/DeliverableDocCard.tsx (双源数据组合先例:槽位 + knowledge_write)
  - src/stores/rndStore.ts (hydrateKnowledgeFromRepo / knowledgeBase 投影,AI 产出枚举来源)
  - .planning/phases/31-doc-workspace/31-UI-SPEC.md §2/§3(列表信息架构与文案契约)
  </read_first>
  <action>
  1. `DocList.tsx`(UI-SPEC §2):
     - 顶部 SegmentedControl 三过滤:`AI 产出`(doc_kind='document')/ `笔记`(doc_kind='note')/ `最近`(按更新时间倒序混合近 20 条)
     - 组内按产品分组(组头 11px text-text-tertiary 产品名 + 数);笔记无产品的归「全局」组(读 productStore 名称,GLOBAL_OWNER 映射「全局」)
     - 列表行:文档名 13px truncate + 更新时间 11px text-text-tertiary + 笔记徽章 `Badge variant="neutral"`(NotePencil 12「笔记」);当前打开行高亮(accent);点击调 openDoc
     - 「新建笔记」:列表区右上 `Button variant="primary" size="sm"`(Plus 14 + 新建笔记)→ docWorkspaceStore.createNote('无标题笔记'),创建即打开并聚焦标题
     - 空态文案逐字用 UI-SPEC Copywriting 表(三种过滤各一条)
     - 非 markdown 文档(doc_kind 之外的源文件类型,若列表数据含):显示但点击置灰提示「预览能力开发中,v1 支持 markdown 文档」
  2. `DocWorkspaceContent.tsx`(填入 Shell 的 children):
     - 顶部条件渲染 WorkspaceConfirmCard(Task 2);文档列表区(可折叠,默认展开,max-h-64 scroll);编辑器区 flex-1
     - 编辑器 = 31-01 的 `MarkdownEditor`(受控:value=currentDoc.content,onChange 进本地 state)
     - 自动保存:onChange 防抖 800ms(UI-SPEC 交互契约 3)+ 失焦即存(window blur 或编辑器 blur);保存调 docWorkspaceStore.saveDoc;保存状态三态指示器在 toolbar 右侧 11px:`编辑中…`→`保存中…`→`已保存`(text-success,2s 后淡出);失败按 UI-SPEC Error 文案 + inline 重试 link 按钮
     - 未打开文档空态:「从上方列表选择文档,或新建一篇笔记开始编辑」
     - mount 时 loadDocs
  </action>
  <verify>
    <automated>npm run lint && grep -c "新建笔记" src/components/workspace/DocList.tsx</automated>
  </verify>
  <acceptance_criteria>
  - DocList.tsx 含 SegmentedControl 三过滤(AI 产出/笔记/最近)、「新建笔记」按钮、三条空态文案(UI-SPEC 逐字)
  - DocWorkspaceContent.tsx 含 MarkdownEditor 接线、800ms 防抖、`编辑中…/保存中…/已保存` 三态
  - 非 markdown 点击提示含「预览能力开发中」
  - `npm run lint` exit 0;dev 真机:新建笔记→输入→800ms 后 saveStatus 到 saved;重启应用笔记仍在(SC-1/SC-2 闭环)
  </acceptance_criteria>
  <done>工作区打开/编辑/自动保存/新建笔记全链路通,版本化落 knowledge_docs</done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceConfirmCard 第三宿主</name>
  <files>src/components/workspace/WorkspaceConfirmCard.tsx</files>
  <read_first>
  - src/stores/chatConsoleStore.ts (pending 字段全集 + confirmKnowledgeWrite/rejectXxx actions 真名)
  - src/ai/pendingCount.ts (selectPendingCount 字段清单先例)
  - src/components/ai/AgentConsole.tsx 或确认卡现渲染处 (各 pending kind 的确认动作调用方式)
  - .planning/phases/31-doc-workspace/31-UI-SPEC.md §5(确认卡契约与文案)
  </read_first>
  <action>
  新建 `WorkspaceConfirmCard.tsx`(D-07/UI-SPEC §5):
  - 订阅 chatConsoleStore 与 pendingCount 同一字段清单:优先取 `pendingConfirmation`/KnowledgeWrite 候选(pendingDeliverables 中的 KnowledgeWriteCandidate);无则不渲染
  - 渲染:`Card variant="elevated"` + 左缘 `border-l-2` warning 色(用语义类,如 `border-l-2 border-warning` — 以现有 token 类名为准,grep warning border 先例);不遮挡编辑器(文档流内插入,编辑器下移)
  - 内容:待确认文档名(13px 600)+ 摘要一行(11px truncate)+ 两按钮:确认写入(`Button variant="primary" size="sm"`)/ 拒绝(`Button variant="secondary" size="sm"`);动作**直调 chatConsoleStore 既有 confirm/reject actions**(与 AgentConsole 同一 action,真相源不动);拒绝无二次确认
  - 其余 pending kind:折叠一行「另有 {N} 项待确认 — 打开 AI 台」(text-warning link-style Button,跳转 AI 台既有入口/tab)
  - 文案逐字用 UI-SPEC Copywriting 表:「确认 AI 写入:{文档名}?」「AI 生成了新的文档内容,确认后写入知识库。」
  </action>
  <verify>
    <automated>npm run lint && grep -c "确认写入" src/components/workspace/WorkspaceConfirmCard.tsx</automated>
  </verify>
  <acceptance_criteria>
  - WorkspaceConfirmCard.tsx 含「确认写入」「拒绝」「另有」折叠行文案,confirm/reject 调用来自 chatConsoleStore 既有 actions(无新建动作/reducer)
  - chatConsoleStore.ts 零改动(真相源不动)
  - `npm run lint` exit 0;dev 真机:触发一次 knowledge_write 候选,工作区出现确认卡,确认后文档进列表且 AgentConsole 队列同步清空
  </acceptance_criteria>
  <done>确认卡工作区多宿主成立,确认/拒绝不离开工作区(SC-3)</done>
</task>

</tasks>

<verification>
- npm run lint 绿
- 三场景端到端:①打开 AI 产出编辑保存 ②新建笔记+知识库 FTS5 中文搜索命中 ③工作区确认卡确认/拒绝
</verification>

<success_criteria>
工作区三场景(D-05)全部可用:编辑保存/笔记/审批,数据落 knowledge_docs,真相源唯一
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-04-SUMMARY.md`
</output>
