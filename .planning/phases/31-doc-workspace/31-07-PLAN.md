---
phase: 31-doc-workspace
plan: 07
type: execute
wave: 3
depends_on: [31-04, 31-06]
files_modified: [src/stores/docWorkspaceStore.ts, src/components/workspace/DocWorkspaceContent.tsx]
autonomous: true
requirements: [DOC-01, DOC-05]

must_haves:
  truths:
    - "知识库首次点击文档,面板弹出且编辑器立即携带文档内容(SC-1.1 修复)"
    - "编辑中文档在保存 round-trip 版本号变更后不被覆写(saveStatus=editing 时跳过采纳)"
    - "工作区可通过 tab 栏同时打开多个文档,点击已开 tab 激活不重复开"
    - "切换或关闭 tab 时,未落盘的 ≤800ms 防抖编辑被立即 flush 保存,零丢失"
    - "禅模式下 tab 栏隐藏;仅 1 个文档打开时 tab 栏隐藏(保持 31-06 瘦身观感)"
  artifacts:
    - path: "src/stores/docWorkspaceStore.ts"
      provides: "多 tab 状态(openDocIds/activeDocId/closeDoc/setActiveDoc)"
      contains: "openDocIds"
    - path: "src/components/workspace/DocWorkspaceContent.tsx"
      provides: "D-14 内容采纳修复 + tab 栏 UI + 切换/关闭 flush"
      contains: "currentDoc?.version"
  key_links:
    - from: "src/components/workspace/DocWorkspaceContent.tsx"
      to: "src/stores/docWorkspaceStore.ts"
      via: "tab 切换/关闭前 flush saveDoc(activeDocId, content)"
      pattern: "closeDoc|setActiveDoc"
    - from: "src/views/KnowledgeBaseView.tsx"
      to: "src/stores/docWorkspaceStore.ts"
      via: "openDoc 追加语义不变,调用方零改动"
      pattern: "openDoc"
---

<objective>
UAT 收口裁定落地(D-14..D-16,2026-09-03):修复 SC-1.1 首开空白 bug、多 tab 文档打开、切换/关闭即保存。

Purpose: SC-1.1 是 UAT 阻断项;多 tab + flush 是用户提出的功能请求,同时修复现有切换丢编辑的潜伏 bug。
Output: 修好的内容采纳 effect + 多 tab store + tab 栏 UI + flush 链路。只改 2 个文件(store + content),tab 栏放 DocWorkspaceContent 内部,Shell 不动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-doc-workspace/31-CONTEXT.md
@.planning/phases/31-doc-workspace/31-HUMAN-UAT.md
@.planning/phases/31-doc-workspace/31-06-SUMMARY.md
@src/stores/docWorkspaceStore.ts
@src/components/workspace/DocWorkspaceContent.tsx
@src/components/workspace/DocWorkspaceShell.tsx
</context>

<decisions>
用户 2026-09-03 UAT 收口裁定(CONTEXT D-14..D-16):
- D-14 SC-1.1 bug 修复:内容采纳 effect deps 加 `currentDoc?.version`,编辑中跳过采纳
- D-15 多 tab:store 增 `openDocIds[] + activeDocId`,面板顶部 tab 栏(标题+关闭),禅模式下隐藏
- D-16 切换即保存:切 tab/关 tab 先 flush 防抖编辑(立即 saveDoc),修复 ≤800ms 丢失
- Planner call(记录):仅 1 个文档打开时隐藏 tab 栏,保持 31-06 面板瘦身观感;tab 栏渲染在 DocWorkspaceContent(非 Shell),使 flush 编排与编辑器内容同处一组件,Shell 零改动
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: D-14 内容采纳修复 + store 多 tab 化</name>
  <files>src/stores/docWorkspaceStore.ts, src/components/workspace/DocWorkspaceContent.tsx</files>
  <read_first>
  - src/components/workspace/DocWorkspaceContent.tsx (34-37 行内容采纳 effect — D-14 bug 现场;46-57 行 doSave/handleChange 防抖链)
  - src/stores/docWorkspaceStore.ts (currentDocId 字段、openDoc/createNote/saveDoc 签名)
  - src/views/KnowledgeBaseView.tsx:187 与 src/components/product/ProductKnowledgeTab.tsx:349 (openDoc 调用方 — 签名不能变)
  </read_first>
  <action>
  1. **D-14 修复(DocWorkspaceContent.tsx 34-37 行)**,将内容采纳 effect 改为:
     ```tsx
     // Adopt store content when doc changes OR when a not-yet-adopted version
     // arrives (first-open race: loadDocs() async, docs=[] at first click — D-14).
     // Skip while editing: a save round-trip bumps version; adopting would clobber.
     useEffect(() => {
       if (saveStatusRef.current === 'editing') return;
       if (timerRef.current) clearTimeout(timerRef.current);
       setContent(currentDoc?.content ?? '');
     }, [currentDocId, currentDoc?.version]); // eslint-disable-line react-hooks/exhaustive-deps
     ```
     `saveStatusRef` 为同步 ref 镜像:`const saveStatusRef = useRef(saveStatus); saveStatusRef.current = saveStatus;`(render 中赋值即可,避免 effect deps 引入 saveStatus 造成已保存→采纳回环)。
  2. **store 多 tab 化(docWorkspaceStore.ts)**:
     - 字段:`currentDocId: string | null` 改为 `openDocIds: string[]` + `activeDocId: string | null`(初始 `[]` / `null`)。`currentDocId` 唯一外部读取者是 DocWorkspaceContent.tsx — 全部改读 `activeDocId`,不留兼容别名。
     - `openDoc(docId)`:追加语义 — `expandPanel(); set((s) => ({ openDocIds: s.openDocIds.includes(docId) ? s.openDocIds : [...s.openDocIds, docId], activeDocId: docId, saveStatus: 'idle', lastError: null }))`。点击已开 tab 激活不重复开。
     - `createNote` 末尾同款:追加 openDocIds + 设 activeDocId。
     - 新增 `setActiveDoc(docId: string)`:仅 `set({ activeDocId: docId, saveStatus: 'idle', lastError: null })`。
     - 新增 `closeDoc(docId: string)`:`set((s) => { const idx = s.openDocIds.indexOf(docId); const next = s.openDocIds.filter((id) => id !== docId); return { openDocIds: next, activeDocId: s.activeDocId === docId ? (next[Math.max(0, idx - 1)] ?? null) : s.activeDocId, saveStatus: 'idle', lastError: null }; })`(关闭后激活左邻,无 tab 则 null)。
  3. DocWorkspaceContent.tsx 内所有 `currentDocId` 改为 `activeDocId`(17/27/47/63/76/84 行等);doSave 签名改为显式参数 `doSave(docId: string, value: string)`(flush 需要向「旧 tab」保存,不能再隐式读 active)。
  </action>
  <verify>
    <automated>npm run lint && grep -q "currentDoc?.version" src/components/workspace/DocWorkspaceContent.tsx && grep -q "openDocIds" src/stores/docWorkspaceStore.ts && grep -q "closeDoc" src/stores/docWorkspaceStore.ts && ! grep -q "currentDocId" src/components/workspace/DocWorkspaceContent.tsx</automated>
  </verify>
  <acceptance_criteria>
  - DocWorkspaceContent.tsx 内容采纳 effect deps 数组含 `currentDocId, currentDoc?.version`(或改名后的 `activeDocId, currentDoc?.version`)
  - DocWorkspaceContent.tsx 含 `saveStatus === 'editing'`(或 saveStatusRef 等价)的采纳跳过守卫
  - docWorkspaceStore.ts 含 `openDocIds: string[]`、`activeDocId`、`closeDoc`、`setActiveDoc`;openDoc 为追加语义(includes 检查)
  - 全 src/ 无残留 `currentDocId` 读取(grep 仅 .planning 命中允许)
  - `npm run lint` exit 0
  </acceptance_criteria>
  <done>首开空白修复 + store 支持 multi-tab;openDoc 调用方(KnowledgeBaseView/ProductKnowledgeTab)零改动仍工作</done>
</task>

<task type="auto">
  <name>Task 2: Tab 栏 UI + 切换/关闭 flush(D-15/D-16)</name>
  <files>src/components/workspace/DocWorkspaceContent.tsx</files>
  <read_first>
  - src/components/workspace/DocWorkspaceContent.tsx (Task 1 改后状态:activeDocId/doSave(docId, value)/timerRef)
  - src/components/workspace/DocWorkspaceShell.tsx (99 行 toolbar 结构 — tab 栏放 content 内、confirm card 之下,不动 Shell)
  - src/stores/uiStore.ts (docZenMode 字段,tab 栏需按其隐藏)
  - src/components/ui/index.ts (Button/Tooltip 可用;tab 本体用原生 button + tokens,对齐 Shell ExpandRail 先例)
  </read_first>
  <action>
  1. **flush 编排(D-16,DocWorkspaceContent 内)**:
     ```tsx
     const flushPendingSave = () => {
       const s = useDocWorkspaceStore.getState();
       if (s.saveStatus === 'editing' && s.activeDocId) {
         if (timerRef.current) clearTimeout(timerRef.current);
         void saveDoc(s.activeDocId, contentRef.current);
       }
     };
     ```
     `contentRef` 同步镜像 content(render 中赋值),保证 flush 拿到最新编辑器内容。切换/关闭 handler 必须**先 flush 再改 activeDocId**(saveDoc 显式接收 docId,异步竞态无影响)。
  2. **Tab 栏(D-15,渲染在 WorkspaceConfirmCard 之下、保存状态行之上)**:
     - 显示条件:`openDocIds.length > 1 && !zen`(zen 取 `useUIStore((s) => s.docZenMode)`;单 tab 隐藏 — planner call,保持 31-06 瘦身观感)
     - 容器:`flex items-center gap-0.5 px-2 h-8 shrink-0 overflow-x-auto border-b border-border-subtle bg-bg-secondary`
     - 每个 tab:原生 button,`max-w-[160px]` 标题 `truncate text-xs`,激活态 `text-text-primary border-b-2 border-accent`,非激活 `text-text-secondary hover:text-text-primary`;onClick → `flushPendingSave(); setActiveDoc(id)`
     - 关闭按钮:button 包 Phosphor `X size={12}`,`aria-label={'关闭 ' + title}`,`text-text-tertiary hover:text-danger`,onClick stopPropagation 后 `flushPendingSave(); closeDoc(id)`
     - 标题取 `docs.find((d) => d.docId === id)?.title ?? id`
  3. 现有 window blur 处理(60-67 行)改用 `contentRef.current` + `doSave(s.activeDocId, ...)` 适配新签名。
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -q "flushPendingSave" src/components/workspace/DocWorkspaceContent.tsx && grep -q "closeDoc" src/components/workspace/DocWorkspaceContent.tsx && grep -q "docZenMode" src/components/workspace/DocWorkspaceContent.tsx</automated>
  </verify>
  <acceptance_criteria>
  - DocWorkspaceContent.tsx 含 `flushPendingSave` 定义,且 setActiveDoc 与 closeDoc 调用前均先调用它(grep 上下文可见 flush → switch/close 顺序)
  - tab 渲染条件含 `openDocIds.length > 1` 与 zen 取反
  - tab 栏类名仅用 tokens(bg-bg-secondary/border-border-subtle/text-text-secondary/text-text-primary/border-accent),无硬编码色值
  - 关闭按钮为 Phosphor `X` + aria-label 含「关闭」
  - window blur handler 使用 contentRef.current 与 activeDocId
  - `npm run lint` exit 0;`npm run build` exit 0
  </acceptance_criteria>
  <done>多 tab 可开/切/关,切换与关闭零编辑丢失,禅模式/单 tab 时 tab 栏隐藏</done>
</task>

</tasks>

<verification>
- `npm run lint` 绿 + `npm run build` 绿
- 真机 UAT 复测锚点(更新 31-HUMAN-UAT.md 结果表时引用):
  - **SC-1.1**(修复主目标):知识库首次点击文档 → 面板弹出且编辑器立即有内容
  - **SC-1.2/1.3**:编辑→自动保存→重启持久(确认 D-14 修复未破坏保存链)
  - **新增 D-15/D-16 项**:开 2+ 文档 → tab 栏出现;切换前改字 → 立即「保存中…」无丢失;关 tab 同理;禅模式 tab 栏隐藏;单文档 tab 栏隐藏
  - **SC-8 回归**:overlay/禅模式/入口移交行为不受影响(Shell 未改动,低风险抽查 SC-8.1/8.3)
</verification>

<success_criteria>
D-14/D-15/D-16 全部成立:首开不空白、编辑中不被覆写、多 tab 可用、切换/关闭 flush 零丢失、禅模式与单 tab 隐藏 tab 栏;SC-1.1 可复测通过
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-07-SUMMARY.md`
</output>
