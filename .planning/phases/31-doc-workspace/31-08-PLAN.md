---
phase: 31-doc-workspace
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: [src/views/KnowledgeBaseView.tsx, src/components/workspace/DocWorkspaceShell.tsx]
autonomous: true
gap_closure: true
requirements: [SC-2.3, SC-7.5]

must_haves:
  truths:
    - "知识库全局搜索:点击搜索结果行,右侧文档面板打开该文档"
    - "窗口缩小时,文档面板宽度不超过 60vw,不遮挡主内容"
    - "重启后陈旧的大 px 宽度值同样被 60vw 上限约束"
  artifacts:
    - path: "src/views/KnowledgeBaseView.tsx"
      provides: "搜索结果行打开文档"
      contains: "openDoc(hit.docId)"
    - path: "src/components/workspace/DocWorkspaceShell.tsx"
      provides: "面板宽度 CSS 上限"
      contains: "maxWidth"
  key_links:
    - from: "src/views/KnowledgeBaseView.tsx"
      to: "docWorkspaceStore.openDoc"
      via: "搜索结果行 onClick"
      pattern: "openDoc\\(hit\\.docId\\)"
    - from: "src/components/workspace/DocWorkspaceShell.tsx"
      to: "aside style"
      via: "inline maxWidth"
      pattern: "maxWidth"
---

<objective>
关闭 Phase 31 UAT 两个非编辑器 gap:SC-2.3 搜索结果不打开面板、SC-7.5 面板宽度不自适应。
Purpose: KB 检索闭环 + 面板交互恢复等比例缩放。
Output: 两处单行级修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-doc-workspace/31-HUMAN-UAT.md
@.planning/phases/31-doc-workspace/31-07-SUMMARY.md

<interfaces>
// src/stores/docWorkspaceStore.ts(:53-62)— openDoc 是唯一 expandPanel 入口,本身无问题:
openDoc(docId: string): void

// src/views/KnowledgeBaseView.tsx 参照物(:379-384 主列表行,已正确):
onClick={() => { setActiveItemId(item.id); openDoc(item.id); }}

// 病灶(:331-341 搜索结果行,缺 openDoc):
onClick={() => void selectSearchHit(hit.docId)}

// DocWorkspaceShell.tsx:88(病灶,绝对 px 直接渲染):
style={zen ? undefined : { width }}
// 拖拽期间 clamp 已存在于 :29-30(window.innerWidth * 0.6),仅拖拽后无约束。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 搜索结果行 onClick 追加 openDoc</name>
  <files>src/views/KnowledgeBaseView.tsx</files>
  <action>
Gap #1(root_cause 见 UAT.md)。:331-341 搜索结果行 onClick 改为调用 openDoc(hit.docId),与 :379-384 主列表行同款。保留既有 selectSearchHit(hit.docId) 调用(左侧预览态切换仍需要),在其后追加 openDoc。openDoc 已在组件作用域可用(主列表行在用,勿重复 import/声明)。
  </action>
  <verify>npm run lint && grep -n "openDoc(hit.docId)" src/views/KnowledgeBaseView.tsx</verify>
  <done>搜索结果行 onClick 同时调 selectSearchHit + openDoc(hit.docId)</done>
</task>

<task type="auto">
  <name>Task 2: aside 加 maxWidth 60vw</name>
  <files>src/components/workspace/DocWorkspaceShell.tsx</files>
  <action>
Gap #5。:88 `style={zen ? undefined : { width }}` 改为 `style={zen ? undefined : { width, maxWidth: '60vw' }}`。纯 CSS 一行,同时覆盖「缩窗时固定宽度」与「重启后 persist 陈旧大值」两个场景。不做 resize 监听(persist 值不回写,视觉被 clamp 即可;UAT 诊断明确的可选增强,跳过)。
  </action>
  <verify>npm run lint && grep -n "maxWidth" src/components/workspace/DocWorkspaceShell.tsx</verify>
  <done>非 zen 模式下 aside 渲染带 maxWidth: '60vw'</done>
</task>

</tasks>

<verification>
npm run lint && npm run build 均通过。
</verification>

<success_criteria>
两个单行修复落库,grep 断言命中;真机复测由用户执行(UAT test 3 / test 8.5)。
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-08-SUMMARY.md`
</output>
