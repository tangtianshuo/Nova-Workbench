---
phase: 31-doc-workspace
plan: 09
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/MarkdownEditorInner.tsx, src/components/ui/MarkdownToolbar.tsx, src/index.css]
autonomous: true
gap_closure: true
requirements: [SC-7.2, SC-7.3, SC-7-roundtrip]

must_haves:
  truths:
    - "对已加样式文本按回车换行,不出现孤立反斜杠,且不落库污染"
    - "点击代码块可见语言选择控件,可切换语言并高亮"
    - "光标在表格内时,toolbar 按钮可插入行/列(前/后各向)"
    - "编辑器视觉全部 Nova tokens,无外部组件默认主题(D-01)"
  artifacts:
    - path: "src/components/ui/MarkdownEditorInner.tsx"
      provides: "归一化移出 value-sync + codeBlockComponent + prism + prism 语言包"
      contains: "codeBlockComponent"
    - path: "src/components/ui/MarkdownToolbar.tsx"
      provides: "4 个行列操作按钮"
      contains: "addRowBefore"
    - path: "src/index.css"
      provides: "code block / prism token 样式(Nova tokens)"
      contains: "milkdown"
  key_links:
    - from: "src/components/ui/MarkdownToolbar.tsx"
      to: "preset-gfm 命令"
      via: "runCmd(callCommand)"
      pattern: "add(Row|Col)(Before|After)"
    - from: "src/components/ui/MarkdownEditorInner.tsx"
      to: "ingest 路径"
      via: "normalizeMarkdown 仅在文档载入时执行一次"
      pattern: "normalizeMarkdown"
---

<objective>
关闭 Phase 31 UAT 三个编辑器 gap:回车斜杠污染(#2)、代码块语言控件+高亮(#3)、表格行列操作(#4)。
Purpose: 编辑器 round-trip 无损 + 代码/表格编辑能力补齐。
Output: Milkdown headless 组装扩展 + toolbar 接线 + token 样式。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-doc-workspace/31-HUMAN-UAT.md
@.planning/debug/repro-enter-slash.mjs

<interfaces>
// MarkdownEditorInner.tsx 现状(病灶):
// - normalizeMarkdown(L44-61)在 value-sync effect(L190-198)每次执行 <br>→\n 替换
// - .use() 链(L160-185):commonmark/gfm/history/listener/clipboard/livePreview,无 codeBlockComponent/prism
// value-sync effect:
useEffect(() => {
  if (loading) return;
  const source = normalizeMarkdown(value);
  if (source !== lastEmitted.current) { get()?.action(replaceAll(source, true)); lastEmitted.current = source; }
}, [value, loading, get]);

// @milkdown/kit 可用导入(headless,无 theme CSS):
import { codeBlockComponent } from '@milkdown/kit/components/code-block';
import { prism, prismConfig } from '@milkdown/kit/plugin/prism';
// preset-gfm 命令(toolbar 直接 callCommand,引擎层已齐):
import { addRowBefore, addRowAfter, addColBefore, addColAfter } from '@milkdown/kit/preset/gfm';
// MarkdownToolbar 现有 runCmd 模式:复用于 4 个新按钮。
// D-01:codeBlockComponent 需 headless 自定义 render(Nova tokens),不引外部主题。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 归一化移出 value-sync,修 Enter 斜杠污染</name>
  <files>src/components/ui/MarkdownEditorInner.tsx, .planning/debug/repro-enter-slash.mjs</files>
  <behavior>
    - Case J:文档中间空段落(Milkdown 序列化为孤立 `<br />` 占位)不得被改写为孤立 `\` 行
    - Case K:Enter 后 doc 无 `\` 文本节点,自动保存的 markdown 无 `\\` 污染
    - ingest 路径:AI 文档的 raw `<br />` 仍归一化为 hardbreak(既有能力保留)
  </behavior>
  <action>
Gap #2(root_cause:Milkdown 空+非最后段落输出字面 `<br />`,normalizeMarkdown 在 value-sync 把它改写为孤立 `\`,重解析成字面反斜杠落库)。修法:normalizeMarkdown 不再对「整行恰为一个 br 占位的空段落」做替换 —— 在 line.replace 前 guard:`if (BR_ONLY_RE.test(line)) return line;`(`BR_ONLY_RE = /^\s*<br\s*\/?>\s*$/i`)。这样 ingest 的行内 `<br>` 仍归一化,而 Milkdown 自产的空段落占位原样 round-trip(解析回空段落,无损)。保持归一化在 value-sync 的位置不变 —— guard 使空段落占位零改写,避开「移路径」的更大重构。运行 `node .planning/debug/repro-enter-slash.mjs` Case J/K 验证(脚本已存在,断言无 `\` 文本节点)。
  </action>
  <verify>node .planning/debug/repro-enter-slash.mjs && npm run lint</verify>
  <done>repro 脚本 Case J/K 通过;Enter 换行不产生孤立反斜杠,不落库</done>
</task>

<task type="auto">
  <name>Task 2: codeBlockComponent(headless 自定义)+ prism 高亮</name>
  <files>src/components/ui/MarkdownEditorInner.tsx, src/index.css</files>
  <action>
Gap #3。`.use()` 链加 `codeBlockComponent.configure({ renderLanguage: ... , renderActions: ... })` 与 `prism`(`prismConfig` 注册常用语言包:ts/js/json/bash/rust/python/markdown/sql 等,@milkdown/kit/plugin/prism 自带依赖,按 kit 文档 import 对应语言模块)。codeBlockComponent 必须 headless 自定义 render:语言下拉用原生 select 或自建 button+list(样式 Nova tokens:`bg-bg-secondary text-text-primary border-border-subtle rounded-[var(--radius-sm)] text-xs`),禁止引入 `@milkdown/theme-nova` 等外部主题 CSS(D-01)。src/index.css 在既有 `.milkdown-editor` 区块追加:`.milkdown-editor pre` / prism token 类(`.token.keyword` 等)颜色全部用 var(--text-*)/var(--accent)/var(--color-*) 语义 token,暗色不需要额外分支。行内代码语言标注:CommonMark 能力边界,UAT 建议可选升级 —— 不做,在 SUMMARY 注明。
  </action>
  <verify>npm run lint && npm run build && grep -c "codeBlockComponent" src/components/ui/MarkdownEditorInner.tsx</verify>
  <done>点击代码块出现语言选择控件,切换语言后 prism 高亮生效;无外部主题 import</done>
</task>

<task type="auto">
  <name>Task 3: toolbar 表格行列 4 按钮</name>
  <files>src/components/ui/MarkdownToolbar.tsx</files>
  <action>
Gap #4(首选方案,UAT 诊断:引擎命令齐全仅缺 UI)。import `addRowBefore, addRowAfter, addColBefore, addColAfter` from '@milkdown/kit/preset/gfm',复用既有 runCmd 模式加 4 个按钮(Phosphor duotone,如 Rows/Columns 或 Table 相关图标,tooltip「上方插行/下方插行/左侧插列/右侧插列」)。命令在光标不在表格内时无效即可 —— 不做 disable 态(需要 sel-context 订阅,成本大于收益;点了没反应可接受)。
  </action>
  <verify>npm run lint && grep -n "addRowBefore" src/components/ui/MarkdownToolbar.tsx</verify>
  <done>光标在表内点按钮插入对应行/列;lint/build 过</done>
</task>

</tasks>

<verification>
npm run lint && npm run build;node .planning/debug/repro-enter-slash.mjs;grep 断言(codeBlockComponent / addRowBefore / 无 theme CSS import)。
</verification>

<success_criteria>
三个 gap 修复落库;repro 脚本通过;编辑器 chunk 仍在预算内(build 输出确认);真机复测由用户执行(UAT test 8)。
</success_criteria>

<output>
After completion, create `.planning/phases/31-doc-workspace/31-09-SUMMARY.md`
</output>
