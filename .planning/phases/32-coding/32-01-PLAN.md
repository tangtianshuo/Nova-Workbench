---
phase: 32-coding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - docs/events/EVENT-SCHEMA-V0.4.md
  - src-tauri/src/engine/migrations/0015_workspace_repo_root.sql
  - src-tauri/src/engine/code_ops.rs
  - src-tauri/src/engine/tools.rs
  - src-tauri/src/engine/mod.rs
  - src-tauri/src/engine/db.rs
  - src-tauri/src/engine/commands.rs
autonomous: true
requirements: [CODE-04]
must_haves:
  truths:
    - "workspace 打开后引擎能持久化读取该工作区的 repo_root(git 根检测)"
    - "repo 外路径经 junction/大小写混写/Nova 数据路径三种形态均被拒绝"
    - "v0.4 新事件种类有清单文档,CI 断言 fixture 覆盖与 event_log 常量互锁"
  artifacts:
    - path: "docs/events/EVENT-SCHEMA-V0.4.md"
      provides: "CP-4 事件 schema 增量清单(code_edit tool_call/tool_result、exec pid、reject_reason、orphan_exec_audit)"
    - path: "src-tauri/src/engine/code_ops.rs"
      provides: "resolve_repo + within_repo 边界校验 + detect_repo_root(fs 向上 walk)"
      contains: "dunce::canonicalize"
    - path: "src-tauri/src/engine/migrations/0015_workspace_repo_root.sql"
      provides: "workspaces.repo_root 列"
  key_links:
    - from: "src-tauri/src/engine/tools.rs"
      to: "src-tauri/src/engine/code_ops.rs"
      via: "ToolCtx.repo_root 字段 + mod 声明"
      pattern: "repo_root"
---

<objective>
CP-4 流程门一次建成(事件 schema 增量文档 + CI 互锁断言)+ repo 作用域锁底座(crate 引入、repo_root 检测与持久化、dunce 边界校验)。

Purpose: 后续所有 plan(exec 收口 / code_ops 工具 / 前端)都依赖 repo_root 来源与安全边界;CP-4 是全 v0.4 首 plan 硬门。
Output: 7 个 crate 依赖、事件清单文档、migration 0015、code_ops.rs 边界层 + 三件套测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-coding/32-CONTEXT.md
@.planning/phases/32-coding/32-RESEARCH.md

<interfaces>
<!-- From src-tauri/src/engine/tools.rs (实查) -->
```rust
pub enum ToolKind { Readonly, KnowledgeWrite, MemoryWrite, Exec, Fs, Deliverable, Ingest, Pm }
pub struct ToolSpec { pub name: &'static str, pub description: &'static str, pub parameters: Value, pub kind: ToolKind, pub idempotency: &'static str }
pub fn registry() -> Vec<ToolSpec>
pub fn execute(conn: &Connection, name: &str, args: &Value, ctx: &ToolCtx<'_>) -> ToolOutcome
pub struct ToolCtx<'a> { /* 现有字段含 workspace_root 等 — read_first 确认 */ }
```
<!-- fs_ops.rs resolve_deep = 逐段 canonicalize 模式,是 resolve_repo 的结构模板 -->
<!-- migrations 目录: src-tauri/src/engine/migrations/0001..0014,forward-only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CP-4 流程门 — 事件 schema 增量文档 + CI 互锁断言 + crate 依赖</name>
  <files>docs/events/EVENT-SCHEMA-V0.4.md, src-tauri/Cargo.toml, src-tauri/src/engine/parity.rs</files>
  <read_first>src-tauri/Cargo.toml, src-tauri/src/engine/parity.rs, src-tauri/src/engine/event_log.rs, src/ai/__tests__/fixtures/projection-cases.json</read_first>
  <action>
1. `src-tauri/Cargo.toml` 加依赖:`similar = "3.2"`, `ignore = "0.4"`, `grep-searcher = "0.1"`, `grep-regex = "0.1"`, `dunce = "1"`, `which = "7"`, `sysinfo = "0.3x 最新"`(以 registry 为准),然后 `cargo check` 通过。
2. 创建 `docs/events/EVENT-SCHEMA-V0.4.md`,列出 v0.4 全部新事件/payload 增量点(一次建成,后续 phase 继承):
   - `code_edit` tool_call / tool_result(新工具事件)
   - `code_read` / `code_grep` / `code_write` tool_call / tool_result
   - exec tool_call payload 增量字段 `pid: number`(spawn 成功后写入,CP-8 主层)
   - confirmation_candidates 增量 kind `code_edit`(params = {operation, path, old_string, new_string, root})
   - confirmation_candidates 增量列 `reject_reason TEXT`(migration 0016,32-03 落地)
   - 新 event_type `orphan_exec_killed`(CP-8 恢复清杀审计)与 `code_edit_auto_rejected`(取消级联,32-03 落地)
   - spawn / pipeline_gate / skill_injected 预留条目(Phase 33/34/35 使用,只列名)
   每条注明:落点表、双侧(TS 投影/Rust 引擎)消费方、parity fixture 文件名。
3. CI 互锁断言:在 `parity.rs`(或其现有测试模块)加一个 `#[test]`,断言 fixture JSON 文件中出现的每个新 event_type/kind 字符串在 Rust 侧有对应常量/matches! 分支(反射式互锁:遍历 `docs/events/EVENT-SCHEMA-V0.4.md` 中标记 `fixture: yes` 的条目名,断言 parity fixture 目录下至少一个 JSON 包含该字符串)。具体实现照 parity.rs 现有 fixture 驱动测试形态;若清单条目尚未有 fixture(32-01 阶段 code_edit 未落地),在文档中标记 `fixture: pending-32-03`,断言只校验非 pending 条目。
  </action>
  <verify>cargo test -p nova-lib parity 2>&1 | tail -5(互锁断言通过); grep -c "dunce\|which\|sysinfo\|similar\|grep-searcher" src-tauri/Cargo.toml >= 5</verify>
  <acceptance_criteria>
- `docs/events/EVENT-SCHEMA-V0.4.md` 存在且包含字符串 `code_edit`、`pid`、`reject_reason`、`orphan_exec_killed`、`spawn`、`pipeline_gate`、`skill_injected`
- Cargo.toml 包含 `dunce`、`which`、`sysinfo`、`similar`、`ignore`、`grep-searcher`、`grep-regex`
- `cargo check` 零 error
- parity.rs 存在读 EVENT-SCHEMA 文档并校验 fixture 覆盖的测试函数
  </acceptance_criteria>
  <done>事件清单文档 + 7 crate + parity 互锁断言落地,cargo test 通过</done>
</task>

<task type="auto">
  <name>Task 2: repo 绑定底座 — repo_root 检测/持久化 + ToolCtx 扩展 + dunce 边界校验三件套</name>
  <files>src-tauri/src/engine/migrations/0015_workspace_repo_root.sql, src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/mod.rs, src-tauri/src/engine/db.rs, src-tauri/src/engine/commands.rs</files>
  <read_first>src-tauri/src/engine/fs_ops.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/db.rs, src-tauri/src/engine/commands.rs, src-tauri/src/engine/migrations/0014_doc_kind_note.sql</read_first>
  <action>
1. migration `0015_workspace_repo_root.sql`:`ALTER TABLE workspaces ADD COLUMN repo_root TEXT;`(表名/结构以 db.rs 实查为准,若 workspace 持久化在别表则改对应表;repo_root 为 NULL = 未绑定)。
2. `code_ops.rs` 新建,先落边界层(工具实现 32-03 再加):
   - `pub fn detect_repo_root(start: &Path) -> Option<PathBuf>` — 纯 fs 向上 walk 找 `.git` 目录(不调 git 二进制),返回首个 git 根的父目录,dunce::canonicalize 后返回。
   - `pub fn resolve_repo(repo_root: &Path, rel: &str) -> Result<PathBuf, ToolOutcome>` — 照 fs_ops::resolve_deep 逐段 canonicalize 模式,全部 canonicalize 换 `dunce::canonicalize`;越界返回 Failed,message = 「路径超出仓库范围,已拒绝:{path}」(UI-SPEC 锁定文案)。
   - `pub fn within_repo(resolved: &Path, repo_root: &Path) -> bool` — 双侧 dunce canonicalize;Windows 上 `to_string_lossy().to_lowercase()` 前缀比较 + 分隔符边界(`a.len()==b.len() || a.as_bytes().get(b.len())==Some(&b'\\')`);非 Windows 字节比较(RESEARCH Code Example 2 原样)。
   - `pub fn is_nova_data_path(p: &Path) -> bool` — 与 app 数据目录(现有 db 路径获取逻辑,db.rs 实查)dunce canonicalize 后前缀比较。
3. `tools.rs`:`ToolCtx` 加 `pub repo_root: Option<PathBuf>`(向后兼容 Option,所有现有构造处补 `None` 或从 workspace 记录读取)。
4. `commands.rs` + db.rs:加 Tauri 命令 `engine_workspace_bind_repo(workspace_id, repo_root: Option<String>)`(设置页可改,CONTEXT 锁定)+ 打开工作区后台检测入口 `detect_and_bind_repo(workspace_id)`(调 detect_repo_root,写入 workspaces.repo_root)。命令注册进 lib.rs invoke_handler(照现有 engine_* 先例)。
5. `#[cfg(test)] mod tests` 三件套(temp dir + 既有测试 conn 形态):
   - junction/symlink 指向 repo 外 → resolve_repo 返回 Failed(Windows 用 `std::os::windows::fs::symlink_dir` 或 junction;非 Windows symlink,`#[cfg]` 对应)
   - repo_root 传 `C:\Repo` 而路径传 `c:\repo\..` 混写大小写 → 判定一致(Windows-only cfg)
   - 目标 = Nova 数据库路径 → 拒绝
  </action>
  <verify>cargo test -p nova-lib code_ops 2>&1 | tail -5(三件套全绿)</verify>
  <acceptance_criteria>
- code_ops.rs 包含 `dunce::canonicalize`、`fn resolve_repo`、`fn within_repo`、`fn detect_repo_root`、`fn is_nova_data_path`
- tools.rs ToolCtx 包含 `repo_root: Option<PathBuf>`
- migrations/0015_workspace_repo_root.sql 存在且含 `repo_root`
- commands.rs 含 `engine_workspace_bind_repo`
- code_ops 测试模块含 junction、大小写(#[cfg(windows)])、nova-data-path 三个测试函数,`cargo test code_ops` 通过
  </acceptance_criteria>
  <done>repo_root 检测/持久化/ToolCtx/边界三件套测试全绿</done>
</task>

</tasks>

<verification>
cargo test -p nova-lib(全量)+ npm run lint。CP-4 文档 + 互锁断言 + repo 边界三件套一次建成。
</verification>

<success_criteria>
- 事件清单文档存在且 CI 互锁测试通过
- repo_root 随 workspace 持久化,可经 Tauri 命令读改
- 边界三件套测试(junction/大小写/Nova 数据路径)通过
</success_criteria>

<output>
After completion, create `.planning/phases/32-coding/32-01-SUMMARY.md`
</output>
