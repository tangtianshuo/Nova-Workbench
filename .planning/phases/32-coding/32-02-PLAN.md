---
phase: 32-coding
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src-tauri/src/engine/exec.rs
  - src-tauri/src/engine/tools.rs
  - src-tauri/src/engine/event_log.rs
  - src-tauri/src/engine/restore.rs
  - src-tauri/src/engine/commands.rs
autonomous: true
requirements: [CODE-03, CODE-05]
must_haves:
  truths:
    - "已知白名单命令直接执行;未知命令首遇产出 exec_approval 候选;git 危险 flag 直接 Failed 不弹卡"
    - "子进程 env 不含任何 *KEY*/*TOKEN*/*SECRET*/*PASSWORD* 变量"
    - "Windows 上 `npm`(npm.cmd)可解析并成功 spawn"
    - "「总是允许」学习命令+首参数二元组,按 workspace_id 存 kv_store"
    - "崩溃后重启,记录过 pid 的存活孤儿 exec 进程被清杀并落审计事件"
  artifacts:
    - path: "src-tauri/src/engine/exec.rs"
      provides: "which 解析 + env scrub + git 黑名单 + 二元组学习 + workspace 级白名单"
      contains: "which::which_in"
    - path: "src-tauri/src/engine/restore.rs"
      provides: "CP-8 孤儿进程恢复清杀(Windows)"
      contains: "sysinfo"
  key_links:
    - from: "src-tauri/src/engine/exec.rs"
      to: "kv_store"
      via: "键 agent.exec.whitelist.{workspace_id}"
      pattern: "agent\\.exec\\.whitelist"
---

<objective>
exec.rs 四绕过面收口(CP-6)+ CP-8 孤儿进程恢复清杀 + exec cwd 对齐 repo_root。

Purpose: CODE-03/CODE-05 的引擎侧全部安全语义;coding run 里 npm/cargo 可用且不可被劫持。
Output: 加固后的 exec.rs(白名单/解析/env/黑名单)+ restore.rs 清杀 + pid 事件落盘。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-coding/32-RESEARCH.md
@.planning/phases/32-coding/32-CONTEXT.md

<interfaces>
From 32-01:
- `ToolCtx.repo_root: Option<PathBuf>`(tools.rs)
- code_ops::within_repo / resolve_repo(repo_root 边界判定,exec PATH 劫持检查复用 within_repo)
From exec.rs(实查要点):
- `spawn_core` 已有 kill_tree(taskkill /T /F)、三态结果、exec_approval kind 先例
- `normalize_command` 现只剥 `.exe`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: exec.rs spawn 收口 — which 解析 + env scrub + git 黑名单 + 二元组学习</name>
  <files>src-tauri/src/engine/exec.rs, src-tauri/src/engine/tools.rs</files>
  <read_first>src-tauri/src/engine/exec.rs, src-tauri/src/engine/tools.rs, src-tauri/src/engine/confirmations.rs, 32-RESEARCH.md Pitfall 1/2/8 + Code Example 1</read_first>
  <action>
1. `normalize_command` 扩展:strip `.exe`/`.cmd`/`.bat` 三种后缀(小写不敏感),白名单匹配 `npm.cmd` 归一为 `npm`。
2. spawn 前解析:`which::which_in(command, Some(sanitized_path), cwd)` — sanitized_path = 系统 PATH 中移除 repo_root 前缀项(防 PATH 劫持);解析出的绝对 path 若 `code_ops::within_repo(resolved, repo_root)` 为 true → Failed(劫持面)。解析失败 → Failed `command not found: {command}`。resolved 为 .cmd/.bat 时直接 `Command::new(resolved)`(std ≥1.77.2 自动 cmd.exe 转发;绝不手拼 `cmd /c`)。
3. env scrub(不 env_clear):复制父 env,删除 name 含 `KEY`/`TOKEN`/`SECRET`/`PASSWORD`(大小写不敏感)的变量;`Command::envs()` 逐项设置。PATH 单独清洗(repo_root 前缀项移除)。
4. git 危险 flag 黑名单:command(归一后)== `git` 且 args 任一元素以 `--output`/`--upload-pack`/`-c`/`--exec`/`--exec-path` 前缀匹配(大小写不敏感)→ 直接 Failed 「已拒绝:git 危险操作({flag})」,不创建候选。const 数组 `GIT_DANGEROUS_FLAGS`。
5. 学习粒度:`add_command_to_whitelist` 改为存二元组 — `WhitelistEntry.subcommands = Some(vec![first_arg])`(字段已存在,schema 零改动);匹配时 command 归一相等 + first_arg 匹配(无首参数的命令 subcommands 保持 None)。
6. 白名单键 workspace 级:`format!("agent.exec.whitelist.{}", workspace_id)`(kv_store JSON 先例);ToolCtx 无 workspace_id 时实查现有白名单键的获取方式并对齐(exec.rs 现状 read_first 确认,保持向后兼容旧键读取可省)。
7. 出厂预置:git status/diff/log + ls/dir(只读,`subcommands: None`)。
8. cwd:candidate params 的 cwd 从 workspace_root 改为 repo_root(有绑定时;无绑定回退 workspace_root),confirmed 重放同源(Pitfall 8)。
9. 测试(`#[cfg(test)]`,既有形态):
   - spawn `cmd /c set`(Windows)/ `env`(unix)断言输出无 `*KEY*`(预先 set 一个 GEMINI_API_KEY 测试变量)
   - `normalize_command("npm.cmd") == "npm"`
   - git 黑名单:`exec git -c ...` → Failed 含「已拒绝」
   - 二元组:`always` 后同 command 不同 first_arg 仍弹候选
   - PATH 劫持:构造含 repo 内假 npm 的 PATH → 解析拒绝或 fallback 系统 PATH
  </action>
  <verify>cargo test -p nova-lib exec 2>&1 | tail -8(新测试全绿)</verify>
  <acceptance_criteria>
- exec.rs 含 `which::which_in`、`GIT_DANGEROUS_FLAGS`、`agent.exec.whitelist`(grep 可见)
- exec.rs 不含手拼 `cmd /c` 字符串拼接(grep `"/c"` 仅允许出现在测试的 env 回显命令中)
- normalize_command 处理 .cmd/.bat
- `cargo test exec` 通过,含 env-scrub、黑名单、二元组三个测试函数
  </acceptance_criteria>
  <done>四绕过面各有测试且通过;npm.cmd 可解析;手拼 cmd /c 为零</done>
</task>

<task type="auto">
  <name>Task 2: CP-8 — exec pid 落事件 + restore 孤儿进程清杀 + 审计事件</name>
  <files>src-tauri/src/engine/exec.rs, src-tauri/src/engine/event_log.rs, src-tauri/src/engine/restore.rs</files>
  <read_first>src-tauri/src/engine/exec.rs, src-tauri/src/engine/restore.rs, src-tauri/src/engine/event_log.rs, src-tauri/src/engine/scheduler.rs, 32-RESEARCH.md Pitfall 5 + Code Example 6</read_first>
  <action>
1. exec.rs:spawn 成功后把 `pid: child.id()` 写进该 tool_call 事件的 payload(照 event_log 现有 payload 写入方式;EVENT-SCHEMA-V0.4.md 已列)。
2. restore.rs:尾切(tail-cut)恢复流程后加一步 `reap_orphaned_execs`(Windows `#[cfg(windows)]`,macOS/Linux 留 TODO debt 注释):
   - 从事件日志收集「consumed 但无对应 tool_result」的 exec tool_call,提取 payload.pid 与 command basename
   - `sysinfo::System::new_all()` 查 PID 存活;存活且 `process.name()` 与记录的 command basename 一致(双保险,防 PID 复用误杀)→ `taskkill /PID {pid} /T /F`
   - 每杀一个/发现一个孤儿,落 event_type `orphan_exec_killed` 审计事件(payload 含 pid、command、action)
3. 测试:单测 reap 逻辑的过滤部分(构造假 pid 列表 + sysinfo 查询,断言不存在的 PID 不产生 kill 且产生审计事件;真进程 kill 用 `cmd /c timeout` 短命进程一条集成测试,Windows-only cfg)。
  </action>
  <verify>cargo test -p nova-lib restore 2>&1 | tail -5</verify>
  <acceptance_criteria>
- exec.rs 事件 payload 含 `pid`(grep `"pid"` 可见)
- restore.rs 含 `sysinfo`、`orphan_exec_killed`、`#[cfg(windows)]`
- event_log.rs 有 orphan_exec_killed event_type 常量/分支
- `cargo test restore` 通过
  </acceptance_criteria>
  <done>pid 落事件;restore 孤儿清杀 + orphan_exec_killed 审计;cargo test restore 通过</done>
</task>

</tasks>

<verification>
cargo test -p nova-lib 全量 + 手动 `npm run tauri:dev` 跑一次含 `npm --version` 的 exec run(狗粮,可在 UAT)。
</verification>

<success_criteria>
- 四绕过面各有测试:env 无 *KEY*、二元组学习、PATH 解析/劫持、git 黑名单
- Windows npm.cmd 可解析执行
- 崩溃恢复清杀 + 审计事件落地
</success_criteria>

<output>
After completion, create `.planning/phases/32-coding/32-02-SUMMARY.md`
</output>
