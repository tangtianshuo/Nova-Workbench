---
phase: 32-coding
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - src-tauri/src/engine/code_ops.rs
  - src-tauri/src/engine/tools.rs
  - src-tauri/src/engine/confirmations.rs
  - src-tauri/src/engine/migrations/0016_confirmation_reject_reason.sql
  - src-tauri/src/engine/commands.rs
  - src-tauri/src/engine/scheduler.rs
  - src/ai/__tests__/fixtures/projection-cases-code-edit.json
  - src/ai/__tests__/phase32CodeEdit.test.ts
autonomous: true
requirements: [CODE-01, CODE-02, ENGINE-01]
must_haves:
  truths:
    - "code_read/code_grep 零确认直接返回(带分页限流);code_write/code_edit 产出 code_edit 候选等待 HITL"
    - "edit old_string 未找到/非唯一/apply 时 stale 三种失败均回带 path+行号的模型可自纠文案"
    - "同文件两次不同 edit = 两个候选 token;同 edit 重试 = 同 token(params_hash)"
    - "confirmed 重放走 Rust tools::execute(engine_code_apply);apply 前重读文件重校验,篡改后 Failed"
    - "reject 附原因落候选行,run 恢复时可读"
  artifacts:
    - path: "src-tauri/src/engine/code_ops.rs"
      provides: "5 工具中 4 个(read/grep/write/edit)+ apply_edit + unified diff 生成"
      contains: "code_edit"
    - path: "src-tauri/src/engine/migrations/0016_confirmation_reject_reason.sql"
      provides: "confirmation_candidates.reject_reason 列"
    - path: "src/ai/__tests__/fixtures/projection-cases-code-edit.json"
      provides: "双侧 parity fixture(CP-4 同 commit)"
  key_links:
    - from: "src-tauri/src/engine/tools.rs registry()"
      to: "src-tauri/src/engine/code_ops.rs"
      via: "code_read/code_grep/code_write/code_edit 四个 ToolSpec"
      pattern: "code_edit"
    - from: "src-tauri/src/engine/commands.rs"
      to: "src-tauri/src/engine/code_ops.rs apply_edit"
      via: "engine_code_apply 命令"
      pattern: "engine_code_apply"
---

<objective>
code_ops 四工具(read/grep/write/edit)Rust 原生落地 + code_edit 确认管线(kind 注册、reject_reason、apply 重校验、confirmed 重放)+ 取消级联 auto-reject + 双侧 parity fixture。

Purpose: CODE-01/CODE-02/ENGINE-01 引擎侧全部语义;这是 agent 写真实代码的核心通道。
Output: code_ops.rs 完整工具层、migration 0016、engine_code_apply、parity fixture 同 commit。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-coding/32-RESEARCH.md
@.planning/phases/32-coding/32-CONTEXT.md

<interfaces>
From 32-01:
- code_ops.rs 已有 resolve_repo/within_repo/is_nova_data_path;ToolCtx.repo_root
- docs/events/EVENT-SCHEMA-V0.4.md(code_edit 事件条目,本 plan 兑现 fixture)
From fs_ops.rs(结构模板,read_first):
- 工具函数签名 `(conn: &Connection, args: &Value, ctx: &ToolCtx) -> ToolOutcome`
- HITL 写路径:create_candidate → Failed/等待;纯同步 `apply_operation` 由 confirmed 重放调用
From confirmations.rs:
- kind 开放字符串;dedup `matches!` 列表加 "code_edit"(一行);params_hash 对 params JSON 计算
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: code_read + code_grep(Readonly 零确认)</name>
  <files>src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/tools.rs</files>
  <read_first>src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/fs_ops.rs, src-tauri/src/engine/tools.rs, 32-RESEARCH.md Architecture Pattern 1 + MP-10</read_first>
  <behavior>
    - Test: code_read 无 offset/limit → 返回文件头部默认行窗(如 2000 行),payload 含 totalLines 与 hasNext
    - Test: code_read offset/limit 分页 → 返回对应行段
    - Test: code_read 目标在 repo 外 → Failed「路径超出仓库范围,已拒绝:{path}」
    - Test: code_read 目标 = Nova 数据路径 → Failed
    - Test: code_grep 命中 → 返回 file:line:text 结构,超过 MAX_GREP_RESULTS 截断并提示收窄
    - Test: code_grep 尊重 .gitignore(ignore::WalkBuilder,临时目录放 .gitignore 验证)
    - Test: ToolCtx.repo_root = None → 四工具统一 Failed「未绑定代码仓库 — 请在设置中指定 repo 目录」
  </behavior>
  <action>
1. `code_read(conn, args, ctx)`:resolve_repo → 读文件;args = {path, offset?, limit?};MP-10 限流:单次默认 2000 行 / 输出超 4KB 走既有 artifact 化先例;二进制文件检测(含 NUL 前 8KB)→ Failed 提示。
2. `code_grep(conn, args, ctx)`:`ignore::WalkBuilder`(repo_root 起,尊重 .gitignore,隐藏目录跳过)+ `grep_regex::RegexMatcher` + `grep_searcher::Searcher` + sinks 收集 `file:line:text`;args = {pattern, path?, max_results?};MAX_GREP_RESULTS = 200 截断附「收窄 pattern 或 path」提示(MP-10)。
3. read/grep 作用域:锁 repo_root(32-RESEARCH Open Question 2 裁定:锁 repo 为主,is_nova_data_path 兜底拒)。
4. tools.rs registry() 注册 `code_read`/`code_grep`(kind: Readonly, idempotency: rerunnable),schema 照 EXEC_DESCRIPTION 先例写 description(ENGINE-01:描述显式「研究类工具,先侦察后行动」)。
  </action>
  <verify>cargo test -p nova-lib code_ops 2>&1 | tail -5</verify>
  <acceptance_criteria>
- code_ops.rs 含 `code_read`、`code_grep`、`WalkBuilder`、`MAX_GREP_RESULTS`
- tools.rs registry 含 `"code_read"` 与 `"code_grep"` ToolSpec
- 「未绑定代码仓库」文案存在
- 行为测试 7 条全部通过
  </acceptance_criteria>
  <done>code_read/code_grep 注册为 Readonly,7 条行为测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: code_write/code_edit 候选 + code_edit kind + reject_reason migration + unified diff</name>
  <files>src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/migrations/0016_confirmation_reject_reason.sql</files>
  <read_first>src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/confirmations.rs, src-tauri/src/engine/fs_ops.rs, src-tauri/src/engine/migrations/0009_confirmation_kind_exec.sql, 32-RESEARCH.md Architecture Pattern 2 + Code Example 3/5</read_first>
  <behavior>
    - Test: code_edit old_string 命中 0 次 → Failed 含 `old_string not found in {path}` 与「re-read the file and retry」
    - Test: old_string 命中 2 次 → Failed 含 `found 2 times` 与全部命中行号 `lines`
    - Test: 唯一命中 → 创建 kind `code_edit` 候选,params JSON 恰为 {operation, path, old_string, new_string, root}(不含全文快照/diff 文本,CP-2)
    - Test: 同文件两次不同 edit → 两条候选不同 token;同 params 重试 → params_hash 相同(CP-2)
    - Test: 候选行带 unified diff 文本(展示字段,生成于候选创建时,similar context_radius(3))
    - Test: code_write → 候选 params = {operation:"write", path, new_content, root}
    - Test: reject(token, Some("原因")) → 候选行 reject_reason 落库
  </behavior>
  <action>
1. migration 0016:`ALTER TABLE confirmation_candidates ADD COLUMN reject_reason TEXT;`。
2. confirmations.rs:dedup `matches!` 加 `"code_edit"`;`reject` 签名加 `reason: Option<&str>`(现有调用处传 None),UPDATE 语句写 reject_reason(RESEARCH Open Question 1 裁定:改签名 + 列)。
3. `code_edit(conn, args, ctx)`:resolve_repo → 读文件 → old_string 字节精确匹配(RESEARCH Pitfall 3:只归一化 path 不归一化 content,MP-11)→ 0/多命中按上述文案 Failed(含行号,Code Example 5)→ 唯一命中 create_candidate("code_edit", params={operation:"edit", path, old_string, new_string, root}),候选展示层附 similar `TextDiff::from_lines` + `.grouped_ops(3)` 生成的 unified diff(存候选 summary/展示字段,按 confirmations.rs 现有展示字段形态实查对齐)。
4. `code_write` 同构,params = {operation:"write", path, new_content, root}(write 天生带全文)。
5. 错误文案三件套照 RESEARCH Pitfall 3 原文(英文,错误文案即模型接口)。
  </action>
  <verify>cargo test -p nova-lib code_ops 2>&1 | tail -5; cargo test -p nova-lib confirmations 2>&1 | tail -3</verify>
  <acceptance_criteria>
- migrations/0016 存在且含 `reject_reason`
- confirmations.rs 的 reject 签名含 `reason: Option<&str>`;dedup matches! 含 `code_edit`
- code_ops.rs 含 `TextDiff::from_lines`、`old_string not found`、`found`…`times` 文案
- CP-2 测试(两 edit 两 token / 同 edit 同 hash)通过
  </acceptance_criteria>
  <done>code_edit 候选管线 + migration 0016 + reject_reason + CP-2 双 token 测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: apply_edit 重校验 + engine_code_apply + 取消 auto-reject + parity fixture</name>
  <files>src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/commands.rs, src-tauri/src/engine/scheduler.rs, src/ai/__tests__/fixtures/projection-cases-code-edit.json, src/ai/__tests__/phase32CodeEdit.test.ts</files>
  <read_first>src-tauri/src/engine/code_ops.rs, src-tauri/src/engine/commands.rs, src-tauri/src/engine/scheduler.rs, src-tauri/src/engine/parity.rs, src/ai/__tests__/fixtures/projection-cases.json, src/ai/__tests__/phase24Confirmations.test.ts, docs/events/EVENT-SCHEMA-V0.4.md</read_first>
  <behavior>
    - Test(CP-3 核心):创建候选后篡改磁盘文件(直接 std::fs 写)→ apply_edit → Failed 含 `file changed since the edit was proposed` 且文件未被改动
    - Test: apply 前重跑 resolve_repo(TOCTOU:候选与 apply 之间路径被换成 junction)→ Failed
    - Test: apply 成功 → 文件内容含 new_string 不含 old_string;返回 ToolOutcome 成功 + 变更摘要
    - Test: run 取消 → 未决 code_edit 候选全部 auto-reject(带 reject_reason = run 取消)且落 `code_edit_auto_rejected` 审计事件
    - TS parity:projection-cases-code-edit.json 双侧 replay 一致(照现有 fixture 驱动测试接入)
  </behavior>
  <action>
1. `apply_edit(conn, params) -> ToolOutcome`(纯同步 core,fs_ops apply_operation 先例):(a) 重跑 resolve_repo;(b) 重读文件重新做 old_string 唯一命中检查;(c) 任一失败 → Failed 按 Pitfall 3 stale 文案;(d) 成功 → 写盘(write 同理直写 new_content)。
2. commands.rs:加 `engine_code_apply` Tauri 命令(照 engine_fs_apply 先例),调 `code_ops::apply_edit`,注册 invoke_handler;TS executeTool 不实现 coding 工具(锁定)。
3. scheduler.rs 取消级联:run 取消路径上,对未决 code_edit 候选批量 `reject(conn, token, Some("run 已取消"))` + 每张落 event_type `code_edit_auto_rejected`(event_log 常量;EVENT-SCHEMA 文档同步去掉 pending 标记)。
4. 双侧 parity fixture `projection-cases-code-edit.json`:覆盖 code_edit tool_call/tool_result(成功 + stale 失败)、reject_reason 候选投影、code_read/code_grep 结果事件;接入 parity.rs fixture 驱动测试与 TS 侧现有 fixture 测试(phase32CodeEdit.test.ts 照 phase24Confirmations.test.ts 形态,ChatSession.fromEvents 投影断言)。TS 测试跑法照 package.json 现有测试命令实查(若无 runner,以 node --test 或既有 scripts 为准,read_first package.json)。
  </action>
  <verify>cargo test -p nova-lib 2>&1 | tail -5; npm test -- phase32CodeEdit(或项目现有等价命令)</verify>
  <acceptance_criteria>
- code_ops.rs 含 `file changed since the edit was proposed` 且 apply 前有重读文件逻辑(grep `fs::read` 于 apply_edit 内)
- commands.rs 含 `engine_code_apply`
- scheduler.rs 含 `code_edit_auto_rejected`
- projection-cases-code-edit.json 存在且双侧测试通过(CP-4:fixture 与实现同 commit)
- CP-3 篡改测试通过(篡改后 apply Failed 且文件未变)
  </acceptance_criteria>
  <done>apply 重校验(CP-3 篡改测试)+ engine_code_apply + 取消 auto-reject + 双侧 parity fixture 同 commit 全绿</done>
</task>

</tasks>

<verification>
cargo test -p nova-lib 全量绿 + TS fixture 测试绿 + npm run lint。EVENT-SCHEMA-V0.4.md 中 code_edit/code_read/code_grep/reject_reason/code_edit_auto_rejected 条目去掉 pending 标记。
</verification>

<success_criteria>
- 5 工具中 4 个落地(exec 已在 32-02),read/grep 零确认、write/edit 过门
- edit 三种失败文案带 path+行号;CP-2/CP-3 测试通过
- confirmed 重放走 Rust;取消级联 auto-reject + 审计
- 双侧 parity fixture 同 commit,CI 互锁断言全绿
</success_criteria>

<output>
After completion, create `.planning/phases/32-coding/32-03-SUMMARY.md`
</output>
