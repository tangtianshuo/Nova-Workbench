---
phase: 32-coding
plan: 04
type: execute
wave: 4
depends_on: ["32-02", "32-03"]
files_modified:
  - package.json
  - src/components/agent/DiffBody.tsx
  - src/components/agent/CodeEditConfirmCard.tsx
  - src/components/AgentConsole.tsx
  - src/stores/chatConsoleStore.ts
  - src/ai/confirmations.ts
autonomous: false
requirements: [CODE-02, CODE-03, CODE-06]
must_haves:
  truths:
    - "code_edit 候选在 AI 台渲染 diff 审批卡(第四宿主),增删行高亮,diff 组件懒加载"
    - "同 turn 多文件变更逐张排队,徽章显示「另有 N 张待审」"
    - "拒绝可附原因,文案说明 run 继续可调整重试"
    - "「沉淀改动摘要」按钮走 knowledge_write 候选流,成功 toast「已存入第二大脑候选,待确认」"
    - "exec 首遇卡三按钮(允许一次/总是允许/拒绝),命令 + 首参数学习粒度高亮可见"
  artifacts:
    - path: "src/components/agent/DiffBody.tsx"
      provides: "react-diff-view 渲染(React.lazy 消费方)"
      contains: "parseDiff"
    - path: "src/components/agent/CodeEditConfirmCard.tsx"
      provides: "diff 审批卡(排队/reject 原因/沉淀按钮)"
    - path: "src/components/AgentConsole.tsx"
      provides: "kind code_edit 分支挂接第四宿主 + exec 三选卡"
  key_links:
    - from: "src/components/agent/CodeEditConfirmCard.tsx"
      to: "src/ai/confirmations.ts"
      via: "code_edit 候选类型解析 + engine_code_apply invoke"
      pattern: "code_edit"
---

<objective>
前端 diff 审批卡(第四宿主)+ exec 首遇三选卡 + 沉淀入口,全部按 32-UI-SPEC 交互契约与设计 token。

Purpose: CODE-02/CODE-06 的用户可见层;agent 改代码的审批体验。
Output: DiffBody(懒加载)、CodeEditConfirmCard、AgentConsole kind 分支、exec 卡扩展、store actions。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-coding/32-UI-SPEC.md
@.planning/phases/32-coding/32-CONTEXT.md

<interfaces>
From 32-03(引擎侧契约):
- 候选 kind = `code_edit`,params = {operation, path, old_string, new_string, root};候选展示层带 unified diff 文本
- Tauri 命令 `engine_code_apply(token)`;reject 走既有确认拒绝通道(reason 参数)
From 前端既有:
- src/ai/confirmations.ts — knowledge_write/deliverable_draft 候选解析先例(row.kind 分支)
- src/stores/chatConsoleStore.ts — pendingConfirmation + confirm/reject actions(唯一真相源)
- src/components/workspace/WorkspaceConfirmCard.tsx — 第三宿主模式(border-l-warning + 排队计数链接)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: react-diff-view 懒加载 DiffBody + code_edit 候选解析</name>
  <files>package.json, src/components/agent/DiffBody.tsx, src/ai/confirmations.ts</files>
  <read_first>package.json, src/ai/confirmations.ts, 32-UI-SPEC.md, 32-RESEARCH.md Pitfall 6 + Code Example 4</read_first>
  <action>
1. `npm install react-diff-view@3.3.3`(不装 diff 包 — parseDiff 自带;token 级高亮需要时再补,RESEARCH 裁定)。
2. `src/components/agent/DiffBody.tsx`(default export,供 React.lazy):
   - `import { Diff, Hunk, parseDiff } from 'react-diff-view'`
   - props `{ unifiedText: string }`;`const [diff] = parseDiff(unifiedText)` 包 try/catch,malformed 时渲染「diff 解析失败」占位(不崩卡)
   - `<Diff viewType="unified" hunks={diff.hunks} diffType={diff.type} className={cn('font-mono text-xs')}>`;行高亮用 token 派生半透明(`bg-success/10`/`bg-danger/10`,经 `@theme` alpha;禁 raw hex)
3. `src/ai/confirmations.ts`:加 `parseCodeEditCandidate(row)` — `row.kind === 'code_edit'` 时返回 `{ token, operation, path, oldString, newString, root, diffText }`(照 knowledge_write 解析先例;diffText 取候选展示字段,字段名以 32-03 实际落库为准)。
  </action>
  <verify>npm run lint; npx vite build 2>&1 | grep -i "diff" 确认 react-diff-view 独立 chunk 且不在主 bundle</verify>
  <acceptance_criteria>
- package.json 含 `react-diff-view`,不含新增 `diff` 依赖
- DiffBody.tsx 含 `parseDiff` 且为 default export;文件内无 UI 组件直接 import 它(仅 React.lazy 消费)
- confirmations.ts 含 `code_edit` 分支与导出的解析函数
- build 产物中 react-diff-view 位于独立 lazy chunk(grep 构建输出)
  </acceptance_criteria>
  <done>react-diff-view 独立 lazy chunk;code_edit 候选解析可用;lint/build 通过</done>
</task>

<task type="auto">
  <name>Task 2: chatConsoleStore 三 actions + CodeEditConfirmCard(排队/拒绝原因/沉淀)</name>
  <files>src/components/agent/CodeEditConfirmCard.tsx, src/stores/chatConsoleStore.ts</files>
  <read_first>src/stores/chatConsoleStore.ts, src/components/workspace/WorkspaceConfirmCard.tsx, src/ai/confirmations.ts, 32-UI-SPEC.md Interaction Contracts 1 + Copywriting Contract</read_first>
  <action>
1. `chatConsoleStore.ts` 加 actions(照既有 confirm/reject 形态):
   - `confirmCodeEdit(token)` → invoke `engine_code_apply`
   - `rejectCodeEdit(token, reason?: string)` → invoke 既有 reject 通道带 reason(命令签名以 32-03 实际为准)
   - `depositCodeEditSummary(candidate)` → 组 {title = 「改动摘要:{path basename}」, content = diff 摘要 + path + old/new 摘录} 走 knowledge_write 候选流(复用第二段确认,零新管线;CODE-06)
2. `CodeEditConfirmCard.tsx`:照 UI-SPEC Interaction 1 —
   - Card variant="elevated" `border-l-2 border-l-warning p-3`;文件路径 mono truncate + title attr + 「代码修改」标签
   - `const DiffBody = React.lazy(() => import('./DiffBody'))`,fallback = 现有 `Skeleton`
   - footer:「应用改动」primary(accent)→ confirmCodeEdit;「拒绝」secondary → 展开 inline Textarea(placeholder 「告诉 agent 怎么改(可选)」)+「拒绝并说明」danger /「直接拒绝」;caption「该文件不会落盘,agent 会根据原因调整重试。」
   - 「沉淀改动摘要」ghost + Phosphor `Bookmarks` duotone 16 → depositCodeEditSummary,成功 `toast({type:'success', title:'已存入第二大脑候选,待确认'})`
   - run 取消 transient 态:候选消失前显示「run 已取消,改动未应用」(数据来源 = auto-reject 后的候选状态)
3. 排队语义(MP-1):props 接收待审 code_edit 候选列表,只渲染队首完整卡;其余以「另有 {N} 张待审」warning 徽章链接(UI-SPEC Copywriting)。
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
  - CodeEditConfirmCard.tsx 含「应用改动」「沉淀改动摘要」「另有」「告诉 agent 怎么改」字符串(UI-SPEC 文案逐字)与 `React.lazy` 消费 DiffBody
  - chatConsoleStore.ts 含 confirmCodeEdit/rejectCodeEdit/depositCodeEditSummary
  - 全部 className 为语义 token(grep 无 `bg-white`/`text-gray-`/hex)
  </acceptance_criteria>
  <done>diff 审批卡组件 + store 三 actions 完成;lint 通过</done>
</task>

<task type="auto">
  <name>Task 3: AgentConsole 第四宿主挂接 + exec 三选卡 + 空态</name>
  <files>src/components/AgentConsole.tsx</files>
  <read_first>src/components/AgentConsole.tsx, src/components/agent/CodeEditConfirmCard.tsx, 32-UI-SPEC.md Interaction Contracts 2 + Copywriting Contract</read_first>
  <action>
1. `AgentConsole.tsx`:确认区 kind 分支挂 `CodeEditConfirmCard`(第四宿主);从 pending 列表过滤 kind=code_edit,队首传完整卡、计数传排队徽章(MP-1)。
2. exec 首遇卡按 UI-SPEC Interaction 2 — 三按钮「允许一次」primary/「总是允许」secondary + tooltip「记住此命令,仅当前工作区生效」/「拒绝」danger;命令 mono 展示,command + first_arg 以 chip/下划线高亮(学习粒度可见);git 黑名单不渲染卡(引擎已 Failed,错误文案走 run 流既有错误展示)。
3. 空状态:无待审改动时按 Copywriting「暂无待审改动 / agent 修改代码时,每份文件会在这里排队等你确认。」(若 AI 台已有等价空态可复用,不重复造)。
  </action>
  <verify>npm run lint; npm run build</verify>
  <acceptance_criteria>
  - AgentConsole.tsx 含 code_edit 分支与排队过滤(队首 + 「另有 N 张待审」)
  - exec 卡含「允许一次」「总是允许」「拒绝」三按钮与 first-arg 高亮
  - 空态文案落地或确认复用既有空态
  </acceptance_criteria>
  <done>第四宿主挂接 + exec 三选卡 + 空态;lint/build 通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: 04 全链验收</name>
  <what-built>diff 审批卡第四宿主 + exec 三选卡 + 沉淀入口(引擎 + 前端全链)</what-built>
  <how-to-verify>
1. `npm run tauri:dev`,设置页绑定 Nova 自身仓库(狗粮)
2. 在 AI 台让 agent 编辑一个测试文件 → 观察逐张排队 diff 卡、增删行高亮、3 行上下文
3. 拒绝并填原因 → 确认文件未落盘、run 继续且 agent 收到原因
4. 应用改动 → 文件落盘,内容含 new_string
5. 让 agent 跑未知命令(如 `node --version`)→ 三选卡出现;「总是允许」后同命令+首参数不再询问
6. 让 agent 跑 `git -c x` → 无卡,run 流出现「已拒绝:git 危险操作」
7. 点「沉淀改动摘要」→ 第二大脑出现待确认候选
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>人工按 how-to-verify 步骤验收;不做代码改动</action>
  <verify>用户回复 approved</verify>
  <done>用户确认全部验收步骤通过</done>
</task>

</tasks>

<verification>
npm run lint + npm run build + 人工 UAT 七步(上方 checkpoint)。
</verification>

<success_criteria>
- diff 卡按 UI-SPEC 全部交互契约可用;react-diff-view 懒加载独立 chunk
- reject 原因回传、沉淀走 knowledge_write 复用流
- exec 三选卡学习粒度可见
</success_criteria>

<output>
After completion, create `.planning/phases/32-coding/32-04-SUMMARY.md`
</output>
