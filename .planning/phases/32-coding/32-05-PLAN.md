---
phase: 32-coding
plan: 05
type: execute
wave: 3
depends_on: ["32-02", "32-03"]
files_modified:
  - src/components/product/TabRunPanel.tsx
  - src/stores/tabRunStore.ts
  - src/components/workspace/WorkspaceHeader.tsx
  - src/views/SettingsView.tsx
  - src-tauri/src/engine/context_assembler.rs
autonomous: false
requirements: [CODE-05, CODE-04, ENGINE-01]
must_haves:
  truths:
    - "TabRunPanel 显示当前工具名 + 目标文件 chip;exec 显示截断命令(~60ch)"
    - "exec 输出折叠展开,展示尾部 N 行实时刷新"
    - "改动文件列表 chips 带待审/已应用/已拒绝状态点"
    - "工作区检测到 repo 时 header 显示「Repo: {basename}」徽章;设置页可改 repo_root;dev 模式有一键狗粮按钮"
    - "coding 工具可用时系统 prompt 含「先研究后行动」契约(ENGINE-01)"
  artifacts:
    - path: "src/components/product/TabRunPanel.tsx"
      provides: "进度行 + exec tail 输出 + 改动文件列表"
    - path: "src/views/SettingsView.tsx"
      provides: "repo_root 设置字段 + 狗粮按钮(import.meta.env.DEV)"
    - path: "src-tauri/src/engine/context_assembler.rs"
      provides: "ENGINE-01 研究先行契约 prompt 段"
  key_links:
    - from: "src/stores/tabRunStore.ts"
      to: "tool_start/tool_output 事件 payload"
      via: "targetFile 提取 + 尾部 N 行 ring buffer"
      pattern: "tool_start"
---

<objective>
TabRunPanel 进度可见(当前工具/目标文件/exec tail/改动文件)+ repo 绑定 UI(badge/设置页/狗粮)+ ENGINE-01 prompt 契约注入。

Purpose: CODE-05 的「进度可见」用户面与 CODE-04 的绑定 UI;ENGINE-01 prompt 侧收口。
Output: TabRunPanel 扩展、tabRunStore ring buffer、repo badge + 设置、context_assembler 契约段。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-coding/32-UI-SPEC.md
@.planning/phases/32-coding/32-CONTEXT.md

<interfaces>
From 32-02/03(引擎契约):
- tool_call 事件 payload:code 工具带 path;exec 带 pid + command
- ToolOutput 事件流已有(尾部输出零引擎改动,RESEARCH Architecture Pattern 5)
From 32-01:
- Tauri 命令 `engine_workspace_bind_repo(workspace_id, repo_root?)` + workspace 打开后台检测
From 前端既有:
- tabRunStore 已处理 tool_start/tool_end/tool_output(实查 247-265 行)
- TabRunPanel.tsx 现有结构(read_first)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabRunPanel 进度 chip + exec tail + 改动文件列表</name>
  <files>src/components/product/TabRunPanel.tsx, src/stores/tabRunStore.ts</files>
  <read_first>src/stores/tabRunStore.ts, src/components/product/TabRunPanel.tsx, 32-UI-SPEC.md Interaction Contracts 3, 32-RESEARCH.md Architecture Pattern 5</read_first>
  <action>
1. tabRunStore:`tool_start` 处理时从 args 提取 `targetFile`(code 工具 = path;exec = command 截断 ~60 字符,full 存 state 供 title);维护 per-run `changedFiles: {path, status: 'pending'|'applied'|'rejected'}[]`(code_edit 候选创建 → pending;apply 事件 → applied;reject/auto-reject → rejected)。
2. exec 输出 ring buffer:tool_output 追加进尾部 N 行(默认 50)缓冲,不展示完整参数(UI-SPEC 锁定)。
3. TabRunPanel 按 UI-SPEC Interaction 3:
   - 进度行:工具图标(Phosphor `Code`/`Terminal`/`MagnifyingGlass` duotone 14-16px)+ 工具名 + 目标文件 chip(mono truncate,title 全文);exec chip = 截断命令
   - exec 输出:默认折叠,header「输出(尾部 N 行)」+ chevron;展开 mono 行实时刷新
   - 改动文件 chips:状态点 warning/success/danger(待审/已应用/已拒绝)
   - 全部语义 token 类;`text-xs font-mono` 输出行
  </action>
  <verify>npm run lint; npm run build</verify>
  <acceptance_criteria>
- tabRunStore 含 `targetFile`/`changedFiles`(或等价命名)提取逻辑与尾部行缓冲(cap 常量)
- TabRunPanel.tsx 含「输出(尾部」字符串、Phosphor Code/Terminal 图标 import
- 无完整参数展示(grep 不出现 args JSON dump)
- className 全语义 token
  </acceptance_criteria>
  <done>TabRunPanel 进度/输出尾部/改动文件三要素可见;lint/build 通过</done>
</task>

<task type="auto">
  <name>Task 2: repo 绑定 UI(badge + 设置页 + 狗粮)+ ENGINE-01 prompt 契约</name>
  <files>src/components/workspace/WorkspaceHeader.tsx, src/views/SettingsView.tsx, src-tauri/src/engine/context_assembler.rs</files>
  <read_first>src/views/SettingsView.tsx, src-tauri/src/engine/context_assembler.rs, src-tauri/src/engine/commands.rs(32-01 的 bind 命令), 32-UI-SPEC.md Interaction Contracts 4</read_first>
  <action>
1. workspace header(实查实际文件名,若无 WorkspaceHeader.tsx 则挂现有 workspace header 组件):repo_root 存在时显示 badge「Repo: {basename}」(accent,`GitBranch` duotone 12px);未检测到无 badge;工具报「未绑定代码仓库」错误走 run 流既有错误展示。
2. SettingsView 工作区设置区:repo_root 字段(Input + 浏览按钮,Tauri dialog 选目录;capability 需要则照现有 fs 先例);改动调 `engine_workspace_bind_repo` 重绑持久化。
3. dev 狗粮:`import.meta.env.DEV` 时显示「绑定 Nova 仓库(狗粮)」按钮,一键把 repo_root 设为应用自身仓库根(路径解析:dev 下取当前 workspace 相对定位 Nova 仓库,或让命令端 detect_and_bind 对 dev workspace 特判 — 采用后者更稳,commands.rs 已有 detect 入口,read_first 后择最小实现)。
4. context_assembler.rs:当 workspace 绑定了 repo_root(coding 工具集可用)时,系统 prompt 注入 ENGINE-01 契约段(中文,与现有 prompt 段形态一致):「你拥有代码工作能力。遵循先研究后行动:先用 code_read/code_grep 侦察理解现状,再定位修改点,然后用 code_write/code_edit 提出修改并等待确认,最后用 exec 验证。研究类工具零确认自由调用;行动类工具每步过审批。未绑定仓库或路径越界会被拒绝,按错误提示调整。」(措辞可微调,语义四步:侦察→定位→修改→验证)。加一条 assembler 测试断言 repo_root 绑定时 prompt 含「先研究后行动」。
  </action>
  <verify>npm run lint; cargo test -p nova-lib context_assembler 2>&1 | tail -3</verify>
  <acceptance_criteria>
- 设置页含 repo_root 输入 + `engine_workspace_bind_repo` 调用;DEV-only 按钮含「绑定 Nova 仓库」字符串
- workspace header 含 `GitBranch` 与「Repo: 」badge 逻辑
- context_assembler.rs prompt 段含「先研究后行动」,且有 repo_root 条件测试通过
  </acceptance_criteria>
  <done>repo badge + 设置页 + 狗粮按钮 + prompt 契约测试通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: 05 全链验收</name>
  <what-built>TabRunPanel 进度/exec tail/改动文件 + repo 绑定 UI + prompt 契约(Phase 32 全量收口)</what-built>
  <how-to-verify>
1. `npm run tauri:dev` → 设置页绑定 Nova 仓库 → workspace header 出现 Repo badge
2. 发起一个 coding run:TabRunPanel 观察当前工具名切换、文件 chip、exec 命令截断
3. 跑长命令(如 `npm install` 在测试 repo):展开输出看尾部行实时刷新;中途点取消 → 未决 diff 卡显示「run 已取消,改动未应用」,无 pending 残留
4. 改动文件 chips 状态点随审批流转(待审→已应用/已拒绝)
5. dev 模式点「绑定 Nova 仓库(狗粮)」→ 一键绑定成功
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>人工按 how-to-verify 步骤验收;不做代码改动</action>
  <verify>用户回复 approved</verify>
  <done>用户确认全部验收步骤通过</done>
</task>

</tasks>

<verification>
npm run lint + npm run build + cargo test 全量 + 人工 UAT 五步。
</verification>

<success_criteria>
- 长任务可取消且无 pending 残留;进度三要素(工具/文件/输出尾部)可见
- repo 绑定自动 + 可改 + 狗粮一键
- ENGINE-01 契约注入有测试
</success_criteria>

<output>
After completion, create `.planning/phases/32-coding/32-05-SUMMARY.md`
</output>
