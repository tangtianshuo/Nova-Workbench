---
phase: 32-coding
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/engine/code_ops.rs
  - src/stores/uiStore.ts
  - src/views/SettingsView.tsx
autonomous: true
gap_closure: true
requirements: [UAT32-GAP-1]

must_haves:
  truths:
    - "切换产品后,该产品关联的工作区成为 activeWorkspace,repo 绑定随之切换(workspace_repo_roots 按 workspace_id 键)"
    - "未绑定 repo 时代码工具不再 NO_REPO 失败——默认落工作区目录"
    - "绑定了 repo 时代码工具仍优先落 repo(用户显式指定 = 绑定)"
    - "repo_root ≠ 工作区路径时设置页有可见提示"
    - "旧 localStorage 数据(无新字段)正常工作"
  artifacts:
    - path: "src-tauri/src/engine/code_ops.rs"
      provides: "resolve_code_target 的 workspace_root 兜底"
      contains: "workspace_root"
    - path: "src/stores/uiStore.ts"
      provides: "setSelectedProductId → activeWorkspaceId 联动"
      contains: "useWorkspaceStore"
    - path: "src/views/SettingsView.tsx"
      provides: "repo ≠ workspace 路径提示 + 产品关联显示"
  key_links:
    - from: "src/stores/uiStore.ts#setSelectedProductId"
      to: "src/stores/workspaceStore.ts#setActiveWorkspaceId"
      via: "getState() 调用(复用 SESS-04 会话结束语义)"
      pattern: "useWorkspaceStore\\.getState\\(\\)\\.setActiveWorkspaceId"
    - from: "src-tauri/src/engine/code_ops.rs#resolve_code_target"
      to: "ToolCtx.workspace_root"
      via: "repo_root.or(workspace_root) 兜底,resolve_repo 同一边界校验"
      pattern: "workspace_root"
---

<objective>
关闭 UAT test#3 gap:产品 ↔ 工作区联动 + 代码目标默认落工作区。

三个根因缺口(诊断见 .planning/debug/workspace-repo-product-binding.md):
(1) Product↔workspace 数据关联已存在(Workspace.projectId,AddWorkspaceModal/SetAsWorkspaceModal 落)但选择层不联动——activeWorkspaceId 是 app 级全局;
(2) resolve_code_target 强制 repo_root,未绑定即 NO_REPO 失败,与"默认落工作区"相反;
(3) detect 向上找 .git 静默逃逸到祖先 repo,UI 无提示。

裁定(planner,依 locked_requirements):
- 关联复用 Workspace.projectId,不加 Product.workspaceId(避免双源;mock ws-1 已有 projectId: 'p1')
- "用户显式指定 root" = 绑定 repo(repo_root 优先)。CP-2 params 五键锁形不得破坏,故**不**新增 per-call root 参数
- workspace_root 兜底必须走与 repo_root 完全相同的 resolve_repo + nova_guard 边界(CP-1 作用域锁不放松)
- 引擎侧 loop_runner 无需改:repo_root 每 run 按 workspace_id 读(workspace_repo_roots),切工作区天然切绑定;ctx.workspace_root 已从 engine_run 的 workspaceRoot 传入(commands.rs:299)

Purpose: 用户语义——工作区和仓库默认同路径、随产品切换、产出默认落工作区。
Output: 3 个文件的最小 diff + Rust 测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-coding/32-UAT.md
@.planning/debug/workspace-repo-product-binding.md

<interfaces>
<!-- 关键现状(已核对): -->

src/stores/workspaceStore.ts:
```typescript
export interface Workspace {
  id: string; name: string; folderPath: string;
  projectId?: string;        // ← product↔workspace 关联已存在,复用它
  repoRoot?: string;         // 32-05 webview 镜像(engine 表是真相源)
  // ...
}
// setActiveWorkspaceId(id): store-guarded,切工作区=结束当前 session(SESS-04 locked,勿改)
// 有 loading(streaming)guard 返回 { success: false, reason: 'streaming' }
```

src/stores/uiStore.ts:105:
```typescript
setSelectedProductId: (id) => set({ selectedProductId: id }),  // ← 联动点
```
产品切换调用方(ProductManagementView:329/380/422、RndCenterView、App.tsx:98)全部走此 action,改这里即全覆盖。

src-tauri/src/engine/code_ops.rs:44-76:
```rust
fn repo_root_or_fail(ctx: &ToolCtx<'_>) -> Result<PathBuf, ToolOutcome>  // 未绑定即 NO_REPO_MSG 失败
fn resolve_code_target(conn, ctx, rel) -> Result<PathBuf, ToolOutcome> {
    let root = repo_root_or_fail(ctx)?;
    let path = resolve_repo(&root, rel)?;   // ← 边界校验,兜底必须复用
    if let Some(o) = nova_guard(conn, &path) { return Err(o); }
    Ok(path)
}
pub const NO_REPO_MSG: &str = "未绑定代码仓库 — 请在设置中指定 repo 目录";
```
ToolCtx(tools.rs:702)已含 `workspace_root: Option<PathBuf>`;code_ops.rs 内测试构造 ToolCtx 的模式见 :722 / :1095。

src-tauri/src/engine/loop_runner.rs:244 — repo_root 每 run 按 workspace_id 从 workspace_repo_roots 读,ENGINE-01 契约注入同源。不改。

src-tauri/src/engine/exec.rs:449 — `cwd = repo_root.unwrap_or(workspace_root)` 已是正确优先级。不改。

src/views/SettingsView.tsx:188-260 — WorkspaceRepoSection,repo_root 输入框 + detect,echo workspace?.repoRoot。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_code_target 增加 workspace_root 兜底(Rust)</name>
  <files>src-tauri/src/engine/code_ops.rs</files>
  <behavior>
    - Test 1: ctx.repo_root = None、ctx.workspace_root = Some(ws) → code_write 目标解析到 ws 内相对路径,成功(当前失败 NO_REPO)
    - Test 2: repo_root 与 workspace_root 同时存在 → 目标落在 repo_root 下(repo 优先,用户显式指定)
    - Test 3: 两者皆 None → 失败,错误信息引导(不再叫"未绑定代码仓库",改为提示无可用代码根:工作区与仓库均未指定)
    - Test 4(边界不放松): workspace_root 兜底路径含 `..` 逃逸 → resolve_repo 照旧拒绝(既有测试已覆盖 resolve_repo,补一条走兜底分支的)
  </behavior>
  <action>
    把 `repo_root_or_fail` 改为 `code_root_or_fail`(或等价):`ctx.repo_root.clone().or_else(|| ctx.workspace_root.clone())`,皆 None 时返回更新后的失败消息(替换 NO_REPO_MSG 文案为双语义:"无可用代码根目录 — 工作区未指定且未绑定仓库",保留常量名 NO_REPO_MSG 避免 32-05 UI copy 引用断链;grep TS 侧确认无对该文案的字符串相等断言,有则同步)。
    resolve_code_target 其余不动:resolve_repo + nova_guard 对兜底 root 一视同仁(CP-1 不放松)。
    code_read/code_grep/code_write/code_edit 四工具全部经 resolve_code_target,一处改全覆盖。
    不加 per-call "root" 参数——CP-2 五键锁形(Phase 32 已锁 decision),用户显式指定 = 绑定 repo。
    loop_runner / exec / commands.rs 零改动(engine_run 已传 workspaceRoot)。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test code_ops</automated>
  </verify>
  <done>未绑 repo 的 workspace 上 code_* 工具以 workspace_root 为根正常读写;绑定时 repo 优先;边界逃逸照旧拒绝。</done>
</task>

<task type="auto">
  <name>Task 2: 切产品联动工作区 + 设置页路径提示</name>
  <files>src/stores/uiStore.ts, src/views/SettingsView.tsx</files>
  <action>
    **uiStore.ts**:`setSelectedProductId` 改为联动——
    ```typescript
    setSelectedProductId: (id) => {
      set({ selectedProductId: id });
      if (!id) return;
      const ws = useWorkspaceStore.getState();
      const bound = ws.workspaces.find((w) => w.projectId === id);
      if (bound && bound.id !== ws.activeWorkspaceId) {
        ws.setActiveWorkspaceId(bound.id); // 复用 SESS-04:切工作区=结束当前 session,含 streaming guard
      }
    }
    ```
    - 顶部 lazy import `useWorkspaceStore`(workspaceStore 不 import uiStore,无环;若打包器报环,用 `import type` + 动态 import 规避)
    - 产品无关联 workspace(projectId 无匹配或 undefined)→ 不动 activeWorkspaceId(旧数据默认行为不变)
    - engine_run 三入口(chatConsoleStore.ts:766 / tabRunStore.ts:228 / CmdKPalette.tsx:79)取 activeWorkspaceId,自然继承,零改动;repo 绑定按 workspace_id 键,随切随换
    - 多 workspace 同 productId:取 find 首个即可(ponytail: 单产品多工作区场景出现再加选择 UI)

    **SettingsView.tsx** WorkspaceRepoSection:
    - 当 workspace.repoRoot 存在且不等于 workspace.folderPath 时,repo_root 输入框下方加一行 warning hint 文本:"仓库根目录与工作区路径不同(工作区是仓库子目录或绑定到了别处)"——消除 detect 向上逃逸的无感知
    - workspace.projectId 有值时,区块标题旁显示关联产品名 chip(从 useProductStore 找 id 对应 name;找不到则不显示)——产品↔工作区关系可见的最小方案
    - 零新组件,只用既有 Badge/hint 样式
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>切到有关联工作区的产品后 activeWorkspaceId 随之切换、repo badge/绑定跟着变;无关联产品行为不变;设置页能看出 repo 与工作区路径不一致及工作区挂哪个产品。</done>
</task>

</tasks>

<verification>
- cd src-tauri && cargo test(全量,确认无回归)
- npm run lint
- 手动 UAT 路径(回归 test#3):绑 repo → 切到另一产品(其工作区未绑 repo)→ 发起 coding run 让 agent 写文件 → 文件落在该工作区 folderPath 下而非 NO_REPO 失败;切回绑了 repo 的产品 → repo badge 恢复、code_* 落 repo
</verification>

<success_criteria>
- UAT test#3 gap 三条 truth 全部可达成;CP-1..CP-3 安全约束原样(boundary 测试全绿);旧 persist 数据无新字段依赖
</success_criteria>

<output>
After completion, create `.planning/phases/32-coding/32-06-SUMMARY.md`
</output>
