---
phase: quick
plan: 260811-vg6
type: execute
wave: 1
depends_on: []
files_modified: [src/views/ScheduleView.tsx]
autonomous: true
requirements: [QUICK-VG6]
---

<objective>
统一 ScheduleView 中"本月日程"侧栏与左侧"日历"卡片的高度。当日程事件超出该高度时,事件列表在侧栏内部滚动,不再撑高整个页面。

Purpose: 视觉对齐两个 Card;防止内容多时触发 main 区域页面级滚动。
Output: src/views/ScheduleView.tsx 两处 className 调整(line 177 容器、line 305 Agenda Card)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/views/ScheduleView.tsx
@src/views/TaskManagementView.tsx

<interfaces>
<!-- 复用项目内既有模式,无新接口 -->

From src/views/TaskManagementView.tsx (line 21):
```tsx
<div className="flex gap-6 h-[calc(100vh-220px)] min-h-[400px]">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固定 ScheduleView 容器高度并对齐 Agenda Card</name>
  <files>src/views/ScheduleView.tsx</files>
  <behavior>
    - 容器高度固定为 `h-[calc(100vh-220px)] min-h-[400px]`(对齐 TaskManagementView 模式),不再用 `h-full min-h-[700px]`
    - 右侧 Agenda Card 增加 `min-h-0`(flex child 防御),使内部 `overflow-y-auto` 真正生效
    - 当 agendaEvents 内容超出 Card 高度时,仅 `.flex-1.overflow-y-auto` 区域内部滚动,Card 高度不变,容器不被撑开,页面 `<main>` 不触发滚动
    - Calendar Card 高度跟随容器,Agenda Card 高度严格等于 Calendar Card
  </behavior>
  <action>
仅两处 className 编辑,不动任何逻辑/结构:

1. **Line 177 容器** —— 把 `flex gap-5 h-full min-h-[700px]` 改成 `flex gap-5 h-[calc(100vh-220px)] min-h-[400px]`。
   - 复用 src/views/TaskManagementView.tsx 的同款 fixed-height 模式(项目既有约定,不引入新模式)。
   - 删掉 `h-full`(父级 main 已是 flex-1 overflow-auto,`h-full` 在 min-h 配合下会让内容撑开)。

2. **Line 305 Agenda Card** —— 把 `w-72 p-5 flex flex-col shrink-0` 改成 `w-72 p-5 flex flex-col shrink-0 min-h-0`。
   - `min-h-0` 是 flex column 子项让内部 `overflow-y-auto` 生效的标准修复(min-height:auto 默认值会阻止子元素收缩到内容以下)。
   - Card 内部第 310 行 `flex-1 space-y-3 overflow-y-auto pr-1` 已存在,无需改动。

**不要:**
- 改 Calendar Card(左)的 className —— 它的 `flex-1 flex flex-col` 会自动匹配容器高度
- 改 Agenda 内部滚动区或事件渲染逻辑
- 加 max-h、固定 px 高度、ResizeObserver 或任何 JS
- 重构布局结构

**为何:`h-[calc(100vh-220px)]` 而不是固定值:** 对齐 TaskManagementView 的既有约定,响应视口高度变化;`min-h-[400px]` 保证小屏可用性。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    - npm run lint (tsc --noEmit) 通过,无类型错误
    - ScheduleView line 177 容器使用 `h-[calc(100vh-220px)] min-h-[400px]`
    - ScheduleView line 305 Agenda Card 包含 `min-h-0`
    - 视觉验证(人工,执行后由 HUMAN-UAT 覆盖):本月日程事件多时,侧栏内部出现独立滚动条;两侧 Card 顶底对齐;页面整体不再被撑高
  </done>
</task>

</tasks>

<verification>
- npm run lint 通过
- 视觉行为(留给 UAT/HUMAN-UAT):resize 窗口时两个 Card 等高;agenda 内容多时仅侧栏内部滚动
</verification>

<success_criteria>
- ScheduleView 容器高度固定,Agenda 与 Calendar Card 等高
- 内容溢出在 Agenda 内部滚动,不撑高页面
- 最小 diff(两行 className),复用项目内既有 fixed-height 模式
</success_criteria>

<output>
After completion, create `.planning/quick/260811-vg6-schedule-height-match-calendar/260811-vg6-SUMMARY.md`
</output>
