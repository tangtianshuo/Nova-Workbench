---
phase: quick
plan: 260813-u7m
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/uiStore.ts
  - src/lib/api.ts
  - src/ai/toolLoop.ts
  - src-tauri/Cargo.toml
  - src-tauri/src/llm.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/components/SettingsApiKeySection.tsx
  - src/stores/__tests__/settingsProvider.test.ts
autonomous: true
requirements: [BUG-OLLAMA-MODEL, FEAT-CONNECTIVITY-PING]
user_setup: []

must_haves:
  truths:
    - "用户在 Settings 修改 Ollama 模型名(例如 my-ornith)后,发送消息时 Rust 端用该模型名调用 Ollama"
    - "Ollama 模型名持久化到 uiStore(刷新后仍保留)"
    - "用户保存 API key 后,Settings 立即 ping 一次对应 provider,根据结果在 toast 和 provider status 显示成功或具体失败原因"
    - "切换到 Ollama provider 时自动 ping localhost:11434 并校验模型名存在"
    - "providerStatuses 区分 已配置且可用 / 已配置但不可用 / 未配置 / 读取中 四态"
    - "Web mode 跳过 ping(只支持 Gemini 由服务端配置),不调用任何 Tauri 命令"
  artifacts:
    - path: "src/stores/uiStore.ts"
      provides: "ollamaModel 字段 + setOllamaModel action + persist 包含"
    - path: "src/lib/api.ts"
      provides: "chatWithTools 接受 ollamaModel;新增 pingProvider wrapper"
    - path: "src/ai/toolLoop.ts"
      provides: "从 useUIStore.getState().ollamaModel 读取并传入 chatWithTools"
    - path: "src-tauri/src/llm.rs"
      provides: "chat_with_tools 接受 ollama_model 参数;新增 ping_provider 函数"
    - path: "src-tauri/src/commands.rs"
      provides: "ChatArgs 加 ollamaModel;新增 ping_provider command"
    - path: "src-tauri/src/lib.rs"
      provides: "ping_provider 在 invoke_handler 注册"
    - path: "src/components/SettingsApiKeySection.tsx"
      provides: "Ollama 模型输入 + 测试连接按钮 + 三态 status + 自动 ping 流程"
  key_links:
    - from: "src/ai/toolLoop.ts"
      to: "src/lib/api.ts#chatWithTools"
      via: "ollamaModel arg"
      pattern: "ollamaModel"
    - from: "src/lib/api.ts#chatWithTools"
      to: "src-tauri/src/commands.rs#chat"
      via: "Tauri invoke args.ollamaModel"
      pattern: "ollamaModel"
    - from: "src-tauri/src/commands.rs#chat"
      to: "src-tauri/src/llm.rs#chat_with_tools"
      via: "ollama_model param"
      pattern: "ollama_model"
    - from: "src/components/SettingsApiKeySection.tsx"
      to: "src-tauri/src/commands.rs#ping_provider"
      via: "pingProvider wrapper after save key / on Ollama switch"
      pattern: "pingProvider"
---

<objective>
Two coupled fixes to the Nova AI provider layer:

1. **Bug fix:** Ollama 模型名硬编码为 `llama3.2`(`src-tauri/src/llm.rs:225-229`),用户拉取的是 `my-ornith` 等自定义模型时无法发起 chat。让模型名可配置,从 uiStore 流到 Rust。
2. **Feature:** 在 Settings 保存 key / 切换 Ollama 时即时 ping provider,把 `providerStatuses` 从「存在性」升级为「可用性」三态语义,失败时 toast 显示具体原因。

Purpose: 解决用户实际遇到的 "模型不存在" 报错 + 兑现 .planning/todos/pending/2026-08-12-setting-api-key-connectivity.md 的连通性验证诉求。
Output: 7-8 个文件、约 150-200 行净 diff、单一 commit 可发布的快速修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-08-12-setting-api-key-connectivity.md
@./CLAUDE.md
@src-tauri/src/llm.rs
@src-tauri/src/commands.rs
@src-tauri/src/state.rs
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml
@src/lib/api.ts
@src/components/SettingsApiKeySection.tsx
@src/stores/uiStore.ts
@src/ai/toolLoop.ts
@src/stores/__tests__/settingsProvider.test.ts

<interfaces>
<!-- Key contracts the executor must satisfy. -->

From src-tauri/src/llm.rs (current):
```rust
pub async fn chat_with_tools(
    provider: Provider,
    api_key: &str,
    messages: Vec<ChatMessage>,
    tools_json: Vec<serde_json::Value>,
    system_prompt: String,
    on_token: &Channel<StreamChunk>,
    cancel: &CancellationToken,
) -> Result<ChatResult, AppError>
```
After this plan: add `ollama_model: Option<String>` as 3rd param.

From src-tauri/src/commands.rs (current ChatArgs):
```rust
#[derive(serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ChatArgs {
    pub messages: Vec<ChatMessage>,
    pub tools: Vec<serde_json::Value>,
    pub system_prompt: String,
    pub provider: Provider,
    pub request_id: String,
}
```
After: add `pub ollama_model: Option<String>`.

From src/lib/api.ts chatWithTools invoke payload:
```ts
const result = await invoke<...>('chat', {
  args: {
    messages, tools, systemPrompt, provider, requestId,
  },
  onToken: channel,
});
```
After: add `ollamaModel` to args object.

From src/stores/uiStore.ts persist partialize:
```ts
partialize: (s) => ({
  activeTab: s.activeTab,
  selectedProductId: s.selectedProductId,
  activeAIProvider: s.activeAIProvider,
}),
```
After: add `ollamaModel: s.ollamaModel`.

From src-tauri/src/error.rs AppError variants (unchanged — ping errors reuse these):
```rust
NetworkError(String), AuthError, ParseError(String), Cancelled, InternalError(String)
```

Provider status upgrade (src/components/SettingsApiKeySection.tsx):
```ts
type ProviderStatus = Record<Provider, boolean | null>;
// After:
type ProviderStatusValue = boolean | 'invalid' | null;
type ProviderStatus = Record<Provider, ProviderStatusValue>;
```

Ollama ping API response shape (from ollama serve `/api/tags`):
```json
{ "models": [ { "name": "my-ornith:latest", "model": "my-ornith:latest", ... } ] }
```
The configured name may or may not have a `:tag` suffix. Match by prefix-before-colon (i.e. `my-ornith` matches `my-ornith:latest`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire Ollama model name from uiStore through to Rust chat_with_tools</name>
  <files>src/stores/uiStore.ts, src/lib/api.ts, src/ai/toolLoop.ts, src-tauri/src/commands.rs, src-tauri/src/llm.rs</files>
  <behavior>
    - uiStore 读取 `ollamaModel` 字段后,刷新浏览器/webview 仍保留(persist partialize 覆盖)
    - `setOllamaModel('my-ornith')` 后 `useUIStore.getState().ollamaModel === 'my-ornith'`
    - chatWithTools 接收 `ollamaModel?: string` 参数,Tauri 路径下作为 `args.ollamaModel` 传入 invoke payload(serde camelCase 自动映射到 Rust `ollama_model`)
    - toolLoop 从 `useUIStore.getState().ollamaModel` 读取并传入 chatWithTools(用 `.getState()`,不用 hook —— 这是普通函数不是组件)
    - Rust `ChatArgs` 接受可选 `ollama_model: Option<String>` 并转发给 `chat_with_tools`
    - Rust `chat_with_tools` 在 `Provider::Ollama` 分支按优先级解析模型名:函数参数 > NOVA_OLLAMA_MODEL 环境变量 > OLLAMA_MODEL 环境变量 > `ollama::LLAMA3_2` 默认值
    - 其他 provider 分支忽略该参数
  </behavior>
  <action>
1. **`src/stores/uiStore.ts`**:
   - `UIState` interface 加 `ollamaModel: string;`
   - `UIState` 加 action signature: `setOllamaModel: (model: string) => void;`
   - 初始值 `ollamaModel: 'llama3.2',`
   - 实现: `setOllamaModel: (model) => set({ ollamaModel: model }),`
   - 在 `partialize` 返回对象里加 `ollamaModel: s.ollamaModel,`(让 persist 持久化)

2. **`src/lib/api.ts`** (`chatWithTools`):
   - `args` 参数对象类型加 `ollamaModel?: string;`
   - 在 Tauri 分支的 `invoke('chat', { args: { ... } })` 的 args 对象里加 `ollamaModel: args.ollamaModel,`(undefined 时 Tauri 会自动当 Option::None,无需手动 undefined 判断)
   - Web fallback 分支不动(只支持 Gemini,不需要)

3. **`src/ai/toolLoop.ts`** (`runToolLoop`):
   - 文件顶部已 import `chatWithTools`;新增 import `useUIStore` from `@/src/stores/uiStore`
   - 在 `chatWithTools({ ... })` 调用里加 `ollamaModel: args.provider === 'ollama' ? useUIStore.getState().ollamaModel : undefined,`
   - 不要用 `useUIStore()` hook 调用 —— 这是个普通异步函数,React 渲染期外。`.getState()` 是 Zustand 的同步快照,符合规范(CLAUDE.md 已记录此模式)

4. **`src-tauri/src/commands.rs`** (`ChatArgs` + `chat`):
   - `ChatArgs` 加字段:`pub ollama_model: Option<String>,`(serde rename_all camelCase 已生效,JS 端 `ollamaModel` 自动映射)
   - `chat` 命令把 `args.ollama_model` 传给 `llm::chat_with_tools`(签名见下)
   - 更新现有 `#[cfg(test)]` 或生产代码中对 `chat_with_tools` 的调用(目前只有 `chat` 命令一处 + `ollama_real_tool_call_uat` 测试一处)
   - `ollama_real_tool_call_uat` 测试:`chat_with_tools(..., None, ...)` 传入 None 保持原行为

5. **`src-tauri/src/llm.rs`** (`chat_with_tools`):
   - 函数签名第 3 个参数加:`ollama_model: Option<String>,`
   - `Provider::Ollama` 分支解析顺序改为:
     ```rust
     let model_name = ollama_model
         .filter(|value| !value.trim().is_empty())
         .or_else(|| {
             std::env::var("NOVA_OLLAMA_MODEL")
                 .or_else(|_| std::env::var("OLLAMA_MODEL"))
                 .ok()
                 .filter(|value| !value.trim().is_empty())
         })
         .unwrap_or_else(|| ollama::LLAMA3_2.to_string());
     ```
   - 其他 provider 分支签名不变,只是多了个未使用参数(用 `let _ = &ollama_model;` 或在分支 match 里不引用即可;Option<String> 编译不会 unused 警告)

**Pitfall:** serde rename_all camelCase 让 JS `ollamaModel` ↔ Rust `ollama_model`。已在 ChatArgs 上启用,新字段自动遵守。
**Pitfall:** 不要把 ollamaModel 加进 web fallback 路径的 fetch body —— server.ts Express 端不认识该字段,会被忽略。只加在 Tauri invoke args。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib chat_with_tools_signature 2>&1 | head -5 && cargo build 2>&1 | tail -20</automated>
    Run `cd D:/Projects/Nova/nova-pm-workspace && npm run lint` (tsc clean — verify toolLoop.ts api.ts uiStore.ts 编译通过).
    Run `cd D:/Projects/Nova/nova-pm-workspace && npx tsx --test src/stores/__tests__/settingsProvider.test.ts` (existing contract still passes — provider list 不变).
    Run `cd D:/Projects/Nova/nova-pm-workspace/src-tauri && cargo test` (all existing Rust tests pass, including provider_serializes_to_lowercase_wire_names and the ignored ollama_real_tool_call_uat compiles).
  </verify>
  <done>
    - `ollamaModel` 在 uiStore 中持久化
    - toolLoop 调用 chatWithTools 时传 ollamaModel(provider === 'ollama' 时)
    - Rust chat_with_tools 接受 Option<String>,Ollama 分支按 参数 > 环境变量 > 默认 解析
    - tsc + cargo test 全绿
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Rust ping_provider + expose via Tauri command + JS wrapper</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/llm.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src/lib/api.ts</files>
  <behavior>
    - `ping_provider(Provider::DeepSeek, "sk-...", None)` 真实 key 时返回 Ok(()),错误 key 返回 `AppError::AuthError`,网络问题返回 `AppError::NetworkError(...)`
    - `ping_provider(Provider::Ollama, "", Some("my-ornith"))`:
      - 若 localhost:11434 不可达 → `AppError::NetworkError("无法连接 Ollama ...")`
      - 若可达但模型不存在 → `AppError::InternalError("模型 X 不存在,可用模型: ...")`
      - 若模型存在 → Ok(())
    - 所有 provider ping 超时上限 5 秒(避免阻塞 UI)
    - 前端 `pingProvider(provider, key, ollamaModel)` wrapper 调用 Tauri command,web mode 下 throw 带 "桌面端可用" 提示
  </behavior>
  <action>
1. **`src-tauri/Cargo.toml`**:
   - `[dependencies]` 加 `reqwest = { version = "0.13", default-features = false, features = ["json", "rustls-tls"] }`
   - 已是 rig-core 的 transitive dep,显式声明避免 `cargo build` 警告
   - 用 rustls-tls 而非 native-tls 避免 Windows OpenSSL 依赖

2. **`src-tauri/src/llm.rs`** 新增 `ping_provider`:
   ```rust
   pub async fn ping_provider(
       provider: Provider,
       api_key: &str,
       ollama_model: Option<String>,
   ) -> Result<(), AppError> { ... }
   ```
   实现策略:
   - **Ollama 分支**(不走 LLM,直接打 Ollama REST):
     ```rust
     let client = reqwest::Client::builder()
         .timeout(std::time::Duration::from_secs(5))
         .build()
         .map_err(|e| AppError::InternalError(format!("http client: {}", e)))?;
     let resp = client.get("http://localhost:11434/api/tags")
         .send().await
         .map_err(|e| AppError::NetworkError(format!("无法连接 Ollama (localhost:11434): {}", e)))?;
     if !resp.status().is_success() {
         return Err(AppError::NetworkError(format!("Ollama 返回 {}", resp.status())));
     }
     let body: serde_json::Value = resp.json().await
         .map_err(|e| AppError::ParseError(format!("Ollama /api/tags 响应解析失败: {}", e)))?;
     let models = body.get("models").and_then(|v| v.as_array())
         .ok_or_else(|| AppError::ParseError("Ollama 响应缺少 models 字段".into()))?;
     let available: Vec<String> = models.iter().filter_map(|m| {
         m.get("name").and_then(|v| v.as_str()).map(String::from)
     }).collect();
     let configured = ollama_model.unwrap_or_else(|| ollama::LLAMA3_2.to_string());
     // 模型名可能带 :tag 也可能不带,Ollama /api/tags 返回的 name 通常带 :latest
     // 用前缀匹配:configured 是 "my-ornith",available 里有 "my-ornith:latest" → match
     let exists = available.iter().any(|name| {
         name == &configured || name.starts_with(&format!("{}:", configured))
     });
     if !exists {
         return Err(AppError::InternalError(format!(
             "模型 {} 不存在,可用模型: {}",
             configured,
             available.join(", ")
         )));
     }
     Ok(())
     ```
   - **其他 provider 分支**(DeepSeek/OpenAI/Anthropic/Gemini):
     发 1-token chat completion,看 HTTP status。复用 rig 的 client init,但用一个**非 streaming** 路径避免 Channel 复杂度。
     
     最小路径:直接用 reqwest 打各家 REST endpoint:
     ```rust
     let client = reqwest::Client::builder()
         .timeout(std::time::Duration::from_secs(5))
         .build()
         .map_err(|e| AppError::InternalError(format!("http client: {}", e)))?;
     let (url, auth_header, body) = match provider {
         Provider::DeepSeek => (
             "https://api.deepseek.com/chat/completions",
             format!("Bearer {}", api_key),
             serde_json::json!({
                 "model": "deepseek-chat",
                 "messages": [{"role": "user", "content": "ping"}],
                 "max_tokens": 1,
                 "stream": false,
             }),
         ),
         Provider::OpenAI => (
             "https://api.openai.com/v1/chat/completions",
             format!("Bearer {}", api_key),
             serde_json::json!({
                 "model": "gpt-4o-mini",
                 "messages": [{"role": "user", "content": "ping"}],
                 "max_tokens": 1,
             }),
         ),
         Provider::Anthropic => (
             "https://api.anthropic.com/v1/messages",
             format!("x-api-key: {}", api_key),  // 实际通过 header map 设置
             serde_json::json!({
                 "model": "claude-3-5-sonnet-latest",
                 "max_tokens": 1,
                 "messages": [{"role": "user", "content": "ping"}],
             }),
         ),
         Provider::Gemini => (
             &format!("https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key={}", api_key),
             String::new(),
             serde_json::json!({
                 "contents": [{"parts": [{"text": "ping"}]}],
                 "generationConfig": {"maxOutputTokens": 1},
             }),
         ),
         Provider::Ollama => unreachable!("handled above"),
     };
     ```
     Anthropic 还需要 `anthropic-version: 2023-06-01` header,用 reqwest RequestBuilder 链式 `.header()` 设置。
     
     响应处理:
     ```rust
     let resp = client.post(url).header("Authorization", &auth_header).json(&body).send().await
         .map_err(|e| AppError::NetworkError(format!("{}: {}", provider, e)))?;
     let status = resp.status();
     if status.as_u16() == 401 || status.as_u16() == 403 {
         return Err(AppError::AuthError);
     }
     if !status.is_success() {
         let text = resp.text().await.unwrap_or_default();
         return Err(AppError::NetworkError(format!("{} 返回 {}: {}", provider, status, truncate(&text, 200))));
     }
     Ok(())
     ```
     `truncate` 是个 5 行 helper,文件底部加:
     ```rust
     fn truncate(s: &str, max: usize) -> String {
         if s.chars().count() <= max { return s.to_string(); }
         let mut out: String = s.chars().take(max).collect();
         out.push('…');
         out
     }
     ```

3. **`src-tauri/src/commands.rs`** 新增 command:
   ```rust
   #[tauri::command]
   pub async fn ping_provider(
       provider: Provider,
       key: Option<String>,
       ollama_model: Option<String>,
   ) -> Result<(), AppError> {
       let api_key = key.unwrap_or_default();
       llm::ping_provider(provider, &api_key, ollama_model).await
   }
   ```
   - 不需要 `State`(ping 是无状态的)
   - key 用 Option 让前端可以省略(Ollama 场景)

4. **`src-tauri/src/lib.rs`**:
   - 在 `invoke_handler` macro 里加 `commands::ping_provider,`

5. **`src/lib/api.ts`** 新增 wrapper(放在 `setProviderKey` 之后):
   ```ts
   export async function pingProvider(
     provider: Provider,
     key: string,
     ollamaModel?: string,
   ): Promise<void> {
     if (!isTauri()) {
       throw new Error('连通性验证仅在桌面端可用');
     }
     const { invoke } = await import('@tauri-apps/api/core');
     await invoke<void>('ping_provider', {
       provider,
       key: key || null,
       ollamaModel: ollamaModel ?? null,
     });
   }
   ```

**Pitfall:** Anthropic header 不是 `Authorization: Bearer`,而是 `x-api-key` + `anthropic-version`,必须用 RequestBuilder 的 `.header()` 链式调用分别设置。DeepSeek/OpenAI 才是 Bearer。
**Pitfall:** reqwest 0.13 默认 feature 包含 native-tls,Windows 上需要 OpenSSL。显式 `default-features = false, features = ["json", "rustls-tls"]` 避免 native-tls 依赖。
**Pitfall:** Ollama `/api/tags` 是 GET 不是 POST。打错方法返回 405。
**Pitfall:** Gemini key 在 URL query 不在 header。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-pm-workspace/src-tauri && cargo build 2>&1 | tail -10</automated>
    Run `cd D:/Projects/Nova/nova-pm-workspace/src-tauri && cargo test` (existing tests still pass; no new test required per spec).
    Run `cd D:/Projects/Nova/nova-pm-workspace && npm run lint` (api.ts 新增 pingProvider export 编译通过).
  </verify>
  <done>
    - reqwest 显式声明在 Cargo.toml,build 通过
    - `ping_provider` 在 llm.rs 实现,5s 超时,5 个 provider 各自路径正确
    - `commands::ping_provider` 注册到 invoke_handler
    - JS 端 `pingProvider` wrapper 存在,web mode throw 友好消息
    - cargo build + tsc 全绿
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire Settings UI — Ollama model input, auto-ping on key save / Ollama switch, three-state status</name>
  <files>src/components/SettingsApiKeySection.tsx, src/stores/__tests__/settingsProvider.test.ts</files>
  <behavior>
    - Ollama 选中时显示 model 输入框 + "测试连接" 按钮,默认值来自 uiStore.ollamaModel
    - 保存 API key 成功后立即 ping provider,根据结果显示 toast + 更新 providerStatuses
    - 切换到 Ollama 时自动 ping,根据结果显示 toast + status
    - providerStatuses 类型升级为 `Record<Provider, boolean | 'invalid' | null>`,UI label 四态:`true`→已配置/已就绪、`'invalid'`→配置但不可用、`false`→未配置、`null`→读取中
    - Ollama status:`true`→已就绪、`'invalid'`→不可用、`null`→读取中(初始/null)
    - settingsProvider.test.ts 仍 pass(provider 列表 / isProvider 不变)
  </behavior>
  <action>
1. **`src/components/SettingsApiKeySection.tsx`** 改动:

   **a. 类型升级:**
   ```ts
   type ProviderStatusValue = boolean | 'invalid' | null;
   type ProviderStatus = Record<Provider, ProviderStatusValue>;
   ```
   `emptyProviderStatus()` 不变(Ollama 初始 `true`,其他初始 `null`)。

   **b. 引入 uiStore + pingProvider:**
   ```ts
   import { pingProvider, isTauri, type Provider } from '@/src/lib/api';
   import { useUIStore } from '@/src/stores/uiStore';
   // ...在组件内:
   const ollamaModel = useUIStore((state) => state.ollamaModel);
   const setOllamaModel = useUIStore((state) => state.setOllamaModel);
   const [isPinging, setIsPinging] = useState(false);
   const [ollamaModelInput, setOllamaModelInput] = useState(ollamaModel);
   ```
   在 `useEffect([ollamaModel])` 里同步 ollamaModelInput(避免外部修改不刷新)。

   **c. 新增 `runPing` helper(单一来源,handleSaveKey + handleProviderChange + 测试按钮共用):**
   ```ts
   const runPing = async (
     provider: Provider,
     key: string,
     ollamaModelValue?: string,
   ): Promise<{ ok: true } | { ok: false; reason: string }> => {
     if (!isTauri()) return { ok: false, reason: 'Web mode 仅支持 Gemini' };
     try {
       await pingProvider(
         provider,
         key,
         provider === 'ollama' ? (ollamaModelValue ?? ollamaModel) : undefined,
       );
       return { ok: true };
     } catch (error) {
       const reason = typeof error === 'string' ? error : (error as Error).message ?? String(error);
       return { ok: false, reason };
     }
   };
   ```
   reason 是 Rust AppError 的 Display 字符串(`invalid api key` / `network error: ...` / `internal error: 模型 X 不存在...` / `parse error: ...` / `cancelled`)。在 toast 里直接展示 — humanizeAIError 已经在 api.ts 里把这些字符串映射成中文,但 pingProvider 不走 humanizeAIError。所以在这里直接显示原 reason(它已经够具体:模型名/网络/AuthError 等)。

   **d. `handleSaveKey` 升级**(在 setProviderKey 成功后追加 ping):
   ```ts
   const handleSaveKey = async () => {
     const key = keyInput.trim();
     if (!key || isSavingKey || webMode || selectedProvider === 'ollama') return;
     setIsSavingKey(true);
     try {
       await invokeProviderCommand<void>('set_provider_key', { provider: selectedProvider, key });
       // ping 阶段
       setIsPinging(true);
       const result = await runPing(selectedProvider, key);
       setIsPinging(false);
       if (result.ok) {
         setProviderStatuses((current) => ({ ...current, [selectedProvider]: true }));
         setKeyInput('');
         toast({ type: 'success', title: `${providerLabel(selectedProvider)} API key 已保存`, description: '连通性验证通过。' });
       } else {
         setProviderStatuses((current) => ({ ...current, [selectedProvider]: 'invalid' }));
         setKeyInput('');  // 清空,但保留 keychain 中的(可能 valid 只是暂时网络)
         toast({ type: 'warning', title: `${providerLabel(selectedProvider)} key 已保存但 ping 失败`, description: result.reason });
       }
     } catch {
       toast({ type: 'error', title: 'API key 保存失败', description: '请检查桌面端钥匙串权限后重试。' });
     } finally {
       setIsSavingKey(false);
       setIsPinging(false);
     }
   };
   ```

   **e. `handleProviderChange` 升级**(Ollama 分支触发 ping):
   ```ts
   const handleProviderChange = async (provider: Provider) => {
     if (provider === selectedProvider || isSwitching) return;
     if (webMode && provider !== 'gemini') { ...原有 toast...; return; }

     setIsSwitching(true);
     try {
       await invokeProviderCommand<void>('set_active_provider', { provider });
       setSelectedProvider(provider);
       setActiveAIProvider(provider);
       setKeyInput('');
       toast({ type: 'success', title: `已切换到 ${providerLabel(provider)}` });

       // Ollama 自动 ping(切到 Ollama 时立刻验证本地服务 + 模型可用)
       if (provider === 'ollama' && isTauri()) {
         setIsPinging(true);
         const result = await runPing('ollama', '', ollamaModel);
         setIsPinging(false);
         if (result.ok) {
           setProviderStatuses((current) => ({ ...current, ollama: true }));
           toast({ type: 'success', title: 'Ollama 在 localhost:11434 可达', description: `模型 ${ollamaModel} 已就绪。` });
         } else {
           setProviderStatuses((current) => ({ ...current, ollama: 'invalid' }));
           toast({ type: 'warning', title: 'Ollama 不可用', description: result.reason });
         }
       }
     } catch { ...原 error toast... } finally {
       setIsSwitching(false);
     }
   };
   ```

   **f. 新增 `handleSaveOllamaModel` + `handleTestOllama`:**
   ```ts
   const handleSaveOllamaModel = async () => {
     const trimmed = ollamaModelInput.trim();
     if (!trimmed) return;
     setOllamaModel(trimmed);
     toast({ type: 'success', title: 'Ollama 模型名已保存', description: `下次 chat 调用将使用 ${trimmed}。` });
     // 保存后自动 ping 验证
     setIsPinging(true);
     const result = await runPing('ollama', '', trimmed);
     setIsPinging(false);
     if (result.ok) {
       setProviderStatuses((current) => ({ ...current, ollama: true }));
       toast({ type: 'success', title: 'Ollama 可达', description: `模型 ${trimmed} 已就绪。` });
     } else {
       setProviderStatuses((current) => ({ ...current, ollama: 'invalid' }));
       toast({ type: 'warning', title: 'Ollama ping 失败', description: result.reason });
     }
   };

   const handleTestOllama = async () => {
     setIsPinging(true);
     const result = await runPing('ollama', '', ollamaModelInput.trim() || ollamaModel);
     setIsPinging(false);
     if (result.ok) {
       setProviderStatuses((current) => ({ ...current, ollama: true }));
       toast({ type: 'success', title: 'Ollama 可达', description: `模型 ${ollamaModel} 已就绪。` });
     } else {
       setProviderStatuses((current) => ({ ...current, ollama: 'invalid' }));
       toast({ type: 'error', title: 'Ollama ping 失败', description: result.reason });
     }
   };
   ```

   **g. Ollama card JSX 重写**(替换原 264-274 行的简单 section):
   ```tsx
   <section className="rounded-[var(--radius-lg)] border border-border-subtle p-5 space-y-4" aria-labelledby="ollama-heading">
     <div className="flex items-center gap-2 text-text-primary">
       <Robot size={18} weight="duotone" />
       <h3 id="ollama-heading" className="text-sm font-semibold">Ollama 本地 provider</h3>
     </div>
     <p className="text-xs leading-5 text-text-secondary">
       Ollama 不需要 API key。请确保已运行 <code className="font-mono">ollama serve</code>,并通过 <code className="font-mono">ollama pull &lt;model&gt;</code> 拉取所需模型。
     </p>
     <div className="space-y-2">
       <label htmlFor="ollama-model-input" className="text-xs text-text-tertiary">本地模型名</label>
       <div className="flex items-center gap-2">
         <Input
           id="ollama-model-input"
           value={ollamaModelInput}
           onChange={(e) => setOllamaModelInput(e.target.value)}
           placeholder="例如 llama3.2 / my-ornith / qwen2.5"
           disabled={isPinging}
         />
         <Button
           type="button"
           variant="secondary"
           size="sm"
           onClick={() => void handleTestOllama()}
           disabled={isPinging}
           loading={isPinging}
         >
           测试连接
         </Button>
         <Button
           type="button"
           variant="primary"
           size="sm"
           onClick={() => void handleSaveOllamaModel()}
           disabled={!ollamaModelInput.trim() || ollamaModelInput.trim() === ollamaModel || isPinging}
         >
           保存模型
         </Button>
       </div>
       <p className="text-[11px] text-text-tertiary">
         当前保存: <code className="font-mono">{ollamaModel}</code>。chat 调用走 <code className="font-mono">http://localhost:11434</code>。
       </p>
     </div>
   </section>
   ```

   **h. provider selector 里的 status label 四态**(更新原 233-246 行的 span):
   ```tsx
   {!isWebUnavailable && (
     option.id === 'ollama'
       ? status === null ? '读取中' : status === 'invalid' ? '不可用' : status ? '已就绪' : '未配置'
       : status === null ? '读取中' : status === 'invalid' ? '配置但不可用' : status ? '已配置' : '未配置'
   )}
   {!isWebUnavailable && status === true && <CheckCircle size={11} weight="fill" className="text-success" />}
   {!isWebUnavailable && status === 'invalid' && <Warning size={11} weight="fill" className="text-warning" />}
   ```
   Import 加 `Warning`(可能已 import)。

   **i. `aria-label` 同步升级**(原 218 行,反映新四态):
   ```tsx
   aria-label={`${option.label},${isWebUnavailable ? '桌面端可用' : status === 'invalid' ? '配置但不可用' : status === true ? '已配置/已就绪' : status === false ? '未配置' : '读取中'}`}
   ```

2. **`src/stores/__tests__/settingsProvider.test.ts`**: 不改测试,只确认仍 pass(PROVIDER_OPTIONS 和 isProvider 都未变,新增 uiStore 字段和 Settings 内部状态不暴露到此模块)。

**Pitfall:** `toast({ type: 'warning', ... })` 需要确认 Toast 组件支持 'warning' 类型。检查 `src/components/ui/Toast.tsx`,如果不支持则用 'error' 并在 description 里强调 "已保存但 ping 失败"。先 grep 确认。
**Pitfall:** `setKeyInput('')` 在 ping 失败时也清空,因为 key 已写入 keychain;用户重新输入新 key 会覆盖。但若只是临时网络问题,keychain 里的值是有效的,下次手动测试连接会恢复。这是 ponytail 取舍(避免维护 "key 未确认" 状态),upgrade path 是加 `setProviderStatuses((cur) => ({...cur, [provider]: 'pending_verification'}))` 第四态。
**Pitfall:** 不要在 ping 失败时调 `set_provider_key` 把 keychain 里的 key 删除 —— key 可能是有效的,只是网络/配额问题。保留让用户决定重试或换 key。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-pm-workspace && npm run lint</automated>
    Run `cd D:/Projects/Nova/nova-pm-workspace && npx tsx --test src/stores/__tests__/settingsProvider.test.ts`.
    Manual UAT (left for user — NOT this task):
    1. 桌面端启动 app,进 Settings,选 Ollama → 看到 model 输入框,输入 "my-ornith",点 "保存模型" → 应显示 "Ollama 可达,模型 my-ornith 已就绪" toast,status 显示 已就绪
    2. 发起一条 chat 消息 → 不再出现 model not found 错误
    3. 切回 DeepSeek,输入一个假 key 保存 → 应显示 "key 已保存但 ping 失败 / invalid api key" warning toast,status 显示 "配置但不可用"
    4. 重新输入正确 key 保存 → 应显示 success toast,status 显示 "已配置"
  </verify>
  <done>
    - Ollama provider card 显示模型输入框 + 测试连接 + 保存按钮
    - 保存 key 后自动 ping,成功 toast / 失败 warning toast + status='invalid'
    - 切换到 Ollama 自动 ping,验证模型可达
    - status 四态 UI 正确渲染(已配置/已就绪、配置但不可用、未配置、读取中)
    - settingsProvider.test.ts 仍 pass
    - tsc clean
  </done>
</task>

</tasks>

<verification>
1. **TypeScript:** `cd D:/Projects/Nova/nova-pm-workspace && npm run lint` — tsc 0 错误
2. **Rust build:** `cd D:/Projects/Nova/nova-pm-workspace/src-tauri && cargo build` — 编译通过
3. **Rust tests:** `cd D:/Projects/Nova/nova-pm-workspace/src-tauri && cargo test` — 现有测试全绿(包括 provider_serializes_to_lowercase_wire_names、apperror_display_is_nonempty_for_every_variant、streamchunk_*;`ollama_real_tool_call_uat` 仍 ignored)
4. **Settings contract test:** `cd D:/Projects/Nova/nova-pm-workspace && npx tsx --test src/stores/__tests__/settingsProvider.test.ts` — 2 tests pass
5. **Net diff scope:** 7-8 文件、~150-200 行(ponytail 约束)
6. **Manual UAT**(用户执行,非本任务):
   - Ollama: 切到 Ollama,输入模型名,保存 → ping 通过 → 发 chat 消息不再 model not found
   - DeepSeek/OpenAI/Anthropic/Gemini: 保存真实 key → ping 通过 toast;保存假 key(如 "sk-invalid") → ping 失败 toast 显示 "invalid api key",status='invalid'
</verification>

<success_criteria>
- [ ] uiStore.ollamaModel 字段持久化(partialize 覆盖)
- [ ] toolLoop 通过 useUIStore.getState().ollamaModel 把模型名传给 chatWithTools
- [ ] chatWithTools 接受 ollamaModel 参数,Tauri invoke payload 含 ollamaModel
- [ ] Rust ChatArgs 含 ollama_model: Option<String>,转发给 llm::chat_with_tools
- [ ] llm::chat_with_tools Ollama 分支按 参数 > NOVA_OLLAMA_MODEL > OLLAMA_MODEL > LLAMA3_2 解析
- [ ] llm::ping_provider 实现,5s 超时,5 个 provider 各自路径(AuthError / NetworkError / InternalError 分类正确)
- [ ] commands::ping_provider 在 invoke_handler 注册
- [ ] api.ts pingProvider wrapper,web mode throw "桌面端可用"
- [ ] SettingsApiKeySection Ollama 卡片显示模型输入框 + 测试按钮
- [ ] handleSaveKey 保存后自动 ping,toast + status 三态反映结果
- [ ] handleProviderChange 切到 Ollama 自动 ping
- [ ] providerStatuses 类型升级,UI label 四态渲染正确
- [ ] npm run lint 通过
- [ ] cargo build + cargo test 通过
- [ ] settingsProvider.test.ts 2/2 通过
</success_criteria>

<output>
After completion, create `.planning/quick/260813-u7m-ollama-my-ornith-llama3-2-settings-provi/260813-u7m-SUMMARY.md`.
</output>
