---
phase: quick-260818-ci0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/data/mockRndData.ts]
autonomous: true
must_haves:
  truths:
    - "知识库默认页面显示 p1/p2/p3/p4 四个产品的文档"
    - "p2/p3/p4 种子条目内容模拟 AI 过程文档/留痕(author 如 'Nova Agent')"
  artifacts:
    - path: "src/data/mockRndData.ts"
      provides: "INITIAL_KNOWLEDGE_BASE p2/p3/p4 桶"
  key_links: []
---

<objective>
知识库默认页只显示 p1 文档。根因:`INITIAL_KNOWLEDGE_BASE` 只有 `p1` 桶,聚合视图 flat() 后无其他产品数据。补 p2/p3/p4 种子条目,内容为 AI 过程文档/留痕性质(用户已确认的产品定位)。
</objective>

<context>
@src/data/mockRndData.ts (393-485 行: p1 条目结构,ProductKnowledgeItem 字段)
@src/data/mockProducts.ts (p2/p3/p4 产品名与定位)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补充 p2/p3/p4 知识库种子条目</name>
  <files>src/data/mockRndData.ts</files>
  <action>
在 `INITIAL_KNOWLEDGE_BASE`(393-485 行)p1 桶之后追加 p2/p3/p4 三个桶,各 2-3 条,严格遵循 ProductKnowledgeItem 字段(id/productId/title/category/tags/author/updatedAt/readTime/isPinned?/summary/content)。id 规则:`kb-p2-1` 等。

内容要求(用户定位:知识库是 AI 撰写的 markdown 过程文档/留痕):
- author 用 AI/agent 角色名,如 `'Nova Agent'`、`'Nova Agent (PRD Skill)'`、`'Nova Agent (QA Skill)'`
- 条目类型模拟 AI 运行留痕:需求评审纪要、spec 草案、架构决策记录 (ADR)、agent 生成报告、测试用例生成报告等
- content 为简短 markdown(2-6 行),风格与 p1 一致
- 语境匹配产品:
  - p2 NovaAgent 移动端智能助手(语音转记/日程提醒/移动协同)— 如"语音输入快速转记链路 spec 草案"、"公测灰度期崩溃归因 agent 报告"
  - p3 DataSense 商业智能中台(NL2SQL/异常归因/看板)— 如"自然语言 SQL 查询准确率评审纪要"、"指标异常归因 ADR"
  - p4 BrandPortal 品牌官网(已发布)— 如"官网视觉资产归档提取纪要"、"客户案例页内容 agent 生成报告"
- updatedAt 取 2025-04 ~ 2025-05 之间;每桶至多 1 条 isPinned: true

不改动 p1 条目,不改动类型定义。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>INITIAL_KNOWLEDGE_BASE 含 p1/p2/p3/p4 四个非空桶,共约 10-13 条,lint 通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>知识库默认页多产品种子数据</what-built>
  <how-to-verify>
    1. `npm run dev` 打开 http://localhost:3000
    2. 切到知识库页 — 应看到 WenXiBuddy(p1)、NovaAgent(p2)、DataSense(p3)、BrandPortal(p4) 四个产品的条目,作者多为 Nova Agent
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>
