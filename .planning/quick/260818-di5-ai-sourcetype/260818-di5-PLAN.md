---
phase: quick-260818-di5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/stores/rndStore.ts, src/ai/tools/knowledgeWrite.ts, src/ai/__tests__/knowledgeWrite.test.ts]
autonomous: true
requirements: [QUICK-DI5]
must_haves:
  truths:
    - "AI 通过 writeKnowledgeArticle 工具创建/更新的知识文档在 repo 中 sourceType 为 'agent'"
    - "UI 手动入口(ProductKnowledgeTab)写入的文档仍为 'user'"
    - "全部既有测试通过"
  artifacts:
    - path: "src/stores/rndStore.ts"
      contains: "sourceType"
    - path: "src/ai/tools/knowledgeWrite.ts"
      contains: "'agent'"
  key_links:
    - from: "src/ai/tools/knowledgeWrite.ts"
      to: "useRndStore.addKnowledgeItem/updateKnowledgeItem"
      via: "新增第 4 参 options.sourceType"
      pattern: "sourceType: 'agent'"
---

<objective>
AI 工具写入的知识库文档被错标 `sourceType: 'user'`,应标 `'agent'`。给 store action 增加 sourceType 参数(默认 'user' 保持所有既有调用方兼容),knowledgeWrite.ts 传 'agent',并补测试锁定。
</objective>

<context>
@src/stores/rndStore.ts (addKnowledgeItem L329 / updateKnowledgeItem L352, 均硬编码 sourceType: 'user')
@src/ai/tools/knowledgeWrite.ts (writeConfirmedArticle L79-118 调 store action)
@src/ai/knowledgeRepo.ts (KnowledgeDocInput.sourceType?: 'seed' | 'agent' | 'user')
@src/ai/__tests__/knowledgeWrite.test.ts (node:test + assert/strict 模式)

对照正确实现:`src/ai/tools/generateDeliverable.ts` 直接调 `getKnowledgeRepo().upsertDoc` 传 `'agent'`。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: store action 加 sourceType 参数 + AI 工具传 'agent'</name>
  <files>src/stores/rndStore.ts, src/ai/tools/knowledgeWrite.ts</files>
  <behavior>
    - addKnowledgeItem(productId, item, opts?: { sourceType?: 'seed'|'agent'|'user' }) → upsertDoc 用 opts?.sourceType ?? 'user'
    - updateKnowledgeItem(productId, itemId, updates, opts?: 同上) → 同样
    - knowledgeWrite.ts writeConfirmedArticle 中两处调用均传 { sourceType: 'agent' }
    - UI 调用方(ProductKnowledgeTab 等)零改动,默认仍 'user'
  </behavior>
  <action>
    1. rndStore.ts:接口(L103-104)与实现(L329、L352)各加可选第 4 参 `opts?: { sourceType?: 'seed' | 'agent' | 'user' }`,upsertDoc 处 `sourceType: opts?.sourceType ?? 'user'`。类型直接复用 knowledgeRepo 的 KnowledgeDocInput['sourceType'] 即可,不要新建类型。
    2. knowledgeWrite.ts writeConfirmedArticle:updateKnowledgeItem 与 addKnowledgeItem 两处调用末尾加 `{ sourceType: 'agent' as const }`。
    3. 不要动 generateDeliverable.ts、polishKnowledgeArticleAI 及 UI 调用方。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>类型检查通过;UI 入口行为不变(默认 'user')</done>
</task>

<task type="auto">
  <name>Task 2: 测试断言 AI 写入 repo 的 sourceType 为 'agent'</name>
  <files>src/ai/__tests__/knowledgeWrite.test.ts</files>
  <action>
    在 "creates only after matching explicit confirmation" 测试末尾(deleteKnowledgeItem 之前)和 "updates in scope" 测试断言后各加 repo 侧断言:
    ```ts
    const { getKnowledgeRepo } = await import('../knowledgeRepo');
    const doc = (await getKnowledgeRepo().getCurrentDocs(productId)).find((d) => d.docId === result.articleId);
    assert.equal(doc?.sourceType, 'agent');
    ```
    复用既有 confirm-then-execute 流程,不新增测试文件。清理沿用既有 deleteKnowledgeItem。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/knowledgeWrite.test.ts && npm test</automated>
  </verify>
  <done>knowledgeWrite 测试全绿,含两条新 sourceType='agent' 断言(create + update 路径);全量 npm test 无回归</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 通过
- `npm test` 全绿(含新断言)
</verification>

<success_criteria>
AI 经 writeKnowledgeArticle 写入的文档在 knowledge repo 中 sourceType === 'agent'(create 与 update 两条路径均有测试锁定);UI 手动写入仍为 'user';零调用方破坏。
</success_criteria>
