---
phase: quick-260818-f3b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/sessionSummaryProjection.ts
  - src/ai/__tests__/sessionSummaryProjection.test.ts
  - src/ai/toolLoop.ts
autonomous: true
must_haves:
  truths:
    - "每次 agent turn 正常完成后,该会话的紧凑纪要以稳定 docId 存在于知识库 repo"
    - "同一 session 二次投影产生 v2(supersede 旧版),不产生重复文档"
    - "无 productId 时(session 与 uiStore 皆空)跳过投影且不影响主循环"
    - "投影失败绝不影响 toolLoop 主流程(fire-and-forget,错误吞掉 console.warn)"
    - "知识库视图自动显示会话纪要(投影桶更新,zustand 自然刷新)"
  artifacts:
    - path: "src/ai/sessionSummaryProjection.ts"
      provides: "纪要派生 + upsertDoc + rndStore 投影桶增量更新"
    - path: "src/ai/__tests__/sessionSummaryProjection.test.ts"
      provides: "Node 测试锁定 docId 稳定 / supersede / 跳过逻辑"
  key_links:
    - from: "src/ai/toolLoop.ts"
      to: "src/ai/sessionSummaryProjection.ts"
      via: "endTurn('completed') 后 fire-and-forget 调用"
      pattern: "projectSessionSummary"
    - from: "src/ai/sessionSummaryProjection.ts"
      to: "src/ai/knowledgeRepo.ts"
      via: "upsertDoc(docId: session-summary-<sessionId>)"
      pattern: "getKnowledgeRepo\\(\\)\\.upsertDoc"
    - from: "src/ai/sessionSummaryProjection.ts"
      to: "src/stores/rndStore.ts"
      via: "useRndStore.getState() 投影桶替换/追加"
      pattern: "useRndStore"
---

<objective>
Agent 会话纪要自动投影进知识库。每次 agent turn 结束(completed)后,把该会话的紧凑纪要以稳定 docId `session-summary-<sessionId>` upsert 为知识库文档(sourceType 'agent'),同 session 新纪要 supersede 旧版;增量更新 rndStore.knowledgeBase 投影桶,知识库视图自动显示。

用户决策(锁定):投影形态 = 会话纪要(turn 级滚动更新,supersede)。不做 PRD 草稿候选投影,不做消息手动入库。不改 UI。
</objective>

<context>
@./CLAUDE.md

关键接口(executor 直接使用,无需探索):

```typescript
// src/ai/knowledgeRepo.ts
interface KnowledgeDocInput {
  docId: string; productId: string; title: string; category: string;
  tags: string[]; summary: string; content: string; author: string;
  sourceType?: 'seed' | 'agent' | 'user' | 'archive_import';
  sourceSessionId?: string; sourceEventId?: string; updatedAt?: string;
}
function getKnowledgeRepo(): KnowledgeRepo  // .upsertDoc(input): Promise<KnowledgeDoc>
function getMemoryKnowledgeRepo(): MemoryKnowledgeRepo  // 测试用
function resetMemoryKnowledgeRepo(): void
// 同 docId 重复 upsertDoc = 新版本 supersede(deliverable-<productId>-<slotCode> 即此模式,见 src/ai/tools/generateDeliverable.ts:57-69)

// src/ai/chatSession.ts
class ChatSession {
  readonly sessionId: string;
  getAllMessages(): ChatMessage[];  // { role: 'user'|'assistant'|'tool', content, toolCallId?, toolName?, timestamp }
}
// ChatSession 可直接 new ChatSession('test-session-id') 构造(Node 测试模式)

// src/stores/rndStore.ts — knowledgeBase: Record<string, ProductKnowledgeItem[]>
// docToItem(doc) 私有;投影条目形状: { id: docId, productId, title, category, tags, author, updatedAt, readTime: '—', summary, content }
// category 必须是 ProductKnowledgeItem['category'] 联合的合法值 — 用 '经验沉淀'(mock 数据已多次使用)

// src/ai/toolLoop.ts — endTurn 定义于 :139-143;正常完成路径 :174-177(result.toolCalls.length === 0 → endTurn('completed', iteration))
// 测试模式参考 src/ai/__tests__/phase16GenerateDeliverable.test.ts(resetMemoryKnowledgeRepo + useUIStore.setState)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sessionSummaryProjection 模块 + 测试</name>
  <files>src/ai/sessionSummaryProjection.ts, src/ai/__tests__/sessionSummaryProjection.test.ts</files>
  <behavior>
    - Test 1: 投影后 repo 中存在 docId `session-summary-<sessionId>` 的文档,sourceType 'agent',category '经验沉淀',sourceSessionId 等于 sessionId
    - Test 2: 同 session 二次投影 → listVersions 长度 2,v2 是 current(supersededAt null),v1 supersededAt 非 null
    - Test 3: useUIStore.selectedProductId 为 null 时(projectSessionSummary 显式传 productId 参数可覆盖)→ 不写 doc,不抛错
    - Test 4: 投影后 rndStore.knowledgeBase[productId] 中存在 id 为该 docId 的条目;二次投影后条目被替换而非追加(桶内同 id 恰一条)
    - Test 5: content markdown 包含工具调用名清单(从 tool 角色消息 toolName 去重统计)
  </behavior>
  <action>
    导出:
    - `deriveSummaryDoc(session: ChatSession, productId: string): KnowledgeDocInput` — 纯函数。title: `${产品名} 会话纪要: ${首条 user 消息截断 20 字}`(产品名从 useProductStore.getState().products 查 productId,查不到用 productId);summary: 最后一条 assistant 消息截断 100 字;content markdown: 会话时间(首条消息 timestamp)、用户轮次数(user 消息数)、工具调用统计+去重清单(tool 消息按 toolName 计数)、最后一条 assistant 回复截断 500 字;tags: ['会话纪要'];author: 'Nova Agent';docId `session-summary-${session.sessionId}`;sourceType 'agent';sourceSessionId session.sessionId。数据全部从 session.getAllMessages() 派生,不读事件表。
    - `projectSessionSummary(session: ChatSession): Promise<void>` — 取 productId:session 无产品绑定概念,直接用 `useUIStore.getState().selectedProductId`;为空则 return(跳过)。然后 `getKnowledgeRepo().upsertDoc(deriveSummaryDoc(...))`,再增量更新投影桶:`useRndStore.setState` 将 docToItem 形状条目(复刻 rndStore.ts:160-173 的映射,或从 rndStore 导出 docToItem — 优先导出复用,一行 export 改动)按 id 替换或前插到 `knowledgeBase[productId]`(同 id 替换保持原位置语义可简化为:过滤掉同 id 后 unshift 到最前)。
    - 模块 React-free(仅 getState 调用),Node 测试可直接加载(遵循 store 保持 React-free 惯例)。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/sessionSummaryProjection.test.ts</automated>
  </verify>
  <done>5 项测试全绿;docId 稳定、supersede、空产品跳过、投影桶增量更新均有断言覆盖</done>
</task>

<task type="auto">
  <name>Task 2: toolLoop endTurn 后 fire-and-forget 接线</name>
  <files>src/ai/toolLoop.ts</files>
  <action>
    在 endTurn 函数体内(:139-143),当 outcome === 'completed' 时追加 fire-and-forget 投影:动态 import(遵循同文件 searchKnowledgeLazy 的 lazy 模式,避免新增顶层 import)后调用 `projectSessionSummary(session)`,不 await;catch 吞掉并 console.warn('[session-summary] projection failed', error)。仅 'completed' 路径 — awaiting_confirmation / tool_limit / awaiting_destructive_confirmation 不投影(用户决策:turn 级滚动更新,正常完成点)。绝不影响返回路径。
  </action>
  <verify>
    <automated>npx tsx --test src/ai/__tests__/phase13ToolLoopEvents.test.ts src/ai/__tests__/sessionSummaryProjection.test.ts && npm run lint</automated>
  </verify>
  <done>toolLoop 既有测试零回归;lint 通过;completed turn 后投影自动触发且失败静默</done>
</task>

</tasks>

<verification>
- npx tsx --test src/ai/__tests__/sessionSummaryProjection.test.ts — 新测试全绿
- 既有 phase13/15/16 toolLoop 相关测试零回归
- npm run lint 通过
</verification>

<success_criteria>
- 同一 session 多次 turn 完成后,知识库中该会话纪要始终恰好一篇当前版本(docId `session-summary-<sessionId>`),版本链可审计
- 知识库视图(不改 UI)自动显示会话纪要,category '经验沉淀'
- 投影完全不影响 toolLoop 主循环时序与返回值
</success_criteria>

<output>
完成后创建 `.planning/quick/260818-f3b-session-summary-projection/260818-f3b-SUMMARY.md`
</output>
