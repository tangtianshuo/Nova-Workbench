---
phase: quick-260818-t54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/stores/workspaceStore.ts, src/views/AgentWorkspaceView.tsx]
autonomous: true
requirements: [T54-01]
must_haves:
  truths:
    - "Agent 工作区对话框顶栏「当前工作区」按钮显示真实 workspaceStore 中的工作区名称"
    - "顶栏 model 按钮显示当前 activeAIProvider(及 ollama 时的 ollamaModel)"
  artifacts:
    - path: "src/stores/workspaceStore.ts"
      provides: "activeWorkspaceId 状态"
    - path: "src/views/AgentWorkspaceView.tsx"
      provides: "顶栏真实数据绑定"
  key_links:
    - from: "src/views/AgentWorkspaceView.tsx"
      to: "useWorkspaceStore.activeWorkspaceId"
      via: "selector 读取"
      pattern: "useWorkspaceStore\\(\\(s\\) => s\\.activeWorkspaceId"
    - from: "src/views/AgentWorkspaceView.tsx"
      to: "useUIStore.activeAIProvider"
      via: "selector 读取"
      pattern: "useUIStore\\(\\(s\\) => s\\.activeAIProvider"
---

<objective>
Agent 工作区顶栏「当前工作区」与 model 按钮从硬编码改为真实数据:工作区名来自 workspaceStore,LLM 名称来自 uiStore.activeAIProvider / ollamaModel。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: workspaceStore 增加 activeWorkspaceId 并接入顶栏显示</name>
  <files>src/stores/workspaceStore.ts, src/views/AgentWorkspaceView.tsx</files>
  <action>
1. workspaceStore.ts: WorkspaceState 增加 `activeWorkspaceId: string | null` 与 `setActiveWorkspaceId: (id: string) => void`(set 直接写入)。初始值 `INITIAL_WORKSPACES[0]?.id ?? null`。partialize 中加入 `activeWorkspaceId` 以持久化。不 bump version(migrate 是 passthrough)。
2. AgentWorkspaceView.tsx: 读取 `activeWorkspaceId` + `workspaces`,派生 `activeWorkspace = workspaces.find(w => w.id === activeWorkspaceId) ?? workspaces[0]`。顶栏「当前工作区」按钮文本显示 `activeWorkspace?.name ?? '当前工作区'`,并加 `max-w-[160px] truncate` 防溢出。CaretDown 保留(切换 UI 留桩,onClick 留空)。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>顶栏按钮显示 ws-1 的名称「WenXiBuddy 核心研发工作区」(或用户持久化的 active workspace),无工作区时回退「当前工作区」</done>
</task>

<task type="auto">
  <name>Task 2: model 按钮显示 activeAIProvider / ollamaModel</name>
  <files>src/views/AgentWorkspaceView.tsx</files>
  <action>
同文件内:从 useUIStore 读取 `activeAIProvider` 与 `ollamaModel`。模块级定义 label 映射:
`const PROVIDER_LABELS: Record<Provider, string> = { deepseek: 'DeepSeek Chat', openai: 'OpenAI GPT', anthropic: 'Claude', gemini: 'Gemini', ollama: 'Ollama' }`(Provider type 从 `@/src/lib/api` 导入)。显示文本:provider === 'ollama' ? `Ollama · ${ollamaModel}` : PROVIDER_LABELS[provider]。替换硬编码「DeepSeek Chat」。CaretDown 保留(切换 UI 留桩)。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>默认显示「DeepSeek Chat」(activeAIProvider 默认 deepseek);切到 ollama 时显示「Ollama · llama3.2」</done>
</task>

</tasks>

<verification>npm run lint 通过;tauri:dev 打开 Agent 工作区,顶栏两个按钮显示真实 store 数据。</verification>
<output>完成后创建 .planning/quick/260818-t54-agent-llm-model-provider/260818-t54-SUMMARY.md</output>
