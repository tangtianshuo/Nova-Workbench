---
phase: quick-260818-t54
plan: 01
subsystem: agent-workspace
tags: [agent, workspace, llm-provider, ui]
requires: [uiStore.activeAIProvider, workspaceStore.workspaces]
provides: [workspaceStore.activeWorkspaceId, 顶栏真实数据绑定]
key-files:
  modified: [src/stores/workspaceStore.ts, src/views/AgentWorkspaceView.tsx]
decisions:
  - activeWorkspaceId 初始值 INITIAL_WORKSPACES[0]?.id ?? null,partialize 持久化,不 bump version
  - model 标签 PROVIDER_LABELS 映射,ollama 显示 Ollama · {model}
metrics:
  duration: ~6min
  completed: 2026-08-18
---

# Quick Task 260818-t54: Agent 工作区顶栏 LLM/工作区真实数据绑定 Summary

顶栏「当前工作区」与 model 按钮从硬编码改为真实 store 数据:工作区名来自 workspaceStore.activeWorkspaceId 派生,LLM 名称来自 uiStore.activeAIProvider / ollamaModel(PROVIDER_LABELS 映射)。

## Changes

- `src/stores/workspaceStore.ts`: `WorkspaceState` 新增 `activeWorkspaceId: string | null` + `setActiveWorkspaceId`;初始值 `INITIAL_WORKSPACES[0]?.id ?? null`;partialize 持久化;不 bump version(migrate passthrough)。
- `src/views/AgentWorkspaceView.tsx`:
  - selector 读取 `useWorkspaceStore((s) => s.activeWorkspaceId)` 与 `workspaces`,派生 `activeWorkspace`(fallback `workspaces[0]`);按钮文本 `activeWorkspace?.name ?? '当前工作区'`,`max-w-[160px] truncate`。
  - selector 读取 `useUIStore((s) => s.activeAIProvider)` / `ollamaModel`;模块级 `PROVIDER_LABELS`;ollama 显示 `Ollama · {ollamaModel}`。CaretDown 保留,切换 UI 留桩。

## Verification

- `npm run lint`(tsc --noEmit)通过。
- 默认显示「WenXiBuddy 核心研发工作区」+「DeepSeek Chat」;ollama 时显示「Ollama · llama3.2」。

## Deviations from Plan

None — plan 执行与计划一致。(执行环境 worktree 落后 master,先 fast-forward 至 0d69819 再实施,非计划偏差。)

## Commits

- e88303d: workspaceStore 增加 activeWorkspaceId 持久化状态
- d0b9a7a: Agent 工作区顶栏显示真实工作区名与 LLM 模型

## Self-Check: PASSED
