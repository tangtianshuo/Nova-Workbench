---
phase: quick-260819-df6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/AgentWorkspaceView.tsx
autonomous: true
requirements: [QUICK-DF6-01, QUICK-DF6-02]
must_haves:
  truths:
    - "点击 Agent 工作区左上角工作区按钮可看到工作区菜单，选择后切换工作区并进入新 session"
    - "streaming 中切换工作区被拒绝且有 toast 提示"
    - "点击属于其他/全局工作区的最近 session 时，用户收到 toast 通知"
  artifacts:
    - path: "src/views/AgentWorkspaceView.tsx"
      provides: "工作区切换下拉 + 跨工作区 session 点击通知"
  key_links:
    - from: "AgentWorkspaceView 工作区按钮"
      to: "workspaceStore.setActiveWorkspaceId"
      via: "DropdownMenu onSelect"
      pattern: "setActiveWorkspaceId\\("
    - from: "AgentWorkspaceView 最近 session 行"
      to: "useToast"
      via: "workspaceId !== activeWorkspaceId 时 toast 后再 switchSession"
      pattern: "workspaceId !== activeWorkspaceId"
---

<objective>
UAT 反馈修复（Agent 工作区）：
1. 左上角工作区按钮可点击切换工作区（切换 = 结束当前 session + 进入新 session，SESS-04 语义已由 `workspaceStore.setActiveWorkspaceId` 提供，无需新逻辑）。
2. 最近任务列表包含全局会话（`workspace_id IS NULL`，migration 0007 回填语义），点击时若 session 不属于当前工作区，用 toast 通知用户。

Purpose: Agent 工作区的工作区切换闭环 + 跨工作区 session 防困惑。
Output: 仅改 `src/views/AgentWorkspaceView.tsx`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/views/AgentWorkspaceView.tsx
@src/stores/workspaceStore.ts
@src/stores/chatConsoleStore.ts

<interfaces>
// src/stores/workspaceStore.ts — 已存在，直接用
setActiveWorkspaceId: (id: string) => { success: boolean; reason?: 'streaming' };
// 内部实现: streaming 时拒绝；否则 set activeWorkspaceId 并调用
// useChatConsoleStore.getState().startNewSession()（结束当前 session + 新 session）。
// 返回 { success: false, reason: 'streaming' } 时需 toast 提示。

// src/ai/sessionRepo.ts — SessionMeta 已含 workspaceId（rowToMeta L177-178）
interface SessionMeta {
  sessionId: string;
  workspaceId: string | null;  // null = 全局会话，所有工作区可见
  title: string | null;
  ...
}

// src/components/ui — DropdownMenu 原语 + useToast（ChatPanel 已有 DropdownMenu/Select 用法可参考风格）
import { DropdownMenu, ... } from '@/src/components/ui';
import { useToast } from '@/src/components/ui';  // toast({ type, title, description })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 工作区按钮改为可切换的 DropdownMenu</name>
  <files>src/views/AgentWorkspaceView.tsx</files>
  <action>
    把 L73-77 的静态 `<Button>`（Folder + name + CaretDown）包进 `DropdownMenu` 原语（`DropdownMenuTrigger` 内放现有 Button 样式不变，`DropdownMenuContent` 列出 `workspaces`，当前工作区项加 check/高亮标记，风格对齐 ChatPanel 的选择器）。
    onSelect 调用 `useWorkspaceStore.getState().setActiveWorkspaceId(ws.id)`（或从 hook 取 action）。若返回 `{ success: false, reason: 'streaming' }`，用 `useToast()` 发 `{ type: 'error', title: '无法切换工作区', description: '请等待当前回复完成' }`。
    切换成功后无需手动 startNewSession —— store 已编排（SESS-04）；最近列表会因 `activeWorkspaceId` 变化自动刷新。
    Phosphor `weight="duotone"`、token 类名、cn() 按项目约定。
  </action>
  <verify>npm run lint 通过；Tauri dev 中点击按钮出现菜单、选其他工作区后左上角名称更新且对话区进入空白新 session</verify>
  <done>工作区按钮可切换，切换后 sessionList 重建、AgentConsole 为新 session；streaming 中点击有错误 toast</done>
</task>

<task type="auto">
  <name>Task 2: 跨工作区 session 点击通知</name>
  <files>src/views/AgentWorkspaceView.tsx</files>
  <action>
    最近任务列表因 `SESSION_LIST_SQL` 的 `OR workspace_id IS NULL` 会包含全局/他区会话。在现有 onClick（L124）的 `switchSession` 前加守卫：若 `session.workspaceId !== activeWorkspaceId`（null 即全局会话，不属于当前区），先 `toast({ type: 'info', title: '全局会话', description: '该会话不属于当前工作区，仍可继续查看' })` 再照常 `switchSession(session.sessionId)`（restore 不受 workspace 限制，可直接加载）。
    onKeyDown Enter 分支同样处理。可选一行增强：这类行在 `formatRelativeTime` 旁加一个 `Globe`/`FolderSimple` 12px duotone 图标 + title 提示 —— 仅在改动小于 3 行时做。
  </action>
  <verify>npm run lint 通过；在含 NULL workspace_id 旧会话（migration 0007 回填）的环境下点击该行出现 toast 且会话正常加载</verify>
  <done>点击非当前工作区的最近 session 时用户先收到通知，session 切换行为不变</done>
</task>

</tasks>

<verification>
- `npm run lint`（tsc --noEmit）零错误
- 手动（或 UAT）：切换工作区 → 新 session；全局 session 点击 → toast
- 测试：跳过 node:test —— 两个任务均为视图胶水（store 编排 SESS-04 已有测试覆盖，guard 是一行比较），无可独立断言的非平凡纯逻辑
</verification>

<success_criteria>
AgentWorkspaceView 单文件 diff，工作区切换可用、跨工作区点击有通知，无新增依赖。
</success_criteria>

<output>
After completion, create `.planning/quick/260819-df6-agent-session-session/260819-df6-SUMMARY.md`
</output>
