---
phase: quick-260903-e4l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/rnd/CompetitorAnalysisTab.tsx
  - src/components/rnd/CodeManagementTab.tsx
  - src/components/rnd/TestManagementTab.tsx
autonomous: true
must_haves:
  truths:
    - "竞品 tab 显示 DEL-REL-02 产物(工作流跑完后无需刷新手动操作即可见)"
    - "测试 tab 显示 DEL-TST-03 产物"
    - "代码 tab 显示其 catalog 槽位产物(若有)"
    - "原有结构化内容(内存快照表格)不受影响"
  artifacts:
    - path: "src/components/rnd/CompetitorAnalysisTab.tsx"
      contains: "DEL-REL-02"
    - path: "src/components/rnd/TestManagementTab.tsx"
      contains: "DEL-TST-03"
  key_links:
    - from: "tabs"
      to: "useRndStore deliverables 投影"
      via: "slot lookup by code (AIRequirementsTab prdSlot 模式)"
      pattern: "useRndStore\\(\\(s\\).*deliverables"
---

<objective>
统一投影层:竞品/代码/测试三个 tab 接上 knowledge_docs → rndStore.deliverables 投影,引擎产物断流修复。

Purpose: 「竞品深度分析」等工作流产物落槽后只在交付物 tab + 知识库可见,业务 tab 自身看不到。
Output: 三个 tab 各新增「AI 产物」文档卡挂载区(增量,不动既有结构化表格)。
</objective>

<context>
@.planning/STATE.md
@src/components/product/AIRequirementsTab.tsx   <!-- prdSlot 先例,第 68 行 -->
@src/stores/rndStore.ts                         <!-- deliverables 投影,hydrateDeliverableSlots 已自动刷新 -->
@src/data/deliverables-catalog.json             <!-- code 单源 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: 竞品 tab AI 产物投影区</name>
  <files>src/components/rnd/CompetitorAnalysisTab.tsx</files>
  <action>
    复制 AIRequirementsTab.tsx:68 的 prdSlot 模式:`const slot = useRndStore((s) => (s.deliverables[productId] ?? []).find((d) => d.code === 'DEL-REL-02'))`(productId 取自组件现有 props/store)。
    在 tab 内容顶部(mock 竞品表格之前)挂一个增量「AI 产物」卡片:slot 存在时显示标题 + 摘要/正文 markdown 渲染(react-markdown,与 AIRequirementsTab 同形态:文档卡,不塞结构化表格);slot 不存在时不渲染该区(或留一句轻提示「运行竞品深度分析工作流后产物将展示在此」)。
    设计令牌语义类(bg-bg-primary / border-border-subtle)、Phosphor duotone 图标、Card from '@/src/components/ui'。投影数据由 hydrateDeliverableSlots(boot + tab run 确认后)自动刷新,不新增触发链。
  </action>
  <verify>npm run lint 通过;grep DEL-REL-02 命中该文件</verify>
  <done>工作流跑完竞品模板确认落槽后,竞品 tab 顶部出现产物文档卡</done>
</task>

<task type="auto">
  <name>Task 2: 测试 + 代码 tab AI 产物投影区</name>
  <files>src/components/rnd/TestManagementTab.tsx, src/components/rnd/CodeManagementTab.tsx</files>
  <action>
    同 Task 1 模式。测试 tab 挂 DEL-TST-03(质量验收)。代码 tab 查 deliverables-catalog.json 确认其对应槽位 code(若无明确对应则挂 DEL-DES-01 之外的代码类槽;若 catalog 无代码类槽,代码 tab 不挂投影区,只在 SUMMARY 注明原因——不硬造映射)。
    空槽提示语与各自 tab 语境一致(如测试 tab:「运行质量验收工作流后产物将展示在此」)。
  </action>
  <verify>npm run lint && npm test 全绿</verify>
  <done>测试 tab 显示 DEL-TST-03 产物;代码 tab 按查明结果挂载或注明无槽</done>
</task>

</tasks>

<verification>
npm run lint 通过;npm test 全绿;三个 tab 原有结构化内容渲染不受影响(投影区为纯增量)。
</verification>

<success_criteria>
工作流产物落槽后,对应业务 tab 无需任何手动刷新即可看到 AI 产物文档卡;无 slot 时 tab 保持原样。
</success_criteria>
