---
phase: quick-260903-gh0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/rndStore.ts
  - src/components/rnd/DeliverableDocCard.tsx
  - src/components/product/CompetitorAnalysisTab.tsx
  - src/components/product/CodeManagementTab.tsx
  - src/stores/__tests__/knowledgeProjection.test.ts
autonomous: true
must_haves:
  truths:
    - "agent 走 knowledge_write(category=竞品分析)确认后,竞品 tab 出现产物卡(取该 category 最新一份)"
    - "agent 走 knowledge_write(category=架构设计)确认后,代码 tab 出现产物卡"
    - "槽位流(generate_deliverable)产物仍正常渲染,且优先于知识归档流"
    - "两流均无内容时 tab 不多渲染任何东西;测试 tab(DEL-TST-03)不接知识流"
  artifacts:
    - path: "src/stores/rndStore.ts"
      provides: "getLatestKnowledgeByCategory 纯选择器"
    - path: "src/components/rnd/DeliverableDocCard.tsx"
      provides: "双源卡:slot 优先,knowledge category 兜底"
    - path: "src/stores/__tests__/knowledgeProjection.test.ts"
      provides: "选择器行为测试"
  key_links:
    - from: "src/components/rnd/DeliverableDocCard.tsx"
      to: "src/stores/rndStore.ts"
      via: "useRndStore 选择器(slot + knowledgeBase)"
      pattern: "getLatestKnowledgeByCategory|knowledgeCategory"
---

<objective>
产物卡双源投影:竞品/代码 tab 的 DeliverableDocCard 在槽位流(DEL-REL-02 / DEL-DEV-01)无内容时,兜底读 knowledgeBase 中对应 category 的最新归档(knowledge_write 流),让 agent 未按剧本调 generate_deliverable 时产物仍可见。

Purpose: 修复诊断确认的展示层缺口(kb-p1-1788401901910 竞品分析落库但竞品 tab 不可见)。
Output: 双源 DeliverableDocCard + 选择器 + 测试。
</objective>

<context>
@.planning/STATE.md
@src/components/rnd/DeliverableDocCard.tsx
@src/ai/tools/knowledgeWrite.ts

已核实的代码事实(无需重复探索):
- `writeKnowledgeArticle` 确认链已即时更新投影:`writeConfirmedArticle` → `store.addKnowledgeItem/updateKnowledgeItem` → rndStore `set()` 同步 push knowledgeBase — 无需补刷新链
- category 枚举见 `src/ai/tools/knowledgeWrite.ts:13`(含 竞品分析、架构设计),与 Rust `tools.rs` 互锁
- `ProductKnowledgeItem` 有 `id/title/category/content/updatedAt` 字段(rndStore)
- 现有挂点:CompetitorAnalysisTab.tsx:87 `code="DEL-REL-02"`、CodeManagementTab.tsx:166 `code="DEL-DEV-01"`;TestManagementTab 不动(约束:DEL-TST-03 无对应 category,不做双源)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 最新归档选择器(TDD)</name>
  <files>src/stores/__tests__/knowledgeProjection.test.ts, src/stores/rndStore.ts</files>
  <behavior>
    - Test 1: getLatestKnowledgeByCategory(items, '竞品分析') 多条时返回 updatedAt 最新的一条
    - Test 2: 无匹配 category 返回 undefined
    - Test 3: updatedAt 相同(或缺失)时取数组首位(稳定兜底,不抛错)
  </behavior>
  <action>
    先写测试(RED),再在 rndStore.ts 导出纯函数:

    ```ts
    export function getLatestKnowledgeByCategory(
      items: ProductKnowledgeItem[],
      category: ProductKnowledgeItem['category'],
    ): ProductKnowledgeItem | undefined
    ```

    实现:filter by category → sort desc by updatedAt(缺失视为 '' 排最后)→ 取 [0]。纯函数、不触 store。npm test 走 tsx --test,测试文件须落在 `src/stores/__tests__/` glob 内。
  </action>
  <verify>
    <automated>npm test -- src/stores/__tests__/knowledgeProjection.test.ts</automated>
  </verify>
  <done>选择器导出且 3 个用例绿</done>
</task>

<task type="auto">
  <name>Task 2: DeliverableDocCard 双源兜底 + 两个 tab 接线</name>
  <files>src/components/rnd/DeliverableDocCard.tsx, src/components/product/CompetitorAnalysisTab.tsx, src/components/product/CodeManagementTab.tsx</files>
  <action>
    1. DeliverableDocCard 增加可选 prop `knowledgeCategory?: ProductKnowledgeItem['category']`。渲染逻辑:slot 有 content → 渲染 slot(现状,优先);slot 空且传了 knowledgeCategory → `useRndStore((s) => getLatestKnowledgeByCategory(s.knowledgeBase[productId] ?? [], knowledgeCategory))`,有则用同一张卡渲染(badge 显示 category 而非 slot code,标题取 item.title,内容 MarkdownRenderer 渲染 item.content);均无 → return null。组件注释更新为双源说明。不新增组件文件 —— 一个卡、两流、优先级一条规则(ponytail:卡语义=最新产物,ProductKnowledgeTab 全量列表不受影响)。
    2. CompetitorAnalysisTab 挂点加 `knowledgeCategory="竞品分析"`;CodeManagementTab 挂点加 `knowledgeCategory="架构设计"`。TestManagementTab 不动(约束)。
    3. 样式沿用现有卡结构与语义 token,无新样式。
  </action>
  <verify>
    <automated>npm run lint && npm test</automated>
  </verify>
  <done>lint + 全量测试绿;双源卡逻辑为 slot 优先、knowledge category 兜底、双空不渲染</done>
</task>

</tasks>

<verification>
npm run lint && npm test 全绿;竞品/代码 tab 各多一个 knowledgeCategory prop;TestManagementTab 无 diff。
</verification>

<success_criteria>
- knowledge_write(category=竞品分析)确认落库后,竞品 tab 出现该产物最新一份的卡
- 槽位流产物渲染不受影响且优先
- 不改模板 prompt、不改工具描述、不动 Rust 侧
</success_criteria>

<output>
完成后创建 `.planning/quick/260903-gh0-tab-knowledge-write/260903-gh0-SUMMARY.md`
</output>
