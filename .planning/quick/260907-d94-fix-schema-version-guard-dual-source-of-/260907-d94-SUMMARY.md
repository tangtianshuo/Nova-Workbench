---
phase: quick-260907-d94
plan: 01
subsystem: storage/persistence
tags: [schema-version, migration, tauri-command, single-source-of-truth]
key-files:
  modified: [src-tauri/src/lib.rs, src/stores/storage/initializeDatabase.ts]
decisions:
  - "Rust migration registry is the sole source of truth for schema_version; TS guard fetches it via invoke('max_schema_version')"
metrics:
  duration: "~10 min"
  completed: 2026-09-07
---

# Quick Task 260907-d94: Fix schema version guard dual source of truth

Schema_version guard now derives its expected value from the Rust migration registry via a new `max_schema_version` Tauri command (`sql_migrations().iter().map(version).max()`); the hardcoded `APP_SCHEMA_VERSION = 14` TS constant is deleted. Next migration (17) needs zero TS changes.

## Root Cause

User's production DB was at version 16 (migrations 15/16 landed in phase 32), but the TS guard hardcoded `APP_SCHEMA_VERSION = 14` and refused startup. Two sources of truth for one fact — every migration bump would recur the failure.

## Tasks Completed

| Task | Commit | Description |
| ---- | ------ | ----------- |
| 1 | 16e251c | `max_schema_version` command derived from registry (no literal), registered in invoke_handler, test asserts max=16 |
| 2 | 35c8dff | TS guard uses `invoke<number>('max_schema_version')`; `APP_SCHEMA_VERSION` deleted |

## Verification

- `cd src-tauri && cargo test migration_registry_tests` — 2 passed (incl. new `max_schema_version_matches_registry_max`)
- `npm run lint` (tsc --noEmit) — clean
- `grep -rn APP_SCHEMA_VERSION src/` — no matches

## Deviations from Plan

- **Worktree sync [Rule 3 - blocking]:** Worktree was at v0.3.3 (b43dded, max migration 14) while the plan assumes master's phase-32 state (migrations 15/16). Fast-forwarded worktree branch to master (`git merge --ff-only master`) before executing; the user's production failure is against this newer state.
- TDD ordering collapsed into one commit per task (test + implementation together); RED/GREEN split adds no value for a one-line derivation.

## Self-Check: PASSED

- Commits 16e251c, 35c8dff present on worktree branch
- Both modified files contain `max_schema_version` per plan artifacts spec
