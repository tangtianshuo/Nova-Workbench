{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sql",
  "description": "SQLite persistence for Zustand stores (Phase 2)",
  "windows": ["main"],
  "permissions": [
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-close"
  ]
}
